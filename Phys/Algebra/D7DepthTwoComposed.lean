/-
  # D7 DEPTH-2 RUNNING COMPOSED — compose the FOLD-FORCED all-orders depth-2 running (the
  #   census-in-census) into the census-conserving endpoint `recomposedEndpoint`, then RESTATE the
  #   composed endpoint AS `1/α(0)`
  #   (arc-D THE DIGITS, docs/SEED_DIGITS.md §D2c/§D7; owner gate-enforcement directives 2026-07-18
  #   19:20 + 19:23; directed successor of N571 D7AugmentedAttestation bf29483, run 890, t_3a070796)
  =================================================================================================

  OWNER-DIRECTED (t_3a070796, run 890; grove-manager owner-authority comments 19:20 + 19:23). The
  inherited ticket framing ("narrow the 136.307 endpoint toward the ≥15-sig-digit floor") is SUPERSEDED:
  the owner ruled the D7AugmentedAttestation ledger is INCOMPLETE by the board's own record — the banked
  DEPTH-2 RUNNING content (the census-in-census, `kappaLeadingDressed w = κ₀/(1−w)`) is neither composed
  into `recomposedEndpoint` nor proven zero, so the D7 gate clause ("every banked contribution INCLUDED
  or proven zero") fails on it. VERIFIED INDEPENDENTLY: `recomposedEndpoint = 2425/18 +
  kappaLeading·C_inner` carries the BARE (leading, first-depth) loop factor `kappaLeading = (1/cutPi)/3`;
  the depth-2 running is genuinely absent. This node COMPOSES it (owner step 1), at the value the kernel
  forces — no fit, no new axiom.

  ## THE FOLD-FORCED COMPOSITION (theory-native, every piece banked — NOT a choice)

  The fold's self-look-back RECURS at loop level (X5): when the gather resolves the one-loop self-overlap,
  that resolving act is itself a gather (census-in-census). Formally the SECOND application dresses the
  loop normalization: `kappaLeading → kappaLeadingDressed w = kappaLeading/(1−w)` (banked X5
  `kappaLeadingDressed_eq_div`, the exact all-orders geometric closed form `κ₀ + w·κ₀ + w²·κ₀ + … =
  κ₀/(1−w)` — NOT a hand truncation). The fold FORCES the single weight `w = depthWeight = 1/3` (banked
  D3 `depthWeight_eq` / `LowTailFoldForcedWeight.foldForcedWeight_value` — the mean Born self-overlap over
  the ONE return surface, band-INDEPENDENT; band-varying weights are the forbidden FIT the fold KILLS,
  proven). So the all-orders dressing factor is `1/(1−1/3) = 3/2`, forced.

  The depth-2 running dresses the LOW-TAIL loop-running content of `recomposedEndpoint` — exactly the
  `kappaLeading·C_inner` term (the two below-Λ screening terms: `leptonTailScreen =
  kappaLeading·leptonCensus·cutLog(mass0/mass1)` and the freeze-out staircase running part
  `kappaLeading·[(7/3)L_mb + (11/3)L_b0 − (5/3)L_b2]`). The NON-running base `2425/18 = 7179/54
  (confinement way-point) + 16/9 (census constant)` carries NO loop factor and is PRESERVED (the
  way-point-preservation of `DepthTwoBandVaryingRunning`: second-depth weight `0` on the high way-point
  bands, since their census-in-census is already inside the all-orders transmutation scales that define
  the band edges — re-dressing there would double-count).

  ## THE KERNEL-FORCED LANDING (disagreement-narrowing-to-agreement, owner-LICENSED)

  Composing the fold-forced all-orders depth-2 running moves the endpoint from the bare `≈ 136.307` to
      recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·C_inner  ≈ 137.0997
  — now ABOVE `137`, ≈ 0.064 from the measured `1/α(0) ≈ 137.036` (REMOVABLE ORIENTATION PROSE ONLY, in
  no proof). The bare `136.307` was the incomplete sum the owner identified; the composed value is the
  kernel-forced completion. NO FIT: the factor `3/2` is `1/(1 − depthWeight)` with `depthWeight = 1/3`
  fold-derived; no weight is pinned to any measured value.

  ## WHAT THIS NODE BANKS (all THEOREM-EXACT / CERTIFIED, foundations-only, no error bar)

    (A) `recomposedEndpointDressed` — the census-conserving endpoint with the fold-forced all-orders
        depth-2 running composed: `2425/18 + kappaLeadingDressed depthWeight · C_inner`.
    (B) `recomposedEndpointDressed_closed` — `= 2425/18 + (3/2)·kappaLeading·C_inner`, via the banked
        `kappaLeadingDressed_eq_div` (at `depthWeight_pos`/`depthWeight_lt_one`) + `depthWeight_eq`
        (the fold-forced factor `3/2`). The dressing tie is banked, non-fit.
    (C) `depthTwo_dresses_recomposed` — the composition STRICTLY RAISES the endpoint:
        `recomposedEndpoint < recomposedEndpointDressed`, and the increment is exactly
        `(1/2)·kappaLeading·C_inner > 0` (the depth-2 running is real, ONE-LAW-upward; W8 content).
    (D) `dressed_offsets_base` — the base (way-point + census constant) is preserved:
        `recomposedEndpointDressed − (3/2)·kappaLeading·C_inner = 2425/18` (way-point-consistency
        adjudicator; the assembly is not a fit).
    (E) `recomposedEndpointDressed_normalform` — the `(3/2)`-scaled normal form
        `2425/18 + kappaLeading·(3·cutLog(mass0/mass1) + (7/2)·L_mb + (11/2)·L_b0 − (5/2)·L_b2)`, for the
        narrowing arithmetic core (W9).
    (F) `recomposedEndpointDressed_restated` — a certified two-sided rational bracket of the composed
        endpoint AS `1/α(0)`: `1370984/1e4 ≤ recomposedEndpointDressed ≤ 1371006/1e4`
        (`[137.0984, 137.1006]`), assembled by the W9 abstract arithmetic core `dressed_core` from the
        banked sharp edge-log brackets (Rung1 `cutLog_b0_sharp`/`cutLog_b2_sharp`/`cutLog_mb_sharp` +
        NB11 `cutLog_m01_sharp`) and the banked `κ` bracket.
    (G) `recomposedEndpointDressed_gt_137` — the composed endpoint EXCEEDS `137` (the completion lifts
        the bare `< 137` above the round number; a real content change, W8).
    (H) TEETH: the increment is strictly positive; the composed bracket is strictly two-sided; the
        composition strictly moves the bare endpoint.
    (I) the welded, non-hollow capstone.

  ## THE HONEST GRADE / SCOPE (SOUL "route COMPLETE vs route NOT-YET-FOUND")

  THEOREM-EXACT / CERTIFIED NOW: (A)–(I) — exact identities / inequalities of `Cut` and a certified
  rational bracket of `Cut`/ℚ, foundations-only, no error bar. This node COMPOSES the owner-named omitted
  depth-2 running (the D7 gate's first failing clause repaired) and restates the composed endpoint AS
  `1/α(0)` at ≈ 5-sig-digit width from the current s6/ext6 instruments.

  ROUTE-NOT-YET-FOUND (NAMED, handed to the ONE directed successor — NEVER asserted / NEVER fitted):
   (i) owner step 2 — the HADRONIC ABOVE-FLOOR occupancy series to a certified value/bracket of width
       `< 1e-12` (the same gForm/sumSq counting that banked floor occupancy `16`, at `n ≥ 2`; monotone +
       bounded ⇒ bracketable), so the ledger is TRULY closed (the above-floor is currently only a
       `≤ κ·9·cutLog R` bound, which cannot support ≥15 digits);
   (ii) the ⛔ OWNER ≥15-SIGNIFICANT-DIGIT FLOOR over the COMPOSED (dressed) endpoint — the same banked
        ContinuumBracket instrument at deeper `1/cutPi` (a `1e12` `cutCos` grid) + `s7` mass grids; no fit,
        no new posited foundation.
  NOT a block: forward autonomous structural work remains; no path here fits a measured value or needs a
  new posited foundation.

  ## PHYSICS-WORDS-REMOVABLE (STANDARD §2; seed G5)

  Delete "coupling / running / screening / census / depth / loop / dressing / way-point / freeze-out /
  flavour / fine-structure / prediction": what remains, over the derived ℝ `Cut`, is that
  `recomposedEndpointDressed = 2425/18 + kappaLeadingDressed depthWeight · C_inner`; that this equals
  `2425/18 + (3/2)·kappaLeading·C_inner`; that `recomposedEndpoint < recomposedEndpointDressed`; that the
  increment is `(1/2)·kappaLeading·C_inner`; that `recomposedEndpointDressed − (3/2)·kappaLeading·C_inner
  = 2425/18`; that `137.0984 ≤ recomposedEndpointDressed ≤ 137.1006`; and that
  `137 < recomposedEndpointDressed`. Pure real / rational arithmetic on the banked derived objects. No
  theorem STATEMENT needs a physics word.

  ## NOT FREE-FLOATING (seed / SOUL rail)

  Every production TYPE mentions the banked derived objects (`recomposedEndpoint` / `kappaLeading` /
  `kappaLeadingDressed` / `depthWeight` / `mass0` / `mass2` / `mbRatioDerived` / `bMass` / `cutLog` /
  `cutPi`, over the derived ℝ `Cut`). The composition is the banked X5 loop-factor dressing at the banked
  D3 fold-forced weight; FALSE over an algebra without the banked two-channel / census / depth-tower /
  Born-square-mass / fold-phase / continuum stack — not generic.

  ## SEED HARD GUARDS (G1–G8)

  - G1 NO ASSERTED VALUE — the dressing factor `3/2` FALLS OUT of `depthWeight_eq` (`= 1/3`) via
    `kappaLeadingDressed_eq_div`; the composed value is computed from banked objects, not premised.
  - G2 NO EMPIRICAL NUMBER — no `137.036`, no PDG, no measured `α` in any statement or proof; the measured
    `1/α(0) ≈ 137.036` is removable orientation prose; the composition DIRECTION is FORCED by `κ(w) > κ₀`
    (self-blindness convergence), NOT by where the measured value sits; the weight is `depthWeight = 1/3`,
    fold-derived, pinned to no target. Explicit NO-FIT.
  - G3 EXACT / CERTIFIED — (A)–(E), (G) exact identities/inequalities; (F) a certified rational bracket;
    the open fronts (above-floor series, ≥15-digit floor) NAMED and handed forward.
  - G4 DERIVED, NOT POSITED — `kappaLeadingDressed` (X5), `depthWeight` (D3), `recomposedEndpoint`
    (PerChannelRecomb), all banked derived over the derived ℝ `Cut`.
  - G5 PHYSICS-WORDS-REMOVABLE — see above.
  - G6 FOUNDATIONS-ONLY — `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`; sorry-free, zero
    posited axioms, no compiled-decision bypass, no heartbeat inflation beyond the banked exp-instrument
    ceiling. Ground field the DERIVED ℝ `Cut`; NO Mathlib `Real`/`Complex` as content; NO floats.
  - G7 ONE LAW — the inherited framing (narrow the incomplete `136.307`) FOUGHT the standard (it would
    polish a provably incomplete sum); the owner + the theory redirect to COMPOSING the fold-forced
    depth-2 running FIRST. We returned to the trunk (the census-in-census recurs, `κ(w) = κ₀/(1−w)`, at
    the fold-forced `depthWeight`) and let the STRUCTURE force the value. No fit, no weakened target, no
    asserted digit. The endpoint bracket assembly stays in the abstract arithmetic core `dressed_core`
    (transcendentals out — W9).
  - G8 TEETH (W8) — the increment `(1/2)·kappaLeading·C_inner` is strictly positive (the running is real);
    the composed endpoint strictly exceeds the bare one and strictly exceeds `137`; the composed bracket
    is strictly two-sided.

  DEPENDENCIES (all banked, foundations-only): `D7NarrowingRung1` (the sharp edge-log brackets
  `cutLog_b0_sharp`/`cutLog_b2_sharp`/`cutLog_mb_sharp`/`cutLog_m01_sharp`) — transitively
  `D7CompositionAttestation` (`recomposedEndpoint_normalform`, `kappa_bracket`), `PerChannelRecomb`
  (`recomposedEndpoint`, `recomposedEndpoint_closed`); `DepthTowerDescent` (`kappaLeadingDressed`,
  `kappaLeadingDressed_eq_div`); `DepthWeight` (`depthWeight`, `depthWeight_eq`, `depthWeight_pos`,
  `depthWeight_lt_one`); `FineStructureLoopNormalization` (`kappaLeading`, `kappaLeading_pos`);
  `GenerationMassRatios` / `UpDownRelativeScaleFoldPhase` / `FreezeoutEdgeCrossSectorReduction` (the
  banked shape objects); the derived `Cut`. Standard Mathlib `norm_num`/`nlinarith`/`linarith`/`ring`/
  `field_simp` MACHINERY on the DERIVED objects (STANDARD §3). NO posited value as content, NO Mathlib ℝ
  as content, NO bridge.
-/
import Phys.Algebra.D7NarrowingRung1
import Phys.Algebra.DepthTowerDescent
import Phys.Algebra.LowTailFoldForcedWeight
import Mathlib.Tactic

namespace Phys.Algebra.D7DepthTwoComposed

open Phys.Algebra
open Phys.Algebra.GenerationMassRatios
open Phys.Algebra.UpDownRelativeScaleFoldPhase
open Phys.Algebra.FreezeoutEdgeCrossSectorReduction
open Phys.Algebra.ComposedFreezeoutDissolved
open Phys.Algebra.AssembledBracket
open Phys.Algebra.PerChannelRecomb
open Phys.Algebra.D7CompositionAttestation
open Phys.Algebra.D7NarrowingRung1
open Phys.Algebra.DepthTowerDescent
open Phys.Algebra.DepthWeight
open Phys.Foundation Phys.Foundation.ContinuumQ

noncomputable section

/-- The inner log-combination `C_inner` of the recomposed endpoint's normal form (the loop-running
    content, carried by the bare `kappaLeading`): `2·cutLog(mass0/mass1) + (7/3)·cutLog mbRatioDerived
    + (11/3)·cutLog (bMass 0) − (5/3)·cutLog (bMass 2)`. -/
def cInner : Cut :=
  2 * cutLog (mass0 / mass1) + (7 / 3) * cutLog mbRatioDerived
    + (11 / 3) * cutLog (bMass 0) - (5 / 3) * cutLog (bMass 2)

/-! ## (A) THE COMPOSED (DEPTH-2-RUNNING) ENDPOINT. -/

/-- ★★★ THE CENSUS-CONSERVING ENDPOINT WITH THE FOLD-FORCED ALL-ORDERS DEPTH-2 RUNNING COMPOSED: the
    non-running base `2425/18` (confinement way-point `7179/54` + census constant `16/9`, preserved) plus
    the low-tail loop-running content with the bare loop factor `kappaLeading` replaced by the banked
    all-orders dressed factor `kappaLeadingDressed depthWeight = kappaLeading/(1−depthWeight)` (X5, at the
    fold-forced weight `depthWeight = 1/3`).

    PROVENANCE (N688–N694): this composition is the GLOBAL SECTION of the descent's
    cover — its census weights (11/3, 10/3, 2) are the local active-span sections
    (N688) PLUS the seam-closure cocycle (N693); the identity is banked as
    `GatherEndpointComposition.gather_endpoint_eq`. The base `2425/18` carries the
    light-gem's global census; the corrected local base is `7259/54` (N689). -/
def recomposedEndpointDressed : Cut :=
  2425 / 18 + kappaLeadingDressed depthWeight * cInner

/-! ## (B) THE CLOSED FORM — the fold-forced factor `3/2`. -/

/-- ★★★ THE CLOSED FORM: `recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·C_inner`. The
    all-orders dressed loop factor at the fold-forced weight is `kappaLeadingDressed depthWeight =
    kappaLeading/(1 − 1/3) = (3/2)·kappaLeading` (banked `kappaLeadingDressed_eq_div` + `depthWeight_eq`).
    The factor `3/2` is FORCED (the mean Born self-overlap `1/3` over the one return surface), never
    chosen. -/
theorem recomposedEndpointDressed_closed :
    recomposedEndpointDressed = 2425 / 18 + (3 / 2) * kappaLeading * cInner := by
  unfold recomposedEndpointDressed
  rw [kappaLeadingDressed_eq_div depthWeight (le_of_lt depthWeight_pos) depthWeight_lt_one,
      depthWeight_eq]
  have h : kappaLeading / (1 - (1 / 3 : Cut)) = (3 / 2) * kappaLeading := by
    have h23 : (1 : Cut) - 1 / 3 = 2 / 3 := by norm_num
    rw [h23]; ring
  rw [h]

/-- The bare recomposed endpoint in the same `cInner` normal form (re-export of the banked
    `recomposedEndpoint_normalform`, folded through `cInner`): `recomposedEndpoint = 2425/18 +
    kappaLeading·C_inner`. -/
theorem recomposedEndpoint_cInner :
    recomposedEndpoint = 2425 / 18 + kappaLeading * cInner := by
  rw [recomposedEndpoint_normalform]; unfold cInner; ring

/-! ## (C) THE COMPOSITION STRICTLY RAISES THE ENDPOINT (the depth-2 running is real, ONE-LAW-upward). -/

/-- The loop-running content `kappaLeading·C_inner` is strictly positive (the bare endpoint exceeds the
    base `2425/18`). Read from the banked sharp edge-log brackets + the `κ` bracket via the Rung1 core. -/
theorem kappaLeading_cInner_pos : 0 < kappaLeading * cInner := by
  have hbare : recomposedEndpoint = 2425 / 18 + kappaLeading * cInner := recomposedEndpoint_cInner
  have hlo : (1363071880 : Cut) / 10000000 ≤ recomposedEndpoint :=
    recomposedEndpoint_restated_sharp.1
  have : (2425 : Cut) / 18 < recomposedEndpoint := by
    have : (2425 : Cut) / 18 < 1363071880 / 10000000 := by norm_num
    linarith
  rw [hbare] at this; linarith

/-- ★★★ THE DEPTH-2 RUNNING STRICTLY RAISES THE ENDPOINT: `recomposedEndpoint <
    recomposedEndpointDressed`, and the increment is exactly `(1/2)·kappaLeading·C_inner > 0`. The
    census-in-census correction is real and ONE-LAW-upward (`κ(w) > κ₀`), NOT a vacuous zero. -/
theorem depthTwo_dresses_recomposed :
    recomposedEndpointDressed = recomposedEndpoint + (1 / 2) * kappaLeading * cInner
      ∧ recomposedEndpoint < recomposedEndpointDressed := by
  have hd : recomposedEndpointDressed = 2425 / 18 + (3 / 2) * kappaLeading * cInner :=
    recomposedEndpointDressed_closed
  have hb : recomposedEndpoint = 2425 / 18 + kappaLeading * cInner := recomposedEndpoint_cInner
  refine ⟨?_, ?_⟩
  · rw [hd, hb]; ring
  · rw [hd, hb]
    have hpos : 0 < kappaLeading * cInner := kappaLeading_cInner_pos
    nlinarith [hpos]

/-! ## (D) THE BASE (WAY-POINT + CENSUS CONSTANT) IS PRESERVED (way-point-consistency adjudicator). -/

/-- ★★ THE BASE IS PRESERVED: `recomposedEndpointDressed − (3/2)·kappaLeading·C_inner = 2425/18` — the
    non-running base (confinement way-point `7179/54` + census constant `16/9`) never moves; only the
    low-tail loop content is dressed. The composition is not a fit. -/
theorem dressed_offsets_base :
    recomposedEndpointDressed - (3 / 2) * kappaLeading * cInner = 2425 / 18 := by
  rw [recomposedEndpointDressed_closed]; ring

/-! ## (E) THE `(3/2)`-SCALED NORMAL FORM (for the narrowing arithmetic core, W9). -/

/-- ★★ THE DRESSED NORMAL FORM: `recomposedEndpointDressed = 2425/18 + kappaLeading·(3·cutLog(mass0/mass1)
    + (7/2)·cutLog mbRatioDerived + (11/2)·cutLog (bMass 0) − (5/2)·cutLog (bMass 2))` — the `(3/2)`-scaled
    coefficients of the loop-running content, transcendentals kept symbolic. -/
theorem recomposedEndpointDressed_normalform :
    recomposedEndpointDressed
      = 2425 / 18 + kappaLeading * (3 * cutLog (mass0 / mass1)
          + (7 / 2) * cutLog mbRatioDerived + (11 / 2) * cutLog (bMass 0)
          - (5 / 2) * cutLog (bMass 2)) := by
  rw [recomposedEndpointDressed_closed]; unfold cInner; ring

/-! ## (F) THE RE-ASSEMBLED RESTATEMENT OF THE COMPOSED ENDPOINT AS `1/α(0)`. -/

set_option maxHeartbeats 2000000 in
/-- THE DRESSED ABSTRACT ARITHMETIC CORE (W9 — opaque `k, L, Lmb, Lb0, Lb2` with the Rung1 sharpened
    bracket hypotheses; transcendentals OUT of the arithmetic): with `k` in the banked `κ` bracket and
    the four sharp logs in their Rung1 windows, `2425/18 + k·(3L + (7/2)Lmb + (11/2)Lb0 − (5/2)Lb2) ∈
    [1370984/1e4, 1371006/1e4]` (`[137.0984, 137.1006]`). The inner combination is in
    `[224069/1e4, 224070/1e4]`. -/
theorem dressed_core (k L Lmb Lb0 Lb2 : Cut)
    (hklo : (1250000000 / 3926990817 : Cut) / 3 ≤ k)
    (hkhi : k ≤ (50000000000 / 157079632679 : Cut) / 3)
    (hL : (815406111272 : Cut) / 100000000000 ≤ L ∧ L ≤ 815406111273 / 100000000000)
    (hLmb : (-40893067 / 10000000 : Cut) ≤ Lmb ∧ Lmb ≤ -40893066 / 10000000)
    (hLb0 : (17337287 : Cut) / 10000000 ≤ Lb0 ∧ Lb0 ≤ 17337288 / 10000000)
    (hLb2 : (-10887238 / 10000000 : Cut) ≤ Lb2 ∧ Lb2 ≤ -10887236 / 10000000) :
    (1370984 / 10000 : Cut)
        ≤ 2425 / 18 + k * (3 * L + (7 / 2) * Lmb + (11 / 2) * Lb0 - (5 / 2) * Lb2)
      ∧ 2425 / 18 + k * (3 * L + (7 / 2) * Lmb + (11 / 2) * Lb0 - (5 / 2) * Lb2)
        ≤ 1371006 / 10000 := by
  obtain ⟨hLlo, hLhi⟩ := hL
  obtain ⟨hmblo, hmbhi⟩ := hLmb
  obtain ⟨hb0lo, hb0hi⟩ := hLb0
  obtain ⟨hb2lo, hb2hi⟩ := hLb2
  have hkpos : 0 < k := lt_of_lt_of_le (by norm_num) hklo
  have hcombo_lo : (224069 / 10000 : Cut)
      ≤ 3 * L + (7 / 2) * Lmb + (11 / 2) * Lb0 - (5 / 2) * Lb2 := by nlinarith
  have hcombo_hi : 3 * L + (7 / 2) * Lmb + (11 / 2) * Lb0 - (5 / 2) * Lb2
      ≤ 224070 / 10000 := by nlinarith
  set C := 3 * L + (7 / 2) * Lmb + (11 / 2) * Lb0 - (5 / 2) * Lb2 with hC
  have hCpos : 0 < C := lt_of_lt_of_le (by norm_num) hcombo_lo
  refine ⟨?_, ?_⟩
  · have h1 : k * C ≥ ((1250000000 / 3926990817 : Cut) / 3) * (224069 / 10000) := by
      have := mul_le_mul hklo hcombo_lo (by norm_num) (le_of_lt hkpos); linarith [this]
    have h2 : ((1250000000 / 3926990817 : Cut) / 3) * (224069 / 10000)
        ≥ 1370984 / 10000 - 2425 / 18 := by norm_num
    linarith
  · have h1 : k * C ≤ ((50000000000 / 157079632679 : Cut) / 3) * (224070 / 10000) := by
      have := mul_le_mul hkhi hcombo_hi (le_of_lt hCpos) (by positivity); linarith [this]
    have h2 : ((50000000000 / 157079632679 : Cut) / 3) * (224070 / 10000)
        ≤ 1371006 / 10000 - 2425 / 18 := by norm_num
    linarith

/-- ★★★ THE RE-ASSEMBLED RESTATEMENT OF THE COMPOSED ENDPOINT AS `1/α(0)`: `137.0984 ≤
    recomposedEndpointDressed ≤ 137.1006` — a certified two-sided rational bracket of `Cut` for the
    census-conserving endpoint with the fold-forced all-orders depth-2 running composed. Assembled by
    `dressed_core` from the banked `κ` bracket + the four Rung1 sharp logs, through the dressed normal
    form. -/
theorem recomposedEndpointDressed_restated :
    (1370984 / 10000 : Cut) ≤ recomposedEndpointDressed
      ∧ recomposedEndpointDressed ≤ 1371006 / 10000 := by
  rw [recomposedEndpointDressed_normalform]
  obtain ⟨hklo, hkhi⟩ := kappa_bracket
  have hb0 : ((17337287 : Cut) / 10000000 ≤ cutLog (bMass 0)
      ∧ cutLog (bMass 0) ≤ 17337288 / 10000000) := cutLog_bMass0_sharp
  have hb2 : ((-10887238 / 10000000 : Cut) ≤ cutLog (bMass 2)
      ∧ cutLog (bMass 2) ≤ -10887236 / 10000000) := cutLog_bMass2_sharp
  exact dressed_core kappaLeading (cutLog (mass0 / mass1)) (cutLog mbRatioDerived)
    (cutLog (bMass 0)) (cutLog (bMass 2)) hklo hkhi cutLog_m01_sharp cutLog_mb_sharp hb0 hb2

/-! ## (G) THE COMPOSED ENDPOINT EXCEEDS 137. -/

/-- ★★★ THE COMPOSED ENDPOINT EXCEEDS `137`: `137 < recomposedEndpointDressed`, re-certified from the
    sharp lower bound `≥ 1370984/1e4 = 137.0984 > 137`. Composing the fold-forced depth-2 running LIFTS
    the bare `recomposedEndpoint < 137` above the round number — a real content change. The measured
    `1/α(0) ≈ 137.036` is REMOVABLE ORIENTATION PROSE (in no proof); the derivation is not steered by it
    (G2 NO-FIT). -/
theorem recomposedEndpointDressed_gt_137 : 137 < recomposedEndpointDressed := by
  have h := recomposedEndpointDressed_restated.1
  have : (137 : Cut) < 1370984 / 10000 := by norm_num
  linarith

/-! ## (H) W8 NON-VACUITY WITH TEETH. -/

theorem dressed_increment_pos : 0 < (1 / 2) * kappaLeading * cInner := by
  have := kappaLeading_cInner_pos; nlinarith [this]

theorem dressed_restated_strict : (1370984 / 10000 : Cut) < 1371006 / 10000 := by norm_num

/-- ★ W8 — THE COMPOSITION GENUINELY MOVES THE ENDPOINT ACROSS `137`: the bare endpoint is `< 137`
    (`recomposedEndpoint_lt_137_sharp`) and the composed endpoint is `> 137`
    (`recomposedEndpointDressed_gt_137`), so the fold-forced depth-2 running carries the endpoint from
    below to above the round number — a genuine, content-bearing composition, not a vacuous re-labelling. -/
theorem composition_crosses_137 :
    recomposedEndpoint < 137 ∧ 137 < recomposedEndpointDressed :=
  ⟨recomposedEndpoint_lt_137_sharp, recomposedEndpointDressed_gt_137⟩

/-! ## (I) THE CAPSTONE — welded, non-hollow. -/

/-- ★★★ THE D7 DEPTH-2 RUNNING COMPOSED (arc-D THE DIGITS, welded landing).

    (A) THE COMPOSED ENDPOINT `recomposedEndpointDressed = 2425/18 + kappaLeadingDressed depthWeight ·
        C_inner` — the census-conserving endpoint with the FOLD-FORCED all-orders depth-2 running (the
        census-in-census) composed at the fold-forced weight `depthWeight = 1/3`;
    (B) THE CLOSED FORM `= 2425/18 + (3/2)·kappaLeading·C_inner` (the factor `3/2 = 1/(1−1/3)` FORCED by
        `depthWeight_eq` via the banked `kappaLeadingDressed_eq_div`);
    (C) THE STRICT UPWARD RAISE `recomposedEndpoint < recomposedEndpointDressed`, increment exactly
        `(1/2)·kappaLeading·C_inner > 0` (the running is real, ONE-LAW-upward);
    (D) THE BASE PRESERVED `recomposedEndpointDressed − (3/2)·kappaLeading·C_inner = 2425/18`
        (way-point + census constant untouched — not a fit);
    (F) THE RESTATEMENT AS `1/α(0)`: `137.0984 ≤ recomposedEndpointDressed ≤ 137.1006` (certified
        two-sided rational bracket from the banked s6/ext6 instruments);
    (G) THE COMPOSED ENDPOINT EXCEEDS `137` (`137 < recomposedEndpointDressed`) — the completion lifts the
        bare `< 137` above the round number;
    (H) TEETH: the increment is strictly positive; the composed bracket is two-sided; the composition
        carries the endpoint from below `137` to above it.

    Statable from no single conjunct alone (non-hollow). ⚠ HONEST GRADE: (A)–(H) THEOREM-EXACT /
    CERTIFIED now (exact identities / inequalities / a certified rational bracket of `Cut`/ℚ,
    foundations-only, no error bar). This node COMPOSES the owner-named omitted depth-2 running (the D7
    gate's first failing clause repaired) — the endpoint moves from the bare `≈ 136.307` to the composed
    `≈ 137.0997`.
    ROUTE-NOT-YET-FOUND (NAMED, handed to the ONE directed successor — NEVER asserted / NEVER fitted):
    (i) the HADRONIC ABOVE-FLOOR occupancy series to a `< 1e-12` certified value/bracket (owner step 2;
    the gForm counting that banked floor `16`, at `n ≥ 2`); (ii) the ⛔ OWNER ≥15-SIGNIFICANT-DIGIT FLOOR
    over the COMPOSED endpoint (deeper `1/cutPi` + `s7` grids). The measured `1/α(0) ≈ 137.036` is
    REMOVABLE ORIENTATION PROSE ONLY. -/
theorem d7DepthTwoComposed_landing :
    -- (B) the closed form
    (recomposedEndpointDressed = 2425 / 18 + (3 / 2) * kappaLeading * cInner)
    -- (C) the strict upward raise + exact increment
    ∧ (recomposedEndpointDressed = recomposedEndpoint + (1 / 2) * kappaLeading * cInner
        ∧ recomposedEndpoint < recomposedEndpointDressed)
    -- (D) the base preserved
    ∧ (recomposedEndpointDressed - (3 / 2) * kappaLeading * cInner = 2425 / 18)
    -- (F) the restatement as 1/α(0)
    ∧ ((1370984 / 10000 : Cut) ≤ recomposedEndpointDressed
        ∧ recomposedEndpointDressed ≤ 1371006 / 10000)
    -- (G) the composed endpoint exceeds 137
    ∧ (137 < recomposedEndpointDressed)
    -- (H) teeth
    ∧ (0 < (1 / 2) * kappaLeading * cInner)
    ∧ ((1370984 / 10000 : Cut) < 1371006 / 10000)
    ∧ (recomposedEndpoint < 137 ∧ 137 < recomposedEndpointDressed) :=
  ⟨recomposedEndpointDressed_closed,
   depthTwo_dresses_recomposed,
   dressed_offsets_base,
   recomposedEndpointDressed_restated,
   recomposedEndpointDressed_gt_137,
   dressed_increment_pos,
   dressed_restated_strict,
   composition_crosses_137⟩

end

end Phys.Algebra.D7DepthTwoComposed
