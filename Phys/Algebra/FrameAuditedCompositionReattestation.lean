/-
  # FRAME-AUDITED COMPOSITION RE-ATTESTATION — arc-D THE DIGITS
  #   (owner redirect, t_4d2581c1; SUCCEEDS the OWNER FRAME AUDIT F1 (N580b) + F2 (N581))
  ==================================================================================================

  OWNER-DIRECTED (t_4d2581c1, the RECOMPOSE + RE-ATTEST node closing the OWNER FRAME AUDIT). The
  freeze-out endpoint narrowing was GATED: the dressed endpoint value was a CONFIRMED DEFECTIVE value,
  so the residual defect had to live in the frame's own surviving structural claims. The owner's
  response — the FRAME AUDIT — re-derived those two claims AB INITIO in the forced arena:

    • F1 (N580b `FrameAuditRecombinationArchitecture`): THE RECOMBINATION ARCHITECTURE. ARCH-B
      (run-both-channels-then-recombine, the isospin channel at its NET running census `netIsoDepth s =
      isoDepth − s`) is FORCED; ARCH-A (the `+70`-constant additive weld) is the Z1-forbidden `s = 0`
      switch-off. F1 CONFIRMS the above-`v` endpoint architecture.
    • F2 (N581 `FrameAuditDressingScope`): THE DRESSING SCOPE. In the F1-forced ARCH-B arena the
      all-orders `3/2 = 1/(1 − w)` (`w = depthWeight = 1/3`, the channel-independent mean Born
      self-overlap over the fold's ONE return surface) dresses SCOPE-(c) — the resummed-vs-unresummed
      partition: ONLY the surviving-κ un-resummed low-tail running `cInner`, EXEMPTING the
      already-all-orders-resummed base `2425/18`. SCOPE-(a) uniform whole-tower is way-point-forbidden;
      SCOPE-(b) per-band-varying is the fold-killed tuning knob. F2 CONFIRMS the banked composition.

  BOTH halves CONFIRM the banked `recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·cInner`
  composition. This node RE-ATTESTS the D7 COMPOSITION-COMPLETENESS over that frame-audited object.

  ## THE OWNER GATE DISCHARGE (the formal check)

  The narrowing was gated by "narrowing a defective value is polishing a bug report". The re-attestation
  is the FORMAL check whether composition-completeness holds over the FRAME-AUDITED object:
    • IF a banked contribution were STILL missing / mis-composed → the real defect → a W1 dissolution
      ticket (hunt the forced piece, never fit);
    • IF the composition is complete → the residual endpoint precision is INSTRUMENT precision (the
      ≥15-significant-digit floor), NOT a frame defect ⇒ RESUME the narrowing.

  This node shows every banked structural fact composes cleanly:
    (A) the F2 DRESSING SCOPE holds over the frame-audited object (SCOPE-(c) closed form + base exempt +
        base is resummed content);
    (B) the F1 RECOMBINATION ARCHITECTURE is the forced ARCH-B (exact shift `≠ 0`, `projectedS ≠ 0`, the
        one-law downward correction);
    (C) the F1↔F2 TIE: the dressing weight is the channel-independent `depthWeight = 1/3`;
    (D) the D7 BARE CENSUS-COMPLETENESS holds (full re-export) AND the dressed object differs from that
        census-complete bare object by EXACTLY the fold-forced increment `(1/2)·kappaLeading·cInner` on
        the SAME `cInner` running content, base identical — so census-completeness of the bare object
        carries to every dressed contribution (nothing added but the forced depth-2 loop factor);
    (E) teeth (the census gem, the dressing step, the increment, the interior weight are all real).

  ⇒ THE COMPOSITION IS COMPLETE over the frame-audited object; the residual endpoint defect is NOT in
  the frame's two surviving structural claims. The ≥15-digit floor is INSTRUMENT precision, handed to the
  ONE directed narrowing successor (a deeper `1/cutPi` grid + a deeper `cutLog(mass0/mass1)` grid, the
  same banked instrument ladder, no new axiom, no fit).

  ## THE HONEST GRADE / SCOPE (SOUL "route COMPLETE vs route NOT-YET-FOUND")

  THEOREM-EXACT (exact identities / inequalities of the derived ℝ `Cut`, foundations-only, no error
  bar): the whole re-attestation is an ASSEMBLY of banked exact theorems — the F1 architecture
  adjudicators, the F2 scope adjudicators, the D7 bare census-completeness, and the bare↔dressed weld.
  NO route-not-yet-found in THIS node: the re-attestation is a proved theorem. The ⛔ OWNER
  ≥15-SIGNIFICANT-DIGIT FLOOR is the ROUTE-NOT-YET-FOUND handed to the ONE directed narrowing successor
  (instrument precision of the derived inputs; deeper partial sums of banked series, no new axiom, no
  fit). The measured `1/α(0) ≈ 137.036` is REMOVABLE PROSE ONLY; no empirical number in any statement or
  proof; the composition is attested by the fold's own architecture + scope + census theorems, NOT by
  where a measured value sits. Explicit NO-FIT.

  ## PHYSICS-WORDS-REMOVABLE (STANDARD §2)

  Delete "coupling / running / screening / band / channel / isospin / hypercharge / census / depth /
  dressing / way-point / transmutation / recombination / resummation / architecture / fine-structure":
  what remains over the derived ℝ `Cut` is that `recomposedEndpointDressed = 2425/18 +
  (3/2)·kappaLeading·cInner` with `recomposedEndpointDressed − (3/2)·kappaLeading·cInner = 2425/18` and
  `2425/18 = 7179/54 + 16/9`; that the two labelled family readings differ at the top entry by
  `bandScreen projectedS topBandRatio` with `projectedS ≠ 0`; that `depthWeight = 1/3 ≠ 0, ≠ 1`; that
  `recomposedEndpointDressed = recomposedEndpoint + (1/2)·kappaLeading·cInner`; that the bare object
  satisfies its closed-form census partition; and the teeth. Pure `Cut` arithmetic / order on the banked
  objects. No theorem STATEMENT needs a physics word.

  ## NOT FREE-FLOATING (SOUL free-floating rail)

  Every production TYPE mentions the banked derived objects: `recomposedEndpointDressed` / `cInner` /
  `kappaLeading` (`D7DepthTwoComposed`), `recomposedEndpoint` (`PerChannelRecomb`), `recombArch` /
  `projectedS` / `bandScreen` / `topBandRatio` / `chargeTraceDepth` (F1 / X6), `depthWeight`
  (`FrameAuditDressingScope` / D3), the banked D7 census-completeness conjunction, all over the derived
  ℝ `Cut`. The re-attestation is literally the conjunction of banked architecture / scope / census
  theorems about these objects. FALSE over an algebra without the banked two-channel / census /
  screening / return-surface / depth-tower / Born-square-mass / continuum stack — not generic, not
  carrier-agnostic.

  ## SEED HARD GUARDS (G1–G8)

  - G1 NO ASSERTED VALUE — the closed form, the base exemption, `2425/18 = 7179/54 + 16/9`, the exact
    architecture shift, `projectedS ≠ 0`, `depthWeight = 1/3`, the bare census-completeness, and the
    `(1/2)·kappaLeading·cInner` increment ALL fall out of banked theorems (`scopeC_closed`,
    `scopeC_exempts_base`, `exempt_base_is_resummed`, `architecture_shift_exact`, `forced_param_ne_zero`,
    `weight_channel_independent`, `recomposed_composition_complete`, `depthTwo_dresses_recomposed`); no
    value premised.
  - G2 NO EMPIRICAL NUMBER — no `137`, no `137.036`, no `0.231`, no measured `α` in any statement or
    proof; no attestation step selected BECAUSE of where a measured value sits; the measured constants
    are this prose only, fully removable. Explicit NO-FIT.
  - G3 EXACT / CERTIFIED — every landing is an EXACT identity / inequality of `Cut`. No open sub-fact in
    THIS node: the re-attestation is proved. The ONE open front (the ≥15-digit floor) is NAMED and handed
    forward as instrument precision, never asserted.
  - G4 DERIVED, NOT POSITED — every input is a banked derived object (the F1 architecture adjudicators,
    the F2 scope adjudicators, the D7 census-completeness, the D7DepthTwoComposed bare↔dressed weld).
  - G5 PHYSICS-WORDS-REMOVABLE — see above.
  - G6 FOUNDATIONS-ONLY — `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`; sorry-free, zero
    posited axioms, no compiled-decision bypass, no heartbeat inflation, NO `foldRetention` (only the
    banked F1 / F2 / D7 modules and their banked transitive deps). Ground field the DERIVED ℝ `Cut`; NO
    Mathlib `Real` / `Complex` as content — Mathlib is MACHINERY only; NO floats anywhere.
  - G7 ONE LAW — the re-attestation did not fight: the banked architecture / scope / census / weld
    theorems close every step. It RETURNS to the trunk (the frame audit's forced ARCH-B census + the
    fold's ONE return surface weight ⇒ the resummed-vs-unresummed content partition) to attest
    completeness — it does not fit, does not weaken the floor, does not assert a digit; it NAMES the
    remaining instrument precision and hands it to ONE successor.
  - G8 TEETH (W8) — the census gem genuinely contributes (`2425/18 ≠ 7179/54`); the dressing is a real
    upward step (`recomposedEndpoint < recomposedEndpointDressed`); the increment is strictly positive
    (`0 < (1/2)·kappaLeading·cInner`); the weight is genuinely interior (`depthWeight ≠ 0, ≠ 1`); the
    architecture shift is nonzero (`projectedS ≠ 0`).

  DEPENDENCIES (all banked, foundations-only): `FrameAuditRecombinationArchitecture`
  (`architecture_shift_exact`, `forced_param_ne_zero`, `archB_below_archA_topBand`);
  `FrameAuditDressingScope` (`scopeC_closed`, `scopeC_exempts_base`, `exempt_base_is_resummed`,
  `weight_channel_independent`, `scopeC_dresses`); `D7CompositionAttestation`
  (`recomposed_composition_complete`); `D7DepthTwoComposed` (`recomposedEndpointDressed`, `cInner`,
  `kappaLeading`, `depthTwo_dresses_recomposed`, `kappaLeading_cInner_pos`); `PerChannelRecomb`
  (`recomposedEndpoint`); the derived `Cut`. Standard Mathlib `norm_num` / `rw` MACHINERY on the DERIVED
  objects (STANDARD §3). NO posited value as content, NO Mathlib ℝ as content, NO bridge.
-/
import Phys.Algebra.FrameAuditRecombinationArchitecture
import Phys.Algebra.FrameAuditDressingScope
import Phys.Algebra.D7CompositionAttestation
import Mathlib.Tactic

namespace Phys.Algebra.FrameAuditedCompositionReattestation

open Phys.Algebra
open Phys.Algebra.BandEdgeList
open Phys.Algebra.CasimirProjectionSelectsS
open Phys.Algebra.DepthWeight
open Phys.Algebra.FrameAuditRecombinationArchitecture
open Phys.Algebra.FrameAuditDressingScope
open Phys.Algebra.D7CompositionAttestation
open Phys.Algebra.D7DepthTwoComposed
open Phys.Algebra.PerChannelRecomb
open Phys.Algebra.RecomposedRunningEndpoint
open Phys.Algebra.CensusEnumerationTermination
open Phys.Algebra.CensusCompletenessClosure
open Phys.Algebra.DecouplingCensusRemoval
open Phys.Algebra.GenerationMassRatios
open Phys.Algebra.UpDownRelativeScaleFoldPhase
open Phys.Algebra.FreezeoutEdgeCrossSectorReduction
open Phys.Algebra.SubBandCompletion
open Phys.Foundation Phys.Foundation.ContinuumQ

noncomputable section

/-! ## (A) THE F2 DRESSING SCOPE holds over the frame-audited object. -/

/-- ★★★ THE FRAME-AUDITED CLOSED FORM (F2 SCOPE-(c)): `recomposedEndpointDressed = 2425/18 +
    (3/2)·kappaLeading·cInner` (`FrameAuditDressingScope.scopeC_closed`). The all-orders `3/2` dresses
    ONLY the surviving-κ un-resummed low-tail running `cInner`; the base is untouched. This is the object
    the re-attestation is over. -/
theorem reattest_scopeC_closed :
    recomposedEndpointDressed = 2425 / 18 + (3 / 2) * kappaLeading * cInner :=
  FrameAuditDressingScope.scopeC_closed

/-- ★★★ THE FRAME-AUDITED BASE EXEMPTION (F2 SCOPE-(c)): `recomposedEndpointDressed −
    (3/2)·kappaLeading·cInner = 2425/18` (`FrameAuditDressingScope.scopeC_exempts_base`). The resummed
    base is NEVER dressed — the census-in-census content of the base is already inside the banked
    all-orders transmutation scales, so re-dressing it would double-count. -/
theorem reattest_base_exempt :
    recomposedEndpointDressed - (3 / 2) * kappaLeading * cInner = 2425 / 18 :=
  FrameAuditDressingScope.scopeC_exempts_base

/-- ★★ THE EXEMPT BASE IS ALREADY-RESUMMED CONTENT: `2425/18 = 7179/54 + 16/9`
    (`FrameAuditDressingScope.exempt_base_is_resummed`) — the confinement way-point `7179/54` (a
    transmutation-scale assembly) plus the `cutPi`-cancelled census gem `16/9`. This is WHY the base is
    exempt: both pieces are already all-orders-resummed. -/
theorem reattest_base_is_resummed : (2425 : Cut) / 18 = 7179 / 54 + 16 / 9 :=
  FrameAuditDressingScope.exempt_base_is_resummed

/-! ## (B) THE F1 RECOMBINATION ARCHITECTURE is the forced ARCH-B. -/

/-- ★★★ THE FRAME-AUDITED ARCHITECTURE SHIFT (F1): the two architecture readings differ at the top band
    by EXACTLY the Z1 anti-screening `bandScreen projectedS topBandRatio`
    (`FrameAuditRecombinationArchitecture.architecture_shift_exact`). ARCH-A over-counts the top-band
    census by exactly the `s` slice the Z1 negative isospin self-census removes. -/
theorem reattest_architecture_shift :
    recombArch 0 [(chargeTraceDepth, topBandRatio)]
        - recombArch projectedS [(chargeTraceDepth, topBandRatio)]
      = bandScreen projectedS topBandRatio :=
  FrameAuditRecombinationArchitecture.architecture_shift_exact

/-- ★★ THE FRAME-AUDITED FORCED PARAMETER (F1): `projectedS ≠ 0`
    (`FrameAuditRecombinationArchitecture.forced_param_ne_zero`) — the Z1 negative isospin gather
    self-census forces a strictly positive reduction, so ARCH-A (`t = 0`, the switch-off) is FORBIDDEN
    and the forced architecture is ARCH-B (`t = projectedS`). -/
theorem reattest_arch_forced : projectedS ≠ 0 :=
  FrameAuditRecombinationArchitecture.forced_param_ne_zero

/-- ★ THE FRAME-AUDITED ONE-LAW DIRECTION (F1): ARCH-B runs strictly BELOW ARCH-A at the top band
    (`FrameAuditRecombinationArchitecture.archB_below_archA_topBand`) — the derived anti-screening
    corrects the additive overshoot in the physically required direction. -/
theorem reattest_arch_direction :
    recombArch projectedS [(chargeTraceDepth, topBandRatio)]
      < recombArch 0 [(chargeTraceDepth, topBandRatio)] :=
  FrameAuditRecombinationArchitecture.archB_below_archA_topBand

/-! ## (C) THE F1↔F2 TIE — the dressing weight is channel-independent. -/

/-- ★★ THE FRAME-AUDITED CHANNEL-INDEPENDENT WEIGHT (the F1↔F2 weld): `depthWeight = 1/3 ≠ 0, ≠ 1`
    (`FrameAuditDressingScope.weight_channel_independent`). The dressing weight is the mean Born
    self-overlap over the fold's ONE return surface — the SAME 2-sphere for every band and every channel
    — so F1 fixes the CENSUS per channel (`netIsoDepth s`) and F2 fixes the ONE shared WEIGHT + the
    content SCOPE. -/
theorem reattest_weight_tie :
    depthWeight = 1 / 3 ∧ depthWeight ≠ 0 ∧ depthWeight ≠ 1 :=
  FrameAuditDressingScope.weight_channel_independent

/-! ## (D) THE D7 BARE CENSUS-COMPLETENESS + the bare↔dressed weld. -/

/-- ★★★ THE BARE CENSUS-COMPLETENESS (D7, full re-export): every banked structural contribution to the
    bare recomposed census-conserving endpoint is INCLUDED or proven zero — the closed form, the
    census-level partition (`16/3 = 10/3 + 2`), both way-point adjudicators (EW `6923/54`, conf
    partial-sum `7179/54`), enumeration exhaustiveness (`chargeTraceDepth` = cap-forced ladder trace at
    `finrank ℚ Uhol = 3`), and termination (freeze below the lightest edge)
    (`D7CompositionAttestation.recomposed_composition_complete`). -/
theorem reattest_bare_census_complete (rb rc rl d : Cut) (rs : List Cut) :
    (recomposedEndpoint
      = 7179 / 54 + bandScreen leptonCensus (mass0 / mass1)
        + (16 / 9
          + kappaLeading * ((7 / 3) * cutLog mbRatioDerived + (11 / 3) * cutLog (bMass 0)
            - (5 / 3) * cutLog (bMass 2))))
    ∧ chargeTraceDepth = ((freezeoutStaircaseCensus : ℚ) : Cut) + leptonCensus
    ∧ recomposedInvAlphaEM [(chargeTraceDepth, topBandRatio)] = 6923 / 54
    ∧ recomposedEndpoint - (leptonTailScreen + freezeoutStaircaseSummand) = 7179 / 54
    ∧ (chargeTraceDepth = ((ladderChargeTrace (Module.finrank ℚ Uhol) : ℚ) : Cut)
        ∧ Module.finrank ℚ Uhol = 3)
    ∧ (invAlphaZero (completeFreezeoutList rb rc rl d ++ rs.map (fun r => (frozenCensus, r)))
        = invAlphaZero (completeFreezeoutList rb rc rl d)) :=
  D7CompositionAttestation.recomposed_composition_complete rb rc rl d rs

/-- ★★★ THE BARE↔DRESSED WELD: `recomposedEndpointDressed = recomposedEndpoint +
    (1/2)·kappaLeading·cInner` (`D7DepthTwoComposed.depthTwo_dresses_recomposed.1`). The frame-audited
    dressed object differs from the census-complete BARE object by EXACTLY the fold-forced depth-2
    increment on the SAME `cInner` running content — the base is identical. Therefore the D7 bare
    census-completeness carries to every dressed contribution: the dressing adds NOTHING but the
    forced `(1/2)·κ·cInner` loop factor, so no banked contribution is missing or double-counted. -/
theorem reattest_dressed_weld :
    recomposedEndpointDressed = recomposedEndpoint + (1 / 2) * kappaLeading * cInner :=
  (D7DepthTwoComposed.depthTwo_dresses_recomposed).1

/-! ## (E) W8 NON-VACUITY WITH TEETH. -/

/-- ★ W8 — THE CENSUS GEM GENUINELY CONTRIBUTES: `2425/18 ≠ 7179/54`. The base is NOT just the
    confinement way-point — the `cutPi`-cancelled census gem `16/9` is a real, nonzero part of it, so the
    resummed base is genuine content and its exemption is not vacuous. -/
theorem census_gem_real : (2425 / 18 : Cut) ≠ 7179 / 54 := by norm_num

/-- ★★ W8 — THE DRESSING IS A REAL UPWARD STEP: `recomposedEndpoint < recomposedEndpointDressed`
    (`FrameAuditDressingScope.scopeC_dresses`). The all-orders `3/2` running of the un-resummed content
    is a REAL upward correction, not a null step — so the re-attestation is over a genuinely dressed
    object. -/
theorem dressing_real :
    PerChannelRecomb.recomposedEndpoint < recomposedEndpointDressed :=
  FrameAuditDressingScope.scopeC_dresses

/-- ★★ W8 — THE DEPTH-2 INCREMENT IS STRICTLY POSITIVE: `0 < (1/2)·kappaLeading·cInner`. The weld
    increment (the ONLY difference between the dressed and the census-complete bare object) is real and
    ONE-LAW-upward — the bare census-completeness carries to the dressed object with a genuine, nonzero
    correction, not a relabelling. -/
theorem increment_pos : (0 : Cut) < (1 / 2) * kappaLeading * cInner := by
  have h : (0 : Cut) < kappaLeading * cInner := D7DepthTwoComposed.kappaLeading_cInner_pos
  nlinarith [h]

/-! ## (F) THE CAPSTONE — the re-attestation, welded, non-hollow. -/

/-- ★★★ THE FRAME-AUDITED COMPOSITION RE-ATTESTATION (welded landing): the D7 composition-completeness
    RE-ATTESTED over the F1+F2-audited object. Every banked structural contribution to the frame-audited
    `recomposedEndpointDressed` composition is INCLUDED or proven exempt/zero:

    (A) ★ THE F2 DRESSING SCOPE holds: `recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·cInner`
        (`reattest_scopeC_closed`) with the base EXEMPT (`reattest_base_exempt`), and the exempt base IS
        already-resummed content `2425/18 = 7179/54 + 16/9` (`reattest_base_is_resummed`);
    (B) ★ THE F1 RECOMBINATION ARCHITECTURE is the forced ARCH-B: the two architecture readings differ at
        the top band by `bandScreen projectedS topBandRatio` (`reattest_architecture_shift`) with
        `projectedS ≠ 0` (`reattest_arch_forced`), and ARCH-B runs strictly below ARCH-A
        (`reattest_arch_direction`);
    (C) ★ THE F1↔F2 TIE: the dressing weight is the channel-independent `depthWeight = 1/3 ≠ 0, ≠ 1`
        (`reattest_weight_tie`);
    (D) ★ THE D7 BARE CENSUS-COMPLETENESS holds (`reattest_bare_census_complete`: closed form + census
        partition + both way-points + enumeration exhaustiveness + termination), AND the frame-audited
        dressed object differs from that census-complete bare object by EXACTLY the fold-forced increment
        `recomposedEndpointDressed = recomposedEndpoint + (1/2)·kappaLeading·cInner`
        (`reattest_dressed_weld`) — so census-completeness of the bare object carries to every dressed
        contribution (nothing added but the forced depth-2 loop factor);
    (E) TEETH: the census gem genuinely contributes (`census_gem_real`); the dressing is a real upward
        step (`dressing_real`); the depth-2 increment is strictly positive (`increment_pos`); the weight
        is genuinely interior (`reattest_weight_tie`).

    Statable from no single conjunct alone (non-hollow). ⚠ HONEST GRADE: (A)–(E) THEOREM-EXACT (exact
    identities & inequalities of the derived ℝ `Cut`, foundations-only, no error bar). The re-attestation
    is a PROVED theorem: the frame-audited composition is COMPLETE — every banked contribution is
    included or proven exempt/zero, so the residual endpoint defect is NOT in the frame's two surviving
    structural claims (the recombination architecture + the dressing scope). The ⛔ OWNER
    ≥15-SIGNIFICANT-DIGIT FLOOR is the ROUTE-NOT-YET-FOUND handed to the ONE directed narrowing successor
    (instrument precision of the derived inputs; deeper partial sums of banked series, no new axiom, no
    fit). The measured `1/α(0) ≈ 137.036` is REMOVABLE PROSE ONLY; no empirical number in any proof; the
    composition is attested by the fold's own architecture + scope + census theorems, NOT by where a
    measured value sits. Explicit NO-FIT. -/
theorem frameAuditedCompositionReattestation_landing (rb rc rl d : Cut) (rs : List Cut) :
    -- (A) the F2 dressing scope holds over the frame-audited object
    (recomposedEndpointDressed = 2425 / 18 + (3 / 2) * kappaLeading * cInner)
    ∧ (recomposedEndpointDressed - (3 / 2) * kappaLeading * cInner = 2425 / 18)
    ∧ ((2425 : Cut) / 18 = 7179 / 54 + 16 / 9)
    -- (B) the F1 recombination architecture is the forced ARCH-B
    ∧ (recombArch 0 [(chargeTraceDepth, topBandRatio)]
        - recombArch projectedS [(chargeTraceDepth, topBandRatio)]
          = bandScreen projectedS topBandRatio)
    ∧ (projectedS ≠ 0)
    ∧ (recombArch projectedS [(chargeTraceDepth, topBandRatio)]
        < recombArch 0 [(chargeTraceDepth, topBandRatio)])
    -- (C) the F1↔F2 tie: the weight is channel-independent
    ∧ (depthWeight = 1 / 3 ∧ depthWeight ≠ 0 ∧ depthWeight ≠ 1)
    -- (D) the bare census-completeness + the bare↔dressed weld
    ∧ ((recomposedEndpoint
          = 7179 / 54 + bandScreen leptonCensus (mass0 / mass1)
            + (16 / 9
              + kappaLeading * ((7 / 3) * cutLog mbRatioDerived + (11 / 3) * cutLog (bMass 0)
                - (5 / 3) * cutLog (bMass 2))))
        ∧ chargeTraceDepth = ((freezeoutStaircaseCensus : ℚ) : Cut) + leptonCensus
        ∧ recomposedInvAlphaEM [(chargeTraceDepth, topBandRatio)] = 6923 / 54
        ∧ recomposedEndpoint - (leptonTailScreen + freezeoutStaircaseSummand) = 7179 / 54
        ∧ (chargeTraceDepth = ((ladderChargeTrace (Module.finrank ℚ Uhol) : ℚ) : Cut)
            ∧ Module.finrank ℚ Uhol = 3)
        ∧ (invAlphaZero (completeFreezeoutList rb rc rl d ++ rs.map (fun r => (frozenCensus, r)))
            = invAlphaZero (completeFreezeoutList rb rc rl d)))
    ∧ (recomposedEndpointDressed = recomposedEndpoint + (1 / 2) * kappaLeading * cInner)
    -- (E) teeth
    ∧ ((2425 / 18 : Cut) ≠ 7179 / 54)
    ∧ (PerChannelRecomb.recomposedEndpoint < recomposedEndpointDressed)
    ∧ ((0 : Cut) < (1 / 2) * kappaLeading * cInner) :=
  ⟨reattest_scopeC_closed,
   reattest_base_exempt,
   reattest_base_is_resummed,
   reattest_architecture_shift,
   reattest_arch_forced,
   reattest_arch_direction,
   reattest_weight_tie,
   reattest_bare_census_complete rb rc rl d rs,
   reattest_dressed_weld,
   census_gem_real,
   dressing_real,
   increment_pos⟩

end

end Phys.Algebra.FrameAuditedCompositionReattestation
