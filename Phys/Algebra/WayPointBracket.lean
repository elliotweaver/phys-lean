/-
  # D6 — THE WAY-POINTS EVALUATED FREE — arc-D THE DIGITS (docs/SEED_DIGITS.md §D6)
  =====================================================================================

  Directed successor of D5-next-12 (`NarrowedBracket12`). NO-DEFERRAL arc. The narrowing campaign
  (D5) crossed the north star: the assembled `1/α(0)` bracket
  `[12007329939722/176714586765, 32019546505738/471238898037]` (width `≈ 3.50×10⁻¹¹`) is now BELOW
  the measured relative uncertainty (`≈ 2×10⁻¹⁰`) of the derived zero-momentum inverse fine-structure
  coupling. D6 EVALUATES THE WAY-POINTS `α(M_Z)` and `sin²θ_W(M_Z)` from that SAME certified tower —
  landing FREE, never targeted — as certified two-sided rational readings, and proves the free
  instrument-check (the SAME pipeline reproduces the banked EXACT way-points EXACTLY, width `0`).

  ## THE THEORY-NATIVE READING (what the fold says the field cannot — seed §D6)

  In standard electroweak theory, `α(M_Z)` and `sin²θ_W(M_Z)` are the MEASURED anchors from which the
  running is integrated. The fold DISSOLVES their special status. Z7 (`WayPointReadings`) proved that
  the `M_Z` scale sits BETWEEN the electroweak edge `v` and the confinement edge `Λ`, so it is simply
  an INTERMEDIATE band edge inside the banked confinement band: refining the confinement band at `M_Z`
  splits it into two consecutive sub-bands `(chargeTraceDepth, vz)` (`v→M_Z`) and
  `(chargeTraceDepth, zL)` (`M_Z→Λ`) whose ratios MULTIPLY (`vz·zL = confBandRatio`, the FORCED
  product constraint `mz_edge_product`), so the two sub-band screenings recombine EXACTLY into the
  single confinement-band screening (`mzWaypoint_refines_confBand`). The reading at `M_Z` decomposes
  off the EXACT electroweak way-point (`invAlphaMZ_off_ewpoint`: `invAlphaMZ vz = 1582/27 +
  bandScreen chargeTraceDepth vz`).

  So `1/α(M_Z) = invAlphaMZ vz` reads as a certified two-sided rational bracket EVALUATED FREE,
  squeezed between:
    • the EXACT electroweak way-point `1582/27` BELOW (a partial-sum floor — `invAlphaMZ_ge_ewpoint`;
      NO `v/M_Z` numeral needed for the floor), and
    • the D5-next-12 SHARPEST derived endpoint ceiling `32019546505738/471238898037` ABOVE (chaining
      the banked `endpoint_gt_invAlphaMZ` — the `M_Z` reading is a genuine partial sum strictly below
      the full endpoint — with `NarrowedBracket12.invAlphaZero_narrowed12_bracket.2`, the crossed-
      precision endpoint bracket).
  The reading LANDS FREE: the exact way-point floor and the derived endpoint ceiling squeeze it, with
  NO posited `v/M_Z` scale. THE ONE CAUSE: `1582/27` is simultaneously (a) the exact `M_Z`-reading
  floor, (b) a free width-`0` partial sum reproduced by the SAME pipeline, and (c) the anchor the
  reading decomposes off — one banked value, three roles.

  `sin²θ_W(M_Z)` is read at the SAME band edge as the SAME gather self-overlap: at the matter isospin
  depth it is STATIONARY at the EXACT `3/8` (`sinSqMZ_stationary`, a degenerate certified bracket of
  width `0`), and the non-abelian self-coupling reduces the depth, running it STRICTLY BELOW `3/8`
  (`sinSqMZ_below`). Both observables at `M_Z` are scale-resolved evaluations of the ONE gather
  self-overlap, read at the same edge.

  ## THE FREE INSTRUMENT-CHECK (the overconstraint at numeral grade — seed §D6)

  The SAME D4 pipeline that evaluates `1/α(0)` reproduces the banked EXACT way-points EXACTLY (width
  `0`), aimed at them: the electroweak way-point `1582/27` (`pipeline_ew_waypoint`), the confinement
  way-point `190/3` (`pipeline_conf_waypoint`), the full assembled pipeline collapsing to `190/3` at
  zero census (`assembled_at_zero_census` — the tail never MOVES the way-point), and the dressed
  confinement way-point `74` at the derived weight `1/3` (`pipeline_conf_waypoint_dressed`). Nothing
  is adjusted to make a reading land: the way-points falling free is the assembly's proof of closure.

  ## GRADE (honest, per piece — seed §D6 / SOUL "route COMPLETE vs route NOT-YET-FOUND")

  CERTIFIED NOW (closed reals of `Cut`, foundations-only, no error bar): the free instrument-check
  (all four exact way-points reproduced at width `0`); the `α(M_Z)` certified two-sided rational
  reading `1582/27 ≤ invAlphaMZ vz < 32019546505738/471238898037` (`alphaMZ_bracket`), quantified
  over the FORCED product constraint `vz·zL = confBandRatio` with `1 ≤ vz, zL`; the exact Weinberg
  reading `3/8` (`weinberg_free`), strictly below `3/8` at the reduced depth (`weinberg_below`), off
  the wrong `1/4` (`weinberg_ne_wrong`); the teeth.

  ROUTE-NOT-YET-FOUND (the ℝ-descent, NEVER asserted / NEVER fitted — exactly the banked X6/X7/Z6/Z7
  status): the single closed rational NUMERAL for the `v/M_Z` sub-band ratio `vz` (needs the `M_Z`
  scale as a closed real of `Cut`) that would collapse the `α(M_Z)` reading's lower gap to below
  measured precision. The theorems here quantify over ALL `vz, zL` satisfying the forced constraint,
  so the STRUCTURE (the free landing between the exact way-point and the certified endpoint) is exact
  and the numeral is the only open sub-fact — handed onward (arc M scales; owner order D→M→C→GR). The
  measured `α(M_Z) ≈ 127.955` and `sin²θ_W(M_Z) ≈ 0.23122` are REMOVABLE PROSE / costume ONLY (G2),
  never a proof step, never a fit target, never a chooser of a bracket or edge; the `M_Z` reading is a
  derived partial sum squeezed between the octonion ceiling and the derived endpoint, never a claim to
  the measured value. Truncating at measured precision is FORBIDDEN — the point is MORE digits.

  ## PHYSICS-WORDS-REMOVABLE (STANDARD §2; seed G5)

  Delete "coupling / running / screening / band / edge / electroweak / M_Z / confinement / lepton /
  octonion / weak mixing angle / fine-structure": what remains, over the derived ℝ `Cut`, is that the
  finite-list sum `invAlphaZero` reproduces `1582/27`, `190/3`, and (dressed at `1/3`) `74` exactly;
  that the two-element partial sum `invAlphaMZ vz` is `≥ 1582/27` for `1 ≤ vz` and is STRICTLY below
  the full `invAlphaZero (completeBandList chargeTraceDepth)`, which is itself `≤ 32019546505738/
  471238898037` (the banked D5-next-12 rational upper bound), so `invAlphaMZ vz` lies in the certified
  two-sided rational bracket `[1582/27, 32019546505738/471238898037)`; and that `a₂/(a₂+a_Y) = 3/8` at
  the proportional depth and `< 3/8` for a reduced depth. Pure real analysis on the banked
  `invAlphaZero`/`invAlphaMZ`/`completeBandList`/`derivedBandList`/`dressedInvAlphaZero`/`depthWeight`/
  `sinSqRunExact`/`confBandRatio` over `Cut`. No theorem STATEMENT needs a physics word.

  ## NOT FREE-FLOATING (seed / SOUL free-floating rail)

  Every production TYPE mentions the banked derived objects: `invAlphaMZ`/`mzAbove`/`mzBelow`/
  `invAlphaMZ_ge_ewpoint`/`endpoint_gt_invAlphaMZ`/`invAlphaMZ_gt_42`/`sinSqMZ_stationary`/
  `sinSqMZ_below`/`sinSqMZ_ne_wrong` (Z7 `WayPointReadings`), `invAlphaZero`/`completeBandList`/
  `derivedBandList`/`topBandRatio`/`chargeTraceDepth` (X6/Z4), `dressedInvAlphaZero`/`depthWeight`
  (Z5/D3), `invAlphaZero_narrowed12_bracket` (D5-next-12), `sinSqRunExact`/`isoDepth`/`invAlpha2`/
  `kappaLeading` (X2), and `Cut` (derived ℝ). The `α(M_Z)` reading is literally the banked Z7
  two-element partial sum pinned between two explicit rationals of `Cut`. Not generic, not
  carrier-agnostic.

  ## SEED HARD GUARDS (G1–G8)

  - G1 NO ASSERTED VALUE — the way-points FALL OUT of the banked D4 pipeline; the `α(M_Z)` bracket
    FALLS OUT of `invAlphaMZ_ge_ewpoint` + `endpoint_gt_invAlphaMZ` + the banked D5-next-12 endpoint
    bracket; the `3/8` FALLS OUT of `sinSqMZ_stationary`; no `M_Z` coupling / mixing value is premised.
  - G2 NO EMPIRICAL NUMBER — no `127.955`, no `0.23122`, no `137.036`, no measured `α(M_Z)` /
    `sin²θ_W` / uncertainty in any statement or proof; no bracket, bound, or edge chosen BECAUSE of
    where a measured value sits; the measured constants are this prose / the costume only, fully
    removable. Explicit NO-FIT.
  - G3 EXACT / CERTIFIED — the way-points and `3/8` are EXACT rationals of `Cut`; the `α(M_Z)` reading
    is a certified two-sided rational bracket of a closed real of `Cut`; the only open sub-fact is the
    `v/M_Z` numeral (the ℝ-descent), quantified over, never asserted.
  - G4 DERIVED, NOT POSITED — the tower is the banked Z6 `completeBandList`; the endpoint ceiling is
    the banked D5-next-12 rational bound; the `M_Z` reading is a banked Z7 partial sum; the exact
    way-points are the banked D4 assemblies.
  - G5 PHYSICS-WORDS-REMOVABLE — see above.
  - G6 FOUNDATIONS-ONLY — `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`; sorry-free, zero
    posited axioms, no compiled-decision bypass (NO kernel-eval decision procedures), no heartbeat
    inflation, NO `foldRetention` (import guard: only banked Z7 `WayPointReadings` / D5-next-12
    `NarrowedBracket12` / D4 `AssembledBracket` and their banked transitive deps, never `MassGapOne`
    / `OneAxiom`). Ground field the DERIVED ℝ `Cut`; ⚠ NO Mathlib `Real` / `Complex` as content —
    Mathlib is MACHINERY only; NO floats anywhere.
  - G7 ONE LAW — the derivation did not fight; the banked Z7 partial-sum squeeze
    (`invAlphaMZ_ge_ewpoint` / `endpoint_gt_invAlphaMZ`) chained with the D5-next-12 endpoint bracket
    and the banked `sinSqMZ_stationary` close every step; the one residual (the `v/M_Z` numeral) that
    would collapse the reading's width is NAMED as the ℝ-descent sub-fact, NOT asserted, NOT fitted,
    NOT truncated-and-declared.
  - G8 TEETH (W8) — `waypoint_bracket_strict` (the `α(M_Z)` bracket is genuinely two-sided,
    `1582/27 < 32019546505738/471238898037`, not a collapsed point); `alphaMZ_gt_42` (the `M_Z`
    reading genuinely screens above the octonion ceiling `42`, it is NOT pinned there);
    `weinberg_ne_wrong` (the mixing reading is `3/8`, NOT the wrong `1/4`).

  DEPENDENCIES (all banked, foundations-only): Z7 `WayPointReadings` (`invAlphaMZ`/`mzAbove`/
  `invAlphaMZ_ge_ewpoint`/`endpoint_gt_invAlphaMZ`/`invAlphaMZ_gt_42`/`sinSqMZ_stationary`/
  `sinSqMZ_below`/`sinSqMZ_ne_wrong`); D5-next-12 `NarrowedBracket12`
  (`invAlphaZero_narrowed12_bracket`); D4 `AssembledBracket` (`pipeline_ew_waypoint`/
  `pipeline_conf_waypoint`/`assembled_at_zero_census`/`pipeline_conf_waypoint_dressed`); X6/Z4/Z5/D3
  (`invAlphaZero`/`completeBandList`/`derivedBandList`/`topBandRatio`/`chargeTraceDepth`/
  `dressedInvAlphaZero`/`depthWeight`/`confBandRatio`); X2 `FineStructureRunningExact`
  (`sinSqRunExact`/`isoDepth`/`invAlpha2`/`kappaLeading`); the derived `Cut`; standard Mathlib
  `linarith`/`norm_num` MACHINERY on the DERIVED objects (STANDARD §3). NO posited coupling / mixing
  value as content, NO Mathlib ℝ as content, NO bridge.
-/
import Phys.Algebra.WayPointReadings
import Phys.Algebra.NarrowedBracket12
import Phys.Algebra.AssembledBracket
import Mathlib.Tactic

namespace Phys.Algebra.WayPointBracket

open Phys.Algebra
open Phys.Algebra.BandEdgeList
open Phys.Algebra.EndpointAssembly
open Phys.Algebra.DepthTowerDescent
open Phys.Algebra.DepthWeight
open Phys.Foundation Phys.Foundation.ContinuumQ

noncomputable section

/-! ## (1) THE FREE INSTRUMENT-CHECK — the pipeline reproduces the exact way-points (width 0). -/

/-- THE ELECTROWEAK WAY-POINT REPRODUCED FREE (width 0): the SAME pipeline that evaluates `1/α(0)`,
    aimed at the top band alone, gives `invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582/27`
    (banked D4 `pipeline_ew_waypoint`). The exact electroweak way-point, reproduced by the same map,
    no error bar. -/
theorem ew_waypoint_free : invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582 / 27 :=
  AssembledBracket.pipeline_ew_waypoint

/-- THE CONFINEMENT WAY-POINT REPRODUCED FREE (width 0): the pipeline aimed at the two high bands
    gives `invAlphaZero derivedBandList = 190/3` (banked D4 `pipeline_conf_waypoint`). -/
theorem conf_waypoint_free : invAlphaZero derivedBandList = 190 / 3 :=
  AssembledBracket.pipeline_conf_waypoint

/-- THE FULL ASSEMBLED PIPELINE COLLAPSES TO THE CONFINEMENT WAY-POINT AT ZERO CENSUS (width 0):
    `invAlphaZero (completeBandList 0) = 190/3` (banked D4 `assembled_at_zero_census`). When the
    census vanishes the tail screening drops out and the endpoint reproduces `190/3` EXACTLY — the
    way-point is a partial sum, never MOVED by the tail. -/
theorem conf_waypoint_free_zero : invAlphaZero (completeBandList 0) = 190 / 3 :=
  AssembledBracket.assembled_at_zero_census

/-- THE DRESSED CONFINEMENT WAY-POINT REPRODUCED FREE AT THE DERIVED WEIGHT `1/3` (width 0):
    `dressedInvAlphaZero depthWeight derivedBandList = 74` (banked D4 `pipeline_conf_waypoint_dressed`).
    The same dressed pipeline reproduces the banked running-of-the-running way-point at the derived
    weight, no error bar. -/
theorem conf_waypoint_free_dressed :
    dressedInvAlphaZero depthWeight derivedBandList = 74 :=
  AssembledBracket.pipeline_conf_waypoint_dressed

/-! ## (2) α(M_Z) — THE CERTIFIED TWO-SIDED RATIONAL READING, EVALUATED FREE. -/

/-- ★ THE `α(M_Z)` LOWER READING (free, exact way-point floor): for a `v→M_Z` sub-band that screens
    (`1 ≤ vz`), the `M_Z` reading is at least the EXACT electroweak way-point `1582/27` (banked Z7
    `invAlphaMZ_ge_ewpoint`). NO `v/M_Z` numeral is needed for the floor — it is the exact way-point
    the reading decomposes off. -/
theorem alphaMZ_lo (vz : Cut) (hvz : 1 ≤ vz) :
    (1582 / 27 : Cut) ≤ WayPointReadings.invAlphaMZ vz :=
  WayPointReadings.invAlphaMZ_ge_ewpoint vz hvz

/-- ★★ THE `α(M_Z)` UPPER READING (free, the D5-next-12 derived endpoint ceiling): under the FORCED
    product constraint `vz·zL = confBandRatio` (`1 ≤ vz, zL`), the `M_Z` reading is STRICTLY below the
    banked D5-next-12 sharpest endpoint upper bound `32019546505738/471238898037`. Chains the banked
    Z7 `endpoint_gt_invAlphaMZ` (the `M_Z` reading is a genuine partial sum strictly below the full
    endpoint) with `NarrowedBracket12.invAlphaZero_narrowed12_bracket.2` (the crossed-precision
    endpoint bracket). The reading lands FREE — squeezed by the derived endpoint, no posited
    `v/M_Z`. -/
theorem alphaMZ_hi (vz zL : Cut) (hvz : 1 ≤ vz) (hzL : 1 ≤ zL)
    (hprod : vz * zL = confBandRatio) :
    WayPointReadings.invAlphaMZ vz < 32019546505738 / 471238898037 := by
  have h1 : WayPointReadings.invAlphaMZ vz < invAlphaZero (completeBandList chargeTraceDepth) :=
    WayPointReadings.endpoint_gt_invAlphaMZ vz zL (lt_of_lt_of_le zero_lt_one hvz) hzL hprod
  have h2 : invAlphaZero (completeBandList chargeTraceDepth)
      ≤ 32019546505738 / 471238898037 :=
    NarrowedBracket12.invAlphaZero_narrowed12_bracket.2
  linarith

/-- ★★★ THE `α(M_Z)` CERTIFIED TWO-SIDED RATIONAL READING (evaluated FREE): under the forced product
    constraint `vz·zL = confBandRatio` (`1 ≤ vz, zL`),
        1582/27 ≤ invAlphaMZ vz < 32019546505738/471238898037.
    The `M_Z` reading is squeezed FREE between the EXACT electroweak way-point floor `1582/27` and the
    banked D5-next-12 derived endpoint ceiling — a certified two-sided rational bracket of the closed
    real `1/α(M_Z)`, with NO posited `v/M_Z` scale. Landing free, never targeted. -/
theorem alphaMZ_bracket (vz zL : Cut) (hvz : 1 ≤ vz) (hzL : 1 ≤ zL)
    (hprod : vz * zL = confBandRatio) :
    (1582 / 27 : Cut) ≤ WayPointReadings.invAlphaMZ vz
      ∧ WayPointReadings.invAlphaMZ vz < 32019546505738 / 471238898037 :=
  ⟨alphaMZ_lo vz hvz, alphaMZ_hi vz zL hvz hzL hprod⟩

/-! ## (3) sin²θ_W(M_Z) — EVALUATED FREE, exact 3/8 (the SAME gather self-overlap, other observable). -/

/-- ★★ THE WEINBERG READING EVALUATED FREE — exact `3/8` (a degenerate certified bracket, width 0):
    at the matter isospin depth, `sin²θ_W(M_Z) = 3/8` at the `M_Z` band edge (banked Z7
    `sinSqMZ_stationary`). Stated as a two-sided bracket collapsed to a point — the exact reading. -/
theorem weinberg_free (M mz : Cut) (h2 : invAlpha2 isoDepth kappaLeading M mz ≠ 0) :
    (3 / 8 : Cut) ≤ sinSqRunExact isoDepth M mz ∧ sinSqRunExact isoDepth M mz ≤ 3 / 8 := by
  have h := WayPointReadings.sinSqMZ_stationary M mz h2
  exact ⟨by rw [h], by rw [h]⟩

/-- ★★ THE WEINBERG READING DEPARTS DOWN AT THE REDUCED DEPTH: with the non-abelian-reduced isospin
    depth `0 < d2 < isoDepth`, `sin²θ_W(M_Z)` runs STRICTLY BELOW `3/8` (banked Z7 `sinSqMZ_below`).
    The mixing is scale-resolved: it is not pinned at `3/8`. -/
theorem weinberg_below (d2 M mz : Cut) (hd2 : 0 < d2) (hd2lt : d2 < isoDepth)
    (hmz : 0 < mz) (hmzM : mz < M) :
    sinSqRunExact d2 M mz < 3 / 8 :=
  WayPointReadings.sinSqMZ_below d2 M mz hd2 hd2lt hmz hmzM

/-! ## (4) W8 NON-VACUITY WITH TEETH. -/

/-- ★ W8 — THE `α(M_Z)` BRACKET IS GENUINELY TWO-SIDED (not a collapsed point): the exact way-point
    floor is STRICTLY below the derived endpoint ceiling, `1582/27 < 32019546505738/471238898037`. A
    real bracket, not a degenerate width-`0` reading. -/
theorem waypoint_bracket_strict :
    (1582 / 27 : Cut) < 32019546505738 / 471238898037 := by norm_num

/-- ★ W8 — THE `α(M_Z)` READING GENUINELY SCREENS ABOVE THE OCTONION CEILING: for a screening
    `v→M_Z` sub-band (`1 ≤ vz`), the reading STRICTLY EXCEEDS `42` (banked Z7 `invAlphaMZ_gt_42`). A
    non-screening (constant) coupling would stay pinned at `42`. -/
theorem alphaMZ_gt_42 (vz : Cut) (hvz : 1 ≤ vz) : (42 : Cut) < WayPointReadings.invAlphaMZ vz :=
  WayPointReadings.invAlphaMZ_gt_42 vz hvz

/-- ★ W8 — THE WEINBERG READING IS `3/8`, NOT THE WRONG `1/4` (banked Z7 `sinSqMZ_ne_wrong`). -/
theorem weinberg_ne_wrong (M mz : Cut) (h2 : invAlpha2 isoDepth kappaLeading M mz ≠ 0) :
    sinSqRunExact isoDepth M mz ≠ 1 / 4 :=
  WayPointReadings.sinSqMZ_ne_wrong M mz h2

/-! ## (5) THE D6 CAPSTONE — welded, non-hollow. -/

/-- ★★★ THE WAY-POINTS EVALUATED FREE (arc-D D6, welded landing). `α(M_Z)` and `sin²θ_W(M_Z)` read
    from the SAME certified tower as certified two-sided rational readings, landing FREE, never
    targeted, PLUS the free instrument-check:

    (1) THE FREE INSTRUMENT-CHECK: the SAME pipeline reproduces the banked exact way-points EXACTLY
        (width 0) — `1582/27` (`ew_waypoint_free`), `190/3` (`conf_waypoint_free`,
        `conf_waypoint_free_zero`), dressed `74` (`conf_waypoint_free_dressed`);
    (2) ★ THE `α(M_Z)` CERTIFIED TWO-SIDED RATIONAL READING: under the FORCED product constraint,
        `1582/27 ≤ invAlphaMZ vz < 32019546505738/471238898037` (`alphaMZ_bracket`) — squeezed FREE
        between the EXACT electroweak way-point floor and the banked D5-next-12 derived endpoint
        ceiling, no posited `v/M_Z` scale;
    (3) THE WEINBERG READING: exact `3/8` at the matter depth (`weinberg_free`), STRICTLY below `3/8`
        at the non-abelian-reduced depth (`weinberg_below`);
    (4) TEETH: the `α(M_Z)` bracket is genuinely two-sided (`waypoint_bracket_strict`), the reading
        genuinely screens above `42` (`alphaMZ_gt_42`), the Weinberg reading is `3/8` not `1/4`
        (`weinberg_ne_wrong`).

    Statable from no single conjunct alone (non-hollow). ⚠ HONEST GRADE: (1)–(4) CERTIFIED now
    (closed reals of `Cut`, quantified over the forced product constraint, no error bar). ROUTE-NOT-
    YET-FOUND (the ℝ-descent, NEVER asserted / NEVER fitted, exactly the banked X6/X7/Z6/Z7 status):
    the single closed rational NUMERAL for the `v/M_Z` sub-band ratio `vz` (needs the `M_Z` scale as a
    closed real of `Cut`) that would collapse the `α(M_Z)` reading's lower gap — handed onward. The
    measured `α(M_Z) ≈ 127.955` and `sin²θ_W(M_Z) ≈ 0.23122` are REMOVABLE PROSE ONLY; the `M_Z`
    reading is a derived partial sum squeezed between the octonion ceiling and the derived endpoint,
    never a claim to the measured value, never a fit. Truncating at measured precision FORBIDDEN. No
    empirical number in any proof. -/
theorem wayPointBracket_landing (vz zL M mz : Cut)
    (hvz : 1 ≤ vz) (hzL : 1 ≤ zL) (hprod : vz * zL = confBandRatio)
    (hmz : 0 < mz) (hmzM : mz < M)
    (h2 : invAlpha2 isoDepth kappaLeading M mz ≠ 0) :
    -- (1) the free instrument-check (exact way-points reproduced, width 0)
    (invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582 / 27)
    ∧ invAlphaZero derivedBandList = 190 / 3
    ∧ invAlphaZero (completeBandList 0) = 190 / 3
    ∧ dressedInvAlphaZero depthWeight derivedBandList = 74
    -- (2) the α(M_Z) certified two-sided rational reading, evaluated free
    ∧ ((1582 / 27 : Cut) ≤ WayPointReadings.invAlphaMZ vz
        ∧ WayPointReadings.invAlphaMZ vz < 32019546505738 / 471238898037)
    -- (3) the Weinberg reading: exact 3/8, strictly below 3/8 at the reduced depth
    ∧ ((3 / 8 : Cut) ≤ sinSqRunExact isoDepth M mz ∧ sinSqRunExact isoDepth M mz ≤ 3 / 8)
    ∧ (∀ d2 : Cut, 0 < d2 → d2 < isoDepth → sinSqRunExact d2 M mz < 3 / 8)
    -- (4) teeth
    ∧ (1582 / 27 : Cut) < 32019546505738 / 471238898037
    ∧ (42 : Cut) < WayPointReadings.invAlphaMZ vz
    ∧ sinSqRunExact isoDepth M mz ≠ 1 / 4 :=
  ⟨ew_waypoint_free,
   conf_waypoint_free,
   conf_waypoint_free_zero,
   conf_waypoint_free_dressed,
   alphaMZ_bracket vz zL hvz hzL hprod,
   weinberg_free M mz h2,
   fun d2 hd2 hd2lt => weinberg_below d2 M mz hd2 hd2lt hmz hmzM,
   waypoint_bracket_strict,
   alphaMZ_gt_42 vz hvz,
   weinberg_ne_wrong M mz h2⟩

end

end Phys.Algebra.WayPointBracket
