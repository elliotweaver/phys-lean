/-
  # N365 — arc-E E9 (candidate / CONSOLIDATION): the arc-E RETURN-SURFACE (`azimuthalTurn`)
  #         normalization family CLOSED + the pure-integer gauge ↔ gravity-thermodynamics
  #         CROSS-LOCKS + the banked dark-matter representative — over the DERIVED ℝ `Cut`.
  =============================================================================================

  ⭐ FOURTH OWNER-AUTHORIZED SEED — GR / BLACK HOLES / DEEP COSMOLOGY, TARGET E9
  (`docs/SEED_GR_BLACKHOLE_COSMOLOGY.md`, §E9). The directed single successor of N364 (E8). E9's
  stated target has TWO halves; a FRESH-worker SELECT (kanban_comment 268, QUIET-DRIFT + the
  ANTI-BULLSHIT LAW in full, W9 MEASURE-FIRST) MEASURED both against the actual bank:

    (a) DARK MATTER = STERILE ν_R is **ALREADY BANKED**: `sterile_nu_dark_matter` (N337 — heavy-sterile
        ∧ light-massless ∧ stable as ONE theorem) and universal darkness `dirac_others_self_blind`
        (N336 — every structured coupling annihilates the self-direction). Re-deriving = duplication.
    (b) The CONSOLIDATION is the remaining beat — but NOT a decorative ∧-bundle of the four arc-E
        capstones (the empty-∧ trap N338 explicitly named). The theory-native forced content is the
        ONE never-banked structural fact that unifies the arc's gravitational-thermodynamic sector.

  ## WHAT THIS NODE BANKS (the never-banked structural fact + the banked dark-matter representative)

  THE FOLD'S RETURN SURFACE (Engine A). The fold looks back and cannot contain itself; the look-back
  is a RETURN that closes. The DERIVED closure is the Born turn `azimuthalTurn = 2·cutPi` (N328). Every
  gravitational/thermodynamic normalization is a flux/period THROUGH that ONE return surface, so each
  equals `azimuthalTurn · (pure count)`. N359 opened this family for the gauge / gravity / EH-action
  sector (`gravAndGauge_share_azimuthalTurn`: α*'s `84π = azimuthalTurn·42`, gravity's `8π =
  azimuthalTurn·4`), but the BLACK-HOLE THERMODYNAMIC members were never in it. This node CLOSES the
  family and locks it, pure-integer, to the gauge sector:

  (1) THE RETURN-SURFACE FAMILY MEMBERS (each normalization homed to `azimuthalTurn · count`):
        `gaussSphere_azimuthal    : gaussSphereSolidAngle    = azimuthalTurn * 2`   (the flux itself)
        `ehAction_azimuthal       : ehActionInverseCoeff     = azimuthalTurn * 8`   (the 16π action)
        `bhEntropy_azimuthal      : bhEntropy M              = azimuthalTurn * 2 * M^2`
        `hawkingPeriod_sch_azimuthal : hawkingPeriod κsg     = azimuthalTurn * 4 * M`  (κsg=(4M)⁻¹)
      The `bhEntropy` and `hawkingPeriod` members are GENUINELY NEW: nobody composed
      `bhEntropy = gravCouplingCoeff·M²/2` (N361) with `gravCouplingCoeff = azimuthalTurn·4` (N359).

  (2) THE `azimuthalTurn`-FREE PURE-INTEGER CROSS-LOCKS (the turn ELIMINATED; only integers relate the
      gauge sector to the gravitational-thermodynamic sector):
        `bhEntropy_gauge_lock : bhEntropy M * 21 = criticalPhaseNumerator * M^2`
            — black-hole entropy and the fine-structure channel count `42` locked by `21 = 42/2`;
              NO `cutPi` survives in the coefficients (both sides are `azimuthalTurn·2·42·M²`).
        `hawkingPeriod_gauge_lock : hawkingPeriod κsg * 42 = criticalPhaseNumerator * 4 * M` (κsg=(4M)⁻¹)
            — the Schwarzschild Hawking period locked to the gauge channel count by pure integers.

  (3) THE E9 CONSOLIDATION CAPSTONE `cosmo_arc_return_surface_consolidated` bundles the NEW Engine-A
      family + the pure-integer locks WITH the banked Engine-B (self-blindness / dark-matter)
      representative, exhibiting arc E as ONE fold-causal structure with TWO faces:
        • the RETURN (`azimuthalTurn`) — normalizes the flux, the action, the entropy, the period;
        • the MISS (self-blindness `othersProj·selfDir = 0`) — the sterile dark direction:
          heavy-sterile `heavyMajorana_selfDir_eigen` (N337) + universally dark
          `dirac_others_self_blind` (N336).

  where `azimuthalTurn`/`gaussSphereSolidAngle`/`gaussSphereSolidAngle_eq`/`cutPi_pos` (N328),
  `gravCouplingCoeff`/`gravCouplingCoeff_azimuthal`/`ehActionInverseCoeff_eq`/`criticalPhaseNumerator`/
  `numerator_azimuthal` (N359 / scale-tower), `bhEntropy`/`hawkingPeriod`/`hawkingPeriod_schwarzschild`
  (N360/N361), `heavyMajorana`/`heavyMajorana_selfDir_eigen`/`selfDir`/`othersProj`/
  `dirac_others_self_blind` (N336/N337) are ALL BANKED; `Cut` is the derived ℝ (N33–N37). NO
  `Real.exp`/`Real.pi`, NO Mathlib number-system content, NO empirical Ω_DM / Λ / T_H value premised.

  ## THE THEORY-NATIVE READING (why this is DERIVED, not an ∧, and where the moat is)

  ONE CAUSE, MANY TERMINATIONS — through TWO faces of the ONE fold. The RETURN face: the SAME derived
  Born turn `azimuthalTurn = 2·cutPi` (the fold's return surface, N328) that normalized `α* = 1/42`
  (`84π = azimuthalTurn·42`, N329), gravity (`8π = azimuthalTurn·4`, N359), the EH action (`16π`), the
  Hawking period (`κsg·β = azimuthalTurn`, N360), and — banked here — the black-hole entropy
  (`azimuthalTurn·2·M²`) is the ONE constant carrying every gravitational-thermodynamic normalization;
  the counts (2, 4, 8, 42) are the only content. The MISS face: the SAME self-blindness `othersProj ·
  selfDir = 0` that capped the cascade at three (N5), made the family blind to generation content
  (N290), and gave the massless / stable / sterile dark direction (N336/N337). The pure-integer lock
  `bhEntropy M · 21 = criticalPhaseNumerator · M²` is the moat: what the field computes with three
  disconnected matching arguments (the Newtonian limit, Euclidean regularity, the first law +
  microstate counting), the theory reads as ONE derived turn scaled by integers — and the turn
  CANCELS, leaving black-hole entropy locked to the fine-structure channel count by the pure `21`.

  ## PHYSICS-WORDS-REMOVABLE (STANDARD §2)

  Delete "gravity / entropy / Hawking / horizon / black-hole / dark-matter / sterile / neutrino /
  cosmology". What survives over the derived `Cut`: `gaussSphereSolidAngle = azimuthalTurn·2`,
  `ehActionInverseCoeff = azimuthalTurn·8`, `bhEntropy M = azimuthalTurn·2·M²`, `bhEntropy M · 21 =
  criticalPhaseNumerator·M²`, `bhEntropy M ≠ azimuthalTurn·3·M²` (for `M ≠ 0`), together with the
  banked matrix facts `(heavyMajorana M).mulVec selfDir = M • selfDir` and `(othersProj·D₀)ᵀ.mulVec
  selfDir = 0`. Pure real-arithmetic (`Cut`) + real-matrix analysis. No physics name does any logical
  work.

  ## HONEST GRADE (mandatory flags — DO NOT over-claim)

    • THEOREM-GRADE (forced math over the derived `Cut`): the return-surface family members
      `gaussSphere_azimuthal`/`ehAction_azimuthal`/`bhEntropy_azimuthal`/`hawkingPeriod_sch_azimuthal`,
      the pure-integer cross-locks `bhEntropy_gauge_lock`/`hawkingPeriod_gauge_lock`, and the W8
      non-vacuity `bhEntropy_azimuthal_ne_three`/`bhEntropy_gauge_lock_ne_twenty`. The two
      `hawkingPeriod` members carry the SAME EXPLICIT metric-specific hypothesis `κsg = (4M)⁻¹` as
      banked N360 (a hypothesis, NEVER asserted; the full surface-gravity build is the childed N360
      remainder), FLAGGED leading-order.
    • BANKED-CITED (arc-C candidate-class, NEVER re-asserted): the dark-matter representative conjuncts
      `heavyMajorana_selfDir_eigen` (N337) + `dirac_others_self_blind` (N336) enter the capstone by
      CITING the banked theorems; the physical dark-matter reading is arc-C candidate-grade.
    • MECHANISM / NAMED-IDENTIFICATION-GRADE (flagged prose, NEVER a theorem): "arc E is ONE
      fold-causal structure with two faces (the RETURN `azimuthalTurn` and the MISS self-blindness)";
      "the counts 2,4,8,42 are the fold's derived channel counts". Like N362's `min`, N360's
      WHY-thermal, the T8 mixing caveat.
    • ROUTE-NOT-YET-FOUND (carried from N364, NAMED not dropped): the FULL slow-roll derivation of
      `n_s = 1 − 2/N` and `r = 8/(c²N²)` from `plateauApproachPot` — needs banked derived-ℝ `cutExp`
      calculus (a chain rule + the antiderivative of `cutExp(cφ)`) that does not exist yet.
    • ⚠️⚠️ STANDING KILL-LINES (prose-only, do NOT soften): E9 dark matter is CANDIDATE-class — the
      sterile-ν_R structure is theorem-grade (N336/N337) but the cosmological abundance / reheating /
      η_B is convention-sensitive and OUT OF SCOPE (do NOT enter the freeze-in integrator swamp); the
      `m₁ = 0` identification is JOINTLY FALSIFIABLE with the ν-spectrum law (any `m₁ > 0` kills both).
      The measured Ω_DM h² = 0.12, Λ, T_H numeric stay REMOVABLE PROSE; no empirical number in any
      statement or proof.

  ## SEED HARD GUARDS

  - G1 NO ASSERTED VALUE — every coefficient (2, 4, 8, 21, 42) DERIVED by composing banked identities
    (`gaussSphereSolidAngle_eq`, `gravCouplingCoeff_azimuthal`, `numerator_azimuthal`,
    `ehActionInverseCoeff_eq`); no numeric value premised.
  - G2 NO EMPIRICAL NUMBER — measured Ω_DM h² / Λ / T_H = REMOVABLE PROSE ONLY; none in any statement
    or proof.
  - G3 SCALES vs RATIOS — `M` is the single dimensionful anchor (banked); the family members are
    homogeneous in `M²` / `M`, the locks are pure-integer relations between banked objects.
  - G4 DERIVED NOT POSITED — the family members and locks are DERIVED by `rw` + `ring` from the banked
    identities; the dark-matter conjuncts are BANKED-CITED. No Mathlib-ℝ content.
  - G5 PHYSICS-WORDS-REMOVABLE — every theorem stands as pure `Cut`-arithmetic / real-matrix analysis.
  - G6 FOUNDATIONS-ONLY — `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`; complete proofs,
    no posited axiom, no sorry, no kernel-trust bypass, no `maxHeartbeats` raise. Ground field the
    DERIVED ℝ `Cut` + banked `azimuthalTurn`/`cutPi`/`gravCouplingCoeff`/`bhEntropy`/`heavyMajorana`
    (⚠ NOT Mathlib-ℝ / `Real.exp` / `Real.pi`; `Matrix` is MACHINERY only).

  ## NOT FREE-FLOATING

  Every theorem's TYPE mentions banked derived objects — `azimuthalTurn`/`gaussSphereSolidAngle`
  (N328), `gravCouplingCoeff`/`ehActionInverseCoeff`/`criticalPhaseNumerator` (N359 / scale-tower),
  `bhEntropy`/`hawkingPeriod` (N360/N361), `heavyMajorana`/`selfDir`/`othersProj` (N336/N337) — all
  over the derived `Cut`. NOT generic over an arbitrary field.

  Foundations-only: no posited axiom, no sorry, no kernel-trust bypass, NO Mathlib number-system
  content import, NO `Real.exp`/`Real.pi`. No empirical Ω_DM / Λ / T_H appears anywhere.
-/
import Phys.Algebra.SpacetimePageCurve
import Phys.Algebra.SterileNeutrinoDarkMatter

namespace Phys.Algebra

open Phys.Foundation Phys.Foundation.ContinuumQ
open Matrix

noncomputable section

/-! ## (1) THE arc-E RETURN-SURFACE (`azimuthalTurn`) NORMALIZATION FAMILY — the NEW members. -/

/-- ★ THE GAUSS-SPHERE FLUX AS A RETURN-SURFACE MEMBER: `gaussSphereSolidAngle = azimuthalTurn · 2`.
    The derived `4π` flux (N328) is the derived Born turn `2·cutPi` scaled by `2`. -/
theorem gaussSphere_azimuthal : gaussSphereSolidAngle = azimuthalTurn * 2 := by
  rw [gaussSphereSolidAngle_eq]; unfold azimuthalTurn; ring

/-- ★ THE EINSTEIN–HILBERT ACTION NORMALIZATION AS A RETURN-SURFACE MEMBER:
    `ehActionInverseCoeff = azimuthalTurn · 8`. The derived `16π` (N359) is the Born turn scaled by
    `8` — twice gravity's `4`. -/
theorem ehAction_azimuthal : ehActionInverseCoeff = azimuthalTurn * 8 := by
  rw [ehActionInverseCoeff_eq]; unfold azimuthalTurn; ring

/-- ★★ BLACK-HOLE ENTROPY AS A RETURN-SURFACE MEMBER (GENUINELY NEW): `bhEntropy M = azimuthalTurn · 2
    · M²`. Composing the banked `bhEntropy M = gravCouplingCoeff·M²/2` (N361) with
    `gravCouplingCoeff = azimuthalTurn·4` (N359) homes the entropy to the ONE derived return surface —
    the `1/4` and the `8π` collapse to the derived Born turn scaled by `2`. -/
theorem bhEntropy_azimuthal (M : Cut) : bhEntropy M = azimuthalTurn * 2 * M ^ 2 := by
  unfold bhEntropy; rw [gravCouplingCoeff_azimuthal]; ring

/-- ★★ THE SCHWARZSCHILD HAWKING PERIOD AS A RETURN-SURFACE MEMBER (GENUINELY NEW): GIVEN the explicit
    metric-specific hypothesis `κsg = (4M)⁻¹` (the surface-gravity computation, kept as a HYPOTHESIS,
    NEVER asserted; the full build is the childed N360 remainder), `hawkingPeriod κsg = azimuthalTurn ·
    4 · M`. The thermal period is the derived Born turn scaled by `4` (gravity's count) times the mass.
    Leading-order (conditional on the hypothesis), flagged. -/
theorem hawkingPeriod_sch_azimuthal (κsg M : Cut) (hM : M ≠ 0) (h : κsg = (4 * M)⁻¹) :
    hawkingPeriod κsg = azimuthalTurn * 4 * M := by
  rw [hawkingPeriod_schwarzschild κsg M hM h, gravCouplingCoeff_azimuthal]

/-! ## (2) THE `azimuthalTurn`-FREE PURE-INTEGER GAUGE ↔ GRAVITY-THERMODYNAMICS CROSS-LOCKS. -/

/-- ★★★ THE ENTROPY ↔ FINE-STRUCTURE PURE-INTEGER LOCK (the moat): `bhEntropy M · 21 =
    criticalPhaseNumerator · M²`. Both sides are `azimuthalTurn·2·42·M²`, so the derived Born turn
    CANCELS and black-hole entropy is locked to the fine-structure channel count `42` by the PURE
    INTEGER `21 = 42/2` — NO `cutPi` in the coefficients. What the field computes with three
    disconnected matching arguments, the theory reads as ONE turn scaled by integers, and here the
    turn drops out entirely. -/
theorem bhEntropy_gauge_lock (M : Cut) :
    bhEntropy M * 21 = criticalPhaseNumerator * M ^ 2 := by
  rw [bhEntropy_azimuthal, numerator_azimuthal]; ring

/-- ★★ THE SCHWARZSCHILD HAWKING PERIOD ↔ FINE-STRUCTURE PURE-INTEGER LOCK: GIVEN `κsg = (4M)⁻¹`,
    `hawkingPeriod κsg · 42 = criticalPhaseNumerator · 4 · M`. The thermal period is locked to the
    fine-structure channel count `42` by pure integers `42, 4` (the derived Born turn cancels).
    Leading-order (conditional on the metric-specific hypothesis), flagged. -/
theorem hawkingPeriod_gauge_lock (κsg M : Cut) (hM : M ≠ 0) (h : κsg = (4 * M)⁻¹) :
    hawkingPeriod κsg * 42 = criticalPhaseNumerator * 4 * M := by
  rw [hawkingPeriod_sch_azimuthal κsg M hM h, numerator_azimuthal]; ring

/-! ## (3) THE E9 CONSOLIDATION CAPSTONE — arc E as ONE fold with two faces (RETURN + MISS). -/

/-- ★★★ E9 — THE arc-E RETURN-SURFACE FAMILY CONSOLIDATED WITH THE BANKED DARK-MATTER REPRESENTATIVE,
    over the banked derived-ℝ `Cut`. Arc E is ONE fold-causal structure with TWO faces:
      THE RETURN (`azimuthalTurn = 2·cutPi`, N328) — the ONE derived constant normalizing every
        gravitational/thermodynamic quantity:
        • `gaussSphereSolidAngle = azimuthalTurn · 2` (the flux),
        • `ehActionInverseCoeff = azimuthalTurn · 8` (the `16π` action),
        • `bhEntropy M = azimuthalTurn · 2 · M²` (the entropy — NEW),
        and the pure-integer lock to the gauge sector, turn-free:
        • `bhEntropy M · 21 = criticalPhaseNumerator · M²` (entropy ↔ the fine-structure count `42`);
      THE MISS (self-blindness `othersProj · selfDir = 0`, N336) — the sterile dark direction:
        • `(heavyMajorana M).mulVec selfDir = M • selfDir` (heavy-sterile, N337),
        • `(othersProj · D₀)ᵀ.mulVec selfDir = 0` (universally dark, N336).
    The gravitational-thermodynamic normalizations and the dark direction are the SAME fold's two
    faces. CANDIDATE-grade for the dark-matter reading (arc-C, BANKED-CITED, NEVER re-asserted); the
    "two faces of one fold" reading is a NAMED IDENTIFICATION (mechanism-grade prose), never a theorem;
    the N364 slow-roll-calculus derivation of `n_s`/`r` is route-not-yet-found and carried forward. -/
theorem cosmo_arc_return_surface_consolidated :
    (gaussSphereSolidAngle = azimuthalTurn * 2)
    ∧ (ehActionInverseCoeff = azimuthalTurn * 8)
    ∧ (∀ M : Cut, bhEntropy M = azimuthalTurn * 2 * M ^ 2)
    ∧ (∀ M : Cut, bhEntropy M * 21 = criticalPhaseNumerator * M ^ 2)
    ∧ (∀ M : Cut, (heavyMajorana M).mulVec selfDir = M • selfDir)
    ∧ (∀ D0 : Matrix (Fin 3) (Fin 3) Cut, (othersProj * D0)ᵀ.mulVec selfDir = 0) :=
  ⟨gaussSphere_azimuthal, ehAction_azimuthal, bhEntropy_azimuthal, bhEntropy_gauge_lock,
   heavyMajorana_selfDir_eigen, dirac_others_self_blind⟩

/-! ## NON-VACUITY (W8): the derived counts are load-bearing — a WRONG count / integer FAILS. -/

/-- NON-VACUITY: black-hole entropy is `azimuthalTurn · 2 · M²`, NOT `azimuthalTurn · 3 · M²` — the
    count `2` (gravity's `4` halved by the first-law `1/2`) is load-bearing. For `M ≠ 0` the two
    differ (`azimuthalTurn > 0` via `cutPi_pos`). A mis-derived coefficient would collapse this. -/
theorem bhEntropy_azimuthal_ne_three (M : Cut) (hM : M ≠ 0) :
    bhEntropy M ≠ azimuthalTurn * 3 * M ^ 2 := by
  rw [bhEntropy_azimuthal]
  have hpi := cutPi_pos
  have hM2 : 0 < M ^ 2 := by positivity
  intro hcon
  unfold azimuthalTurn at hcon
  nlinarith [hcon, hpi, hM2]

/-- NON-VACUITY: the entropy ↔ fine-structure lock is `· 21`, NOT `· 20` — the pure integer `21 = 42/2`
    is load-bearing. For `M ≠ 0` the wrong integer fails. A mis-counted channel number would break the
    turn-free lock. -/
theorem bhEntropy_gauge_lock_ne_twenty (M : Cut) (hM : M ≠ 0) :
    bhEntropy M * 20 ≠ criticalPhaseNumerator * M ^ 2 := by
  rw [bhEntropy_azimuthal, numerator_azimuthal]
  have hpi := cutPi_pos
  have hM2 : 0 < M ^ 2 := by positivity
  intro hcon
  unfold azimuthalTurn at hcon
  nlinarith [hcon, hpi, hM2]

end

end Phys.Algebra
