/-
  # N### — ARC-D D5-next-3 (FOURTH NARROWING NODE, directed successor of D5-next-2/NarrowedBracket2):
    THE NARROWING CAMPAIGN, node 4 — SHARPEN THE `m01` FLAVOR FLOOR AND AIM THE COS INSTRUMENT AT
    split@5, re-composing the assembled `1/α(0)` bracket to a STRICTLY NARROWER width over the derived
    ℝ `Cut`.

  ARC-D (THE DIGITS, docs/SEED_DIGITS.md §D5) is THE NARROWING CAMPAIGN — child as many narrowing
  nodes as it takes until the derived `1/α(0)` bracket is BELOW the measured uncertainty (the north
  star, D7). D5-next-2 (`NarrowedBracket2`) banked the third rung: at the derived census
  `d = chargeTraceDepth = 16/3` the pipeline `endpoint_assembled_bracket` fed with
  `cutLog(mass0/mass1) ∈ [163/20, 8157/1000]` (pinned to the `m01` floor) and `1/cutPi ∈ [25/79, 250/783]`
  (D5 cos split@4 both ends) gave `48290/711 ≤ 1/α(0) ≤ 159646/2349`, width `8344/185571 ≈ 0.045`.
  THIS node attacks BOTH residual fronts the parent named and re-composes to a bracket `≈ 26×` narrower.

  ★ THE TWO FRONTS (the parent's crux resolved on both sides).

    FRONT (1) — SHARPEN THE `m01` FLAVOR FLOOR. The parent NAMED `cutLog` as pinned to the `m01`
    flavor-bracket floor and handed the successor the job of sharpening `m01`. Doing so reveals the
    floor was NOT irreducible — it was a LOOSE reading. The banked `m01_bracket_lo/hi`
    (`EndpointAssembly`) route `mass0/mass1` through the massRatio DETOUR
    (`m₁ ≤ 485/1e5·m₂`, `m₂ ≤ 595/1e4·m₀`, cross-multiplied to `1e9/288575 ≤ mass0/mass1 ≤ 1e9/286902`,
    width `≈ 20.2`). But `mass0/mass1` reads MORE TIGHTLY DIRECTLY from the banked Born-square mass
    brackets: `mass0 ∈ [237943/1e5·237943/1e5, 237944/1e5·237944/1e5]`,
    `mass1 ∈ [4034/1e5·4034/1e5, 40365/1e6·40365/1e6]` (N340), so
    `mass0_lo/mass1_hi ≤ mass0/mass1 ≤ mass0_hi/mass1_lo`, i.e. `mass0/mass1 ∈ [17374/5, 17396/5]`
    (width `≈ 4.34`, `≈ 4.6×` tighter than the detour). The DIRECT bracket sharpens `cutLog` to
    `[8153/1000, 8155/1000] = [8.153, 8.155]`, width `1/500 = 0.002` — narrower than the parent's
    `7/1000` and NESTED strictly inside it. (`m01_lo_sharp`/`m01_hi_sharp`, `cutLog_lo_sharp`/
    `cutLog_hi_sharp`, D1 exp instrument at partial-sum index `17`/`20`.)

    FRONT (2) — SQUEEZE `1/cutPi` VIA split@5. `1/cutPi` has NO floor (`cosRemBound_lt` guarantees any
    width). Aiming the cos instrument at split@5 (`partialCos_five = 1 − x²/2 + x⁴/24 − x⁶/720 + x⁸/40320`,
    remainder `cosRemBound x 5 ≤ x¹⁰/1814400`) pushes the least-zero bracket to `leastCosZero ∈
    [15707/10000, 15709/10000]`:
        • UPPER: `cutCos (15709/10000) < 0` at split@4 (a sharper sign endpoint than the parent's `79/50`);
        • LOWER: `0 < cutCos x` for `0 ≤ x < 15707/10000` at split@5 (the degree-10 positivity
          `1 − x²/2 + x⁴/24 − x⁶/720 + x⁸/40320 − x¹⁰/1814400 > 0` for `x² < 246709849/1e8`).
      Hence `cutPi ∈ [15707/5000, 15709/5000]` and `1/cutPi ∈ [5000/15709, 5000/15707]`, width `≈ 4×10⁻⁵`
      (nested strictly inside the parent's `[25/79, 250/783]`) — three orders of magnitude below the
      `cutLog` floor. `1/cutPi` is no longer the bottleneck; `cutLog` again is (now `0.002`).

  ★ THE RE-COMPOSED NARROWER NUMERAL. Feed BOTH sharpened residuals into the banked D4
  `endpoint_assembled_bracket` at `d = chargeTraceDepth = 16/3`:

        190/3 + (16/9)·(8153/1000)·(5000/15709)  ≤  1/α(0)  ≤  190/3 + (16/9)·(8155/1000)·(5000/15707),
        i.e.  9606370/141381  ≤  invAlphaZero (completeBandList chargeTraceDepth)  ≤  9605390/141363,

  a certified two-sided rational bracket of `Cut` of width `≈ 0.00172` — nested strictly inside
  D5-next-2's `[48290/711, 159646/2349]` (width `≈ 0.045`) and `≈ 26×` narrower. The dressed endpoint
  at the derived weight `w = 1/3` lands `3813518/47127 ≤ · ≤ 3813154/47121` the same way. This is the
  campaign's fourth rung; its successor keeps sharpening BOTH fronts (deeper exp indices tighten the
  `m01`/`cutLog` floor further; deeper cos indices tighten `1/cutPi`).

  ★ NO-FIT (G2/G7 — the credibility guard). NO measured value steers ANY bracket. The cos endpoints
  `15707/10000`, `15709/10000` are chosen by where the derived cosine's SIGN is certifiable at split@5/
  split@4, NOT by where `π/2` sits; the `m01` endpoints `17374/5`, `17396/5` are the DIRECT quotient of
  the banked Born-square mass brackets, and the cutLog endpoints `8153/1000`, `8155/1000` are chosen by
  where the derived exp partial-sum brackets certify that quotient's log, NOT by `137.036`. Truncating
  at the measured precision is FORBIDDEN — the campaign drives to MORE digits. The measured `1/α(0) ≈
  137.036` (the derived value lands `≈ 67.95`, nowhere near it) and `π` appear NOWHERE in any proof —
  removable prose only. The ANY-WIDTH guarantees (`cosRemBound_lt`, `expRemBound_lt`) prove the
  narrowing is a THEOREM path, not a hope.

  ★ THE ONE CAUSE (the trunk). The SAME C6 order/Cauchy-completeness that built the derived ℝ, made
  its `xⁿ/n!` and alternating `x²ⁿ/(2n)!` series converge, and forced `cutExp`/`cutLog`/`cutPi` now
  tightens every one of their rational brackets to any demanded width by taking one more term of the
  same series — AND the SAME banked Born-square masses (N340) read their own ratio more tightly than the
  massRatio detour did. One engine, deeper readings.

  PHYSICS-WORDS-REMOVABLE (STANDARD §2): there are NO physics words in the content. Delete
  "coupling"/"fine-structure"/"flavor"/"mass"/"census": the theorems are pure statements that a derived
  closed real of `Cut` (`invAlphaZero (completeBandList (16/3))`) is pinned between explicit rationals
  `9606370/141381` and `9605390/141363`, read from certified two-sided rational brackets of `1/cutPi`
  (via the derived cosine's least zero) and `cutLog(mass0/mass1)` (via the derived exp series and the
  banked Born-square quotient). No name is load-bearing.

  FREE-FLOATING check (SOUL rail): every production TYPE mentions the banked derived objects
  (`cutCos`/`cutPi`/`leastCosZero`/`cutLog`/`mass0`/`mass1`/`invAlphaZero`/`completeBandList`/
  `dressedInvAlphaZero`/`depthWeight` over `Cut`). Not generic, not carrier-agnostic.

  Foundations-only: no posited axiom, no sorry, no kernel-compiled decision bypass, no bridge, NO
  Mathlib number-system content import, NO `Real.pi`/`Real.cos`/`Real.exp`, NO floats. The only Mathlib
  used is `tsum`/order/IVT/infimum/`norm_num`/`nlinarith` MACHINERY operating ON the derived `Cut`.
  Import guard: NarrowedBracket2 (transitively NarrowedBracket/InvCutPiBracket/AssembledBracket/D3/D2/
  D1/Z6/N325/N323/N340) only; NEVER `Phys/OneAxiom/*`.
-/
import Phys.Algebra.NarrowedBracket2

namespace Phys.Foundation
namespace ContinuumQ

open Filter Set
open scoped Topology BigOperators

noncomputable section

/-! ## (A) THE split@5 COS PARTIAL SUM AND THE FURTHER-SQUEEZED `1/cutPi` BRACKET. -/

/-- `partialCos x 5 = 1 − x²/2 + x⁴/24 − x⁶/720 + x⁸/40320` (the fifth partial cosine sum). -/
theorem partialCos_five (x : Cut) :
    partialCos x 5 = 1 - x ^ 2 / 2 + x ^ 4 / 24 - x ^ 6 / 720 + x ^ 8 / 40320 := by
  unfold partialCos cosTermC
  simp only [Finset.sum_range_succ, Finset.sum_range_zero]
  norm_num; ring

/-- ★ THE SHARPER UPPER SIGN FACT: `cutCos (15709/10000) < 0`, split at index 4
    (`partialCos (15709/10000) 4 = −719039100237722316297241/720000000000000000000000000`, remainder
    `< 719039100237722316297241/720000000000000000000000000`). A sharper sign endpoint than D5-next-2's
    `cutCos (79/50) < 0`. -/
theorem cutCos_15709_neg : cutCos (15709 / 10000 : Cut) < 0 := by
  have hb := (cutCos_bracket (15709 / 10000) (by norm_num) 4 (by norm_num)).2
  rw [partialCos_four] at hb
  have hpart : (1 : Cut) - (15709 / 10000) ^ 2 / 2 + (15709 / 10000) ^ 4 / 24
      - (15709 / 10000) ^ 6 / 720
      = -719039100237722316297241 / 720000000000000000000000000 := by norm_num
  rw [hpart] at hb
  have hrem : cosRemBound (15709 / 10000 : Cut) 4
      < 719039100237722316297241 / 720000000000000000000000000 := by
    unfold cosRemBound; norm_num [Nat.factorial]
  linarith

/-- ★ THE SHARPER LOWER POSITIVITY FACT: `0 < cutCos x` for `0 ≤ x < 15707/10000`, split at index 5
    (`cosRemBound x 5 ≤ x¹⁰/1814400`, and `1 − x²/2 + x⁴/24 − x⁶/720 + x⁸/40320 − x¹⁰/1814400 > 0` for
    `x² < 246709849/1e8`). Extends D5-next-2's `< 783/500` positivity out to `< 15707/10000` — this is
    the split@5 deepening the cos instrument (the parent's `1/cutPi` front). -/
theorem cutCos_pos_of_lt_15707 (x : Cut) (hx0 : 0 ≤ x) (hx : x < 15707 / 10000) :
    0 < cutCos x := by
  have hx2 : x ^ 2 < 246709849 / 100000000 := by nlinarith [hx0, hx]
  have hxN : x ^ 2 < (((2 * 5 + 1) * (2 * 5 + 2) : ℕ) : Cut) := by push_cast; nlinarith [hx2]
  have hb := (cutCos_bracket x hx0 5 hxN).1
  rw [partialCos_five] at hb
  have hden : (0 : Cut) < 1 - x ^ 2 / (((2 * 5 + 1) * (2 * 5 + 2) : ℕ) : Cut) := by
    push_cast; nlinarith [hx2]
  have hinv2 : (1 - x ^ 2 / (((2 * 5 + 1) * (2 * 5 + 2) : ℕ) : Cut))⁻¹ ≤ 2 := by
    rw [inv_le_comm₀ hden (by norm_num)]
    push_cast; nlinarith [hx2]
  have hrembd : cosRemBound x 5 ≤ x ^ 10 / 1814400 := by
    unfold cosRemBound
    have hcoef : (0 : Cut) ≤ x ^ (2 * 5) / ((2 * 5).factorial : Cut) := by positivity
    have hstep : x ^ (2 * 5) / ((2 * 5).factorial : Cut)
        * (1 - x ^ 2 / (((2 * 5 + 1) * (2 * 5 + 2) : ℕ) : Cut))⁻¹
        ≤ x ^ (2 * 5) / ((2 * 5).factorial : Cut) * 2 :=
      mul_le_mul_of_nonneg_left hinv2 hcoef
    calc x ^ (2 * 5) / ((2 * 5).factorial : Cut)
            * (1 - x ^ 2 / (((2 * 5 + 1) * (2 * 5 + 2) : ℕ) : Cut))⁻¹
          ≤ x ^ (2 * 5) / ((2 * 5).factorial : Cut) * 2 := hstep
      _ = x ^ 10 / 1814400 := by norm_num [Nat.factorial]; ring
  have hpos : 0 < 1 - x ^ 2 / 2 + x ^ 4 / 24 - x ^ 6 / 720 + x ^ 8 / 40320 - x ^ 10 / 1814400 := by
    nlinarith [hx2, hx0, sq_nonneg x, sq_nonneg (x ^ 2), sq_nonneg (x ^ 3), sq_nonneg (x ^ 4),
      sq_nonneg (x ^ 5), pow_nonneg hx0 4, pow_nonneg hx0 6, pow_nonneg hx0 8, pow_nonneg hx0 10,
      mul_nonneg hx0 (pow_nonneg hx0 3)]
  linarith [hb, hrembd, hpos]

/-- ★ `leastCosZero ≤ 15709/10000` (sharper UPPER bound): the IVT on `[0,15709/10000]` supplies a
    cosine zero `≤ 15709/10000` (since `cutCos (15709/10000) < 0 ≤ cutCos 0`), and `csInf_le`. -/
theorem leastCosZero_le_15709 : leastCosZero ≤ 15709 / 10000 := by
  have h02 : (0 : Cut) ≤ 15709 / 10000 := by norm_num
  have hcont : ContinuousOn cutCos (Icc (0 : Cut) (15709 / 10000)) := cutCos_continuous.continuousOn
  have hmem : (0 : Cut) ∈ Icc (cutCos (15709 / 10000)) (cutCos 0) := by
    rw [cutCos_zero]; exact ⟨le_of_lt cutCos_15709_neg, by norm_num⟩
  obtain ⟨c, hc, hcval⟩ := (intermediate_value_Icc' h02 hcont) hmem
  have hc2 : c ≤ 2 := le_trans hc.2 (by norm_num)
  have hcmem : c ∈ cosZeroSet := ⟨⟨hc.1, hc2⟩, hcval⟩
  exact le_trans (csInf_le cosZeroSet_bddBelow hcmem) hc.2

/-- ★ `15707/10000 ≤ leastCosZero` (sharper LOWER bound): the cosine is strictly positive on
    `[0,15707/10000)`, so no zero lives below `15707/10000`, hence `15707/10000 ≤` the infimum
    (`le_csInf`). -/
theorem le_leastCosZero_15707 : (15707 / 10000 : Cut) ≤ leastCosZero := by
  apply le_csInf cosZeroSet_nonempty
  intro b hb
  by_contra hlt
  rw [not_le] at hlt
  have hb0 : 0 ≤ b := hb.1.1
  have hpos := cutCos_pos_of_lt_15707 b hb0 hlt
  rw [hb.2] at hpos
  exact lt_irrefl _ hpos

/-- ★★ THE FURTHER-TIGHTER `cutPi` BRACKET: `cutPi ∈ [15707/5000, 15709/5000]`, from
    `cutPi = 2·leastCosZero` and `leastCosZero ∈ [15707/10000, 15709/10000]`. Nested strictly inside
    D5-next-2's `[783/250, 79/25]`. -/
theorem cutPi_bracket_split5 : (15707 / 5000 : Cut) ≤ cutPi ∧ cutPi ≤ 15709 / 5000 := by
  unfold cutPi
  refine ⟨?_, ?_⟩
  · have := le_leastCosZero_15707; linarith
  · have := leastCosZero_le_15709; linarith

/-- ★★★ THE FURTHER-SQUEEZED RESIDUAL BRACKET: `1/cutPi ∈ [5000/15709, 5000/15707]` — a certified
    two-sided rational bracket of `Cut` for the derived `1/cutPi`, width `≈ 4×10⁻⁵`, nested strictly
    inside D5-next-2's `[25/79, 250/783]`. -/
theorem inv_cutPi_bracket_split5 :
    (5000 / 15709 : Cut) ≤ 1 / cutPi ∧ 1 / cutPi ≤ 5000 / 15707 := by
  obtain ⟨hlo, hhi⟩ := cutPi_bracket_split5
  have hpos : 0 < cutPi := cutPi_pos
  refine ⟨?_, ?_⟩
  · rw [le_div_iff₀ hpos]; nlinarith [hhi, hpos]
  · rw [div_le_iff₀ hpos]; nlinarith [hlo, hpos]

/-- ★ W8 — THE FURTHER-SQUEEZED RESIDUAL BRACKET IS STRICTLY TWO-SIDED: `5000/15709 < 5000/15707`. -/
theorem inv_cutPi_split5_strict : (5000 / 15709 : Cut) < 5000 / 15707 := by norm_num

/-- ★ W8 — THE split@5 `1/cutPi` BRACKET IS NESTED INSIDE D5-next-2's `[25/79, 250/783]`:
    `25/79 ≤ 5000/15709` and `5000/15707 ≤ 250/783` — the narrowing genuinely shrinks the bracket from
    both ends. -/
theorem inv_cutPi_split5_nested :
    (25 / 79 : Cut) ≤ 5000 / 15709 ∧ (5000 / 15707 : Cut) ≤ 250 / 783 := by
  refine ⟨by norm_num, by norm_num⟩

end

end ContinuumQ
end Phys.Foundation

namespace Phys.Algebra.NarrowedBracket3

open Phys.Algebra
open Phys.Algebra.BandEdgeList
open Phys.Algebra.DepthTowerDescent
open Phys.Algebra.EndpointAssembly
open Phys.Algebra.DepthWeight
open Phys.Algebra.AssembledBracket
open Phys.Algebra.GenerationMassRatios
open Phys.Foundation Phys.Foundation.ContinuumQ

noncomputable section

/-! ## (B) THE SHARPENED `m01` FLAVOR FLOOR — the DIRECT Born-square quotient bracket (tighter than the
    banked massRatio-detour `m01_bracket_lo/hi`). -/

/-- ★★ THE DIRECT `m01` LOWER BRACKET: `17374/5 ≤ mass0/mass1`. Reads `mass0/mass1` DIRECTLY from the
    banked Born-square mass brackets `mass0_lo` (`237943/1e5·237943/1e5 ≤ mass0`) and `mass1_hi`
    (`mass1 ≤ 40365/1e6·40365/1e6`) — NOT through the loose massRatio detour. `17374/5 = 3474.8 ≤
    mass0_lo/mass1_hi ≈ 3474.85`. -/
theorem m01_lo_sharp : (17374 / 5 : Cut) ≤ mass0 / mass1 := by
  have hm1 : (0 : Cut) < mass1 := mass1_pos
  rw [le_div_iff₀ hm1]
  have h1 : mass1 ≤ (40365 : Cut) / 1000000 * (40365 / 1000000) := mass1_hi
  have h2 : (237943 : Cut) / 100000 * (237943 / 100000) ≤ mass0 := mass0_lo
  nlinarith [h1, h2]

/-- ★★ THE DIRECT `m01` UPPER BRACKET: `mass0/mass1 ≤ 17396/5`. Reads `mass0/mass1` DIRECTLY from
    `mass1_lo` (`4034/1e5·4034/1e5 ≤ mass1`) and `mass0_hi` (`mass0 ≤ 237944/1e5·237944/1e5`).
    `mass0_hi/mass1_lo ≈ 3479.19 ≤ 3479.2 = 17396/5`. Width `≈ 4.34`, `≈ 4.6×` tighter than the banked
    detour `[1e9/288575, 1e9/286902]` (width `≈ 20.2`). -/
theorem m01_hi_sharp : mass0 / mass1 ≤ (17396 / 5 : Cut) := by
  have hm1 : (0 : Cut) < mass1 := mass1_pos
  rw [div_le_iff₀ hm1]
  have h1 : (4034 : Cut) / 100000 * (4034 / 100000) ≤ mass1 := mass1_lo
  have h2 : mass0 ≤ (237944 : Cut) / 100000 * (237944 / 100000) := mass0_hi
  nlinarith [h1, h2]

/-- ★★ THE SHARPENED cutLog CONTENT LOWER BRACKET: `8153/1000 ≤ cutLog(mass0/mass1)` (i.e. `8.153 ≤`).
    Composes the DIRECT flavor bracket `17374/5 ≤ mass0/mass1` (`m01_lo_sharp`) with `cutLog_ge_certified`
    at partial-sum index `17` (`partialExp (8153/1000) 17 + expRemBound (8153/1000) 17 ≤ 17374/5`)
    through `cutLog_mono`. Tighter than D5-next-2's `163/20 = 8.150`. -/
theorem cutLog_lo_sharp : (8153 / 1000 : Cut) ≤ cutLog (mass0 / mass1) := by
  have hmono : cutLog ((17374 : Cut) / 5) ≤ cutLog (mass0 / mass1) :=
    cutLog_mono (by norm_num) m01_lo_sharp
  have hge : (8153 / 1000 : Cut) ≤ cutLog ((17374 : Cut) / 5) := by
    apply cutLog_ge_certified ((17374 : Cut) / 5) (8153 / 1000) 17 (by norm_num) (by norm_num)
    show partialExp (8153 / 1000) 17 + expRemBound (8153 / 1000) 17 ≤ (17374 : Cut) / 5
    unfold partialExp expTermC expRemBound
    simp only [Finset.sum_range_succ, Finset.sum_range_zero]
    norm_num [Nat.factorial]
  linarith

/-- ★★ THE SHARPENED cutLog CONTENT UPPER BRACKET: `cutLog(mass0/mass1) ≤ 8155/1000` (i.e. `≤ 8.155`).
    Composes the DIRECT flavor bracket `mass0/mass1 ≤ 17396/5` (`m01_hi_sharp`) with
    `cutLog_le_certified` at partial-sum index `20` (`17396/5 ≤ partialExp (8155/1000) 20`) through
    `cutLog_mono`. Width `1/500 = 0.002` — narrower than D5-next-2's `7/1000` and NESTED inside its
    `[163/20, 8157/1000]`. The `m01` floor was NOT irreducible; the direct bracket sharpens it. -/
theorem cutLog_hi_sharp : cutLog (mass0 / mass1) ≤ (8155 / 1000 : Cut) := by
  have hpos : (0 : Cut) < mass0 / mass1 :=
    lt_of_lt_of_le (by norm_num : (0 : Cut) < 17374 / 5) m01_lo_sharp
  have hmono : cutLog (mass0 / mass1) ≤ cutLog ((17396 : Cut) / 5) :=
    cutLog_mono hpos m01_hi_sharp
  have hbnd : cutLog ((17396 : Cut) / 5) ≤ (8155 / 1000 : Cut) := by
    apply cutLog_le_certified ((17396 : Cut) / 5) (8155 / 1000) 20 (by norm_num) (by norm_num)
    show (17396 : Cut) / 5 ≤ partialExp (8155 / 1000) 20
    unfold partialExp expTermC
    simp only [Finset.sum_range_succ, Finset.sum_range_zero]
    norm_num [Nat.factorial]
  linarith

/-- ★ W8 — THE SHARPENED cutLog CONTENT BRACKET IS GENUINELY TWO-SIDED: `8153/1000 < 8155/1000` (a real
    reading of width `1/500`, not a collapsed point). -/
theorem cutLog_sharp_strict : (8153 / 1000 : Cut) < 8155 / 1000 := by norm_num

/-- ★ W8 — THE SHARPENED cutLog BRACKET IS NESTED INSIDE D5-next-2's `[163/20, 8157/1000]`:
    `163/20 ≤ 8153/1000` and `8155/1000 ≤ 8157/1000` — the direct-quotient sharpening genuinely shrinks
    the cutLog floor from both ends. -/
theorem cutLog_sharp_nested : (163 / 20 : Cut) ≤ 8153 / 1000 ∧ (8155 / 1000 : Cut) ≤ 8157 / 1000 := by
  refine ⟨by norm_num, by norm_num⟩

/-! ## (C) THE RE-COMPOSED FURTHER-NARROWER NUMERAL — the D4 assembled pipeline with BOTH residuals
    sharpened, at the derived census `d = chargeTraceDepth = 16/3`. -/

/-- ★★★ THE FURTHER-NARROWED CONCRETE BRACKET of the derived `1/α(0)`. Feed BOTH sharpened residuals —
    `cutLog(mass0/mass1) ∈ [8153/1000, 8155/1000]` and `1/cutPi ∈ [5000/15709, 5000/15707]` — into the
    banked D4 `endpoint_assembled_bracket` at `d = chargeTraceDepth = 16/3`:

        190/3 + (16/9)·(8153/1000)·(5000/15709)  ≤  1/α(0)  ≤  190/3 + (16/9)·(8155/1000)·(5000/15707),
        i.e.  9606370/141381  ≤  invAlphaZero (completeBandList chargeTraceDepth)  ≤  9605390/141363.

    A certified two-sided rational bracket of `Cut` of width `≈ 0.00172` — nested strictly inside
    D5-next-2's `[48290/711, 159646/2349]` (width `≈ 0.045`) and `≈ 26×` narrower. The campaign's
    fourth rung. ⚠ NO-FIT: the residual endpoints are chosen by instrument certifiability, not tuned to
    a measured value. -/
theorem invAlphaZero_narrowed3_bracket :
    (9606370 / 141381 : Cut) ≤ invAlphaZero (completeBandList chargeTraceDepth)
      ∧ invAlphaZero (completeBandList chargeTraceDepth) ≤ 9605390 / 141363 := by
  have hd : 0 ≤ chargeTraceDepth := le_of_lt chargeTraceDepth_pos
  have hb := endpoint_assembled_bracket chargeTraceDepth hd
    (8153 / 1000) (8155 / 1000) (by norm_num) cutLog_lo_sharp cutLog_hi_sharp
    (5000 / 15709) (5000 / 15707) (by norm_num)
    inv_cutPi_bracket_split5.1 inv_cutPi_bracket_split5.2
  obtain ⟨hlo, hhi⟩ := hb
  constructor
  · have harith : (9606370 / 141381 : Cut)
        ≤ 190 / 3 + chargeTraceDepth / 3 * (8153 / 1000) * (5000 / 15709) := by
      rw [chargeTraceDepth_eq]; norm_num
    exact le_trans harith hlo
  · have harith : (190 / 3 + chargeTraceDepth / 3 * (8155 / 1000) * (5000 / 15707) : Cut)
        ≤ 9605390 / 141363 := by
      rw [chargeTraceDepth_eq]; norm_num
    exact le_trans hhi harith

/-- ★★ THE FURTHER-NARROWED DRESSED BRACKET at the derived weight `w = 1/3`: the running-of-the-running
    endpoint `dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth)` pinned with BOTH
    residuals sharpened, at `d = 16/3`:

        74 + (8/3)·(8153/1000)·(5000/15709)  ≤  ·  ≤  74 + (8/3)·(8155/1000)·(5000/15707),
        i.e.  3813518/47127  ≤  ·  ≤  3813154/47121. -/
theorem dressed_narrowed3_bracket :
    (3813518 / 47127 : Cut) ≤ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth)
      ∧ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth) ≤ 3813154 / 47121 := by
  have hd : 0 ≤ chargeTraceDepth := le_of_lt chargeTraceDepth_pos
  have hb := dressed_endpoint_assembled_bracket chargeTraceDepth hd
    (8153 / 1000) (8155 / 1000) (by norm_num) cutLog_lo_sharp cutLog_hi_sharp
    (5000 / 15709) (5000 / 15707) (by norm_num)
    inv_cutPi_bracket_split5.1 inv_cutPi_bracket_split5.2
  obtain ⟨hlo, hhi⟩ := hb
  constructor
  · have harith : (3813518 / 47127 : Cut)
        ≤ 74 + chargeTraceDepth / 2 * (8153 / 1000) * (5000 / 15709) := by
      rw [chargeTraceDepth_eq]; norm_num
    exact le_trans harith hlo
  · have harith : (74 + chargeTraceDepth / 2 * (8155 / 1000) * (5000 / 15707) : Cut)
        ≤ 3813154 / 47121 := by
      rw [chargeTraceDepth_eq]; norm_num
    exact le_trans hhi harith

/-! ## (D) W8 TEETH — the narrowing is genuine (strictly two-sided, strictly narrower, nested). -/

/-- ★ W8 — THE FURTHER-NARROWED BRACKET IS STRICTLY TWO-SIDED (non-degenerate):
    `9606370/141381 < 9605390/141363`. -/
theorem invAlphaZero_narrowed3_strict : (9606370 / 141381 : Cut) < 9605390 / 141363 := by norm_num

/-- ★ W8 — THE FURTHER-NARROWED BRACKET IS STRICTLY NARROWER THAN D5-next-2's: its width
    `9605390/141363 − 9606370/141381` is strictly below D5-next-2's width `159646/2349 − 48290/711`.
    This is the campaign's core progress tooth — the assembled bracket genuinely shrank a fourth time
    (BOTH fronts attacked: the `m01`/`cutLog` floor sharpened via the direct Born-square quotient, and
    `1/cutPi` squeezed via split@5). -/
theorem invAlphaZero_narrowed3_width_lt :
    (9605390 / 141363 - 9606370 / 141381 : Cut) < 159646 / 2349 - 48290 / 711 := by norm_num

/-- ★ W8 — THE FURTHER-NARROWED BRACKET IS NESTED INSIDE D5-next-2's: `48290/711 ≤ 9606370/141381` and
    `9605390/141363 ≤ 159646/2349`. A monotone refinement — the new bracket sits entirely within the
    old, so no earlier reading is contradicted. -/
theorem invAlphaZero_narrowed3_nested :
    (48290 / 711 : Cut) ≤ 9606370 / 141381 ∧ (9605390 / 141363 : Cut) ≤ 159646 / 2349 := by
  refine ⟨by norm_num, by norm_num⟩

/-- ★ W8 — THE FURTHER-NARROWED ENDPOINT GENUINELY SCREENS ABOVE THE HIGH-BAND WAY-POINT:
    `190/3 < 9606370/141381` (the lepton tail runs `1/α` genuinely up at the physical census, not a
    collapse). -/
theorem invAlphaZero_narrowed3_gt_waypoint :
    (190 / 3 : Cut) < invAlphaZero (completeBandList chargeTraceDepth) := by
  have h := invAlphaZero_narrowed3_bracket.1
  have hgt : (190 / 3 : Cut) < 9606370 / 141381 := by norm_num
  linarith

/-! ## (E) THE D5-next-3 CAPSTONE — welded, non-hollow. -/

/-- ★★★ THE NARROWING CAMPAIGN, FOURTH RUNG (arc-D D5-next-3, welded landing). BOTH residual fronts the
    parent named attacked, and the D4 pipeline re-composed to a strictly narrower certified two-sided
    rational bracket of the derived `1/α(0)`:

    (1) THE FURTHER-SQUEEZED RESIDUAL: `1/cutPi ∈ [5000/15709, 5000/15707]`
        (`inv_cutPi_bracket_split5`), from `cutPi ∈ [15707/5000, 15709/5000]` (`cutPi_bracket_split5`),
        `leastCosZero ∈ [15707/10000, 15709/10000]` — read off the D5 cos instrument at split@5 (lower
        positivity) / split@4 (upper sign);
    (2) THE SHARPENED cutLog CONTENT: `cutLog(mass0/mass1) ∈ [8153/1000, 8155/1000]`
        (`cutLog_lo_sharp` / `cutLog_hi_sharp`) — read off the D1 exp instrument at partial-sum index
        `17` (lower) / `20` (upper) through the DIRECT Born-square `m01` bracket `[17374/5, 17396/5]`
        (`m01_lo_sharp` / `m01_hi_sharp`), `≈ 4.6×` tighter than the banked massRatio detour; the `m01`
        floor was NOT irreducible;
    (3) THE RE-COMPOSED FURTHER-NARROWER NUMERAL: `9606370/141381 ≤ invAlphaZero (completeBandList
        chargeTraceDepth) ≤ 9605390/141363` (`invAlphaZero_narrowed3_bracket`), width `≈ 0.00172`;
    (4) THE DRESSED FURTHER-NARROWER BRACKET: `3813518/47127 ≤ · ≤ 3813154/47121`
        (`dressed_narrowed3_bracket`);
    (5) TEETH: the narrowed bracket is strictly two-sided (`invAlphaZero_narrowed3_strict`), strictly
        narrower than D5-next-2's (`invAlphaZero_narrowed3_width_lt`), nested inside D5-next-2's
        (`invAlphaZero_narrowed3_nested`), and screens above `190/3`
        (`invAlphaZero_narrowed3_gt_waypoint`); the residual and cutLog brackets are each strictly
        two-sided and nested (`inv_cutPi_split5_strict`/`inv_cutPi_split5_nested`,
        `cutLog_sharp_strict`/`cutLog_sharp_nested`).

    Statable from no single conjunct alone (non-hollow). ⚠ HONEST GRADE: THEOREM-route, CERTIFIED
    (certified two-sided rational brackets of closed reals of `Cut`, no error bar). The bracket is
    `≈ 26×` narrower than D5-next-2's but still WIDE relative to the measured precision; the campaign
    continues. ★ THE UPDATED BOTTLENECK: `1/cutPi` is now `≈ 4×10⁻⁵` (three orders below `cutLog`), so
    `cutLog` (now `0.002`, sharpened but not floored) is again the dominant residual — the successor
    sharpens the `m01` bracket further (deeper Born-square mass brackets or the amplitude brackets
    upstream) and/or aims the exp instrument deeper. The measured `1/α(0) ≈ 137.036` and `π` are
    REMOVABLE PROSE ONLY; no empirical number in any proof; explicit NO-FIT (the residual endpoints are
    chosen by instrument certifiability at the chosen index, not by the measured value). -/
theorem narrowedBracket3_landing :
    -- (1) the further-squeezed residual bracket
    ((5000 / 15709 : Cut) ≤ 1 / cutPi ∧ 1 / cutPi ≤ 5000 / 15707)
    ∧ ((15707 / 5000 : Cut) ≤ cutPi ∧ cutPi ≤ 15709 / 5000)
    -- (2) the sharpened cutLog content bracket (m01 floor sharpened)
    ∧ ((8153 / 1000 : Cut) ≤ cutLog (mass0 / mass1) ∧ cutLog (mass0 / mass1) ≤ 8155 / 1000)
    -- (3) the re-composed further-narrower numeral
    ∧ ((9606370 / 141381 : Cut) ≤ invAlphaZero (completeBandList chargeTraceDepth)
        ∧ invAlphaZero (completeBandList chargeTraceDepth) ≤ 9605390 / 141363)
    -- (4) the dressed further-narrower bracket at the derived weight
    ∧ ((3813518 / 47127 : Cut) ≤ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth)
        ∧ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth) ≤ 3813154 / 47121)
    -- (5) teeth: strictly two-sided, strictly narrower than D5-next-2, nested inside D5-next-2, screens above 190/3
    ∧ (9606370 / 141381 : Cut) < 9605390 / 141363
    ∧ ((9605390 / 141363 - 9606370 / 141381 : Cut) < 159646 / 2349 - 48290 / 711)
    ∧ ((48290 / 711 : Cut) ≤ 9606370 / 141381 ∧ (9605390 / 141363 : Cut) ≤ 159646 / 2349)
    ∧ (190 / 3 : Cut) < invAlphaZero (completeBandList chargeTraceDepth) := by
  exact ⟨inv_cutPi_bracket_split5, cutPi_bracket_split5,
    ⟨cutLog_lo_sharp, cutLog_hi_sharp⟩,
    invAlphaZero_narrowed3_bracket, dressed_narrowed3_bracket,
    invAlphaZero_narrowed3_strict, invAlphaZero_narrowed3_width_lt,
    invAlphaZero_narrowed3_nested, invAlphaZero_narrowed3_gt_waypoint⟩

end

end Phys.Algebra.NarrowedBracket3
