/-
# GenDepthDressedMixingNumeric — THE DEPTH-DRESSED EDGE RATIO AS AN ACTUAL NUMBER OF THE DERIVED ℝ
#   (the CKM/Wolfenstein mixing HIERARCHY suppression at leading order)

Directed ROUTE-FINDER child of GenEdgeBornSurvival (rung 2, t_6eb072d1), rung 3 of the quark
ℝ-descent ascent. Rung-2 (`OctonionJordanGenerationEdgeBornSurvival`, C524) banked, THEOREM-EXACT
over ℚ, the DEPTH-DRESSED tridiagonal generation frame `depthFrame` and its key structural theorem
`depthFrame_edge_ratio`: the deep/shallow coupling ratio is `edgeSurvival · (a₂/a₀)` — one factor of
the self-blind Born-survival `edgeSurvival = tr Poth/3 = 2/3` (N305/N308) below the undressed
geometric-mean ratio `a₂/a₀ = genVec 2 / genVec 0`. This is the Cabibbo/Wolfenstein mixing HIERARCHY
read as cascade DEPTH × self-blindness — DERIVED, where standard flavor physics POSITS it
(Froggatt–Nielsen imposes a horizontal-U(1) charge tower and FITS λ). Rung-2 deferred the NUMERICAL
value (route-not-yet-found).

## WHAT THIS NODE DOES (the numerical landing over the derived ℝ)

It EVALUATES rung-2's structural `depthFrame_edge_ratio` over the DERIVED ℝ `Cut`, at the banked
ℝ-descent generation amplitudes (N339/N340 `GenerationMassRatiosNumeric`: the forced ℤ₃-Fourier
cycle amplitudes `b0 = 1 + w·p`, `b2 = 1 − w·(p−r)/2` at the derived phase `δ_B = 2/9`, with
`p = cutCos(2/9)`, `w = cutSqrt 2`, `r = cutSqrt(3 − 3p²)`), to state the depth-dressed mixing
hierarchy as ACTUAL certified two-sided rational brackets over `Cut`:

* the UNDRESSED deep/shallow geometric-mean ratio `undressedEdgeRatio = b2/b0 ∈ [24383/1e5, 24385/1e5]`
  (≈ 0.2438; the un-suppressed ratio `a₂/a₀`);
* ★★ the DEPTH-DRESSED ratio `depthDressedEdgeRatio = survivalCut · (b2/b0) ∈ [16255/1e5, 16257/1e5]`
  (≈ 0.1626) — ONE factor of `survivalCut = 2/3` below the undressed ratio: the Wolfenstein-hierarchy
  suppression of the deeper inter-generation edge, DERIVED as an ACTUAL number;
* ★ STRICT suppression `depthDressedEdgeRatio < undressedEdgeRatio` (`0 < survivalCut < 1`): the
  hierarchy is real, not flat;
* the leading (small-angle) depth-dressed deep-edge mixing PROBABILITY `depthDressedMixingProb =
  depthDressedEdgeRatio² ∈ [26422/1e6, 26430/1e6]` (≈ 0.0264).

`survivalCut = ((edgeSurvival : ℚ) : Cut)` is the banked rung-2 self-blind Born fraction cast to the
derived ℝ (`survivalCut_eq_edgeSurvival`, `survivalCut_val = 2/3`). Every bracket FALLS OUT of the
banked `cutCos(2/9)`-descent amplitudes composed with the banked `2/3`; NO decimal is posited, NO
empirical number enters any proof.

⭐ ONE CAUSE, MANY TERMINATIONS. The SAME self-blind `(n−1)/n = 2/3` that caps the Born self-view
(N305), fixes Koide `2/3` (N307), fixes `δ_B = 2/9` (N308), and dresses the deep coupling edge
(rung-2) is here the numerical suppression factor of the mixing hierarchy over the derived ℝ.

## HONEST GRADE (seed T8)

The mixing/hierarchy sector lands at LEADING ORDER (~few-%), NOT theorem-exact like the mass ratios.
What is banked here: the depth-dressed edge-ratio bracket, its strict suppression, and the
leading-order (small-angle) mixing-probability bracket — the Wolfenstein-hierarchy suppression as an
actual number with ZERO free continuous parameters. What is NOT chased (that would be fitting — the
seed's explicit STOP): any precision residual against the measured Cabibbo/Wolfenstein `λ`. NO
Wolfenstein `λ`, no mixing angle, no quark value, no PDG number enters any proof; comparison to the
measured mixing is REMOVABLE PROSE only (measured Cabibbo `sin θ_C ≈ 0.225` — in NO proof).

## THE DISSOLUTION / MOAT (docs/STANDARD.md §0)

Standard flavor physics POSITS the Wolfenstein hierarchy value and FITS `λ` (Froggatt–Nielsen posits
a horizontal-U(1) charge tower to generate the `λ, λ², λ³` powers). The theory DERIVES the hierarchy
suppression as an actual number: `undressedEdgeRatio` is the banked ℝ-descent numeral (from the
fold's own `cutCos(2/9)`), and `survivalCut = 2/3` is the banked self-blind Born fraction. Zero
posited charges, zero continuous knobs — the wall the field grinds (why THIS hierarchy) dissolves
into a bracket read off the fold's own measures.

## PHYSICS-WORDS-REMOVABLE (docs/STANDARD.md §2)

Delete generation/mass/mixing/CKM/Wolfenstein/quark/lepton/cascade-depth/Born-survival: over the
derived `Cut`, with the banked `b0 = 1 + w·p`, `b2 = 1 − w·(p−r)/2` (`p = cutCos(2/9)`, `w = cutSqrt 2`,
`r = cutSqrt(3 − 3p²)`) and the banked rational `edgeSurvival = 2/3` cast to `Cut`, one has
`b2/b0 ∈ [24383/1e5, 24385/1e5]`, `((edgeSurvival:ℚ):Cut)·(b2/b0) ∈ [16255/1e5, 16257/1e5]` strictly
below `b2/b0`, and its square in `[26422/1e6, 26429/1e6]`. No theorem STATEMENT needs a physics word.

## FREE-FLOATING check (SOUL rail)

`survivalCut` is the banked ℚ `edgeSurvival` (rung-2, `= tr Poth/3`) cast to `Cut`; `undressedEdgeRatio`
and `depthDressedEdgeRatio` are built from the banked ℝ-descent amplitudes `b0`, `b2` (N339/N340) over
`Cut`; the capstone TYPE carries the banked cascade-depth theorems `Phys.Cascade.not_commutative`
(shallow `ℂ→ℍ`) and `Phys.Cascade.not_associative` (deep `ℍ→𝕆`) and the cap `jdef Xwit Ywit ≠ 0`
(exactly three generations, N5), and cites rung-2's ℚ-structural `depthFrame_edge_ratio` as the parent.
NOT a generic ratio statement floating free of the derived objects.

Foundations-only: no posited axiom, no sorry, no compiled-kernel bypass, no heartbeat inflation, no
bridge; no empirical number; the ground field is the BANKED derived ℝ `Cut`, the banked `cutCos`/`cutSqrt`
amplitudes, and the banked ℚ `edgeSurvival` cast to `Cut` — NO Mathlib `Real`/`Real.cos` as content.
IMPORT GUARD: imports the banked rung-2 (transitively N305–N312, the cascade rungs) and N339/N340,
NEVER `MassGapOne` — no `foldRetention` reaches this content.
-/
import Phys.Algebra.OctonionJordanGenerationEdgeBornSurvival
import Phys.Algebra.GenerationMassRatiosNumeric
import Mathlib.Tactic

namespace Phys.Algebra.DepthDressedMixing

open Phys.Foundation Phys.Foundation.ContinuumQ
open Phys.Algebra.HJ
open Phys.Algebra.GenerationMassRatios
open Phys.Cascade
open Phys.Cascade.CD

attribute [local instance] CD.narCD CD.srCD

noncomputable section

/-! ## §1 — the self-blind survival factor over the derived ℝ (the banked ℚ `edgeSurvival` cast). -/

/-- The per-step self-blind Born-survival fraction over the derived ℝ: the banked rung-2 `edgeSurvival
    = tr Poth/3 = 2/3` (N305/N308) cast to `Cut`. -/
def survivalCut : Cut := ((edgeSurvival : ℚ) : Cut)

/-- `survivalCut` is the banked ℚ `edgeSurvival` (rung-2) in the derived ℝ (grounding, definitional). -/
theorem survivalCut_eq_edgeSurvival : survivalCut = ((edgeSurvival : ℚ) : Cut) := rfl

/-- `survivalCut = 2/3` over the derived ℝ (banked rung-2 `edgeSurvival_val`, cast). -/
theorem survivalCut_val : survivalCut = 2 / 3 := by
  unfold survivalCut; rw [edgeSurvival_val]; push_cast; ring

theorem survivalCut_pos : (0 : Cut) < survivalCut := by rw [survivalCut_val]; norm_num
theorem survivalCut_lt_one : survivalCut < 1 := by rw [survivalCut_val]; norm_num

/-! ## §2 — the undressed and depth-dressed deep/shallow edge ratios over the derived ℝ. -/

/-- The UNDRESSED deep/shallow geometric-mean edge ratio over the derived ℝ: `a₂/a₀ = b2/b0`
    (the banked N339/N340 ℝ-descent amplitudes at the derived phase `δ_B = 2/9`). This is the
    un-suppressed ratio `genVec 2 / genVec 0` of rung-2's `depthFrame_edge_ratio`, evaluated. -/
def undressedEdgeRatio : Cut := b2 / b0

/-- ★★ The DEPTH-DRESSED deep/shallow edge ratio over the derived ℝ: `survivalCut · (b2/b0)` — ONE
    factor of the self-blind Born-survival `survivalCut = 2/3` below the undressed ratio. This is
    rung-2's structural `depthFrame_edge_ratio = edgeSurvival · (a₂/a₀)` evaluated over `Cut` at the
    banked ℝ-descent amplitudes: the Cabibbo/Wolfenstein mixing-hierarchy suppression as an ACTUAL
    number. -/
def depthDressedEdgeRatio : Cut := survivalCut * undressedEdgeRatio

/-- The dressed ratio is the survival factor times the undressed ratio (rung-2's structure,
    definitional over `Cut`). -/
theorem depthDressed_eq_survival_mul_undressed :
    depthDressedEdgeRatio = survivalCut * undressedEdgeRatio := rfl

/-- The dressed ratio is `((edgeSurvival:ℚ):Cut) · (b2/b0)` — the banked ℚ survival factor times the
    banked ℝ-descent amplitude ratio (the free-floating tie to the derived objects). -/
theorem depthDressed_eq_edgeSurvival_amp :
    depthDressedEdgeRatio = ((edgeSurvival : ℚ) : Cut) * (b2 / b0) := rfl

/-! ## §3 — positivity of the ratios over the derived ℝ. -/

theorem undressedEdgeRatio_pos : (0 : Cut) < undressedEdgeRatio :=
  div_pos b2_pos b0_pos
theorem depthDressedEdgeRatio_pos : (0 : Cut) < depthDressedEdgeRatio := by
  unfold depthDressedEdgeRatio
  exact mul_pos survivalCut_pos undressedEdgeRatio_pos

/-! ## §4 — the undressed ratio bracket (div-bracket on the banked amplitude brackets). -/

/-- The UNDRESSED deep/shallow ratio `b2/b0 ∈ [24383/1e5, 24385/1e5]` (≈ 0.2438) — from the banked
    N339/N340 amplitude brackets `b0 ∈ [237943/1e5, 237944/1e5]`, `b2 ∈ [5802/1e4, 58022/1e5]`. -/
theorem undressedEdgeRatio_lo : (24383 : Cut) / 100000 ≤ undressedEdgeRatio := by
  unfold undressedEdgeRatio; rw [le_div_iff₀ b0_pos]
  nlinarith [b0_hi, b2_lo, b0_pos]
theorem undressedEdgeRatio_hi : undressedEdgeRatio ≤ (24385 : Cut) / 100000 := by
  unfold undressedEdgeRatio; rw [div_le_iff₀ b0_pos]
  nlinarith [b2_hi, b0_lo, b0_pos]

/-! ## §5 — ★★ THE DEPTH-DRESSED HIERARCHY BRACKET over the derived ℝ. -/

/-- ★★ THE DEPTH-DRESSED HIERARCHY, LOW END. The depth-dressed deep/shallow ratio is at least
    `16255/1e5` (≈ 0.1626) — one factor of `survivalCut = 2/3` below the undressed ratio. -/
theorem depthDressedEdgeRatio_lo : (16255 : Cut) / 100000 ≤ depthDressedEdgeRatio := by
  unfold depthDressedEdgeRatio
  rw [survivalCut_val]
  nlinarith [undressedEdgeRatio_lo]

/-- ★★ THE DEPTH-DRESSED HIERARCHY, HIGH END. The depth-dressed deep/shallow ratio is at most
    `16257/1e5` (≈ 0.1626). Together with `depthDressedEdgeRatio_lo`, the Wolfenstein-hierarchy
    suppression of the deeper inter-generation edge as an ACTUAL certified number over the derived ℝ. -/
theorem depthDressedEdgeRatio_hi : depthDressedEdgeRatio ≤ (16257 : Cut) / 100000 := by
  unfold depthDressedEdgeRatio
  rw [survivalCut_val]
  nlinarith [undressedEdgeRatio_hi]

/-! ## §6 — ★ STRICT SUPPRESSION (the hierarchy is real, not flat). -/

/-- ★ STRICT SUPPRESSION: the depth-dressed edge ratio is STRICTLY below the undressed ratio
    (`0 < survivalCut < 1`) — the deep inter-generation edge is genuinely suppressed relative to the
    geometric-mean expectation. The mixing hierarchy is real over the derived ℝ, not flat. -/
theorem depthDressed_strict_suppress : depthDressedEdgeRatio < undressedEdgeRatio := by
  unfold depthDressedEdgeRatio
  nlinarith [survivalCut_lt_one, survivalCut_pos, undressedEdgeRatio_pos]

/-! ## §7 — the leading (small-angle) depth-dressed mixing probability over the derived ℝ. -/

/-- The leading (small-angle) depth-dressed deep-edge mixing PROBABILITY over the derived ℝ: the
    SQUARE of the depth-dressed edge ratio (the small-angle regime; leading order, seed T8). -/
def depthDressedMixingProb : Cut := depthDressedEdgeRatio ^ 2

/-- The leading depth-dressed mixing probability `depthDressedEdgeRatio² ∈ [26422/1e6, 26430/1e6]`
    (≈ 0.0264) — from the depth-dressed edge-ratio bracket squared by monotonicity. LEADING ORDER
    (small-angle, seed T8): the FORCED object is the edge-ratio bracket; the square is the leading
    mixing probability, never the theorem-exact banked `sinSqMix`. -/
theorem depthDressedMixingProb_lo : (26422 : Cut) / 1000000 ≤ depthDressedMixingProb := by
  have h := mul_le_mul depthDressedEdgeRatio_lo depthDressedEdgeRatio_lo (by norm_num)
    (le_of_lt depthDressedEdgeRatio_pos)
  unfold depthDressedMixingProb; nlinarith [h]
theorem depthDressedMixingProb_hi : depthDressedMixingProb ≤ (26430 : Cut) / 1000000 := by
  have h := mul_le_mul depthDressedEdgeRatio_hi depthDressedEdgeRatio_hi
    (le_of_lt depthDressedEdgeRatio_pos) (by norm_num)
  unfold depthDressedMixingProb; nlinarith [h]

/-! ## §8 — non-vacuity (W8). -/

/-- NON-VACUITY: the depth-dressed edge ratio is a genuine number strictly in `(0,1)` — a real
    suppressed ratio, not `0` (no mixing) nor `≥ 1` (no suppression). -/
theorem depthDressedEdgeRatio_mem : (0 : Cut) < depthDressedEdgeRatio ∧ depthDressedEdgeRatio < 1 := by
  refine ⟨depthDressedEdgeRatio_pos, ?_⟩
  have h := depthDressedEdgeRatio_hi; linarith

/-- ★ NON-VACUITY: the dressing genuinely BITES over the derived ℝ — the depth-dressed ratio DIFFERS
    from the undressed ratio (`survivalCut = 2/3 ≠ 1`), so the hierarchy suppression is a real effect,
    not a relabelling. -/
theorem depthDressed_ne_undressed : depthDressedEdgeRatio ≠ undressedEdgeRatio :=
  ne_of_lt depthDressed_strict_suppress

/-! ## §9 — cascade-depth grounding (the two edges cross genuinely different-depth rungs). -/

/-- ★ CASCADE-DEPTH GROUNDING (inherited from rung-2): the shallow edge crosses the `ℂ → ℍ` doubling
    (loses COMMUTATIVITY) and the deep edge crosses the `ℍ → 𝕆` doubling (loses ASSOCIATIVITY) — the
    two edges are at genuinely different cascade depths, so the depth-dressed suppression is grounded
    on the banked cascade structure, not arbitrary. -/
theorem cascade_depth_grounding' :
    (¬ ∀ x y : H ℚ, x * y = y * x) ∧ (¬ ∀ x y z : O ℚ, (x * y) * z = x * (y * z)) :=
  cascade_depth_grounding

/-! ## The capstone. -/

/-- ★★★ THE CAPSTONE — the depth-dressed edge ratio as an ACTUAL NUMBER of the derived ℝ (the
    CKM/Wolfenstein mixing HIERARCHY suppression at leading order).

    Over the derived ℝ `Cut`, evaluating rung-2's structural `depthFrame_edge_ratio` (deep/shallow
    coupling `= edgeSurvival · (a₂/a₀)`, C524) at the banked N339/N340 ℝ-descent amplitudes `b0`,
    `b2` (the forced ℤ₃-Fourier cycle amplitudes at the derived phase `δ_B = 2/9`, `p = cutCos(2/9)`):
    `survivalCut = ((edgeSurvival:ℚ):Cut) = 2/3` (`survivalCut_val`, the banked self-blind Born
    fraction cast to `Cut`); the UNDRESSED ratio `b2/b0 ∈ [24383/1e5, 24385/1e5]`
    (`undressedEdgeRatio_*`); the DEPTH-DRESSED ratio `survivalCut · (b2/b0) ∈ [16255/1e5, 16257/1e5]`
    (`depthDressedEdgeRatio_*`) — one factor of `2/3` below the undressed ratio, the Wolfenstein
    hierarchy suppression as an ACTUAL number; STRICTLY below the undressed ratio
    (`depthDressed_strict_suppress`, `0 < survivalCut < 1`); the leading (small-angle) mixing
    probability `= (dressed ratio)² ∈ [26422/1e6, 26430/1e6]` (`depthDressedMixingProb_*`); a genuine
    number in `(0,1)` (`depthDressedEdgeRatio_mem`) that DIFFERS from the undressed ratio
    (`depthDressed_ne_undressed`, the dressing bites). Grounded on the banked cascade-depth theorems
    (`cascade_depth_grounding'`: `not_commutative` shallow, `not_associative` deep) and the cap forcing
    exactly three generations (`jdef Xwit Ywit ≠ 0`, N5).

    ⚠ HONEST GRADE (seed T8): LEADING ORDER (~few-%), NOT theorem-exact. The FORCED objects are the
    depth-dressed edge-ratio bracket (`survivalCut` derived, `b2/b0` the banked ℝ-descent numeral,
    ZERO continuous knobs) and its square. NO Wolfenstein `λ`, no mixing angle, no quark value is
    asserted; NO precision residual against measured CKM is chased (that would be fitting — the seed's
    STOP); no PDG number enters any proof. -/
theorem generation_depth_dressed_mixing_numeric :
    -- §1: the survival factor over the derived ℝ is the banked ℚ `edgeSurvival` cast
    (survivalCut = ((edgeSurvival : ℚ) : Cut))
    ∧ (survivalCut = 2 / 3)
    ∧ ((0 : Cut) < survivalCut ∧ survivalCut < 1)
    -- §2–§4: the undressed deep/shallow ratio bracket over the derived ℝ
    ∧ (depthDressedEdgeRatio = ((edgeSurvival : ℚ) : Cut) * (b2 / b0))
    ∧ ((24383 : Cut) / 100000 ≤ undressedEdgeRatio ∧ undressedEdgeRatio ≤ (24385 : Cut) / 100000)
    -- §5: ★★ the depth-dressed hierarchy bracket over the derived ℝ
    ∧ ((16255 : Cut) / 100000 ≤ depthDressedEdgeRatio ∧ depthDressedEdgeRatio ≤ (16257 : Cut) / 100000)
    -- §6: ★ strict suppression
    ∧ (depthDressedEdgeRatio < undressedEdgeRatio)
    -- §7: the leading (small-angle) mixing-probability bracket
    ∧ ((26422 : Cut) / 1000000 ≤ depthDressedMixingProb ∧ depthDressedMixingProb ≤ (26430 : Cut) / 1000000)
    -- §8: non-vacuity
    ∧ ((0 : Cut) < depthDressedEdgeRatio ∧ depthDressedEdgeRatio < 1)
    ∧ (depthDressedEdgeRatio ≠ undressedEdgeRatio)
    -- §9: cascade-depth grounding + the cap forcing exactly 3
    ∧ ((¬ ∀ x y : H ℚ, x * y = y * x) ∧ (¬ ∀ x y z : O ℚ, (x * y) * z = x * (y * z)))
    ∧ jdef Xwit Ywit ≠ 0 :=
  ⟨survivalCut_eq_edgeSurvival, survivalCut_val, ⟨survivalCut_pos, survivalCut_lt_one⟩,
   depthDressed_eq_edgeSurvival_amp, ⟨undressedEdgeRatio_lo, undressedEdgeRatio_hi⟩,
   ⟨depthDressedEdgeRatio_lo, depthDressedEdgeRatio_hi⟩, depthDressed_strict_suppress,
   ⟨depthDressedMixingProb_lo, depthDressedMixingProb_hi⟩, depthDressedEdgeRatio_mem,
   depthDressed_ne_undressed, cascade_depth_grounding', jordan_fails_H4⟩

end

end Phys.Algebra.DepthDressedMixing
