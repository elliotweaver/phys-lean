| SpacetimeSurfaceGravity | SELECT (theory-native; generic frontier — discharge two un-derived hypotheses of the banked black-hole thermodynamics sub-tower) | Phys/Algebra/SpacetimeSurfaceGravity.lean | THE SURFACE-GRAVITY DISCHARGE — the two black-hole metric HYPOTHESES the banked Hawking-temperature (N360) and entropy (N361) theorems carried are now THEOREMS: the surface gravity `κ_sg = (4M)⁻¹` and the horizon radius `r_h = 2M`, DERIVED over the derived ℝ `ContinuumQ.Cut`. THEOREM-route, foundations-only, exact over Cut. ── ★ THE MACHINERY BUILT (W1 step 1 — Mathlib-absence is not a wall): the RECIPROCAL DERIVATIVE on the derived ℝ, `hasCutDerivAt_inv : d/dr(r⁻¹) = −(r²)⁻¹` at `r ≠ 0` — the one calculus rule the banked BH tower lacked (absent from the foundation `CutHasDerivAt` API AND N361's local `HasCutDerivAt` {sq, const_mul, congr}), built from `continuousAt_inv₀` + the banked order topology (N34 `instOrderTopologyCut`): the difference quotient `t⁻¹·((x+t)⁻¹−x⁻¹)` equals `−((x+t)·x)⁻¹` on the punctured nbhd where `x+t≠0`, tending to `−(x²)⁻¹`. ── ★ THE THEORY-NATIVE ANCHOR (the 1/r² is FORCED by the return surface, not a posited metric): the fold's self-look-back closes on the banked 2-sphere (N328 `gaussSphereSolidAngle=4π`, the SAME surface that fixed `α*=1/42` via `84π` and gravity's `8πG` via `4π`, N359); a conserved source spreads over `A(r)=gaussSphereSolidAngle·r²`, so `E(r)=(gaussSphereSolidAngle·M)/(gaussSphereSolidAngle·r²)=M·r⁻²` — the solid angle CANCELS, the `1/r²` INDEPENDENT of the value `4π` (`invSquare_from_flux`). The metric function `f=1−2M·r⁻¹`; its derivative `f'(r)=2M·(r²)⁻¹` (`metricF_hasDerivAt`, const_mul + negation congruence on the difference quotient); the horizon `r_h=2M` is where `f` VANISHES (`metricF_horizon_zero`, `f(2M)=0`); and the surface gravity `κ_sg(M):=f'(2M)/2=(4M)⁻¹` (`surfaceGravity_eq`). ── WHAT IS PROVED (12 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}): hasCutDerivAt_inv (★★ the machinery); invSquare_from_flux (★ solid angle cancels ⇒ 1/r²); metricF (def f=1−2M/r); metricF_hasDerivAt (★★ f'=2M/r²); metricF_horizon_zero (★ f(2M)=0, r_h=2M derived); metricF_deriv_ne_zero (★ W8 the falloff bites, f'≠0); surfaceGravity (def f'(2M)/2); surfaceGravity_eq (★★★ κ_sg=(4M)⁻¹, hypothesis DISCHARGED); surfaceGravity_ne_wrong (★ W8 κ_sg=(4M)⁻¹≠(2M)⁻¹, dropping the horizon `2` gives the wrong value — the derivation bites); surfaceGravity_pos (★ W8 0<κ_sg for 0<M, Born positivity); hawkingTemp_derived (★★★ T_H=1/(gravCouplingCoeff·M)=1/8πM with the hypothesis GONE, re-homes N360 hawkingTemp_schwarzschild supplying the DERIVED κ_sg); hawkingPeriod_derived (★★ β=gravCouplingCoeff·M=8πM, re-homes N360); bhEntropy_area_derived (★★ S=A/4 on the DERIVED horizon r_h=2M, re-homes N361 bhEntropy_eq_area_quarter); surface_gravity_structure (★★★ CAPSTONE welded, statable from no single conjunct alone — anchored on the derived-ℝ return-surface objects, the free-floating anchor of the whole GR sub-tower N359/N360/N361, NOT the octonion arena). ── ⭐ ONE CAUSE MANY TERMINATIONS: the SAME derived return surface gaussSphereSolidAngle=4π (the fold's Born-turn closure, N328) that normalized α* (84π, N329) and gravity's 8πG (N359) here forces the inverse-square law whose horizon and surface gravity fix T_H and S=A/4 — one return surface, the coupling, the coupling coefficient, and now the black-hole thermodynamics. ── DISSOLUTION/MOAT: the field POSITS Schwarzschild + COMPUTES κ_sg=f'(r_h)/2 by matching Euclidean regularity; the theory reads the 1/r² off the fold's own return surface (the 4π VALUE irrelevant to the exponent) and derives κ_sg from the reciprocal-derivative rule built here — the two hypotheses N360/N361 carried are discharged, not matched. ── HONEST GRADE: discharges the HORIZON-LEVEL metric facts (r_h=2M, κ_sg=(4M)⁻¹) — exactly what T_H and S=A/4 consume; r_h=2M is a theorem GIVEN f (the zero of f); the full nonlinear Lorentzian INTERIOR metric on a topologized STVC stays CHILDED (N349/N363, a heavy build). ── NO-FIT (G1/G2): no measured mass, no ℏ/G/c numerics; (4M)⁻¹, 2M/r², r_h=2M all FALL OUT (reciprocal-deriv rule + field_simp/ring); measured entropy/temperature = removable prose only; M the single symbolic dimensionful anchor (G3). ── ℝ-VIGILANCE (G5): ALL over the derived ℝ Cut; the reciprocal derivative from continuousAt_inv₀ + the banked order topology; NO Mathlib-ℝ/Real.pi/Real.inv as content — Mathlib topology/field = MACHINERY on the derived carrier. ── FREE-FLOATING check PASS: every production TYPE cites the banked Cut objects (HasCutDerivAt N361, gaussSphereSolidAngle N328, hawkingTemp/hawkingPeriod N360, bhArea/bhEntropy/gravCouplingCoeff N361/N359) — the reciprocal derivative is stated on Cut and IMMEDIATELY consumed by the metric function's surface gravity → the discharged T_H. NOT a generic calculus lemma. ── PHYSICS-WORDS-REMOVABLE: delete gravity/horizon/black-hole/surface-gravity/Schwarzschild/Hawking/metric → over Cut: HasCutDerivAt (fun r=>r⁻¹) (−(x²)⁻¹) x for x≠0; (g·M)/(g·r²)=M·r⁻²; HasCutDerivAt (fun r=>1−2M·r⁻¹) (2M·(r²)⁻¹) r; 1−2M·(2M)⁻¹=0; (2M·((2M)²)⁻¹)/2=(4M)⁻¹; hawkingTemp((4M)⁻¹)=1/(gravCouplingCoeff·M); gravCouplingCoeff·M²/2=(gaussSphereSolidAngle·(2M)²)/4. Pure calculus/field arithmetic on banked objects. Build clean (lake build Phys EXIT 0, Built SpacetimeSurfaceGravity 7.6s, LIGHT — continuousAt_inv₀/tendsto_congr'/field_simp/ring/linarith + banked hasCutDerivAt_const_mul/gaussSphereSolidAngle_pos/hawkingTemp_schwarzschild/bhEntropy_eq_area_quarter cites, no brute, no heartbeat raise). Scratch probes workbench/inverse-square-bh/{PREREG.md,Probe1-4.lean,AxCheck.lean} (all EXIT 0, isolated reaudit 12 decls foundations-only). GATE-FAST D0–D6: pending (background). C527 bites 1=527 (tied to surfaceGravity_eq + surfaceGravity_ne_wrong via sgFlag_forced). SELECT node (fresh worker, generic frontier); hands ONE directed successor — the generic SELECTION ticket (a fresh worker selects the next theory-native beat). |
