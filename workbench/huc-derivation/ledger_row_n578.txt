

N578 — TopAnchorInsertionPinning (ARC-M/O2 KEYSTONE, t_c748d2d6, OWNER-AUTHORIZED huc CAMPAIGN node 2): DERIVE THE UP-SECTOR TOP-AT-ANCHOR BORN-SQUARE PINNING Mu^2=M*ewClosureRatio (=v) AB INITIO — promote the banked arc-O2 top-link=1 (empty cascade-insertion product) from mechanism-flagged prose to a THEOREM of Cut, closing the last remaining anchor half of N577's huc reduction. | Emergent (arc-M/O2 dimensional-transmutation keystone) | ✅ | gate-fast D0–D6 GREEN: C577 bites 1=577; 12 newly-banked theorems foundations-only [propext,Classical.choice,Quot.sound] (independently axiom-audited vs the freshly-built olean); append-only leaf commit. THE THEORY-NATIVE HEART — the empty cascade-insertion product. The banked arc-O2 chain (ScaleTowerDischargeArcM, N417) writes the absolute-scale cascade as the electroweak anchor dressed by cascade-insertion factors absScaleChainR φ τ ℓ=ewClosureRatio·(φ·s1BornProb)·τ·ℓ. The down/light sector carries a NON-empty insertion bundle [cutCos foldPhase·s1BornProb, cutCos(2/3)] (each a strictly-positive sub-unit Born amplitude) that suppresses it STRICTLY BELOW the anchor (banked absScaleChainDerived_lt_anchor). The top is the HEAVIEST cascade member: its insertion DEPTH is ZERO ⇒ its insertion list is EMPTY ⇒ the empty insertion product is the identity 1 (List.prod_nil, universal fact of the insertion monoid — NOT a fitted 1) ⇒ its chain value collapses to the anchor itself, ewClosureRatio=v/M. So Mu^2=M·insertionChain []=M·ewClosureRatio (top_at_anchor_pinning). THE DISCRIMINATOR IS FORCED BY THE COUNTING, NOT TARGET-SHAPED: at-anchor IFF trivial-insertion (atAnchor_iff_trivial_insertion: since ewClosureRatio≠0, insertionChain L=ewClosureRatio ↔ L.prod=1); one sub-unit insertion ⇒ strictly below (one_subunit_insertion_below_anchor); the banked non-empty bundle lands strictly below (downLight_below_anchor, wrapping absScaleChainDerived_lt_anchor). So the top's ZERO insertion depth FORCES it to the anchor; any non-zero depth would put it strictly below. huc FULLY DERIVED: feeding the top-at-anchor pinning + the definitional floor half into N577 huc_from_born_anchors yields (Mu/Mlam)^2=1/confBundle as a THEOREM (huc_fully_derived) — the candidate promoted, the N576 double-count question CLOSED, the last anchor half derived. Non-vacuity: top_pinning_satisfiable (M=1, Mu=cutExp(−14π/3)=√(v/M)), anchor_split_strict (non-empty bundle < empty=anchor), top_above_floor (confinementScaleRatio<ewClosureRatio, Λ/v=confBundle<1). NO-FIT: measured 1/α(0), v, m_t, Λ_QCD, PDG masses removable prose; no empirical number in any statement/proof; the pinning lands where the empty insertion product + the banked anchor put it. UNBROKEN (ewClosureRatio/ewClosureRatio_eq/_pos the banked n=9 rung; absScaleChainDerived/absScaleChainDerived_eq_anchor_mul/absScaleChainDerived_lt_anchor the banked arc-O2 chain; huc_from_born_anchors the banked N577 reduction; cutCos/foldPhase/s1BornProb/cutCos_two_thirds the banked derived amplitudes; confinementScaleRatio_eq/confBundle the banked colour/bundle rungs; cutExp_strictMono/cutPi_pos/List.prod_nil/mul_left_cancel₀ standard machinery; all over the DERIVED ℝ Cut) + COMPLETE (12/12 foundations-only, no sorry/axiom/native_decide/maxHeartbeats) + WORDS-REMOVABLE (delete top/up/down/light/confinement/anchor/mass/scale/cascade/insertion/electroweak/Born → insertionChain L=ewClosureRatio·L.prod; insertionChain []=ewClosureRatio; absScaleChainDerived=insertionChain [derived bundle]; insertionChain L=ewClosureRatio↔L.prod=1; 0<c<1⇒insertionChain [c]<ewClosureRatio; Mu^2=M·insertionChain []⇒Mu^2=M·ewClosureRatio; (Mu/Mlam)^2=1/confBundle; pure arithmetic/order of Cut) + NOT free-floating (every TYPE mentions the banked ewClosureRatio/absScaleChainDerived/cutCos/foldPhase/s1BornProb/confBundle/confinementScaleRatio) + NO BRIDGE (the top-link=1 is DERIVED as the empty insertion product, not asserted; huc is a theorem not a hypothesis). SELECTED (DIRECTED successor of N577, the last open anchor half of the huc campaign). MEASURE-FIRST (W9): workbench/huc-derivation/ (probe_node2 EXIT 0 the full route end-to-end; probe_node2b EXIT 0 the sharp discriminator; audit_check EXIT 0 all 12 foundations-only). Production olean LIGHT (lake build 6.9s the module). RESUME: single-flight run 905; PREREG-in-module-doc + probes + production + costume + audit + wiring + gate + docs all this run; committed BEFORE the gate. THE NEXT NODE — the ONE directed successor: recompose the freeze-out endpoint with huc now a THEOREM (edgeLight_pinned no longer needs huc as a hypothesis) → re-run the D7 attestation → re-narrow through the banked instrument ladder → D7 RESTATE 1/α(0) at ≥15 sig digits → D8.