# N362 — arc-E E6: THE PAGE CURVE / INFORMATION PRESERVATION (grounded on the banked FINITE bit-count)

## SELECT (fresh worker, theory-native FIRST)

**Target:** E6 (docs/SEED_GR_BLACKHOLE_COSMOLOGY.md §E6), realized as the **Page curve grounded on the
banked FINITE entropy** N361, NOT a generic entanglement-entropy abstraction and NOT an unbanked
η-transport (verified: banked "faithful" objects are rep-faithfulness G₂/colour/Ricci — a different
thing; the ticket's abstract "η faithful ⇔ finite depth" object is NOT banked).

**Theory-native hook (fold-causal):** fold → self-blindness two-valued → `s₀ = cutLog 2` per cell
(N361) → `S = N·s₀` a FINITE binary bit-count (`bhEntropy_cell_decomposition`) → the total info
budget is FINITE → information is redistributed between hole and radiation, never destroyed → the
radiation's fine-grained entropy returns to 0 at complete evaporation. The classical "info loss"
puzzle is the divergent static-coordinate face — the SAME artefact N360's Euclidean closure resolved.

**Anti-bullshit gate:** passed in full (see kanban comment 267). Every TYPE cites the banked
`bhEntropy`/`bhArea`/`cellArea`/`cellEntropy` over the derived `Cut`; `pageRadEntropy` is built FROM
`bhEntropy`; the finiteness bound reduces to N361's `bhEntropy_cell_decomposition`.

## HONEST GRADE (SEED §E6: "Mechanism-grade; carry the honest caveat")

- **THEOREM-GRADE (genuine theorems about the banked `bhEntropy`):** start=0, end=0, ≤ total,
  ≤ the N361 finite cell-count, =total/2 at the crossing (the Page point), and — W8 — the final
  radiation entropy ≠ the total (info NOT lost). All foundations-only over the derived `Cut`.
- **MECHANISM-GRADE (flagged prose, NEVER asserted as a theorem):** the identification "fine-grained
  radiation entropy = `min`(emitted-coarse, remaining-coarse)" rests on the global state being pure =
  the fold's TOTAL self-blindness (the whole is blind to itself ⟹ pure). The `min` is a DEFINITION
  (like N360's `κ_sg=(4M)⁻¹` and N361's `r_h=2M` explicit hypotheses); its physical justification is
  the flagged mechanism layer. NO "global purity" theorem is posited.

## MEASURE (W9)

Full production set (16 decls) compiled clean in 8.5s (user 2.9s) — `probe_full.lean`. No
instrument-cost risk. `Cut` is `LinearOrder + Field + IsStrictOrderedRing`, so `min`/`min_eq_left`/
`min_eq_right`/`min_self`/`min_le_*` all apply.

## GUARDS

- G1 no asserted value (the whole curve DERIVED from the banked `bhEntropy`; the Page point =total/2
  DERIVED, not asserted).
- G2 no empirical number in any proof (Page time / any measured quantity: removable prose only).
- G3 M/M₀ symbolic dimensionful anchors; the entropy ratios/bounds dimensionless-derived.
- G4 derived not posited (info preservation = the finiteness of the banked cell-count).
- G5 physics-words-removable (delete information/Page/radiation/black-hole/horizon/quantum → pure
  statements about `min(bhEntropy M₀ − bhEntropy M, bhEntropy M)`).
- G6 foundations-only ⊆ {propext, Classical.choice, Quot.sound}; no sorry/axiom/native-decide/
  maxHeartbeats-raise. Built on the banked derived `Cut` + banked `bhEntropy`/`bhArea`/`cellArea`/
  `cellEntropy`; NO Mathlib Real/log as content.
- W8 non-vacuity: `pageRad_end_ne_total` — a WRONG claim (info lost: final radiation entropy = total)
  fails; costume C387 pins it.

## ONE SUCCESSOR

Directed child toward E7 (the singularity = the closure surface: BH interior ends on the banked
closure-transition surface, a single Kasner leg, K(r_cross)=1) if E6 lands.
