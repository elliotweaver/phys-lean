| RecombinedEndpoint | D2b (ARC-D REMAINDER, owner-directed t_fb4ef585 decision C) — compose the banked Z2 two-channel recombination weld into the endpoint tower | Phys/Algebra/RecombinedEndpoint.lean | THE CHANNEL RECOMBINATION WELD — the photon zero-momentum EM coupling is the RECOMBINATION of the two derived electroweak channels (Z2 TwoChannelDescent): 1/α_em = 1/α₂ + 1/α_Y, with sin²θ_W = a₂/(a₂+a_Y) the recombination weight. The endpoint tower (X6/Z6/D7) composed only the SINGLE fold coupling from the octonion ceiling; THIS node composes the banked two-channel weld — ASSEMBLY of banked objects, not a new derivation (the DOMINANT summand the D7/D8 gap arithmetic detected, owner-corrected from "route-not-yet-found" to "banked weld never composed"). THEOREM-route, foundations-only, exact/certified over the derived ℝ Cut. ── ★ THE ONE STRUCTURE (Q = T₃ + Y, cross-term traceless — the driver of the FREE weld): chargeTraceDepth = Tr(Q²) = Tr(T₃²) + Tr(Y²) = isoDepth + hyperDepth (16/3 = 2 + 10/3, census_additive, falling out of banked chargeTraceDepth_eq/isoDepth_eq/hyperDepth_eq). bandScreen is LINEAR in the census (κ·census·cutLog r), so the two channel screenings over the SAME ratios SUM to the single charge screening (bandScreen_channel_split; totalScreeningWith_channel_split by induction on the finite band list). Hence the two channels' summed screening EQUALS the single-channel screening; the ONLY new content is the SECOND (hypercharge) ceiling. ── WHAT IS PROVED (18 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}): census_additive (★★ 16/3=2+10/3, Q=T₃+Y); bandScreen_channel_split (★★ per-band split); totalScreeningWith (def per-channel tower screening) + totalScreeningWith_channel_split (★★ list split by induction); isoTower/hyperTower/invAlphaEM (defs — the two channel towers + the recombined EM coupling as their sum); invAlphaEM_two_channel (★★★ the weld: invAlphaEM = isoTower + hyperTower, each carrying its own ceiling+census); invAlphaEM_closed_charge (★★★ = 112 + totalScreeningWith chargeTraceDepth = two ceilings 42+70 + census-split screening); totalScreeningWith_eq_totalScreening (reduces to banked X6 totalScreening on a full-charge-census list); invAlphaEM_eq_single_plus_hyper (★★★ = invAlphaZero + 70 — the +70 IS invAlphaYHigh = (5/3)·invAlphaHigh, banked, NOT a dial); completeBandList_all_charge (every band of the complete tower at census 16/3); composed_endpoint (★★★ invAlphaEM (completeBandList chargeTraceDepth) = invAlphaZero (…) + 70 — the D7 partial numeral lifted by the second ceiling); recombination_weight (★★ invAlpha2High/(invAlpha2High+invAlphaYHigh) = 42/112 = 3/8) + recombination_weight_eq_highMixing (★★ = Z2's banked sinSqRun_at_high — the weld's weight is a banked theorem); composed_bracket (★★★ [D7_lo+70, D7_hi+70]); composed_pinned (★★★ decimal window (1379475880261/1e10, 1379475880262/1e10) = 137.9475880261…); plus70_load_bearing (★ W8 invAlphaEM≠invAlphaZero); both_channels_present (★ W8 isoDepth≠0∧hyperDepth≠0); census_split_ne_trivial (★ W8 2≠10/3 asymmetric); weight_ne_wrong (★ W8 3/8≠1/4); recombinedEndpoint_landing (★★★ CAPSTONE welded, statable from no single conjunct alone — anchored on the banked derived-Cut objects invAlphaZero/invAlpha2High/invAlphaYHigh/isoDepth/hyperDepth/chargeTraceDepth/completeBandList/theNumeral_bracket/sinSqRun). ── ⭐ ONE CAUSE: the SAME Q=T₃+Y that gives the banked charge census 16/3 (X3) here SPLITS it additively into the two channel censuses (2, 10/3), making the two-channel recombination weld a FREE consequence of census additivity + bandScreen linearity — the second ceiling (70=(5/3)·42) is the only new content, and it IS the ~69 dominant summand. ── DISSOLUTION/MOAT: the field MEASURES 1/α_em(0) via hadronic vacuum polarization (~2e-10 error bar) and treats the electroweak ceilings as measured inputs; the theory DERIVES both ceilings (42 = cascade stop, 70 = (5/3)·42 GUT normalization) and composes the recombination from census additivity — no measured input, the +70 falls out of invAlphaYHigh. ── HONEST GRADE: THEOREM-EXACT NOW (census additivity, channel split, weld, closed form, +70 summand, weight 3/8, composed endpoint, composed certified bracket 137.9475880261…). ROUTE-NOT-YET-FOUND (BOUNDED refinements, NEVER asserted/fitted): the Z2 isospin anti-screening magnitude s (bounded 0<s<isoDepth) and the freeze-out edge ratios rb,rc,rl (the quark ℝ-descent, entering ONLY through the banked bounded freeze-out term ≤ κ·9·cutLog R, D8-freezeout — my rung-3 category-error catch stands). With the weld composed, these no longer close any large gap — they are the final narrowing toward the ≥15-digit numeral. ── NO-FIT (G1/G2): no 137, no 137.036, no measured α/sin²θ_W/uncertainty; +70/112/3/8/the composed bracket all FALL OUT of banked ceilings + census additivity + the banked D7 bracket; measured value = removable prose only; no bracket/count chosen because of where 137.036 sits (the recombined LO lands at 137.9476, ABOVE measured — the bounded refinements remain, never fitted). ── ℝ-VIGILANCE (G6): ALL over the derived ℝ Cut; bandScreen/totalScreening/invAlphaZero/the ceilings from banked N342/X6; NO Mathlib-ℝ as content — List/linarith/norm_num/ring = MACHINERY. ── FREE-FLOATING check PASS: every production TYPE cites the banked Cut objects (invAlphaZero/totalScreening/bandScreen/chargeTraceDepth X6, invAlpha2High/invAlphaYHigh/isoDepth/hyperDepth/sinSqRun N342/Z2, completeBandList Z6, theNumeral_bracket/theNumeral_pinned D7). NOT generic. ── PHYSICS-WORDS-REMOVABLE: delete coupling/channel/isospin/hypercharge/photon/recombination/electroweak → over Cut: chargeTraceDepth=isoDepth+hyperDepth; bandScreen isoDepth r + bandScreen hyperDepth r = bandScreen chargeTraceDepth r; invAlphaEM bs = 112 + totalScreeningWith chargeTraceDepth bs; on a full-charge-census list invAlphaEM bs = invAlphaZero bs + 70; invAlpha2High/(invAlpha2High+invAlphaYHigh)=3/8; composed value in [D7_lo+70,D7_hi+70]. Pure Cut arithmetic on banked objects. Build clean (lake build Phys.Algebra.RecombinedEndpoint EXIT 0, Built 13s, LIGHT — List.map_congr_left/List induction + linarith/norm_num/ring + banked cites, no brute, no native_decide, no heartbeat raise). Scratch probes workbench/recombined-endpoint/{PREREG.md,Probe1.lean,Probe2.lean,AxCheck.lean} (all EXIT 0, isolated reaudit 18 decls foundations-only). GATE-FAST D0–D6: pending (background). C529 bites 1=529 (tied to plus70_load_bearing + both_channels_present + census_split_ne_trivial + weight_ne_wrong via recFlag_forced). OWNER-DIRECTED node (ARC-D REMAINDER, back on the rail); hands ONE directed successor — re-narrow the recombined sum → the D7 restatement AS 1/α(0) at the ≥15-digit floor. NO generic SELECT (rail: M1 not yet handed). |
