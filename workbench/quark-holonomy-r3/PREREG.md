# PREREG — quark ℝ-descent ascent, rung 3 (t_f765d780)

## STATUS: DIRECTED ROUTE-FINDER child. Target pre-decided by rung-2's hand-forward. My first job is
## the S4 anti-bullshit gate on THIS directed target, then execute. (NOT a generic SELECT.)

## THE DIRECTED TARGET
EVALUATE rung-2's structural `depthFrame_edge_ratio` (deep/shallow coupling = `edgeSurvival·(a₂/a₀)`)
over the DERIVED ℝ `Cut`, at the banked ℝ-descent generation numerals, to state the DEPTH-DRESSED
leading-order mixing HIERARCHY as an ACTUAL certified two-sided rational bracket over `Cut`.

## THE LAUNCH PLATFORM (all banked, all read + compile-verified)
- rung-2 `edgeSurvival = tr Poth/3 = 2/3` (ℚ, `edgeSurvival_val`), and the structural
  `depthFrame_edge_ratio : deep/shallow = edgeSurvival·(genVec 2/genVec 0)` — the a₂/a₀ hierarchy
  one factor of edgeSurvival below undressed (Phys/Algebra/OctonionJordanGenerationEdgeBornSurvival.lean).
- N339/N340 ℝ-descent amplitudes over `Cut`: `b0 = 1+w·p`, `b2 = 1−w·(p−r)/2` (= genVec 0, genVec 2
  at M=1, the derived phase δ_B=2/9), with brackets `b0∈[237943/1e5,237944/1e5]`,
  `b2∈[5802/1e4,58022/1e5]`, `b0_pos`,`b2_pos` (Phys/Algebra/GenerationMassRatiosNumeric.lean).
- The banked derived ℝ `Cut` (Field + LinearOrder + IsStrictOrderedRing).

## THE COMPOSITION (the numeric landing) — arithmetic VERIFIED (workbench/quark-holonomy-r3/check.py)
The undressed deep/shallow geometric-mean ratio is `a₂/a₀ = (a₁a₂)/(a₀a₁) = genVec 2/genVec 0`, which
over `Cut` at the derived phase is `b2/b0`. So:
- UNDRESSED ratio `b2/b0 ∈ [24383/1e5, 24385/1e5]` (≈0.2438; the un-suppressed geometric-mean ratio).
- DEPTH-DRESSED ratio `depthDressedEdgeRatio := ((edgeSurvival:ℚ):Cut)·(b2/b0) ∈ [16255/1e5, 16257/1e5]`
  (≈0.1626; ONE factor of edgeSurvival=2/3 below undressed — the Wolfenstein-hierarchy suppression as
  an ACTUAL number, DERIVED).
- STRICT suppression `depthDressedEdgeRatio < b2/b0` (edgeSurvival<1: the hierarchy is real).
- LEADING mixing prob (small-angle) = the SQUARE `depthDressedEdgeRatio^2 ∈ [26422/1e6, 26429/1e6]`
  (≈0.0264) — the depth-dressed deep-edge mixing probability at leading order (NOT claimed = banked
  sinSqMix; it is the square of the derived edge ratio, small-angle regime, honestly leading-order).
- non-vacuity: dressed ∈ (0,1); dressed ≠ undressed (the dressing bites over Cut).
Cast tie `((edgeSurvival:ℚ):Cut) = 2/3` compiles (push_cast; ring). All 4 probe families EXIT 0.

## ANTI-BULLSHIT GATE (S4) — run honestly, in writing
1. **FOLD-CAUSAL.** fold → self-blindness → each cascade doubling is one more self-look-back step,
   surviving with the self-blind Born fraction (n−1)/n=2/3 (N305 `born_fraction_others`, the SAME
   fact that forced the Born split/Koide/δ_B). rung-2 DERIVED (not posited) that the deep (ℍ→𝕆)
   edge carries one extra such factor → the deep/shallow coupling ratio = edgeSurvival·(a₂/a₀). This
   node EVALUATES that forced ratio over the DERIVED ℝ at the banked cutCos(2/9)-descent amplitudes.
   Each arrow forced; the number is the shadow of "cascade depth × self-blindness."
2. **DISSOLUTION.** Standard flavor physics POSITS the Wolfenstein hierarchy value (Froggatt–Nielsen
   posits a horizontal-U(1) charge tower and FITS λ). The theory DERIVES the hierarchy suppression
   as an actual number with ZERO posited charges and ZERO continuous knobs — the a₂/a₀ ratio is the
   banked ℝ-descent numeral (from cutCos(2/9)), the 2/3 is the banked self-blind fraction. The wall
   the field grinds (why THIS hierarchy) dissolves into a bracket read off the fold's own measures.
3. **GOAL-WORDS-REMOVABLE.** Delete quark/lepton/mixing/CKM/Wolfenstein/mass/generation: over the
   derived `Cut`, with the banked `b0=1+w·p`, `b2=1−w·(p−r)/2` (p=cutCos(2/9), w=cutSqrt 2,
   r=cutSqrt(3−3p²)) and the banked rational `edgeSurvival=2/3`, one has `b2/b0∈[24383/1e5,24385/1e5]`
   and `((edgeSurvival:ℚ):Cut)·(b2/b0)∈[16255/1e5,16257/1e5]`, strictly below `b2/b0`. Pure math
   survives; the reason it is the next node (evaluate rung-2's structural ratio as a number) survives.
4. **NAME THE TEMPTATION.** The saturated-worker temptation: re-derive N343's mixing-angle brackets
   (`sinSqMix mass_i mass_j`) under a new name, or FIT the dressed number to the measured Cabibbo λ.
   I REFUSE both — N343 is banked (W4.5) and fitting is forbidden. My target is the ONE unbuilt
   numeric object rung-2 explicitly deferred (route-not-yet-found): the depth-DRESSED edge ratio as a
   number = edgeSurvival × the banked ℝ-descent generation ratio. Genuinely new; never fitted.
5. **HONESTY.** The honest theory-native result is the depth-dressed edge-ratio bracket (and its
   square) over `Cut`, at LEADING ORDER (seed T8 caveat — NOT theorem-exact like the masses). I do
   NOT chase precision residuals against measured CKM (that is fitting, the seed's explicit STOP). No
   quark value, no Wolfenstein λ, no PDG number enters any proof; data comparison is removable prose.

## GUARDS
- No Wolfenstein λ / mixing angle / quark value asserted (2/3 = banked edgeSurvival; b0/b2 = banked
  N339/N340; every bracket a LANDING composed from banked numerals, no decimal posited-and-checked).
- No fit to PDG; no empirical number in any proof; comparison to data is removable prose only.
- BANKED derived ℝ (`Cut`, `b0`,`b2`,`edgeSurvival`) — NO Mathlib-ℝ as content.
- Foundations-only: `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`.
- Free-floating rail: every production TYPE mentions the banked derived objects (`edgeSurvival`,
  `b0`,`b2` over `Cut`, the cascade `not_commutative`/`not_associative`, the cap `jdef Xwit Ywit≠0`)
  — NOT a generic ratio statement. `depthFrame_edge_ratio` cited as the ℚ-structural parent.
- ONE successor on the chain tail, no fork.

## COST BUDGET / KILL
All content is div-bracket `le_div_iff₀`/`div_le_iff₀` + `nlinarith` on banked Cut brackets +
`push_cast` casts + rational `nlinarith`. Probe EXIT 0 in <1 min. Expected LIGHT (gate-fast ~1 min).
KILL: any single obligation >60s → decompose per-bracket (each its own banked lemma). No unbounded
compile; no heartbeat inflation; no brute normalizer on expanded coordinates.

## PRODUCTION FILE
`Phys/Algebra/GenerationDepthDressedMixingNumeric.lean`, namespace `Phys.Algebra.DepthDressedMixing`.
Capstone `generation_depth_dressed_mixing_numeric`. Node label `GenDepthDressedMixingNumeric`. C525.
