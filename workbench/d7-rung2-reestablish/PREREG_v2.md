# PREREG v2 — COMPOSE THE FOLD-FORCED DEPTH-2 RUNNING into recomposedEndpoint (owner gate-enforcement, superseding the inherited "narrow 136.307" framing) → re-attest truly-closed → re-narrow
## (t_3a070796, run 890; grove-manager owner directives 19:20 + 19:23 — the operative charter)

## THE MID-TURN CORRECTION (why the ticket body is superseded)
My first kanban_show returned a STALE snapshot (comments:[]). The fresh show (+ the AUDIT-NOTE precedent
from the reverted 82d8aaa) revealed two grove-manager (owner-authority) directives posted during run 889
that SUPERSEDE the inherited "narrow 136.307 toward ≥15 digits" framing:
 - 19:20 "the attestation you inherited is INCOMPLETE by the board's own record; narrowing 136.307 now
   would polish a provably incomplete sum." Two named omissions:
   (1) the banked DEPTH-2 RUNNING content is neither composed nor proven zero — the D7 gate clause fails;
   (2) the above-floor term is only a κ·9·cutLog R BOUND — cannot support ≥15 digits.
 - 19:23 ordered directive: (1) compose the depth-2 running (kernel-forced value); (2) derive above-floor
   to <1e-12; (3) re-attest truly-closed; (4) re-narrow; (5) restate; (6) D8. NO-BLOCK mandate stands.

VERIFIED INDEPENDENTLY (not taken on faith):
 - `recomposedEndpoint = 2425/18 + kappaLeading·C_inner` where `kappaLeading = (1/cutPi)/3` is the BARE,
   LEADING (first-depth) loop factor. The depth-2 running (dress κ by 1/(1−w)) is genuinely NOT composed.
   grove-manager claim (1) CONFIRMED.
 - The above-floor term is `≤ κ·9·cutLog R` (a bound, orders wider than 1e-11). Claim (2) CONFIRMED.

I reverted my premature Rung2 (11c746d → 6a68dcd); tree back to bf29483-equivalent, 0 references.

## THE FRONT (owner step 1: compose the FOLD-FORCED depth-2 running — the theory-native, non-fit move)
The low-tail RUNNING content of recomposedEndpoint is exactly `kappaLeading·C_inner` (the two below-Λ
loop terms: leptonTailScreen = kappaLeading·2·cutLog(mass0/mass1), and the freezeoutStaircase running
part kappaLeading·[(7/3)L_mb+(11/3)L_b0−(5/3)L_b2]). The base `2425/18 = 7179/54 (confinement way-point)
+ 16/9 (census constant)` is NON-running (no loop factor) and is preserved (way-point preservation:
DepthTwoBandVaryingRunning, weight 0 on high bands).

The census-in-census (depth-2 running) DRESSES the loop factor: `kappaLeading → kappaLeadingDressed w =
kappaLeading/(1−w)` (banked X5 `kappaLeadingDressed_eq_div`). The fold FORCES the single weight
`w = depthWeight = 1/3` (banked D3/`LowTailFoldForcedWeight.foldForcedWeight_value`, the mean Born
self-overlap over the ONE return surface — band-INDEPENDENT; band-varying = forbidden fit, proven).
⇒ dressing factor `1/(1−1/3) = 3/2`. This is NOT chosen — every piece is banked and fold-forced.

THE COMPOSED ENDPOINT (kernel-forced value, mpmath dps=40 verified, workbench/.../composed.py):
  recomposedEndpointDressed = 2425/18 + kappaLeadingDressed(depthWeight)·C_inner
                            = 2425/18 + (3/2)·kappaLeading·C_inner  ≈ 137.0997
(the bare 136.307 was the incomplete sum; composing the fold-forced depth-2 running moves it to ≈137.0997
— now ABOVE 137, ~0.064 from the measured 1/α(0)≈137.036 [REMOVABLE ORIENTATION PROSE ONLY]).

## THE THEOREMS (production; every TYPE mentions banked derived Cut objects)
A. recomposedEndpointDressed def := 2425/18 + kappaLeadingDressed depthWeight · C_inner (the composed,
   depth-2-running endpoint).
B. recomposedEndpointDressed_closed : = 2425/18 + (3/2)·kappaLeading·C_inner  — via kappaLeadingDressed_eq_div
   (depthWeight_pos/lt_one) + depthWeight_eq (the fold-forced factor 3/2). The tie is banked, non-fit.
C. depthTwo_dresses_recomposed : recomposedEndpointDressed = recomposedEndpoint + (1/2)·kappaLeading·C_inner
   ∧ recomposedEndpoint < recomposedEndpointDressed  — the composition STRICTLY RAISES the endpoint
   (the depth-2 running is real, ONE-LAW-upward; W8 content).
D. dressed_offsets_base : recomposedEndpointDressed − (3/2)·kappaLeading·C_inner = 2425/18  — the base
   (way-point + census constant) preserved (way-point-consistency, non-fit adjudicator).
E. recomposedEndpointDressed_normalform : = 2425/18 + kappaLeading·(3·cutLog(mass0/mass1) + (7/2)·L_mb
   + (11/2)·L_b0 − (5/2)·L_b2)  — the (3/2)-scaled coefficients, for the narrowing core (W9).
F. recomposedEndpointDressed_bracket : a certified two-sided rational bracket of the composed endpoint
   AS 1/α(0) (via a W9 abstract arithmetic core over the banked edge-log + κ brackets — Rung1-grade
   ~9 sig digits from the existing s6/ext6 instruments; deeper narrowing = the next rung).
G. capstone (welded, non-hollow).

## THE HONEST GRADE (SOUL route-COMPLETE vs route-NOT-YET-FOUND)
THEOREM-EXACT / CERTIFIED NOW: A–E exact identities/inequalities of Cut (foundations-only, no error bar) —
the composition, the fold-forced factor 3/2, the upward direction, the base preservation, the normal form.
F a certified rational bracket of the composed endpoint AS 1/α(0) at the current instrument depth.
ROUTE-NOT-YET-FOUND (NAMED, handed forward, NEVER asserted/fitted): (i) the ≥15-sig-digit floor over the
COMPOSED (dressed) endpoint — deeper 1/cutPi + s7 mass grids, no new axiom, no fit; (ii) owner step (2):
the above-floor occupancy series to a <1e-12 certified value/bracket (the same gForm counting that banked
floor=16, at n≥2) — a genuine construction, the successor's charter. NOT a block: forward autonomous
structural work; no path here fits a measured value or needs a new axiom.

## W9 COST BUDGET / KILL
A–E are assembly of banked foundations-only theorems (kappaLeadingDressed_eq_div, depthWeight_eq,
recomposedEndpoint_normalform); closers ring/field_simp/linarith over banked lemmas — near-zero cost.
F reuses the Rung1 abstract-core pattern with (3/2)-scaled coefficients + the banked s6/ext6 brackets.
KILL: any single obligation >90s ⇒ decompose. The endpoint assembly stays in an abstract arithmetic core
(transcendentals out — W9). No monolithic brute on an expanded coordinate goal.

## G1–G8 SEED HARD GUARDS
G1 NO ASSERTED VALUE — the factor 3/2 FALLS OUT of depthWeight_eq (=1/3) via kappaLeadingDressed_eq_div;
   the composed value is computed, not premised.
G2 NO EMPIRICAL NUMBER — no 137.036/PDG in any statement/proof; measured value removable prose; the
   composition direction is FORCED by κ(w)>κ₀ (self-blindness), NOT by where the measured value sits.
   Explicit NO-FIT: the weight is depthWeight=1/3, fold-derived, not pinned to any target.
G3 EXACT/CERTIFIED — A–E exact; the open fronts (≥15-digit floor, above-floor series) NAMED + handed.
G4 DERIVED NOT POSITED — kappaLeadingDressed (X5), depthWeight (D3), recomposedEndpoint (all banked).
G5 PHYSICS-WORDS-REMOVABLE — delete every physics word; pure real arithmetic on banked Cut objects.
G6 FOUNDATIONS-ONLY — #print axioms ⊆ {propext, Classical.choice, Quot.sound}; derived ℝ Cut ground,
   Mathlib machinery only; no Mathlib ℝ as content, no floats, no native_decide, no sorry/axiom.
G7 ONE LAW — the inherited framing (narrow the incomplete 136.307) was the drift; the owner+the theory
   redirect to composing the fold-forced depth-2 running FIRST. We did not fit; the factor is forced.
G8 TEETH — depthTwo_dresses_recomposed (strict raise: the running is real); dressed_offsets_base (the
   base genuinely preserved); the composed bracket strictly two-sided.

## FORK GUARD / ONE-SUCCESSOR RAIL
Board clean (no children). Exactly ONE forward successor at wind-down: a DIRECTED child for owner step (2)
— derive the above-floor occupancy series to <1e-12 + continue narrowing the COMPOSED endpoint toward the
≥15-digit floor → … → D8. NO generic SELECT before D8 (owner rail). NOT D8 (floor not reached).
