# N23 PREREG — frozen BEFORE production Lean. THE COMPACT-FORM / TYPE-G₂ PINNING, increment 1.

## Target (ticket / ROADMAP §N23)
Prove FORWARD the sharpest TRACTABLE compact-form / type-G₂ invariant of the banked
14-dim Lie algebra `derivationLieQ` (N21, full 91-pair bracket table; N22 faithful 7-rep),
NO posited G₂. Candidates: (A) irreducibility of the faithful 7-rep, (B) Killing-form
nondegeneracy=semisimplicity, (C) Killing negative-definiteness, (D) simplicity.

## MEASUREMENT (W9 — route decided by a bounded measurement, NOT a guess)
Numerics re-verified (W6, workbench/N22.../killing_rep.py + N23.../route_probe.py +
design_probe.py, exact-rational, reusing the BANKED 14 derivations + bracket table):

  • (A) IRREDUCIBILITY HOLDS: orbit-span from every imaginary unit is full (7); the
    associative algebra generated by ρ(D_k) inside End(ImO) is the FULL 49-dim ⟹
    absolutely irreducible (Burnside). BUT formalizing needs a LieRingModule instance on
    ImO + "no proper invariant submodule" over the ABSTRACT 7-space — multi-run setup.
    → CHILD.
  • (B) KILLING NONDEGENERACY HOLDS, and the feared 14×14 det FACTORS: K is BLOCK-DIAGONAL
    into 7 disjoint 2×2 blocks [[-16,±8],[±8,-16]] (det 192 each) ⟹ det K = 192^7 =
    9618527719784448. Structurally trivial — BUT Mathlib's `killingForm = trace(ad∘ad)`
    must be COMPUTED abstractly (the ad-matrices in derivBasis = the structure-constant
    table, then traces of products) — the W9 beast the ticket flags. Tractable via the
    block structure but heavy (matrix machinery over the abstract basis). → CHILD.
  • (C) NEGATIVE-DEFINITENESS: each 2×2 block neg-definite; full W9. → CHILD.
  • ★ PERFECTNESS / NOT-SOLVABLE — THE GIFT (design_probe.py): EVERY basis derivation is a
    SINGLE banked bracket: D0=-½⁅D1,D2⁆, D1=½⁅D0,D2⁆, D2=-½⁅D0,D1⁆, D3=½⁅D5,D6⁆,
    D4=-⁅D0,D7⁆, D5=⁅D0,D6⁆, D6=-⁅D0,D5⁆, D7=⁅D0,D4⁆, D8=-⁅D3,D13⁆, D9=-⁅D0,D12⁆,
    D10=⁅D0,D11⁆, D11=-⁅D0,D10⁆, D12=⁅D0,D9⁆, D13=⁅D3,D8⁆ — all from the banked `br_*`
    table. So `⁅L,L⁆ = ⊤` (perfect) ⟹ `¬ IsSolvable ℚ derivationLieQ` is the CLEANEST
    sharp type-G₂-direction invariant, built ENTIRELY from the banked bracket table +
    basis spanning + Mathlib derived-series API. NO 14×14 det, NO abstract-7-space rep
    setup. Bounded ext+coe lemmas, each per-bracket cheap (the W9.3 freeze-on-compile
    discipline; each br_* identity is already banked).

## ROUTE TAKEN — (3) DECOMPOSE (W1 step 3 / W3): bank PERFECTNESS + NOT-SOLVABLE this node.
This is a genuine forward increment pinning `derivationLieQ` toward the compact simple form:
a perfect Lie algebra is non-solvable, non-nilpotent, non-abelian — the structural opposite
of the solvable radical. Semisimple ⟹ perfect; this is the cleanly-tractable NECESSARY edge
of the simplicity claim (D), banked forward from the crown-jewel bracket table. The heavier
invariants (A irreducibility, B/C Killing nondegeneracy+definiteness, D full simplicity,
then the automorphism Lie GROUP) are childed onto the chain tail — never asserted, never a bridge.

## DELIVERABLE (N23) — `Phys/Algebra/DerivationPerfect.lean`:
  1. `bL : Fin 14 → derivationLieQ` — the 14 basis derivations as Lie-algebra elements
     (⟨D{i}E, IsDerivQ⟩); `bL_coe : (bL i : End) = D{i}E`.
  2. `bracketL_i_j : ⁅bL i, bL j⁆ = c • bL k` — the 14 generator brackets, lifted from
     the banked `br_*` End-level table via `LieSubalgebra.coe_bracket` + `Subtype.ext`.
  3. `bL_mem_derived : bL k ∈ ⁅(⊤:LieIdeal ℚ derivationLieQ), ⊤⁆` — each generator is a
     smul of a bracket of two ⊤-elements (`LieSubmodule.lie_mem_lie` + smul-closure).
  4. ★ `derivationLieQ_perfect : ⁅(⊤:LieIdeal ℚ derivationLieQ), ⊤⁆ = ⊤` — perfectness:
     the toSubmodule contains all 14 `bL k` which span (via derivBasis ↦ ⊤), so = ⊤
     (`LieSubmodule.toSubmodule_eq_top`).
  5. ★★ `derivationLieQ_not_solvable : ¬ LieAlgebra.IsSolvable ℚ derivationLieQ` — from
     perfectness: `derivedSeries ℚ derivationLieQ k = ⊤` for all k (Mathlib
     `derivedSeries_eq_top`, since `derivedSeries _ _ 1 = ⁅⊤,⊤⁆ = ⊤`), but ⊤ ≠ ⊥
     (nontrivial: the witness derivation), so no k gives ⊥.
  6. Anti-vacuity / non-triviality: `derivationLieQ` is nontrivial (the banked nonzero
     witness `witnessLieQ` ≠ 0 / D2E ≠ 0), so ⊤ ≠ ⊥ has teeth.
  7. Costume C43: a WRONG invariant FAILS — assert `⁅D0E, D1E⁆ = 0` (algebra abelian /
     derived series collapses ⟹ solvable) → contradicts banked `br_0_1 = -2•D2E` with
     `D2E ≠ 0`; the false core must not close.

## GO / NO-GO
GO if: (4) perfectness + (5) not-solvable elaborate clean foundations-only AND C43 bites.
NO-GO / re-scope: if the basis-spanning-in-the-subalgebra (3→4) fights the kernel (the
derivBasis lives in `derivationQ`, the LieIdeal in `derivationLieQ` — possibly distinct
subtypes), then bank the unbundled facts: the 14 `bracketL_i_j` generator-bracket
identities + `¬ IsLieAbelian derivationLieQ` (immediate from one nonzero bracket), and
child the full perfectness assembly.

## KILL BUDGET (W9)
  • Compile-cost KILL: 90s per single obligation in a bounded probe. If any one lemma
    exceeds 90s → STOP, decompose/reframe; do NOT inflate maxHeartbeats (default only).
  • The br_* lifts are per-bracket cheap (each banked br_* already elaborates). The only
    risk is the spanning assembly — MEASURE in scratch before production.
  • Turn-budget checkpoint at ~55%: if not banked, COMMIT what compiles, child the rest,
    FINALIZE with turns to spare. Bank-as-you-go (W9.8): commit each clean lemma instantly.

## STANDARD self-check (applied before "done")
  • UNBROKEN: bL/brackets/perfect built from banked derivationLieQ + derivBasis + the
    br_* table; Mathlib LieIdeal/derivedSeries/IsSolvable are MACHINERY on the derived
    object (§3). No posited G₂.
  • COMPLETE: foundations-only ⊆ {propext,Classical.choice,Quot.sound}; verify #print axioms.
  • PHYSICS-WORDS-REMOVABLE: delete "g₂/gauge/compact/exceptional" ⟹ pure statement that
    the 14-dim Lie algebra of Leibniz-derivations of CD³(ℚ) is PERFECT (equals its own
    derived ideal) and hence NOT solvable. No physics word load-bearing.

## ANTI-VACUITY (W8) — the costume MUST bite
A WRONG claim must fail: asserting a generator bracket vanishes (abelian ⟹ solvable),
or claiming the algebra is solvable. Reuse banked derivationLieQ + the br_* table + the
nonzero witness — never a fresh table. Perfectness is NOT automatic (abelian/solvable
algebras are not perfect), so the result has teeth.
