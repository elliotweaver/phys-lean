# N132 PREREG — THE GLOBAL REVERSE SURJECTIVITY, CLOSED UNCONDITIONALLY AGAINST THE REFLECTION ALPHABET

Frozen BEFORE compute. KILL classifies any fight as INSTRUMENT failure (W9), not theory.

## THE MEASURE-FIRST FINDING (THE ONE LAW — W1 step 2, dissolution through the trunk)

The ticket's literal priority-(i) residual `k ∈ genIsomMonoidLin` (every EvC-orthogonal
QvC-isometry IS a `biMulLin` word) was **already proved FALSE by N105**
(`xNegLin_not_mem_genIsomMonoidLin`, `planeRotLin_not_mem_genIsomMonoidLin`): every bare
`biMulLin` word FIXES both `t` AND `x` (`genIsomMonoidLin_fixes_fst/_snd`), reaching only the
octonion-block SO(8); a general compact factor (a ninth-direction SO(9) rotation) MOVES `x`, so
it is provably NOT a `biMulLin` word. The residual was framed against the WRONG generating
alphabet — grinding it harder is the drift trap.

REFRAME THROUGH THE TRUNK: the chain ALREADY BANKED, at N128, the theory-native answer.
`lorentz_exhaustion_M` (in `LorentzContinuumGenerationTotallyIsotropic.lean`): EVERY linear
BvC-isometry `S` (`IsBvIsomLin S`) lies in `Submonoid.closure houseHolderMSet` — a finite product
of indefinite Householder reflections (the Cartan–Dieudonné generators). This is UNCONDITIONAL
(`haniso_exists_thm` discharged the totally-isotropic case via Born positivity, Witt index 1).
Combined with N51's `linIsom_BvC_preserved` (`IsQvIsomC g → ∀ p q, BvC (g p)(g q) = BvC p q`,
i.e. `IsBvIsomLin g`), the global reverse surjectivity closes UNCONDITIONALLY against the
reflection alphabet — the HEAVY residual DISSOLVES (the theory makes the hard thing easy).

## THE TARGET (N132)

Bank the UNCONDITIONAL global reverse surjectivity, as a complete classification:

1. `bvIsomLinMonoid : Submonoid (Module.End Cut STVC)` — the submonoid of ALL linear BvC-isometries
   (carrier `{S | IsBvIsomLin S}`; `one_mem` trivial, `mul_mem` = `IsBvIsomLin.comp`). NEW object.
2. ★★ `linQvIsom_mem_reflection_closure` — THE HEADLINE: every linear QvC-isometry `g`
   (`IsQvIsomC g`) lies in `Submonoid.closure houseHolderMSet` (a finite reflection word).
   Via `linIsom_BvC_preserved` (N51) → `IsBvIsomLin g` → `lorentz_exhaustion_M` (N128). The
   global reverse surjectivity for an ARBITRARY QvC-isometry, UNCONDITIONAL — discharging N131's
   conditional residual `k ∈ genIsomMonoidLin → …` by switching to the alphabet that actually
   reaches the full group.
3. ★★ `reflection_closure_eq_bvIsomLinMonoid` — THE EQUALITY: `Submonoid.closure houseHolderMSet
   = bvIsomLinMonoid`. `⊆` by `Submonoid.closure_le` + `houseHolderM_isom` (each reflection is a
   BvC-isometry); `⊇` by `lorentz_exhaustion_M`. `le_antisymm`. The generated reflection group
   IS exactly the full linear isometry group SO⁺(1,9) — classification, not conditional.
4. W8 teeth: the CONCRETE boost `(5/3,4/3)` is a genuine non-identity BvC-isometry
   (`boostEndC_isBvIsomLin`), hence ∈ `closure houseHolderMSet` (non-vacuity of the unconditional
   surjectivity on a real boost), and a witness reading `BvC mTimeUnit = 1` that the costume flips.

## GO / NO-GO

GO (bank N132): theorems 1–4 PROVE, foundations-only (`#print axioms ⊆
{propext, Classical.choice, Quot.sound}`), gate D0–D6 green, costume C164 (`1 = 132`) bites a
WRONG reverse-surjectivity / reflection-word / classification claim, committed + docs.

NO-GO / reframe: if the equality or the QvC corollary does NOT assemble from the banked
`lorentz_exhaustion_M` + `houseHolderM_isom` + `linIsom_BvC_preserved` — STOP, re-measure which
banked fact is mis-stated; do NOT invent a hypothesis. (Both directions are banked; expected to
assemble cleanly.)

## KILL (W9 — instrument budget)

- No single obligation compiles > 90s. If any probe obligation > 60s → DECOMPOSE / reframe (every
  obligation here is `closure_le` / `le_antisymm` / a one-line banked-lemma application — expect
  near-instant).
- Production build cached target < 40s; if > 120s, instrument wall — measure/decompose.
- NO inflated `maxHeartbeats`, NO `native_decide`, NO monolithic brute normalizer. None needed.

## W8 — anti-vacuity

Costume C164 = `1 = 132`, DISTINCT from C160 `1=128` … C163 `1=131`. The false numeric: a WRONG
read-off of the boost's Minkowski self-overlap (the boost IS a reflection word; its moved timelike
axis still reads `BvC = 1`, not `132`). PASS_SIGNATURE ERE matches a trailing `1 = 132`.

## PHYSICS-WORDS-REMOVABLE

Delete Lorentz/boost/rotation/reflection/Householder/Minkowski/isometry/surjectivity/generation/
Cartan/Dieudonné/SO⁺(1,9)/Spin/word/cover/classification → pure linear algebra over the derived
ℝ `Cut`, `STVC`, the bilinear form `BvC`: the submonoid generated by the maps
`p ↦ p − (2·(BvC a a)⁻¹·BvC a p)•a` (for `BvC a a ≠ 0`) equals the submonoid of all `Cut`-linear
operators preserving `BvC`; and every `Cut`-linear operator preserving `QvC` lies in that
generated submonoid. No theorem STATEMENT needs a physics word.
