# N30a PREREG — the Lie→associative SEMISIMPLE-MODULE BRIDGE
## `IsSemisimpleModule A_I ↥ImO`  (FORWARD, NO posited G₂, NO bridge)

FROZEN before compute. Run under task t_7d78977c. Decided by bounded MEASUREMENT (below).

## TARGET
For a Lie ideal `I` of `derivationLieQ`, with
`A_I := Algebra.adjoin ℚ (Set.range (fun x : I => imRep (I.incl x))) : Subalgebra ℚ (Module.End ℚ ↥ImO)`,
prove `IsSemisimpleModule ↥A_I ↥ImO` (= `ComplementedLattice (Submodule ↥A_I ↥ImO)`).
Plus the submodule dictionary + the commutant lemma (a commuting ideal J lands in `End_{A_I}`),
for N30b. NO posited G₂, NO bridge.

## THE ROUTE — decided by MEASUREMENT (THE ONE LAW: transport N27, do NOT re-derive)
Work ENTIRELY within `↥ImO` with the DEFINITE form `gImO := gBil.restrict ImO`. This is
CHEAPER than the O ℚ transport the ticket sketched: the orthogonal complement of a ℚ-subspace
of `↥ImO` is ALREADY a genuine complement IN `↥ImO` (no `⊓ ImO`, no subtype image/comap
order-iso). The banked N28 `restrictOp_skew ImO` already gives generator skew-adjointness.

Decomposition (bank-as-you-go, W9.8):
- L1 (gImO infra): `gImO`, `gImO_isRefl` (from `gBil_restrict_symm ImO`),
  `gImO_restrict_nondegenerate` (anisotropy: `gForm_self_eq_zero`, mirrors
  `gBil_restrict_nondegenerate`), `imRep_isSkewAdjoint_gImO D` (reuse `restrictOp_skew ImO`,
  with `restrictOp ImO _ D = imRep D`). CHEAP.
- L2 (generic): `skewAdjoint_mapsTo_orthogonal` — a `T` skew-adjoint for reflexive `gImO`
  preserving a subspace `N` preserves `gImO.orthogonal N`. (mirror of banked
  `deriv_mapsTo_orthogonal`, generic operator.) CHEAP.
- L3 (A_I-stability of the orthogonal): for `P : Submodule ↥A_I ↥ImO`,
  `gImO.orthogonal (P.restrictScalars ℚ)` is A_I-stable, via `Algebra.adjoin_induction`
  (base = generators preserve it by L2 since P closed under generators; algebraMap = ℚ-scalar
  preserves a ℚ-submodule; add/mul = preservers compose).
- L4 (the bridge): build the complement `P' : Submodule ↥A_I ↥ImO` (carrier
  `gImO.orthogonal (P.restrictScalars ℚ)`, smul_mem' = L3) and prove `IsCompl P P'` by
  transferring `IsCompl (P.restrictScalars ℚ) (gImO.orthogonal (P.restrictScalars ℚ))`
  (from `isCompl_orthogonal_of_restrict_nondegenerate gImO_isRefl (L1)`) across the
  scalar-restriction order-embedding (inf/⊥/⊤ preserved; sup forced to ⊤). ⟹
  `ComplementedLattice (Submodule ↥A_I ↥ImO)` ⟹ `IsSemisimpleModule`.
- L5 (commutant, for N30b): a commuting ideal J (`⁅I,J⁆=⊥`) acts in `End_{A_I}(↥ImO)`:
  each `imRep (J.incl y)` commutes with every generator (`imRep_commute_of_lie_zero`), hence
  with all of A_I (`Algebra.adjoin` centralizer is a subalgebra / `Algebra.adjoin_induction`).
- L6 (dictionary, OPTIONAL for N30b): `Submodule ↥A_I ↥ImO` ↔ I-invariant ℚ-subspaces.
  Tag OPTIONAL — child to N30b if not cheap.

## MEASUREMENT (W9 — done in bounded probes BEFORE any heavy commit)
- probe: `A_I` forms cleanly (ticket MEASURED ~13s, probe6). `Module ↥A_I ↥ImO` instance: MEASURE
  (`A.moduleLeft` per ticket).
- probe: `restrictOp ImO _ D = imRep D` (defeq?) so `restrictOp_skew` transports.
- probe: `isCompl_orthogonal_of_restrict_nondegenerate` signature for `gImO`.
- probe: the scalar-restriction order-embedding API (`Submodule.restrictScalars_injective`,
  `restrictScalars` of ⊤/⊥/⊓/mem_sup).

## GO / NO-GO
- GO: MEASURE L1..L4 API costs in bounded probes first; then bank L1→L2→L3→L4 in order, COMMIT
  each the instant it elaborates clean (W9.8). If L4 lands within budget, BANK the bridge; add
  L5 (commutant) if cheap; child N30b with L6 + the prime-7 collapse. Else DECOMPOSE: bank what
  compiled, child the remainder onto the chain tail, complete N30a as "decomposed".
- NO-GO (forbidden): assert `IsSemisimpleModule` without the proof; import a Mathlib G₂ and
  match; weaken the target; inflate maxHeartbeats; grind a monolith; re-measure a dead route.

## KILL (W9 compile-cost budget)
- 90s/obligation in a bounded probe. Any single sub-lemma > 90s = INSTRUMENT failure ⟹
  restructure/decompose, never grind. NEVER an unbounded compile in the turn budget.
- TURN-BUDGET CHECKPOINT at ~55%: if the bridge is not compiling clean by then, STOP adding,
  bank what is clean, child the remainder, decompose-complete N30a. N28 timed out TWICE;
  finalize EARLY.

## ANTI-VACUITY (W8) — the new costume MUST bite (id C51)
A WRONG claim must FAIL to compile: e.g. assert the orthogonal complement is NOT a complement
(`IsCompl P P'` replaced by a false disjointness/codisjointness), or assert
`¬ IsSemisimpleModule ↥A_I ↥ImO` — the banked forward result proves it IS, leaving `⊢ False`.
Reuse banked decls only; NEVER a fresh table.

## PHYSICS-WORDS-REMOVABLE (STANDARD §2)
Delete "g₂/gauge/exceptional/compact/simple": the theorem remains a complete proof that the
associative subalgebra generated by the action of a Lie ideal on the invariant 7-dim subspace
`ker(star+1)` of the Cayley–Dickson double of a double of a double of ℚ acts SEMISIMPLY (every
submodule has a complement).
