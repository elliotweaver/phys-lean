# PREREG — Census-Completeness Closure (t_f7628301)

## SELECT (redirect, comment 504)
Ticket asked to restate the banked closed real AS 1/α(0) at ≥15 figures. ONE LAW fired: "≥15 figures"
imports the MEASURED 1/α(0) to known precision (fit-shaped); the numbers have wandered across ≥5 nodes
(135.06/137.95/138.97/136–137) — the ONE-LAW signal that "assemble a 15-figure numeral that IS 1/α(0)"
is the wrong framing. STANDARD: measured value removable prose; a partial sum may NOT be declared the
full coupling; the theory PREDICTS. So completeness ATTESTATION = a STRUCTURE theorem (the census-level
local→global gather closing), NOT a numeral-precision match.

## DELIVERABLE (non-hollow, NEW cross-module weld) — the CENSUS-LEVEL COMPLETENESS CLOSURE
The completeness question ("is the banked assembly the complete 1/α(0)?") turns on whether the census is
EXHAUSTIVELY partitioned across the assembly's parts with nothing omitted / double-counted. Bank that the
gather CLOSES at the census level:
- `freezeoutStaircaseCensus : ℚ := 2*((bandCensus[2,1,1,2,1]−bandCensus[2,1,1,2]) + (bandCensus[2,1,1,2]−bandCensus[2,1,1]))`
  — the doubled total of the banked freeze-out removal STEPS (SubBandCompletion).
- `freezeoutStaircaseCensus = 10/3` (via freezeout_step_bottom/charm = colouredCensusTerm 1/2).
- ★ `((freezeoutStaircaseCensus:ℚ):Cut) = colouredCensus` — the freeze-out staircase re-supplies EXACTLY
  the coloured content the census-conserving lepton tail REMOVED (DecouplingCensusRemoval).
- ★★ COMPLETENESS CLOSURE: `chargeTraceDepth = ((freezeoutStaircaseCensus:ℚ):Cut) + leptonCensus` —
  the full census 16/3 = (freeze-out staircase 10/3) + (lepton-tail singlet 2), the partition CLOSES:
  every unit carried by exactly one part, nothing omitted, nothing double-counted (local→global gather).
- ★ SCREENING-LEVEL CLOSURE: the screening OMITTED by the census-conserving endpoint (decoupling_removal)
  = `bandScreen ((freezeoutStaircaseCensus:ℚ):Cut) (mass0/mass1)` — the omitted 1/α content IS the
  freeze-out staircase's census-weighted screening.
- Teeth: staircase ≠ 0 (10/3); staircase ≠ singlet (10/3 ≠ 2, no double-count collapse); staircase ≠
  full (10/3 ≠ 16/3, neither part alone is the whole); leptonCensus ≠ full (banked); steps distinct
  (freezeout_steps_differ, banked); removal strictly positive (removal_pos, banked — the omitted slice
  is genuinely nonzero, must be re-supplied).

## GRADE (honest)
THEOREM-EXACT now: the census-level completeness closure + screening-level closure + teeth (exact
identities/inequalities of Cut/ℚ, foundations-only). ROUTE-NOT-YET-FOUND (NAMED, child ONE route-finder):
the ENUMERATION EXHAUSTIVENESS + TERMINATION — that the finite banked band list is THE COMPLETE threshold
set from 42 to 0, with (i) the threshold COUNT forced by the N5 generation cap (finitely many charged
species ⇒ finitely many thresholds) and (ii) TERMINATION: below the lightest charged edge the active
census is 0, so 1/α freezes at its zero-momentum value — plus the ℝ-descent edge numerals. Without (i)+(ii)
the assembly is provably a partial sum (STANDARD forbids declaring it the full coupling).

## HARD GUARDS
No fit (endpoint NOT claimed = 1/α(0)); s stays derived 1/8 (untouched); NO empirical number in any
statement/proof (137.036/135.06/138.97 removable prose only); NO new axiom; derived ℝ Cut only, NO
Mathlib ℝ as content. Physics-words-removable. NOT free-floating (every TYPE cites banked census objects).

## ONE-SUCCESSOR RAIL
The route-finder (exhaustive N5-cap threshold enumeration + termination-to-zero) IS the single successor;
REPLACES the generic SELECT. Exactly one non-terminal successor at complete.
