# PREREG — D7 COMPOSITION-COMPLETENESS: compose the TWO owner-named forced summands → close attestation → re-narrow → D7 restate AS 1/α(0) (whatever the kernel forces)
## (t_480559d6, OWNER R2v2; grove-manager decision 2026-07-18: fit-detection CONFIRMED, band-varying weights = WRONG OBJECT, nothing overturns LowTailFoldForcedWeight 3643441)

## THE FRONT (owner-directed, D7-gate completeness enforcement — NOT steering)
The raw census-conserving landing (~136.3) is banked (D7CompositionAttestation 8445013: recomposedEndpoint ∈ [3406/25, 13639/100], recomposedEndpoint < 137). The manager's ledger-audit finds EXACTLY TWO enumerated contributions with NAMED FOLD SOURCES already banked AS STRUCTURE that were never COMPOSED as explicit summands of the completeness attestation:

(1) THE MIRROR THRESHOLD CENSUS SHIFT — the N277 doubling-tear mirror multiplet TL(3)⊕Sg(1)⊕TR(3). Its census shift at the transmutation join is a COUNTING theorem (content counting, mirrorMultCensus = finrank ℚ TR = 3), DISTINCT from the SCALE ratio j=1 (JoinSplitRatioDerived, metric degeneracy). Its DESCENT contribution is the banked `joinContent w j` — and at the DERIVED splitting ratio (joinSplitRatio = 1, isometric last doubling) it contributes EXACTLY 0 (joinContent_at_derived_eq_zero). ⇒ INCLUDED, PROVEN ZERO (the D7 gate's explicit alternative).

(2) THE HADRONIC LEVEL WEIGHTS ABOVE THE FLOOR — D2e banked floor occupancy = 16 EXACTLY (HadronicLevelWeights.floorOccupancy_eq_sixteen). The above-floor occupancy series (r₈(n), n≥2) is the freeze-out/hadronic term's fine structure. The banked FreezeoutReweightingBounded already PROVED: occupancy count 16 ≠ freeze-out census 9 (occupancy_ne_census_total — DISTINCT objects), and ANY census-conserving reweighting is bounded by κ·9·cutLog R (census_conserving_reweight_bounded). ⇒ INCLUDED as a BOUNDED census-conserving refinement (the counting is monotone + bounded; the field measures it as resonance enhancement).

## THE HONEST GRADE (SOUL route-COMPLETE vs route-NOT-YET-FOUND)
THEOREM-EXACT / CERTIFIED NOW:
 - the mirror threshold census shift is a DERIVED count (=3) whose composed descent contribution is EXACTLY 0 at the derived splitting (a genuine INCLUDED-and-proven-zero summand — the D7 gate's own alternative);
 - the above-floor level weights are a BOUNDED census-conserving refinement (κ·9·cutLog R ceiling), the floor weight = 16 exact, distinct from the census 9;
 - the AUGMENTED completeness attestation: both summands now explicitly accounted (INCLUDED-proven-zero / INCLUDED-bounded), joining the banked census partition + way-points + enumeration + termination.
 - the endpoint bracket UNCHANGED by composition (both summands contribute 0 / a bounded refinement below current width) → recomposedEndpoint still ∈ [3406/25, 13639/100], still < 137.

THE KERNEL-FORCED LANDING: composing both summands does NOT move the endpoint into [136.85,137.10]; it stays at ~136.3 < 137. THAT is the fold's PREDICTION (disagreement-as-prediction, licensed by the owner: "if it lands elsewhere, THAT is the fold's prediction and D7 states it AS IS"). The measured 137.036 stays removable prose. NO FIT: the summand that WOULD lift toward 137 (a large joinContent from mis-reading j as topBandRatio) is the REFUTED conflation (joinSplitRatio_ne_topBand); the fold-forced j=1 gives 0.

ROUTE-NOT-YET-FOUND (named, NEVER asserted): the general r₈(n) closed form (Jacobi eight-square, not in Mathlib) — carried bounded exactly as the arc has always carried it; and the ≥15-sig-digit floor (the D5 narrowing campaign, deeper partial sums) — no new axiom, no fit.

## THE THEOREMS (production; every TYPE mentions banked derived Cut / O ℚ objects)
A. mirror_threshold_census_shift : mirrorMultCensus = 3 ∧ (0 < joinContent w j for 1<j) ∧ joinContent w joinSplitRatio = 0  — the shift is a real count, its composed descent contribution vanishes at the DERIVED ratio (INCLUDED-proven-zero).
B. abovefloor_weights_bounded : the general reweighting (any census-conserving above-floor weighting) bounded by κ·9·cutLog R (re-export census_conserving_reweight_bounded); floor weight 16 ≠ census 9 (occupancy_ne_census_total).
C. augmented_composition_complete : the FULL attestation — the banked recomposed_composition_complete conjuncts PLUS (A) join INCLUDED-proven-zero PLUS (B) above-floor INCLUDED-bounded ⇒ ledger CLOSED, no further layer.
D. augmented endpoint UNCHANGED: recomposedEndpoint restated bracket [3406/25,13639/100] and < 137 CARRY THROUGH (the two summands do not move it) — re-export.
E. d7 restate AS 1/α(0): recomposedEndpoint ∈ [3406/25,13639/100] AS 1/α(0), < 137 the fold's PREDICTION (disagreement licensed).
F. capstone (welded, non-hollow).

## W9 COST BUDGET / KILL
All new theorems are ASSEMBLY of banked foundations-only theorems (joinContent_at_derived_eq_zero, occupancy_ne_census_total, census_conserving_reweight_bounded, recomposed_composition_complete, recomposedEndpoint_restated_bracket). No new heavy tactic; every closer is `exact`/`⟨…⟩`/`linarith`/`norm_num` over banked lemmas. KILL: any obligation >60s ⇒ decompose (should be near-zero — it's a re-export/conjunction node).

## FORK GUARD / ONE-SUCCESSOR RAIL
Board is clean (no children). Exactly ONE forward successor at wind-down: either D8 capstone (if this banks the full augmented attestation) or the next narrowing rung toward the ≥15-digit floor — a DIRECTED child, replacing the SELECT ticket. NO generic SELECT before D8 (owner rail).
