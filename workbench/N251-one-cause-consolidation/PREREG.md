# PREREG — N251 (MANAGER TAKEOVER): THE ONE-CAUSE CONSOLIDATION `u1` (the fold-root √−1)

Frozen BEFORE production compute. Owner-authorized manager takeover (session directive:
"place N250 in a blocked state and take over... go over the recent nodes you know can be done more
theory native, up to and including n250, and do them in this session in the theory native way").
Witness protocol suspended for THIS node by explicit owner directive; FULL DISCIPLINE retained.

## THE DIAGNOSIS (what the takeover corrects)
N248/N249/N250 are foundations-only and theory-native AT THE CORE (each uses the one-cause
"colour that acts as zero on the charged 6 also kills the singlet ⟹ kills all of ImO ⟹ zero by
simplicity of Der(O)"). The DEFECT is not broken math — it is FRAGMENTATION that buries the moat:
- N248 `colourRepV_ne_zero` (nontriviality) is the `D = DI` SPECIAL CASE of N249
  `colourRepV_faithful` (injectivity) — the same argument run twice.
- N250 is a packaging corollary of N249 (injective LieHom ⟹ dim-8 image + commutant).
- NONE of the three makes explicit the actual theory-native cause: `u1`, the DERIVED fold-root
  √−1 (N2 `mulJ_isFoldRoot`), is ONE element wearing FOUR faces that FORCE the whole structure.

## THE THEORY-NATIVE TARGET (single capstone, no new coordinate work)
Bank ONE capstone `colour_charge_one_cause_u1` exhibiting `u1` as the single cause:
  (a) `chargeOp = crossOp u1`                          — u1 IS the charge-operator input (rfl)
  (b) `chargeOpV ∘ chargeOpV = −id`                    — u1 squares to the complex structure (N242)
  (c) `chargeStabilizer = colourCentralizer`           — colour IS the centralizer of u1 (rfl)
  (d) `∀ D, Commute (colourRepV D) chargeOpV`          — ℂ-linearity FORCED BY (c), not coincidence
  (e) `Function.Injective colourRepV`                  — faithful = parent 7-rep faithfulness (N22)
  (f) `finrank colourRepImg = 8`                       — dim-8 image, downstream of (e)
  (g) `∀ M ∈ colourRepImg, Commute M chargeOpV`        — image in the ℂ-linear commutant

THE MOAT SENTENCE (what the consolidation makes explicit that the fragments buried): the induced
colour rep's ℂ-linearity is NOT an independently-proven property — it is FORCED because
`colourCentralizer` is DEFINED (N201) as the g₂-stabilizer of `JO = L_{u1}`, and `chargeOp` is
built from the SAME `u1`. One derived element (the fold's √−1) is the tear input, the charge
operator, the complex structure, AND the thing colour is the centralizer of. Faithfulness is the
parent fundamental-7 faithfulness (simplicity of Der(O)) read through the u1-charge eigenspace
split `ImO = ker ⊕ range = 1 ⊕ 6`. ONE CAUSE, MANY TERMINATIONS.

## GO / NO-GO
GO iff ALL hold:
  - (a)–(g) all assemble from BANKED lemmas (verified in recon: recon_capstone.lean compiles,
    #print axioms = [propext, Classical.choice, Quot.sound]).
  - The capstone TYPE mentions banked tower objects (chargeOp/crossOp/u1/chargeOpV/colourCentralizer/
    colourRepV/colourRepImg) — no free-floating / carrier-agnostic statement (SOUL rail).
  - Words-removable: delete "charge/colour/su(3)/complex/hypercharge/fold-root" → pure statement
    about `crossOp u1` on the derived `ImO`, its restriction's square = −id, the centralizer, the
    faithful restriction map, its dim-8 image in the commutant. No physics word load-bearing.
  - #print axioms ⊆ {propext, Classical.choice, Quot.sound} vs the BUILT olean.
NO-GO / reject if:
  - Any face needs a NEW coordinate bash or a not-yet-banked fact (then it is not a consolidation —
    reassess; do NOT weaken, do NOT posit).
  - The capstone would require re-opening N248/N249/N250 proofs (it must SIT ABOVE them, citing
    banked names — extend, never fork; the existing banked lemmas STAY, this adds the unifying view).

## COST KILL (W9)
Every face is a citation or `rfl` (recon: instant). BUDGET: whole-module olean ≤ 120s. KILL: if any
face does NOT close by direct citation within a bounded probe, it was never a consolidation —
STOP, do not grind, do not inflate heartbeats.

## COSTUME (1 row, must bite at the math field)
Next id C282. Bite the dim-8 / single-cause claim: a bogus reading that the consolidated image has
the wrong dimension (e.g. `8 = 282`) or that ℂ-linearity fails, reduces through the banked
`finrank_colourRepImg` / `colourRepV_commute` to a false numeric, rejected at the math field.

## DISCIPLINE
Extend never fork (N248–250 banked lemmas remain; this adds the unifying capstone module above
them). No bridge, no second axiom, no Mathlib number/algebra as content (ℚ is the coefficient
field; u1/chargeOp/ImO stay derived REAL; NO ℚ[i]-as-ground, NO Algebra ℚ (Dbl ℚ), NO TensorProduct
— the N242 complex-field-metalanguage warning light stays lit). RESUME-first: recon_capstone.lean
is the frozen probe. Independently axiom-audit vs the built olean before gate.
