# N368 — arc-F F3: proton stability = the ΔB ∈ n_gen·ℤ topological selection rule

## SELECT (fresh worker, theory-native FIRST — full record)

### S1 — arc synthesis
N363–N367 climbed arc-E to its close (E7 singularity=closure, E8 inflation=C6 eternal-approach,
E9 consolidation) then opened arc-F (SM structural observables that are NOT masses/couplings):
- **N366 (F1)** — anomaly cancellation via the colour ladder's charge-conjugation (Hodge) complement:
  the B−L-type charge `blCharge n S = (2|S|−n)/n` is ODD under `S↦Sᶜ` (a fixed-point-free involution),
  so `Σ blCharge = Σ blCharge³ = 0` (linear grav² and cubic U(1)³ anomalies vanish); the `∅` singlet
  (the self-blind ν_R) is REQUIRED (punctured sum = 1 ≠ 0). Grounded on `finrank ℚ Uhol = 3`.
- **N367 (F2)** — strong CP θ̄=0: the J₃(O ℚ) reduced determinant is real + gauge-fixed, no θ dial.

BANKED launch platform for F3: the colour exterior ladder `Λ*(colour-n)`, `blCharge`/`complPerm`/
`compl_fixedpoint_free`/`sum_zero_of_compl_odd` (N366), the derived generation/colour count
`finrank ℚ Uhol = 3` (N42c, `finrank_Uhol`), gauge-universality (generation-blind). NO homotopy /
sphaleron / winding structure is banked (grep-confirmed) — the topological B-violation channel is a
BUILD (W1).

### S2 — theory-native next beat (GOAL + REFRAMING lenses)
- **GOAL lens.** N366 mined the complement-ODD (B−L) channel: it is CONSERVED (a free sign-reversing
  involution ⇒ anomaly-free). The fold's untold half is the complement-EVEN (B+L) channel — the ONLY
  place baryon number can change. The fold story: the colour ladder's single free structure is the
  complement (charge conjugation); B−L rides its odd part (conserved), B+L rides its even part (the
  topological/anomalous channel). The theory's next thing to say: B-violation exists ONLY in the B+L
  channel and it is QUANTIZED.
- **REFRAMING lens.** Standard GUTs: proton decay is a generic dim-6 operator, τ_p a FREE scale set by
  M_GUT (SU(5): ~10³⁴ yr). The theory has NO such operator basis to choose. Gauge B-violation is
  topological (gauge-field winding) and gauge-UNIVERSAL (generation-blind, banked), so one topological
  transition acts on the COMPLETE colour ladder in ALL n_gen generations at once ⇒ ΔB is quantized in
  units of `n_gen = finrank ℚ Uhol = 3`. A single-proton decay ΔB=−1 ∉ 3ℤ ⟹ TOPOLOGICALLY FORBIDDEN.
  The proton is effectively stable; only gravity (dim-6/M_Pl²) gives τ_p ~ 10⁴⁴ yr. The "proton decay
  problem" DISSOLVES into a selection rule forced by the derived generation count.

### S3 — reconcile
Arc (SEED §F3) points at exactly this and the theory-native derivation ALIGNS and DEEPENS it: the
quantum = n_gen is NOT asserted — it is tied to the SAME complement structure that gave N366's anomaly
cancellation. ONE CAUSE (the complement splits the ladder charge): B−L conserved (N366) AND B+L
quantized-anomalous (N368). Proceed.

### S4 — anti-bullshit gate
1. **FOLD-CAUSAL.** fold → recursive self-blindness → CD cascade → octonions (stop) → J₃(O) matter
   arena + Der(O)=g₂ colour → colour exterior ladder `Λ*(colour-3)`, `finrank ℚ Uhol = 3` → the
   complement involution is the ladder's only free structure → complement-odd = B−L conserved (N366);
   complement-even = B+L, the topological channel → gauge-universal topological transition shifts ONE
   baryon per generation ⇒ ΔB = n_gen·k → ΔB=−1 ∉ n_gen·ℤ (n_gen=3≥2) FORBIDDEN. Each arrow forced.
2. **DISSOLUTION.** Standard = proton decay generic, τ_p a free M_GUT scale (SU(5) ~10³⁴ yr). Theory
   DISSOLVES: no free operator/scale; ΔB integer-quantized by the derived n_gen; single-proton ΔB=−1
   has no witness. A genuine wall→selection-rule dissolution.
3. **GOAL-WORDS-REMOVABLE.** Delete proton/baryon/decay/sphaleron: "the image of `k ↦ n_gen·k`
   (n_gen = finrank ℚ Uhol) is n_gen·ℤ, which excludes −1 for n_gen ≥ 2". A forced arithmetic reason
   survives. ✓
4. **TEMPTATION.** The saturated worker would (a) grind F2's remaining gauge-θ Tier-2 half, or (b)
   build a generic "3ℤ subgroup of an arbitrary group" abstraction. REJECT (b) (free-floating rail):
   every headline TYPE mentions `Module.finrank ℚ Uhol` and the sum over `Fin (finrank ℚ Uhol)`; the
   generic `n` version is only the construction. Chose F3 (the theory's genuine next beat — the B+L
   complement-even channel that N366 left untold) over grinding F2's debt.
5. **HONESTY.** This IS the fold's next beat. The n_gen-quantum-of-B-violation is theorem-grade; the
   physical identification (topological quantum = 't Hooft/sphaleron transition, per-generation baryon
   = the top ladder form) is FLAGGED PROSE; τ_p ~ 10⁴⁴ yr is PREDICTION-grade removable prose. No BS.

## HONEST GRADE (carry it)
- **THEOREM-GRADE** (foundations-only, over ℚ/ℤ, grounded on `finrank ℚ Uhol`): the selection-rule
  arithmetic `baryonShift n k = n·k`; `ΔB=−1` forbidden at the derived n_gen (n_gen=3≥2); B−L
  conservation `ΔB=ΔL` (ties to N366's conserved odd channel); B+L = 2·n_gen·k (the anomalous even
  channel); W8 teeth (n_gen=1 would ALLOW ΔB=−1 — the derived n_gen≥2 is load-bearing).
- **PREDICTION-grade** (FLAGGED, removable prose): proton effectively stable, τ_p ~ 10⁴⁴ yr — a
  falsifiable framework prediction distinguishing from SU(5)'s ~10³⁴ yr.
- **MECHANISM / identification** (FLAGGED PROSE, never a Lean equation): topological quantum = n_gen
  (gauge-universal per-generation baryon shift); 't Hooft/sphaleron channel = the B+L complement-even
  direction; gravity dim-6/M_Pl² = the sole surviving decay operator.

## PLAN (over ℚ/ℤ + Fin, grounded on finrank ℚ Uhol; standard Mathlib as MACHINERY only)
Namespace `Phys.Algebra` (reuse `finrank_Uhol`, `Uhol`, N366 objects in scope).
Construction (generic n):
- `baryonShift (n : ℕ) (k : ℤ) : ℤ := ∑ _g : Fin n, k`  (one baryon shift per generation per winding)
- `leptonShift (n : ℕ) (k : ℤ) : ℤ := ∑ _g : Fin n, k`  (B−L conserved ⇒ ΔL = ΔB)
- `baryonShift_eq : baryonShift n k = (n:ℤ)*k`  (Finset.sum_const, card_univ)
- `bMinusL_conserved : baryonShift n k − leptonShift n k = 0`
- `bPlusL_anomalous : baryonShift n k + leptonShift n k = 2*(n:ℤ)*k`
- `baryonShift_mem_multiples : ∃ m, baryonShift n k = (n:ℤ)*m`  (m=k)
- `single_unit_forbidden (hn : 2 ≤ n) : ∀ k, baryonShift n k ≠ -1`  (from n∣1 ⇒ n≤1 contra)
- `single_unit_allowed_at_one : baryonShift 1 (-1) = -1`  (W8: n=1 breaks it)
Grounded headlines (types mention `Module.finrank ℚ Uhol`):
- `gen_count_ge_two : 2 ≤ Module.finrank ℚ Uhol`  (finrank_Uhol; 2≤3)
- `proton_decay_forbidden : ∀ k, baryonShift (Module.finrank ℚ Uhol) k ≠ -1`
- `baryon_violation_quantum : ∀ k, baryonShift (Module.finrank ℚ Uhol) k = 3*k`
- `generation_bMinusL_conserved`, `generation_bPlusL_anomalous`
- capstone `proton_stability_selection_rule`
W8 costume C393: bogus "single-proton decay is allowed at the derived n_gen" reduces to a false
numeric via a flag tied to `proton_decay_forbidden`.

## GUARDS
G1 no asserted values (n·k, −1-forbidden fall out). G2 no empirical number in proof (τ_p, SU(5) 10³⁴
= prose/costume only). G3 ΔB dimensionless integer; τ_p a scale = prose. G4 3ℤ rule from finrank ℚ
Uhol, not posited. G5 words-removable (delete proton/baryon → n·ℤ image excludes −1). G6
foundations-only ⊆ {propext, Classical.choice, Quot.sound}; no sorry/axiom/native_decide. ℝ-vigilance:
ℚ/ℤ + Fin only, NO Mathlib-ℝ. W8: C393. W9: arithmetic is trivial-cost; one file, one commit.
