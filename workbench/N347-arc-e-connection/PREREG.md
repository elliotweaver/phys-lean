# N347 — arc-E E1b: the CONNECTION = the fold's look-back transport; metric-compatibility EXACT from η²=1

## SELECT (fresh worker, theory-native FIRST — S1–S4)
DIRECTED successor of N346 (E1a, the weak-field metric g=η+h + exact linearized-inverse residual).
E1b builds the NEXT forced object: the CONNECTION (look-back transport) of that metric.

**Theory-native question asked FIRST (QUIET DRIFT guard):** what does the theory say the connection
IS, and what is its ONE forced fact? Answer: the connection is NOT a posited gauge field — it is the
fold's LOOK-BACK TRANSPORT (the same object that normalized α, the H² look-back face): how the Born
self-overlap (metric) is carried infinitesimally from a point to its neighbour. Its DEFINING property
is metric-compatibility (∇g=0): the transport PRESERVES the self-overlap. The theory-native heart:
metric-compatibility is NOT an imposed condition (as standard GR imposes it to solve for Γ) — it is a
CONSEQUENCE, FORCED by η²=1 (the flat self-overlap undoing itself — the SAME "one cause" that made
N346's linearized inverse close). One cause (η²=1), another termination (the transport preserves the
metric). This is exactly "we DERIVE what the field POSITS."

## Target (the SMALLEST forced first beat, W3-decomposed per ticket HONEST GRADE)
Over MetricEnd = Module.End Cut STVC (the N346 carrier, derived-ℝ):
- `lbConn k := ½ • (η · k)` — the look-back transport generated by a metric VARIATION `k`.
  Physically `k = ∂_λ h` (the directional change of the field h); the directional-derivative
  machinery that PRODUCES `k = ∂h` is a heavier W1 build (multivariate operator-field derivative on
  the derived-ℝ tangent structure — NOT banked; the banked cut_powerseries_deriv is scalar) → CHILDED
  to E1b-cont / E1c. This node builds the ALGEBRAIC metric-compatibility TEMPLATE the derivative feeds.
- `lbConnAdj k := ½ • (k · η)` — its EvC-adjoint (for self-adjoint k).
- ★ `metricCompat : lbConnAdj k · η + η · lbConn k = k` — metric-compatibility of the transport,
  EXACT, forced by η²=1 (`eta_mul_self`, N346). ½+½=1 closes it. The DEFINING Levi-Civita property,
  DERIVED not imposed. (Faithful operator rendering of ∂g = A†g + gA at g=η, ∂g=k, A=½ηk.)
- `lbConn_flat : lbConn 0 = 0` (no variation ⟹ no transport — flat, no curvature).
- `lbConn_add` (linearity), `lbConn_smul` (homogeneity).
- `lbConn_isEvCAdjoint : IsEvCAdjoint k k → IsEvCAdjoint (lbConn k) (lbConnAdj k)` (transport-adjoint
  structure via banked sigOpC_isEvCAdjoint_self; the physical k=∂h is self-adjoint when h is).
- W8 non-vacuity (C372): the WRONG connection (missing the ½ — `η·k`) FAILS metric-compat for the
  concrete `timeProj` variation (gives 2k ≠ k). The ½ is genuinely load-bearing.
- `lbConn_ne_zero : lbConn timeProj ≠ 0` (non-trivial transport for a nonzero variation).

## Anti-bullshit gate (ANTI-BULLSHIT LAW, run in writing)
STANDARD version: the Levi-Civita connection — Christoffel symbols built from the metric, with
metric-compatibility IMPOSED as a defining axiom, solved for Γ = ½g⁻¹(∂g+∂g−∂g).
What the theory DOES to it: (1) grounds the metric in the banked Born-derived (1,9) signature (η =
sigOpC, N90); (2) reads the connection as the look-back transport (H² face), not a posited field (G4);
(3) DERIVES metric-compatibility from η²=1 instead of imposing it — the exact "one cause, many
terminations" content. Words-removable: delete "connection/transport/Christoffel/gravity/metric" — a
pure statement about ½·η·k for the involution η (η²=1) over the derived field Cut. NOT free-floating:
every type mentions the banked MetricEnd = Module.End Cut STVC and the banked sigOpC. Temptations
REJECTED: (a) import Mathlib manifold/connection API + state Levi-Civita generically → FREE-FLOATING +
UNEARNED-IMPORT, REJECTED (built on banked Cut/sigOpC, Module.End as machinery only). (b) build the
full derivative-carrying Christoffel now → the multivariate operator-field derivative is a heavier W1
build, W3-CHILDED honestly (ticket sanctions "metric-compatibility of the transport ... correct and
sufficient — child the rest"). (c) conflate the variation k with the metric perturbation h → NAMED
honestly: k is the metric VARIATION (∂h), the derivative producing it is childed. GENUINELY DIFFERENT
from textbook GR: textbook IMPOSES ∇g=0 and solves; here ∇g=0 FALLS OUT of the flat self-overlap
η²=1. Machinery/leading-order beat — asserts NO physical value; flagged honestly in the docstring.

## Guards: G1 no value asserted (machinery). G2 no empirical number. G4 connection IS look-back
## transport, built on derived Cut/sigOpC, NOT posited; metric-compat PROVED not assumed. G5
## words-removable. G6 foundations-only, NO Mathlib-ℝ/manifolds/deriv as content (Module.End/LinearMap
## machinery on derived objects OK). W8 C372 (wrong-coefficient fails). W9 LIGHT (dimension-free ring
## algebra + η²=1; no coordinate bash — probes close instantly).

## ONE SUCCESSOR: DIRECTED child E1c — the curvature = the connection's non-closure (the look-back
## transport failing to commute; Riemann/Ricci from lbConn), OR E1b-cont if the directional-derivative
## machinery producing k=∂h must land first. Single tail successor.
