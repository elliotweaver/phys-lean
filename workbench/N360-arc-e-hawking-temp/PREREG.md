# N360 — arc-E E4: the Hawking temperature T_H = κ/2π from Euclidean smoothness = the fold's return-surface closure

## SELECT (theory-native FIRST, fresh worker — QUIET DRIFT + ANTI-BULLSHIT gate)

The ticket points at E4: T_H = κ/2π = 1/8πM. I asked the theory-native question FIRST, not the
ticket's framing.

**Standard version (what the field does):** Wick-rotate to imaginary time τ; the Euclidean section of
the black-hole metric near the horizon is a 2-plane in polar form `ds² ≈ dρ² + ρ²(κ_sg dτ)²`. Demand
REGULARITY at ρ=0 (no conical singularity) — this is an EXTERNAL smoothness posit — which forces the
angle `κ_sg·τ` to have period 2π, i.e. τ ~ τ+β with `β = 2π/κ_sg`. Then β is read off as the inverse
temperature: `T_H = 1/β = κ_sg/2π`. Finally κ_sg = 1/(4M) is computed from the Schwarzschild metric,
giving `T_H = 1/8πM`. The "2π" is a posited full-turn; the metric computation supplies κ_sg.

**What the theory DOES (the moat):**
- The "no conical singularity" condition is NOT an external regularity posit — it IS the fold's
  RETURN-SURFACE CLOSURE. A 2-plane point map `P(ρ,θ) = (ρ·cutCos θ, ρ·cutSin θ)` closes (returns to the
  same point) under `θ ↦ θ + azimuthalTurn` and ONLY under a full turn: `azimuthalTurn = 2·cutPi` (N328)
  is the DERIVED period of the derived trig (`cutCos_add_two_cutPi`/`cutSin_add_two_cutPi`, N325). A
  SMALLER period (a half-turn cutPi) provably does NOT close (`cutCos_add_cutPi = −cutCos` → sign flip →
  a genuinely different point → a conical singularity). So the "2π" is the DERIVED Born-circle turn, and
  smoothness = closure is FORCED by the periodicity of the derived trig, not posited.
- **★ THE SHARED BORN TURN / MOAT (arc-A + arc-E cross-tie, N359 extended).** The SAME derived
  `azimuthalTurn = 2·cutPi` that fixes the Euclidean period ALSO normalized (i) the fine-structure
  coupling `α*=1/42` via `84π = azimuthalTurn·42` (N329), and (ii) the gravitational coupling
  `8π = azimuthalTurn·4` (N359 gravCouplingCoeff). Now it fixes the thermal period. The exact cross-tie:
  `β = gravCouplingCoeff·M` (the Euclidean period is the SAME 8π coupling coefficient times the mass),
  since `hawkingPeriod = azimuthalTurn/κ_sg` and for `κ_sg=(4M)⁻¹`, β = azimuthalTurn·4·M =
  gravCouplingCoeff·M. THREE physical normalizations — α*, gravity's coupling, and the Hawking period —
  are all the ONE derived Born turn scaled by the fold's derived counts.
- **★ T_H is the RECIPROCAL PERIOD.** T_H = 1/β = κ_sg/azimuthalTurn = κ_sg/(2·cutPi). The temperature is
  born from the period, which is born from the closure. The "WHY-thermal" (look-back-is-modular: the
  Euclidean period IS the fold's periodic return on itself) is the MECHANISM layer — flagged honestly.

**The split (forced core vs metric-specific home) — honest grade:**
- **UNCONDITIONAL FORCED CORE:** the closure `eucPlane closes under azimuthalTurn and NOT half-turn`
  (W8 non-vacuity), the period `hawkingPeriod κ_sg = azimuthalTurn/κ_sg` fixed by smoothness
  (`κ_sg · hawkingPeriod = azimuthalTurn`), and `hawkingTemp κ_sg = κ_sg/(2·cutPi)`. All for SYMBOLIC
  surface gravity κ_sg — the theory-native heart, fully forced, no metric posit.
- **CONDITIONAL cross-tie to 1/8πM:** GIVEN `κ_sg = (4M)⁻¹` (an explicit HYPOTHESIS, the Schwarzschild
  surface-gravity computation — a metric-specific piece NOT banked, so kept as a hypothesis, NEVER
  asserted), `hawkingPeriod = gravCouplingCoeff·M = 8·cutPi·M` and `hawkingTemp = 1/(gravCouplingCoeff·M)
  = 1/(8·cutPi·M)`. This homes the "1/8πM" to the banked coupling coefficient gravCouplingCoeff (N359).
  The surface-gravity computation κ_sg=1/(4M) (from f'(r_h)/2, f=1−2M/r) is the metric-specific layer —
  FLAGGED leading-order, CHILDED (the full metric build).

**Temptations named + rejected:**
- (a) posit β = 2π/κ_sg by "smoothness". REJECTED — the smoothness IS the derived-trig closure; I DERIVE
  the period from `cutCos_add_two_cutPi` and PROVE a smaller period fails (W8 non-vacuity), not posit it.
- (b) assert κ_sg = 1/(4M) as a fact / assert T_H = 1/8πM. REJECTED (G1). κ_sg=(4M)⁻¹ stays an explicit
  HYPOTHESIS (not a posit); the 1/8πM form is a conditional theorem homing to gravCouplingCoeff.
- (c) generic thermodynamics abstraction ("define β, T=1/β, prove positivity"). REJECTED (free-floating).
  The content is the CONCRETE derived-trig closure + the shared-azimuthalTurn moat; every TYPE cites
  cutCos/cutSin/azimuthalTurn/cutPi/gravCouplingCoeff.
- (d) import Mathlib Real.pi / Real.exp / manifolds as content. REJECTED (ℝ-vigilance) — build on the
  banked derived Cut + cutPi/azimuthalTurn/gravCouplingCoeff.

**Words-removable (G5):** delete Hawking/temperature/horizon/Euclidean/thermal/black-hole/surface-gravity/
conical → pure statements over the derived Cut: `P(ρ,θ)=(ρ·cutCos θ, ρ·cutSin θ)` closes under +azimuthalTurn
and not +cutPi; `κ·(azimuthalTurn/κ)=azimuthalTurn`; `1/(azimuthalTurn/κ)=κ/(2·cutPi)`; for x=(4M)⁻¹,
azimuthalTurn/x = gravCouplingCoeff·M. Pure arithmetic + derived-trig periodicity. No physics name is
load-bearing.

## SMALLEST FORCED BEAT (W9 MEASURE-FIRST)
The closure + period + temperature relations are all shallow arithmetic on banked periodicity lemmas
(cutCos_add_two_cutPi, cutCos_add_cutPi, cutCos_cutPi, cutCos_zero, azimuthalTurn=2·cutPi,
gravCouplingCoeff_azimuthal). No heavy tsum/analysis — the analysis was paid at N325/N328. Compile-cost
budget: each lemma should close in <5s (ring/simp on banked equalities). KILL: if any single obligation
exceeds ~30s or needs maxHeartbeats raise → measure/decompose (not expected).

## PLAN (production module Phys/Algebra/SpacetimeHawkingTemperature.lean, ns Phys.Algebra)
1. `eucPlanePoint (ρ θ : Cut) : Cut × Cut := (ρ*cutCos θ, ρ*cutSin θ)` — near-horizon Euclidean 2-plane.
2. `eucPlane_closes_azimuthalTurn`: closes under θ↦θ+azimuthalTurn (cutCos/cutSin_add_two_cutPi).
3. `eucPlane_half_turn_conical` (W8): at ρ=1,θ=0, `eucPlanePoint 1 cutPi ≠ eucPlanePoint 1 0` — a
   half-turn does NOT close (conical singularity); the FULL azimuthalTurn is required.
4. `hawkingPeriod (κsg : Cut) : Cut := azimuthalTurn / κsg` (β = 2π/κ_sg).
5. `hawkingPeriod_smoothness (κsg≠0): κsg * hawkingPeriod κsg = azimuthalTurn` — period×surface-gravity = one full Born turn.
6. `hawkingTemp (κsg : Cut) : Cut := 1 / hawkingPeriod κsg`.
7. `hawkingTemp_eq (κsg≠0): hawkingTemp κsg = κsg / (2*cutPi)` — T_H = κ/2π (azimuthalTurn=2·cutPi).
8. `hawkingPeriod_schwarzschild (h: κsg=(4*M)⁻¹, M≠0): hawkingPeriod κsg = gravCouplingCoeff * M` (=8·cutPi·M) — β=8πM homed to gravCouplingCoeff.
9. `hawkingTemp_schwarzschild (h: κsg=(4*M)⁻¹, M≠0): hawkingTemp κsg = 1/(gravCouplingCoeff*M)` (=1/(8·cutPi·M)) — T_H=1/8πM homed.
10. `hawkingTemp_pos (κsg>0): 0 < hawkingTemp κsg`.
11. Bundle `hawking_temperature_structure` (the heart).
12. Costume C385 (a WRONG period/factor must fail).

## HONEST GRADE
Theorem-grade for the UNCONDITIONAL forced core (closure→period→T_H=κ/2π), which the SEED calls the
strongest part of arc E. The 1/8πM cross-tie is CONDITIONAL on κ_sg=(4M)⁻¹ (explicit hypothesis, the
metric-specific surface-gravity computation — CHILDED, leading-order). The WHY-thermal (look-back-is-
modular) is the mechanism layer — flagged prose. M/κ_sg symbolic dimensionful anchors (G3); T_H's numeric
value removable prose only (G2). MOAT flagged: β = gravCouplingCoeff·M ties the thermal period to the
banked 8π coupling — α*/gravity/Hawking-period all the ONE derived Born turn.
