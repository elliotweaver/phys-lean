# PREREG — D7 FULL COMPOSITION-COMPLETENESS ATTESTATION + first re-narrow + restate AS 1/α(0)
## (t_570a603a, owner R2 continuation; PerChannelRecombination df7223f's named successor)

## THE FRONT (owner-directed, structural; route is mine)
Over the banked `recomposedEndpoint` (Phys.Algebra.PerChannelRecomb, closed real of Cut):
  recomposedEndpoint = 7179/54 + bandScreen leptonCensus (mass0/mass1) + freezeoutStaircaseSummand
    freezeoutStaircaseSummand = 16/9 + κ·[(7/3)cutLog mbRatioDerived + (11/3)cutLog(bMass 0) − (5/3)cutLog(bMass 2)]
1. FULL D7 composition-completeness ATTESTATION as a THEOREM: every banked structural contribution
   (two high bands via way-points 6923/54→7179/54; below-Λ lepton tail census 2; quark freeze-out
   staircase coloured 10/3 at derived quark edges; every census) INCLUDED or proven zero. Assemble the
   BANKED pieces: belowLambda_census_partition (16/3=10/3+2), the way-point adjudicators, the
   enumeration+termination (CensusEnumerationTermination), the recomposed closed form.
2. RE-NARROW: sharpen cutLog(mass0/mass1) from banked coarse [1,9] (AssembledBracket) to [8, 82/10]
   via the banked ContinuumBracket instrument (cutLog_ge_certified / cutLog_le_certified + cutLog_mono
   + m01_bracket_lo/hi). A genuine tighter reading.
3. D7 RESTATE AS 1/α(0): explicit rationals lo < recomposedEndpoint < hi from the sharpened cutLog(m01)
   + banked edge-log brackets + banked 1/cutPi (ext6). NO-FIT; the measured 137.036 is removable prose;
   the certified bracket EXCLUDES it ⇒ STATE the fold's prediction, never bend it.

## THE HONEST GRADE / SCOPE (SOUL route-COMPLETE vs route-NOT-YET-FOUND)
THEOREM-EXACT / CERTIFIED NOW:
 - the FULL composition-completeness attestation (structure theorem — NOT a numeral-precision match,
   per the honest prior CensusCompletenessClosure finding);
 - the sharpened cutLog(m01) ∈ [8, 82/10];
 - a CERTIFIED restatement bracket lo < recomposedEndpoint < hi (width ~0.14, ~3 sig digits agreeing).
ROUTE-NOT-YET-FOUND (NAMED, handed to ONE directed narrowing successor): the ≥15-SIG-DIGIT floor.
 The endpoint's precision is bottlenecked by the DERIVED inputs' current bracket widths:
   • mass0/mass1 ∈ [3465.30, 3485.51] — only ~3 sig digits (from the fold-phase cutCos(2/9) numerals);
   • edge logs Lb0∈[1.7,1.8], Lb2∈[−1.2,−1] — ~1 digit each (dominate the endpoint width);
   • 1/cutPi banked to ~12 digits (ext6, sharpenable).
 To reach 15 digits EVERY derived input must be sharpened (deeper cutCos partial sums → b0/b1/b2 →
 mass ratio → cutLog; deeper edge-log brackets). That is the D5 narrowing campaign — "as many rungs as
 it takes" (SEED §D5/§D7). THIS node banks the attestation + first sharpened restatement; the narrowing
 rungs are the successor's. NOT a block: forward autonomous structural work remains (each rung is a
 derivation, no new axiom, no fit).

## THE THEOREMS (production; every TYPE mentions banked derived Cut objects — not free-floating)
A1. recomposed_composition_complete : the FULL attestation — conjunction of
     (census partition 16/3=10/3+2) ∧ (way-point EW 6923/54) ∧ (way-point conf partial-sum 7179/54) ∧
     (enumeration budget cap-forced + exhausted) ∧ (termination freeze below lightest edge) ∧
     (closed form) — every structural contribution accounted, none omitted, none double-counted.
B1. cutLog_m01_ge_8 : (8 : Cut) ≤ cutLog (mass0/mass1)   [ContinuumBracket cutLog_ge_certified a=8]
B2. cutLog_m01_le_82 : cutLog (mass0/mass1) ≤ 82/10       [ContinuumBracket cutLog_le_certified b=8.2]
B3. cutLog_m01_sharpened : 8 ≤ cutLog(mass0/mass1) ∧ cutLog(mass0/mass1) ≤ 82/10  (strict 8<82/10 tooth)
C1. endpoint_combo_bracket : 43/3 ≤ combo ∧ combo ≤ 47/3   [abstract-core; assemble sharpened + edge logs]
C2. recomposedEndpoint_restated_bracket : lo ≤ recomposedEndpoint ∧ recomposedEndpoint ≤ hi
     with lo=3406/25 (136.24), hi=13639/100 (136.39); STRICT lo<hi; width < 15/100.  ← D7 restatement AS 1/α(0)
C3. recomposedEndpoint_excludes_measured : recomposedEndpoint < 137   (the fold's PREDICTION — measured
     1/α(0)≈137.036 removable prose only; the certified bracket lies strictly below it).
D1. capstone landing (welded, non-hollow).

## W9 COST BUDGET / KILL (abstract-core pattern, mirror PerChannelRecombination.pos_core)
The endpoint bracket assembly: prove an OPAQUE-variable arithmetic core (k=kap, L=cutLog(m01), and
the three edge logs as opaque vars with their bracket hypotheses), keep transcendentals OUT of the
arithmetic; instantiate with banked brackets. partialExp at a=8/b=8.2 have numerators ≤ ~23 digits —
norm_num [Nat.factorial] on a range-14 sum. KILL: if any single obligation >90s or a monolithic
nlinarith on unexpanded cutLog/cutPi, STOP and decompose to a banked sub-lemma. Import DAG compiles
(Probe1). Never a heavy normalizer on unfolded transcendentals.

## FORK GUARD / ONE-SUCCESSOR RAIL
Exactly ONE forward successor: D7 narrowing rung 1 (directed child) — sharpen the derived mass-ratio
input mass0/mass1 (via deeper cutCos(2/9) partial sums → b0/b1) and the edge logs toward the ≥15-digit
floor. NO generic SELECT before D8 banks (owner: rail violation).
