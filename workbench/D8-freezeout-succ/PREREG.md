# PREREG — D8-freezeout-succ: THE FREEZE-OUT EDGE RATIOS rb,rc,rl — CROSS-SECTOR SCALE REDUCTION

Node t_7cdb8950. DIRECTED SUCCESSOR of JoinSplitRatioDerived (s=1/8 DERIVED & composed, j=1 DERIVED &
discharged). LAST remaining bounded residual of the ComposedNumeralObstruction: the freeze-out edges
rb,rc,rl (the quark-threshold ℝ-descent).

## THE RESUME PICTURE (what prior runs found — DECISIVE, and it CHANGES the ceiling)

The FIRST route-finder rung (banked: `OctonionJordanGenerationCyclePhaseLineIndependence`,
`phase_no_deviation`) REFUTED the earlier "deviated quark phase" hypothesis. The banked forced facts:
- The generation Fano triple ASSOCIATES (`gen_triple_assoc_zero`: assoc u2 u1 (octCross u1 u2)=0).
- The turning eigenvalue of the ℚ-permutation holonomy is line-INDEPENDENT −1 (`see_Poth`).
- ∴ the cycle phase is the SAME Born count δ_B = 2/9 on the quark generation line as on the lepton
  ℂ-line: `δ_quark = δ_lepton = 2/9`. The old PREREG's "un-banked deviated quark phase" ceiling is GONE.

So the quark WITHIN-sector shape is the SAME banked shape as the lepton sector (N340), and the ONLY
quark/lepton distinction lives in (i) the CP form assoc3 (banked Z3), (ii) the sector FRAMES (banked
Z3 CKM), and (iii) the ABSOLUTE relative SCALE between sectors. rb,rc,rl are a (iii) object.

## THE THEORY-NATIVE HEART (what the fold's own amplitude form says — the SCALE-FACTORING)

Banked (N307/N309/N340): `genVec M A p r = ![M+A·p, M−A(p+r)/2, M−A(p−r)/2]`, `massVec = genVec²`,
Koide-forced `A = √2·M`. Substituting, EVERY amplitude factors as `M × shape`, where the shape
`(b0,b1,b2) = (1+w·p, 1−w(p+r)/2, 1−w(p−r)/2)` is fixed ENTIRELY by the SHARED derived phase δ=2/9
(via p=cutCos(2/9), banked N340) and w=√2 — NO free parameter beyond the ONE scale M per sector.

CONSEQUENCE 1 (WITHIN-sector, M cancels): the three masses of ANY sector are `M²·(b0²,b1²,b2²)`, so
the two within-sector mass RATIOS are the SAME banked N340 lepton numerals `[483/1e5,485/1e5]`,
`[594/1e4,595/1e4]`, at ANY scale M≠0. PROVEN in probe (`withinSector_ratios_lepton`). This is the
theory-native discharge of the WITHIN-sector part of the quark spectrum: it is NOT route-not-yet-found
— it is FORCED equal to the banked lepton descent, because the phase is line-independent.

CONSEQUENCE 2 (CROSS-sector, scale survives): a ratio of masses from DIFFERENT sectors carries the
relative sector scale `(M_i/M_j)²`, which does NOT cancel. The freeze-out edges are exactly this:
- rb = bottom-band edge = removes a DOWN-type flavour (level 1) → m_b/m_c genre (down heavy / up mid)
- rc = charm-band edge = removes an UP-type flavour (level 2) → m_c/m_s genre (up mid / down light)
- rl = light-band edge = the light/confinement edge → m_s/Λ genre.

## THE EXACT STRUCTURAL FINDING (the theorem this node banks — no fit, no posit)

The freeze-out edges FACTOR into (banked within-sector shape ratios) × (the relative sector scales).
Precisely, over the derived ℝ Cut, with sectors carrying scales Mu (up), Md (down), Mλ (confinement
unit): each edge = (a banked shape-ratio numeral, forced by δ=2/9) × (a relative sector-scale factor).
The WITHIN-sector shape content is DISCHARGED to the banked N340 numerals; the CROSS-sector residual
is EXACTLY the relative sector scales (Mu/Md, Md/Mλ), which is the up/down/confinement SCALE assignment
— the SAME object arc M (SEED_MASSES M1) is chartered to derive (SEED_MASSES: arc M consumes "D's
quark-ratio numerals (D2/D5 output)" and derives the absolute scales/anchors).

So this node does the theory-native REDUCTION: it PROVES rb,rc,rl reduce to (banked shape numerals) ×
(the two relative sector scales), collapsing THREE route-not-yet-found reals to ONE structural object
(the up/down/confinement relative scale), and NAMES that as the M1 relative-scale assignment — the
single directed successor toward M1. This is the repeated-deferral protocol done right: NAME the exact
missing structure (the relative sector-scale assignment) and hand ONE directed child toward it.

## WHY THIS IS NOT A BULLSHIT REPACKAGING (S4 anti-bullshit gate, in writing)

1. FOLD-CAUSAL. fold → self-blindness → Born count δ=2/9 (line-independent, banked rung-1) → the
   within-sector shape is the ONE banked shape at ANY scale (M factors out of genVec by A=√2M). Each
   arrow forced. The reduction is the shadow of "the phase is a count, not a scale".
2. DISSOLUTION. The field treats the quark thresholds as ~6 independent Yukawa inputs. The theory
   DISSOLVES the within-sector part entirely (= the lepton numerals, forced) and reduces the WHOLE
   freeze-out tail to ONE structural residual (the relative sector scale). That is a genuine collapse
   of the front, not a relabel: three route-not-yet-found reals → one named scale object.
3. GOAL-WORDS-REMOVABLE. Delete quark/lepton/mass/generation/freeze-out/threshold: over Cut, with
   bShape=![b0,b1,b2] (banked), sectorMass M k = M²·bShape k², the within-sector cross-ratios equal
   the banked N340 brackets independent of M, and any cross-sector ratio carries (Mi/Mj)². Pure math
   survives; the reason it is the next node (reduce the last residual to the M1 scale object) survives.
4. NAME THE TEMPTATION. The saturated temptation: assert rb,rc,rl as decimals landing 1/α at 137.036
   (FIT — forbidden), OR re-bank FreezeoutComposition's bound under a new name (gold-plating). I refuse
   both. My load-bearing NEW content is `sectorMass_eq` (the M²-factoring) + `withinSector_ratios_lepton`
   (the forced universality) + the cross-sector residual identification — genuinely new, never banked,
   and it REDUCES rather than repackages.
5. HONESTY. rb,rc,rl are NOT pinned to numbers this node (that needs the M1 relative scale, honestly
   arc-M's charter). The honest deliverable is the EXACT reduction theorem + the named M1 residual +
   the ONE directed child. No quark value asserted, no fit to 137.036/PDG, no empirical number in any
   proof; measured comparison stays removable prose.

## GUARDS
- No edge/quark-mass/scale value asserted; the within-sector numerals are the banked N340 brackets (not
  newly posited); the cross-sector residual stays a bounded/quantified scale object, never pinned here.
- No fit to 137.036/PDG; no empirical number in any proof; data comparison removable prose only.
- BANKED derived ℝ (Cut, b0/b1/b2, mass0/1/2, N340 brackets) — NO Mathlib-ℝ as content.
- Foundations-only: #print axioms ⊆ {propext, Classical.choice, Quot.sound}.
- Free-floating rail: every production TYPE mentions the banked derived objects (bShape from b0/b1/b2
  over Cut, sectorMass, massRatio_1_2/massRatio_2_0 N340, freezeoutTail/censusBottomBand from D2).
- ONE successor on the chain tail (directed toward M1 relative-scale), no fork.

## COST BUDGET / KILL
All content is `ring`/`nlinarith` on banked Cut brackets + `sectorMass_eq` scale-factoring (linear).
Probe compiled EXIT 0 in ~11s. Expected LIGHT (gate-fast ~1 min). KILL: any single obligation >60s →
decompose per-ratio (each its own banked lemma). No unbounded compile; no brute normalizer.

## PRODUCTION FILE
`Phys/Algebra/FreezeoutEdgeCrossSectorReduction.lean`, namespace
`Phys.Algebra.FreezeoutEdgeCrossSectorReduction`. Capstone `freezeoutEdge_crossSector_reduction`. C539.
Node label `FreezeoutEdgeCrossSectorReduction`.

## RESOLUTION (run 844 — repair of inherited WIP + honest joint welded)

The prior reclaimed run (843) left `Phys/Algebra/FreezeoutEdgeCrossSectorReduction.lean` as production
WIP that (a) did NOT build (2 errors: a stray `ring` after `field_simp` at edgeProduct_within_sector;
a `nlinarith` at crossSector_scale_survives that lacked the positivity fact `0 < sectorMass Md 2`),
and (b) OVER-CLAIMED its grade — the docstring asserted "the within-sector quark shape IS the banked
lepton shape (rung-1)" without importing/citing the rung and without naming the full cross-corpus
joint. Crime-scene analysis (STANDARD independently applied):

1. VERIFIED the thesis is theory-native and CORRECT (nearly drifted AGAINST the theory here — the
   standard-physics intuition "quarks aren't Koide" is about OBSERVED post-mixing masses, not the
   within-sector NORM ratios). The banked corpus FORCES within-sector universality: the Koide balance
   A²=2M² is line-BLIND (computed on the Fin 3→ℚ coefficient vector, balance_forces_koide/forced_sqrt2),
   the phase is a line-independent holonomy Born count (cycle_phase_value), and N310 SEPARATES Q from
   the associator — so the within-sector norm ratios have NO quark-vs-lepton deviation channel.
2. FIXED both build errors.
3. WELDED the honesty: imported the rung, added `within_sector_phase_line_independent` (re-exports
   cycle_phase_value ∧ gen_triple_assoc_zero ∧ fano_assoc3_ne_zero), welded it into the capstone as
   conjunct (0), and rewrote the GRADE docstring to name the THREE load-bearing cited facts + the
   post-mixing point explicitly.
4. Full Phys builds green; 14 decls foundations-only; C539 bites 1=539.

DELIVERED: the exact structural REDUCTION (three route-not-yet-found reals → one M1 relative-scale
object), NOT a numeric pin of rb,rc,rl (honestly the M1 charter). ONE directed successor toward the
M1 relative-scale assignment.
