| N70 THE DEFLATION / PEEL-OFF ENGINE — the inductive STEP of the full spectral existence theorem (peel one eigenpair, restrict to its EvC-orthogonal complement, recurse) over the DERIVED ℝ `Cut` and the terminal algebra `O Cut := CD (H Cut)`. N63–N69 banked the FORWARD direction (GIVEN an EvC-orthonormal eigen-family — or, in N69, a concrete coordinate operator shown to BE diagonal over a given family — the diagonal operator `Σᵢ λᵢ Pᵢ` and its `cutSqrt`-per-eigenprojection square root at every finite rank); the MISSING HALF the existence proof actually RUNS on is the REVERSE STEP: the spectral theorem is proved by INDUCTION on dimension, and its inductive step is DEFLATION — peel one eigenpair off, restrict to its EvC-orthogonal complement, recurse on a strictly smaller space. DERIVED from the trunk (deflation IS subtracting the banked EvC-orthogonal eigenprojection `projC` N63; self-adjointness preserved by the closure under `−` of the reference form `EvC` N58 = Born self-overlap positivity N56/N57; the invariant complement IS the orthogonality of the deflated image to the peeled direction), NOT a posited spectral theorem / operator calculus / inner product, NOT Mathlib spectral theory, NOT a posited Lorentz group, NOT Mathlib ℝ/ℂ as content (the continuum is the DERIVED `Cut`), NOT a bridge. W1 REFRAME + W9 MEASURE-FIRST + W3 DECOMPOSE (THE ONE LAW) — the ticket TARGET is the HEAVY group-manifold core (the FULL n-dim spectral EXISTENCE for an ARBITRARY operator, the GLOBAL polar/KAK assembly, connectedness, the GLOBAL reverse KAK surjectivity `qvIsomMonoidC` on the connected component `≤ genIsomMonoidC2`, the full `Spin(9)→SO(9)` exhaustion); MEASURE-FIRST (ROADMAP §N70 priority (i)) confirms the EXISTENCE for an ARBITRARY operator over `Cut` (the eigenpair EXTRACTION that SEEDS the recursion) and the connectedness / surjectivity / exhaustion each need group-manifold machinery Mathlib lacks over `Cut` — the genuine HEAVY sub-nodes (childed N71). The immediately-bankable, theory-native STRUCTURAL piece the full existence consumes is the INDUCTIVE STEP itself: `deflateC g l u := g − l • projC u`. W9 MEASURE-FIRST: a LIGHT node — the abstract trio is `EvC_sub_left/right` (`map_sub` of the bundled N68 forms) + `abel`/one `rw`; the rank-`n` reduction is `Function.update` + `Finset.sum_update_of_mem`/`add_sum_erase` + `abel`; one scratch probe + production compiled clean ≤20s, all 11 decls foundations-only at first compile, KILL=30s/obligation never approached, NO inflated maxHeartbeats / NO native_decide / NO brute; no name collisions; non-vacuity independently checked (`deflateC txMix 9 mixA = specOpN ![0,4,25] mix3` peels the `mixA` eigenvalue `9` off N69's genuine 10-dim-mixing operator, lowering its effective rank). W4.5/W1/W3 DECOMPOSE: the FULL n-dim spectral EXISTENCE for an ARBITRARY operator (the eigenpair EXTRACTION SEEDING this recursion) + the GLOBAL polar/KAK assembly + connectedness + the GLOBAL reverse KAK surjectivity + the full `Spin(9)→SO(9)` exhaustion is the genuine W1 HEAVY group-manifold remainder, childed N71. | Emergent (depth 3) | ✅ (LANDED) | ★★★ OVER THE DERIVED COMPLETE ORDERED FIELD `Cut` AND THE TERMINAL ALGEBRA `O Cut := CD (H Cut)` OVER IT, FOR THE SYMMETRIC BILINEAR FORM `EvC` AND THE BANKED EvC-ORTHOGONAL EIGENPROJECTION `projC u := p ↦ (EvC u p)•u` (N63), THE DEFLATED OPERATOR `deflateC g l u := g − l • projC u` IS THE COMPLETE INDUCTIVE STEP OF THE SPECTRAL EXISTENCE THEOREM: (1) EvC-SELF-ADJOINTNESS PRESERVED `deflateC_isEvCSymm` (FOR ANY EvC-SELF-ADJOINT `g`, VIA `IsEvCSymm_sub` THE CLOSURE UNDER `−` COMPLETING N63's add/smul); (2) ANNIHILATES THE PEELED EIGENVECTOR `deflateC_eigen_kills` (FOR A UNIT EIGENPAIR `EvC u u = 1`, `g u = l • u`, `deflateC g l u u = 0`); (3) ★★ THE RECURSION ENABLER `deflateC_image_orth` (THE ENTIRE IMAGE IS EvC-ORTHOGONAL TO `u`: `EvC u (deflateC g l u w) = 0` FOR ALL `w`, VIA THE PRESERVED SELF-ADJOINTNESS THROUGH `deflateC_eigen_kills` — SO THE EvC-ORTHOGONAL COMPLEMENT OF `u` IS AN INVARIANT SUBSPACE, THE INDUCTIVE DESCENT ON DIMENSION); THE RANK-`n` REDUCTION `deflateC_specOpN` (DEFLATING THE GENERAL RANK-`n` SPECTRAL OPERATOR `specOpN l u` N68 AT ITS `k`-TH EIGENPAIR ZEROES COEFFICIENT `k`: `deflateC (specOpN l u) (l k) (u k) = specOpN (Function.update l k 0) u`, THE CRANK rank-`n` → rank-`(n−1)`); NON-VACUOUSLY `deflateC_txMix_fst : deflateC txMix 9 mixA = specOpN ![0,4,25] mix3` (THE `mixA` EIGENVALUE `9` PEELED OFF N69's COORDINATE 10-DIM-MIXING WITNESS `txMix`, EFFECTIVE RANK DROPPED TO `{mixB(4),mixW3(25)}`), DERIVED FROM THE BANKED `projC` (N63) + `EvC`/`IsEvCSymm`/`EvC_symm` (N58) + `specOpN`/`EvCLeft`/`EvCRight` (N68) + `txMix`/`mix3`/`mix3_orthonormal`/`txMix_eq_specOpN`/`specOpN_read` (N69/N68) OVER THE ORDERED DERIVED ℝ, NO posited spectral theorem, NO posited inner product, NO posited operator calculus, NO posited Lorentz group, NO Mathlib ℝ/ℂ as content, NO bridge. `Phys/Algebra/LorentzContinuumDeflation.lean` (namespace `Phys.Algebra`), 11 decls foundations-only `[propext, Classical.choice, Quot.sound]` (independently axiom-audited against the built olean + Audits/AxiomAudit.lean). `EvC_sub_left`/`EvC_sub_right`/`EvC_zero_left` (the reference form over `−` and the zero vector, via `map_sub` of the bundled N68 `EvCLeft`/`EvCRight`). `IsEvCSymm_sub` (self-adjointness closed under `−`). `deflateC`/`deflateC_apply` (the deflated operator). `deflateC_isEvCSymm` (1). `deflateC_eigen_kills` (2). ★★ `deflateC_image_orth` (3 — the recursion enabler). ★ `deflateC_specOpN` (the rank-`n` reduction). ★ `deflateC_txMix_fst` (W8 non-vacuity). Costume C101 (`LorentzContinuumDeflationWrongReadCostume.lean`) bites a WRONG deflation reading — that the `mixA` eigenvalue is left at `3` not removed (the `mix3 0` eigenblock of `deflateC txMix 9 mixA` reads `3`); banked N70 gives `deflateC txMix 9 mixA = specOpN ![0,4,25] mix3` (`deflateC_txMix_fst`) reading the peeled coefficient `0` at the `mix3 0` eigenblock (`specOpN_read` over `mix3_orthonormal`), so the wrong claim asserts `0 = 3`, reducing through the actual `deflateC_txMix_fst`/`specOpN_read` definitions to the false numeric `0 = 3` (distinct from C84 `8=4`, …, C99 `36=64`, C100 `36=25`). THE MOAT: that the spectral theorem is proved by deflation — peel an eigenpair, restrict to its orthogonal complement, recurse — is standard linear algebra; the novelty is that the deflated operator, its preserved self-adjointness, its annihilation of the eigenvector, the orthogonality of its image to the peeled direction, and the rank-`n` reduction DESCEND from the banked EvC-orthogonal eigenprojection `projC` (N63) + the reference form `EvC` (N58, itself the Born self-overlap positivity N56) + the rank-`n` spectral operator `specOpN` (N68) + the coordinate witness `txMix` (N69) over the DERIVED ℝ `Cut` — NO posited operator calculus, NO Mathlib spectral theory (`Module.End`/`LinearMap`/`Function.update`/`Finset.sum`/`map_sub` are standard Mathlib MACHINERY on the DERIVED objects) — and the W1 reframe (deflation IS subtracting the trunk eigenprojection; the invariant complement IS the orthogonality of the deflated image) is exactly what banks the REVERSE half of the spectral existence the forward N63–N69 did not have. WORDS-REMOVABLE: delete "Lorentz/boost/rotation/spectral/eigenspace/eigenvalue/eigenvector/eigenprojection/eigenbasis/eigenpair/eigen-family/deflation/peel-off/diagonal/orthonormal/self-adjoint/positive-semidefinite/isometry/Spin/SO(9)/KAK/polar/Cartan/invariant/recursion/dimension/rank" → over the derived complete ordered field `Cut` and the Cayley–Dickson double³ `O Cut`, for the symmetric bilinear form `EvC`, `projC u := p ↦ (EvC u p)•u`, and `deflateC g l u := g − l • projC u`: `deflateC g l u` is EvC-symmetric when `g` is; for `EvC u u = 1` and `g u = l • u` it sends `u` to `0` and `EvC u (deflateC g l u w) = 0` for all `w`; and `deflateC (specOpN l u) (l k) (u k) = specOpN (Function.update l k 0) u`, with `deflateC txMix 9 mixA = specOpN ![0,4,25] ![mixA,mixB,mixW3]`, pure math. | — (the FULL n-dim spectral EXISTENCE for an ARBITRARY EvC-self-adjoint operator — the eigenpair EXTRACTION SEEDING the deflation recursion — + the GLOBAL polar/KAK assembly for an ARBITRARY `g` + connectedness + the GLOBAL reverse KAK surjectivity + the full `Spin(9)→SO(9)` exhaustion — childed N71)
