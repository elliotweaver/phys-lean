| DressingSeamResolved | ARC-D REMAINDER (directed route-finder successor of ComposedFreezeoutDissolved, owner-directed t_c488fbdc) — RESOLVE the composition-completeness attestation / the dressing-recombination-layer seam (the LAST barrier before D7 restate AS 1/α(0)): determine STRUCTURALLY, from the trunk, whether the D2c per-channel depth-dressing is drift to remove (a) or real with a compensating dominant negative summand missing (b) | Phys/Algebra/DressingSeamResolved.lean | THE DRESSING/RECOMBINATION SEAM RESOLVED (branch (a): the depth-dressing is way-point-forbidden drift; the genuine composed 1/α(0) is the leading-κ way-point-consistent tower) — directed route-finder successor of ComposedFreezeoutDissolved. THEORY-NATIVE RESOLUTION (the ONE LAW: the seam is the composition FIGHTING the target → the framing is incomplete; do NOT push the dressing harder, do NOT fit s — ask what the chain's OWN landings say). The seam is decided WITHOUT any measured value, by the chain's own banked internal anchors: the exact WAY-POINTS the assembly must reproduce (SEED_DIGITS §D4/§D6 free instrument-check). The banked assembly composes with the LEADING loop normalization κ₀=kappaLeading=1/(3·cutPi) and reproduces TWO banked exact way-points: the electroweak invAlphaZero[(chargeTraceDepth,topBandRatio)]=1582/27 (X6 invAlphaZero_topBand_value — the SAME 1582/27 X7 FineStructureMZReadings builds the PHYSICAL 1/α(M_Z) upon) and the confinement invAlphaZero derivedBandList=190/3 (Z4 derivedBandList_assembly). The D2c/D2d DEPTH-DRESSING replaces κ₀ by κ(w)=κ₀/(1−w), multiplying the screening by 1/(1−w); at the DERIVED weight w=depthWeight=1/3 (D3, the mean Born self-overlap over the return surface, 1/(1−w)=3/2), BOTH way-points MOVE: 42+(448/27)/(1−1/3)=602/9≠1582/27 (dressed_topBand_moves) and dressedInvAlphaZero depthWeight derivedBandList=74≠190/3 (dressed_conf_moves, = D3's banked 74). A uniformly depth-dressed tower therefore FAILS the free instrument-check, and reproduces the way-points ONLY at w=0 (dressed_reproduces_only_at_w0: dressedInvAlphaZero 0 derivedBandList=190/3, dressed_topBand_at_w0=1582/27). So the ×1.465 inflation is DRIFT: the census-in-census content is already carried by the banked all-orders transmutation scales that DEFINE the band edges (topBandRatio=cutExp(28·cutPi/3), the exact 448/27), so applying /(1−w) on top DOUBLE-COUNTS it. WHY BRANCH (a) NOT (b): the depth-tower factoring is UNIFORM across all bands (dressedTotalScreening_factor), so there is no band-selective dressing available — the dressing is EITHER on every band (moving the way-points, forbidden) OR on no band (w=0, leading-κ); the way-points force the latter; a compensating negative summand (b) would also have to un-dress the way-points, which the assembly's own way-point theorems forbid. Hence the genuine composed object is the way-point-consistent leading-κ recombined tower invAlphaEM(completeBandList chargeTraceDepth)=invAlphaZero(completeBandList chargeTraceDepth)+70 (D2b composed_endpoint, certified 137.9475880261…) plus the banked determined freeze-out summand (parent freezeout_at_pinned_dissolved, 16/9+κ·[…]) — both leading-κ, both way-point-consistent; the ×1.465-inflated ~150 object is EXCLUDED. ⭐ ONE CAUSE: the SAME banked exact way-points 1582/27 (electroweak) and 190/3 (confinement) that X7 built the physical 1/α(M_Z) reading upon are the internal anchors that adjudicate the recombination-layer seam — one set of derived way-points simultaneously anchors the M_Z reading AND excludes the depth-dressing of the assembled tower. GRADE: THEOREM-EXACT now (leading_topBand, leading_conf, dressed_topBand_moves, dressed_conf_moves, dressed_reproduces_only_at_w0, resolved_composed_is_leading, dressing_seam_resolved, teeth — exact identities/inequalities of Cut). ROUTE-COMPLETE: the composition-completeness barrier the parent named is DISCHARGED (the ×1.465 overshoot was drift; the genuine composed object is unambiguous); the remaining work is the ≥15-digit narrowing of the leading-κ composed numeral, handed to the ONE directed successor (D7 restate AS 1/α(0) ≥15 sig digits → D8 capstone → hand M1). NO fit (no 137.036/0.231/PDG/Λ_QCD in any statement or proof; 137.95, dressed ~150, 602/9, 74 removable orientation prose; fit s≈1.9 named ONLY to exclude — the WAY-POINTS decide the seam, not the measured value). Physics-words-removable (delete coupling/running/screening/band/dressing/way-point: pure Cut arithmetic on banked invAlphaZero/bandScreen/dressedInvAlphaZero/totalScreening/chargeTraceDepth/topBandRatio/derivedBandList/depthWeight survives), not free-floating (every TYPE cites the banked way-point + dressed-tower objects). GATE-FAST D0–D6 PASSED; 15 decls foundations-only ⊆ {propext,Classical.choice,Quot.sound}; C543 bites 1=543 (dressed_topBand_ne: 602/9≠1582/27, the dressing genuinely moves the banked way-point). Directed successor toward D7 restate AS 1/α(0) ≥15 sig digits → D8 → hand M1. |
