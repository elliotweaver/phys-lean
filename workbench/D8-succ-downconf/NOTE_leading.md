# NOTE — LeadingComposedAssembly (t_800fa6e0): the leading-κ composed 1/α(0) ASSEMBLED + the HONEST
#        D7 attestation (one genuinely-uncomposed summand NAMED: the D2e Born-weight modulation)

The parent (DressingSeamResolved) resolved the DRESSING seam (branch (a): the /(1−w) depth-dressing
is way-point-forbidden drift; the physical composed object is LEADING-κ). This node ASSEMBLED the
leading-κ composed object as a determined closed form and established the D7 composition-completeness
attestation HONESTLY — and found the composition is NOT yet complete.

## WHAT WAS BANKED (Phys/Algebra/LeadingComposedAssembly.lean, C544, 11 decls foundations-only)

THE ASSEMBLY (theorem-exact):
  composed_leading_closed :
    invAlphaEM(completeBandList chargeTraceDepth) − projectedS·totalScreeningWith 1 (…)
      + totalScreening(freezeoutTail (edgeBottom..)(edgeCharm..)(edgeLight..))
    = 112 + (chargeTraceDepth − projectedS)·totalScreeningWith 1 (…)
          + (16/9 + κ·[(7/3)L_mb + (11/3)L_b0 − (5/3)L_b2])
  leading_is_dressed_at_w0 : invAlphaEM − s·T = dressedInvAlphaEM 0 s (…)   (excluded /(1−w) removed)

THE INVENTORY (each composed summand a theorem): recombination_composed (+70 weld, D2b),
antiscreen_composed (s = 1/8, projectedS_eq), join_contributes_zero (joinContent = 0 at derived j=1).

THE HONEST D7 ATTESTATION: composition_inventory_complete_up_to_bornweights — every banked summand
INCLUDED or proven zero EXCEPT the ONE genuinely-uncomposed summand.

## THE ONE GENUINELY-UNCOMPOSED SUMMAND (the ONE LAW signal, honest)

The freeze-out uses the PLATEAU censuses (11/3, 10/3, 2). But D2e (HadronicLevelWeights, banked)
enumerated that the freeze-out's FINE STRUCTURE is the per-level BORN OCCUPANCY of the mass-gap
spectrum #{w:O ℚ | IntLat w ∧ gForm w w = n}: floor weight = 16 = 2·dim(O ℚ) DERIVED EXACTLY, but
the general r₈(n), n≥2 (the Jacobi eight-square count) ROUTE-NOT-YET-FOUND. The plateau-census
freeze-out is the level-counting-BLIND approximation; the Born-weight modulation is the uncomposed
refinement.

NUMERIC (removable orientation prose, NO empirical number in any proof, workbench edges_signs.py):
  bare recombined invAlphaEM        ≈ 137.9476
  − anti-screening (s=1/8, s·T)     ≈ 0.6081  → 137.3394
  + freeze-out plateau summand      ≈ 1.6324  → 138.9718   (OVERSHOOTS the bare tower!)
  measured 1/α(0)                   ≈ 137.0360  (orientation only)
The plateau freeze-out drove the composition FURTHER from where the tower sat — the wrong thing at
the wrong magnitude. A composition that FIGHTS the target is INCOMPLETE, not a prediction. Stating a
≥15-digit bracket AS 1/α(0) here would be FALSE; pinning s≈1.9 to hit 137 would be G2-forbidden fit.

## THE SUCCESSOR'S TARGET (the directed route-finder)

DERIVE the D2e Born-weight modulation of the freeze-out fine structure — the general per-level
occupancy r₈(n), n≥2 (the Jacobi eight-square count on the banked gForm / IntLat over O ℚ), which
tightens the freeze-out from the plateau-census approximation to the Born-weight-modulated value —
then complete the freeze-out → the composition is COMPLETE → D7 restate AS 1/α(0) ≥15 sig digits →
D8 capstone → hand M1. NOT a runway (autonomous theory-native structural work remains: the Born-weight
modulation is a banked-object count, buildable — build Jacobi's eight-square count if Mathlib lacks it,
W1 step 1). The route-finder is the ONE forward child (REPLACES the generic SELECT; M1 not yet handed).

## GRADE
THEOREM-EXACT (assembly + inventory + attestation + teeth). WAY-POINT, NOT the ≥15-digit endpoint
(complete-up-to the D2e Born-weight modulation, route-not-yet-found). No fit, no empirical number.
