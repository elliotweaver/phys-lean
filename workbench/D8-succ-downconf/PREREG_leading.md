# PREREG — LeadingComposedAssembly: ASSEMBLE the way-point-consistent LEADING-κ composed 1/α(0)
#          as a determined closed form + the D7 composition-completeness ATTESTATION as a theorem
#          — with the ONE genuinely-uncomposed summand NAMED (the D2e Born-weight modulation),
#          route-finder childed (repeated-deferral protocol).

Node t_800fa6e0. DIRECTED SUCCESSOR of `DressingSeamResolved`. The parent RESOLVED the
dressing/recombination seam (branch (a): the depth-dressing is way-point-forbidden drift; the
physical composed object is LEADING-κ). This node ASSEMBLES the leading-κ composed object as a
determined closed form and establishes the D7 composition-completeness attestation — HONESTLY.

## PART 1 — THE ASSEMBLY (theorem-exact, real forward progress) — PROBE PASSES

The way-point-consistent LEADING-κ composed object assembles to a determined closed form of `Cut`
(probe `workbench/D8-succ-downconf/LeadingComposeProbe.lean` COMPILES clean):

  composed_leading (Mu Md Mlam) :=
      invAlphaEM (completeBandList chargeTraceDepth)                       [D2b +70 weld]
        − projectedS · totalScreeningWith 1 (completeBandList chargeTraceDepth)   [Z1 anti-screen, s=1/8]
        + totalScreening (freezeoutTail (edgeBottom..) (edgeCharm..) (edgeLight..))  [freeze-out plateau]
    = 112
      + (chargeTraceDepth − projectedS) · totalScreeningWith 1 (completeBandList chargeTraceDepth)
      + (16/9 + κ·[(7/3)L_mb + (11/3)L_b0 − (5/3)L_b2])

via `invAlphaEM_closed_charge` (112 + charge screening), `totalScreeningWith_homog` (census factors),
`freezeout_at_pinned_dissolved` (parent's determined freeze-out), then `ring`. Equivalently the leading-κ
anti-screened tower = `dressedInvAlphaEM 0 projectedS (completeBandList chargeTraceDepth)` (the D2c
dressed object AT w=0 — the parent's excluded /(1−w) removed). This is theorem-exact, foundations-only.

## PART 2 — THE COMPOSITION INVENTORY (what IS composed — theorems)

Every banked structural summand of the endpoint, and its composition status:
  • the two high bands + lepton tail (completeBandList)            — COMPOSED (invAlphaZero, X6/Z4/Z6)
  • the +70 recombination weld (isoTower + hyperTower)            — COMPOSED (D2b composed_endpoint)
  • the isospin anti-screening −s·T (s = projectedS = 1/8, Z1)    — COMPOSED (D2c, projectedS_eq)
  • the join content (j = joinSplitRatio = 1 ⇒ joinContent = 0)   — COMPOSED, contributes ZERO
        (JoinSplitRatioDerived joinContent_at_derived_eq_zero)
  • the freeze-out PLATEAU summand (censuses 11/3, 10/3, 2)        — COMPOSED (parent dissolved form)

## ⚠ PART 3 — THE ONE GENUINELY-UNCOMPOSED SUMMAND, NAMED (honest — the ONE LAW signal)

The D7 completeness gate (SEED_DIGITS §D7) requires EVERY banked structural contribution INCLUDED
or PROVEN zero. One remains genuinely uncomposed, and it is a BANKED, ENUMERATED contribution — the
D2e HADRONIC LEVEL WEIGHTS (`HadronicLevelWeights.lean`, banked):

  The freeze-out term uses the PLATEAU censuses (11/3, 10/3, 2) as the per-band screening weights.
  But the arc ITSELF enumerated (D2e / SEED_DIGITS §D2e) that the freeze-out's FINE STRUCTURE is the
  per-level BORN OCCUPANCY of the mass-gap spectrum — `#{ w : O ℚ | IntLat w ∧ gForm w w = n }`, the
  fold's own density-of-states count. D2e DERIVED the FLOOR weight EXACTLY (`floorOccupancy_eq_sixteen`:
  16 = 2·dim(O ℚ)) but flagged the GENERAL per-level weight r₈(n) for n≥2 as ROUTE-NOT-YET-FOUND (the
  classical Jacobi eight-square formula 16·σ₃*(n), Jacobi's theorem not banked in usable form). The
  plateau-census freeze-out is the LEVEL-COUNTING-BLIND approximation; the Born-weight modulation is
  the enumerated-but-uncomposed refinement. FreezeoutComposition.lean ITSELF flags the composition
  "remains PARTIAL … the dominant missing structural summand … not yet identified."

The ONE LAW confirms this from the numbers (removable orientation prose, NO empirical number in any
proof): the assembled leading-κ composed object lands at ≈138.97, OVERSHOOTING the bare recombined
tower (≈137.95) — adding the freeze-out drove it FURTHER from where the tower sat, and the plateau
freeze-out (+1.63) is doing the wrong thing at the wrong magnitude. A composition that FIGHTS the
target this way is INCOMPLETE, not a prediction (SEED_DIGITS §D7: "a disagreement from a composition
WITHOUT the attestation is not a prediction — it is an incomplete sum, and the missing summand is the
next child"). The Born-weight modulation of the freeze-out (D2e) is that missing summand — NAMED.

## THE DECISION (repeated-deferral protocol + SEED_DIGITS §D7)

1. BANK the theory-native forward progress: the assembled leading-κ composed object as a DETERMINED
   closed form (theorem-exact); the composition inventory (each composed summand a theorem); the join
   content = 0 attestation; the leading-κ = dressed-at-w0 identity.
2. Establish the completeness attestation as a HONEST theorem: the composition is COMPLETE-UP-TO the
   D2e Born-weight modulation of the freeze-out fine structure — every OTHER banked summand INCLUDED
   or proven zero, and the ONE remaining (D2e r₈(n), n≥2) NAMED, its structure identified (the Jacobi
   eight-square count on the banked gForm), route-not-yet-found. This is NOT a ≥15-digit endpoint — a
   bracket over this composition is a WAY-POINT, correctly named.
3. CHILD ONE directed route-finder toward the D2e Born-weight modulation (the freeze-out fine
   structure via the banked gForm level occupancy / Jacobi eight-square count), which then feeds the
   completed freeze-out → D7 restate AS 1/α(0) ≥15 sig digits → D8 → hand M1. This is NOT a runway
   (autonomous theory-native structural work remains — the Born-weight modulation is a banked-object
   count, buildable). The route-finder is the ONE forward child (REPLACES the generic SELECT).

## WHY THIS IS NOT BULLSHIT (S4 anti-bullshit gate)
1. FOLD-CAUSAL. Every composed summand banked back to the fold (invAlphaEM ← two-channel recombination;
   projectedS ← Casimir×depthWeight; freeze-out ← κ·census·cutLog of pinned edges ← fold phase +
   confinement rung). The assembly is pure banked cutLog/screening algebra + ring.
2. DISSOLUTION vs HONESTY. I do NOT force a ≥15-digit numeral over an incomplete sum (that would be
   FALSE — the composition overshoots by a determined amount) and I do NOT fit s to hit 137.036
   (G2-forbidden). I bank the determined assembly and NAME the one uncomposed summand — genuine
   forward progress WITHOUT a false or fitted numeral.
3. WORDS-REMOVABLE. Delete coupling/screening/freeze-out/census/Born: over Cut, the assembled closed
   form is pure cutLog/screening arithmetic on the banked objects; the completeness attestation is a
   statement about which banked summands appear in the sum. Survives.
4. NAME THE TEMPTATION. The saturated temptation: "the seam is resolved (parent), so the composition
   is now COMPLETE — narrow to 15 digits and call it 1/α(0)." REFUSED: the parent resolved the
   DRESSING seam (the /(1−w) drift), NOT the D2e Born-weight completeness. The numbers (138.97 overshoot)
   and the banked D2e/FreezeoutComposition flags show a genuinely-uncomposed summand remains. Declaring
   completeness here would be the exact "bless the easy adjacent step with a fold story" bullshit.
5. HONESTY. Assembly + inventory + attestation THEOREM-EXACT; the D2e Born-weight modulation NAMED,
   route-not-yet-found, childed.

## THE NODE: Phys/Algebra/LeadingComposedAssembly.lean
THEOREM-EXACT (exact identities of Cut, foundations-only):
  • composed_leading_closed          — the assembled leading-κ composed object = determined closed form
  • leading_is_dressed_at_w0         — = dressedInvAlphaEM 0 projectedS (charge list) + freeze-out
  • join_contributes_zero            — joinContent depthWeight joinSplitRatio = 0 (D2d, composed as zero)
  • antiscreen_composed              — the −s·T summand at s=projectedS=1/8 (projectedS_eq)
  • composition_inventory_complete_up_to_bornweights — the attestation: every banked summand INCLUDED
      or zero EXCEPT the NAMED D2e Born-weight modulation of the freeze-out (route-not-yet-found)
  • TEETH: composed_ne_bare (the anti-screen+freeze-out genuinely move the bare tower);
    antiscreen_positive (s·T > 0, the reduction is real); join_zero_genuine (joinContent=0 not vacuous —
    joinContent_pos at j>1 shows it's genuinely the derived j=1 that zeros it)
  • capstone leadingComposedAssembly_landing (welded, non-hollow)

⚠ GRADE (honest): the assembly + inventory + attestation are THEOREM-EXACT. The composition is
COMPLETE-UP-TO the D2e Born-weight modulation (route-not-yet-found), so this is a WAY-POINT, NOT the
≥15-digit endpoint. NO empirical number in any proof; 138.97/137.95/137.036/1.63 removable orientation
prose; NO-FIT explicit; ground field the DERIVED ℝ Cut.

## GUARDS
- G1 values fall out (the closed form from banked screening/cutLog algebra). G2 no empirical number.
- G3 exact. G4 ONE directed route-finder child (Born-weight modulation), linear tail. G5 words-removable.
- G6 foundations-only ⊆ {propext,Classical.choice,Quot.sound}; NO Mathlib ℝ as content; import guard
  DressingSeamResolved (transitive: parent + banked stack); NO foldRetention/MassGapOne/OneAxiom.
- G7 ONE LAW — the composition FIGHTS (overshoots) → the framing is incomplete → NAME the summand,
  child the route-finder; do NOT push a false numeral, do NOT fit s.
- G8 teeth per node.

## COST BUDGET / KILL
All content is rw[banked closed forms] + ring on Cut atoms (probe compiles instantly). Expected LIGHT
(gate-fast ~1 min). KILL: any single obligation >60s → decompose per-lemma. No brute normalizer.

## COSTUME C544, node LeadingComposedAssembly, capstone leadingComposedAssembly_landing.
