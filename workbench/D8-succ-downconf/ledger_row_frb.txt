| FreezeoutReweightingBounded | ARC-D REMAINDER (owner-directed t_076fc81e, directed route-finder successor of LeadingComposedAssembly) — CORRECT the parent's mis-selection (the "D2e Born-weight modulation of the freeze-out" is NOT the dominant missing summand of 1/α(0)) with a theorem-backed structural result, NAME the ACTUAL dominant missing summand, re-child ONE corrected route-finder from the trunk | Phys/Algebra/FreezeoutReweightingBounded.lean | THE FREEZE-OUT REWEIGHTING IS A BOUNDED, CENSUS-CONSERVING O(1) REFINEMENT — directed route-finder successor of LeadingComposedAssembly. THE SELECTION CATCH (SOUL: selection is the highest-stakes act; a finishing worker saturated with the machinery it just used is the worst chooser): the parent directed this node to "DERIVE the D2e Born-weight modulation of the freeze-out fine structure (build the Jacobi eight-square count r₈(n)=16·σ₃*(n) if Mathlib lacks it) → complete the freeze-out → D7 restate AS 1/α(0)". This node ran the ANTI-BULLSHIT GATE on that target FIRST and found it a MIS-SELECTION, refuted by a BANKED THEOREM. An EARLIER banked node in the SAME arc — FreezeoutComposition (imported transitively by the parent) — already proved as THEOREMS freezeout_bounded (totalScreening(freezeoutTail rb rc rl) ≤ κ·9·cutLog R) and composed_bounded (the composed endpoint exceeds the certified partial numeral by AT MOST κ·9·cutLog R), stating in-module "the freeze-out is a BOUNDED, logarithmically-small census refinement, NOT the dominant contribution … the dominant missing structural summand … its structure is not yet identified." The parent then named "the Born-weight modulation of the freeze-out" as the ONE missing summand — WRONG on two counts: (1) IT CONFLATES TWO DISTINCT COUNTS — the freeze-out band weights are the R-ratio CENSUS plateaus (11/3,10/3,2; the banked SubBandCompletion coloured slice), while the D2e "Born occupancy" r₈(n) (integer-lattice degeneracies 16,112,448,…) is a DIFFERENT object (the mass-gap density of states); there is NO banked derived map identifying them (grep: levelOccupancy/floorOccupancy appear in NO freeze-out/screening def); the level-1 coincidence r₈(1)=16=floor weight is not an identification of census with occupancy. (2) EVEN IF COMPOSED IT CANNOT COMPLETE THE SUM — this node PROVES the freeze-out under ANY nonneg per-band reweighting is bounded by κ·(Σw)·cutLog R (freezeout_reweight_bounded, generalizing the banked freezeout_bounded from the specific census to arbitrary weights), and a census-conserving reweighting (Σw=9) stays under the SAME κ·9·cutLog R ceiling (census_conserving_reweight_bounded); reweighting differs from the census freeze-out by a FIXED linear form in the edge logs (reweight_is_linear_perturbation), an O(1) perturbation, NEVER the dominant bulk. NUMERIC CONFIRMATION (removable orientation prose, NO empirical number in any proof, workbench/reweight_magnitude.py): census freeze-out +1.63, equal-weight (3,3,3) +1.92, skew (5,3,1) +0.97 — ANY census-9 reweighting stays O(1); the ≈1.94 composed overshoot above the measured region is NOT a freeze-out sub-effect. THE DELIVERABLE (a strictly stronger structural fact + a route correction): the general reweighting bound GENERALIZES the banked freezeout_bounded to arbitrary weights AND closes off the mis-selected route (grinding Jacobi's eight-square theorem for a proven-bounded phantom is REFUSED); the census instance IS the banked freeze-out (census_reweight_is_banked, non-hollow weld); the D2e occupancy 16 ≠ 9 the freeze-out census total (occupancy_ne_census_total) and 16 ≠ 11/3,10/3,2 (occupancy_ne_band_censuses), refuting the conflation; the banked composed_bounded is re-exported (freezeout_not_dominant). THE STRUCTURAL CEILING NAMED (REPEATED-DEFERRAL PROTOCOL): the "dominant missing summand of 1/α(0)" front has been deferred across ≥5 nodes and the latest MIS-attributed the gap to a bounded freeze-out sub-effect; the banked composition tower (ceiling 42, +70 weld →112, anti-screening at s=1/8, BOUNDED freeze-out) is PROVABLY insufficient to reach the measured region as a completeness theorem, and the missing summand is NOT in the freeze-out layer — it must be identified FROM THE TRUNK (a recombination/anti-screening-layer summand, OR the diagnosis that the ABSOLUTE 1/α(0) numeral awaits arc M's quark-mass ℝ-descent, the edges rb,rc,rl being quark-threshold ratios verified route-not-yet-found across Z3/X9/ZArcValidationCapstone/SEED_MASSES, so D's endpoint is a WAY-POINT until M supplies the ℝ-descent). GRADE: THEOREM-EXACT now (census_reweight_is_banked, freezeout_reweight_bounded, census_conserving_reweight_bounded, reweight_is_linear_perturbation, reweight_delta_from_totalScreening, occupancy_ne_census_total, occupancy_ne_band_censuses, freezeout_not_dominant, teeth — exact inequalities/identities of Cut + exact natural counts); ROUTE-NOT-YET-FOUND (NAMED, never asserted/fitted): the ACTUAL dominant missing summand of 1/α(0), identified from the trunk, handed to the ONE corrected route-finder successor. The composed object stays a WAY-POINT, NOT the ≥15-digit endpoint. ⭐ ONE CAUSE: cutLog's monotonicity (banked cutLog_mono) caps each band by the largest edge and κ>0 scales — the SAME census-conservation ceiling structure the banked node found, now for ANY weighting; the freeze-out cannot be the dominant summand under census OR Born occupancy. NO fit (no 137.036/0.231/PDG/Λ_QCD in any statement or proof; 138.97/137.95/1.63/1.94 removable orientation prose; fit s≈1.9 FORBIDDEN, not used; grinding Jacobi's theorem for the bounded phantom REFUSED). Physics-words-removable (delete coupling/screening/freeze-out/census/Born/occupancy/hadronic: over Cut, for nonneg wb,wc,wl and 1<rb,rc,rl≤R, κ(wb·cutLog rb+wc·cutLog rc+wl·cutLog rl)≤κ(wb+wc+wl)·cutLog R; =totalScreening(freezeoutTail) at the census weights; and (Nat.card(levelOccupancy 1):Cut)=16≠9), not free-floating (every TYPE cites kappaLeading/cutLog/cutLog_mono/totalScreening/freezeoutTail/censusBottomBand/composed_bounded/levelOccupancy/floorOccupancy_eq_sixteen/invAlphaZero/completeFreezeoutList over Cut/O ℚ). GATE-FAST D0–D6: pending (background). 12 decls foundations-only ⊆ {propext,Classical.choice,Quot.sound}; C545 bites 1=545 (occupancy_ne_census_total: 16≠9, the D2e Born occupancy count and the freeze-out census total are genuinely distinct objects — the parent's conflation refuted). Build clean (lake build Phys EXIT 0, Built FreezeoutReweightingBounded 23s, LIGHT — ring/linarith/mul_le_mul_of_nonneg_left/norm_num/positivity + banked cutLog_mono/kappaLeading_pos/freezeout_closed/composed_bounded cites, no brute, no native_decide, no heartbeat raise). Scratch probes workbench/D8-succ-downconf/{PREREG_reweight.md,ReweightProbe.lean,ReweightProbe2.lean,reweight_magnitude.py,AuditReweight.lean} (all EXIT 0, isolated reaudit 12 decls foundations-only). Directed CORRECTED route-finder successor toward the ACTUAL dominant missing summand of 1/α(0) from the trunk (recombination/anti-screening-layer summand OR the arc-M ℝ-descent diagnosis) → then D7 restate AS 1/α(0) ≥15 sig digits → D8 → hand M1. NO generic SELECT (rail: a directed child REPLACES the generic SELECT; M1 not yet handed). NOT a runway (autonomous theory-native structural work remains — identifying the dominant missing summand from the trunk is a buildable route-finding target). |
