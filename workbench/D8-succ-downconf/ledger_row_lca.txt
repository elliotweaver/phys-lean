| LeadingComposedAssembly | ARC-D REMAINDER (owner-directed t_800fa6e0, directed successor of DressingSeamResolved) — ASSEMBLE the way-point-consistent LEADING-κ composed 1/α(0) as a determined closed form + establish the D7 composition-completeness ATTESTATION honestly; the ONE genuinely-uncomposed summand NAMED (the D2e Born-weight modulation of the freeze-out fine structure), route-finder childed | Phys/Algebra/LeadingComposedAssembly.lean | THE LEADING-κ COMPOSED 1/α(0) ASSEMBLED AS A DETERMINED CLOSED FORM + THE HONEST D7 ATTESTATION — directed successor of DressingSeamResolved (which resolved the dressing seam: branch (a), the /(1−w) depth-dressing is way-point-forbidden drift, the physical object is leading-κ). THE ASSEMBLY (theorem-exact): the way-point-consistent leading-κ composed object — the banked +70-recombined tower invAlphaEM(completeBandList chargeTraceDepth), MINUS the isospin anti-screening −s·totalScreeningWith 1 (…) at s=projectedS=1/8, PLUS the parent's determined freeze-out summand at the pinned edges — assembles to a DETERMINED closed form of Cut: 112 + (chargeTraceDepth−projectedS)·totalScreeningWith 1 (completeBandList chargeTraceDepth) + (16/9 + κ·[(7/3)L_mb+(11/3)L_b0−(5/3)L_b2]) (composed_leading_closed, via invAlphaEM_closed_charge + totalScreeningWith_homog + parent freezeout_at_pinned_dissolved + ring). Equivalently the leading-κ anti-screened tower IS the D2c dressed object AT w=0 (leading_is_dressed_at_w0: invAlphaEM − s·T = dressedInvAlphaEM 0 s (…) — the parent's excluded /(1−w) removed). THE COMPOSITION INVENTORY (each a theorem): the +70 recombination weld COMPOSED (recombination_composed = D2b composed_endpoint), the anti-screening COMPOSED at the derived s=1/8 (antiscreen_composed = projectedS_eq), the join content COMPOSED as ZERO (join_contributes_zero: joinContent depthWeight joinSplitRatio = 0, the derived j=1 zeros it, joinContent_at_derived_eq_zero). ⚠ THE HONEST D7 ATTESTATION (composition_inventory_complete_up_to_bornweights): every banked structural summand is INCLUDED or PROVEN zero EXCEPT the ONE genuinely-uncomposed summand — the D2e HADRONIC LEVEL WEIGHTS (HadronicLevelWeights.lean). The freeze-out uses the PLATEAU censuses (11/3,10/3,2) as per-band weights, but the arc ITSELF enumerated (D2e/SEED_DIGITS §D2e) that the freeze-out's FINE STRUCTURE is the per-level BORN OCCUPANCY of the mass-gap spectrum #{w:O ℚ | IntLat w ∧ gForm w w=n}; D2e DERIVED the FLOOR weight EXACTLY (floorOccupancy_eq_sixteen: 16=2·dim(O ℚ)) but flagged the GENERAL per-level weight r₈(n), n≥2 (the Jacobi eight-square count 16·σ₃*(n), Jacobi's theorem not banked usable) as ROUTE-NOT-YET-FOUND. The plateau-census freeze-out is the level-counting-BLIND approximation; the Born-weight modulation is the enumerated-but-uncomposed refinement (FreezeoutComposition ITSELF flags the composition \"remains PARTIAL … the dominant missing structural summand … not yet identified\"). THE ONE LAW confirms it from the numbers (removable orientation prose, NO empirical number in any proof): the assembled leading-κ composed object lands ≈138.97, OVERSHOOTING the bare recombined tower ≈137.95 — the plateau freeze-out drove it FURTHER, doing the wrong thing at the wrong magnitude; a composition that FIGHTS the target this way is INCOMPLETE, not a prediction (SEED_DIGITS §D7: a disagreement from a composition WITHOUT the attestation is an incomplete sum, and the missing summand is the next child). So this is a WAY-POINT, NOT the ≥15-digit endpoint. GRADE: THEOREM-EXACT now (composed_leading_closed, leading_is_dressed_at_w0, recombination_composed, antiscreen_composed, join_contributes_zero, composition_inventory_complete_up_to_bornweights, teeth — exact identities of Cut / populated-level counts); ROUTE-NOT-YET-FOUND (NAMED, never asserted/fitted): the D2e Born-weight modulation of the freeze-out fine structure (the general per-level occupancy r₈(n), n≥2, the Jacobi eight-square count on the banked gForm) — the ONE genuinely-uncomposed summand, handed to the ONE directed route-finder successor (complete the freeze-out fine structure → D7 restate AS 1/α(0) ≥15 sig digits → D8 → hand M1). ⭐ ONE CAUSE: the SAME banked Born self-overlap gForm=Σ8 coord² that quantizes the mass-gap spectrum to ℕ AND fixes the floor value to 1 ALSO fixes the floor multiplicity to 2·dim(O)=16 — one positive-definite integer quadratic form; the freeze-out's uncomposed fine structure is the higher-level count of that SAME form. NO fit (no 137.036/0.231/PDG in any statement or proof; composed 138.97, bare 137.95, freeze-out 1.63 removable orientation prose; fit s≈1.9 FORBIDDEN, not used). Physics-words-removable (delete coupling/screening/freeze-out/census/Born/hadronic: the assembled closed form is pure cutLog/screening arithmetic on banked invAlphaEM/totalScreeningWith/totalScreening/freezeoutTail/projectedS/joinContent over Cut; the attestation is a statement about which banked summands appear in the sum), not free-floating (every TYPE cites invAlphaEM/invAlphaEM_closed_charge/composed_endpoint (D2b), totalScreeningWith/totalScreeningWith_homog/dressedInvAlphaEM (D2c), projectedS/projectedS_eq, joinContent/joinContent_at_derived_eq_zero/joinContent_pos (D2d), freezeoutTail/totalScreening/edgeBottom/edgeCharm/edgeLight (D8/N340), freezeout_at_pinned_dissolved/kappaLeading/bMass/mbRatioDerived (parent stack), completeBandList/chargeTraceDepth (X6/Z6), floorOccupancy_eq_sixteen/levelOccupancy (D2e) over Cut/O ℚ). GATE-FAST D0–D6: pending (background). 11 decls foundations-only ⊆ {propext,Classical.choice,Quot.sound}; C544 bites 1=544 (bornweight_floor_nonzero: Nat.card(levelOccupancy 1)=16≠0 — the uncomposed Born-weight modulation acts on genuinely-populated levels, a real missing summand). Build clean (lake build Phys.Algebra.LeadingComposedAssembly EXIT 0, Built 11s, LIGHT — rw[banked closed forms]+ring+positivity+linarith on Cut atoms, no brute, no heartbeat raise). Scratch probe workbench/D8-succ-downconf/{PREREG_leading.md,LeadingComposeProbe.lean,leading_compose.py,edges_signs.py,AuditLeading.lean}. Directed route-finder successor toward the D2e Born-weight modulation of the freeze-out fine structure → complete the freeze-out → D7 restate AS 1/α(0) ≥15 sig digits → D8 capstone → hand M1. NO generic SELECT (rail: a directed child REPLACES the generic SELECT; M1 not yet handed). NOT a runway (autonomous theory-native structural work remains — the Born-weight modulation is a banked-object count, buildable). |
