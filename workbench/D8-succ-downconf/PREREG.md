# PREREG — D8-succ-downconf: THE DOWN/CONFINEMENT RELATIVE-SCALE LEG = THE CONFINEMENT BUNDLE

Node t_afa60160. DIRECTED SUCCESSOR of `UpDownRelativeScaleFoldPhase` (parent DERIVED the DOMINANT
up/down leg `(Md/Mu)² = mbRatioDerived`). This node DERIVES the REMAINING leg — the down/confinement
offset `Md/Mλ` — theory-native, WELDS it to the parent's up/down leg → the FULL up/down/confinement
relative-scale assignment pins → the grandparent's freeze-out edges pin from bounds to VALUES.

## THE THEORY-NATIVE HEART (what the fold's own two BANKED tower rungs say)

The confinement floor is BANKED, theory-native, no fit:
- X9 `QuarkHadronTower.confBundle = confinementScaleRatio · topBandRatio = Λ/v = cutExp(−8π/3)`
  (`confBundle_eq`), the octonion colour/space quotient-gap `84π·(1/7 − 1/9) = 8π/3` of the ONE 84π
  budget. Banked positivity (`confBundle_pos`), strict suppression `< 1` (`confBundle_lt_one`).
- arc-I `ConfinementHadronicMass.confinementScaleRatio = Λ/M = cutExp(−12π)` (the colour n=7 rung).

The down-sector cascade scale is ALSO BANKED, parameter-free:
- arc-O O2 `ScaleTowerDischargeArcM.absScaleChainDerived = Md/M` (N417) — a DETERMINED derived `Cut`,
  `0 < absScaleChainDerived < ewClosureRatio < 1`, factoring `absScaleChainDerived = ewClosureRatio ·
  (cutCos foldPhase · s1BornProb · 1 · cutCos(2/3))` (`absScaleChainDerived_eq_anchor_mul`).

★ THE LEG READS OFF THE TWO BANKED RUNGS (exactly as the up/down leg read off the fold phase). Two
complementary theorem-exact readings, both with the SHARED electroweak edge `v/M = ewClosureRatio`
CANCELING:

(A) THE TOWER-RUNG READING (route-COMPLETE, no hypothesis):
    downConfLeg := absScaleChainDerived / confinementScaleRatio = (Md/M)/(Λ/M) = Md/Λ
    and the shared edge cancels: `downConfLeg = (cutCos foldPhase · s1BornProb · 1 · cutCos(2/3)) /
    confBundle` (`downConfLeg_edge_cancel`, from `absScaleChainDerived_eq_anchor_mul` +
    `confinement_eq_edge_bundle`). A DETERMINED derived `Cut`, no free parameter.

(B) THE RELATIVE-SCALE-SQUARED READING (welds to the parent, parallel to `(Md/Mu)² = mbRatioDerived`):
    downConfBundle := mbRatioDerived / confBundle
    = the parent's up/down fold-phase Born weight (Md/Mu)² DIVIDED by the confinement bundle Λ/v.
    Under the top-at-anchor identification `(Mu/Mλ)² = 1/confBundle` (the up-sector scale relative to
    the confinement floor IS the banked confBundle Λ/v — the confinement-sector analogue of the top
    sitting at the electroweak anchor), this IS `(Md/Mλ)²`:
      (Md/Mλ)² = (Md/Mu)²·(Mu/Mλ)² = mbRatioDerived · (1/confBundle) = downConfBundle.

★ THE WELD TO THE GRANDPARENT'S edgeLight (the light-band edge pins). In the grandparent tower
`edgeLight Md lam = sectorMass Md 2 / lam`. With the confinement floor as a Born-square scale `lam =
Mλ²`, `edgeLight Md (Mλ²) = (Md/Mλ)²·bMass 2` (`edgeLight_weld`, pure `sectorMass_eq` arithmetic),
so under (B):
    edgeLight Md (Mλ²) = downConfBundle · bMass 2     (`edgeLight_pinned`)
— a DETERMINED derived value. The grandparent's `rl` light-band edge pins from route-not-yet-found to
a value. The `rb`/`rc` edges ALREADY pin from the parent's up/down leg (`edgeBottom_pinned` =
`mbRatioDerived·bMass0/bMass2`, `edgeCharm_pinned` = `1/mbRatioDerived`). So the FULL up/down/
confinement relative-scale assignment is DERIVED, and the freeze-out summand pins
(`freezeout_summand_pins`, the banked `freezeout_at_derived_edges` at the pinned edges).

Numerically (removable prose, NEVER a claim, NEVER fit): downConfBundle = (Md/Mλ)² ≈ 72.84,
Md/Mλ ≈ 8.53; Md/v ≈ 0.484; confBundle Λ/v ≈ 2.30e-4. Leading-order / candidate consistency only.

## GRADE (honest, per piece — SOUL "route COMPLETE vs route NOT-YET-FOUND")

THEOREM-EXACT NOW (exact identities/inequalities of `Cut`, foundations-only, banked-rung arithmetic):
`downConfLeg_edge_cancel` (the two-rung ratio with the edge canceling); `downConfBundle` closed as
`mbRatioDerived/confBundle` + positivity; `edgeLight_weld` (`sectorMass_eq`); `edgeLight_pinned` (the
weld, given the two relative-scale identifications as hypotheses — pure algebra); `edgeBottom_pinned`,
`edgeCharm_pinned` (the up/down edges pin from the parent leg); `freezeout_summand_pins` (banked
closed form at the pinned edges); teeth.

CANDIDATE / MECHANISM (owner QT1/confinement route genre, NEVER fit — the physical IDENTIFICATION,
flagged prose, NEVER a physics-words-load-bearing theorem): that `confinementScaleRatio`/`confBundle`
IS the physical confinement floor Λ, that `absScaleChainDerived` IS the physical down-sector scale
Md, and that the top-at-anchor identification `(Mu/Mλ)² = 1/confBundle` holds physically. The
downConfBundle ≈ 72.84 / (Md/Mλ ≈ 8.53) numeric is REMOVABLE PROSE; no empirical number in any proof.

The two relative-scale identifications `(Md/Mu)²=mbRatioDerived` and `(Mu/Mλ)²=1/confBundle` enter
`edgeLight_pinned` as HYPOTHESES (typed premises), NOT asserted equalities — the theorem says "IF the
up/down leg is the fold-phase weight AND the up/confinement offset is the confinement bundle, THEN the
light edge is downConfBundle·bMass2". The first hypothesis is the parent's banked landing; the second
is the confinement-sector top-at-anchor identification (candidate). Both are banked/derived objects,
not free reals — the leg is a determined function of the two banked rungs.

## WHY THIS IS NOT BULLSHIT (S4 anti-bullshit gate, in writing)

1. FOLD-CAUSAL. Both rungs banked back to the fold: confBundle = Λ/v ← the octonion 84π colour/space
   quotient (X9 ← arc-I ← N2 cascade-stop); absScaleChainDerived = Md/M ← the arc-M cascade anchored
   at ewClosureRatio × the DERIVED fold-phase amplitude cutCos(cutPi/8) (N416) × the banked survival
   2/3 (N308). The leg is the RATIO of two forced rungs of the ONE closure anchor M — the shared
   electroweak edge cancels. No new atom, no free real.
2. DISSOLUTION. The SM treats Λ_QCD and the down-quark Yukawa scale as unrelated inputs measured at
   unrelated scales. The theory reads BOTH off the ONE 84π-budget tower (confinement = colour n=7
   rung; down cascade = arc-M anchor), so their ratio is a determined exponential quotient — a genuine
   collapse of two free dials, not a relabel.
3. WORDS-REMOVABLE. Delete confinement/down/quark/hadron/mass/scale/Λ: over Cut, downConfLeg =
   absScaleChainDerived/confinementScaleRatio = (banked factor bundle)/confBundle (edge cancels);
   downConfBundle = mbRatioDerived/confBundle; edgeLight Md (Mλ²) = (Md/Mλ)²·bMass2; under the two
   ratio hypotheses = downConfBundle·bMass2. Pure arithmetic on banked cutExp/cutCos/mbRatioDerived/
   confBundle/sectorMass. Survives; the reason it's next (read the parent's remaining leg off the two
   banked confinement rungs) survives.
4. NAME THE TEMPTATION. The saturated temptation: assert Md/Mλ = PDG m_s/Λ_QCD (FIT — forbidden), OR
   fabricate a down-sector Yukawa texture. REFUSED both. The NEW content is the two-rung ratio + the
   edge-cancellation + the edgeLight weld — genuinely new, and it PINS the four-times-deferred light
   edge, not repackages.
5. HONESTY. The physical identifications are candidate; the Cut-arithmetic is theorem-exact. No PDG
   number in any proof; no fit.

## GUARDS
- No scale/ratio value asserted or fitted; downConfBundle/downConfLeg FALL OUT of banked confBundle +
  absScaleChainDerived + mbRatioDerived.
- No 137.036/0.231/PDG m_s/Λ_QCD in any proof; measured comparison REMOVABLE PROSE only. Explicit
  NO-FIT.
- BANKED derived ℝ Cut + confBundle/confinementScaleRatio (X9/arc-I) + absScaleChainDerived (arc-O
  O2) + mbRatioDerived (parent) + sectorMass/bMass/edgeLight (grandparent N340 shape) + cutExp/cutCos.
  NO Mathlib-ℝ as content.
- Foundations-only: #print axioms ⊆ {propext, Classical.choice, Quot.sound}. IMPORT GUARD:
  UpDownRelativeScaleFoldPhase (parent), QuarkHadronTower (X9), ScaleTowerDischargeArcM (arc-O O2) —
  all foundations-only; NO foldRetention/MassGapOne/OneAxiom.
- Free-floating rail: every production TYPE cites confBundle/confinementScaleRatio/absScaleChainDerived/
  mbRatioDerived/edgeLight/sectorMass/bMass. downConfBundle is literally mbRatioDerived÷the banked
  confinement bundle. FALSE over an algebra without the banked confinement/cascade rungs.
- ONE successor on the chain tail (directed toward the re-narrow → D7 restate AS 1/α(0) ≥15 digits →
  D8 → hand M1), no fork.

## COST BUDGET / KILL
All content is `field_simp`/`ring`/`nlinarith`/`div_pos` on banked Cut brackets. Probes 1/2/3 compiled
EXIT 0. Expected LIGHT (gate-fast ~1 min). KILL: any single obligation >60s → decompose per-lemma. No
unbounded compile; no brute normalizer.

## PRODUCTION FILE
`Phys/Algebra/DownConfinementRelativeScale.lean`, namespace
`Phys.Algebra.DownConfinementRelativeScale`. Capstone `downConf_relativeScale_confinementBundle`.
Costume C541. Node `DownConfinementRelativeScale`.
