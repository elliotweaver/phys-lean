# NOTE — LeadingCarrierExhausted (t_58f0f1ea): the CORRECTED route-finder's finding — the front is CLOSED

## THE TASK vs THE FINDING

The task asked: identify the ACTUAL dominant missing summand of 1/α(0) FROM THE TRUNK, surveying
theory-native (a) a recombination/anti-screening-layer summand OR (b) the arc-M ℝ-descent diagnosis.
Running the SELECT protocol + anti-bullshit gate FIRST (SOUL: selection is the highest-stakes act; a
finishing worker saturated with the composition machinery is the worst chooser) DERIVED the answer —
and it is NEITHER (a) nor (b) as stated; it is the sharper theorem-backed finding that CLOSES the front.

## (a) REFUTED — the leading inventory is EXHAUSTED

Every enumerated leading summand of the composed 1/α(0) is composed / derived / zero / bounded:
  • +70 recombination weld  — COMPOSED (RecombinedEndpoint.composed_endpoint; both channels present,
    the charge census splits FREE, only the second GUT ceiling 70 is new content)
  • anti-screening s=1/8     — DERIVED (CasimirProjectionSelectsS.projectedS_eq; NO route-not-yet-found)
  • join content            — ZERO (JoinSplitRatioDerived.joinContent_at_derived_eq_zero at j=1)
  • freeze-out              — BOUNDED under ANY reweighting (parent freezeout_reweight_bounded)
There is NO enumerated-but-uncomposed leading summand. (a) is empty.

## (b)'s RATIONALE REFINED — the edges are ALREADY pinned to derived closed reals

The task's (b) says D "awaits arc-M's ℝ-descent that pins the edges rb,rc,rl." FALSE: the edges are
pinned to DERIVED closed reals — freezeout_at_pinned_dissolved gives rb=mbRatioDerived·(bMass0/bMass2),
rc=1/mbRatioDerived, rl=downConfBundle·bMass2; FreezeoutEdgeCrossSectorReduction DISCHARGED the
within-sector shape to the banked N340 numerals and collapsed the cross-sector residual to the
relative scale, itself pinned by the fold phase (mbRatioDerived, N416) + the confinement weld
(confBundle, X9). So D is NOT blocked on unpinned edges.

## THE HONEST DIAGNOSIS — the leading carrier is DETERMINED, the front is CLOSED

Every leading summand composed/derived/zero/bounded AND the edges pinned ⇒ the leading composed
carrier is a DETERMINED closed real (leading_carrier_determined, re-export composed_leading_closed):
  leadingCarrier = 112 + (chargeTraceDepth−projectedS)·totalScreeningWith 1 (…)
                       + (16/9 + κ·[(7/3)L_mb + (11/3)L_b0 − (5/3)L_b2])
— NO free parameter, NO uncomposed leading summand. Its residual above the certified recombined
endpoint is the determined closed real totalScreening(freezeout)−projectedS·totalScreeningWith 1 (…)
(residual_above_recombined, NEW ring identity). The "dominant missing summand of 1/α(0)" front —
deferred ≥5 nodes — is CLOSED at the leading census layer: there is none; the leading carrier is a
DETERMINED WAY-POINT.

The ≥15-sig-digit AS-1/α(0) restate is NOT available from the leading layer: the determined value's
residual to the measured constant is genuinely SUB-LEADING structure the leading census tower provably
lacks; pinning it via the fit s≈1.9 is G2-forbidden and refused. Per the OWNER-BINDING board order
D→M→C→GR (SEED_DIGITS §D8), the ONE directed successor is arc-M target M1 (the unit peg / absolute-
scale skeleton); D's ≥15-digit restate resumes AFTER the dimensionful skeleton exists.

## WHAT WAS BANKED (Phys/Algebra/LeadingCarrierExhausted.lean, C546, 9 decls foundations-only)
leadingCarrier (def), leading_carrier_determined, residual_above_recombined (NEW ring),
residual_freezeout_pinned, leading_inventory_exhausted, freezeout_general_bounded,
residual_antiscreen_pos (teeth), residual_counts_distinct (teeth), leadingCarrierExhausted_landing.

## THE SUCCESSOR (the ONE directed child — REPLACES the generic SELECT)
Arc-M target M1 (SEED_MASSES §M1): THE ONE UNIT PEG — declare the chain's single dimensionful
convention as ONE flagged module (peg maps the banked M-scale tower to the human unit; unique-up-to-
units theorem). Carry the deferred-D7 note. Board order D→M→C→GR.

## GRADE
THEOREM-EXACT (all re-exports + one ring identity, exact identities/inequalities of Cut, foundations-
only). The front CLOSED at the leading layer (theorem). D's leading carrier a DETERMINED WAY-POINT;
the ≥15-digit AS-1/α(0) restate DEFERRED to resume after arc M. No fit, no empirical number in any
proof. Composed object stays a WAY-POINT, NOT the ≥15-digit endpoint.
