# N135 PREREG — THE GLOBAL COVER HOMOMORPHISM Pin(1,9) ↠ SO⁺(1,9) (the twisted adjoint)

FROZEN before compute. KILL classifies a fight as INSTRUMENT failure, not theory failure.

## THE MEASURED FRONT (W1/W9 measure-first, W4.5 advance-not-repin)
N132 closed the Lorentz sector (every group element = a finite reflection word,
`reflection_closure_eq_bvIsomLinMonoid`). N133 banked the generator-level cover WITH A SIGN
(`reflection_clifford_conj : ι(houseHolderM a v) = -(QvC a)⁻¹•(ι a·ι v·ι a)`). N134 banked the
even/Spin two-generator cover (`cover_compose_two`, sign-free for EVEN words). The forward joint
(priority (i)) the even/Spin cover OPENS is the GLOBAL cover homomorphism along an ARBITRARY
reflection WORD — the SURJECTIVITY `Pin(1,9) ↠ SO⁺(1,9)` as a derived map, NOT a posited group.

## THE THEORY-NATIVE REFRAME (W1 step 2 — the sign DISSOLVES)
The N133 sign `-(QvC a)⁻¹` is exactly what the TWISTED adjoint (grade involution) absorbs.
Mathlib `CliffordAlgebra.involute` (`involute_ι : involute(ι m) = -ι m`) gives the textbook Pin
cover map `Ad'_u(x) := involute(u)·x·u⁻¹`. At the generator level:
  involute(ι a)·ι v·(ι a)⁻¹ = (-ι a)·ι v·((QvC a)⁻¹•ι a) = -(QvC a)⁻¹•(ι a·ι v·ι a) = ι(houseHolderM a v)
— SIGN-FREE, the clean cover at the generator level (N133's sign absorbed by involute).
The twisted adjoint COMPOSES: Ad'_{uw} = Ad'_u ∘ Ad'_w (involute multiplicative + mul_inv_rev),
so along a reflection WORD `g = R₁···Rₙ` the covering unit is `u = ι a₁···ι aₙ` (a Pin element),
and `Ad'_u(ι v) = ι(g v)`. By N132 every Lorentz element IS such a word ⟹ GLOBAL surjectivity.

## GO / NO-GO
GO target (priority i — the forward joint, NOT a lateral re-pin):
  (A) `twistedAdjoint_ι_reflection` — the SIGN-FREE generator cover via twisted adjoint:
      `involute(ι a)·ι v·(↑(cliff_unit a)⁻¹) = ι(houseHolderM a v)`  [smallest obligation; MEASURE]
  (B) `cover_global_exists` — THE GLOBAL HOMOMORPHISM / SURJECTIVITY: for every
      `g ∈ Submonoid.closure houseHolderMSet` (= bvIsomLinMonoid, N132 = SO⁺(1,9)), there EXISTS a
      Clifford UNIT `u : CliffCˣ` with `∀ v, involute(↑u)·ι v·↑u⁻¹ = ι(g v)`. By `closure_induction`.
  (C) `cover_global_bvIsom` — restated over `bvIsomLinMonoid` (every BvC-isometry covered), via
      `reflection_closure_eq_bvIsomLinMonoid` (N132).
  W8 teeth: a concrete instance (the timelike reflection covered) + a numeric costume read-off.

NO-GO / DECOMPOSE: if `closure_induction` mul-case associativity fights (it should not — it is
`noncomm_ring`/`mul_assoc` over the Units coercion), bank (A) + the single-generator framing and
child the global induction. If the Units bookkeeping resists, bank (A) and child (B).

## KILL (instrument budget — W9)
- Per probe: bounded `lake env lean` on an ISOLATED file. KILL a probe at > 120s wall.
- No `maxHeartbeats` inflation. No monolithic brute over coordinates. The proofs are structural
  rewrites (`involute_ι`, `cliffSq`, `mul_inv_rev`, `mul_assoc`, `noncomm_ring`) — if any single
  obligation needs `ring`/`simp` over a fully-expanded coordinate STVC goal, that is the wrong
  framing → STOP, reframe through the Clifford relation (cliffSandwich/reflection_clifford_conj).
- ≥2 probes ending dead on the SAME obligation having banked nothing ⟹ W1 dissolution ticket.

## ANTI-DRIFT (THE ONE LAW)
If the twisted adjoint "doesn't compose cleanly", that is an INSTRUMENT framing issue (Units
bookkeeping), NOT a theory failure — the cover IS a homomorphism. Return to: involute is an
AlgHom (multiplicative), so Ad'_{uw}=Ad'_u∘Ad'_w is forced. Never weaken to "covers up to sign".

## COSTUME C167 (W8 — must BITE)
A WRONG global-cover / twisted-adjoint / Pin-word read-off reducing to a FALSE numeric DISTINCT
from C115 1=7 … C166 1=134. Plan: `1 = 135`. Reuse a banked read-off (the timelike twisted-adjoint
cover value or the Spin scalar = 1); claiming 135 forces 1=135. ERE PASS_SIGNATURE trailing substring.

## PHYSICS-WORDS-REMOVABLE
Delete cover/Pin/Spin/spinor/reflection/Householder/Lorentz/Minkowski/timelike/isometry/
homomorphism/surjectivity/word/generator/grade/involution → pure algebra over the derived Cut,
STVC, BvC, the Clifford algebra of QvCQuad: for every g in the submonoid generated by the
reflection maps, there is a unit u with involute(u)·ι v·u⁻¹ = ι(g v). No statement needs a physics word.
