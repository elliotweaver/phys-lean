# PREREG — C1 THE FRAME THEOREM (UNDRESSED-FORCED), owner sign-off (A) (t_4e5c5d5a, run 866)

## OWNER SIGN-OFF (A) — the C1 decree WITHDRAWN; my run-865 adjudication CORRECT
The manager's ±s·T ledger inconsistency is REAL and is decided by the chain's OWN structure — but
the structure selects the **UNDRESSED** s-term (→ leadingCarrier), and the decreed DRESSED frame
(→ measured) is the whole-stack reading the s-derivation rejects (the fit-signature). Execute (A):
 (1) bank C1 UNDRESSED-FORCED — consistent leading composition = banked `leadingCarrier`, s-term undressed;
 (2) fix false "137.03x window" prose on `CasimirProjectionSelectsS` + `ComposedNumeralSPinned`;
     exclude the dressed reading under its true name (whole-stack = rejected);
 (3) NAME the +1.94 residual as THE DEPTH-2 RUNNING CONTENT — the ONE directed successor front.

## THE FRAME THEOREM (math, all banked objects, physics-words-removable)
Let U := totalScreeningWith 1 (completeBandList chargeTraceDepth) > 0 (screeningUnit_pos).
- UNDRESSED anti-screening term = `projectedS · U` (banked; the term inside `leadingCarrier` via
  `residual_above_recombined`: leadingCarrier − invAlphaEM(...) = totalScreening(freezeout) − projectedS·U).
- DRESSED effective magnitude = `projectedS / (1 − depthWeight)` = 3/16
  = `casimirRatio · (depthWeight/(1−depthWeight))` (the WHOLE-STACK Casimir reading, weight 1/2).
- The derived `projectedS = casimirRatio · depthWeight` = 1/8 (ONE depth, weight 1/3), banked.
- TEETH: `3/16 ≠ 1/8` (dressed overturns derived s); `depthWeight/(1−depthWeight)=1/2 ≠ depthWeight=1/3`
  (dressing re-selects the whole-stack weight, not the one-depth weight the s-derivation used).
- NO BAND-SELECTIVE SPLIT: the D2c carrier dresses s and tower by the SAME /(1−w)
  (`dressedInvAlphaEM_antiscreen`: dressedInvAlphaEM w s = dressedInvAlphaEM w 0 − (s·U)/(1−w));
  s dressed ⟺ tower dressed. The way-points force w=0 on that shared carrier
  (`dressed_reproduces_only_at_w0`), so the s-term is UNDRESSED at the same w as the tower.
- ⇒ CONSISTENT COMPOSITION = banked undressed `leadingCarrier` (`leading_carrier_determined`),
  residual = undressed `projectedS·U` (`residual_above_recombined`).
- Inflation tooth: `projectedS·U < projectedS·U/(1−depthWeight)` (dressing genuinely inflates; U>0).

## ANTI-BULLSHIT GATE (honest)
- Trace: fold → cascade-stop → su(2)_L Casimir dichotomy (N277/N279) → casimirRatio 3/8 →
  × depthWeight 1/3 (ONE depth, D3) = projectedS 1/8 (banked) → the D2c carrier's uniform /(1−w)
  (banked closed form) → the banked way-points force w=0 (DressingSeamResolved) → s-term undressed →
  banked leadingCarrier. EVERY arrow banked/forced. NO measured value enters any proof.
- Standard version: none. Standard physics FITS s to hit measured (fit value ~1.90 / dressed 3/16
  lands ~measured); we EXCLUDE the dressed reading STRUCTURALLY (whole-stack = the value the
  s-derivation rejected), never because of where measured sits. G2 clean.
- Words-removable: delete coupling/screening/isospin/anti-screening/dressing/depth/way-point → pure
  Cut arithmetic about projectedS/casimirRatio/depthWeight/totalScreeningWith/leadingCarrier/
  dressedInvAlphaEM/dressedInvAlphaZero/derivedBandList.
- Not free-floating: every TYPE mentions banked derived objects on Cut.
- Load-bearing tooth: the way-point w=0 (dressed_reproduces_only_at_w0) is what forces UNDRESSED;
  without it the s-term placement is free. Non-hollow: statable from no single conjunct.

## THEOREMS (production Phys/Algebra/AntiScreeningFrameUndressed.lean)
1. dressedSEff_eq_wholeStack : projectedS/(1−depthWeight) = casimirRatio·(depthWeight/(1−depthWeight))
2. dressedSEff_value : projectedS/(1−depthWeight) = 3/16
3. wholeStack_value : casimirRatio·(depthWeight/(1−depthWeight)) = 3/16
4. dressed_overturns_derivedS : projectedS/(1−depthWeight) ≠ projectedS   (3/16 ≠ 1/8)  [TEETH]
5. wholeStack_weight_ne_oneDepth : depthWeight/(1−depthWeight) ≠ depthWeight  (1/2 ≠ 1/3)  [TEETH]
6. antiscreen_shares_tower_factor : dressedInvAlphaEM w s bands = dressedInvAlphaEM w 0 bands
     − (s·totalScreeningWith 1 bands)/(1−w)  (re-export; no independent s-dressing knob)
7. waypoints_force_w0 : dressedInvAlphaZero 0 derivedBandList = 190/3
     ∧ dressedInvAlphaZero depthWeight derivedBandList ≠ 190/3  (re-export dressed_reproduces_only_at_w0)
8. dressedAntiScreen_gt_undressed : projectedS·U < projectedS·U/(1−depthWeight)  [inflation TEETH]
9. consistent_composition_is_leadingCarrier : leadingCarrier … = 112 + (chargeTraceDepth−projectedS)·U + freeze-out
     (re-export leading_carrier_determined) ; residual = totalScreening(freezeout) − projectedS·U (undressed)
     (re-export residual_above_recombined).
10. Capstone antiScreeningFrameUndressed_landing (welded, non-hollow).

## PROSE FIXES (banked comment-only; triggers FROZEN-TOUCH → full gate, safe single-flight)
- CasimirProjectionSelectsS.lean §86-88 + §120: replace "the composed endpoint at s=1/8 lands in the
  137.03x window … consistency check" with the TRUE statement: the UNDRESSED composed endpoint at
  s=1/8 is the DETERMINED way-point `leadingCarrier ≈ 138.97`, ABOVE the measured region; the +1.94
  residual is the DEPTH-2 sub-leading running content (route-not-yet-found, named, handed forward);
  the dressed reading (which would land near measured) is EXCLUDED as the whole-stack value the
  s-derivation rejects. NO forbidden gate strings.
- ComposedNumeralSPinned.lean §54-57 + §104: same correction.

## COST BUDGET / KILL
All proofs = rw + norm_num/linarith/field_simp on rational closed forms over Cut, or re-exports of
banked theorems. Cheap. KILL: any single obligation > 60s or leaf build > 5min → measure/decompose (W9).

## SUCCESSOR (ONE directed child, fork-guard)
DERIVE THE DEPTH-2 RUNNING CONTENT: the census-in-census screening composed at SECOND depth as a
RUNNING (band-varying) correction to leadingCarrier — each band's census re-screened by the fold's
own content between edges (the structure D2c pointed at but mis-composed as uniform static dressing;
DressingSeamResolved's verdict against UNIFORM dressing STANDS and is consistent with a band-varying
running second-depth composition). From the trunk: banked X5 census-in-census + band structure +
M-skeleton edges. Then: full attestation → re-narrow → D7 restate AS 1/α(0) ≥15 sig digits → D8.
NO generic SELECT until D8 handed. Decompose within-chain as needed.
