# FINDING — the depth-2 running content DERIVED (owner step 1); it is FORCED POSITIVE; enumeration EXHAUSTED

## Owner step 1 EXECUTED (scratch aiming, seed-permitted): the depth-2 running content

The owner's (A) sign-off named THE DEPTH-2 RUNNING CONTENT as this ticket's step (1): "the
census-in-census screening composed at SECOND depth as a RUNNING correction to the leading carrier —
each band's census re-screened by the fold's own content BETWEEN edges ... band-varying by
construction, not the forbidden uniform split." Derived it from the trunk:

- The genuine, way-point-consistent band-varying second-depth object = the census-in-census dressing
  (Z5 `dressedBandScreen`, D = bandScreen + w·D, w = depthWeight = 1/3 from D3 Born self-overlap)
  applied to the SUB-LEADING bands ONLY (lepton tail + quark freeze-out, all BELOW the confinement
  way-point 190/3), leaving the way-point bands (top/conf) at leading κ.
- This is genuinely DISTINCT from the DressingSeamResolved-excluded UNIFORM dressing: dressing the
  sub-leading tail does NOT move either banked way-point (both 1582/27 and 190/3 remain internal
  partial sums, confWaypoint_free). So it is NOT way-point-forbidden — the owner is RIGHT that a
  band-varying running is available where the uniform one was excluded.
- Sub-leading screening = totalScreening(leptonTail) + freeze-out = (67.9476 − 63.333) + 1.632
  = 4.614 + 1.632 = 6.246 (POSITIVE). Second-depth content = w·6.246 = +2.08 (first correction), or
  6.246·(1/(1−w) − 1) = +3.12 (full dressing). EITHER WAY POSITIVE.

## THE DECISIVE SIGN THEOREM (theorem-backable from banked positivity)

The depth-2 running content is FORCED NON-NEGATIVE: it is w · (Σ positive band screenings), with
  - w = depthWeight > 0  (D3 `depthWeight_pos`, Born positivity of the return-surface self-overlap),
  - each bandScreen > 0   (banked `bandScreen_pos` / `freezeoutTail_screens` for screening edges).
So the census-in-census SECOND depth can only ADD. It CANNOT be the dominant negative summand.

## THE EXHAUSTION (every summand of the composed 1/α(0) now derived AND sign-adjudicated)

| summand                       | status                         | sign / value        |
|-------------------------------|--------------------------------|---------------------|
| octonion ceiling 42           | EXACT (N316)                   | +42                 |
| +70 hypercharge weld          | EXACT (RecombinedEndpoint)     | +70                 |
| bare charge screening         | certified (TheNumeral)         | +25.9476            |
| ⇒ bare recombined tower       | banked THEOREM                 | 137.9476 (OVERSHOOTS 137.036 by +0.91 — at the CERTIFIED LEADING layer) |
| anti-screening −s·T, s=1/8    | DERIVED, UNDRESSED (frame thm) | −0.608 (ONLY negative; ≪ gap) |
| join content                  | ZERO (JoinSplitRatioDerived)   | 0                   |
| freeze-out                    | determined (ComposedFreezeout) | +1.632 (positive)   |
| uniform depth dressing        | way-point-EXCLUDED (DressingSeam) | drift/0          |
| band-varying depth-2 running  | NOW DERIVED (this run)         | +2.08 (POSITIVE)    |

Composed floor (bare − full anti-screen, everything else ≥0): 137.34.  leadingCarrier: 138.97.
With depth-2: ~141. EVERY level overshoots measured 137.036 at the 3rd sig fig; the MORE complete
the composition, the FURTHER from measured (a ONE-LAW signal on the additive framing).

## THE STRUCTURAL CEILING

No forced census/band/depth/freeze-out summand supplies a negative content of the needed ~0.9–1.9.
The only negative channel (anti-screening) has TOTAL magnitude 0.608 < 0.91 (the bare-tower overshoot
alone). Reaching measured requires either the G2-forbidden fit (dressed s = 3/16 = whole-stack, the
frame theorem's excluded reading; or s≈0.52) OR a sign/layer/placement change to a BANKED node.

## THE CONTRADICTION requiring owner adjudication

- Ticket step (4) + owner (A): "state the bracket AS 1/α(0); if it excludes measured, state it AS the
  fold's prediction — the derivation does not bend."
- Banked ComposedFreezeoutDissolved (in-module): "Stating a ≥15-digit bracket AS 1/α(0) while the
  composed endpoint is a determined value away from the measured one would be FALSE."
These directly contradict FOR THE GROSS (1.4–3%, non-beyond-measurement) disagreement we are in.

## OPTIONS (owner decision I cannot infer — mirrors the vindicated run-865 pattern)
R1: state the overshoot as the fold's BOLD prediction (a 1.4–3% falsifiable disagreement, honestly
    labelled as a LEADING-layer disagreement not a digit-12 precision prediction); authorize
    correcting the banked "would be FALSE" prose; then D7-restate → D8.  Which value?
    138.97 (leadingCarrier, if depth-2 double-counts per DressingSeam) or ~141 (if depth-2 is genuine).
R2: the gross disagreement (worse the more complete) is a ONE-LAW signal that the additive composition
    FRAMING has a structural error at a banked node (the freeze-out's additive placement, the
    recombination layer, or the weld); authorize a framing route-finder into banked-node structure.

NO fit executed; NO banked file modified; NO false completion.
