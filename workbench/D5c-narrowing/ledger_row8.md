| D5-next-8 | arc-D DIGITS D5-next-8 (docs/SEED_DIGITS.md §D5; directed successor of D5-next-7/NarrowedBracket7; THE NARROWING CAMPAIGN, node 9 — ATTACK 1/cutPi's LOWER BOUND WITH A STRUCTURAL POSITIVITY ROUTE to escape the near-π/2 nlinarith-margin W9 ceiling [the SOLE binding residual after cutLog's collapse], re-compose the assembled 1/α(0) bracket strictly narrower) | Phys/Algebra/NarrowedBracket8.lean | THE NARROWING CAMPAIGN, NODE 9 — the sole binding residual 1/cutPi's near-π/2 nlinarith-margin ceiling DISSOLVED (NOT ground past) by the DOUBLE-DOUBLE-ANGLE positivity route: two applications of the fold's OWN double-angle identity cutCos_double, folding the ill-conditioned near-π/2 truncated-cosine positivity to a WELL-CONDITIONED near-π/8 one, pushing leastCosZero's certified LOWER bound to the π/2 grid ceiling 157079632/1e8; then the D4 pipeline re-composed to a certified two-sided rational bracket of the derived 1/α(0) that is ≈5.05× NARROWER than D5-next-7's and nested strictly inside it. THEOREM-route, CERTIFIED. ── ★ THE ANTI-DRIFT CRUX ACTED ON (the ONE LAW read correctly — the instrument fought, return to the trunk). The parent MEASURED that the SINGLE-application double-angle route (cutCos x=2·(cutCos(x/2))²−1 with a near-π/4 lower bound on cutCos(x/2)) was at a hard nlinarith-margin ceiling: pushing leastCosZero's lower bound past 157079620/1e8 fails the degree-10 positivity certificate (ProbeD7a/ProbeD7b/ProbeHpoly all confirmed it dies), because even at π/4 the fixed threshold L=7071068/1e7 has arccos(L) as its mathematical ceiling and finer thresholds make the near-π/4 nlinarith margin collapse. That is NOT a theory wall (the fold's cosine genuinely stays positive up to its least zero ≈π/2); it is the coordinate nlinarith fighting the residual near-cancellation. The trunk already banked cutCos_double (from the fold's OWN addition formula cutCos_add + Pythagorean); applying it TWICE gives cutCos x=2·(2·(cutCos(x/4))²−1)²−1, reducing the near-π/2 positivity to a bound on cutCos(x/4) at x/4≈π/8, where the cosine value is ≈0.924 — and, crucially, the SMALL argument ≈0.393 (not ≈0.785) makes the truncated series low-order-dominated, so the split@6 nlinarith positivity closes with a robust margin even where the single-double-angle route died. ONE CAUSE: the same fold cosine identity that dissolved the parent's ceiling once, applied a second time. ── FRONT (1, THE SOLE BINDING RESIDUAL) — 1/cutPi via the double-double-angle route. cutCos_gt_pi8_632 proves cutCos y>144356177/156250000 for 0≤y<9817477/25000000 (well-conditioned near π/8, cert window ≈3.6×10⁻¹⁰ but not a near-cancellation — the small argument is low-order-dominated; threshold C satisfies 2·(2C²−1)²−1>0, the double-double-angle closure); cutCos_pos_of_lt_157079632 assembles 0<cutCos x for 0≤x<157079632/1e8 via cutCos_double∘cutCos_double + the near-π/8 bound, closed with a STAGED chain (c>C ⟹ c²>C² ⟹ u=2c²−1>2C²−1 ⟹ u²>(2C²−1)² ⟹ 2u²−1>0, each step well-conditioned); le_leastCosZero_157079632 via le_csInf. 157079632/1e8 is the integer floor of the true π/2·1e8=157079632.68 (4·(π/8)=π/2) — the tightest achievable value at the 1e8 grid, only 0.68×10⁻⁸ below the true half-period, a ≈12×10⁻⁸ improvement over the parent's 157079620/1e8. Keeping the parent's split@6 upper sign (cutCos_157079634_neg from NarrowedBracket7): leastCosZero∈[157079632/1e8,157079634/1e8], width 2×10⁻⁸ (was 1.4×10⁻⁷). cutPi∈[39269908/12500000,78539817/25000000] (cutPi_bracket_ext3), 1/cutPi∈[25000000/78539817,3125000/9817477] (inv_cutPi_bracket_ext3), width ≈4.05×10⁻⁹ (≈7× narrower than the parent's 2.8×10⁻⁸), nested strictly inside the parent's [25000000/78539817,2500000/7853981] from the upper end (lower reused; inv_cutPi_ext3_nested). ── FRONT (2) — cutLog reused from the parent (no longer binding). After D5-next-7 cutLog's contribution collapsed to ≈2.8×10⁻⁹ while 1/cutPi's was ≈4.4×10⁻⁷, so this node reuses the parent's split@5 cutLog(mass0/mass1)∈[815406110/1e8,815406115/1e8] (cutLog_lo_s5/cutLog_hi_s5) unchanged. ── THE RE-COMPOSED FURTHER-NARROWER NUMERAL: feed the sharpened 1/cutPi and the reused cutLog into the banked D4 endpoint_assembled_bracket at d=chargeTraceDepth=16/3 → invAlphaZero_narrowed8_bracket: 48029320130/706858353 ≤ invAlphaZero(completeBandList chargeTraceDepth) ≤ 12007329895/176714586, width ≈8.70×10⁻⁸ (vs D5-next-7's ≈4.40×10⁻⁷, ≈5.05× narrower). The lower endpoint is IDENTICAL to the parent's (same cutLog_lo × inv_cutPi_lo, since only the upper leastCosZero bound moved) — the narrowing is entirely from the upper end. Dressed at the derived weight w=1/3: dressed_narrowed8_bracket 19066651594/235619451 ≤ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth) ≤ 9533325691/117809724. ── TEETH (W8): invAlphaZero_narrowed8_strict (48029320130/706858353<12007329895/176714586 genuinely two-sided), invAlphaZero_narrowed8_width_lt (★ the CORE PROGRESS TOOTH: new width strictly below D5-next-7's 4802931616/70685829−48029320130/706858353), invAlphaZero_narrowed8_nested (48029320130/706858353≤48029320130/706858353 [lower reused] ∧ 12007329895/176714586≤4802931616/70685829 — a monotone refinement from the upper end, no earlier reading contradicted), invAlphaZero_narrowed8_gt_waypoint (190/3<invAlphaZero — the lepton tail runs 1/α genuinely up), plus inv_cutPi_ext3_strict/inv_cutPi_ext3_nested. CAPSTONE narrowedBracket8_landing welds all five blocks (double-double-angle 1/cutPi + squeezed cutPi + reused cutLog + re-composed numeral + dressed + four assembled teeth), statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7): NO measured value steers ANY bracket — the leastCosZero lower 157079632/1e8 is chosen by where the derived cosine's SIGN is certifiable (the π/2 grid floor via the double-double-angle route), the upper 157079634/1e8 by the split@6 sign, the 1/cutPi endpoints by 1/cutPi=1/(2·leastCosZero), and the cutLog endpoints by where the derived exp partial-sum brackets certify the Born-square quotient's log at index 27/30 — NOT by 137.036; truncating at measured precision FORBIDDEN (the campaign drives to MORE digits). The narrowed 1/α(0) bracket lands ≈67.9476, WIDE relative to measured precision and nowhere near 137 (the campaign continues); the measured 1/α(0)≈137.036 and π≈3.14159 appear NOWHERE in any proof (removable prose only). ── THE UPDATED BOTTLENECK (the successor's directed target): 1/cutPi's LOWER bound is now at the π/2 1e8-grid ceiling (leastCosZero_lo=157079632/1e8 is the integer floor of the true half-period 157079632.68, so no further 1e8-tick is possible without a finer grid); its UPPER endpoint (from leastCosZero's upper bound 157079634/1e8) can still be sharpened by reading the split@6/split@7 upper sign one tick nearer the true zero (e.g. 157079633/1e8 needs split@7), and/or by lifting leastCosZero's LOWER bound to a 1e9-grid via the same double-double-angle route (157079632.7/1e8 ⟶ 1570796327/1e9); cutLog can be sharpened cheaply (split@6 pB). The successor should sharpen leastCosZero's UPPER bound and/or lift the whole 1/cutPi bracket onto a 1e9-grid via the same banked route. ── FOUNDATIONS: 14 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}, NO foldRetention; import guard NarrowedBracket7 only (transitively NarrowedBracket6/5/4/3/2/NarrowedBracket/InvCutPiBracket/AssembledBracket/D3/D2/D1/Z6/N325/N323/N340 + the banked ContinuumTrigAdd/ContinuumTrigPi trig machinery incl. cutCos_double); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.cos/Real.exp, NO native_decide, NO floats (scratch numerics for aiming only). PHYSICS-WORDS-REMOVABLE: delete coupling/fine-structure/flavor/mass/census — pure statements that a derived closed real of Cut (invAlphaZero(completeBandList(16/3))) is pinned between explicit rationals 48029320130/706858353 and 12007329895/176714586, read from certified rational brackets of 1/cutPi (the derived cosine's least zero, via the fold's own double-angle identity applied twice) and cutLog(mass0/mass1) (the derived exp series and the Born-square quotient). GATE-FAST D0–D6 PASSED. C515 bites 1=515 (tied to invAlphaZero_narrowed8_width_lt). Directed successor of D5-next-7; hands ONE directed child toward D5-next-9 (the next narrowing node — sharpen leastCosZero's UPPER bound [split@7] and/or lift the whole 1/cutPi bracket onto a 1e9-grid via the same double-double-angle route, driving the assembled width toward below measured precision, then D6). |
