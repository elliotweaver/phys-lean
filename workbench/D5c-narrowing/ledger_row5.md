| D5-next-5 | arc-D DIGITS D5-next-5 (docs/SEED_DIGITS.md §D5; directed successor of D5-next-4/NarrowedBracket4; THE NARROWING CAMPAIGN, node 6 — ATTACK 1/cutPi WITH THE FOLD'S OWN DOUBLE-ANGLE IDENTITY to escape the parent's degree-10 nlinarith-margin W9 ceiling, AND SHARPEN cutLog via the achievable m01 floor at deeper exp indices, re-compose the assembled 1/α(0) bracket to a strictly narrower width) | Phys/Algebra/NarrowedBracket5.lean | THE NARROWING CAMPAIGN, NODE 6 — the parent's 1/cutPi nlinarith-margin ceiling DISSOLVED (NOT raised) by the fold's OWN double-angle identity, AND cutLog sharpened via the achievable m01 floor, then the D4 pipeline re-composed to a certified two-sided rational bracket of the derived 1/α(0) that is ≈39× NARROWER than D5-next-4's and nested strictly inside it. THEOREM-route, CERTIFIED. ── ★ THE THEORY-NATIVE HEART (the ONE LAW in action — the instrument fought, return to the trunk). The parent hit a genuine INSTRUMENT-COST ceiling (W9): the truncated-cosine positivity near leastCosZero≈π/2 was closed by an nlinarith certificate search on a degree-10 polynomial, and that search only closes with a comfortable margin (z_lo≤15707705/1e7) — it timed out when pushed nearer the zero. That is NOT a theory wall (the fold's cosine genuinely stays positive up to its least zero); it is the coordinate nlinarith fighting a NEAR-CANCELLATION: at x≈π/2 the cosine value is ≈0, so the partial sum is a giant alternating cancellation and the slack the certificate needs is exponentially thin. The trunk already banked the fold's OWN addition formula cutCos_add (the cosine-of-a-sum, the Cauchy product of the defining series — nobody posited it) and the Pythagorean identity cutSin²+cutCos²=1. Together they give the DOUBLE-ANGLE identity cutCos(2·y)=2·(cutCos y)²−1 (cutCos_double), and this converts the ill-conditioned near-π/2 positivity into a WELL-CONDITIONED near-π/4 one: cutCos x>0 for x<2·y reduces to cutCos y>1/√2 for y<x/2, and at y≈π/4 the cosine value is ≈0.707 — a healthy sum with NO cancellation — so the SAME banked cutCos_bracket instrument reads it at split@6 and a single nlinarith closes with enormous margin. ONE CAUSE: the same cosine the campaign has been reading, folded through the fold's own addition law. ── FRONT (1) — 1/cutPi via the double-angle route: cutCos_gt_invSqrt2_of_lt proves cutCos y>7071068/1e7 for 0≤y<7853980/1e7 (well-conditioned near π/4, threshold L with L²−1/2=2.66×10⁻⁸>0); cutCos_pos_of_lt_15707960 assembles 0<cutCos x for 0≤x<15707960/1e7 via cutCos_double + the near-π/4 bound (2·L²−1>0); cutCos_15707964_neg the split@6 upper sign (partialCos_six+cosRemBound<0, an exact rational near-cancellation norm_num handles); leastCosZero∈[15707960/1e7,15707964/1e7] (le_leastCosZero_15707960 via le_csInf, leastCosZero_le_15707964 via IVT+csInf_le), width 4×10⁻⁷ (≈66× narrower than the parent's [15707705/1e7,15707970/1e7]), lower endpoint only 3×10⁻⁷ below true π/2 (parent's was 2.6×10⁻⁶). cutPi∈[3141592/1e6,31415928/1e7] (cutPi_bracket_double), 1/cutPi∈[10000000/31415928,1000000/3141592] (inv_cutPi_bracket_double), width 8.1×10⁻⁸, nested strictly inside the parent's [500000/1570797,1000000/3141541] (inv_cutPi_double_nested). ── FRONT (2) — cutLog sharpened via the ACHIEVABLE m01 floor at deeper exp indices (cheap, orthogonal). With p=cutCos(2/9) already at width 2×10⁻⁹ (parent), the m01=mass0/mass1 floor from the banked Born-square amplitude brackets (b0/b1 at width 3×10⁻⁸) is genuinely [3477.4708,3477.4760]; the parent rounded it generously to [3477.44,3477.48]. Reading the ACHIEVABLE floor [3477470/1000,3477477/1000] (m01_lo_ach/m01_hi_ach, from the SAME banked mass0/mass1_lo/hi_sharp) through the D1 exp instrument at partial-sum index 23 (lower, cutLog_lo_ach via cutLog_ge_certified) / 26 (upper, cutLog_hi_ach via cutLog_le_certified) through cutLog_mono gives cutLog(mass0/mass1)∈[81540600/1e7,81540630/1e7] (width 3×10⁻⁶, ≈24× narrower than the parent's 6×10⁻⁵), nested strictly inside its [8.15405,8.15411] (cutLog_ach_nested). ── THE RE-COMPOSED FURTHER-NARROWER NUMERAL: feed BOTH sharpened residuals into the banked D4 endpoint_assembled_bracket at d=chargeTraceDepth=16/3 → invAlphaZero_narrowed5_bracket: 800488690/11780973 ≤ invAlphaZero(completeBandList chargeTraceDepth) ≤ 80048852/1178097, width ≈2.87×10⁻⁶ (vs D5-next-4's ≈1.1×10⁻⁴, ≈39× narrower). Dressed at the derived weight w=1/3: dressed_narrowed5_bracket 317777534/3926991 ≤ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth) ≤ 31777747/392699. ── TEETH (W8): invAlphaZero_narrowed5_strict (800488690/11780973<80048852/1178097 genuinely two-sided), invAlphaZero_narrowed5_width_lt (★ the CORE PROGRESS TOOTH: new width strictly below D5-next-4's 1921144130/28273869−960586690/14137173), invAlphaZero_narrowed5_nested (960586690/14137173≤800488690/11780973 ∧ 80048852/1178097≤1921144130/28273869 — a monotone refinement, no earlier reading contradicted), invAlphaZero_narrowed5_gt_waypoint (190/3<invAlphaZero — the lepton tail runs 1/α genuinely up), plus inv_cutPi_double_strict/inv_cutPi_double_nested/cutLog_ach_strict/cutLog_ach_nested. CAPSTONE narrowedBracket5_landing welds all five blocks (double-angle 1/cutPi + squeezed cutPi + sharpened cutLog + re-composed numeral + dressed + four assembled teeth), statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7): NO measured value steers ANY bracket — the leastCosZero endpoints 15707960/1e7, 15707964/1e7 are chosen by where the derived cosine's SIGN is certifiable (via the double-angle route at split@6 for the lower, the direct split@6 sign for the upper), NOT by where π/2 sits; the m01 endpoints 3477470/1000, 3477477/1000 are the achievable DIRECT quotient of the banked Born-square mass brackets and the cutLog endpoints 81540600/1e7, 81540630/1e7 by where the derived exp partial-sum brackets certify that quotient's log at index 23/26, NOT by 137.036; truncating at measured precision FORBIDDEN (the campaign drives to MORE digits). The narrowed 1/α(0) bracket lands ≈67.9476, WIDE relative to measured precision and nowhere near 137 (the campaign continues); the measured 1/α(0)≈137.036 and π≈3.14159 appear NOWHERE in any proof (removable prose only). ── THE UPDATED BOTTLENECK (the successor's directed target): the parent's 1/cutPi nlinarith ceiling is DISSOLVED (the double-angle route reads leastCosZero to 3×10⁻⁷ of the true zero with large margin to spare, and can push further); the assembled width is now DOMINATED by cutLog (≈3×10⁻⁶ vs 1/cutPi's ≈1.2×10⁻⁶ contribution). The successor sharpens cutLog further (deeper exp indices / a tighter m01 floor toward the achievable [3477.4708,3477.4760]) and may push leastCosZero still nearer π/2 via the same banked double-angle route. ── FOUNDATIONS: 23 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}, NO foldRetention; import guard NarrowedBracket4 only (transitively NarrowedBracket3/2/NarrowedBracket/InvCutPiBracket/AssembledBracket/D3/D2/D1/Z6/N325/N323/N340 + the banked ContinuumTrigAdd/ContinuumTrigPi trig machinery); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.cos/Real.exp, NO native_decide, NO floats (scratch numerics for aiming only). PHYSICS-WORDS-REMOVABLE: delete coupling/fine-structure/flavor/mass/census — pure statements that a derived closed real of Cut (invAlphaZero(completeBandList(16/3))) is pinned between explicit rationals 800488690/11780973 and 80048852/1178097, read from certified rational brackets of 1/cutPi (the derived cosine's least zero, via the fold's own double-angle identity) and cutLog(mass0/mass1) (the derived exp series and the Born-square quotient). GATE-FAST D0–D6 PASSED. C512 bites 1=512 (tied to invAlphaZero_narrowed5_width_lt). Directed successor of D5-next-4; hands ONE directed child toward D5-next-6 (the next narrowing node — sharpen cutLog further [deeper exp indices / a tighter m01 floor toward [3477.4708,3477.4760], the now-dominant residual] and optionally push leastCosZero still nearer π/2 via the same double-angle route, driving the assembled width toward below measured precision, then D6). |
