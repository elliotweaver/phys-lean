| D5-next-12 | arc-D DIGITS D5-next-12 (docs/SEED_DIGITS.md §D5; directed successor of D5-next-11/NarrowedBracket11; THE NARROWING CAMPAIGN, node 13 — SHARPEN 1/cutPi, ONCE AGAIN the SOLE binding residual after the split@6 cutLog collapse, via a 1e11-grid LIFT of its whole bracket using the SAME banked double-double-angle route [two applications of the fold's OWN cutCos_double], re-aimed one digit deeper; re-compose the assembled 1/α(0) bracket strictly narrower — and it crosses BELOW measured precision, the north star) | Phys/Algebra/NarrowedBracket12.lean | THE NARROWING CAMPAIGN, NODE 13 — the binding residual 1/cutPi's whole bracket LIFTED from the exhausted π/2 1e10-grid onto a 1e11 GRID via the SAME banked double-double-angle route (two applications of the fold's OWN cutCos_double), pushing leastCosZero's certified LOWER bound to the π/2 1e11-grid FLOOR 157079632679/1e11 (the UPPER bound is the SAME rational at both grids, 157079632680/1e11 = 15707963268/1e10, so the split@8 upper sign leastCosZero_le_15707963268 is reused VERBATIM — the sharpening is purely one-sided); then the D4 pipeline re-composed to a certified two-sided rational bracket of the derived 1/α(0) that is STRICTLY NARROWER than D5-next-11's (~8.5×) and nested strictly inside it — and whose width ≈3.50×10⁻¹¹ is NOW BELOW the measured relative uncertainty ~2×10⁻¹⁰ of 1/α(0). THEOREM-route, CERTIFIED. ── ★ THE STRUCTURAL WIN (the ONE LAW read correctly — the finer grid does not fight because the trunk lever removed the near-cancellation). After D5-next-11 sharpened cutLog to width 10⁻¹¹ (contribution ≈5.7×10⁻¹²), the 1e10-grid 1/cutPi (contribution ≈2.9×10⁻¹⁰, ≈50× larger) was ONCE AGAIN the SOLE binding residual, with its leastCosZero bracket at the π/2 1e10-grid floor/ceiling [15707963267/1e10,15707963268/1e10] — the 1e10 bracket was exhausted. The escape is NOT a finer coordinate grind but the SAME double-double-angle positivity route re-aimed at a 1e11-grid argument. Because that route reduces near-π/2 positivity to a WELL-CONDITIONED near-π/8 lower bound (cutCos y≈0.924, a healthy low-order-dominated sum with no cancellation), its conditioning is grid-INDEPENDENT: the same certification that closed at 1e10 closes at 1e11 — only the constants gain one digit. THE W9 SUBTLETY at 1e11: the near-π/8 cert window shrank to ≈2.55×10⁻¹³ (≈20× thinner than 1e10), so the degree-12 nlinarith needed the EXACT tight square bound y²<(157079632679/4e11)² (no rounding slack) together with the monotonicity products (B−y²)·y^(2k)≥0 supplying the polynomial factorization; measured first in a bounded probe (ProbePi11.lean, closes ≈38s) before committing. ONE CAUSE: the fold's own double-angle identity, applied twice, one digit deeper. ── FRONT (1, THE BINDING RESIDUAL, LIFTED) — 1/cutPi onto the 1e11 grid (LOWER endpoint only). cutCos_gt_pi8_11 proves cutCos y>9238795325115/1e13 for 0≤y<157079632679/4e11 (near-π/8, split@6, remainder factor ≤101/100; threshold C=0.9238795325115 satisfies 2·(2C²−1)²−1>0 [value ≈2.23×10⁻¹²] AND C>cos(π/8)); cutCos_pos_of_lt_157079632679 assembles 0<cutCos x for 0≤x<157079632679/1e11 via cutCos_double∘cutCos_double + the near-π/8 bound, closed with a STAGED chain; le_leastCosZero_157079632679 via le_csInf. 157079632679/1e11 is the integer FLOOR of the true π/2·1e11=157079632679.49, only 0.49×10⁻¹¹ below. For the UPPER bound, the 1e11 ceiling 157079632680/1e11 IS the SAME rational as the parent's 1e10 ceiling 15707963268/1e10 (both = 1.5707963268, the integer ceiling of the true π/2 at both grids), so the split@8 leastCosZero_le_15707963268 is reused VERBATIM. leastCosZero∈[157079632679/1e11,15707963268/1e10], width ≈4.9×10⁻¹² (was 1×10⁻¹⁰, ≈20× tighter, from the LOWER end). cutPi∈[157079632679/5e10,3926990817/1250000000] (cutPi_bracket_ext6), 1/cutPi∈[1250000000/3926990817,50000000000/157079632679] (inv_cutPi_bracket_ext6), width ≈2.03×10⁻¹² (≈10× narrower than the parent's 2.03×10⁻¹¹); nested inside the parent's [1250000000/3926990817,5000000000/15707963267] from the UPPER end (the LOWER bound reused verbatim, inv_cutPi_ext6_nested). ── FRONT (2) — cutLog reused VERBATIM from the parent (cutLog(mass0/mass1)∈[815406111272/1e11,815406111273/1e11], cutLog_lo_s6/cutLog_hi_s6) — FAR below the 1/cutPi floor, not re-derived. ── THE RE-COMPOSED FURTHER-NARROWER NUMERAL: feed the 1e11-lifted 1/cutPi and the reused cutLog into the banked D4 endpoint_assembled_bracket at d=chargeTraceDepth=16/3 → invAlphaZero_narrowed12_bracket: 12007329939722/176714586765 ≤ invAlphaZero(completeBandList chargeTraceDepth) ≤ 32019546505738/471238898037, width ≈3.50×10⁻¹¹ (vs D5-next-11's ≈2.99×10⁻¹⁰). The LOWER endpoint is reused verbatim (both the 1/cutPi lower bound and the cutLog lower bound are unchanged); the UPPER endpoint moved inward. Dressed at the derived weight w=1/3: dressed_narrowed12_bracket 4766662862506/58904862255 ≤ dressedInvAlphaZero depthWeight (completeBandList chargeTraceDepth) ≤ 12711100966610/157079632679. ── TEETH (W8): invAlphaZero_narrowed12_strict (genuinely two-sided), invAlphaZero_narrowed12_width_lt (★ the CORE PROGRESS TOOTH: new width strictly below D5-next-11's 16009773252014/235619449005−12007329939722/176714586765), invAlphaZero_narrowed12_nested (12007329939722/176714586765≤12007329939722/176714586765 [lower reused verbatim] ∧ 32019546505738/471238898037≤16009773252014/235619449005 — a monotone refinement from the UPPER end), invAlphaZero_narrowed12_gt_waypoint (190/3<invAlphaZero), plus inv_cutPi_ext6_strict/inv_cutPi_ext6_nested. CAPSTONE narrowedBracket12_landing welds all five blocks, statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7): NO measured value steers ANY bracket — the leastCosZero lower endpoint 157079632679/1e11 is chosen by where the derived cosine's SIGN is certifiable (the π/2 1e11-grid floor via the double-double-angle route), the upper reused from the split@8 sign, the 1/cutPi endpoints by 1/cutPi=1/(2·leastCosZero), the cutLog endpoints reused — NOT by 137.036; truncating at measured precision FORBIDDEN. The narrowed 1/α(0) bracket lands ≈67.9476, nowhere near 137; the measured 1/α(0)≈137.036 and π≈3.14159 appear NOWHERE in any proof (removable prose only). ── ★ THE NORTH-STAR CROSSING (the successor's directed target CHANGES to D6): the assembled width ≈3.50×10⁻¹¹ is NOW BELOW the measured relative uncertainty ~2×10⁻¹⁰ of 1/α(0) (absolute ≈1.36×10⁻⁸ about the derived value ≈67.9476) — the derived numeral is bracketed to MORE significant digits than the measured value carries. The narrowing campaign has reached its width target; the next node is directed at D6 (the way-points α(M_Z), sin²θ_W(M_Z) evaluated FREE from the SAME certified tower, docs/SEED_DIGITS.md §D6), then D7 (the numeral STATED, with the beyond-measurement digits flagged as the chain's standing PREDICTION). ── FOUNDATIONS: 14 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}, NO foldRetention; import guard NarrowedBracket11 only (transitively the whole banked chain + the banked cutCos_bracket cosine instrument, cutCos_double double-angle identity, and the split@6 cutLog_lo_s6/cutLog_hi_s6); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.cos/Real.exp, NO native_decide, NO floats (scratch numerics for aiming only). PHYSICS-WORDS-REMOVABLE: delete coupling/fine-structure/flavor/mass/census — pure statements that a derived closed real of Cut (invAlphaZero(completeBandList(16/3))) is pinned between explicit rationals 12007329939722/176714586765 and 32019546505738/471238898037, read from certified rational brackets of 1/cutPi (the derived cosine's least zero, via the fold's own double-angle identity applied twice on a 1e11 grid) and cutLog(mass0/mass1). GATE-FAST D0–D6 PASSED. C519 bites 1=519 (tied to invAlphaZero_narrowed12_width_lt). Directed successor of D5-next-11; hands ONE directed child toward D6 (the way-points evaluated free — the campaign width target is met). |
