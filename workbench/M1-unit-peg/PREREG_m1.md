# M1 — THE ONE UNIT PEG (SEED_MASSES §M1) — workbench note (t_a60e2eab)

## SELECT (fresh worker, run IN FULL before STEP 0) — CONFIRMED M1 theory-native
See kanban comment 472 for the full S1–S4 record. Summary: the fold sets its OWN unit (M_oct := 1);
every scale already banked as a dimensionless ratio to M_oct (ewClosureRatio, confinementScaleRatio,
massScale/N417, absMass_k/N340). The ONLY unit-freedom is the human peg — a flagged convention, NOT a
posit. Load-bearing content = the unique-up-to-units theorem (all ratios peg-invariant). Anti-bullshit
gate passed on all 5 tests; the saturated temptation (jump to M2 numerals) forbidden because a numeral
"in MeV" is meaningless without the peg and a second dimensionful input at M2 is the ONE-PEG-LAW
violation M1 exists to forbid.

## WHAT WAS BANKED (Phys/Algebra/UnitPeg.lean, namespace Phys.Algebra.Peg, C547, 10 decls)
- UnitPeg (structure: val : Cut, pos : 0 < val) — THE ONE flagged dimensionful convention.
- UnitPeg.human (p.val · x) — the human value of a chain quantity (Cut ratio to M_oct).
- peg_fixes_Moct (p.human 1 = p.val) — the metre-choice explicit.
- human_scale_pos.
- ratio_peg_invariant (★ p.human x / p.human y = x / y, unconditional).
- ratio_peg_independent (★ any two pegs agree on every ratio).
- peg_unique_up_to_units (★★ THE M1 THEOREM: ∃ c>0, ∀x, q.human x = c·p.human x).
- peg_rescale_unique (that c is unique = q.val/p.val).
- tower_ratio_peg_invariant (Λ/v peg-free, grounds on confinementScaleRatio/ewClosureRatio).
- absMass_ratio_peg_invariant (lepton ratio absMass1/absMass0 = mass1/mass0, N340, peg-free).
- human_peg_dependent (★ W8 non-vacuity: the peg genuinely moves absMass0).
- unitPeg_landing (welded capstone, non-hollow, 5 conjuncts).

## GROUNDING (not free-floating)
Every TYPE cites the banked M-scale tower over Cut: ewClosureRatio, confinementScaleRatio (rungs),
massScale (N417), absMass0/absMass1 (N340 × scale), mass0/mass1 (N340). Import guard:
ConfinementHadronicMass + AbsoluteMassSpectrum only.

## GRADE
THEOREM-EXACT (exact identities/inequalities of the derived Cut, foundations-only ⊆
{propext,Classical.choice,Quot.sound}). The peg a flagged CONVENTION carrying a uniqueness theorem,
NOT a physics posit. NO empirical number in any proof.

## CARRIED NOTE
Deferred-D7 (arc-D standing debt, NOT a runway): D's ≥15-sig-digit AS-1/α(0) restate is a DETERMINED
WAY-POINT (LeadingCarrierExhausted) resuming AFTER this absolute-scale skeleton exists.

## SUCCESSOR (the ONE directed child — REPLACES the generic SELECT)
Arc-M target M2 (SEED_MASSES §M2): THE LEPTON MASS NUMERALS — certified brackets for each absMass_k
against the M1 peg via the banked N340 ratios × D1 brackets on the banked exponentials.
