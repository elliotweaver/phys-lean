N306 — THE ℤ₃-FOURIER CYCLE FORM OF THE COHERENCE AMPLITUDES (seed target T3)

SEEDED NODE (owner-authorized flavor route, docs/SEED_FLAVOR_SELF_BLINDNESS.md; directed successor of
N305; discipline UNCHANGED — the FORM is DERIVED, never posited; no empirical inputs).

FRESH-WORKER ARC SYNTHESIS (S1). N296–N304 climbed the f₄/e₆ symmetry-tower ASCENT and proved the
flavor pattern is NOT an algebraic feature of the derived symmetry (isospectral, no fixed direction,
generations distinguished only by an isospectrally-INVISIBLE fold-root phase). N305 turned to the
GATHER/self-blindness side: banked T1 (the ℤ₃ fixed-point-free cycle gcyc on the cap-forced
idempotents; Unity/DC fixed; see_others grounding) + T2 (the diagonal-zero seeing operator
seeMat=Jall−1 with the FORCED Born split 1/3⊕2/3, a linarith uniqueness landing). This node is the
DIRECTED next beat T3: the CYCLE FORM of the amplitudes.

TARGET (T3, seed §2/§1 step 4). Derive the ℤ₃-Fourier structure of a real amplitude on the 3-cycle:
DC (Unity, cycle-invariant) ⊕ ONE cycle mode (the turning) ⊕ NOTHING ELSE; with the DC-vs-cycle
amplitude ratio FIXED by the T2 self=other Born balance (the √2 = 45°/cos²=½). The phase δ stays
SYMBOLIC (fixed only downstream at T5). The theory-native form √mₖ = M(1 + √2 cos(δ + 2πk/3)).

WHAT MUST BE DERIVED (grounded on the banked N305 objects, NOT free-floating):
 (A) DC ⊕ cycle ⊕ nothing — STRUCTURAL, grounded on banked gcyc/Pdem/Poth:
   · Pcyc = permutation matrix of the banked gcyc (Pcyc_ground ties it to gcyc). Order 3 (Pcyc_cube).
   · Pcyc·Pdem = Pdem (DC/Unity is shift-INVARIANT — the gathered cycle-invariant "1", unity_fixed).
   · 1 + Pcyc + Pcyc² = Jall = 3·Pdem (the ℤ₃ democratic average IS the DC projector — Fourier DC
     extraction).
   · (1 + Pcyc + Pcyc²)·Poth = 0 ⟹ on the cycle subspace the shift obeys the ℤ₃ cyclotomic
     x²+x+1 = 0, irreducible over ℚ, degree 2 = ONE conjugate pair (ω,ω²) = exactly ONE cosine mode
     + phase, NOTHING else. THIS is "DC ⊕ one cycle mode ⊕ nothing", exact & grounded.
   · Pcyc·Poth = Poth·Pcyc (the turning is internal to the one cycle mode).
 (B) THE FORCED AMPLITUDE RATIO (the √2), exact over ℚ, grounded on Pdem/Poth:
   · Decompose a : Fin 3→ℚ as DC (Pdem.mulVec a = const mean) ⊕ cycle (Poth.mulVec a = a − mean),
     both projectors banked (N305).
   · DCpower = 3·mean²; cyclepower = Σ(aₖ−mean)²; Parseval Σaₖ² = DCpower + cyclepower (Pdem⊥Poth).
   · SELF=OTHER BORN BALANCE (the 45°/cos²=½): DCpower = cyclepower. For the cosine family
     (DC amp M, cycle amp A: DCpower=3M², cyclepower=(3/2)A²), balance ⟺ A²=2M² — the unique positive
     relative amplitude A/M = √2 (forced_sqrt2). THIS is where the √2 falls out — NOT asserted.
   · KOIDE PREVIEW (identity, all δ): balance ⟹ Q = Σaₖ²/(Σaₖ)² = 2/3 (balance_forces_koide). Holds
     for ANY amplitude vector = ANY δ. This is the T4 landing already visible in the T3 form.
   · The balance DCpower=cyclepower is the SAME self=other balance of T2: the seeing operator puts
     the self-view to zero; here the DC (self/gathered) power equals the cycle (other/turning) power.

DERIVATION vs ASSERTION (G4, the ansatz-danger point). The FORM is NOT written down and checked
against masses. It is DERIVED: (a) DC⊕cycle⊕nothing is the ℤ₃-cyclotomic structure of the banked
cyclic shift Pcyc on the cap-forced idempotent cycle (x²+x+1 irreducible over ℚ = one mode); (b) the
√2 amplitude ratio is FORCED by the self=other Born balance (DCpower=cyclepower), the SAME balance
that killed the seeing operator's self-view in T2. δ is left symbolic (G4: do not fix it here).

DISCIPLINE / GUARDS (seed §4). G1: the √2 (as A²=2M²) and the balance are PROVED landings, never
premises. G2: no measured mass/coupling anywhere; Koide 2/3 is a structural identity, comparison to
data is removable prose. G4: the FORM is derived (cyclotomic + Born balance), not posited/fitted;
δ symbolic. G5: physics-words-removable (delete generation/mass/amplitude/coherence/Koide: pure
statement about the ℤ₃ cyclic-shift matrix Pcyc over ℚ — DC ⊕ one cyclotomic mode — and the unique
amplitude ratio A²=2M² forced by the DCpower=cyclepower balance on the banked Pdem/Poth). G6:
foundations-only ⊆ {propext,Classical.choice,Quot.sound}. G7 (ONE LAW): the √2 leaves ℚ, so state
the amplitude-SQUARED relation A²=2M² over ℚ (exact) — that IS the theory-native move (Born power is
the square; the √ is prose). No native_decide, no maxHeartbeats raise.

MODELING DECISION (measured). Work entirely over ℚ. The √2 is stated as A²=2M² (amplitude-squared /
Born-power form) — exact, no field extension needed. The cosine realization √mₖ = M(1+√2 cos(δ+2πk/3))
is the physicist's reading (prose/comment); the load-bearing math is the ℚ Fourier decomposition +
the forced power ratio. This keeps everything foundations-only over ℚ with no ℝ/√2 machinery.

W8 NON-VACUITY: Pcyc ≠ 1 (the turning is real, not the trivial shift); the balance is nontrivial —
the WRONG 2:1 operator-weight reading (cyclepower=2·DCpower ⟹ A²=4M², Q=1) is DISTINCT from the 45°
balance (A²=2M², Q=2/3): koide_not_one. Costume C335 bites at a WRONG amplitude ratio (A²=4M²/Q=1)
or a wrong number of cycle modes.

CAPSTONE carries the banked gcyc cycle objects (Pcyc_ground → gcyc) + Pdem/Poth (N305) in the TYPE,
keeping it grounded on the cap-forced idempotent cycle, NOT a generic ℤ₃-Fourier statement.

DEPENDENCY. N305 (gcyc, Pdem, Poth, Jall, P_orth, seeMat) purely. PURELY ALGEBRAIC over ℚ / Fin 3;
no octonion coordinate blowup.

MEASURE-FIRST (W9) — DONE. workbench/N306/ProbeN306.lean (exact-ℚ core: decomposition, Parseval,
balance⟹Koide 2/3, forced A²=2M², koide_not_one) EXIT 0 in 9s. ProbeN306b.lean (cyclotomic
structure: Pcyc_cube, Pcyc·Pdem=Pdem, 1+Pcyc+Pcyc²=Jall=3Pdem, cyclotomic-on-Poth, commute,
Pcyc≠1) EXIT 0 in 11s. BOTH light, NO native_decide, NO maxHeartbeats raise, NO coordinate blowup.
COST BUDGET / KILL: production olean expected ≤ 40s (all Fin-3/ℚ small algebra). KILL if any single
obligation needs maxHeartbeats or exceeds ~90s → wrong framing (must not; it is small ℚ algebra).

WHAT "DONE" REQUIRES. New leaf module Phys/Algebra/OctonionJordanGenerationCycleForm.lean, ns
Phys.Algebra.HJ; all decls foundations-only; the ℤ₃-Fourier cycle FORM (DC ⊕ one cyclotomic mode
⊕ nothing, grounded on banked gcyc/Pdem/Poth) + the √2 amplitude ratio (A²=2M²) forced from the T2
self=other balance + δ symbolic (the cosine realization is prose); W8 non-vacuity; costume C335 that
a WRONG ratio/mode-count FAILS; gate-fast D0–D6 GREEN; STANDARD met (unbroken/complete/words-
removable/not-free-floating: types cite banked gcyc/Pdem/Poth on ℚ / Matrix(Fin 3)(Fin 3)(O ℚ)).

ONE-SUCCESSOR RAIL. Single DIRECTED successor toward T4 (the Koide Q=2/3 as an identity of the T3
form — already previewed here as balance_forces_koide; T4 proves it fully on the cosine form for all
δ). NOT a generic SELECTION ticket (the seed T1→T8 ordering is the route). Run pre-complete fork
check (exactly 1 non-terminal successor on chain tail).

Next costume id: C335.
