# PREREG — RE-ATTEST D7 COMPOSITION-COMPLETENESS over the F1+F2-audited composition (t_4d2581c1)

## THE OWNER MANDATE (recompose + re-attest, then resume the narrowing)
The OWNER FRAME AUDIT is COMPLETE: F1 (N580b, the recombination architecture — ARCH-B forced) + F2
(N581, the dressing scope — SCOPE-(c) resummed-vs-unresummed forced) both re-derived AB INITIO in the
forced arena, BOTH CONFIRMING the banked `recomposedEndpointDressed` composition. This node
RE-ATTESTS the D7 composition-completeness over that frame-audited object, then hands the resumed
narrowing forward as ONE directed successor.

## THE TASK (step 1 of the owner mandate; step 2 = the Rung-3 successor)
(1) RE-ATTEST the D7 COMPOSITION-COMPLETENESS over the frame-audited object: the banked
`recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·cInner` IS the F1+F2-audited composition —
every banked contribution INCLUDED (the ARCH-B census, the SCOPE-(c) dressing) or proven exempt/zero
(the resummed base). This is ASSEMBLY of banked objects, not new derivation. Cite F1 (the
architecture) + F2 (the dressing scope) as the now-audited structural claims.
(2) THEN resume the ≥15-sig-digit narrowing — the reverted D7NarrowingRung3 WIP (ext7 1/cutPi + s7
L_m01, workbench/d7-kappa-narrow, fully de-risked) ports directly. That is the ONE directed successor
(each rung a SEPARATE successor, ONE-SUCCESSOR RAIL).

## THE DECISION POINT (owner gate discharge)
The narrowing was GATED because the dressed endpoint 137.0996710997 was a CONFIRMED DEFECTIVE value
(audited-everywhere + not-exact). The frame audit re-derived the frame's two surviving structural
claims; BOTH confirm the composition. So the "narrowing a defective value is polishing a bug report"
gate is DISCHARGED for the frame layer. This re-attestation is the FORMAL check: does composition-
completeness hold over the frame-audited object?
  • IF a banked contribution is STILL missing / mis-composed → the real defect → W1 dissolution ticket.
  • IF the composition is complete → resume the narrowing (hand Rung 3).
The assembly below shows every banked structural fact composes cleanly (F1 architecture ✓, F2 scope ✓,
D7 census-completeness ✓, dressed = bare + fold-forced (1/2)·κ·cInner increment with exempt base ✓), so
the composition IS COMPLETE and the residual endpoint precision is INSTRUMENT precision (the ≥15-digit
floor), NOT a frame defect ⇒ RESUME the narrowing.

## THE WELD (what this node banks — all THEOREM-EXACT, foundations-only, assembly of banked objects)
- A. F2 DRESSING SCOPE over the frame-audited object:
    scopeC_closed (recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·cInner),
    scopeC_exempts_base (dressed − (3/2)·κ·cInner = 2425/18),
    exempt_base_is_resummed (2425/18 = 7179/54 + 16/9).
- B. F1 RECOMBINATION ARCHITECTURE:
    architecture_shift_exact (ARCH-A − ARCH-B = bandScreen projectedS topBandRatio at the top band),
    forced_param_ne_zero (projectedS ≠ 0 — ARCH-B forced, the s=0 switch-off avoided),
    archB_below_archA_topBand (the one-law direction — ARCH-B corrects the ARCH-A overshoot down).
- C. THE F1↔F2 TIE: weight_channel_independent (depthWeight = 1/3, ≠0, ≠1 — one shared weight).
- D. THE BARE CENSUS-COMPLETENESS (D7): recomposed_composition_complete (full re-export) + the
    bare↔dressed weld: recomposedEndpointDressed = recomposedEndpoint + (1/2)·kappaLeading·cInner
    (depthTwo_dresses_recomposed.1) — the dressed object's ONLY difference from the census-complete
    bare object is the fold-forced (1/2)·κ·cInner increment on the SAME cInner running content, base
    identical; so census-completeness of the bare ⇒ every dressed contribution is accounted.
- E. TEETH (W8): the census gem genuinely contributes (2425/18 ≠ 7179/54); the dressing is a real
    upward step (recomposedEndpoint < recomposedEndpointDressed); the increment is strictly positive
    (0 < (1/2)·kappaLeading·cInner); the weight is genuinely interior (depthWeight ≠ 0, ≠ 1).
- F. THE WELDED CAPSTONE: all of A–E conjoined, statable from no single conjunct alone.

## GRADE (honest)
THEOREM-EXACT (exact identities/inequalities of Cut, foundations-only): the whole re-attestation is an
ASSEMBLY of banked exact theorems. NO route-not-yet-found in THIS node — the re-attestation is a proved
theorem. The ≥15-sig-digit floor is the ROUTE-NOT-YET-FOUND handed to the Rung-3 successor (instrument
precision, no fit, no new axiom). Measured 1/α(0) ≈ 137.036 REMOVABLE PROSE ONLY; no empirical number in
any statement or proof. Explicit NO-FIT.

## PHYSICS-WORDS-REMOVABLE
Delete every physics word: what remains over Cut is that recomposedEndpointDressed = 2425/18 +
(3/2)·kappaLeading·cInner with base 2425/18 = 7179/54 + 16/9 exempt; that the two architecture readings
differ at the top band by bandScreen projectedS topBandRatio with projectedS ≠ 0; that depthWeight =
1/3 ≠ 0, ≠ 1; that recomposedEndpointDressed = PerChannelRecomb.recomposedEndpoint + (1/2)·κ·cInner
with the bare object census-complete; and the teeth. Pure Cut arithmetic on the banked objects.

## COST BUDGET / KILL (W9)
Every proof is a citation / projection of a banked theorem + norm_num on rationals — no heavy
normalizer, no coordinate expansion, no cutLog/cutPi unfolding. KILL: if any single obligation needs
>30s STOP and decompose (none should). Probe-build this ONE module in isolation before committing.

## DELIVERABLE
Module Phys/Algebra/FrameAuditedCompositionReattestation.lean, namespace
Phys.Algebra.FrameAuditedCompositionReattestation. Imports: FrameAuditRecombinationArchitecture (F1),
FrameAuditDressingScope (F2), D7CompositionAttestation (bare census-completeness); D7DepthTwoComposed /
PerChannelRecombination transitively. Costume C582 (bites 1=582). AxiomAudit: import line + #print
axioms lines. Ledger N582 row.

## FORK GUARD
Exactly ONE forward successor: the D7 Narrowing Rung 3 (ext7 1/cutPi + s7 L_m01). No generic SELECT
before the narrowing → D7 → D8 chain completes.
