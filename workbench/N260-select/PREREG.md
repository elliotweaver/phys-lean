# N260 PREREG — THE GATHER TEAR REGENERATES THE AMBIENT (the 7 Lie-generates all of so(7))

## TARGET
Over the derived octonion rung `O ℚ = CD (H ℚ)`, fundamental 7 = `ImO`, inside the single derived
`so(7) = skewAdjointLieSubalgebra bornBil`: the gather-tear fundamental-7 (`range crossEmb`, N225)
Lie-GENERATES the WHOLE ambient — the 14-dim derivation algebra g₂ (`range imRepL`, the adjoint the
field POSITS as the structure group) is generated by brackets of the 7:

  ★★★ `crossEmb_lieSpan_eq_so7` :
        `LieSubalgebra.lieSpan ℚ (Module.End ℚ ImO) (↑(LinearMap.range crossEmb))
           = skewAdjointLieSubalgebra bornBil`
      (the Lie subalgebra generated by the fundamental-7 tear IS all of so(7))
  ★★  `finrank_lieSpan_crossEmb = 21`  (it fills all 21 dims; W8 non-vacuity — genuinely all)
  ★★★ (the ⊇ engine) `range imRepL ≤ (lieSpan …).toSubmodule` : the adjoint-14 g₂ is REGENERATED
      from brackets of the 7 (NOT posited)
  ★★  W8 TEETH witness `proj14 ⁅crossOp (imBasis 1), crossOp (imBasis 2)⁆ ≠ 0` — the 14-part of a
      concrete tear-bracket is nonzero (so the regeneration is genuine, not vacuous)

## THE DISSOLUTION (STANDARD §0 / moat)
The field POSITS the structure/derivation group g₂ (or a GUT) as a GIVEN container that matter
embeds in. The theory INVERTS + DISSOLVES it: the ambient `so(7)` is DERIVED FIRST (the gather tear
`so(7) = 14 ⊕ 7`, N225, simple), and the 14-dim derivation algebra is GENERATED by brackets of the
fold's own tear directions (the 7). No containing group posited — the ambient closes on itself from
the tear. This is DEEPER than the deferred "12→21 SM-factors-fill-so7": that is the standard-physics
FACTOR framing and needs the ugly `su(3)+su(2) → g₂(14)` coordinate rank (measured: no clean
structural proof — the W9 grind N259 rightly deferred). The tear framing (7→21) is the fold-native
object and closes CLEAN via g₂-irreducibility. REDIRECT per SELECT S3 (FOLLOW THE THEORY).

## ONE CAUSE MANY TERMINATIONS
The SAME g₂-irreducibility of the fundamental 7 (`no_proper_invariant_ImO` / the adjoint-14 version
`imRepL_range_no_proper_invariant`) that (elsewhere) forces atom-irreducibility here forces the
tear's brackets to REGENERATE the entire simple ambient: one simplicity fact, the whole 21.

## MEASURED EXACT (workbench/N260-select/*.py + /tmp probes, exact ℚ on the banked octonion tower)
- `[range crossEmb, range crossEmb]` spans EXACTLY 21 = all so(7) in ONE bracket layer (dim rank).
- All 21 pair-brackets `⁅crossOp eᵢ, crossOp eⱼ⁆` are MIXED (both proj7 and proj14 nonzero) ⟹ a
  SINGLE witness bracket suffices for the ⊇ nonzero step.
- 7 ⟂ 14 under `traceForm7`: `traceForm7 (crossOp a) (imRep D) = 0` for ALL a ∈ ImO, D ∈ g₂
  (verified identically zero over the full g₂ basis).  [On imBasis: `tr7 (mul7 (Km i) Imat) = 0`
  for i=0..6.]
- WITNESS integers (banked qI calculus): with `crossOp (imBasis 1) = qI Km1`, `crossOp (imBasis 2)
  = qI Km2` (`crossOp_imBasis_eq_qI`, N257) and `imRep DI = qI Imat` (`imRep_DI_eq_qI`, N253):
    `tr7 (mul7 (mul7 Km1 Km2 − mul7 Km2 Km1) Imat) = −12 ≠ 0`   (the 14-part witness value)
    `tr7 (mul7 (mul7 Km1 Km2 − mul7 Km2 Km1) (Km k)) = {6,0,0,0,0,0,0}` (7-part present but IRRELEVANT
       to the witness — killed by orthogonality)
- (deferred, NOT this node) the SM reductive FACTORS `⟨both⟩_Lie = 21`, `⟨colour∪isospin⟩_Lie = 14`
  — TRUE but needs `su(3)+su(2)→g₂` coordinate rank, no clean structural proof; the tear route
  supersedes it as the clean fold-native capstone.

## ROUTE (all structural at the End/submodule level except two bounded qI `decide` facts — NO octonion coordinate bash)
Let `L := LieSubalgebra.lieSpan ℚ (Module.End ℚ ImO) (↑(range crossEmb))`.

(1) `L ≤ so7`:  `so7 = skewAdjointLieSubalgebra bornBil` is a LieSubalgebra containing `range
    crossEmb` (`crossEmb_range_le_so7'` at submodule level; lift to the LieSubalgebra carrier), and
    `lieSpan_le`/`subset_lieSpan` give `L ≤ so7`.

(2) THE ORTHOGONALITY LEMMA `traceForm7_crossOp_imRep_DI` : `∀ c, traceForm7 (crossOp c) (imRep DI)
    = 0`. Both bilinear/linear in the argument; vanish on `imBasis` via `crossOp_imBasis_eq_qI` +
    `imRep_DI_eq_qI` + `traceForm7_qI` ⟹ `tr7 (mul7 (Km i) Imat) = 0` (`decide`, i=0..6); extend to
    all `c` by `Module.Basis.ext`/linearity on the banked basis of `ImO`.  (only 7 integer decides)

(3) THE WITNESS `proj14_bracket_ne_zero` : `proj14 ⁅crossOp (imBasis 1), crossOp (imBasis 2)⁆ ≠ 0`.
    Let `B := ⁅crossOp (imBasis 1), crossOp (imBasis 2)⁆`. `B ∈ so7` (bracket of two so7 elts).
    Functional `φ X := traceForm7 X (imRep DI)`:
      - `φ B = −12`: `B = qI (mul7 Km1 Km2 − mul7 Km2 Km1)` (via `crossOp_imBasis_eq_qI`, `qI_comp`,
        `qI` additive, Lie-bracket = commutator in `End`), then `traceForm7_qI` + `decide`.
      - `φ (proj7 B) = 0`: `proj7 B ∈ range crossEmb` (`proj7_apply_mem`), so `= crossOp c`; (2) gives 0.
      - `B = proj7 B + proj14 B` (`proj7_add_proj14` + `projSo7_apply_self` on `B ∈ so7`), φ linear ⟹
        `φ (proj14 B) = φ B − φ (proj7 B) = −12 ≠ 0` ⟹ `proj14 B ≠ 0`.

(4) THE ⊇ ENGINE `range imRepL ≤ L.toSubmodule`:
    - `proj14 ⁅crossOp a, crossOp b⁆ ∈ L.toSubmodule` for all a b: the bracket ∈ L (Lie subalgebra,
      generators in `range crossEmb ⊆ L`); `proj7 (bracket) ∈ range crossEmb ⊆ L`; `proj14 (bracket)
      = projSo7 (bracket) − proj7 (bracket) = bracket − proj7 (bracket) ∈ L` (bracket ∈ so7 ⟹ projSo7
      = id; L a submodule).
    - `W := Submodule.span ℚ { proj14 ⁅crossOp a, crossOp b⁆ : a b }`. `W ≤ range imRepL`
      (`proj14_apply_mem`); `W ≤ L.toSubmodule` (above).
    - `W` is `adEnd`-invariant: `adEnd D (proj14 ⁅crossOp a, crossOp b⁆) = proj14 ⁅imRep D, ⁅crossOp
      a, crossOp b⁆⁆` (`proj14_equivariant`) and Jacobi + `crossOp_imRep_intertwine`
      (`⁅imRep D, crossOp a⁆ = crossOp (D a)`) ⟹ `= proj14 ⁅crossOp (D a), crossOp b⁆ + proj14
      ⁅crossOp a, crossOp (D b)⁆ ∈ W`.
    - `W ≠ ⊥` (the (3) witness ∈ W). So `0 < finrank W`; `imRepL_range_no_proper_invariant` ⟹
      `¬ (finrank W < 14)` ⟹ `finrank W ≥ 14`; with `W ≤ range imRepL` (finrank 14) ⟹
      `W = range imRepL` (`Submodule.eq_of_le_of_finrank_le`). So `range imRepL = W ≤ L.toSubmodule`.

(5) so7 ≤ L:  `sup_eq_so7`: `range imRepL ⊔ range crossEmb = so7.toSubmodule`; both ≤ L.toSubmodule
    ((4) + `subset_lieSpan`) ⟹ `so7.toSubmodule ≤ L.toSubmodule` ⟹ `so7 ≤ L`.

(6) `crossEmb_lieSpan_eq_so7`: (1) + (5) ⟹ `L = so7` (`le_antisymm`).
    `finrank_lieSpan_crossEmb = 21`: rewrite L = so7, `finrank_so7_eq_21`.

## LEVERS (all banked — to grep-confirm before writing)
- `crossEmb`, `crossEmb_range_le_so7'`, `crossOp`, `crossOp_mem_so7` (N223/N225)
- `crossOp_imRep_intertwine` (`⁅imRep D, crossOp a⁆ = crossOp (D a)`, N223, TowerGatherObstructionAdjointSo7)
- `imRepL`, `imRep`, `imRep_injective`, `imRepL_range_no_proper_invariant` (N22/FFTKnownSummandsIrreducible)
- `sup_eq_so7`, `finrank_so7_eq_21`, `skewAdjointLieSubalgebra bornBil`, `bornBil` (N225/N28)
- `proj7`, `proj14`, `proj7_apply_mem`, `proj14_apply_mem`, `proj7_add_proj14`, `proj14_equivariant`,
  `proj7_range` (= range crossEmb), `proj14_range` (= range imRepL) (N231)
- `projSo7`, `projSo7_apply_self` (fixes so7) (N230)
- `traceForm7`, `traceForm7_qI`, `traceForm7_symm`, `imRep_DI_eq_qI`, `Imat`, `qI`, `qI_comp`, `tr7`,
  `mul7` (N253); `crossOp_imBasis_eq_qI`, `Km`, `Km0..Km6`, `imBasis` (N257/DerivationStabilizerSplit)
- `adEnd`, `adEnd_apply` (N227); Mathlib `lieSpan`/`subset_lieSpan`/`lieSpan_le`/`mem_lieSpan`,
  `Submodule.eq_of_le_of_finrank_le`, `Submodule.span`, `Module.Basis.ext`

## COSTUME
C291: bites at `finrank_lieSpan_crossEmb = 21` (a WRONG reading — the 7 does NOT generate all of
so(7), the generated algebra is smaller than 21 — reduces through the banked finrank to `21 = 291`).
Pair (291, 21) fresh; RHS 291 distinct from every prior right-hand value.

## SCOPE — HONESTY CLAUSE
Banks: the tear-7 Lie-generates so(7) (lieSpan = so7) + finrank 21 + the ⊇ regeneration of the
adjoint-14 + the nonzero witness ONLY. Does NOT claim the SM-reductive-FACTORS generation (12→21,
deferred, needs su(3)+su(2)→g₂ rank), NOR the weak mixing angle (still structurally blocked).

## FREE-FLOATING CHECK (SOUL rail)
Every headline TYPE mentions the banked tower objects (`crossEmb`/`imRepL`/`skewAdjointLieSubalgebra
bornBil`/`crossOp`/`imRep`/`proj14` on the derived `O ℚ`) — the CONCRETE lieSpan of the banked
fundamental-7 tear, NOT a carrier-agnostic "irreducible module generates its ambient". All new names
grep-clean before writing.

## PHYSICS-WORDS-REMOVABLE
Delete gauge/colour/isospin/hypercharge/structure-group: the pure statement "the Lie subalgebra
generated by `range crossEmb` (dim 7) is the full 21-dim `skewAdjointLieSubalgebra bornBil`, and the
14-dim `range imRepL` is contained in it" stands as pure math about the banked cross-embedding and
derivation algebra of `O ℚ`.
