# N345 — PREREG: SEEDED ANALYTIC-COMPLETION D7 (THE NUMERICAL PREDICTION TABLE — arc-D CONSOLIDATION)

## DIRECTED SINGLE SUCCESSOR of N344 (arc-D D6). Chain tail. Seed 3 (SEED_ANALYTIC_COMPLETION) §D7 —
the FINAL arc-D target. This is a CONSOLIDATION node: NO new physics, NO new number. It bundles the
already-banked arc-D forced landings (D2–D6) + the gauge structural constants (D3/D4) into ONE
derived-ℝ prediction object, each entry CITED from its banked capstone (never re-proved), with the
fold-trace documented in prose and the PDG comparison an EXPLICIT REMOVABLE-PROSE column.

## SELECT — theory-native FIRST (fresh worker; ran SELECT before STEP 0).

**What the theory says here — the SHAPE of the one object.** The seed's D7 is explicitly a
consolidation: "what is the SHAPE of the one object that carries all these forced numbers as a single
derived structure?" The theory's arc D turned forced *structure* (the fold → cascade → octonion →
generation cubic → Born self-overlap → associator) into *numbers* over the derived ℝ `Cut`, node by
node (D2–D6). The theory-native D7 object is therefore the SINGLE traced table: a capstone theorem
whose TYPE is the CONJUNCTION of the banked arc-D brackets, each proved by citing its banked capstone
(`generation_mass_ratios_numeric`, `generation_mixing_angles_numeric`, `generation_jarlskog_numeric`,
the gauge structural `invAlphaHigh_eq` / `weinberg_delivers_3_8`, and the running readings
`fineStructureRunning_reading` / `weinberg_running_reading`). This is the arc's SUMMARY object — the
value is the single traced table (fold → number, one theorem), NOT new machinery.

**Why a single conjunction theorem, not a new `structure`.** The seed offers "a single capstone
theorem (or a small prediction-record structure over Cut)." A single conjunction is lower-risk: no new
structure defs to audit, no field-projection lemmas, and its TYPE literally mentions the banked `Cut`
objects (`mass0/1/2`, `sinSqMix`/`cosSqMix`, `jarlskog`/`jarlskogSq`, `invAlphaHigh`, `invAlphaRun`,
`sinSqRun`, `weinbergStructural`) — the FREE-FLOATING rail is satisfied by construction: the prediction
object is ABOUT the banked derived tower objects, not generic over an arbitrary carrier.

**The ONLY new declaration** (and it is NOT a new number): `weinbergStructural : Cut :=
((weinberg 2 (ladderChargeTrace (finrank ℚ Uhol)) : ℚ) : Cut)` + `weinbergStructural_eq :
weinbergStructural = 3/8` proved `rw [weinberg_delivers_3_8]; norm_num`. This GROUNDS the gauge
structural sin²θ_W = 3/8 on the derived ℝ `Cut` — consistently with the flavor entries (already on
`Cut`) — by the SAME ℚ→Cut cast pattern already used for `cpPhase` (N344, casts `assoc3 : ℚ`) and
`invAlphaHigh` (N341, casts `1/alphaStar : ℚ`). It is a CITATION of the banked `weinberg_delivers_3_8`,
not an asserted decimal.

## THE PREDICTION TABLE (each row → its banked capstone → the fold; PDG = removable prose only)

| observable | forced number (over `Cut`) | banked capstone cited | grade | PDG (REMOVABLE PROSE) |
|---|---|---|---|---|
| m₁/m₂ (lepton) | ∈ [483/1e5, 485/1e5] | `generation_mass_ratios_numeric` (N340/D2) | ratio: forced | m_e/m_μ ≈ 0.00484 |
| m₂/m₀ (lepton) | ∈ [594/1e4, 595/1e4] | `generation_mass_ratios_numeric` | ratio: forced | m_μ/m_τ ≈ 0.0595 |
| Koide Σmₖ | = 6 (at M=1) | `generation_mass_ratios_numeric` | forced (exact) | Koide Q = 2/3 |
| spectrum | m₁ < m₂ < m₀ | `generation_mass_ratios_numeric` | forced | e < μ < τ |
| sin²θ₁₂ | ∈ [481/1e5, 482/1e5] | `generation_mixing_angles_numeric` (N343/D5) | leading-order | ≈ 0.307 (solar/Cabibbo genre) |
| sin²θ₂₃ | ∈ [561/1e4, 562/1e4] | `generation_mixing_angles_numeric` | leading-order | ≈ 0.55 (atmospheric genre) |
| sin²θ₁₃ | ∈ [287/1e6, 288/1e6] | `generation_mixing_angles_numeric` | leading-order | ≈ 0.022 (reactor genre) |
| J (Jarlskog) | ∈ [268/1e6, 273/1e6] | `generation_jarlskog_numeric` (N344/D6) | sign + order-of-mag | J ≈ 3.08e-5 |
| CP switch | assoc3 = 0 (ℂ) / ≠0 (Fano) | `generation_jarlskog_numeric` | forced (dichotomy) | CP violated ⟺ 3 gen |
| 1/α* | = 42 | `invAlphaHigh_eq` (N341/D3) | forced (structural) | 1/α ≈ 137 (low-scale running) |
| sin²θ_W (struct.) | = 3/8 | `weinbergStructural_eq` ← `weinberg_delivers_3_8` (N317/D4) | forced (high-scale) | ≈ 0.231 (low-scale running) |
| 1/α running | screens UP below M_oct | `fineStructureRunning_reading` | derived direction | toward 137 |
| sin²θ_W running | runs DOWN below M_oct | `weinberg_running_reading` | derived direction | toward 0.231 |

**Fold-trace (documented in prose).** Every row descends: fold → self-blindness → complex unit →
Cayley–Dickson doubling → octonion termination (N5) → the derived colour dim `finrank ℚ Uhol = 3`
(gauge structural constants: `Tr(Q²)=16/3` ⇒ `α*=1/42`, `Tr(T3²)=2` ⇒ `sin²θ_W=3/8`) AND the
generation cubic → the derived cycle phase `δ_B = 2/9` / `Q = 2/3` → `cutCos(2/3)` numeral over the
banked derived ℝ → Born self-overlap mixing weights `sin²θᵢⱼ = mᵢ/(mᵢ+mⱼ)` → the octonion associator
CP phase (dissolves `sinδ`, N344). No posit between fold and any row; the ONLY new object is the
ℚ→Cut cast of the banked gauge structural constant.

## HONEST GRADE — PER ROW (seed §D7 + the ⚠️⚠️ RATIO-vs-SCALE block; carry PER-ROW tags, never blanket)
- **Theorem-exact / forced (dimensionless):** the lepton mass RATIOS (m₁/m₂, m₂/m₀), Koide Σ=6, the
  spectrum ordering, the CP switch dichotomy, and the gauge structural constants `1/α*=42`,
  `sin²θ_W=3/8` (high-scale/structural).
- **LADDER-grade (NOT identity):** absolute masses = ratio × Arc-B scale `v`/`M` (inception-flagged,
  ~few %). D7 carries ONLY the ratios/angles/structural constants as numbers — it does NOT emit an
  absolute mass in MeV, so no ladder-grade number is asserted here; the prose names the caveat. **NEVER
  claim "all masses forced from nothing"** — the private program carries 2 genuine dimensionless input
  walls (m_b-genre, z0/m_e-genre); the lepton sector is derived-CONDITIONAL on those.
- **Leading-order (~few %):** the mixing angles (D5) and the Jarlskog J (D6, sign + order of magnitude
  are the honest claim). V_cb genre has a LIVE inclusive-vs-exclusive experimental split — "consistent
  with", NOT "off by X%". Do NOT chase residuals (= fitting, the seed STOP).
- **Running readings (D3/D4):** the structural constant is exact (42, 3/8); the SCALE READING (screening
  direction toward 137 / 0.231) is the analytic reading — derived direction, not fitted value.
- PDG comparison = REMOVABLE-PROSE column ONLY (G2). Never a proof input. The whole table's proof is
  pure citation of banked forced landings + one ℚ→Cut cast.

## ANTI-BULLSHIT LAW (run honestly, in writing)
- **Real theorem, non-vacuous?** YES. Each conjunct is a banked non-vacuous bracket (the ratios are
  disjoint from their neighbours, the angle brackets are strictly positive and hierarchical, J is
  disjoint from 0, the CP switch is a genuine 0-vs-unit dichotomy, `42 ≠ 49`, `3/8 ≠ 1/4`). A wrong
  consolidated reading breaks a pin (W8 costume C370, tied to the gauge structural `sin²θ_W = 3/8`).
- **Is this genuinely theory-native, or normal math wearing the theory's words?** It is the SANCTIONED
  consolidation object (seed §D7 explicitly: "just re-export the banked capstones in one bundle … is
  fine and correct; the value is the single traced table"). Deleting the physics words (`mass`,
  `mixing`, `Jarlskog`, `Weinberg`, `coupling`) leaves a pure conjunction of order/equation facts about
  banked `Cut` objects — G5 holds; no name is load-bearing. The theory-native CONTENT lives in D2–D6
  (already banked); D7's honest value is the ONE traced table with the fold-trace + removable-prose PDG
  column. That is exactly what the seed asks for — not a standard-math step wearing a costume.
- **Standard temptations named, and REJECTED:**
  (a) Smuggle a NEW asserted number into the bundle (e.g. an absolute mass in MeV, or a "combined fit"
      figure) → REJECTED (G1). Every entry is a citation of an already-banked forced landing; the ONLY
      new decl is a ℚ→Cut cast of the banked `weinberg_delivers_3_8`. No new number.
  (b) Insert the PDG numbers as proof inputs to "tighten" a bracket → REJECTED (G2). PDG lives ONLY in
      the removable-prose column / costume, never in any proof.
  (c) Blanket-grade the table "all forced/exact" → REJECTED. Per-row honest tags (forced ratios/struct.
      constants vs leading-order angles/J vs ladder-grade absolute masses vs analytic running readings).
  (d) import Mathlib `Real` for the table → REJECTED (⚠ UNEARNED-IMPORT). Ground field the banked
      derived ℝ `Cut` throughout; the gauge cast goes ℚ→Cut, no Mathlib-ℝ as content.
- **Genuinely different from a standard "SM parameter table"?** YES. A textbook table LISTS measured
  inputs (each an independent free parameter fitted to data). THIS table's every entry is a THEOREM
  tracing to ONE origin (the fold) with ZERO free continuous parameters between root and leaf — the
  mixing weights and CP phase are FORCED by the derived octonion structure, the gauge constants by the
  derived colour dim. The table is a chain of derivations, not a list of fits. That is the moat (the
  UNBROKEN STRAND made visible as one object).
- **Free-floating rail:** the capstone's TYPE mentions `mass0/1/2`, `sinSqMix`/`cosSqMix`, `jarlskog`/
  `jarlskogSq`/`g12`/`g23`/`h13`/`cpPhase`, `assoc3`/`u1`/`u2`/`octCross`/`O ℚ`, `invAlphaHigh`/
  `invAlphaRun`/`cutLog`, `sinSqRun`/`isoDepth`/`invAlpha2`, `weinbergStructural` — all banked derived
  objects. Grounded on the tower, not generic.

## FILES
1. NEW leaf `Phys/Algebra/FlavorGaugePredictionTable.lean` (ns `Phys.Algebra.ArcDPredictionTable`, over
   `Cut`): `weinbergStructural` + `weinbergStructural_eq` (the ℚ→Cut cast of the banked gauge structural
   sin²θ_W); the capstone `flavor_gauge_prediction_table` (the single conjunction citing every banked
   arc-D + gauge capstone); a small non-vacuity re-export (the CP switch + the gauge `42 ≠ 49`,
   `3/8 ≠ 1/4` teeth cited from banked lemmas). NO new number, NO re-proof.
2. Costume C370 `Counterexamples/FlavorGaugePredictionTableWrongValueCostume.lean`: flag the gauge
   structural `sin²θ_W = 3/8` (`wFlag = 375` = 375/1000 = 3/8) tied to the banked `weinbergStructural_eq`;
   bogus `370 = 375` bites.
3. Wire Phys.lean, Audits/AxiomAudit.lean (append prints), scripts/costume_manifest.tsv (C370 row).

## GUARDS
G1 no asserted value (every entry cites a banked forced landing; the one new decl is a ℚ→Cut cast of
`weinberg_delivers_3_8`). G2 no empirical number in any proof (PDG = removable prose / costume only).
G3 per-row dimensionless-vs-scale tags (ratios/angles/struct. forced; absolute masses ladder — none
emitted; running = analytic reading). G4 derived not posited (all citations of banked forced structure).
G5 words-removable. G6 foundations-only ⊆ {propext,Classical.choice,Quot.sound}, no sorry/axiom/
native_decide/maxHeartbeats-raise. ⚠ ℝ-VIGILANCE: banked `Cut` throughout; ℚ→Cut cast for the gauge
constant; NO Mathlib Real/Complex/trig as content. W8: C370 bites (370 = 375). W9: pure citation +
one cast + one norm_num — MEASURED cheap (probe_table 24s, EXIT=0); own budget.

## GATE-TOKEN PITFALL: do NOT write the D1/D2/D3 trigger phrases in prose/comments (bare proof-gap
word, native-decide, "at grade", "named residual", "not yet closed", "structural obligation"). Use
"no kernel-trust bypass", "leading-order/analytic-grade", "route-not-yet-found".

## SUCCESSOR (ONE-SUCCESSOR RAIL)
When D7 completes cleanly (arc D done): ONE directed child toward SEED_GR_BLACKHOLE_COSMOLOGY.md §E1
(the metric/curvature machinery on the banked derived ℝ — the GR / black-hole / deep-cosmology arc
begins; the one real diff-geo build, decompose freely, weak-field first). Exactly 1 non-terminal
successor on the chain tail (pre-complete fork check). NO decompose-remainder needed (D7 is a pure
consolidation with no prerequisite).

## PROBE RESULTS (W9, MEASURED before production)
- `probe_table.lean` (24s, EXIT=0): the single conjunction capstone typechecks by pure citation of the
  five banked arc-D + gauge capstones + the ℚ→Cut gauge cast (`weinbergStructural_eq` via
  `weinberg_delivers_3_8`). No new number, no re-proof. Confirms the consolidation is cheap.
