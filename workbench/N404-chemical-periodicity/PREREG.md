# N404 PREREG — arc-L L3 (MECHANISM, beyond-key)

## TARGET
CHEMICAL PERIODICITY = THE MODULAR RECURRENCE FORCED BY SHELL CLOSURE.
Filling the banked antisymmetric shells (N403) restarts every `d` quanta because
`⋀^(d+1)` of the finite carrier vanishes (banked `shell_closes_beyond_capacity`);
valence occupancy = `Z mod d` recurs with period `d = carrierDim` (the derived carrier
dimension, banked `fibre_finrank = 2`). Closed-shell (noble) landmark = banked top singlet.
One-cause weld on the banked 7-carrier ImO (period 7).

## GRADE: MECHANISM (seed §L3, beyond-key). NO chemistry number fitted (G1/G2 NO-FIT LOUD).
Do NOT fit 2/8/18/32 (needs un-banked angular-momentum sub-shell tower) — NOT attempted, flagged.

## BANKED LAUNCH PLATFORM (N403, ns Phys.Algebra.AtomicShell)
- `fermiSector N := ⋀[Cut]^N StateFibre`; `sector_capacity : finrank = C(2,N)`.
- `shell_subsingleton_beyond_capacity {N} (2<N) : Subsingleton (fermiSector N)`.
- `orbital_occupancy` C(2,1)=2; `orbital_filled` C(2,2)=1; `orbital_nonvacuous` Nontrivial(fermiSector 2).
- `imO_capacity : finrank ⋀^N ImO = C(7,N)`; `imO_closes_beyond_seven {N}(7<N)`.
- `Phys.Quantum.fibre_finrank : finrank Cut StateFibre = 2`.

## PRODUCTION DECLS (all compiled in probe2, EXIT 0)
carrierDim (=finrank Cut StateFibre), carrierDim_eq, valence, completedShells,
filling_decomposition, ★valence_periodic, valenceSector_dim_periodic,
★★filling_restarts (Subsingleton overfill vs Nontrivial restart — the load-bearing banked weld),
group_particle_hole, noble_recurs, noble_shell_is_filled_singlet,
imO_valenceSector_dim_periodic, imO_filling_restarts, shell_total_fillings, capstone.

## COSTUME C429 (bites 1=429): flag = "periodicity genuinely recurs (valence at Z=3 = valence at Z=1
= Nontrivial shell) YET overfill closes" — tied via filling_restarts.

## GUARDS
G1 no asserted values ✓ (period = derived finrank, not posited). G2 no empirical number ✓.
G3 derived from banked closure ✓. G4 words-removable ✓ (delete chemistry/period/valence/noble →
modular recurrence of exterior-power dims of a finite carrier). G5 foundations-only ✓.
Free-floating ✓ (types mention fermiSector/StateFibre/ImO/carrierDim=banked finrank).
W8 ✓ (filling_restarts: Subsingleton vs Nontrivial genuinely bites). W4.5 ✓ (exactly L3).

## W9 measure-first: probe1 EXIT 0 (combinatorics), probe2 EXIT 0 (full production). CHEAP node.
