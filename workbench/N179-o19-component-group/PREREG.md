# N179 PREREG — THE ABSTRACT GROUP OBJECT of the O(1,9) DISCRETE COMPONENT GROUP (π₀(O(1,9)) = 4)

FROZEN before compute.

## FRONT CHOSEN (W4.5 measure-first / W1 reframe)
Front (i): the abstract group object of the N173 O(1,9) discrete component group
{1, parityLin, timeRevLin, ptLin}. N173 banked these ONLY as elements of the MONOID
`Module.End Cut STVC` with the Klein relations (parityLin_sq/timeRevLin_sq/ptLin_sq,
parity_timeRev_comm, ptLin_eq) and distinctness (fullO19_components_distinct). It NEVER
formed a group, NEVER computed card/exponent/abstract class/iso. This OPENS the π₀-type
classification of the FULL O(1,9) — a DIFFERENT object from the gauge G₂ Klein-four N178
(that subgroup lives in Aut(O) = G₂ ⊂ O(8); this one is the component group of the
indefinite O(1,9) orthogonal group, P/T/PT reflections of spacetime).

## THE LIFT (the only genuinely-new step vs N178)
The reflections are in the monoid `Module.End Cut STVC`, not a group. Each squares to 1
(banked), so each is a UNIT. Lift via `Units.mkOfMulEqOne R R R_sq` into the group
`(Module.End Cut STVC)ˣ`, form `Subgroup (Module.End Cut STVC)ˣ`, then reuse the EXACT
N178 pattern (ncard=4, exponent=2, IsKleinFour, not cyclic, ≃* Multiplicative (ZMod 2 ×
ZMod 2)). All Klein-table + distinctness facts transport from `Module.End` to units via
`Units.ext` / `congrArg Units.val`.

## GO / NO-GO
GO if the candidate production file (units lift + Subgroup + the 5 abstract-group-object
theorems mirroring N178) compiles clean with `#print axioms` ⊆ {propext, Classical.choice,
Quot.sound}, and the costume C211 bites (a wrong card/exponent forces a false numeric).

NO-GO / DECOMPOSE if the unit-lift or the Subgroup closure proves heavy (> KILL).

## KILL (instrument-failure threshold — W9)
- Any single obligation (the Subgroup mul_mem closure, the ncard, the exponent) that does
  not compile within ~60s in a bounded probe is an INSTRUMENT failure → measure/decompose,
  NOT a longer grind.
- Whole candidate file warm-compile target < ~30s (N178 warm olean was 8.3s; this is the
  same shape + a thin unit-lift, expect similar). If > 120s, DECOMPOSE into the unit-lift
  file + the group-object file.

## COSTUME (anti-vacuity, W8)
C211 must bite a WRONG claim of THIS front, reducing to a false numeric DISTINCT from the
banked battery (… C209 177=1, C210 4=1). Use `1 = 179` (the component-group order is
genuinely 4, NOT 1; a wrong "trivial/collapsed component group" claim forces 1 = 179 via
the order being 4 ≠ 1 phrased to a distinct numeric). Concretely: claim the component
group is trivial ⇒ Nat.card = 1, but it is 4 ⇒ contradiction; encode the wrong numeric as
`1 = 179` distinct from C210's `4 = 1`.

## PHYSICS-WORDS-REMOVABLE
Delete parity/time-reversal/Lorentz/O(1,9)/component/π₀/reflection/spacetime: the
statements stand as pure group theory — the explicit 4-element subgroup of the unit group
`(Module.End Cut STVC)ˣ` generated by the two involutive endomorphisms
{parityLin, timeRevLin} of `Cut × Cut × O Cut` has Nat.card = 4, Monoid.exponent = 2,
satisfies IsKleinFour, is not cyclic, and is abstractly ≃* Multiplicative (ZMod 2 × ZMod 2).
