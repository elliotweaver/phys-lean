## ✅ N292 LANDED — THE FOLD-ROOT-DRESSED FAMILY-BREAKING COSET of the derived colour-centralizer su(3) in `f₄ = Der(H₃(O))`: N287 measured (never banked) that the centralizer of the derived colour su(3) inside the derived `f₄` is a dim-8 SIMPLE su(3), of which the banked family `so(3)` (N288/N289) is the maximal compact and a 5-dim COSET is the symmetric complement; N291 built ONE member of that complement (`Mc = innerMul (slotA u1)(slotB 1)`). THIS node builds the full OFF-DIAGONAL coset as the FOLD-ROOT (`u1`) DRESSING of the three family generators — `kAB := innerMul (slotA u1)(slotB 1)` [= N291's Mc], `kBC := innerMul (slotB u1)(slotC 1)`, `kCA := innerMul (slotC u1)(slotA 1)` — each obtained from a family generator (`famC = innerMul (slotA 1)(slotB 1)`, etc.) by replacing the first real-unit slot with the fold-root `u1`. It banks: (1) `kBC, kCA ∈ derH3` (with N291's `kAB`, the three off-diagonal fold-root-dressed members, via `innerMul_mem_derH3` + `Xz_herm`); (2) COLOUR-NEUTRAL — `⁅jActL D, kBC⁆ = 0` for every colour `D` (the fold-root is colour-blind, N266/N201); (3) the GENERATION-ROTATION ACTIONS `kAB (slotA 1) = slotB u1`, `kAB (slotB 1) = slotA u1`, `kAB (slotC 1) = 0` — the fold-root-DRESSED analog of the family rotation (contrast the undressed family `famC (slotB 1) = slotA 1`, N288), moving the three cap-forced generations THROUGH the fold's own √−1; (4) FAMILY-VISIBLE (W8) — `kAB (slotA 1) = slotB u1 ≠ 0` (NOT in N290's family-blind core). THE READING: the family `so(3)` is the maximal compact of the larger derived `su(3) = Z_{f4}(colour)`, and the fold-root-dressed coset is its symmetric complement — the family-symmetry-BREAKING sector. METHOD: a fresh-context SELECT (docs/NODE_SELECTION.md S1–S4, comments 210–215) chose, through the anti-bullshit gate, the THEORY-NATIVE forward beat — placing N291's Mc in its forced algebraic home (the derived su(3) ⊃ family-so(3) symmetric pair) — over the saturated adjacent step (the (7,5) module character). MEASURED-FIRST (`workbench/N292-select/`, exact-Fraction octonion + numpy, all EXIT 0): `Z_{f4}(colour)` = dim 8, bracket-closed, Killing all-negative ⇒ compact simple su(3) = family-so(3)[3] ⊕ fold-root-dressed coset[5]; the su(3)/so(3) symmetric-pair CLOSURE `⁅kAB,kBC⁆ = famB` cyclic (residual 0.0, full-operator identity) MEASURED and de-risked (the derivation-Jacobi route `kAB·(slotB u1) = −slotA 1`, `kAB·(slotC 1) = 0`), but CHILDED as a directed W1 dissolution node (the N288→N289 analog — the operator route needs the adE-mixed-Leibniz tower). THE DISSOLUTION/MOAT: the field POSITS `F₄ ⊃ SU(3)_c × SU(3)_flavour` and CHOOSES the SU(3)_flavour → SO(3) breaking by hand (a flavour spurion); the theory DERIVES SU(3)_flavour = Z_{f4}(colour) and its breaking to the observed family `so(3)` as the fold-root dressing (the coset IS `u1·(family)`, colour-neutral because `u1` is colour-blind). THE OBJECTS (all in `Phys/Algebra/OctonionJordanColourCentralizerCoset.lean`, ns `Phys.Algebra.HJ`, 8 audited decls): `cosetBC_mem_derH3`/`cosetCA_mem_derH3`; ★★ `cosetBC_colour_neutral`; `reQ_u1`/`Dg000_zero`; ★★ `kAB_slotA_one`/`kAB_slotB_one`/`kAB_slotC_one`; ★★ `kAB_moves_generation` (W8); ★★★ capstone `colour_centralizer_coset` (bundles: kBC,kCA ∈ derH3 ∧ colour-neutral ∧ the three generation-slot actions ∧ family-visible ∧ carries `jordan_fails_H4`). THE ONE CAUSE, MANY TERMINATIONS: the SAME octonion non-associativity that stops the cascade (N2c), caps the Hermitian Jordan tower at order 3 (N5) — furnishing three matter slots (N267), the family `so(3)` (N288/N289) and its self-blindness (N290) — furnishes the derived `f₄` with the fold-root-dressed family-breaking coset that moves the generations through the fold's own √−1. 8 decls foundations-only `[propext, Classical.choice, Quot.sound]` (INDEPENDENTLY axiom-audited vs the freshly-built olean `workbench/N292-select/reaudit.out` EXIT 0 + gate D6-fast 8 newly-banked audit lines). Costume C321 (`⊢ 1 = 321`) bites at the coset-non-triviality certificate `((innerMul (slotA u1)(slotB 1)(slotA 1)) 0 2).re.im.re = 1` (tied via `kAB_slotA_one`; a WRONG reading — the coset is trivial / family-INVARIANT like N290's core / it does not move the generations / Z_{f4}(colour) has no family-breaking sector — reduces to the false numeric `1 = 321`). Physics-words-removable (delete colour/family/generation/flavour/breaking: the three members `innerMul (slotA u1)(slotB 1)`, `innerMul (slotB u1)(slotC 1)`, `innerMul (slotC u1)(slotA 1)` of the `LieSubalgebra derH3` are annihilated by every derivation killing `u1`, and `innerMul (slotA u1)(slotB 1)(slotA 1) = slotB u1` etc.; pure math, stands). NOT free-floating (every headline TYPE cites the banked `innerMul`/`slotA/B/C`/`derH3`/`jActL`/`u1` on `Matrix (Fin 3)(Fin 3)(O ℚ)`; capstone carries `jordan_fails_H4`). MEASURE-FIRST (W9): the whole route de-risked BEFORE production (`workbench/N292-select/probe_*.lean`, all EXIT 0); whole-module olean LIGHT (~8s; NO native_decide, NO octonion coordinate ring, NO proof-kernel maxHeartbeats raise). Advances ★4 (family replication — the family symmetry sits inside a larger derived su(3)), joined to ★5 (the family-symmetry-BREAKING sector, the fold-root-dressed coset) and ★2 (colour-neutral, electroweak-adjacent). SCOPE (docs/RUNBOOK.md W4.5): the three fold-root-dressed coset members + membership + colour-neutrality + generation-slot actions + non-vacuity ONLY. NOT the su(3)/so(3) symmetric-pair CLOSURE (CHILDED as a directed W1 node); NOT the full 8-dim su(3) as a bundled LieSubalgebra; NOT the (7,5) module character; NOT the ★2 mixing-angle VALUE (route-not-yet-found); NOT `= physical SU(3)_flavour` (removable prose). RESUME: single-flight run. Next: the ONE successor — a directed W1 dissolution node for the su(3)/so(3) symmetric-pair closure.

