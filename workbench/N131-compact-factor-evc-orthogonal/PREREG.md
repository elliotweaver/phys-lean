# N131 PREREG — THE COMPACT FACTOR OF AN ARBITRARY BvC-ISOMETRY IS EvC-ORTHOGONAL
*(the forced forward joint wiring N130's converse polar decomposition into N104's single-residual reduction)*

FROZEN before compute. KILL classifies a fight as INSTRUMENT failure (W9), not theory.

## MEASURE-FIRST FINDING (the smallest obligation of each priority, per W1/W9)

The ROADMAP §N131 names two priorities. Measured against what the chain ACTUALLY banks:

- **(i) GLOBAL REVERSE SURJECTIVITY** — "every EvC-orthogonal QvC-isometry IS a `biMulLin` word",
  closing `qvIsomMonoidC ≤ genIsomMonoidC2`. N104 (`evCOrthogonal_word_compact_part_mem_gen2`)
  REDUCED the full converse to this SINGLE residual. MEASURED: this residual is genuinely
  **W1-HEAVY group-manifold content**. N112–N114 already established that the spatial SO(9)
  word-membership needs the FULL ninth-direction generator family (`ninthFamilyGenSetLin`,
  `axisRotLin e c s`), and banked only the separator / non-commutation facts
  (`axisRotLin_word_not_commute`, `enlargedAxisNinth_ne_enlargedTwoPlaneNinth`) — never the full
  membership. N124's `gFormC_isom_mem_closure` exhausts SO(8) of `O Cut` over the **Householder
  reflection alphabet** `houseHolderSet` (PURE Born reflections `houseHolder a`, NO octonion
  multiplication), while `genIsomMonoidC2`/`genIsomMonoidLin` are generated by **octonion
  bimultiplications** `biMulFun`/`biMulLin` — connecting the two generating sets IS the Spin(8)
  triality content. NOT closeable in one run. This is the genuine W1 remainder, childed since N97.

- **(ii) OPERATOR TOPOLOGY over Cut** — Mathlib has no `TopologicalSpace (Module.End Cut STVC)`
  over the derived Cut. MEASURED: a W1-HEAVY construction node, orthogonal to (i). Not this node.

## THE FORCED BANKABLE FORWARD JOINT (THE ONE LAW — what the chain wants, not the mis-framed target)

N130 banked `bvIsomLin_polar_path_decomp`: an arbitrary `IsBvIsomLin S` factors `S = k·γ(1)` with
`k := S∘(p^{1/2})⁻¹` a `QvC`-isometry (`IsQvIsomC` only). But N104's reduction
`evCOrthogonal_word_compact_part_mem_gen2` consumes a STRONGER hypothesis on its input `g`:
`horth : h.comp g = LinearMap.id` (EvC-ORTHOGONALITY, i.e. `g* ∘ g = id`). N130 NEVER banked that
the compact factor `k` of an arbitrary BvC-isometry is EvC-orthogonal — only that it is `IsQvIsomC`.

**THE MISSING JOINT** (the genuine N131 forward content): *the compact factor `k` of an arbitrary
BvC-isometry is an EvC-ORTHOGONAL QvC-isometry* — its own EvC-adjoint composes back to the identity.
This is exactly the `horth` input of N104's reduction. Banking it WIRES N130 → N104, conditionally
closing the global surjectivity on the single clean residual `k ∈ genIsomMonoidLin` (childed N132 as
the honest W1 dissolution ticket — the literal Spin(9)/triality word membership).

The proof is the same calc body already inside `comp_polar_decomposition` (N93,
`LorentzContinuumCompactPart.lean`) which proves `k* ∘ k = LinearMap.id` for ANY `g` with an
EvC-adjoint + injective. The new content: feed an ARBITRARY BvC-isometry (via N130's
`bvIsomLin_has_evCAdjoint` + `bvIsomLin_injective`) into it, so the EvC-orthogonality is now
available for EVERY BvC-isometry, and the compact factor `k` together with its adjoint `kadj`
satisfies `kadj ∘ k = id` AND `IsQvIsomC k`.

## GO / NO-GO

GO if, over the derived ℝ `Cut` + terminal algebra `O Cut` + spacetime `STVC` + indefinite
Minkowski form `BvC`, all foundations-only:
1. `bvIsomLin_compact_factor_evCAdjoint` — the compact factor `k := S∘(p^{1/2})⁻¹` of an arbitrary
   BvC-isometry HAS the explicit EvC-adjoint `kadj := (p^{1/2})⁻¹∘h` (`IsEvCAdjoint k kadj`).
2. `bvIsomLin_compact_factor_evCOrthogonal` — ★ THE JOINT: `kadj ∘ k = LinearMap.id` (the compact
   factor is EvC-orthogonal — its EvC-adjoint composes back to id). The exact `horth` input of N104.
3. `bvIsomLin_compact_factor_isQvIsomC` — `IsQvIsomC k` (re-export from N130's decomposition, the
   compact factor is a QvC-isometry).
4. ★★ `bvIsomLin_compact_factor_word_mem_gen2` — THE CONDITIONAL CLOSURE: IF `k ∈ genIsomMonoidLin`
   (the single residual childed N132), THEN the bridged compact factor `endToFunEnd k ∈
   genIsomMonoidC2` — wiring N130's arbitrary-BvC-isometry compact factor straight into N104's
   `evCOrthogonal_word_compact_part_mem_gen2`. The global surjectivity reduced to the SINGLE clean
   word-membership residual, for an ARBITRARY BvC-isometry.
5. W8 teeth: `bvIsomLin_boost_compact_factor_evCOrthogonal` — the concrete boost (5/3,4/3)'s compact
   factor is EvC-orthogonal (`kadj∘k = id`), derived ONLY from its `IsBvIsomLin` property; AND a
   self-overlap / coordinate read-off the costume C163 bites with a false numeric `1 = 131`.

NO-GO / DECOMPOSE if any obligation resists after trunk-reframe → bank the EvC-orthogonality joint
(items 1–3, the clean structural piece), child the remainder.

## KILL (W9 INSTRUMENT budget)
- Per-obligation compile KILL: **90s**. Any single obligation exceeding 90s = INSTRUMENT failure →
  measure/decompose/reframe, NOT a longer grind. (N130 probes were 7.8–22.2s; this node reuses the
  SAME banked calc bodies, expect comparable.)
- NO inflated `maxHeartbeats`, NO `native_decide`, NO monolithic brute over expanded coordinates.
- If a route blows up once, do NOT re-run it — restructure or child.
- ≥2 obligations ending at full budget banking nothing = STOP, child the remainder.

## ANTI-VACUITY (W8)
Costume C163 MUST bite a WRONG compact-factor-EvC-orthogonality / reverse-surjectivity /
generation-closure claim, reducing to a false numeric `1 = 131` DISTINCT from C160 1=128, C161
1=129, C162 1=130. Reuse banked objects (the boost (5/3,4/3) compact factor self-overlap /
coordinate read-off). The W8 boost-compact-factor witness GENUINELY reads a true value over the
derived field; the costume's wrong value forces `1 = 131`, must NOT compile.

## PHYSICS-WORDS-REMOVABLE
Delete Lorentz/boost/rotation/compact/orthogonal/isometry/polar/KAK/Cartan/adjoint/Minkowski/
signature/Spin/SO(9)/SO⁺(1,9)/generation/generator/word/triality/bridge → what remains: over the
derived complete ordered field `Cut`, `O Cut`, the positive-definite form `EvC`, the indefinite form
`BvC`, for a `Cut`-linear `S` with `∀ p q, BvC (S p)(S q) = BvC p q`, the operator
`k := S∘(specOpN (cutSqrt∘c)⁻¹ u)` has an explicit EvC-adjoint `kadj` with `kadj∘k = id` and
`∀ p, QvC (k p) = QvC p`; and IF `k` lies in the closure of the `biMulLin` generator set THEN
`endToFunEnd k` lies in the closure of the bimultiplication generator set on `Function.End STVC`.
No theorem STATEMENT needs a physics word.

## STANDARD
Unbroken: every object DERIVED back to N130/N104/N93 ← the fold-derived ℝ Cut + terminal algebra
O Cut. Complete: `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`, no sorry/axiom/
native_decide. Words-removable: above. NO posited Lorentz group, NO Mathlib ℝ/ℂ as content, NO bridge.
