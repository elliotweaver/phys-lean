# N155 PREREG — COMPACT WORD-INDUCTION ASSEMBLY (the set-form `JoinedIdC` engine)

## FROZEN before compute. KILL classifies a fight as INSTRUMENT failure, not theory.

### MEASURE-FIRST FINDINGS (what the chain ACTUALLY produced)
- N154 banked `bvIsom_joined_id_of_compact_joined`: an arbitrary `BvC`-isometry `S` lies in a
  preconnected subset of `endOpC` with `1`, PROVIDED its polar compact factor `k` is path-joined
  to id by a CONTINUOUS PATH (the `hcompact` hypothesis, in path-function form).
- N105 PROVED the literal residual (R) "every EvC-orthogonal `QvC`-isometry IS a `biMulLin` word"
  is **FALSE** for the `biMulLin` alphabet: the ninth-direction (`x ↔ 𝕆`) rotations are NOT reached
  (`xNegLin_not_mem_genIsomMonoidLin`, `planeRotLin_not_mem_genIsomMonoidLin`).
- N106 (`genIsomMonoidLinPlus`) enlarged the alphabet; N114/N116 left the FULL `SO(9)` exhaustion
  (every compact `QvC`-isometry = a finite product of joined 2-plane rotation factors) as the
  explicit **childed HEAVY remainder** — NOT banked. The single `rotPath` only reaches the circle
  MINUS the antipode (`rotPath_joins_id_rotation` requires `1 + c ≠ 0`).
- ∴ the UNCONDITIONAL discharge of `hcompact` rests on the heavy open word-membership/exhaustion
  residual. This is a genuine W1/W9 HEAVY node, NOT a one-run grind.

### DECISION (W3 decompose / W1 option (a) — the ticket's sanctioned route)
Bank the immediately-bankable, genuinely-NEW structural piece — the **set-form assembly engine** —
which is strictly cleaner and more composable than N154's path-form, and child the word-membership.

The KEY REFRAME (THE ONE LAW / trunk-native): N154's `hcompact` is in PATH-FUNCTION form
(`∃ κ k0 k1, Continuous κ ∧ κ k0 = 1 ∧ κ k1 = k`), which does NOT compose under products (no
`Path.trans`/`unitInterval` over `Cut`). The trunk-native object is the **preconnected SET** form
`JoinedIdC g := ∃ Conn, IsPreconnected Conn ∧ 1 ∈ Conn ∧ g ∈ Conn` — which IS closed under
multiplication via `IsPreconnected.union` at the shared point + the right-mult glue
`endOpC_mul_right_cont` (N153). The set of operators joined to `1` in `endOpC` forms a SUBMONOID
of `Module.End Cut STVC`. This is the genuinely-new content and it dissolves the non-composability.

### GO / NO-GO
- **GO** = ALL of:
  1. `JoinedIdC` predicate + `joinedIdC_one` (the identity, `{1}` is preconnected).
  2. ★ `joinedIdC_mul` — THE ENGINE: `JoinedIdC a → JoinedIdC b → JoinedIdC (a*b)`
     (right-translate `A * b` preconnected via `endOpC_mul_right_cont`, union with `B` at shared `b`).
  3. ★ `joinedIdC_listProd` — WORD-INDUCTION: every finite product of `JoinedIdC` factors is `JoinedIdC`.
  4. `joinedIdC_planeRotLin` — every `rotPath`-reachable 2-plane rotation (`c²+s²=1`, `1+c≠0`) is `JoinedIdC`.
  5. ★★ `bvIsom_joinedIdC_of_compact_joinedSet` — THE FULL REDUCTION in set form: an arbitrary
     `BvC`-isometry is `JoinedIdC` PROVIDED its compact polar factor is `JoinedIdC` (the single
     residual, now in the COMPOSABLE set form). Strictly cleaner than N154.
  6. ★★ `bvIsom_joinedIdC_of_compact_word` — THE CONDITIONAL DISCHARGE: if every compact
     `QvC`-isometry is a finite product of `JoinedIdC` factors (the childed word residual), then
     EVERY `BvC`-isometry is `JoinedIdC` — the full `SO⁺(1,9)` path-connectedness CONDITIONAL on the
     compact-word residual, assembled purely from the engine.
  7. W8 NON-VACUITY: `joinedIdC_halfTurn` — the ANTIPODE half-turn `planeRotLin (-1) 0` (NOT
     `rotPath`-reachable, `1+(-1)=0`) IS `JoinedIdC`, as the product of two quarter-turns each
     `JoinedIdC` (`planeRotLin 0 1 * planeRotLin 0 1 = planeRotLin (-1) 0` by `planeRotLin_mul`).
     The word-induction genuinely EXTENDS the reach beyond the single Cayley chart.
  8. gate D0–D6 GREEN; costume C187 (1=155 distinct) bites; foundations-only; committed; LEDGER+ROADMAP;
     child the word-membership/exhaustion residual onto the chain tail.
- **NO-GO / DECOMPOSE** = if the engine itself resists (it should NOT — pure `IsPreconnected.union`),
  bank what compiles and child the rest. Failure / silent scope-reduction NOT on the menu.

### KILL BUDGET (W9 — instrument failure thresholds)
- These are ALL light `IsPreconnected.union`/`List.rec`/`congr`/`ring` proofs over already-banked
  objects. NO monolithic brute, NO inflated maxHeartbeats. KILL any single obligation that does not
  close in the DEFAULT heartbeat budget within ~2 min → it means the framing is wrong (THE ONE LAW),
  reframe through the trunk, do NOT inflate the ceiling.
- Probe in isolation first (`lake env lean` on a workbench copy), grep full errors, MOVE to production
  the instant a lemma compiles clean (bank-as-you-go).

### ANTI-VACUITY (W8)
`joinedIdC_halfTurn` reaches the antipode — provably OUTSIDE the single `rotPath` chart — so the
word-induction is not decoration. Costume C187 bites a WRONG `JoinedIdC`/half-turn/word-assembly
read-off, reducing to a false `1 = 155` (distinct from C183 1=151 … C186 1=154).

### PHYSICS-WORDS-REMOVABLE
Delete rotation/compact/isometry/path-connected/half-turn/word/Lorentz/SO/Spin/Cayley/circle →
`JoinedIdC g` = "g lies in a preconnected subset of the operator topology `endOpC` over the derived
ℝ `Cut` together with `1`"; `joinedIdC_mul` = "this set is closed under the ring multiplication";
pure topology + algebra over the derived objects. No theorem STATEMENT needs a physics word.
