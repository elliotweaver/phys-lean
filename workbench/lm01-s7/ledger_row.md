
---

## N574 — ARC-D (THE DIGITS): THE `L_m01` s7 MASS GRID (parallel narrowing child of t_2b137452)

`Phys/Algebra/Lm01GridS7.lean` (commit pending). A directed narrowing child that RESUMES the `L_m01`
sensitivity front named across the D7 dressed campaign (git 82d8aaa / 11c746d W9 ordering;
NarrowedBracket11's own §★ closing note): after the edge logs and κ, the SECOND-ranked residual
feeding `recomposedEndpointDressed` is the split@6 `L_m01 = cutLog(mass0/mass1)` (banked at width
`10⁻¹¹` in `NarrowedBracket11.cutLog_lo_s6/hi_s6`). This node sharpens it ONE GRID STEP DEEPER
(split@7) and banks a tightened two-sided rational bracket over the banked derived `Cut` objects.

THE WIN. Read the SAME derived cycle-phase cosine `cutCos(2/9)` at split@7 via the banked
`cutCos_bracket` (`partialCos (2/9) 7 = 128864817323146777/132113476427399775`, remainder
`≈ 8.2×10⁻²¹`), giving `pB ∈ [.../5e17, .../1e18]` (width `2×10⁻¹⁸`, was `10⁻¹⁵` at s6). Re-propagate
through the banked Born-square amplitude chain (`GenerationMassRatios.wB/rB/b0/b1/mass0/mass1`) to
`m01 = mass0/mass1 ∈ [347747283710459/1e11, 17387364185523/5e9]` (width `10⁻¹¹`, `≈ 3×10⁵×` tighter
than s6's `3×10⁻⁶`), then feed the D1 exp instrument (`cutLog_ge/le_certified`) at partial-sum index
`38`:

    cutLog(mass0/mass1) ∈ [815406111272599/1e14, 815406111272603/1e14]  (width 4×10⁻¹⁴)

— `≈ 250×` narrower than the banked s6 `[815406111272/1e11, 815406111273/1e11]` (width `10⁻¹¹`) and
NESTED strictly inside it. The `m01` floor was NOT irreducible at s6: reading the SAME cosine numeral
one term deeper (split@7) at a deeper exp index (`38`) sharpens it a further `≈ 250×`.

DECLS (16 foundations-only, #print axioms ⊆ {propext, Classical.choice, Quot.sound}):
`partialCos_two_ninths_seven`, `pB_lo_s7`/`pB_hi_s7`, `wB_lo_s7`/`wB_hi_s7`, `rB_lo_s7`/`rB_hi_s7`,
`m01_lo_s7`/`m01_hi_s7`, `cutLog_lo_s7`/`cutLog_hi_s7` (the deliverable), the W8 teeth
`cutLog_s7_strict`/`cutLog_s7_width_lt`/`cutLog_s7_nested`/`m01_s7_strict_nested`, and the welded
capstone `lm01GridS7_landing`. C574 bites `1 = 574`.

⚠ HONEST GRADE: THEOREM-route, CERTIFIED (certified two-sided rational brackets of closed reals of the
derived `Cut`, no error bar). A SHARPER INSTRUMENT for the second-ranked `L_m01` residual; wiring it
into `recomposedEndpointDressed` is a downstream re-assembly (handed forward; runs after the parallel κ
/ edge-log builds so the composed endpoint is not touched concurrently).

⚠ NO-FIT: every endpoint chosen by where the derived series' bracket is CERTIFIABLE at the chosen index
(split `7` / exp index `38`), NEVER by where any measured value sits; no empirical number in any proof.
Words-removable: every theorem is a statement about certified rational brackets of a derived closed real
of `Cut`.

ONE-SUCCESSOR RAIL: parallel child of the D7 dressed campaign; hands forward a SELECTION ticket for the
next narrowing rung / re-assembly (owner: NO generic SELECT before D8 banks — the successor stays on the
D7 narrowing rail: wire s7 `L_m01` + the parallel κ ext7 + edge logs into `recomposedEndpointDressed`).
