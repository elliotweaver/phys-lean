# PREREG — D7 DEPTH-2 COMPOSED: compose the banked depth-2 running content into the census-conserving
#   descent (kernel-forced value) → occupancy proven not-a-term → re-attest truly-closed → D7 restate
## (t_3a070796, OWNER R2v2; grove-manager gate-enforcement + restart 2026-07-18 supersede "narrow 136.307")

## RESUME FINDING
The inherited task framing ("narrow 136.307") is SUPERSEDED by grove-manager's two comments: the
D7AugmentedAttestation (bf29483) ledger is INCOMPLETE — (a) the banked depth-2 running content
(LowTailFoldForcedWeight 3643441) is neither composed nor proven zero; (b) the above-floor occupancy
enters only as a BOUND (≤κ·9·cutLog R ~1e-11), which cannot support ≥15 digits. Prior WIP
(d7-rung2-reestablish) was a narrowing of the OLD 136.307 object — NOT reused (it polishes an
incomplete sum). This node executes the owner's ordered plan (1)-(3)+status, decomposing (4)-(6) forward.

## THE COMPOSITION (theory-native, FORCED, verified mpmath+Fraction)
recomposedEndpoint (banked, PerChannelRecomb) = 7179/54 + leptonTailScreen + freezeoutStaircaseSummand,
normal form 2425/18 + κ·(2·L_m01 + (7/3)L_mb + (11/3)L_b0 − (5/3)L_b2), κ=(1/cutPi)/3 ≈136.307 (<137).
The lepton tail sits at LEADING order (coeff 2). The banked DEPTH-2 running content
(LowTailFoldForcedWeight.foldForcedLowTailScreen) dresses the census-conserving LOW (lepton) tail by
the fold-forced (3/2) = 1/(1−depthWeight), depthWeight=1/3 (D3, mean Born self-overlap over the return
surface — FORCED, band-independent, per-band variation a fit the fold KILLS). Composing it:
  recomposedEndpointRun := 7179/54 + bandVaryingScreen (withWeight depthWeight (leptonTail leptonCensus))
                             + freezeoutStaircaseSummand
     = 7179/54 + (3/2)·leptonTailScreen + freezeoutStaircaseSummand      (foldForcedLowTailScreen+telescope)
     = 2425/18 + κ·(3·L_m01 + (7/3)L_mb + (11/3)L_b0 − (5/3)L_b2)         (coeff 2→3 on L_m01)
     = recomposedEndpoint + (1/2)·leptonTailScreen                        (= recomposedEndpoint + κ·L_m01).
Kernel-forced value ≈ 137.17236  — ABOVE measured 1/α(0)≈137.036 (removable prose). Disagreement
REDUCED from ~0.73 (below) to ~0.14 (above); the derivation NEVER bends. NO FIT: only the banked
(3/2) is composed; the freeze-out (threshold/edge content, zero second-depth weight like the high
way-point bands, highBands_preserved) is NOT dressed — no banked theorem for it; inventing it = a fit.

## THE OCCUPANCY (owner concern 2 — the ≥15-digit-blocking bound, resolved theory-native, NO Jacobi wall)
The above-floor Born occupancy r₈(n), n≥2 (HadronicLevelWeights, route-not-yet-found: needs Jacobi's
8-square thm, not in Mathlib) entered D7AugmentedAttestation ONLY as a BOUND ≤κ·9·cutLog R. That bound
is ~1e-11 — cannot support ≥15 digits (owner correct). RESOLUTION (the gate's "proven zero"
alternative, NOT the Jacobi wall): the determined endpoint recomposedEndpointRun = 2425/18 + κ·(...)
is OCCUPANCY-INDEPENDENT — its closed form contains NO occupancy term; the freeze-out contribution is
the fold-forced CENSUS-weighted determined form; the occupancy is a DISTINCT count
(occupancy_ne_census_total, 16≠9) that appears in NO screening/freeze-out definition. So the occupancy
adds ZERO additive bulk to the determined endpoint: the ≤κ·9·cutLog R bound was never a limit on the
ENDPOINT's precision (a refinement not a term). INCLUDED-proven-not-a-term (zero) → the determined
endpoint IS narrowable to ≥15 digits. (This is the density-of-states the field measures as resonance
enhancement; the fold COUNTS via census+edges, needs it as no separate term.)

## THE THEOREMS (production; every TYPE mentions banked derived Cut objects)
(A) foldForced_lepton_dressed : bandVaryingScreen (withWeight depthWeight (leptonTail leptonCensus))
      = 3/2 * leptonTailScreen  (foldForcedLowTailScreen + leptonTailScreen_telescope) — the FORCED (3/2).
(B) recomposedEndpointRun (def) := 7179/54 + bandVaryingScreen (withWeight depthWeight (leptonTail
      leptonCensus)) + freezeoutStaircaseSummand.
(C) recomposedEndpointRun_closed : = 7179/54 + 3/2*leptonTailScreen + freezeoutStaircaseSummand (via A).
(D) recomposedEndpointRun_from_recomposed : = recomposedEndpoint + 1/2*leptonTailScreen (the depth-2
      increment; W8 the increment is >0, = κ·L_m01 — the second-depth self-overlap, real not vacuous).
(E) recomposedEndpointRun_normalform : = 2425/18 + κ·(3·L_m01 + (7/3)L_mb + (11/3)L_b0 − (5/3)L_b2).
(F) endpoint_run_core (W9 abstract core; opaque k,L,Lmb,Lb0,Lb2; coeff 3 on L; transcendentals OUT):
      2425/18 + k·(3L + ...) ∈ [1371723607/1e7, 1371723611/1e7].
(G) recomposedEndpointRun_restated : recomposedEndpointRun ∈ [1371723607/1e7, 1371723611/1e7]
      = [137.1723607, 137.1723611] (~9 sig digits) via F + banked sharp brackets (Rung1) + kappa_bracket.
(H) recomposedEndpointRun_predicts_above_137 : 137 < recomposedEndpointRun (kernel-forced; ABOVE
      measured; disagreement-as-prediction, licensed). W8: also < 138 (genuinely bracketed, two-sided).
(I) endpoint_run_occupancy_independent : the determined endpoint closed form (E) references no
      occupancy; occupancy_ne_census_total (16≠9) — the freeze-out is census-determined, occupancy a
      distinct count → zero additive bulk (INCLUDED-proven-not-a-term, replacing the ≤κ·9·cutLog R bound).
(J) augmented_composition_complete_run : re-attestation — the banked augmented ledger PLUS depth-2
      running INCLUDED (composed summand) PLUS above-floor occupancy INCLUDED-proven-independent ⇒
      ledger TRULY CLOSED, endpoint a DETERMINED occupancy-independent closed real narrowable to ≥15 digits.
(K) capstone (welded, non-hollow).

## HONEST GRADE
THEOREM-EXACT NOW: (A)-(K) exact identities/inequalities/certified rational brackets of Cut/ℚ/ℕ,
foundations-only. The depth-2 running is COMPOSED (kernel-forced 137.172); the occupancy is
proven-not-a-term; the endpoint is a DETERMINED occupancy-independent closed real. ROUTE-NOT-YET-FOUND
(NAMED, handed to ONE directed narrowing successor, NEVER asserted/fitted): the ⛔ OWNER ≥15-SIG-DIGIT
FLOOR — sharpen L_m01/L_mb/L_b0/L_b2/κ to <1e-14 via deeper exp N + deeper grids (same banked
ContinuumBracket instrument, no new axiom, no fit) → D8. The measured 137.036 stays removable prose.

## W9 COST BUDGET / KILL
(A)-(E) are re-exports/ring over banked exacts (~near-zero). (F) is the abstract core: same shape as
banked endpoint_core_s1 (coeff 2→3), measured tractable, maxHeartbeats 2000000. (G) assembles F +
banked sharp brackets (Rung1 cutLog_*_sharp) + kappa_bracket. KILL: any obligation >90s ⇒ decompose
(should not fire — mirrors the banked Rung1 proofs). Transcendentals OUT of the arithmetic (W9).

## G1–G8 SEED HARD GUARDS
G1 the (3/2), coeff 3, brackets ALL fall out of banked foldForcedLowTailScreen/telescope/sharp
brackets/kappa_bracket; nothing premised. G2 no 137.036/PDG in any statement or proof; 137.172 & 137.036
removable prose; 137< is a certified consequence; explicit NO-FIT (only banked content composed; freeze-out
NOT dressed). G3 exact/certified; the ≥15-digit floor NAMED+handed. G4 every input a banked derived
object over derived ℝ Cut. G5 words-removable (delete coupling/running/census/occupancy → pure Cut
arithmetic). G6 #print axioms ⊆ {propext,Classical.choice,Quot.sound}; derived Cut ground; Mathlib
machinery only; no floats/native_decide. G7 ONE LAW — composed the BANKED forced content (did not
invent freeze-out running), resolved occupancy via proven-not-a-term (NOT the Jacobi wall, NOT a
weakened bound), stated the kernel-forced 137.172 AS IS. G8 teeth: increment >0 real; bracket two-sided;
137< genuine; occupancy≠census genuine.

## FORK GUARD / ONE-SUCCESSOR RAIL
Board clean (no children). Exactly ONE forward successor: a DIRECTED narrowing rung toward the
≥15-digit floor over recomposedEndpointRun (the now-depth-2-composed determined endpoint) → D8.
NO generic SELECT before D8 (owner rail). NOT D8 (≥15-digit floor not reached).
