# N159 PREREG — THE OCTONION-BLOCK 2-PLANE ROTATION GENERATOR IS JoinedIdQvC (non-vacuously)

FROZEN before compute. KILL classifies a fight as INSTRUMENT failure (W9), not theory.

## THE NODE (specified against what the chain ACTUALLY produced)

N158 reduced the genuine TIME-FIXING compact factor's `JoinedIdQvC` to the `JoinedIdQvC` of its
x-fixing SO(8) octonion-block stabilizer (`joinedIdQvC_of_timeFixing_evCOrth_peel`). That residual
factor fixes both time AND the x-axis, acting purely on the 8-dim octonion block 𝕆.

FRONT (i), MEASURED. The lift is ALREADY BANKED: `octBlockEndC R : Module.End Cut STVC` (N129,
`LorentzContinuumGenerationBoostRotationSplit`) maps `(t,x,v) ↦ (t,x,R v)`, FIXES the (t,x) plane
(`octBlockEndC_fixes_mTimeUnit`/`octBlockEndC_fixes_mSpaceUnit` N130), and is a `BvC`-isometry when
R is a `gFormC`-isometry (`octBlockEndC_isBvIsomLin` N129). `BvC_self : BvC p p = QvC p` (N51) ⇒ a
`BvC`-isometry is a `QvC`-isometry. So the residual is ONLY: produce the octonion-block ROTATION
generators and show their lift is `JoinedIdQvC` (in the identity path-component, non-vacuously).

## THE DELIVERABLE (the genuinely-new front, base + word; the full exhaustion childed)

The elementary generator of the octonion-block SO(8) stabilizer is a 2-plane rotation `octPlaneRot
e f c s` of `span{e,f} ⊆ 𝕆` (orthonormal e,f; circle point c²+s²=1), identity on the complement:
  R v = v + ((c-1)·gFormC v e − s·gFormC v f)•e + (s·gFormC v e + (c-1)·gFormC v f)•f
DERIVED from: e↦c·e+s·f, f↦−s·e+c·f, w⊥{e,f}↦w (the standard 2-plane rotation, written
coordinate-free via the Born form gFormC). This is the octonion-block analogue of N113's `axisRotLin`
(which rotates span{x,e}); octPlaneRot rotates span{e,f} ⊆ 𝕆, touching NEITHER t NOR x.

Targets, into the NON-VACUOUS `JoinedIdQvC` (N157):
  T1 `octPlaneRot` def + `octPlaneRot_apply` + `octPlaneRot_one_zero` (c=1,s=0 ⇒ id).
  T2 `octPlaneRot_isGFormCIsom` — gFormC-isometry for orthonormal e,f + circle point (completing the
     square; Born positivity is the trunk). [W9 RISK — measure first; decompose into bilinear
     sub-lemmas if the monolith fights.]
  T3 `octBlockEndC (octPlaneRot e f c s)` is a `QvC`-isometry fixing t,x (via banked lift).
  T4 `octPlaneRotPath e f m := octBlockEndC (octPlaneRot e f (cayleyC m)(cayleyS m))` — continuous in
     endOpC (cayley rationals + octonion smul/add continuity), starts at 1 (`octPlaneRot_one_zero`),
     reaches `octBlockEndC (octPlaneRot e f c s)` (`cayley_surj`).
  T5 ★★ `joinedIdQvC_octBlockEndC_octPlaneRot` — THE CORE: the lifted generator is `JoinedIdQvC`
     (the cayley path is a preconnected set OF ISOMETRIES joining 1 to it). NON-VACUOUS.
  T6 word: a finite product of lifted generators is `JoinedIdQvC` (`joinedIdQvC_mul`/`_listProd`).
  T7 ★ W8: a CONCRETE lifted quarter-turn (`octPlaneRot e₂ je2 0 1`, orthonormal banked pair)
     genuinely MOVES an octonion axis (reads 1 on the moved slot) AND is `JoinedIdQvC`.

The full SO(8) exhaustion (every x-fixing octonion-block gFormC-isometry rotation = a finite product
of octPlaneRot generators, via N124 `gFormC_isom_mem_closure` reflection word + even-pair=rotation)
is the genuine HEAVY remainder Mathlib lacks over the derived Cut — CHILDED N160 as the forward node.

## GO / NO-GO
GO: T1–T7 compile foundations-only {propext,Classical.choice,Quot.sound}; costume C191 bites a WRONG
octonion-block-rotation/lift/cayley-path/JoinedIdQvC read with a false numeric DISTINCT from the
banked battery (use 1=159); phrased in JoinedIdQvC (NOT vacuous JoinedIdC); gate D0–D6 green; STANDARD
(unbroken/complete/words-removable) applied; N160 childed onto the chain tail.
NO-GO → DECOMPOSE: bank whatever subset compiles clean (e.g. T1–T3 the generator + lift), child the
path/JoinedIdQvC remainder. Honest W1 block if it resists after measure + reframe.

## KILL (W9 instrument budget)
- KILL any single tactic at 60s / obvious memory blowup. If `octPlaneRot_isGFormCIsom`'s closing
  `ring`/`nlinarith` exceeds KILL on the smallest sub-obligation → DECOMPOSE into named bilinear
  identities (`gFormC_add_two_axes_self` first), each banked separately (W9.3). NEVER inflate
  maxHeartbeats; NEVER re-run a measured-dead monolith.
- Probe budget ~25 turns; FINALIZE (commit + gate-bg + child + complete) with turns to spare (W3).
- THE ONE LAW (W5): if octPlaneRot fights after reframe, fall back to the banked-isometry
  `genTwoPlaneLin` (N103, isometry ALREADY proved) + child the path — do NOT grind.

## ANTI-DRIFT
- NON-VACUOUS target only: `JoinedIdQvC` (N157), NEVER the vacuous `JoinedIdC` (joinedIdC_everything).
- NO posited topology/connectedness/Lorentz/Pin/Spin/gauge/G₂/SO(8)/compact group, NO Mathlib ℝ/ℂ
  as content (field + path parameter are BOTH the derived Cut), NO bridge.
- Physics-words-removable: delete rotation/Lorentz/SO(8)/octonion/Givens/isometry/… → the statement
  stands as pure math about octBlockEndC + octPlaneRot + the derived Cut + endOpC + JoinedIdQvC.
