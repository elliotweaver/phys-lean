| M4 | arc-M MASSES M4 (docs/SEED_MASSES.md §M4; directed successor of M3/ConfinementScaleNumeral; owner-binding board order D→M→C→GR; SELECT + anti-bullshit gate run FIRST, fresh, confirmed M4 theory-native) — THE SCALE-TOWER NUMERALS: the full dimensionful skeleton (M, v, Λ and every derived band-edge ratio) as certified two-sided rational brackets against the M1 peg, one pipeline, the banked way-points free | Phys/Algebra/ScaleTowerNumerals.lean | THE FULL DIMENSIONFUL SKELETON EVALUATED AS CERTIFIED NUMERALS. THEOREM-EXACT (all 14 audited decls exact rational inequalities/identities of the derived Cut, foundations-only ⊆ {propext,Classical.choice,Quot.sound}). ── ★ THE THEORY-NATIVE HEART / ONE CAUSE (every constant is FORCED; the whole tower is ONE geometric ladder in cutExp(π/3)): all three edge scales and all three band ratios are POWERS OF THE ONE SMALL-ARG EXPONENTIAL cutExp(cutPi/3) — octEdgeScale = M/M = 1 (cascade stop), ewEdgeScale = v/M = cutExp(−28π/3) (M2), confEdgeScale = Λ/M = cutExp(−12π) (M3); M/v = topBandRatio = cutExp(28π/3) = cutExp(cutPi/3)^28 (the NEW numeral), v/Λ = confBandRatio = cutExp(8π/3) = cutExp(cutPi/3)^8 (M3), M/Λ = confinementScaleRatio⁻¹ = cutExp(12π) = cutExp(cutPi/3)^36 (M3). The exponents 28,8,36 are the ONE 84π grammar budget quotiented by derived dimensions, and they ADD (28+8=36) — so the three scales are ONE geometric ladder, NOT three independent inputs with a fine-tuning problem; the standard hierarchy problem DISSOLVES (the hierarchy is the exponent size). This node AIMS arc-D's D1 instrument at the last un-evaluated edge ratio M/v and welds the full skeleton. ── FRONT (1, THE M/v RECIPROCAL-POWER FORM): topBandRatio_eq_pow28 (★ M/v = topBandRatio = cutExp(cutPi/3)^28, via topBandRatio_eq + cutExp_natMul — mirrors M3's confBandRatio_eq_pow8). ── FRONT (2, ★ THE M/v SCALE NUMERAL): topBandRatio_bracket (★★★ M/v = topBandRatio ∈ [54220·1e8, 54226·1e8] ≈ 5.422×10¹² — the ratio of the octonion ceiling to the electroweak edge, the reciprocal of the M2 ewClosureRatio, pinned via the 28th power of the M2 small-arg exp bracket, kernel norm_num; the standing prediction for the octonion/gravitational-ceiling-to-electroweak ratio, GeV comparison removable prose), topBandRatio_pos (genuine positive derived scale ratio, via topBandRatio_gt_one). ── FRONT (3, ★ THE COMPOSITION TOOTH — the ONE-ladder consistency): tower_ratio_composition (★★★ topBandRatio · confBandRatio = confinementScaleRatio⁻¹, i.e. (M/v)·(v/Λ) = M/Λ — the EXACT exponent-addition identity cutExp(cutPi/3)^28 · cutExp(cutPi/3)^8 = cutExp(cutPi/3)^36 via pow_add, 28+8=36; the M/v and v/Λ numerals compose EXACTLY into the M/Λ numeral, no new input, no fine-tuning DOF between the rungs). ── FRONT (4, THE THREE EDGES PEGGED): pegged_octEdge (★ p.human octEdgeScale = p.val — the tower top rung IS the peg, via peg_fixes_Moct), pegged_ewEdge (★ p.human ewEdgeScale ∈ [p.val·184405/1e18, p.val·184445/1e18] — v against the peg), pegged_confEdge (★ p.human confEdgeScale ∈ [p.val·42408/1e21, p.val·42414/1e21] — Λ against the peg); ALL under the SINGLE M1 peg, no second dimensionful input (THE ONE-PEG LAW). ── FRONT (5, THE THREE BAND-RATIO NUMERALS COLLECTED): bandRatio_numerals (★★ M/v ∈ [54220·1e8,54226·1e8] new, v/Λ ∈ [4347,4350] M3, M/Λ ∈ [23577·1e12,23581·1e12] M3 — all pinned by powers of the SAME cutExp(cutPi/3)). ── FRONT (6, ★★★ THE FIVE BANKED WAY-POINTS RE-EMERGE FREE): waypoints_free (invAlphaZero [] = 42, bandScreen chargeTraceDepth topBandRatio = 448/27, bandScreen chargeTraceDepth confBandRatio = 128/27, invAlphaZero [(chargeTraceDepth,topBandRatio)] = 1582/27, invAlphaZero derivedBandList = 190/3 — all EXACT banked rationals over the SAME derived edges this module brackets; the dimensionful skeleton and the dimensionless coupling readings share ONE derived tower, way-points fall out free). ── TEETH (W8): tower_edges_ordered (★ the three edge scales strictly ordered Λ < v < M via edges_ordered — a genuine derived hierarchy), bandRatio_ordered_from_brackets (★ the band ratios strictly ordered M/v > v/Λ > 1 RE-DERIVED FROM THE BRACKETS — the M/v bracket lies entirely above the v/Λ bracket, 54220·1e8 > 4350, which lies above 1), tower_widths_positive (each lo<hi strictly — genuine two-sided brackets, not collapsed), composition_nontrivial (★ the composed M/Λ > 23576·1e12 — the exponent-addition ladder genuinely climbs 28+8=36 rungs, not a degenerate 1). CAPSTONE scaleTowerNumerals_landing welds all six blocks (the M/v numeral, the composition tooth, the three edges pegged, the three band-ratio numerals, the five way-points free, the edge+ratio ordering), statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7 sharpened): NO measured scale (M_Planck/v/Λ_QCD/GeV) in ANY statement or proof — the comparison is removable-prose ONLY; the beyond-measurement digits are the STANDING PREDICTION; no bracket depth/route/sub-band selected by where a measured value sits; THE ONE-PEG LAW: cited (M1 UnitPeg), never duplicated, no second dimensionful input. ── FOUNDATIONS: 14 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}; imports ConfinementScaleNumeral + BandEdgeList + LeptonMassNumerals + UnitPeg + ContinuumBracket + ContinuumTrigEval (transitively the banked M-scale tower + topBandRatio + confBandRatio + confinementScaleRatio + the derived edges + the way-points + D1 + M2/M3 brackets + the M1 peg); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.exp/Real.*, NO kernel-compiled decisions, NO floats, NO sorry, NO posited axiom; NEVER Phys/OneAxiom/*. PHYSICS-WORDS-REMOVABLE: delete mass/scale/hierarchy/electroweak/confinement/edge/peg — pure certified two-sided rational inequalities lo<x<hi for the derived Cut number topBandRatio, the exact exponent-addition identity topBandRatio·confBandRatio = confinementScaleRatio⁻¹, an ordering of derived positives, non-degenerate widths, and the banked exact rationals 42,448/27,128/27,1582/27,190/3; no physics word does logical work; a physicist reads the scale-tower numerals OUT of the theorems. NOT FREE-FLOATING: every production TYPE cites the banked derived objects over Cut (topBandRatio, confBandRatio, confinementScaleRatio, octEdgeScale, ewEdgeScale, confEdgeScale, chargeTraceDepth, bandScreen, invAlphaZero, derivedBandList, cutExp, cutPi, the M1 UnitPeg). GATE-FAST D0–D6: pending (background). C550 bites 1=550 (climbFlag: the certified numerals force a strictly-climbing non-degenerate geometric ladder 1 < confBandRatio < topBandRatio via bandRatio_ordered_from_brackets — not a hollow/collapsed bracket). Build clean (lake build Phys.Algebra.ScaleTowerNumerals EXIT 0, Built 6.0s, LIGHT — pow_le_pow_left₀/norm_num/linarith/pow_add on Cut atoms + banked topBandRatio_eq/cutExp_natMul/M2/M3 bracket cites, no brute, no native_decide, no heartbeat raise; crux 28th-power norm_num reciprocal measured 8.1s before build, no W9 risk). Scratch probes workbench/M4-scale-tower-numeral/{PREREG.md,design.py,verify.py,Probe.lean} (all verified; ALL ASSERTIONS PASSED). CARRIED NOTE (deferred-D7, arc-D standing debt, NOT a runway): D's ≥15-sig-digit AS-1/α(0) restate resumes AFTER the absolute-scale skeleton is complete. Directed ONE successor toward M5 (SEED_MASSES §M5 — THE HEAVY QUARK SECTOR: certified brackets for the heavy-sector masses from the banked Z3 texture ratios + D's ratio numerals × the derived anchors). NO generic SELECT (rail: a directed child REPLACES the generic SELECT). NOT a runway (autonomous theory-native evaluation work remains — arc M is a buildable evaluation/completion arc). |
