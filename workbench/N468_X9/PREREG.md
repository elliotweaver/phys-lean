# N468 / X9 — PREREG (anti-bullshit gate, in writing)

## Target (seed §X9)
THE ABSOLUTE QUARK MASSES + ANY OTHER FORCED CONSTANT ALONG THE WAY — placing the quark/hadron
mass sector on the SAME finite coupling band tower assembled in X6/X8, honestly graded per piece.

## NON-DUPLICATION CHECK (the X8 lesson — run FIRST, in writing)
grep survey of the banked quark/hadron sector:
- BANKED (CITE, do NOT re-derive):
  - ConfinementHadronicMass (arc I): `confinementScaleRatio = cutExp(−12π)` (Λ/M, colour n=7 rung),
    `confinementScaleRatio < ewClosureRatio`, `confinementScale_lt_vev` (Λ<v), the 84π budget lock
    `rungExponent(7)·7 = criticalPhaseNumerator` (`confinementRung_budget_lock`), the Higgs-independent
    positive mass floor `confinement_mass_floor_pos`, `hadronic_mass_origin` (capstone).
  - HadronicSpectrum (X4/N463): `gapSpectrum = ℕ`, floor 1, `quarkonium_level_spacing`.
  - ChiralGenerationChargeTrace (N317): `Tr(Q²)=16/3` (the census content used by X2/X6).
  - ScaleTowerLadder / ScaleTowerNumeratorLock (arc B): `ewClosureRatio = cutExp(−28π/3)` (v/M),
    `numerator_triple_lock : (12π)·7 = (28π/3)·9 = (3π)·28`.
  - FineStructureZeroMomentum (X6): `topBandRatio = ewClosureRatio⁻¹ = cutExp(28π/3)`,
    `invAlphaZero_topBand_value = 1582/27`.
  - LeptonMassTower (X8): `leptonBundle = massScale · topBandRatio`, `massScale = ewClosureRatio·leptonBundle`.
  - GenerationMassRatiosNumeric (N340): the LEPTON Koide mass ratios (σ₁=6M²). <-- LEPTON, not quark.
- NOT BANKED (⟹ route-NOT-YET-FOUND, never fabricate):
  - Any quark-sector Yukawa mass ratio / texture (m_u:m_d:m_s, heavy c/b/t). N340 is the LEPTON sector.
  - The explicit closed ratio Λ/v (confinement-scale ÷ electroweak-scale) — grep empty ⟹ GENUINELY NEW.

VERDICT: the genuinely NEW, forced, theory-native X9 content is placing the confinement/hadron mass
SCALE on the SAME finite coupling band tower (the confinement-sector analogue of X8's lepton weld):
`Λ/v = e^(−8π/3)` exact, obtained by multiplying Λ/M by the SHARED topBandRatio edge that X6's
coupling and X8's lepton mass read. This is nowhere in the corpus (grep empty). The individual quark
Yukawa masses need a not-yet-banked quark texture ⟹ route-NOT-YET-FOUND, enumerated for X10, NEVER asserted/fitted.

## THE NEW LANDING (each a real theorem on banked derived objects over Cut)
- `confBundle := confinementScaleRatio * topBandRatio`  (Λ/M · M/v = Λ/v — confinement scale × the
   SHARED coupling-tower edge). ★ the analogue of X8 `leptonBundle = massScale · topBandRatio`.
- `confBundle_eq : confBundle = cutExp(−(8·cutPi/3))`  (★ NEW exact closed ratio Λ/v; the cutExp
   arithmetic: cutExp(−12π)·cutExp(28π/3)=cutExp(−12π+28π/3)=cutExp(−8π/3)).
- `confBundle_eq_confToEw : confBundle = confinementScaleRatio / ewClosureRatio`  (the ratio reading).
- `confinement_eq_edge_bundle : confinementScaleRatio = ewClosureRatio * confBundle`  (★ the
   confinement scale = electroweak band edge × the confinement suppression bundle — the SAME edge the
   coupling/leptons read; analogue of X8 `massScale_eq_edge_bundle`).
- `confBundle_lt_one : confBundle < 1`  (★ strict suppression below the electroweak edge; −8π/3 < 0).
- `confBundle_pos : 0 < confBundle`.
- `rung_gap_eq : 12·cutPi − 28·cutPi/3 = 8·cutPi/3`  (the confinement−electroweak rung gap = the
   colour/space quotient-difference of the ONE 84π budget: 84π·(1/7 − 1/9) = 8π/3).
- `confBundle_lt_leptonBundle`? — OPTIONAL, only if clean; skip if it drags.
- `coupling_reading : invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582/27` (cite X6 — the SAME
   topBandRatio edge the confinement scale factors through: the ONE-CAUSE weld, one finite tower).
- teeth (W8): `confBundle_ne_one`, `confBundle_ne_zero`, `confBundle_ne_ewClosureRatio` (Λ/v ≠ v/M),
   `rung_gap_ne_zero`.
- capstone `quarkHadronTower_landing` (welded ∧ of the new joints + cited banked scale/ordering + shared-edge reading).

## HONEST GRADE (per piece — the point)
- theorem-exact NOW: confBundle = e^(−8π/3), the edge factoring, strict suppression, the rung-gap
  84π-budget identity, the shared-edge coupling reading.
- anchored-on-derived-scale: absolute confinement scale Λ = M·e^(−12π) (cited ConfinementHadronicMass);
  hadron mass = confinement energy (cited `hadronic_mass_origin`, candidate/mechanism prose).
- route-NOT-YET-FOUND (enumerate for X10, NEVER assert/fit): individual current-quark Yukawa mass
  ratios/absolutes (no banked quark texture — only LEPTON Koide N340), the physical MeV numeral of any
  scale (M_oct→MeV unit choice). These are NAMED residuals for the X10 attestation, not fabricated.

## DISCIPLINE
- UNBROKEN: every object cited from banked ConfinementHadronicMass/X6/arc-B/X4/derived-ℝ trunk; only
  new machinery = cutExp_add/cutExp_mul_neg/field_simp/ring on the DERIVED Cut objects.
- COMPLETE: #print axioms ⊆ {propext,Classical.choice,Quot.sound}; NO sorry/axiom/native_decide/foldRetention.
  IMPORT GUARD: import ConfinementHadronicMass + FineStructureZeroMomentum (X6), NEVER MassGapOne.
- WORDS-REMOVABLE: delete confinement/hadron/quark/coupling/electroweak → cutExp(−12π)·cutExp(28π/3)
  = cutExp(−8π/3), the factoring, 0<·<1 — pure real analysis on Cut.
- ℝ-vigilance: banked derived Cut/cutExp/cutPi only. NO Mathlib Real/Complex as content.
- G2 no fit: PDG quark masses / Λ_QCD / measured α REMOVABLE PROSE ONLY. G3 exact. G7 one law. G8 teeth.
- FREE-FLOATING check: every TYPE mentions banked confinementScaleRatio/topBandRatio/ewClosureRatio/
  invAlphaZero/chargeTraceDepth over Cut.

## COST BUDGET / KILL
- Small module (~15 lemmas, cutExp arithmetic + field_simp/ring). No monolithic brute. Compile ~1 module.
- KILL: if any single lemma's cutExp arithmetic doesn't close in a bounded probe, MEASURE the smallest
  sub-obligation and decompose; do NOT inflate maxHeartbeats. No blind rerun after any timeout.

## SUCCESSOR (one-successor rail, anti-Lovelock arc)
ONE directed successor toward X10 (the MANDATORY airtight validation capstone). The individual-quark-mass
residual is a NAMED residual X10 enumerates in its NO-RESIDUAL attestation — NOT a separate fork.
