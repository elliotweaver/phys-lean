# PREREG — Compose s=1/8 into D2c, clear the ComposedNumeralObstruction (t_5e44d867)

## FRONT (theory-native, run the anti-bullshit gate)
- The `ComposedNumeralObstruction` proved: while `s` FREE over `(0, isoDepth=2)`, the composed
  endpoint `dressedInvAlphaEMJoin` floats by >1 (the two admissible points s=1/2,3/2 already spread
  >1; ~14.6 across the full range) → a ≥15-digit two-sided bracket is MATHEMATICALLY IMPOSSIBLE.
  The obstruction NAMED its own resolution: "the numeral cannot be restated until s is DERIVED."
- The s-campaign (IsospinAntiScreeningCrossNorm → AntiScreeningTemperingCeiling →
  CasimirProjectionSelectsS) DERIVED `s := projectedS = (funCas/adjCas)·depthWeight = (3/8)·(1/3) =
  1/8` as a theorem of BANKED objects (Casimir eigenvalues N277/N279 with uniqueness teeth, depth
  weight D3). Foundations-only, C536 bites.
- THIS node composes that banked derived POINT into the D2c/D2d closed form and proves the
  s-driven >1 spread COLLAPSES TO A POINT (0), clearing the s-side of the obstruction.

## ANTI-BULLSHIT GATE (honest)
- Trace: fold → cascade-stop O ℚ (N2c) → su(2)_L Casimir dichotomy (N277 adj −8 / N279 fund −3) →
  casimirRatio 3/8 (banked) → × depthWeight 1/3 (D3 return-surface geometry) = projectedS 1/8
  (banked) → substitute into banked dressedInvAlphaEMJoin closed form → s-spread formula
  (composed_s_difference, banked) at (projectedS, projectedS) = 0. EVERY arrow banked/forced.
- Standard version: none. Standard physics FITS s to hit 137.036 (fit value ~1.90, G2-forbidden).
  We do NOT fit; s is the fold-forced 1/8, nowhere near 1.90. The composed endpoint landing in the
  137.03x window is CONSISTENCY prose only, removable, NO measured digit in any proof.
- Words-removable: delete coupling/screening/isospin/anti-screening/join/depth → pure real
  arithmetic about the banked `dressedInvAlphaEMJoin`/`projectedS`/`composed_s_difference` over Cut.
- Not free-floating: every TYPE mentions the banked derived objects (dressedInvAlphaEMJoin,
  projectedS, composed_s_difference, chargeTraceDepth, depthWeight, isoDepth, completeBandList).
- Load-bearing tooth: projectedS = 1/8 is LOAD-BEARING — with a FREE s the spread is >1 again;
  pinning to the DERIVED 1/8 is what collapses it. The s-coefficient chargeTraceDepth − projectedS
  = 125/24 ≠ 0 (the screening base survives; pinning did not kill the running).

## THEOREMS (production Phys/Algebra/ComposedNumeralSPinned.lean)
1. pinnedS_eq : projectedS = 1/8 (re-export, orientation).
2. composed_at_pinnedS_closed : dressedInvAlphaEMJoin w projectedS j bands =
     112 + ((chargeTraceDepth − projectedS)·totalScreeningWith 1 bands)/(1−w) + joinContent w j.
     ★ THE COMPOSITION of s=1/8 into D2c/D2d.
3. pinnedS_coefficient : chargeTraceDepth − projectedS = 125/24 (derived nonzero const, no free s).
4. pinnedS_single_admissible : 0 < projectedS ∧ projectedS < isoDepth (re-export projectedS_admissible).
5. s_spread_at_pinnedS_zero : dressedInvAlphaEMJoin w projectedS j bands −
     dressedInvAlphaEMJoin w projectedS j bands = 0  (via banked composed_s_difference; the >1
     formula collapses to 0 at the single pinned point). ★ THE CLEARING.
6. free_s_gap_exceeds_pinned_gap : pinned-s gap (0) < free-s two-point gap (>1) at depthWeight —
     the CONTRAST proving the collapse is genuine (0 strictly below the banked >1 obstruction).
7. Capstone composedNumeralSPinned_landing (welded, non-hollow).

## COST BUDGET / KILL
All proofs are rw + ring/norm_num/linarith on rational closed forms over Cut (cheap; no decide over
boxes, no monolithic ring on expanded coordinates). KILL: if any single obligation exceeds ~60s or
the leaf build exceeds ~5min → measure/decompose (W9). Expect << that.

## SUCCESSOR (ONE directed child, fork-guard)
DERIVE the join splitting ratio j (now the dominant remaining bounded residual) from the banked
transmutation-join scale (arc-B ewClosureRatio/topBandRatio, N277 quasi-degeneracy) → then
freeze-out edges rb,rc,rl → re-narrow → D7 restate AS 1/α(0) at ≥15 sig digits → D8 → hand M1.
NO generic SELECT until M1 handed. max_retries=25.

## RESULT (banked)
Phys/Algebra/ComposedNumeralSPinned.lean — built EXIT 0 (17s), 9 decls foundations-only.
The composition composed_at_pinnedS_closed + the clearing pinned_s_gap_eq_zero /
obstruction_cleared_s_side (spread 0 < free-s gap >1). C537 bites 1=537 (tied to
obstruction_cleared_s_side). Coefficient chargeTraceDepth − projectedS = 125/24 ≠ 0 (running survives).
NO fit (s=1/8 forced, fit ~1.90 excluded). Successor: DERIVE join ratio j → freeze-out → re-narrow → D7 → D8 → M1.
