# PREREG — N467 / ARC-X X8: THE ABSOLUTE LEPTON MASSES = THE COUPLING BAND TOWER'S ELECTROWEAK EDGE × THE FLAVOR RATIOS

## The anti-bullshit gate, run IN WRITING (SOUL law)

### 1. The trace fold → … → node, each arrow forced
- fold → self-blindness → complex unit → Cayley–Dickson doubling → TERMINATES at octonions (42 imaginary
  self-overlap channels, N313/N314). The gather resolves all 42 at the octonion ceiling → 1/α* = 42.
- The finite band tower descends from that ceiling (X6, `invAlphaZero`). Its **electroweak band edge** is the
  banked exact rung `v/M = ewClosureRatio = cutExp(−28·cutPi/3)` (arc B / N332); the coupling tower's top-band
  ratio is its inverse `topBandRatio = ewClosureRatio⁻¹ = cutExp(28·cutPi/3)` (X6 `topBandRatio_eq`), and the
  coupling READING at that edge is the exact `invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582/27` (X6
  `invAlphaZero_topBand_value`).
- The absolute mass scale `massScale = absScaleChainDerived` (N417 / arc-O O2, discharge of arc M) is a
  DETERMINED parameter-free derived Cut, and it FACTORS through the SAME electroweak rung:
  `massScale = ewClosureRatio · (cutCos foldPhase · s1BornProb · 1 · cutCos(2/3))` (banked
  `absScaleChainDerived_eq_anchor_mul`). So the mass scale sits at the SAME band edge the coupling reads.
- The banked flavor eigenvalue ratios `mass_k = b_k²` at the derived cycle phase δ_B=2/9 (N340) give the
  spectrum; `absMass k = massScale · mass_k` (N427). Every arrow forced back to the fold; nothing posited.

### 2. The standard version, and what the theory DOES to it
- STANDARD: the Standard Model needs the electroweak scale `v` PLUS a free Yukawa per fermion (~a dozen free
  numbers), and treats the fine-structure coupling and the fermion masses as UNRELATED inputs measured
  separately at different scales.
- THEORY: BOTH the coupling `1/α` and the absolute lepton mass scale are READINGS of the ONE finite tower from
  the octonion ceiling, at the SAME electroweak band edge `ewClosureRatio = topBandRatio⁻¹`. The coupling reads
  the SCREENING across the top band (= 1582/27); the masses read the SCALE of that band edge (ewClosureRatio)
  times the derived suppression bundle times the forced flavor ratios. This is the ONE-CAUSE weld: one tower,
  two observables, one edge. NOT two separate calculations.

### 3. What is genuinely NEW over the banked modules (the non-duplication check — THE crux)
- N427 (`AbsoluteMassSpectrum`, arc P) ALREADY banks `absMass k = massScale·mass_k`, ordering, ratio
  inheritance, scale-invariant Koide, the breaking direction. **I will NOT re-derive any of it — I IMPORT and
  CITE it.** Re-deriving it in arc-X clothing would be a bullshit duplicate; the gate would pass it but it
  would fork the strand.
- X6 (`FineStructureZeroMomentum`) banks `topBandRatio = ewClosureRatio⁻¹`, `topBandRatio_eq`,
  `invAlphaZero_topBand_value = 1582/27`. N427 NEVER mentions `topBandRatio` (it is arc-X).
- **THE NEW CONTENT: `leptonBundle := massScale · topBandRatio` — the mass scale measured in the COUPLING
  tower's electroweak-edge units — and its identity `leptonBundle = (cutCos foldPhase · s1BornProb · cutCos(2/3))`
  (the derived suppression factor), plus the WELD capstone tying `invAlphaZero_topBand_value = 1582/27`
  (coupling reading) and `massScale · topBandRatio = leptonBundle` (mass reading) to the ONE `topBandRatio`.**
  This connects the arc-P mass sector to the arc-X coupling band tower — a joint that exists nowhere in the
  banked corpus.

### 4. Delete the physics words — does a real reason survive?
Delete mass/lepton/coupling/electroweak/fine-structure/Yukawa: over Cut, with `S = absScaleChainDerived` and
`t = ewClosureRatio⁻¹`, one has `S·t = (cutCos foldPhase · (2/3) · cutCos(2/3))`, `0 < S·t < 1` (since
`S < ewClosureRatio` and `ewClosureRatio · t = 1`), `S = ewClosureRatio · (S·t)`, the products `S·mass_k` carry
the banked N340 brackets, and `invAlphaZero [(d, t)] = 1582/27`. Pure real analysis on Cut. No name load-bearing.

### 5. The standard temptation, named — is my target genuinely different / genuinely re-derived?
- TEMPTATION: re-package N427's `absolute_mass_spectrum` as "X8" (it already computes `massScale·mass_k`), or
  numerically bracket `massScale` (a ~1e-13 M_oct-unit number that is only meaningful up to the M_oct→MeV unit
  = removable prose). Both would be bullshit: the first duplicates a banked node; the second chases a numeral
  the task explicitly forbids ("do NOT chase residuals by adding structure"; the GeV value needs the M_oct unit
  = removable prose, route-NOT-YET-FOUND).
- MY TARGET IS DIFFERENT: it does NOT recompute the spectrum; it WELDS the banked spectrum's scale to the
  banked coupling tower's electroweak band edge via `topBandRatio` — the arc-X unifying frame. The deliverable
  is "the ratio structure (theorem-exact) × the derived-anchor expression (= the electroweak band edge ×
  the derived bundle)," exactly what the task names as the deliverable when the absolute GeV numeral is
  route-NOT-YET-FOUND.

## HONEST GRADE (per piece)
- THEOREM-EXACT (no error bar): the weld `massScale · topBandRatio = leptonBundle`; the bundle identity
  `leptonBundle = cutCos foldPhase · s1BornProb · cutCos(2/3)`; the suppression `0 < leptonBundle < 1`; the
  tower-frame form `absMass k = ewClosureRatio · leptonBundle · mass_k`; the coupling reading at the same edge
  `invAlphaZero_topBand_value = 1582/27` (cited); the flavor ratios `abs_ratio_12/20`, ordering, Koide (cited).
- ANCHORED-ON-DERIVED-SCALE (theorem-grade determined Cut object, parameter-free): the absolute masses
  `absMass k = massScale · mass_k` themselves as derived reals, `0 < massScale < ewClosureRatio < 1`.
- ROUTE-NOT-YET-FOUND / REMOVABLE PROSE (enumerated for X10, NEVER asserted/fitted): the physical GeV numeral
  of each mass (needs the M_oct → MeV unit conversion, a human unit choice); the e/μ/τ labelling of the three
  ordered eigenvalues. The measured PDG masses (0.511, 105.7, 1776.9 MeV) are removable prose only.

## GUARDS
- G1 no asserted values (leptonBundle FALLS OUT of the banked factoring + cancellation; 1582/27 cited from X6).
- G2 no empirical number in any proof (PDG masses / GeV / M_oct = removable prose; explicit NO-FIT).
- G3 EXACT where exact (the weld, bundle identity, suppression, tower-frame form — exact facts of Cut).
- G4 DERIVED not posited (massScale N417, topBandRatio X6, mass_k N340; nothing free).
- G5 physics-words-removable (see §4).
- G6 foundations-only ⊆ {propext, Classical.choice, Quot.sound}; ℝ-vigilance (banked derived Cut/cutCos/cutExp/
  cutPi; NO Mathlib Real/Complex as content). IMPORT GUARD: imports X6 (FineStructureZeroMomentum, no MassGapOne)
  + N427 (AbsoluteMassSpectrum, audited foundations-only, no foldRetention) — verify #print axioms NO foldRetention.
- G8 teeth: leptonBundle ≠ 1 (the suppression genuinely bites — masses below the ew edge, not at it);
  massScale ≠ ewClosureRatio (cited); the spectrum nondegenerate (cited).

## FREE-FLOATING check
Every TYPE mentions the banked derived objects: `massScale`/`absMass k` (N427), `topBandRatio`/`ewClosureRatio`/
`invAlphaZero`/`chargeTraceDepth` (X6), `mass_k`/`abs_ratio_*` (N340/N427), `foldPhase`/`s1BornProb` (N416/N408),
`cutCos`/`cutExp`/`cutPi`/`Cut` (derived ℝ). `leptonBundle` is literally `massScale · topBandRatio`, not a
generic real. Grounded on the tower.

## WELD (non-hollow capstone)
`leptonMassTower_landing` welds: (coupling) `invAlphaZero [(chargeTraceDepth, topBandRatio)] = 1582/27` ∧
(mass) `massScale · topBandRatio = leptonBundle` ∧ (bundle identity) `leptonBundle = …` ∧ (suppression)
`0 < leptonBundle < 1` ∧ (tower-frame) `absMass k = ewClosureRatio · leptonBundle · mass_k` ∧ (ratios/ordering
cited). Statable from none alone — the coupling reading and the mass reading SHARE `topBandRatio`.

## SUFFICIENCY (W4.5)
Welds the absolute lepton mass spectrum to the coupling band tower's electroweak edge (the arc-X frame), NOT
the absolute quark masses (X9) nor the airtight validation capstone (X10). The GeV numeral is route-NOT-YET-FOUND.

## SUCCESSOR
One directed successor toward X9 (SEED_EXACT_DRESSING.md §X9 — the absolute quark masses + any other constant).
