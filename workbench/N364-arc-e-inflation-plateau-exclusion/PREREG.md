# N364 — arc-E E8 (candidate-class): INFLATION = THE FOLD'S ETERNAL-APPROACH (C6), the plateau class SELECTED by self-blindness (no fixed point), monomial/hilltop EXCLUDED — over the derived ℝ `Cut`

## SELECT (fresh worker, theory-native FIRST — QUIET DRIFT + anti-bullshit gate)

**MEASURED reality (verified against the actual bank, W9 MEASURE-FIRST, NOT the ticket's framing):**
- **NO banked slow-roll / e-fold / spectral-index / tensor structure** anywhere in `Phys/` (grep
  `efold|slowRoll|spectral|inflat|attractor` = empty). E8 is a fresh sector.
- **NO general derivative / chain-rule / integral machinery over `Cut`.** The ONLY banked scalar
  differentiation is `cut_powerseries_deriv` (power series at `0`, `ContinuumScalarDeriv`). There is
  no `HasDerivAt`-style chain rule for `cutExp`, no antiderivative of `cutExp(cφ)`. So the FULL
  slow-roll derivation of `n_s = 1−2/N` and `r = 8/(c²N²)` FROM a potential `V(φ)` (which needs
  `ε = ½(V'/V)²`, `η = V''/V`, and the e-fold integral `N = ∫ V/V′ dφ`) is a genuine multi-node
  calculus build — NOT reachable in one node. **This is a W9 MEASURE result: build the smaller
  forced beat, child the calculus.**
- **BANKED and REUSABLE:** the derived-ℝ exponential `cutExp` (N174) with `cutExp_pos` (Born
  positivity, self-overlap), `cutExp_zero`, `cutExp_strictMono`, `cutExp_le_one_of_nonpos`,
  `cutExp_add`, `cutExp_continuous`. The scale-tower suppression `cutExp(−c/g²)` (N320
  `DimensionalTransmutation`). All over the derived `Cut`.

**Theory-native hook (fold-causal — the C6 / self-blindness primitive, THE MOAT):** the trunk
primitive **C6** says *best coherence is NOT a fixed point — eternal approach*; **self-blindness**
says the look-back has *no nonzero fixed point*. Physically, inflation IS a period of eternal
approach: the field rolls toward, but never reaches, the coherence value. Realized on the derived
continuum, the potential that embodies "approach a coherence asymptote you never reach, with no
fixed point" is the exponential-flattening plateau
      `plateauPot c φ = (1 − cutExp(−c·φ))²`
built on the fold's OWN Born-positive self-overlap exponential `cutExp` (the SAME `cutExp` that
suppresses the scale tower, N320). This class is SELECTED — not chosen to fit data — because among
the named candidate classes it is the UNIQUE one that is **bounded above (a coherence asymptote)
AND never attains its bound (no fixed point = self-blindness / C6)**:
- **plateau** `(1−cutExp(−cφ))²`: on `φ ≥ 0` it is `< 1` for ALL `φ` and `= 1` for NO `φ` — bounded,
  never attained → SURVIVES (the eternal approach, no fixed point).
- **monomial** `φ^p`: UNBOUNDED above (no coherence asymptote to approach at all) → EXCLUDED.
- **hilltop** `1 − (φ/μ)^p`: bounded above by `1` AND ATTAINS `1` at `φ = 0` (a stationary maximum =
  a fixed point) → EXCLUDED (the fold forbids a nonzero fixed point).

That structural dichotomy is the fold's selection principle and is provable as pure `cutExp`/`Cut`
statements (THEOREM-GRADE). The observable relations `n_s = 1−2/N` (plateau), `n_s = 1−(p+2)/(2N)`
(monomial), `r_plateau = 8/(c²N²)`, `r_mono = 4p/N` are the class-universal slow-roll relations;
their derivation FROM the potential is CHILDED (needs the banked calculus). What is DERIVED here at
theorem-grade is the EXCLUSION arithmetic on those relations (over `Cut`): the monomial's tensor
ratio parametrically EXCEEDS the plateau's (`r_mono/r_plateau = p·c²·N/2 → ∞`, "monomials die on
r"), and monomials are never bluer in `n_s` (`n_s_mono ≤ n_s_plateau` for `p ≥ 2`).

**Anti-bullshit gate (ANTI-BULLSHIT LAW, in writing):**
- **STANDARD version:** inflation from a POSITED inflaton potential `V(φ)` tuned by hand to match the
  measured `n_s ≈ 0.965` and small `r`; the plateau chosen BECAUSE it fits the data (small tensors,
  red-but-near-1 tilt); `ε, η` computed from `V` by ordinary calculus.
- **What the THEORY does:** the plateau class is SELECTED by the fold's OWN structure — the eternal
  approach (**C6**: coherence is not a fixed point) realized as the Born-positive self-overlap
  `cutExp` (**self-blindness**: no nonzero fixed point). It is the UNIQUE named class that is
  bounded-yet-never-attained; monomials have no asymptote (unbounded), hilltops have an attained
  fixed-point maximum. Nothing is tuned to data; the class falls out of the trunk primitives, and
  the `cutExp` is the very object that suppresses the scale tower (N320) — one exponential, two
  physical roles (the moat cross-tie).
- **Words-removable (G5):** delete "inflation / inflaton / plateau / spectral / tensor / potential /
  cosmology / de-Sitter": the theorems stand as pure statements over `cutExp`/`Cut` — the map
  `φ ↦ (1 − cutExp(−cφ))²` is `≥ 0`, is `< 1` for all `φ ≥ 0`, is `= 1` for no `φ ≥ 0`, is strictly
  increasing on `φ > 0`; `φ ↦ φ^p` is unbounded above; `φ ↦ 1 − (φ/μ)^p` attains its supremum `1` at
  `φ = 0`; and the rational functions `4p/N` vs `8/(c²N²)` and `1−(p+2)/(2N)` vs `1−2/N` satisfy the
  stated `Cut`-arithmetic inequalities. No physics word does any logical work.
- **Temptations REJECTED:** (a) plug `N = 57` / assert `n_s = 0.965` / assert `r ~ 10⁻⁹` — REJECTED
  (G2, `N`,`c`,`p` stay SYMBOLIC; the measured numbers are removable prose only); (b) claim
  `n_s = 1−2/N` is DERIVED from `plateauPot` here — REJECTED (no banked calculus; flagged
  CANDIDATE, the slow-roll derivation CHILDED/named in the handoff); (c) import Mathlib `Real`/trig
  as content — REJECTED (built on the banked `cutExp`/`Cut`); (d) claim the plateau is THE unique
  inflaton potential over ALL potentials — REJECTED (it is the unique class under the two structural
  criteria among the THREE NAMED classes; flagged NAMED-IDENTIFICATION, not a global uniqueness
  theorem); (e) assert a hilltop `n_s` formula I cannot derive — REJECTED (hilltop excluded
  STRUCTURALLY by its attained fixed-point maximum, the theory-native reason; its `n_s`-defect is
  flagged standard-cosmology prose).

**Free-floating guard (FREE-FLOATING trap):** the node's content object is `plateauPot c φ`, built
LITERALLY from the banked `cutExp` over the derived `Cut` — the TYPE mentions `cutExp`/`Cut`. The
structural-selection theorems (bounded / never-attained / unbounded / attains-max) are the
theorem-grade MOAT and are genuinely ABOUT the `cutExp` plateau. The exclusion arithmetic is
supporting/candidate; it is stated over `Cut` and bundled in the capstone WITH the `cutExp`
selection so the node is grounded on the derived tower, not a generic ordered field.

## HONEST GRADE (mandatory flags — SEED §E8 "CANDIDATE-class", the plateau class a NAMED IDENTIFICATION)

- **THEOREM-GRADE (genuine theorems on the banked `cutExp`/`Cut`):** `plateauPot_nonneg`,
  `plateauPot_lt_one` (bounded, `φ ≥ 0`), `plateauPot_ne_bound` (NEVER attains the plateau — the
  no-fixed-point / eternal-approach core), `plateauPot_strictMono` (the approach), `monoPot_unbounded`
  (no asymptote), `hilltopPot_attains_max` (the fixed-point maximum), and the exclusion arithmetic
  `rMono_gt_rPlateau` / `rPlateau_eq_rMono_mul_suppression` (the `1/N` tensor suppression) /
  `nsMono_le_nsPlateau`. All foundations-only over the derived `Cut`.
- **MECHANISM/NAMED-IDENTIFICATION-GRADE (flagged prose, NEVER an asserted theorem):** the
  identification "plateau = the fold's C6 eternal-approach realized as `cutExp`", "self-blindness =
  the plateau's no-fixed-point", and "the plateau class is forced-by-exclusion among the named
  classes" — like N362's `min`, N360's WHY-thermal, the T8 mixing caveat.
- **ROUTE-NOT-YET-FOUND (CHILDED / NAMED in the handoff, NEVER asserted here):** the FULL slow-roll
  derivation of the observable relations `n_s = 1−2/N` and `r = 8/(c²N²)` FROM `plateauPot` via
  `ε = ½(V′/V)²`, `η = V″/V`, and the e-fold integral `N = ∫ V/V′ dφ` — requires banked derived-ℝ
  calculus (a `cutExp` chain rule + the antiderivative of `cutExp(cφ)`) that does NOT exist yet. The
  relations `nsPlateau`/`rPlateau`/`nsMono`/`rMono` are DEFINED here as the class-universal
  relations (candidate-grade); only the EXCLUSION comparisons between them are proved.

## MEASURE (W9)

Probe `probe.lean` will type-check the core: `plateauPot` on `cutExp`, `plateauPot_lt_one`/
`plateauPot_ne_bound` (via `cutExp_pos` + `cutExp_le_one_of_nonpos`), `monoPot_unbounded`,
`hilltopPot_attains_max`, and the four arithmetic exclusion lemmas. Expect no instrument-cost risk
(no heavy `tsum`, no `decide` on big terms — `nlinarith`/`ring`/order lemmas on `Cut`). KILL budget:
if any single obligation needs `> 30s` user time, DECOMPOSE. `Cut` is `LinearOrder + Field +
IsStrictOrderedRing`. All decl names collision-checked via `grep -rn` before production.

## GUARDS (SEED_GR relaxes NONE)

- G1 no asserted value (the class SELECTED structurally; `n_s`,`r` relations SYMBOLIC in `N`,`c`,`p`;
  no numeric value premised).
- G2 no empirical number in any proof (measured `n_s≈0.965`, `r~10⁻⁹`, the e-fold count `N` as a
  measured quantity = REMOVABLE PROSE / costume only; `N`,`c`,`p` stay SYMBOLIC).
- G3 scales-vs-ratios (`n_s`, `r` dimensionless/derived functions of `N`; no dimensionful anchor
  needed here).
- G4 derived not posited (the structural selection PROVED on `cutExp`; the class relations DEFINED
  and flagged candidate, their slow-roll derivation CHILDED).
- G5 physics-words-removable (delete inflation/inflaton/plateau/slow-roll/spectral/tensor/cosmology
  → pure `cutExp`/`Cut` statements).
- G6 foundations-only ⊆ {propext, Classical.choice, Quot.sound}; no sorry/axiom/native-decide/
  maxHeartbeats-raise. Built on the banked derived `cutExp`/`Cut`; NO Mathlib `Real` as content.
- W8 non-vacuity: `plateauPot_selects_not_vacuous` — a WRONG "the plateau attains its bound (has a
  fixed point like the hilltop)" mis-reading FAILS; costume C389 pins it.

## ONE SUCCESSOR (decision at finalize)

Directed child toward **E9** (dark matter = sterile ν_R, candidate-class: the right-handed neutrino
with `y₁=0` stable + dark as ONE theorem with `m₁=0`; per SEED §E9 when E9 lands the chain's next
course is ALREADY SET toward `SEED_SM_COMPLETION` F1). E8 LANDS as the structural beat; the FULL
slow-roll calculus derivation of `n_s=1−2/N`/`r=8/(c²N²)` is NAMED in the handoff as
route-not-yet-found (reachable once the derived-ℝ `cutExp` calculus is banked), NOT silently
dropped. Fork check: exactly 1 non-terminal successor on the chain tail.
