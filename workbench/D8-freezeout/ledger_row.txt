
| D8-freezeout | arc-D DIGITS D8-freezeout (docs/SEED_DIGITS.md §D2/§D7; directed successor of D7/TheNumeral; THE FREEZE-OUT COMPOSITION — compose the D2 freeze-out summand the D7 gate named as missing, and establish its QUANTITATIVE CHARACTER as a theorem) | Phys/Algebra/FreezeoutComposition.lean | THE FREEZE-OUT COMPOSITION — compose the freeze-out summand the D7 composition-completeness gate named as missing, and PROVE its quantitative character. THEOREM-route for the composition structure + the bound; ROUTE-NOT-YET-FOUND (never asserted/fitted) for the edge ratios rb,rc,rl (the quark mass ℝ-descent) and the dominant missing summand of the genuine 1/α(0), both handed to the ONE directed route-finder successor. ── ★ THE MARQUEE FINDING (corrects the D7 conjecture that the freeze-out is the DOMINANT contribution to the running): the three DERIVED plateau censuses total EXACTLY 9 (freezeout_total_census: 11/3+10/3+2 = 9, from D2 censusBottomBand_val/censusCharmBand_val/censusLightBand_val), so the freeze-out summand = κ·(11/3)·cutLog rb + κ·(10/3)·cutLog rc + κ·2·cutLog rl (freezeout_closed, unfolding bandScreen + the plateau censuses on D2 freezeoutTail_screening), which for screening edge ratios all ≤ R (1<R) is BOUNDED by κ·9·cutLog R (freezeout_bounded, via the banked cutLog_mono + mul_le_mul) — a BOUNDED, LOGARITHMICALLY-SMALL census refinement, NOT the dominant summand D7 conjectured (for any bounded edge span it is a small increment). THE ONE CAUSE: the D2 census conservation (the freeze-out re-counts a FINITE resolved content, total 9) IS the ceiling on its screening. ── FRONT (2, THE COMPOSED ENDPOINT): composed_split (invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth) = invAlphaZero(completeBandList chargeTraceDepth) + totalScreening(freezeoutTail rb rc rl), = D7 attestation_tower_split at chargeTraceDepth), composed_bounded (★ invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth) ≤ 32019546505738/471238898037 + κ·9·cutLog R for edges ≤ R — the composed endpoint exceeds the certified D7 partial-tower numeral by AT MOST a bounded log term, chaining theNumeral_bracket.2 with freezeout_bounded), composed_gt_partial (invAlphaZero(completeBandList chargeTraceDepth) < invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth), re-exports D7 attestation_partial — the freeze-out genuinely screens). ── TEETH (W8): freezeout_bound_pos (0 < κ·9·cutLog R for 1<R — the bound is a real ceiling, not vacuous ≤0, via kappaLeading_pos + cutLog_pos + positivity), freezeout_total_census_ne_high (9 ≠ 16/3 — the freeze-out is a genuinely distinct, bounded layer, not a duplicate of the high bands), freezeout_summand_pos (0 < totalScreening(freezeoutTail rb rc rl) for 1<rb,rc,rl, cites D2 freezeoutTail_screens). CAPSTONE freezeoutComposition_landing welds all seven blocks, statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7): NO measured value steers ANY statement — the census 9, the closed form, and the bound FALL OUT of the D2 plateau censuses + cutLog_mono; no edge/coupling value is premised; the measured 1/α(0)≈137.036, the PDG quark masses, and Λ_QCD appear NOWHERE in any proof (removable prose only); no bound/edge chosen BECAUSE of where a measured value sits; truncating at measured precision FORBIDDEN. ── ROUTE-NOT-YET-FOUND (the ℝ-descent, NEVER asserted/fitted; verified route-not-yet-found across FOUR banked sources — Z3 QuarkTexture "the NUMERICAL quark mass ratios are ROUTE-NOT-YET-FOUND", X9 QuarkHadronTower "there is NO banked quark texture — only the LEPTON Koide N340", ZArcValidationCapstone quarkMassRatioNumerals = routeNotYetFound, SEED_MASSES arc M CONSUMES D's quark-ratio numerals): the freeze-out edge ratios rb,rc,rl as closed reals of Cut (the quark mass ℝ-descent), AND the dominant missing structural summand of the genuine 1/α(0) — the composed value with a bounded freeze-out lifts the D7 partial numeral by at most κ·9·cutLog R, so a further (larger) summand is required and its structure is not yet identified. Both handed to the ONE directed route-finder successor. The composition therefore remains PARTIAL; a disagreement with the measured value from a still-partial composition is an incomplete sum, NOT a prediction. ── FOUNDATIONS: 10 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}, NO foldRetention; import guard TheNumeral only (transitively the whole banked chain + D2 SubBandCompletion, Z6 EndpointAssembly, X6 FineStructure*, AssembledBracket cutLog_mono); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.cos/Real.exp, NO native_decide, NO floats (scratch numerics for aiming only, workbench/D8-freezeout/aim_fz*.py). PHYSICS-WORDS-REMOVABLE: delete coupling/running/screening/band/edge/freeze-out/census/quark/hadronic/fine-structure — pure statements that censusBottomBand+censusCharmBand+censusLightBand=9 over Cut, that totalScreening(freezeoutTail rb rc rl)=kappaLeading·(11/3)·cutLog rb+kappaLeading·(10/3)·cutLog rc+kappaLeading·2·cutLog rl, that for 1<R and edges ≤R this is ≤kappaLeading·9·cutLog R, that invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth)=invAlphaZero(completeBandList chargeTraceDepth)+totalScreening(freezeoutTail rb rc rl), and that the composed value is ≤32019546505738/471238898037+kappaLeading·9·cutLog R. GATE-FAST D0–D6 PASSED. C522 bites 1=522 (tied to freezeout_total_census_ne_high). Directed successor of D7; hands ONE directed route-finder child toward the quark-sector freeze-out edge ratios rb,rc,rl as closed reals of Cut (the quark mass ℝ-descent via the Z3 forced-cubic Fano-line phase / X4 ℕ-spectrum anchor) — the missing summand of the genuine 1/α(0). |
