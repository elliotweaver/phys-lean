# N321 — SEEDED GAUGE ROUTE B2: the transmutation coefficient c (★ BRIDGE) — PREREG + DECOMPOSE decision

FRESH worker, run 623. Task t_7ade765d. Directed successor of N320 (B1, the structural form
`transmutationRatio c g2 := cutExp(−(c/g2))` banked, c an HONEST PARAMETER). B2 = derive the VALUE
of `c` from the cascade β-function structure ({28, 9, 4π}).

This is a ★ BRIDGE — the highest bar. The PREREG records the FULL attack, the precise single gap,
the anti-bullshit refusal to assert, and the W3 DECOMPOSE decision (child N322, the β-coefficient
structural object from which c falls out).

════════════════════════════════════════════════════════════════════════════
## S1 — ARC SYNTHESIS (where B2 sits)

SEED 2 (gauge→scale→cosmology). ARC A COMPLETE (A1 φ/‖φ‖²=42 · A2 channel tower · A3 ★ BRIDGE 42α=1
· A4 α*=1/42 · A5 5/3 · A6 sin²θ_W=3/8 · A7 one-coupling coherence). ARC B OPENED at N320 (B1): the
STRUCTURAL FORM `v/M = cutExp(−(c/g²))` on the banked derived-ℝ `cutExp`, with `g²` grounded to the
banked `alphaStar = couplingWeight = 1/42 ∈ ℚ` (`transmutationRatio_at_alphaStar`). B2 is the ★
BRIDGE: the exponent coefficient `c`. B3 (Newton's G·M²=e^(−6π), gravity's first appearance) is later.

════════════════════════════════════════════════════════════════════════════
## S2 — STATE SURVEY (what is banked around {28, 9, 4π, 42, α*})

Ground state: HEAD 18637d3 (N320 banked, gate-fast D0–D6 GREEN), git tree clean, workbench/N321 was
empty (genuine fresh start, not a resume).

THE FORM (N320, Phys/Algebra/DimensionalTransmutation.lean, REUSE — W2, do NOT re-derive):
  `transmutationRatio c g2 := cutExp (−(c / g2))`   (v/M, the dimensionless suppression, on `Cut`)
  `transmutationScale M c g2 := M · transmutationRatio c g2`
  + all order consequences (lt_one, mono_c, mono_g2, at_alphaStar, inflation_gt).
  ⇒ At the banked coupling g² = α* = 1/42, the exponent is **−c/g² = −42·c**.

WHERE {28, 9} LIVE (banked structural counts — the raw material for c):
  • **28** = `finrank_selfAdj_eq_28` (TowerGatherSymAntisymDecomp) = dim Sym²(ImO), the SYMMETRIC /
    self-adjoint / METRIC-carrying side of `End ℚ ImO = Sym²7(28) ⊕ Λ²7(21=so(7))`; 28 = 1 ⊕ 27
    (trivial ⊕ traceless-symmetric). Also = dim so(8). The ANTISCREENING / bosonic-metric candidate.
  • **21** = so(7) (the skew / gauge channels), `finrank_so7_eq_21`. 49 = 28 + 21 = dim End(ImO).
  • **42** = `channels_O` = `imChannelCount (finrank ℚ ImO)` = n(n−1) = 7·6 = the interaction-channel
    count = **1/α\*** (`inverseAlphaStar_eq_42`). Also = `assoc3NormSq` (‖φ‖², A1/N313).
  • **9** appears three structural ways: `finrank_spaceSub = 9` (SpacetimeSignature, the SPATIAL
    dims of the derived 1+9 spacetime STV = ℝ⊕O); `n_c² = 9` (colour²; the `2·24/9 = 16/3` charge
    trace, N317); the naive ℍ pair count `n² = 3² = 9` (the diagonal-not-dropped alternative).
  • **α\*** = `couplingWeight` = `bornWeight 0 1` = 1/42 ∈ ℚ (N315/N316), the UNIFORM per-channel
    Born weight; `g²/4π` in physics prose (fully REMOVABLE — A4 already de-π'd `g²/4π → 1/42`).

WHAT IS **NOT** BANKED (the genuine gaps):
  • **No derived-ℝ π (`cutPi`) anywhere in `Phys/`.** The `4π` = "the derived Gauss sphere" (confirmed
    by the GR seed E1/E3 and the arc-D seed D1): its construction is arc-D/D1 machinery (`cutSin/cutCos`
    on `Cut`), NOT yet built. BUT the theory's established habit is to **DE-π**: N308 turned the
    geometric `2π/9 → 2/9` (a rational Born count via holonomy=statistics); A4 turned `g²/4π → 1/42`.
    So the theory-native reading is that `c` is a **de-π'd RATIONAL** and the `4π` is REMOVABLE prose,
    NOT a derived-π dependency.
  • **No cascade β-function / screening / antiscreening object exists in `Phys/`.** (grep for
    screen/beta/asymptotic hits only unrelated substrings — GaugeFamilyCoupling etc. are flavor, not
    β-function.) The arc-D seed D3 EXPLICITLY names this as unbuilt: *"If the running needs a
    β-function build, that is a legitimate child node — build it, don't assert."* Arc-D G4: *"the
    running is derived from the cascade β-structure, not fitted."* → the β-structure is a KNOWN
    unbuilt prerequisite.

════════════════════════════════════════════════════════════════════════════
## S3 — THE ATTACK ON c (three convergent framings, all reducing to ONE named gap)

The exponent at the banked coupling is −42·c. Three theory-native framings for c:

  (F1) SM/instanton reading: `c = 2π/b₀`, b₀ the one-loop β-coefficient. Needs b₀ = f(28,9) [unbanked]
       + a π factor [unbanked / de-π'd].
  (F2) De-π'd rational (the theory's own habit, N308/A4): `c = N/42` (or `= geo/b₀`), N a FORCED
       combination of {28, 9}, the 4π REMOVED as prose. Needs the forced combination [unbanked].
  (F3) Self-overlap count (N320's OWN theory-native reading — the exp is the derived ℝ's Born
       self-overlap, NOT an RG loop): `c·42 = a structural Born-count` over the cascade channels — the
       antiscreening (metric, 28) vs screening (matter, 9) channel balance. Needs the forced balance
       [unbanked].

**ALL THREE converge on the SAME single gap:** there is no banked theorem forcing the specific
combination of {28, 9} (and the de-π'd/Gauss 4π) into `c`. That combination — the cascade
β-coefficient as an antiscreening−screening channel balance of the banked structural counts — is the
missing structural object. It is a THEOREM to build (about banked counts), NOT an axiom and NOT an
empirical number ⇒ NOT a CHAIN-RUNWAY (SOUL: route-not-yet-found ≠ new axiom).

════════════════════════════════════════════════════════════════════════════
## S4 — ANTI-BULLSHIT GATE (the ★ BRIDGE bar — the crux is G4)

NAME-THE-TEMPTATION, each REJECTED:
  (a) Assert `c = 2/3` (28/42) or `c = 3/14` (9/42) or `c = 2π/b₀` for a guessed b₀ — because it
      "looks nice" / matches the measured v/M hierarchy. → G4 HIDDEN-BRIDGE. This is *posit-and-check-
      against-data*, the EXACT failure the ★ BRIDGE bar and the predecessor's asserted-2/9 died of.
      REJECTED. No value of c is written down.
  (b) Run a numerical probe to "find" which combination of {28,9,4π} reproduces the observed
      hierarchy. → That probe IS the G4 violation (fitting to data). REJECTED — no fishing probe run.
  (c) Import Mathlib-ℝ π to supply the 4π. → UNEARNED-IMPORT (content). REJECTED — the theory de-π's
      (N308/A4); if a genuine Gauss-π is needed it is the arc-D/D1 derived `cutPi`, a further child.
  (d) Bank a `c := geo/b₀` DEFINITION renaming c into (geo, b₀) without deriving either. → BULLSHIT
      dressing (words-removable-but-vacuous). REJECTED — that derives nothing.

HONESTY: I have genuinely attacked c from three framings; every one reduces to the same unbuilt
structural object (the forced {28,9} β-coefficient balance). Forcing c now is impossible WITHOUT a
posit. The ★ BRIDGE is a real multi-node derivation (the seed's own §GO-IN-WITH-NO-FEAR: "DECOMPOSE
across as many nodes as it takes; the g₂ characterization took ~15"). The first forceable sub-step is
the β-coefficient structural object — which the arc-D seed itself flags as a legitimate unbuilt child.

════════════════════════════════════════════════════════════════════════════
## DECISION — W3 DECOMPOSE (block-as-dependency), NO c asserted

B2's deliverable (c derived) is NOT achieved and cannot be without positing. Per W1 step 3 + W3:
enqueue ONE directed child (N322) = the cascade β-coefficient structural object, from which c falls
out; then this node (B2) is DECOMPOSED onto it. c is NEVER asserted. The ★ BRIDGE is scoped to a
dedicated attack (fresh worker, full budget, unsaturated), exactly as the flavor ★ BRIDGE crux
(N308 holonomy=statistics) was a derived banked identity, not an assertion.

CHILD N322 (the single successor, on the chain tail): derive the cascade β-function coefficient b₀
as the FORCED antiscreening−screening balance of the banked structural channel counts — the metric /
Sym²(ImO) = 28 antiscreening side vs the matter / spatial-colour = 9 screening side — from which the
transmutation exponent numerator `c` falls out (de-π'd, the 4π Gauss-sphere as removable prose per
N308/A4; OR, if a genuine derived-π is needed, name it as a further arc-D/D1 child). Feed the derived
c into the banked N320 `transmutationRatio` to land the concrete suppression. DERIVE the combination
from the banked counts; NEVER assert it, NEVER fish it against data. If the combination itself needs a
further prerequisite, name it and decompose again (recursive W1). max_retries=25, assignee phys-novelty.

RAW MATERIAL for N322 (mapped here so the worker does NOT re-survey): 28 = finrank_selfAdj_eq_28
(TowerGatherSymAntisymDecomp); 21 = so(7); 42 = channels_O = 1/α* = assoc3NormSq; 9 = finrank_spaceSub
/ n_c² / naive-ℍ n²; α* = couplingWeight = 1/42 (N315/N316); the form = N320 transmutationRatio; the
de-π template = N308 (2π/9→2/9 via holonomy=statistics) + A4 (g²/4π→1/42).
