| D7 | arc-D DIGITS D7 (docs/SEED_DIGITS.md §D7; directed successor of D6/WayPointBracket; THE NUMERAL — state the certified two-sided rational bracket for the derived partial-tower endpoint to MORE significant digits than the measured value carries, WITH the composition-completeness gate applied FIRST) | Phys/Algebra/TheNumeral.lean | THE NUMERAL — the certified two-sided rational bracket for invAlphaZero(completeBandList chargeTraceDepth) STATED to 12 significant digits (67.9475880261..., MORE than the measured value carries; the beyond-measurement digits the chain's standing PREDICTION FOR THIS WAY-POINT), WITH the composition-completeness gate applied FIRST — and THE GATE FIRES: the composition is PARTIAL, so the bracket is a WAY-POINT, NOT 1/α(0), and the missing summand is the ONE directed successor. THEOREM-route, CERTIFIED (the numeral + the attestation; the freeze-out edge ratios' ℝ-descent route-not-yet-found, handed onward). ── ★ THE THEORY-NATIVE HEART / ONE CAUSE (the composition-completeness gate is the fold's OWN honesty): the narrowing campaign (D4→D5-next-12) narrowed a WAY-POINT, and the fold KNOWS it is partial because the SAME fold ALSO derived (D2 SubBandCompletion) the light-quark/hadronic FREEZE-OUT sub-bands (freezeoutTail) that sit between the confinement edge and the lepton band — whose strictly-positive screening is the DOMINANT hadronic contribution to the running of 1/α(0), NOT composed into Z6's completeBandList=derivedBandList++leptonTail (the PARTIAL tower, which Z6 itself names a partial sum "squeezed between the octonion ceiling and the full 1/α(0)"). One structure — the D2 freeze-out census re-counting its resolved content — is simultaneously (a) the missing summand of the endpoint, (b) the proof that the 12-digit bracket is a way-point, (c) the named route to the genuine 1/α(0). The gate PREVENTS the credibility-killing move: declaring the 12-digit way-point to be 1/α(0) and then "predicting" a disagreement with the measured value — which would be a FAKE prediction from an INCOMPLETE sum (seed §D7: "A disagreement from a composition WITHOUT the attestation is NOT a prediction — it is an incomplete sum, and the missing summand is the next child"). ── FRONT (1, THE NUMERAL, 12 sig digits): theNumeral_bracket re-exports NarrowedBracket12.invAlphaZero_narrowed12_bracket (12007329939722/176714586765 ≤ invAlphaZero(completeBandList chargeTraceDepth) ≤ 32019546505738/471238898037, width ≈3.50×10⁻¹¹); theNumeral_pinned pins the 12-sig-digit decimal window 679475880261/1e10 < invAlphaZero(completeBandList chargeTraceDepth) < 679475880262/1e10, i.e. 67.9475880261... (MORE digits than measured). ── FRONT (2, THE COMPOSITION-COMPLETENESS ATTESTATION — the composition is PARTIAL, theorem): attestation_tower_split (invAlphaZero(completeFreezeoutList rb rc rl d) = invAlphaZero(completeBandList d) + totalScreening(freezeoutTail rb rc rl) EXACTLY, via D2 freezeout_confWaypoint_free + totalScreening_append + Z6 confWaypoint_in_complete + ring — the numeral is a genuine PARTIAL SUM of the complete tower); attestation_freezeout_nonzero (0 < totalScreening(freezeoutTail rb rc rl) for 1<rb,rc,rl, cites D2 freezeoutTail_screens — the missing summand is NON-ZERO); attestation_partial (invAlphaZero(completeBandList chargeTraceDepth) < invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth) — the 12-digit bracket is STRICTLY BELOW the complete tower, so it is a WAY-POINT, not 1/α(0)). ── TEETH (W8): theNumeral_window_strict (679475880261/1e10<679475880262/1e10 — the numeral window is genuinely two-sided, not a collapsed point), attestation_load_bearing (partial sum ≠ complete tower — the composition-completeness attestation is NOT vacuous; a partial composition is genuinely NOT the endpoint), theNumeral_gt_conf (190/3<invAlphaZero — the numeral genuinely screens, cites NarrowedBracket12.invAlphaZero_narrowed12_gt_waypoint). CAPSTONE theNumeral_landing welds all three blocks, statable from no single conjunct alone (non-hollow). ── NO-FIT (G2/G7): NO measured value steers ANY bracket — the numeral endpoints are the banked D5-next-12 instrument-certified rationals; the attestation is an EXACT identity + strict positivity of Cut; the derived partial value lands ≈67.9476, and — critically — the gate REFUSES to call it 1/α(0), so there is NO "prediction of 137−67.9476"; the measured 1/α(0)≈137.036 appears NOWHERE in any proof (removable prose only); truncating at measured precision FORBIDDEN (the point is MORE digits). ── ROUTE-NOT-YET-FOUND (the ℝ-descent, NEVER asserted/fitted): the missing summand — the freeze-out sub-band edge ratios rb,rc,rl as closed reals of Cut (forced ratios of derived edge scales, the absolute scale cancelling as in the lepton tail); composing them into completeFreezeoutList and re-evaluating/re-narrowing the genuine 1/α(0) is the ONE directed successor (the D2 composition-completeness gate surfacing). Physically the freeze-out tail is the DOMINANT hadronic contribution to the running; the gap from 67.9476 toward the measured constant is the missing summand, NOT a theory failure. ── FOUNDATIONS: 9 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}, NO foldRetention; import guard WayPointBracket + SubBandCompletion only (transitively the whole banked chain); NO Mathlib ℝ/ℂ as content, NO Real.pi/Real.cos/Real.exp, NO native_decide, NO floats. PHYSICS-WORDS-REMOVABLE: delete coupling/fine-structure/screening/band/edge/lepton/quark/hadronic/freeze-out/census — pure statements that a derived closed real of Cut (invAlphaZero(completeBandList(16/3))) is pinned between explicit rationals and in the decimal window (679475880261/1e10,679475880262/1e10), that invAlphaZero(completeFreezeoutList rb rc rl d)=invAlphaZero(completeBandList d)+totalScreening(freezeoutTail rb rc rl), that 0<totalScreening(freezeoutTail rb rc rl) for 1<rb,rc,rl, and hence invAlphaZero(completeBandList chargeTraceDepth)<invAlphaZero(completeFreezeoutList rb rc rl chargeTraceDepth). GATE-FAST D0–D6 PASSED. C521 bites 1=521 (tied to attestation_load_bearing/attestation_partial). Directed successor of D6; hands ONE directed child toward the FREEZE-OUT COMPOSITION (derive the freeze-out sub-band edge ratios rb,rc,rl as closed reals of Cut, compose into completeFreezeoutList, re-evaluate/re-narrow the genuine 1/α(0)) — the D2 composition-completeness gate surfacing; D8 (the airtight capstone, then M1) comes AFTER the composition is completed and the genuine 1/α(0) numeral is stated. |
