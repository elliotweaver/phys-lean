# N153 PREREG — the COMPACT-factor (ninth-direction rotation) path joined to the identity in `endOpC`

## Frozen target (chosen front, priority (i) sub-obligation)
With N152 banking the BOOST/cone part (`specPathOp_joins_id_sqrt`: positive operator `p^{1/2}`
path-joined to `id` through the cone, continuous in `endOpC`), the remaining open structure for
the FULL path-connectedness of the `SO⁺(1,9)` identity component is the COMPACT factor — the
ninth-direction 2-plane rotation `planeRotLin c s` (N105, circle `c²+s²=1`) must be path-joined
to the identity `planeRotLin 1 0 = id` (banked `planeRotLin_one`) through a CONTINUOUS path of
GENUINE ISOMETRIES in the operator topology `endOpC` (N152).

## THE ONE LAW reframe (the trunk-native dissolution — NO posited angle)
A rotation path "by angle t·θ" is the standard transcendental trap (needs `cos`/`sin`/`exp`,
none of which exist continuously over the derived `Cut`). RETURN TO THE TRUNK: the circle has a
RATIONAL parametrization (Cayley / stereographic projection):
    c(m) = (1 − m²)/(1 + m²),   s(m) = 2m/(1 + m²)
- ALWAYS on the circle: c(m)² + s(m)² = ((1−m²)² + 4m²)/(1+m²)² = (1+m²)²/(1+m²)² = 1 (field id).
- Denominator never vanishes: 1 + m² ≥ 1 > 0 in the ordered field `Cut`.
- m = 0 ↦ (c,s) = (1,0) ↦ planeRotLin 1 0 = id (banked). So the path STARTS at the identity.
- RATIONAL in m ⟹ continuous over `Cut` (NO `cos`/`sin`/`exp`) — the same dissolution that turned
  the boost flow into AFFINE arithmetic (N96/N152) turns the rotation flow into RATIONAL arithmetic.
- Surjective onto the circle minus (−1,0): for any (c,s) with c²+s²=1, c≠−1, take m = s/(1+c).

So the rotation factor is path-joined to the identity WITHOUT a posited angle — exactly parallel
to N152 dissolving the boost factor WITHOUT a posited exp.

## GO / NO-GO (measured BEFORE production)
- GO if: (M1) `Continuous (fun m : Cut => (1−m²)/(1+m²))` [division continuity over Cut, denom ≠ 0];
  (M2) `cayleyC m ^ 2 + cayleyS m ^ 2 = 1` by field arithmetic; (M3) each per-coordinate evaluation
  `m ↦ planeRotLin (cayleyC m) (cayleyS m) p` continuous ⟹ `Continuous (rotPath)` in endOpC via
  `endOpC_continuous_iff`; (M4) `rotPath 0 = id` (via `planeRotLin_one`).
- NO-GO / DECOMPOSE if: division continuity over `Cut` is NOT available (would be a W1 sub-node:
  build `HasContinuousInv₀ Cut` / continuity of `x↦x⁻¹` at nonzero pts over the order topology).

## KILL (instrument-failure budget, W9)
- Any single obligation that does not compile within ~90s in a bounded probe ⟹ INSTRUMENT failure,
  NOT theory failure. MEASURE the smallest sub-obligation; decompose; never inflate maxHeartbeats,
  never throw a monolithic brute. If division continuity resists ⟹ child a `HasContinuousInv₀ Cut`
  W1 node, bank the field-arithmetic + (if reachable) the continuity-modulo-inv, block.
- If the FULL group path-connectedness (KAK product of boost-path ∘ rotation-path joining id to an
  arbitrary g) does not assemble cheaply this run ⟹ DECOMPOSE: bank the rotation-factor path-join
  (this node), child the KAK-product assembly as the next node.

## Costume (next id C185) — must BITE
A WRONG rotation-path / Cayley / circle / path-join claim reducing to a FALSE numeric DISTINCT
from the banked battery (… C184 1=152): use 1=153 or a distinct value. Reuse banked objects
(a wrong cayleyC/cayleyS read, a wrong planeRotLin read, a wrong rotPath continuity/endpoint).

## Physics-words-removable
Delete rotation/Lorentz/compact/SO(9)/Spin(9)/isometry/path-connected/connectedness/angle/boost
→ pure math: a rational map `Cut → Cut × Cut` landing on the unit circle, continuous, sending 0 to
(1,0); the induced family of linear endomorphisms of `STVC` is continuous in the induced operator
topology and its image on an interval is preconnected and contains the identity.
