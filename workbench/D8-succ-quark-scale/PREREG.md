# PREREG — D8-succ-quark-scale: THE UP/DOWN RELATIVE SECTOR SCALE = THE FOLD PHASE'S BORN WEIGHT

Node t_d519eff5. DIRECTED SUCCESSOR of FreezeoutEdgeCrossSectorReduction (parent reduced the freeze-out
edges rb,rc,rl to ONE structural object: the up/down/confinement RELATIVE SECTOR-SCALE assignment
(Mu/Md, Md/Mλ)). This node DERIVES the dominant leg — the up/down relative scale — theory-native.

## THE REPEATED-DEFERRAL DIAGNOSIS (RUNBOOK: name the ceiling, then CLIMB — do NOT circle laterally)

The SAME front has been deferred across FOUR consecutive nodes for the SAME structural reason:
- Z3 `QuarkTexture` — the CROSS-sector observed quark ratios flagged route-not-yet-found.
- X9 `QuarkHadronTower` — records verbatim "there is NO banked QUARK-sector Yukawa texture — only the
  LEPTON Koide N340"; the individual quark scales enumerated as route-not-yet-found.
- D8 `FreezeoutComposition` / `ComposedNumeralObstruction` — rb,rc,rl deferred as bounded residuals.
- Parent — reduced them to ONE object (the relative sector scale) and named it as arc-M's M1 charter.

WHY each deferred (the ceiling, diagnosed): forcing the quark relative scale from the structure present
AT THAT NODE would need a scale-ratio the fold had not yet earned — specifically, a determined
fold-native scale for the up-vs-down separation. That structure IS the **fold phase P over the derived
ℝ**. It is now BANKED: N416 `CascadeFoldPhase` derives `foldPhase = cutPi/8` (the ℍ-level Cayley–Dickson
fold angle, the third rung of the amplitude-halving cascade J²=−1 → π/2 → π/4 → π/8). The ceiling is
CLEARED not by circling but by CLIMBING to the now-banked richer structure and reading the scale off it.

The owner-authorized QT1 route (docs/SEED_OWED_FACES §1b, cited in the banked `PredictionRegistry`)
states the up/down leg EXPLICITLY: `m_b/m_t = (tan²P·cosP·√(2/3))²`, route-COMPLETE (P banked N416,
survival 2/3 banked N308) but NEVER built as a Lean module (`PredictionRegistry` names it a NAMED GAP,
"no quark-texture module exists in Phys/"). This node builds it — the honest completion of a banked
deferral, NOT a new bridge.

## THE THEORY-NATIVE HEART (what the fold's own phase says the up/down scale is)

The banked M2 gear (N408 `ScaleTowerGearDecomposition`) already banked the Born-square decomposition
`mbBornRatio phaseBorn = phaseBorn · (tr Poth/3)` with `tr Poth/3 = 2/3` the self-blind survival (N308)
and `phaseBorn = (tan²P·cosP)²` DEFERRED "because it needs the fold phase P, route-not-yet-found".
P is now banked. Fill the deferred slot:

  foldPhaseBorn := (tan²P·cosP)² = (sin²P/cosP)² = sin⁴P/cos²P   (P = foldPhase = cutPi/8)

Using the banked half-angle amplitudes `cutCos(cutPi/8)² = (1+q)/2` (N416 `cutCos_eighth_sq`) and the
Born-circle Pythagorean `cutSin(cutPi/8)² = (1−q)/2`, with `q := cutCos(cutPi/4)`, `q² = 1/2` (N416
`cutCos_quarter_sq`), `0 < q` (`cutCos_quarter_pos`), the fold-phase Born factor CLOSES:

  foldPhaseBorn = (1−q)² / (2(1+q))                    [EXACT derived Cut value]
  mbRatioDerived := foldPhaseBorn · (2/3) = (1−q)² / (3(1+q))   [the QT1 law (b), P filled]

★ THE RELATIVE-SCALE READING (the marquee — this IS the parent's residual). In the parent's own
sector tower `sectorMass M k = M²·(bShape k)² = M²·bMass k` (banked `sectorMass_eq`), the cross-sector
HEAVIEST-to-HEAVIEST ratio has its shape CANCEL entirely (index 0 both sides):

  sectorMass Md 0 / sectorMass Mu 0 = (Md²·bMass 0)/(Mu²·bMass 0) = (Md/Mu)²   (`crossSector_heaviest_ratio`)

— a PURE relative-sector-scale ratio, no shape content. So the up/down relative sector scale is:

  (Md/Mu)² = mbRatioDerived = (1−q)²/(3(1+q))     ⟹  Md/Mu = √mbRatioDerived,  a DETERMINED derived value.

The up/down mass hierarchy is NOT a free Yukawa dial — it is the fold phase's OWN ℍ-level Born weight
(the SAME fold angle P that sets CP/mixing, banked CKMTwoSectorPhase). The parent's dominant residual
is PINNED from route-not-yet-found to a determined derived Cut value.

Numerically (removable prose, NEVER a claim, NEVER fit): mbRatioDerived ≈ 0.01675; PDG m_b/m_t ≈ 0.024
(scale/scheme dependent). Leading-order / candidate consistency only.

## GRADE (honest, per piece — SOUL "route COMPLETE vs route NOT-YET-FOUND")

THEOREM-EXACT NOW (exact identities/inequalities of Cut/ℚ, foundations-only): foldPhaseBorn closed form
`(1−q)²/(2(1+q))` + positivity + the fold-phase square identities (from banked N416); mbRatioDerived
closed form `(1−q)²/(3(1+q))` + positivity + strict `< 1` (down genuinely below top); the Cut-gear
restricts to the banked ℚ-gear `mbBornRatio` (faithful domain extension); the cross-sector
heaviest ratio = `(Md/Mu)²` (from the parent `sectorMass_eq`, shape cancels); teeth.

CANDIDATE (owner-authorized QT1 route SEED_OWED_FACES §1b, NEVER fit): the IDENTIFICATION that
mbRatioDerived IS the physical m_b/m_t = the up/down relative sector scale `(Md/Mu)²`. The ≤few% PDG
comparison is REMOVABLE PROSE; no empirical number in any proof.

ROUTE-NOT-YET-FOUND (the remaining leg, handed to the ONE directed successor): the down/confinement
offset `Md/Mλ` — the confinement scale ratio `Λ/M = cutExp(−12π)` and `Λ/v = cutExp(−8π/3)` are BANKED
(X9 `QuarkHadronTower`, `ConfinementHadronicMass`); welding the confinement leg to the fold-phase
up/down leg pins the full relative-scale assignment → then the freeze-out summand pins → re-narrow →
D7 restate AS 1/α(0) ≥15 digits → D8 → hand M1.

## WHY THIS IS NOT A BULLSHIT REPACKAGING (S4 anti-bullshit gate, in writing)

1. FOLD-CAUSAL. fold → J²=−1 quarter-turn → amplitude-halving cascade π/2→π/4→π/8 (N416) → foldPhase =
   cutPi/8 → its Born factor sin⁴P/cos²P → × the self-blind survival 2/3 (N308) = the up/down scale.
   Every arrow banked/forced. The relative scale is the shadow of "the ℍ-fold angle IS the hierarchy".
2. DISSOLUTION. The SM treats m_b, m_t (and the up/down split) as independent Yukawa inputs. The theory
   DISSOLVES the up/down relative scale into a determined function of the ONE banked fold phase — a genuine
   collapse of a free dial, resolving a front deferred across FOUR nodes, not a relabel.
3. WORDS-REMOVABLE. Delete quark/up/down/top/mass/sector/scale/fold-phase: over Cut, foldPhaseBorn =
   sin⁴P/cos²P at P=cutPi/8 = (1−q)²/(2(1+q)), q=cutCos(cutPi/4), q²=1/2; mbRatioDerived = foldPhaseBorn·
   (tr Poth/3) = (1−q)²/(3(1+q)); it restricts to the banked ℚ-gear; sectorMass Md 0/sectorMass Mu 0 =
   (Md/Mu)². Pure arithmetic on banked cutCos(cutPi/8), cutCos(cutPi/4), Poth, sectorMass. Survives; the
   reason it's the next node (read the parent's dominant residual off the now-banked fold phase) survives.
4. NAME THE TEMPTATION. The saturated temptation: assert mbRatioDerived = PDG 0.0242 (FIT — forbidden),
   OR re-bank the M2 gear under a new name (gold-plating). REFUSED both. The load-bearing NEW content is
   foldPhaseBorn (the fold-phase slot FILLED over Cut, never before built) + the closed form + the
   crossSector_heaviest_ratio pin — genuinely new, and it PINS a four-times-deferred residual, not repackages.
5. HONESTY. The physical identification is candidate (owner QT1 route); the Cut-arithmetic is theorem-exact.
   No PDG number in any proof; no fit; the confinement leg honestly named + handed to the successor.

## GUARDS
- No scale/ratio value asserted or fitted; mbRatioDerived FALLS OUT of banked cutCos(cutPi/8) + N308.
- No 137.036/0.231/PDG in any proof; measured comparison REMOVABLE PROSE only. Explicit NO-FIT.
- BANKED derived ℝ Cut + cutCos/cutSin/cutPi (N323/N416) + Poth (N308) + parent sectorMass (N340 shape).
  NO Mathlib-ℝ as content; NO cutTan needed (dissolved to sin/cos over the Born square).
- Foundations-only: #print axioms ⊆ {propext, Classical.choice, Quot.sound}. IMPORT GUARD: CascadeFoldPhase
  (N416), ScaleTowerGearDecomposition (N408), FreezeoutEdgeCrossSectorReduction (parent) — NO foldRetention.
- Free-floating rail: every production TYPE cites foldPhase/cutCos/cutSin (N416), s1BornProb/Poth (N308),
  sectorMass/bMass (parent, N340 shape). mbRatioDerived is literally foldPhaseBorn × the banked survival.
- ONE successor on the chain tail (directed toward the confinement leg → re-narrow), no fork.

## COST BUDGET / KILL
All content is `nlinarith`/`field_simp`/`ring` on banked Cut brackets (q²=1/2, half-angle squares). Probe
compiled EXIT 0 (m_b/m_t = 0.01675 = key value, verified). Expected LIGHT (gate-fast ~1 min). KILL: any
single obligation >60s → decompose per-lemma. No unbounded compile; no brute normalizer.

## PRODUCTION FILE
`Phys/Algebra/UpDownRelativeScaleFoldPhase.lean`, namespace `Phys.Algebra.UpDownRelativeScaleFoldPhase`.
Capstone `upDown_relativeScale_foldPhase`. Costume C540. Node `UpDownRelativeScaleFoldPhase`.

## RESOLUTION (run 845 — landed)

Production `Phys/Algebra/UpDownRelativeScaleFoldPhase.lean` builds green; full Phys green (3944 jobs);
17 decls foundations-only ⊆ {propext, Classical.choice, Quot.sound}. Costume C540 bites (1 = 540, one
error, tied to `crossSector_scale_genuine`). Wired into Phys.lean + AxiomAudit; manifest row + LEDGER
row (794) added.

DELIVERED: the up/down relative sector scale DERIVED as `mbRatioDerived = (1−q)²/(3(1+q))`, q=cutCos(cutPi/4),
q²=1/2 — the fold phase P=cutPi/8's own ℍ-level Born weight — resolving the four-times-deferred quark-scale
front by CLIMBING to the now-banked fold phase (N416). The parent's cross-sector heaviest ratio = (Md/Mu)²
(shape cancels) = mbRatioDerived: the up/down hierarchy IS the fold phase's Born weight. Theorem-exact
arithmetic; candidate physical identification (owner QT1 route). The down/confinement leg Md/Mλ named +
handed to the ONE directed successor.
