MEASURE-FIRST FINDING (N309 / seed T6) — the route decision. RESOLVED.

═══ THE STRUCTURAL REDUCTION (ℚ-exact, DE-RISKED in ProbeN309b/c/Ev, all compile) ═══
Masses = Born squares of the banked amplitude cycle:  m_k = (genVec M A p r k)² = a_k².
The mass SPECTRUM is captured by its three symmetric functions (roots of the forced cubic
t³ − σ₁ t² + σ₂ t − σ₃):
  σ₁(m) = Σm  = 6M²                     ← FORCED, δ-INVARIANT (parseval + Koide balance)
  σ₂(m) = Σ_{i<j} m_i m_j = (9/4)M⁴ − 6M·e₃    ← phase enters ONLY via e₃
  σ₃(m) = Πm = e₃²                              ← phase
where e₃ := a₀a₁a₂ is the SINGLE phase-carrying amplitude product.
General ℚ-exact form (no conic, no balance, pure ring):
  e₃ = M³ − M·A²·(3p²+r²)/4 + A³·p·(p²−r²)/4.
On the conic 3p²+r²=3 with A²=2M²:  e₃ = (M²/2)(A·(4p³−3p) − M) = (M³/2)(√2·cos3δ − 1).
⇒ THE THREE MASS RATIOS ARE FORCED FUNCTIONS OF: Q=2/3 (giving σ₁) + the SINGLE derived
  phase δ_B (giving σ₂,σ₃ via e₃). ZERO free shape parameters; M is the one scale (G3).

═══ THE THEORY-NATIVE TIE (the deep point) ═══
The phase enters the cubic through cos(3δ_B). And 3δ_B = 3·(2/9) = 2/3 = T = the banked
self-seen fraction (tr Poth/3). This is EXACTLY total_holonomy_closes (3·δ_B = 2/3 = T).
So the spectrum's phase-shape parameter is the cosine of the TOTAL LOOP HOLONOMY = the
self-seen Born fraction. The mass spectrum ties directly to the banked N308 holonomy closure.

═══ ROUTE DECISION (W9 MEASURE-FIRST) ═══
Route (a) [three ℚ-exact rational mass ratios]: STRUCTURALLY IMPOSSIBLE. Forced A/M=√2 is
irrational; ratios reduce to u=√2·cos(3δ_B); u is never ℚ. NO ℚ-exact individual ratios.
Route (b) [numerical ratios]: need cos(3δ_B)=cos(2/3 rad). δ_B=2/9 is a Born COUNT (radians,
seed §1 step6), NOT a rational conic point (cos(2/9) transcendental) ⇒ the individual numerical
ratios require the ANALYTIC ℝ (not yet descended from the fold, STANDARD §3). This is
route-NOT-YET-FOUND = a FORWARD ℝ-DESCENT BUILD dependency — NOT an ℝ import now, NOT a
CHAIN-RUNWAY block, NOT empirical. [The measured lepton match 206.77/3477 is REMOVABLE PROSE, G2.]

CONCLUSION: T6 = the FORCED ℚ-EXACT MASS-SPECTRUM STRUCTURE banks NOW (σ₁=6M² forced,
cubic with ONE phase parameter e₃, the holonomy tie, non-vacuity). The numerical evaluation
is the standing ℝ-tower arc, noted in handoff, NOT childed (avoid fork). Single successor = T7
(quark associator deviation), the seed's directed next flavor beat (stays ℚ-exact/algebraic).

═══ ANTI-BULLSHIT GATE (run honestly) ═══
1. fold→node each arrow forced: fold→self-blind→cap-3(N5/267)→ℤ₃ cycle(N305)→Born 2/3(N305)→
   cycle form(N306)→Koide ∀δ(N307)→δ_B fixed(N308)→MASSES=Born squares, spectrum=forced cubic,
   σ₁ forced by Koide, phase via e₃ tied to holonomy(N309). ✓
2. Standard: SM = 3 free Yukawas. Theory: σ₁←Koide(forced), shape←single derived δ_B ⇒ ratios
   DERIVED, 0 free shape params. Genuine dissolution. ✓
3. words-removable: delete mass/generation/Koide → pure ℚ statement about Born squares of genVec:
   Σb=6M², Σb_ib_j=(9/4)M⁴−6M·e₃, Πb=e₃², forced cubic. Stands as pure algebra. ✓
4. NAME-THE-TEMPTATION: (a) import ℝ, eval cos(2/9), "match leptons" → REJECTED (G2 prose,
   STANDARD §3). (b) assert rational ratios → IMPOSSIBLE (irrational). (c) reverse-engineer from
   masses → REJECTED (G1). ✓
5. free-floating: TYPE cites banked genVec/Qkoide/parseval/DCpower_genVec/cyclepower_genVec +
   total_holonomy_closes; capstone carries jordan_fails_H4. NOT generic. ✓
NOT gold-plating N307 (W4.5): masses (Born squares) are a NEW object; σ₁=6M² a NEW δ-invariant
power-sum; the cubic reduction to one phase param is NEW spectrum structure. OPENS, not re-pins. ✓
