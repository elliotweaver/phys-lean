# N4 PREREG — THE GENERATED OPERATOR ALGEBRA + MODULE (SPINOR) STRUCTURE
*Frozen before production compute. Task t_c8f08116. Builds FORWARD on N3 (`Phys/Algebra/Operator.lean`).*

## TARGET (a theorem, over the banked octonion shape `CD (CD B)`; concrete on `O ℚ`)

The SECOND purely-algebraic structure the terminal algebra forces, building directly on N3's
`clifford_op` / `L_sq_op`:

(i) THE FULL GENERATING RELATIONS, as forced laws (generic, over `CD (CD B)`):
   - `unit_sq_op`   : imaginary `a` with `Nrm a = 1` ⟹ `Lop a * Lop a = -1` in `End`.
   - `ortho_anticomm`: imaginary `a, b` with `bilin a b = 0` ⟹ `Lop a*Lop b + Lop b*Lop a = 0`.
   These generalize N3's single-pair `L_unit_sq` / `units_anticommute` to ANY orthonormal
   imaginary pair, governed by the BANKED `bilin`/`Nrm` (NOT a posited form). Then a CONCRETE
   full imaginary basis of `O ℚ` (the 7 standard imaginary coordinate units) realizing the
   relations: each imaginary (`star = -unit`), each `Nrm = 1`, pairwise `bilin = 0` — hence all
   7 square to `-1` and all 21 pairs anticommute under `Lop`. (A genuine ℚ-basis of the imaginary
   subspace; ℚ is the cascade ground ring, NOT the continuum — gate not tripped.)

(ii) THE GENERATED SUBALGEBRA + MODULE:
   - `gen` : the unital associative subalgebra of `AddMonoid.End (CD (CD B))` GENERATED by the
     imaginary left-multiplications (`Subalgebra ℤ` adjoin, or `Subring.closure`). It is unital
     associative by construction (a sub of the associative `End` ring).
   - `CD (CD B)` is a MODULE over `gen` via the natural application action (`f • y = f y`),
     restricted from the tautological `End`-module structure.
   - CONTENT (non-vacuity): the generators `Lop a` (imaginary) lie in `gen`; the Clifford
     relations hold INSIDE `gen`; the module action recovers the octonion left-mult
     (`Lop a • y = a * y`); and `gen` is genuinely non-commutative / richer than scalars
     (carries `op_nonassoc`-type content) so the module is not the trivial scalar action.

THE MOAT: posit-vs-derive. The division-algebra-physics field POSITS the Clifford module; we
DERIVE it from the cascade — `gen` and its module action are built from the banked `CD`/`star`/
`Lop`, no Mathlib `CliffordAlgebra`/`QuadraticForm` asserting the structure IS a Clifford algebra.

## GO  (all required; gate-green is necessary, NEVER sufficient)
- (i) and (ii) PROVED as theorems over `CD (CD B)` (no new carrier posited; form = banked bilin/Nrm).
- Foundations-only: `#print axioms` ⊆ {propext, Classical.choice, Quot.sound}; no sorry/axiom/
  native_decide/bridge.
- UNBROKEN: forward from N3's `Lop`/`clifford_op`/`L_sq_op` + banked `CD`/`star`/`bilin`/`Nrm` +
  standard Mathlib (`AddMonoid.End`, `Subalgebra`/`Subring`, `Module`). NO Mathlib
  `CliffordAlgebra`/`QuadraticForm` import asserting the structure.
- PHYSICS-WORDS-REMOVABLE: delete "spinor"/"gauge"/"Clifford" → "the imaginary left-mult
  endomorphisms generate a unital associative subalgebra of `End` with prescribed
  anticommutation/square relations governed by the self-overlap form, and the terminal algebra is
  a module over it" stands as pure algebra.
- COSTUME that BITES (ONE new row): a WRONG generating relation. Chosen: two DISTINCT imaginary
  basis units that COMMUTE under `Lop` (contradicts the new whole-basis anticommutation) — must
  FAIL to compile with a clean false core. (Not a `+`-square; that is C16 already, for `e₂`.)

## NO-GO / KILL  (classify a fight as INSTRUMENT failure, not theory failure)
- If (ii)'s module-over-subalgebra appears to need ℝ / completeness / a Lie GROUP / a real-spectral
  argument → STOP. That is the ⚠ STANDING DEPENDENCY GATE (continuum), NOT a workaround. N4 is
  purely algebraic and must NOT silently `import Mathlib...Real`. (ℤ/ℚ module structure is the
  expected home and does NOT trip the gate — ℚ is the cascade ground ring.)
- If "basis" seems to force ℝ → it does not; the imaginary subspace has a ℚ-basis.
- If a sub-lemma FIGHTS (won't close, feels forced) → THE ONE LAW (W5): return to the trunk —
  the operators ARE anticommuting roots of `-1`, the module action IS left multiplication. NEVER
  add a hypothesis/axiom to pass a fight; NEVER weaken the target.
- If a hard sub-lemma still fights after the theory-native reframe → DECOMPOSE (W3): bank what
  compiles foundations-only + a costume that bites, child the remainder onto the chain tail,
  complete as "decomposed." NEVER bridge/bypass/scope-reduce. NEVER run out of turns mid-task.

## ANTI-VACUITY (W8) — a too-easy result is a BUG until proven otherwise
- The subalgebra/module existence is trivial in Lean once instances are present; the CONTENT is
  the relations holding inside `gen` + action = octonion mult + non-commutativity. If deleting
  the physics words leaves a theorem that says nothing (e.g. "M is a module over some subring"),
  that is vacuous — the relations + faithful/nontrivial action are what make it bite.
- Costume MUST fail to compile (a wrong commute relation accepted ⟹ vacuous).
