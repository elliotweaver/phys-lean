# SCALE-LAYER FINDING — the ascent's ceiling (this run, t_0b31c1e9)

This run is the NAMED ASCENT of the repeated-deferral protocol (5th consecutive node on `s`).
The ticket directs: DERIVE the absolute cross-normalization that SELECTS `s` among the banked
admissible census candidates {1, 3/2} from the RUNNING/TRANSMUTATION SCALE (the structure one
rung UP from the census layer). Attack theory-native FIRST (the ONE LAW); if it genuinely
resists, name the exact NEW blocker + (a) child ONE further ascent OR (b) raise CHAIN RUNWAY.

## What was already exhausted (prior runs — banked / documented)
- GROUP-THEORY levers (this node's prior run, `ANALYSIS.md`): Casimir ratio 8:3, g₂ projectors
  proj7/proj14, anomaly quadratic index — ALL under-determined (several land in (0,isoDepth)) AND,
  at root, each needs a canonical adjoint→fundamental transport map that the banked Schur /
  cascade-stop obstruction N196 (`hom_iotaImH_to_kappaTop_eq_zero`) FORBIDS. Inequivalent reps on
  different carriers carry no intertwiner; a Casimir RATIO is not a transport coefficient.
- CENSUS layer (predecessor `AntiScreeningTemperingCeiling`, banked): PROVED the census layer
  under-determines `s` — `ratioTempered = 3/2` (the convention-free 3:8 tempering) and
  `doublingTempered = 1` (the doubling-square / std su(2) Dynkin) are BOTH admissible yet DISAGREE
  (`temperings_disagree`). Admissibility is necessary-not-sufficient. Named the missing datum as
  the ABSOLUTE cross-normalization living one rung up in the running/transmutation scale.

## THE NEW FINDING (this run — the SCALE layer, the structure the census layer was told to climb to)
I examined the actual banked running/transmutation SCALE the ticket names as the source of the
selecting datum:
  - `DimensionalTransmutation` (N320): `v/M = cutExp(−c/g²)`, `c` an HONEST PARAMETER explicitly
    deferred to the B2 bridge — supplies no selecting datum for `s`.
  - `ScaleTowerLadder` (N332): the EW rung `v/M = e^(−28π/3)` = the n=9 quotient of the 84π
    grammar. ⚠ CARRIES A BANKED HONEST-GRADE CAVEAT (verbatim, lines 63–79):
      • "the whole tower carries a PERMANENT INCEPTION CAP — the deviations in the key are
        {+0.013,+0.046,+0.092,+0.060} dex, a ~few-% ladder, NOT theorem-exact."
      • the deepest reading — "{7,9,28} divisors as the relevant-direction eigenvalues
        λ_n = −n/(84π) of the one critical fixed point) is a B7 target ... NOT built (it does not
        fall out cleanly at B5)."
      • "The seed is explicit that a cleaner identity-grade landing DOES NOT EXIST here — this
        ladder-grammar assembly IS the authorized B5 target, and grinding for more is chasing a
        phantom."
  - `WeinbergAngleRunning` / `TwoChannelDescent`: NO banked saturation / fixed-point / floor pins
    `netIsoDepth` or `s`. The saturation floor `netIsoDepth → 0⁺` is `s → isoDepth = 2` = the
    BOUNDARY (halvingReduced, inadmissible), not an interior selection of {1, 3/2}.

## THE CEILING, stated precisely (the DEEPENED repeated-deferral diagnosis)
The absolute cross-normalization that would SELECT `s` to an EXACT (identity-grade) interior value
lives in the running/transmutation SCALE tower. That tower is banked AND owner-affirmed
(SOUL arc-wide note: "B is LADDER-grade not identity — do NOT grind c for a phantom exact
identity — the 84π=2π·42 grammar IS the landing") as LADDER-GRADE with a PERMANENT INCEPTION CAP.
An identity-grade selecting datum therefore DOES NOT EXIST in the named richer structure. The one
richer structure that both the prior group-theory analysis and the banked ladder name — the
C6-dynamical critical-fixed-point / RG-eigenvalue structure λ_n = −n/(84π) — is exactly the "does
not fall out cleanly / phantom to grind for" B7 target the owner flagged.

## THE HONEST NUMERICS (workbench/s-tempering/numerics.py — orientation, prose only, G2)
D2c composed: dressedInvAlphaEM(1/3, s) = 112 + (16/3 − s)·T·(3/2), T = screening unit ≈ 4.865
(from the banked D7 numeral lower bound). Census-native candidates:
  s = 1   (Dynkin)  → 1/α(0) ≈ 143.6   (+ join + freeze-out ADD MORE on top)
  s = 3/2 (3:8)     → 1/α(0) ≈ 140.0   (+ join + freeze-out ADD MORE on top)
  s ≈ 1.9 (needed to hit measured ~137.036) → near the inadmissible boundary; FITTING, G2-forbidden.
The census-native `s ∈ {1,3/2}` do NOT land near the measured value (~2% high, before join/freeze
push higher) — RIGHT IN the scale tower's few-% ladder band. Demanding a 15-EXACT-digit selection
of `s` is demanding IDENTITY-grade precision from a structure banked as LADDER-grade — the phantom.

## WHY (a) [child a further ascent] is NOT genuinely available
The only named richer structure is the C6 RG-eigenvalue fixed point (λ_n = −n/(84π)), which is
owner-flagged phantom-to-grind + banked "does not fall out cleanly." Childing a 6th s-worker to
grind it = the circling the SOUL explicitly warns against on the 5th node. Not a genuine
autonomous forward node — it is the phantom.

## WHY this is option (b) [CHAIN RUNWAY / needs_input] — the exact missing datum, named
Landing the arc-D D7 north-star (≥15 EXACT significant digits of 1/α(0)) genuinely requires EITHER
  (i) an EXTERNAL/EMPIRICAL number — fitting `s` to ≈1.9 to reach the measured 137.036 (G2-FORBIDDEN); OR
  (ii) a SCOPE/DIRECTION decision only the owner can make: accept the census-native `s ∈ {1,3/2}`
       ladder-grade PREDICTION (1/α(0) ≈ 140–150, DISAGREEING with measurement) as the fold's
       standing prediction — a momentous claim I must NOT bank autonomously — and RESTATE D7/D8 at
       LADDER-grade, which directly conflicts with SEED_DIGITS's binding ≥15-exact-digit floor
       ("D8 does NOT pass below the ≥15-digit floor").
The collision is between (A) the arc-D constitution's identity-grade 15-exact-digit north-star and
(B) the banked + owner-affirmed LADDER-grade of the scale tower the composed endpoint AND the
s-selection both depend on. Resolving it is a direction decision above the worker's authority.

NOT declaring `s` un-derivable (the theory is the ground; `s` is FORCED, route-not-yet-found). The
finding is narrower and exact: the EXACT selecting datum lives in a ladder-grade structure, so an
identity-grade selection is the phantom the owner flagged; the arc's identity-grade north-star
needs an owner direction decision (or an empirical input, which is forbidden).
