# M3 — THE CONFINEMENT SCALE NUMERAL (SEED_MASSES §M3) — PREREG (t_69b383b5)

## SELECT (fresh worker) — CONFIRMED M3 theory-native
Trace: fold → complex unit → CD cascade → octonion STOP → ImO = colour (dim 7) → the ONE 84π
grammar budget → the octonion/colour rung exponent rungExponent(7) = 12·cutPi (banked lock_imO:
12π·7 = 84π) → confinementScaleRatio = Λ/M = cutExp(−12π) (banked confinementScaleRatio_eq). Every
arrow forced; NO free parameter. The numeral FALLS OUT of the banked closed form × the M1 peg.
Anti-bullshit gate: delete "confinement/scale/hadron" → certified two-sided rational bracket for the
derived Cut cutExp(−12π) + exact ratio identities. Words-removable. Not free-floating (cites
confinementScaleRatio, confBandRatio, bandScreen, chargeTraceDepth, UnitPeg — all banked derived over
Cut). No textbook analogue (nobody derives Λ/M = e^(−12π) from a colour dimension). PASS. Board order
D→M→C→GR ⇒ M3 confirmed.

## ROUTE (reuse the M2 pipeline; crux measured, no W9)
- confinementScaleRatio = Λ/M = cutExp(−12π), and −12π = −(36·(π/3)), so
  confinementScaleRatio = (cutExp(π/3)^36)⁻¹ (confinementScaleRatio_as_inv_pow36).
- Reuse M2's banked cutExp_pi3_bracket (cutExp(π/3) ∈ [284965/1e5, 284966/1e5]) and cutExp_natMul.
- THE NUMERAL: confinementScaleRatio ∈ [42408/1e21, 42414/1e21] (≈ 4.241×10⁻¹⁷ in M_oct := 1),
  via the 36th-power reciprocal: lo·Ehi^36 ≤ 1 and 1 ≤ hi·Elo^36 (kernel norm_num, both PASS with margin).
- THE RATIOS (reproduced):
  · M/Λ = confinementScaleRatio⁻¹ = cutExp(12π) = cutExp(π/3)^36 ∈ [23577·1e12, 23581·1e12] (exact + bracket).
  · v/Λ = confBandRatio = cutExp(8π/3) = cutExp(π/3)^8 ∈ [4347, 4350] (banked confBandRatio_eq + bracket).
- THE 128/27 BAND RE-EMERGES FREE: cite bandScreen_conf_exact (bandScreen chargeTraceDepth
  confBandRatio = 128/27) — the SAME confBandRatio = v/Λ = E^8 whose bracket this module computes;
  the exact screening 128/27 lands from the same E = cutExp(π/3) pipeline, no new input.

## CRUX MEASURED (Phys/Algebra/M3Probe.lean, deleted) — 6.6s build, all 6 norm_num PASS
- (42408/1e21)·(284966/1e5)^36 ≤ 1 ✓   1 ≤ (42414/1e21)·(284965/1e5)^36 ✓
- 4347 ≤ (284965/1e5)^8 ✓   (284966/1e5)^8 ≤ 4350 ✓
- 23577·1e12 ≤ (284965/1e5)^36 ✓   (284966/1e5)^36 ≤ 23581·1e12 ✓

## ONE-PEG LAW
Λ = confinementScaleRatio is a pure ratio to M_oct := 1; bracket is on that ratio. Cite M1 UnitPeg;
pegged_confinementScale_bracket scales by the ONE positive peg. NO second dimensionful input.

## W8 TEETH
widths positive (lo<hi), strict scale ordering Λ < v < M (from brackets: hi_Λ < lo_v etc. OR from
banked confinementScaleRatio_lt_ewClosureRatio + ewClosureRatio_lt_one), the 128/27 band load-bearing.

## GRADE
THEOREM-EXACT (certified two-sided rational inequalities + exact ratio identities of the derived Cut,
foundations-only). NO measured Λ_QCD / MeV in any statement/proof — comparison is removable prose;
beyond-measurement digits are the STANDING PREDICTION. Ground field the DERIVED Cut. NO Mathlib-ℝ,
NO floats, NO native_decide. CARRY the deferred-D7 note.

## SUCCESSOR (the ONE directed child — REPLACES the generic SELECT)
Arc-M M4 (SEED_MASSES §M4): THE SCALE-TOWER NUMERALS — M, v, and every Z4 band edge as certified
brackets against the peg (the full dimensionful skeleton, way-points free).
