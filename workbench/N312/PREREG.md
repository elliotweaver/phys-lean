# N312 PREREG — the two-sector FRAME OVERLAP (seed T8 remainder, the ℚ-exact structural core)

## Arc synthesis (S1, fresh worker)
N305→N311 built the flavor stack: ℤ₃ self-blind cycle (T1), Born weights 1/3⊕2/3 (T2),
ℤ₃-Fourier amplitude cycle genVec + √2 (T3), Koide Q=2/3 ∀δ (T4), δ_B=2/9 from holonomy=statistics
(T5), mass ratios = forced cubic (T6), quark associator deviation / Q⊥I₃ separation (T7), and N311
the SINGLE-SECTOR tridiagonal frame massMat (T8 ℚ-exact half): corner-zero M₁₃=0 from cascade
termination, geometric-mean coupling ℚ-exact via Born square, GST leading angle tan²θ=mᵢ/mⱼ.

## THE THEORY-NATIVE KEY (the ONE LAW reframe — do NOT reach for ℝ eigen-machinery)
The mass-coupling matrix massMat is the **Born SELF-OVERLAP of the amplitude vector, restricted to
cascade adjacency**: massMat i j = aᵢ·aⱼ (= genVec i · genVec j) everywhere EXCEPT the corner
(0,2)/(2,0) where cascade termination forces 0. `Born = self-overlap` is the TRUNK. This DISSOLVES
the diagonalization:
 • Each 2×2 adjacency block [[aᵢ²,aᵢaⱼ],[aᵢaⱼ,aⱼ²]] = (aᵢ,aⱼ)ᵀ(aᵢ,aⱼ) is RANK-1 (det=0, ℚ-exact).
 • Its eigenvector for the LARGE eigenvalue is the RATIONAL amplitude direction (aᵢ,aⱼ), eigenvalue
   aᵢ²+aⱼ²=mᵢ+mⱼ; the null direction (aⱼ,−aᵢ) has eigenvalue 0. Both ℚ-EXACT — a generic tridiagonal
   has irrational eigenvectors; OURS has RATIONAL eigenvector directions because it is a Born overlap.
 • So the per-sector mass-basis FRAME directions are rational → the two-sector frame overlap
   V = U†D has ℚ-EXACT inner-product numerators; only the normalization √(norm) needs the derived ℝ.

Therefore NO ℝ eigen-machinery need be built for the STRUCTURAL node — the ONE LAW default (keep it
ℚ-exact via Born squares, exactly the N305→N311 pattern). The actual angles (arccos/arcsin), the
normalization √, the CP-phase numerical dressing are the honestly-deferred ℝ-descent remainder.

## TARGET (ℚ-exact, this node)
(1) BORN SELF-OVERLAP STRUCTURE: massMat i j = genVec i · genVec j off the corner; corner = 0. The
    coupling matrix IS the amplitude self-overlap minus the cascade-non-adjacent corner.
(2) RANK-1 EIGENSTRUCTURE of the adjacency block (grounded on massMat): det of the 2×2 coupled block
    = 0 (rank-1); amplitude direction (aᵢ,aⱼ) is eigenvector, eigenvalue mᵢ+mⱼ; (aⱼ,−aᵢ) null. ℚ-exact.
(3) TWO-SECTOR FRAME OVERLAP V (2×2 leading block): overlap entries = inner products of the two
    sectors' rational amplitude-direction frames. The leading Cabibbo mixing squared is ℚ-exact:
    |V_mix|² = (uⱼdᵢ − uᵢdⱼ)² / ((uᵢ²+uⱼ²)(dᵢ²+dⱼ²)), and orthogonality
    |V_aligned|²+|V_mix|² = 1 (Brahmagupta–Fibonacci ℚ identity). Aligned numerator = uᵢdᵢ+uⱼdⱼ.
(4) CP tie (structural, referencing N310): the CP invariant is carried by the banked assoc3 — nonzero
    on the octonionic Fano line (quarks), zero on the ℂ-line (leptons). Numerical Jarlskog deferred.
(5) W8 non-vacuity: two distinct sectors give nonzero V_mix (genuine mixing); a WRONG overlap value
    fails; the overlap is not the identity (frames genuinely misaligned).

## DISCIPLINE
G1 no asserted values (the overlap FALLS OUT of the Born self-overlap eigenstructure). G2 no empirical
CKM/PMNS/Jarlskog in any proof (removable prose). G4 the overlap is massMat DIAGONALIZED, not a fitted
ansatz. G5 words-removable (delete mixing/CKM/CP/generation: pure statements about genVec/massMat
inner products over ℚ). G6 foundations-only, no native_decide/sorry/axiom/heartbeat-raise. G7 ONE LAW
(the ℝ eigen-machinery is DISSOLVED by the Born-square framing — NOT imported, NOT blocked, NOT
empirical). FREE-FLOATING rail: TYPE cites banked massMat/genVec + the N310 assoc3/N5 cap grounding.

## W9 measure-first: probe Probe.lean verifies (1)(2)(3) close by ring/field_simp/decide before production.
## Successor: if this + N311 structurally CLOSE T8 → SEED 2 A1 directed child. If the per-edge Born
## suppression + CP-phase structural tie need one more node → that decompose-remainder is the successor.
## Decision deferred to post-derivation honest assessment.
