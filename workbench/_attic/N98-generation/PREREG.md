# N98 PREREG — frozen GO/NO-GO + KILL before compute

## Node
N98 — the next forced node after N97 (THE PACKAGED REVERSE-SURJECTIVITY / POLAR-PATH
DECOMPOSITION WITNESS). Priority order from ROADMAP §N98 forward frontier:
  (i) the ALGEBRAIC GENERATION statement — a CONCRETE isometry's compact part is a finite
      product of banked biMulFun 2-plane rotations, or a bounded generation lemma (NO topology);
  (ii) EXTEND the biMulFun-product family toward a generic-rank product / a longer word;
  (iii) the literal operator TOPOLOGY over Cut (W1 HEAVY core — MEASURE existence first);
  (iv) the full Spin(9)→SO(9) exhaustion.

## REPRESENTATION SPLIT (measured constraint)
The generation world (genIsomMonoidC2, biMulFun) lives in `Function.End STVC` (raw functions).
N93/N97 (compact part, specOpN, EvC-adjoint) live in `Module.End Cut STVC` (linear operators).
Bridging Function.End ↔ Module.End is itself non-trivial. THEREFORE: the cheapest immediately
bankable piece stays ENTIRELY within the Function.End generation world — extend the
biMulFun-product family (priority ii), composing banked N94/N95/N55 lemmas. The Module.End-side
compact-part identification (priority i) and the operator topology (iii) are the HEAVY remainder.

## TARGET (GO)
Bank a genuinely-NEW structural generation piece within the Function.End world that ADVANCES
the exhaustion (not gold-plating N94/N95). Lead candidate, MEASURE-FIRST:
  - biMulFun u u' with DISTINCT anticommuting unit-imaginaries u ≠ u' is a genuine rotation whose
    real-axis action is 1 ↦ u·u' (a NEW imaginary unit), reaching elements the half-turn
    (u=u', involution) family does not; AND/OR
  - the N95 imaginary-plane product C = biMulFun w w ∘ biMulFun u u is an INVOLUTION
    (C∘C = id), the order-2 structure of the imaginary-plane rotation; AND/OR
  - a length-3 word negating span{1,u,w,z} (a new reachable configuration).
Pick whichever MEASURES cleanest. Each a theorem, foundations-only, ∈ genIsomMonoidC2 where
membership applies, with a concrete e₂/ιe₂ non-vacuous witness (W8 teeth).

## NO-GO / KILL (instrument-failure classification, W9)
- KILL any single decl that does not elaborate in < 30s on warm oleans → instrument wall:
  measure/decompose/reframe, do NOT inflate maxHeartbeats, do NOT brute a coordinate goal.
- If the Module.End ↔ Function.End bridge (priority i) is needed and resists after one bounded
  probe → it is the W1 HEAVY remainder: child it, do NOT grind.
- If the operator topology over Cut (priority iii) is probed and Mathlib has no
  TopologicalSpace (Module.End Cut STVC) constructible cheaply → W1 dissolution ticket + child.
- THE ONE LAW: if a proof fights, return to the trunk (octonion alternative laws
  mul_flex/mul_mul_right, the Born composition gFormC_comp). NEVER weaken the target, NEVER add
  a hypothesis to pass.

## DISCIPLINE
Scratch lean isolated in this dir (lake env lean, grep full errors). The instant a lemma compiles
clean → MOVE to production Phys/Algebra/<NewFile>.lean + wire (Phys.lean + AxiomAudit import AT
END + #print axioms) + commit BEFORE chasing the full theorem. Costume C130 bites a WRONG
generation/biMulFun-product claim, false numeric DISTINCT from C129 4/3=40 (… C126 1=9, C127 1=12,
C128 3/2=13, C129 4/3=40). Gate bg. STANDARD check (unbroken/complete/words-removable). Child the
next node onto the chain tail (single-flight). FINALIZE with turns to spare (anti-timeout).
