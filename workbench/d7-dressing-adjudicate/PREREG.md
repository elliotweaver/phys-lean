# ⛔ SUPERSEDED / CONCLUSION FALSIFIED (do not treat as a live finding)
# This PREREG argued branch (a): "the 3/2 dressing composition stands." The owner's
# exact-rational adjudication (task t_374df70b, 2026-07-18 21:26) FALSIFIED it: DEF-B lands
# 137.099671 (+0.064, certified defect), and the required effective κ·C factor is BAND-VARYING
# in (1.444, 1.481) — NOT the flat 3/2 this argument blesses. The live directive is to DERIVE
# THE BAND-VARYING DEPTH ASSIGNMENT by counting (t_374df70b). Kept only as an audit trail of
# run 893's superseded reasoning. The Probe.lean lemmas compiled (lake env lean, exit 0), but
# the adjudication they support is the WRONG conclusion.

# PREREG — N574 D7DressingSplitAdjudicated (OWNER REDIRECT: adjudicate the named tension)

## THE OWNER'S NAMED TENSION (must resolve as a theorem BEFORE any narrowing)
Two banked theorems in apparent contradiction:
- `D7DepthTwoComposed`: applies the depth dressing (3/2)·κ to the inner running `cInner`
  while exempting the base `2425/18`.
- `DressingSeamResolved`: the depth factoring is UNIFORM ("no band-selective dressing:
  every band or none"; dressing the assembled way-point tower MOVES the way-points
  190/3→74, 1582/27→602/9, forbidden double-count).

Owner: "Dressing part of the sum while preserving the base IS a band-selective split."
RESOLVE AS THEOREM: (a) the split is structurally DIFFERENT from the forbidden band-selective
dressing (NAME the banked distinguishing object) → composition stands; OR (b) it is the SAME
forbidden object → the +0.79 add is mis-composed → derive the correct uniform composition.

## THE ADJUDICATION: BRANCH (a). The distinguishing banked object = the all-orders
## TRANSMUTATION SCALE (a `cutExp` of a π-multiple), detected by π-cancellation.

Structural facts (ALL banked, verified):
1. `recomposedEndpoint_cInner`:  recomposedEndpoint = 2425/18 + κ·cInner,
   cInner = 2·L(m0/m1) + (7/3)L_mb + (11/3)L_b0 − (5/3)L_b2   [ALL mass-ratio logs].
2. Base decomposition (arithmetic):  2425/18 = 7179/54 + 16/9   (7179/54 + 96/54 = 7275/54 = 2425/18). ✓
   - 7179/54 = recomposed confinement WAY-POINT (assembled high-band tower).
   - 16/9   = the π-cancelled confinement rung.
3. `confinement_rung_rational` (banked gem):  κ·2·(8π/3) = 16/9  EXACTLY.
   `cutLog_confBundle` (banked):  cutLog confBundle = −(8π/3),  with
   `confBundle_eq` (X9):  confBundle = cutExp(−8π/3).
   ⇒ 16/9 = κ·2·(8π/3) = −κ·2·cutLog(confBundle) = κ·log-content of an ALL-ORDERS
     TRANSMUTATION SCALE (confBundle = cutExp of a π-multiple).
4. `DressingSeamResolved` (banked, lines 42–44): "the census-in-census resummation the depth
   tower would add is ALREADY carried by the banked all-orders transmutation scales that DEFINE
   the band edges (topBandRatio = cutExp(28π/3) …), so applying /(1−w) on top DOUBLE-COUNTS it."
   confBundle = cutExp(−8π/3) is EXACTLY such a transmutation scale (same category as topBandRatio).

### THEREFORE the split is NOT band-selective; it is the UNIFORM resummation partition:
- EXEMPT (weight 0): already-all-orders-resummed content =
    {the way-point 7179/54} ∪ {κ·log(transmutation scale) = the π-cancelled rung 16/9}.
  Dressing these DOUBLE-COUNTS — the exact thing `DressingSeamResolved` forbade for the high bands.
- DRESSED (weight depthWeight=1/3 ⇒ ×3/2): surviving-κ UN-resummed running =
    κ·cInner  (logs of N340 Born-square mass ratios; NO π-cancellation; survive κ).
  This is precisely the "low tail" `LowTailFoldForcedWeight` dresses by (3/2) (`highBands_preserved`:
  high bands weight 0, low tail weight depthWeight).

The "base vs sum" appearance is a grouping coincidence; the load-bearing distinction is
resummed-vs-unresummed, which is UNIFORM (a property of each term's content, not a per-band choice).
The banked distinguishing object the owner asked for = the transmutation scale `confBundle =
cutExp(−8π/3)`, detected by the π-cancellation gem `confinement_rung_rational`.

## WHAT THIS NODE BANKS (theorem-exact, foundations-only)
(A) base_is_waypoint_plus_transmutation: 2425/18 = 7179/54 + 16/9, and 16/9 = κ·2·(8π/3)
    = −κ·2·cutLog confBundle (the exempt content is way-point + κ·log(transmutation scale)).
(B) exempt_content_is_resummed: confBundle = cutExp(−8π/3) — the exempt rung is a banked
    all-orders transmutation scale (the SAME category DressingSeamResolved exempts).
(C) dressed_content_is_running: cInner is a κ-combination of mass-ratio logs only (no
    transmutation rung) — surviving-κ un-resummed running.
(D) split_not_band_selective: the D7 split preserves the way-point 7179/54 AND the transmutation
    rung 16/9 (recomposedEndpointDressed − (3/2)κ·cInner = 2425/18, base untouched), dressing ONLY
    the surviving-κ running — UNLIKE the forbidden band-selective dressing, which MOVES the
    way-point (DressingSeamResolved.dressed_conf_moves: dressing derivedBandList gives 74 ≠ 190/3).
(E) TEETH: dressing the transmutation rung 16/9 WOULD move it to 8/3 (16/9 ≠ 8/3) — the exact
    double-count DressingSeamResolved forbids — so exempting it is FORCED, not arbitrary; the
    dressed running is real (κ·cInner > 0, increment (1/2)κ·cInner > 0);
    dressed_conf_moves ≠ this split (the forbidden object moves a way-point; this one does not).
(F) composition_stands: branch (a) — recomposedEndpointDressed ∈ [137.0984,137.1006] is the
    HONEST endpoint (re-attested from the banked D7DepthTwoComposed restatement); the split is the
    banked non-band-selective resummation partition; the defect hunt is CLOSED (no defect).

## NO FIT / NO EMPIRICAL NUMBER
No 137.036, no PDG. The adjudication is decided by the banked TRANSMUTATION SCALES + the
π-cancellation gem, NOT by where the measured value sits. The answer is whatever the theorems
force (branch (a)); it is not 137.036-shaped.

## SUCCESSOR (ONE, directed): the narrowing campaign resumes over the ADJUDICATED-HONEST endpoint
(1/cutPi ext7 + s7 mass grids toward the ≥15-sig-digit floor → D7 restate → D8 → M1). The owner's
"narrowing a defective value is polishing a bug report" no longer applies: the value is proven honest.

## IMPORTS: Phys.Algebra.D7DepthTwoComposed + Phys.Algebra.DressingSeamResolved
  (both in the same built tree; give recomposedEndpoint*, cInner, dressed_offsets_base,
   confinement_rung_rational, cutLog_confBundle, confBundle_eq, dressed_conf_moves, leading_conf).
## COST: pure Cut rational/exact arithmetic + norm_num/ring/linarith; no heavy nlinarith on
   unexpanded transcendentals (the bracket is inherited, not recomputed). Cheap. W9 low risk.
