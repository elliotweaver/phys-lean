# PREREG — FreezeoutEdgeNumerals (t_bcab7183)

## SELECT verdict (comment 507)
Front = the freeze-out edge NUMERALS rb (bottom), rc (charm), rl (light). Deferred ≥4× — diagnosed
FRESH as a genuine NUMERAL job (F-native, SEED_DIGITS §D1/§D2e/§D5), NOT missing structure. The charm
edge (rc) is ALREADY banked (FreezeoutEdgeDescentDischarged.charmEdgeLog_bracket). Open sub-facts =
bottom (rb) + light (rl) edge logs, reducible via the banked ComposedFreezeoutDissolved dissolution to
certified brackets on cutLog(mass0), cutLog(mass2), cutLog(downConfBundle) — all bracketable NOW via
the banked ContinuumBracket instrument + cutLog_mono. No route-finder child needed.

## Target theorems (all over derived ℝ Cut, foundations-only)
- Lb0_bracket:  17/10 ≤ cutLog(bMass 0) ≤ 18/10        [mass0∈[56/10,57/10]; N=12/14]
- Lb2_bracket:  −12/10 ≤ cutLog(bMass 2) ≤ −1          [via cutLog(1/mass2)∈[1,12/10]; 1/mass2∈[100/34,100/33]; N=8/10]
- Ldcb_bracket: 4 ≤ cutLog(downConfBundle) ≤ 45/10     [dcb∈[7280/100,7288/100]; N=6/12]
- bottomEdgeLog_bracket: L_mb+L_b0−L_b2 ∈ [−7/5,−1]    (L_mb∈[−41/10,−4] banked)
- lightEdgeLog_bracket:  L_dcb+L_b2 ∈ [14/5,7/2]
- charmEdgeLog_bracket_reexport: 4 ≤ cutLog(1/mbRatioDerived) ≤ 41/10  (re-export)
- teeth: strict widths; bottomEdgeLog_neg; lightEdgeLog_pos; shape_logs_distinct
- freezeoutEdgeNumerals_landing (welded, 12 conjuncts, non-hollow)

## Numeric aiming (scratch, workbench/edge-numerals/aim.py — ALL PASS, comfortable margins)
mass0∈[5.6617,5.6617], mass2∈[0.33663,0.33666], 1/mass2∈[2.9704,2.9706], dcb∈[72.8,72.88].
All exp partial-sum obligations verified with fractions (kernel-exact). Probe.lean: heaviest N=14/12
obligations elaborate exit-0 in ~40s wall (2.9s CPU) — cost bounded, NO W9 wall.

## Guards
NO fit; s=1/8 untouched; NO empirical number in any statement/proof (thresholds = removable prose);
NO new axiom; NO Mathlib ℝ as content (all over derived Cut); NO truncation to measured decimal.
Import guard: FreezeoutEdgeDescentDischarged, HeavyQuarkNumerals, AssembledBracket, GenerationMassRatiosNumeric,
ContinuumBracket — all foundations-only; NEVER MassGapOne/OneAxiom.
Words-removable: pure cutLog rational-inequality statements over Cut.
Not free-floating: every type mentions banked mass0/mass2/bMass/downConfBundle/mbRatioDerived/cutLog.

## Cost budget / KILL
Per-obligation norm_num [Nat.factorial] on partial sums up to N=14. KILL: if any single obligation
exceeds ~60s CPU, decompose the partial sum into a banked partialExp waypoint lemma (N340 pattern).
Measured: full probe ~2.9s CPU. Safe.
