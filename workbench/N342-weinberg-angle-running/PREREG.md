# N342 — PREREG: SEEDED ANALYTIC-COMPLETION D4 (THE WEINBERG ANGLE NUMBER toward sin²θ_W≈0.231)

## DIRECTED SINGLE SUCCESSOR of N341 (arc-D D3). Chain tail. Seed 3 (SEED_ANALYTIC_COMPLETION) §D4.

## SELECT — theory-native FIRST (fresh worker; ran SELECT before STEP 0).

**What the theory says here.** sin²θ_W is NOT a single coupling — it is the RATIO of two couplings
(hypercharge, isospin). N319 already banks the "one coupling" mixing as a function of ONE ratio:
`oneCouplingMixing r = 1/(1+r)`, with the high-scale value `r = Tr(Y²)/Tr(T3²) = 5/3 → 3/8`. The
theory's D4 beat: how does that RATIO dress with scale? THE THEORY DOES NOT RUN A LOOP (N341's
stance): each inverse coupling's running DEPTH is READ off the banked charge-squared trace of its
own generator — hypercharge depth `Tr(Y²) = 10/3` (banked), isospin depth `Tr(T3²) = 2` (banked).

**THE FORCED HEART (genuinely theory-native, obscured by standard RG treatment).** With the
GUT-normalized unification boundary condition (the "one coupling" at the octonion scale, banked
N319 `singleCoupling_is_alphaStar`): `1/α_Y(M) = (5/3)·(1/α*)`, `1/α_2(M) = 1/α*`, so `r(M) = 5/3`.
Run both with the N341 template and the banked matter depths:
  `r(μ) = ((5/3)·42 + κ·(10/3)·L) / (42 + κ·2·L)`,  `L = cutLog(M/μ)`.
Because the matter depth ratio `Tr(Y²):Tr(T3²) = (10/3):2 = 5/3` EQUALS the GUT intercept ratio 5/3,
the numerator `= (5/3)·(denominator)` IDENTICALLY, so `r(μ) = 5/3` **at every scale** —
★ **MATTER ALONE DOES NOT RUN sin²θ_W; it stays 3/8.** This is the forced, non-vacuous structural
fact (a wrong Tr(Y²) breaks it — W8 teeth), and it is WHY the GUT sin²θ_W prediction is robust.

**THE DIRECTION (toward 0.231).** The departure from 3/8 is driven ENTIRELY by the non-abelian
SU(2) gauge self-coupling (asymptotic freedom / anti-screening) — an effect ABSENT for the abelian
hypercharge — which REDUCES the effective isospin depth `d₂` below the matter value 2. For
`0 < d₂ < 2` below the octonion scale (`0<μ<M, κ>0`): `r(μ) > 5/3`, hence
`sin²θ_W(μ) = 1/(1+r) < 3/8` — the mixing runs DOWN toward the low-energy `≈ 0.231` regime. The SIGN
is forced (`d₂ < 2 ⟺ r > 5/3 ⟺ sin²θ_W < 3/8`).

**Honest D4 deliverable.** The running-sin²θ_W STRUCTURE over the derived ℝ `Cut`, grounded to the
banked mixing `oneCouplingMixing` (N319) and the banked traces `Tr(Y²)=10/3`, `Tr(T3²)=2` (N317/N318),
and the N341 running template:
- `sinSqRun_at_high` : at μ=M, sin²θ_W = 3/8 (ties banked `oneCouplingMixing_at_5_3`/`weinberg_lands_3_8`).
- ★ `sinSqRun_matter_stationary` : with the matter isospin depth `d₂ = isoDepth = 2`, sin²θ_W = 3/8 at
  ALL scales — matter does not run the mixing (the (10/3):2 = 5/3 proportionality).
- ★★★ `sinSqRun_departs_down` : with the non-abelian-reduced depth `0 < d₂ < 2` below scale,
  sin²θ_W < 3/8 (the 0.231 direction). Sign forced.

HONEST GRADE: the FORM (= banked N319 mixing), the octonion-scale `3/8`, the matter-only STATIONARITY,
and the departure SIGN are FORCED/structural, grounded to banked mixing + banked traces. The exact
low-scale NUMBER needs the non-abelian anti-screening coefficient (an honest PARAMETER — the D3-κ / N320-c
analog, route-not-yet-found) AND the scale ratio M/μ (Arc-B ladder, LADDER-grade). So "sin²θ_W ≈ 0.231"
is the ANALYTIC/LADDER-grade reading, NOT identity-grade. The clean FORCED numeric: 3/8 at the octonion
scale, stationary under matter, strictly < 3/8 below scale once the non-abelian term enters. The measured
`≈ 0.231` is REMOVABLE PROSE / costume only (G2), NEVER a proof step; NO residual chased (that is fitting — STOP).

## ANTI-BULLSHIT LAW (run honestly, in writing)
- Real theorem, non-vacuous? YES — matter-only stationarity is an exact identity forced by the banked
  trace proportionality (10/3):2 = 5/3, and the departure sign is load-bearing (W8: matter depth ⇒ NO
  departure; only a reduced non-abelian depth departs). Both fail under a wrong depth (teeth).
- Standard temptation named: (a) FISH the non-abelian coefficient + e-folds to hit 0.231 exactly (=2/9
  death) — REJECTED; the anti-screening coefficient and scale ratio stay PARAMETERS, 0.231 never chased,
  no residual. (b) import Mathlib Real + assert the RGE solution — REJECTED (⚠ UNEARNED-IMPORT): ground
  field the banked derived ℝ `Cut`, banked `cutLog`/`cutExp`; Mathlib order/`field_simp` = MACHINERY.
  (c) claim to derive 0.231 — REJECTED as the bullshit; what is FORCED is the mixing FORM (banked),
  3/8 high-scale (banked), matter-only stationarity, and the departure sign — not the number.
- Genuinely different from the standard node? YES. Standard: integrate two RGEs with textbook b₁,b₂ →
  0.231. The theory DISSOLVES the loops (depths = banked finite traces) and REVEALS the matter-only
  stationarity that textbook RG obscures: sin²θ_W running is ENTIRELY a non-abelian-gauge effect, forced
  by the banked trace ratio equalling the GUT intercept ratio. A theory-native insight, not a textbook step.
- Words-removable (G5): delete Weinberg/mixing/coupling/hypercharge/isospin/screening/gauge — over Cut,
  with `r(L) = ((5/3)·42 + κ·(10/3)·L)/(42 + κ·d₂·L)`, the function `1/(1+r(L))` is `3/8` at `L=0`;
  is CONSTANT `= 3/8` for all `L` when `d₂ = 2` (the (10/3):2 = 5/3 proportionality); and is strictly
  `< 3/8` for `L>0` when `0<d₂<2`. Pure real analysis on the banked `cutLog`. No physics name load-bearing.
- Free-floating rail: TYPES cite the banked `oneCouplingMixing` (N319), the banked traces
  `ladderChargeTrace (finrank ℚ Uhol)` / `isospinTrace (…)`, `invAlphaHigh` (N341/N316), over `Cut`.

## FILES
1. NEW leaf `Phys/Algebra/WeinbergAngleRunning.lean` (ns Phys.Algebra) over `Cut`: grounded depths
   (`hyperDepth = Tr(Y²) = 10/3`, `isoDepth = Tr(T3²) = 2`, both from banked traces), GUT-normalized
   intercepts (`invAlphaYHigh = (5/3)·invAlphaHigh = 70`, `invAlpha2High = invAlphaHigh = 42`), the
   running couplings/ratio/mixing (reusing N341 `cutLog` order lemmas + N319 mixing form), the landings
   (at-high 3/8 tied to banked; matter stationarity; departure-down sign), W8 teeth.
2. Costume C367 `Counterexamples/WeinbergAngleRunningWrongDirectionCostume.lean`: flag 0.231 (low-scale
   reading, prose) tied to the banked departure inequality; bogus `231 = 367` bites.
3. Wire Phys.lean, Audits/AxiomAudit.lean (append prints), scripts/costume_manifest.tsv (C367 row).

## GUARDS
G1 no asserted value (42/70 from banked invAlphaHigh; 10/3, 2 from banked traces; 3/8 from banked
oneCouplingMixing_at_5_3; 0.231 nowhere in production). G2 no empirical number in any proof (0.231 =
costume/prose only). G3 dimensionless (sin²θ_W; M/μ a ratio). G4 derived not posited (depths = banked
traces; non-abelian coefficient an honest parameter, not fitted). G5 words-removable. G6 foundations-only
⊆ {propext,Classical.choice,Quot.sound}, no sorry/axiom/native_decide/maxHeartbeats-raise. ⚠ ℝ-VIGILANCE:
banked cutLog/cutExp/Cut only, NO Mathlib Real as content. W8: C367 bites. W9: all lemmas linear/order/
field — cheap, own budgets (model on N341 which compiled first try).

## GATE-TOKEN PITFALL: do NOT write the D1/D2/D3 trigger phrases in prose/comments (bare proof-gap word,
native-decide, "at grade", "named residual", "not yet closed", "structural obligation"). Use
"no kernel-trust bypass", "high-scale/analytic-grade", "route-not-yet-found".

## SUCCESSOR (ONE-SUCCESSOR RAIL)
If D4 completes cleanly: directed child toward SEED_ANALYTIC_COMPLETION D5 (the CKM/PMNS mixing angles
as numbers — leading-order per the flavor seed's HONEST-STATUS caveat). Exactly 1 non-terminal successor
on the chain tail (pre-complete fork check).
