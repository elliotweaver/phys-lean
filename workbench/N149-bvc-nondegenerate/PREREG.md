# N149 PREREG — BvC / polar nondegeneracy (the keystone of Lemma Z)

Frozen BEFORE compute. KILL classifies a fight as INSTRUMENT failure, not theory.

## MEASURE (done, recorded)
- The FULL Lemma Z `∀ x : CliffC, (∀ v, contractLeft (polarBilin QvCQuad v) x = 0) → ∃ μ, x = algebraMap μ`
  is the genuine W1 BUILD. Via Mathlib `changeForm_contractLeft` + `equivExterior`
  (`CliffC ≃ₗ ExteriorAlgebra Cut STVC`, char ≠ 2) it transports to: *an exterior element killed by a
  SEPARATING family of contractions is degree-0 (a scalar)*. That needs an Euler/degree-operator
  identity over a basis — Mathlib has NO center-of-Clifford, NO graded center, NO exterior
  interior-product degree lemma (grepped ABSENT). A coordinate brute over the basis is 2^10 = 1024
  monomials = the W9 instrument wall. So the full degree argument is a multi-lemma heavy W1 BUILD.
- The KEYSTONE the entire degree argument CONSUMES is the SEPARATING property of the contraction
  family — i.e. NONDEGENERACY of the polar form / BvC: the only datum N148 left implicit. N148
  measured it ABSENT (no stvcBasis/orthogonal/Nondegenerate in Phys/).
- BvC nondegeneracy is IMMEDIATELY BANKABLE from the trunk: STVC = Cut × Cut × O Cut, with
  `BvC p q = p.1*q.1 − p.2.1*q.2.1 − gFormC p.2.2 q.2.2`. The octonion block is the Born form
  `gFormC`, banked POSITIVE-DEFINITE (`gFormC_pos_of_ne` N56, `gFormC_eq_zero_iff`). Testing against
  mTimeUnit=(1,0,0), mSpaceUnit=(0,1,0), and (0,0,w) extracts each coordinate ⇒ p = 0.

## TARGET (this node — the W3 decomposition piece, sufficient + genuinely new)
Bank the keystone, child the global Lemma-Z degree argument:
  (Z1) `BvC_nondegenerate : ∀ p : STVC, (∀ q : STVC, BvC p q = 0) → p = 0`
       (the indefinite Minkowski form separates points — derived, not posited).
  (Z2) `QvCQuad_polar_nondegenerate : ∀ w : STVC, (∀ v : STVC, QuadraticMap.polar QvCQuad v w = 0) → w = 0`
       (the SEPARATING property of the polar-dual contraction family — the precise hypothesis the
       childed degree argument consumes, restated through `polar QvCQuad = 2·BvC`).
  (Z3) `polarBilin_separating : ∀ w : STVC, (∀ v : STVC, QuadraticMap.polarBilin QvCQuad v w = 0) → w = 0`
       (the `polarBilin` form, the exact shape appearing in `contractLeft (polarBilin v)`).
  W8 teeth: `BvC_mTimeUnit_self : BvC mTimeUnit mTimeUnit = 1` (≠ 0 — the form genuinely detects the
       timelike axis; non-vacuity; the read-off C181 negates).

## GO / NO-GO (frozen)
- GO if Z1–Z3 + the W8 read-off compile foundations-only in a probe and bank clean.
- NO-GO / DECOMPOSE-FURTHER if Z1 itself fights (it must not — pure coordinate extraction over the
  banked block form). If even Z1 resists → that is itself an instrument signal, return to trunk.
- The GLOBAL Lemma Z (degree argument) is NOT attempted here — childed as the immediate next node
  with this nondegeneracy keystone banked as its hypothesis-discharger.

## KILL budget (W9)
- Each obligation Z1/Z2/Z3/W8 is a finite coordinate computation: KILL = 90 s / obligation. No
  `ring`/`simp` over expanded 1024-monomial goals (that is the childed degree argument, not this).
- If any single obligation exceeds 90 s ⇒ INSTRUMENT failure, decompose that obligation, do NOT
  inflate maxHeartbeats. No exponential / tsum / coordinate-brute over the Clifford basis here.

## ANTI-VACUITY (W8)
- Costume C181 must bite a WRONG nondegeneracy / separating / polar / BvC read-off, reducing to a
  false numeric DISTINCT from the banked battery (… C179 1=147, C180 2=148). Use `1 = 149`.
- `BvC_mTimeUnit_self = 1` is the genuine read-off; claiming `= 149` forces `1 = 149`.

## PHYSICS-WORDS-REMOVABLE
Delete Minkowski/metric/signature/timelike/spacelike/nondegeneracy/separating/polar/Born/
interior-product/Clifford/Lorentz: what remains is pure algebra over the derived field Cut —
the bilinear form BvC on Cut × Cut × O Cut whose only point with all pairings zero is 0, restated
for `QuadraticMap.polar`/`polarBilin` of `QvCQuad`, and `BvC (1,0,0) (1,0,0) = 1`.
