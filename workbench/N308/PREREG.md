# N308 — PREREG: THE CRUX (seed T5). δ_B = 2/9 from HOLONOMY = STATISTICS.

## The target
Derive the cycle phase `δ_B = 2/9` as the FORCED solution of the "holonomy = Born
return probability" consistency on the ℤ₃ visibility cycle — NEVER asserted. The
seed states the fixed-point equation `n·T(1−T) = T` (unique nontrivial root
`T = (n−1)/n`, per-edge phase `δ_B = T(1−T)`; n=3 → T=2/3, δ_B = (2/3)(1/3) = 2/9).
The equation must be a THEOREM (holonomy=statistics DERIVED from self-blindness),
not a posited ansatz solved backward (G4).

## THE ROUTE I FOUND (theory-native, non-circular, banked-grounded)

The load-bearing insight: **the holonomy=statistics equation `n·T(1−T)=T` is
PROVABLY EQUIVALENT (for T≠0) to the banked N305 self-blindness self-view balance
`(1−T)·(n−1) + T·(−1) = 0`** — both reduce to the single fact `nT = n−1`, i.e.
`1−T = 1/n`, i.e. **`tr Pdem = 1` (the democratic/Unity gather is rank-1)**. So the
identification is NOT posited: it is a theorem that the loop-closure equation IS the
banked self-blindness balance in disguise. This is the "one cause, many terminations"
trunk: the rank-1 democratic gather (self-blindness) simultaneously forces the Born
split T=(n−1)/n (N305), Koide 2/3 (N307), and now δ_B = 2/9 (N308). ONE fact, three
landings.

Grounding on the banked cycle operators (anti-free-floating):
- **`seeMat = Pcyc + Pcyc²`** — the self-blind SEEING operator (statistics) IS the
  forward+backward cycle HOLONOMY operator. From banked `cyc_sum_Jall`
  (1+Pcyc+Pcyc²=Jall) and `seeMat = Jall−1`. This literally makes "holonomy =
  statistics" an operator identity on the banked objects.
- self-blindness `see_diag_zero` (diag seeMat = 0) ⟺ (Pcyc+Pcyc²)_{ii}=0: forward
  and backward holonomies cancel per-step (a probe cannot return to itself in one
  step either direction). `cycle_holonomy_on_Poth`: (Pcyc+Pcyc²)Poth = −Poth = `see_Poth`.
- **δ_B = T(1−T) = (tr Pdem/n)(tr Poth/n)** — the Born self↔other interference =
  product of the two banked spectral weights (Born = self-overlap, trunk). For n=3:
  (1/3)(2/3) = 2/9. The total loop holonomy n·δ_B = 3·(2/9) = 2/3 = T = the self-seen
  fraction. Holonomy = statistics.

## The derivation chain (each a real theorem, foundations-only)
L1 `born_fixedpoint_solve`  n·T(1−T)=T, T≠0, n≠0 ⟹ T=(n−1)/n     [pure ℚ algebra]
L1 `cyclephase_value`       same hyps ⟹ T(1−T) = (n−1)/n²          [pure ℚ algebra]
L2 `holonomy_statistics_is_selfblind` n·T(1−T)=T ⟺ (1−T)(n−1)+T(−1)=0 (T≠0)  [DERIVES the identification]
L3 `seeMat_eq_cycle_holonomy`  seeMat = Pcyc + Pcyc²                [banked cyc_sum_Jall]
L3 `cycle_holonomy_on_Poth`    (Pcyc+Pcyc²)Poth = (−1)•Poth         [banked see_Poth]
L3 `selfblind_balance_gen`     (1−trPoth/3)(3−1)+(trPoth/3)(−1)=0   [banked weight_balance]
L3 `selfseen_satisfies_fixedpoint` 3·(T·(1−T))=T at T=trPoth/3      [L2.mpr + L3]
L4 `selfseen_is_two_thirds_derived` trPoth/3 = (3−1)/3 = 2/3        [L1 solve]
L4 `cyclephase_delta_B_derived`     trPoth/3·(1−trPoth/3) = (3−1)/3² = 2/9  [L1 value]
L4 `cyclephase_delta_B`             = 2/9   ;  `..._traces` (trPdem/3)(trPoth/3)=2/9
W8 `cyclephase_not_selfseen` 2/9≠2/3 ; `cyclephase_not_democratic` 2/9≠1/3 ;
   `fixedpoint_excludes_half` 3·(½·½)≠½

## GUARDS honored
G1 no asserted values (2/3, 2/9 fall out of L1 applied to L3). G2 no empirical number
(Brannen δ_B≈2/9 is removable prose/costume only). G4 equation DERIVED (⟺ banked
self-blindness balance), not posited-and-checked. G5 words-removable (all theorems
pure ℚ/matrix). G6 foundations-only. G7 one law (it derived — no fight).

Free-floating: types cite banked seeMat/Pcyc/Pdem/Poth/trace; capstone carries
jordan_fails_H4 (cap→exactly 3). NOT generic fixed-point algebra.

Costume C337: 9·δ_B = 2 forced (via cyclephase_delta_B); bogus min(337,2)=337 → 2=337 bites.
