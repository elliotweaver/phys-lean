# PREREG — FRAME AUDIT F2: THE DRESSING SCOPE in the ARCH-B arena (owner redirect, t_8ba2ab5c)

## THE OWNER MANDATE (F2, the SECOND half of the frame audit; SUCCEEDS F1 N580b)
Narrowing is GATED (dressed endpoint 137.0996710997 = CONFIRMED DEFECTIVE). F1 (N580b) re-derived
AB INITIO the RECOMBINATION ARCHITECTURE and FIXED F2's arena: ARCH-B (run-both-channels-then-
recombine, isospin at its NET running census netIsoDepth s = isoDepth − s) is FORCED; ARCH-A (the
+70-constant additive weld) is the Z1-forbidden s=0 switch-off. F2 re-derives the SECOND surviving
structural claim: THE DRESSING SCOPE.

OWNER F2 (verbatim gist): "THE DRESSING SCOPE — the all-orders 3/2 fixed-point applies to WHICH
object? Its forcing theorem was proved WITHIN the single-tower frame; if F1 flips the architecture,
re-derive the dressing scope IN the forced architecture (per-channel content dresses at its own
census — the banked X5 census-in-census applied where the architecture puts it)."

## THE THREE CANDIDATE SCOPES (all banked-adjacent; this node ADJUDICATES — no new derivation)

The all-orders census-in-census depth factor is `1/(1−w) = 3/2` at the fold-forced weight
`w = depthWeight = 1/3` (D3 `depthWeight_eq`; the mean Born self-overlap over the ONE return
surface, `cutPi`-cancelled, CHANNEL-INDEPENDENT). The live question is WHICH content it multiplies:

  SCOPE-(a) UNIFORM WHOLE-TOWER dressing (the assembled tower, way-points included).
    Banked VERDICT: FORBIDDEN. `DressingSeamResolved` proves it MOVES both banked exact way-points:
      42 + (448/27)/(1−1/3) = 602/9 ≠ 1582/27   (`dressed_topBand_moves`)
      dressedInvAlphaZero (1/3) derivedBandList = 74 ≠ 190/3  (`dressed_conf_moves`)
    reproducing them ONLY at w=0 (`dressed_reproduces_only_at_w0`). The high-band census-in-census
    content is ALREADY inside the banked all-orders TRANSMUTATION SCALES that DEFINE the band edges
    (topBandRatio = cutExp(28π/3), confBandRatio = cutExp(8π/3)); re-applying /(1−w) there
    DOUBLE-COUNTS. So (a) is EXCLUDED by the way-points.

  SCOPE-(b) PER-CHANNEL VARYING (each band its own weight w_b, "its own band's self-overlap census").
    Banked VERDICT: FORBIDDEN. `LowTailFoldForcedWeight` proves the per-band VARYING weight COLLAPSES
    to the single fold-forced constant `depthWeight = 1/3` (`foldForcedLowTailScreen`,
    `foldForcedWeight_value`); per-band variation has NO fold source (D3: one return surface, one
    weight) and its ONLY motive is to TUNE = a FIT (the t_374df70b anti-bullshit gate KILLED exactly
    this: distinct w_b to land the effective factor inside (13/9, 40/27), excluded). So (b) is a knob
    the fold KILLS.

  SCOPE-(c) THE RESUMMED-vs-UNRESUMMED PARTITION (the forced scope): the 3/2 dresses ONLY the
    surviving-κ UN-RESUMMED RUNNING content (the low-tail mass-ratio logs — `cInner`), and EXEMPTS the
    already-all-orders-resummed content = the way-point band edges (transmutation scales) + the
    π-cancelled rational rungs (the base `2425/18 = 7179/54 way-point + 16/9 census gem`, banked
    `BaseNormalFormAudit`). This is UNIFORM (a property of each term's CONTENT — is it a
    transmutation scale / π-cancelled rung, or a surviving-κ log — NOT a per-band choice), so it
    escapes (b)'s fit; and it PRESERVES the way-points, so it escapes (a)'s exclusion.

## THE F1-TIE (why (c) is FORCED, ab initio, IN the ARCH-B arena)

The scope is NOT chosen; it is FORCED by the SAME single fact F1 used — the fold's ONE return
surface. The dressing weight w = depthWeight is the mean Born self-overlap over the ONE return
surface (D3), the SAME 2-sphere for EVERY band. Therefore:
  • the weight is CHANNEL-INDEPENDENT (both channels dress at the same w — F1's arena: per-channel at
    each channel's OWN census, but the census asymmetry is in the census netIsoDepth s, NOT in w).
  • per-band variation (b) has no source (channel-independence ⇒ NOT band-varying).
  • the exempt/dressed split is by CONTENT (resummed vs not), not by band, so it is the SAME UNIFORM
    factoring applied to the un-resummed content — which is exactly what `D7DepthTwoComposed` banked:
      recomposedEndpointDressed = 2425/18 (EXEMPT base) + (3/2)·kappaLeading·cInner (DRESSED running),
    with `dressed_offsets_base`: recomposedEndpointDressed − (3/2)·κ·cInner = 2425/18 (base UNTOUCHED).

So the F2 verdict = SCOPE-(c): in the ARCH-B arena the all-orders 3/2 dresses the un-resummed
low-tail running per channel at its own net census (which, the weight being channel-independent,
composes to the SAME uniform (3/2)·κ·cInner the banked D7 object carries), EXEMPTING the resummed
way-point/π-cancelled content. This is way-point-consistent (unlike (a)) and fit-free (unlike (b)).
It CONFIRMS the banked `recomposedEndpointDressed` composition as the F2-forced dressing scope.

## THE DISCRIMINATING THEOREMS (ASSEMBLY of banked exacts — the way-points adjudicate)

WP1 (a-moves-topBand):  42 + totalScreening[(charge,top)]/(1−depthWeight) = 602/9 ≠ 1582/27
    (re-export DressingSeamResolved.dressed_topBand_moves) — SCOPE-(a) fails the EM way-point.
WP2 (a-moves-conf):     dressedInvAlphaZero depthWeight derivedBandList = 74 ≠ 190/3
    (re-export DressingSeamResolved.dressed_conf_moves) — SCOPE-(a) fails the confinement way-point.
WP3 (leading reproduces both): invAlphaZero[(charge,top)] = 1582/27 ∧ invAlphaZero derivedBandList
    = 190/3 (re-export leading_topBand / leading_conf) — the shared s-independent anchors both scopes
    must reproduce (the free instrument-check).
BV1 (b-collapses): bandVaryingScreen (withWeight depthWeight lowTail) = (3/2)·totalScreening lowTail
    (re-export LowTailFoldForcedWeight.foldForcedLowTailScreen) — SCOPE-(b) COLLAPSES to the single
    fold constant; there is no varying family.
KN1 (b-knob-real-but-fixed): bandVaryingScreen [(0,b)] ≠ bandVaryingScreen [(depthWeight,b)] for a
    nonzero-screening band (re-export perBand_knob_real) — the per-band slot is a genuine knob the
    fold FIXES to 1/3 (real content, not vacuous), so fixing it is a derivation, not a fit.
SC1 (c-scope closed form): recomposedEndpointDressed = 2425/18 + (3/2)·kappaLeading·cInner
    (re-export D7DepthTwoComposed.recomposedEndpointDressed_closed) — the FORCED scope.
SC2 (c-exempts-base):  recomposedEndpointDressed − (3/2)·kappaLeading·cInner = 2425/18
    (re-export dressed_offsets_base) — SCOPE-(c) dresses ONLY the running, EXEMPTS the resummed base.
BASE1 (base is resummed content): 2425/18 = 7179/54 + 16/9 (re-export BaseNormalFormAudit
    .base_eq_waypoint_plus_gem) — the exempt base IS the confinement way-point 7179/54 (a
    transmutation-scale assembly) plus the π-cancelled census gem 16/9. This is WHY (c) exempts it:
    it is already all-orders-resummed, exactly the category (a) double-counts.
TIE (F1 arena tie): the dressing weight is the channel-INDEPENDENT depthWeight = 1/3
    (re-export weight_derived_not_free / depthWeight_eq), so per-channel dressing at each channel's
    OWN net census (F1) uses the SAME w — the census asymmetry lives in netIsoDepth s, not in w.
    This is the F1↔F2 weld: F1 fixes the CENSUS per channel, F2 fixes the WEIGHT (one, shared) and
    the SCOPE (resummed-vs-unresummed content, uniform).

TEETH (W8):
  T1 dressed_topBand_ne 602/9 ≠ 1582/27 (SCOPE-(a) genuinely moves the EM way-point — the exclusion
     has content);
  T2 perBand_knob_real (the per-band slot is a GENUINE knob — the collapse to one weight is real
     content);
  T3 scope-(c) genuinely dresses: recomposedEndpoint < recomposedEndpointDressed
     (re-export depthTwo_dresses_recomposed.2 — the 3/2 running is real, ONE-LAW-upward);
  T4 the three scopes are GENUINELY DISTINCT at the top-band reading: (a)'s 602/9 ≠ leading 1582/27,
     and the exempt base 2425/18 ≠ the dressed base — the adjudication separates real alternatives.

## THE VERDICT (F2, completing the frame audit)
In the F1-forced ARCH-B arena, the all-orders 3/2 census-in-census dresses the SURVIVING-κ
UN-RESUMMED low-tail running content ONLY (SCOPE-(c)), at the SINGLE channel-independent fold-forced
weight depthWeight = 1/3, per channel at its own NET running census (the census asymmetry in
netIsoDepth s, the weight shared). It EXEMPTS the already-all-orders-resummed content (the
transmutation-scale way-point band edges + the π-cancelled rational rungs = the base 2425/18). This
is the ONLY scope that (i) reproduces the banked exact way-points 1582/27 & 190/3 (SCOPE-(a) MOVES
them, excluded), and (ii) is fit-free (SCOPE-(b) is the fold-killed tuning knob). The frame audit is
thereby COMPLETE: F1 (the census per channel) + F2 (the weight + scope) both re-derived ab initio in
the forced arena. The banked `recomposedEndpointDressed` IS the F1+F2-consistent composition.

## GRADE (honest)
THEOREM-EXACT (exact identities/inequalities of Cut, foundations-only): the whole adjudication is an
ASSEMBLY of banked exact way-points + banked collapse/closed-form theorems. NO route-not-yet-found;
the F2 verdict is a proved theorem. Measured 1/α(0) ≈ 137.036 REMOVABLE PROSE ONLY; no empirical
number in any statement or proof; the scope is adjudicated by the fold's own way-points +
return-surface geometry, NOT by where a measured value sits. Explicit NO-FIT. The fit s≈1.9 / the
band-varying window (13/9, 40/27) named ONLY to be EXCLUDED.

## PHYSICS-WORDS-REMOVABLE
Delete every physics word: what remains over Cut is that dressing the assembled tower uniformly
(SCOPE-a) moves the partial sums 1582/27 → 602/9 and 190/3 → 74 (≠); that the leading (w=0) tower
reproduces 1582/27 and 190/3; that the per-band-varying screen at the fold weight collapses to
(3/2)·totalScreening (SCOPE-b has no free family); that recomposedEndpointDressed = 2425/18 +
(3/2)·kappaLeading·cInner with the base 2425/18 = 7179/54 + 16/9 preserved (SCOPE-c); that the
weight is the channel-independent 1/3. Pure Cut arithmetic on banked objects. No theorem STATEMENT
needs a physics word.

## COST BUDGET / KILL (W9)
Every proof is a re-export / rw-through-banked-equality + norm_num on rationals — no heavy
normalizer, no coordinate expansion, no cutLog/cutPi unfolding into arithmetic. KILL: if any single
obligation needs >30s STOP and decompose (none should — all are citations of banked theorems + the
three-way distinctness norm_nums). PROBE the non-pure-citation steps FIRST (the distinctness teeth).

## DELIVERABLE
Module Phys/Algebra/FrameAuditDressingScope.lean, namespace Phys.Algebra.FrameAuditDressingScope.
Imports: Phys.Algebra.D7DepthTwoComposed (⇒ DressingSeamResolved transitively? verify) +
Phys.Algebra.DressingSeamResolved + Phys.Algebra.LowTailFoldForcedWeight + Phys.Algebra
.BaseNormalFormAudit + Phys.Algebra.PerChannelDressing (weight_derived_not_free). All banked,
foundations-only. Costume C581 (bites 1=581). ONE directed successor: RECOMPOSE the endpoint on the
F1+F2-consistent composition → re-attest → narrowing re-runs → D7 → D8.

## FORK GUARD
Exactly ONE forward successor: the recompose/re-attest node. No generic SELECT before the frame audit
→ recompose → re-attest → D7 → D8 chain completes.
