# PREREG — PER-CHANNEL RUNNING-DESCENT RECOMBINATION over the census-conserving frame
## (t_5ab08d30, owner R2 continuation; DecouplingCensusRemoval 89a1c6b's named successor)

## THE FRONT (owner-directed, structural constraints only; route is mine)
Assemble the FULL recomposed endpoint over the census-conserving descent:
 - EACH channel descends with its FULL derived census (matter + gather self-census, the Z1 minus).
 - Channels RECOMBINE at the read point via the banked Z2/X7 weight (3/8), NOT a constant ceiling added.
 - Below each banked M-skeleton edge, content DECOUPLES = REMOVED from descent (subtraction, not summand).
 - ADJUDICATORS: banked exact way-points reproduced by the SAME pipeline; #print axioms foundations-only;
   way-points force the frame; measured value removable prose; NO-FIT absolute.

## WHAT IS ALREADY BANKED (the launch platform — REUSE, do not re-derive)
- RecomposedRunningEndpoint (t_c74b069f): the per-channel running descent with DERIVED s=1/8 folded in.
  * recomposedInvAlphaEM [(_,top)] = 6923/54  (EW way-point, iso census reduced by s=1/8)  [recomposed_EW_waypoint]
  * 6923/54 + bandScreen chargeTraceDepth confBandRatio = 7179/54  (below v iso DECOUPLES, full charge screens conf)  [recomposed_conf_waypoint]
  * bandScreen projectedS topBandRatio = 7/18 (the exact anti-screening shift, cutPi cancels)  [bandScreen_projectedS_top]
  * recomposedInvAlphaEM_closed : = 112 + totalScreeningWith recomposedCharge bands
  * recomposedCharge = 125/24 = chargeTraceDepth − s (census conserved)  [recomposedCharge_conserves]
- DecouplingCensusRemoval (89a1c6b): below threshold coloured content REMOVED; leptonCensus=2, colouredCensus=10/3.
- CensusCompletenessClosure: freeze-out staircase (10/3) re-supplies EXACTLY the removed coloured slice; 16/3 = 10/3 + 2.
- ComposedFreezeoutDissolved: freeze-out summand DISSOLVES to determined closed form 16/9 + κ·[(7/3)L_mb+(11/3)L_b0−(5/3)L_b2].
- FreezeoutEdgeNumerals: all three edge logs certified brackets (Lb0∈[17/10,18/10] etc).
- EndpointAssembly: leptonTail_screen_telescope, endpoint_closed, way-points free.

## THE THEORY-NATIVE STRUCTURE (what this node assembles — the below-Λ census-conserving tail)
RecomposedRunningEndpoint stopped at the confinement edge Λ (recomposed conf way-point 7179/54).
Below Λ the descent has TWO census-conserving parts (CensusCompletenessClosure):
  (a) the LEPTON tail carrying the SINGLET census leptonCensus=2 (coloured DECOUPLED, removed) →
      bandScreen 2 (mass0/mass1)  [leptonTail_screen_telescope]
  (b) the QUARK freeze-out staircase re-supplying the removed coloured slice (10/3), at the QUARK edges,
      = the DETERMINED dissolved summand 16/9 + κ·[...]  [ComposedFreezeoutDissolved]
These two carry the WHOLE below-Λ census (10/3 + 2 = 16/3) with none omitted, none double-counted.

DEFINE the recomposed census-conserving endpoint:
  recomposedEndpoint := 7179/54 (recomposed conf way-point) + leptonTail-screen(census 2) + freeze-out-staircase(coloured @ quark edges)

## THE THEOREMS (production; every TYPE mentions banked derived Cut objects — not free-floating)
T1. recomposed_belowLambda_split : the below-Λ tail census-conserving parts sum to the full census
    (re-export census_completeness_closure over the running descent; the leptonic tail + freeze-out staircase = 16/3).
T2. recomposedEndpoint_closed : recomposedEndpoint = 7179/54 + bandScreen leptonCensus (mass0/mass1)
      + [16/9 + κ·((7/3)cutLog mbRatioDerived + (11/3)cutLog(bMass 0) − (5/3)cutLog(bMass 2))]
    (assembled from recomposed_conf_waypoint + leptonTail_screen_telescope + freezeout_summand_dissolved).
    A DETERMINED closed real of Cut — no free parameter (s=1/8 pinned, edges dissolved to banked logs).
T3. ★ WAY-POINT ADJUDICATOR (EW): the SAME pipeline reproduces the banked recomposed EW way-point 6923/54
    as a PARTIAL SUM (the below-v structure never moves it). recomposed_EW_waypoint re-exported through the assembly.
T4. ★ WAY-POINT ADJUDICATOR (conf): the recomposed conf way-point 7179/54 is a PARTIAL SUM of recomposedEndpoint
    (the below-Λ tail is appended, never moves 7179/54). recomposedEndpoint − (tail) = 7179/54.
T5. census-conserving DIRECTION: recomposedEndpoint sits STRICTLY BELOW the additive-to-lepton-edges variant
    (coloured freezes out at quark edges, fz bounded/small, vs coloured screening all the way to lepton edges).
    Uses removal_pos genre / freeze-out bounded.
T6. teeth: s=1/8 genuinely bites (7/18 ≠ 0); the two below-Λ parts distinct (10/3 ≠ 2); the freeze-out determined
    summand genuinely uses the confinement rung (16/9 ≠ 0); the endpoint ≠ the recomposed conf way-point (tail nonzero).
T7. capstone (welded, non-hollow).

## GRADE (honest)
THEOREM-EXACT NOW: the closed-form assembly, the way-point reproductions (adjudicators), the direction, the teeth.
ROUTE-NOT-YET-FOUND (named, handed to ONE successor = D7 full attestation → re-narrow → 1/α(0) restate ≥15 digits):
  the ≥15-digit certified bracket AS 1/α(0), read through the banked instrument ladder. THIS node supplies the
  recomposed census-conserving CLOSED FORM the numeral evaluates; the numeral is the successor's.
The measured 1/α(0)≈137.036, recomposed≈136.33, additive≈137.56 are REMOVABLE ORIENTATION PROSE ONLY. NO FIT.

## NUMERICS (scratch, REMOVABLE PROSE — never a proof input, never a fit target)
  recomposed EW way-point   6923/54 = 128.204
  recomposed conf way-point 7179/54 = 132.944
  + leptonTail screen(census 2, m0/m1~3475) ~ 1.730
  + freeze-out staircase (dissolved, coloured @ quark edges) ~ 1.65 [bracket 1.60..1.70 over banked edge logs]
  = recomposed census-conserving endpoint ~ 136.33
  (additive-to-lepton variant ~137.56; measured ~137.036 — all PROSE)

## COST BUDGET / KILL (W9)
All proofs are `rw` through banked equalities + `norm_num`/`linarith`/`ring` on rationals — no heavy normalizer,
no coordinate expansion. KILL: if any single obligation needs >60s or a monolithic ring on unfolded cutLog, STOP,
decompose into a banked sub-lemma. Import DAG already confirmed to compile (Probe1, EXIT 0).

## FORK GUARD
Exactly ONE forward successor: the D7 full completeness attestation over this recomposed assembly (directed child).
No generic SELECT (owner: generic SELECT before D8 banks is a rail violation).
