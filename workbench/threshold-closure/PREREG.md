# PREREG — THRESHOLD CLOSURE CAMPAIGN (owner-locked line: the seam reading, driven to
# theorem or death) — FROZEN 2026-08-08, head ead20e91
Work dir: workbench/threshold-closure/. Recon phase writes ONLY here.

## THE READING UNDER TEST (frozen verbatim, the owner-approved line)
N574's per-threshold over-counts are not errors: at EVERY decoupling seam the delta is
EXACTLY the just-decoupled flavour's census term — the signature of the fold's engine
(self-look-back → MISS → CLOSURE covering the miss) appearing at the descent's seams.
Decoupling is a MISS (the descent's local look below the edge no longer sees the
flavour); the fold's engine says a miss is never clean deletion — it is covered by a
persisting closure. The active-span count (5/3 etc.) is the LOCAL chart's section; the
old count (2 etc.) is chart + closure = the GLOBAL section. Their difference is the
descent's local→global gluing defect, and 1/α(0) is a GATHER-level object. The mirror
term was the chain including its own LOOK once; the threshold closures are the chain
including its own EDGES. Same One-axiom move, second appearance.

## WHAT MUST BE DERIVED (the one question; magnitude NEVER assumed)
From the banked cascade/miss/closure machinery + the banked removal frame, derive the
closure term generated at a decoupling seam:
  (Q1) EXISTENCE: does the banked engine force a closure at a descent threshold at all?
  (Q2) CONTENT: what census does the closure carry? (Hypothesis: the decoupled
       flavour's colouredCensusTerm — to be DERIVED, not assumed.)
  (Q3) ANCHOR: on which span does the closure screen (the span ABOVE the edge it
       decoupled from? the span below? the whole descent?) — this decides which log
       multiplies it.
  (Q4) WEIGHT: with what coefficient does it enter the gather (1? w = depthWeight?
       1/(1−w)? something else the engine forces)?
THE CHECK (only AFTER Q1–Q4 are frozen): do the forced (content × anchor × weight)
values match N574's three deltas (strange 1/3 on the light span, charm 4/3 on the charm
span, bottom 1/3 on the bottom span — grep overcount_bottom/charm/light for the exact
banked deltas)? MATCH ⇒ the old composition is the GLOBAL section, now derived, with
the seam law as its theorem. MISMATCH ⇒ bank whatever was forced; the endpoint is
whatever it then is. NO ENDPOINT ARITHMETIC before the Q1-Q4 verdicts are frozen in
the recon report.

## WHERE TO LOOK (the banked engine's instances — recon phase A)
The engine has appeared before. Enumerate its BANKED instances and extract the common
mechanism, then ask what that mechanism does at a threshold:
  1. THE MIRROR INSERTION (N586/N589/N679): the chain's self-look enters ONCE, weight
     w·(census)·κ·α² — forced by the unary law D = κ₀ + w·D. The template: a
     self-reference term FORCED into the total with derived weight.
  2. THE OBSTRUCTION (N585/N681): the gather's coherence deficit −(3/5)·κ — the gather
     failing to see itself coherently enters the endpoint ONCE, subtracted.
  3. THE CASCADE CLOSURES (the foundational stack): each fold level's miss is covered
     by the next level's structure — grep the foundational modules for the closure
     formalization (Fold.lean, the cascade arc, TowerGatherObstruction/gluingDefect,
     TowerGatherCocycle, TowerGatherPhysicalState gluesGlobally_iff — the LOCAL vs
     GLOBAL gluing machinery ALREADY BANKED in the Phase/gather lane).
  4. THE REMOVAL FRAME ITSELF (DecouplingCensusRemoval): read decoupling_removal's
     actual algebra — what happens to the removed content's screening in the banked
     equations. Does the frame REMOVE it from the descent's forward flow while its
     accumulated screening REMAINS in the total (i.e., the ledger keeps what the chart
     forgets)? The distinction between the descent's STATE (local) and the descent's
     ACCUMULATED SCREENING (global) may already be in the banked algebra unnoticed.
  5. THE GLUING-DEFECT LANE (TowerGather*): the repo HAS a formal gluing-defect object
     (gluingDefect, barMultDefect, cocycle identities, gluesGlobally_iff). The seam
     reading predicts the threshold deltas ARE gluing-defect instances. Can the banked
     gluingDefect machinery be instantiated on the descent's cover (the spans as charts,
     the thresholds as overlaps)? This is the most fold-native formalization route:
     the descent as a presheaf over the scale line, the composition as its global
     section, the deltas as the obstruction cocycle.

## THE CAMPAIGN PHASES
A (recon, delegated, ~2 sessions): extract the engine's mechanism from instances 1-5;
  answer Q1-Q4 as a DERIVATION SKETCH with every step pinned to a banked theorem or
  honestly marked NEW-DERIVATION-NEEDED; freeze the verdict BEFORE any endpoint
  arithmetic; then and only then run the check against N574's deltas.
B (theory nodes, worker lanes): bank the seam law — the closure existence/content/
  anchor/weight as theorems (the N589-style instantiation on the endpoint), plus the
  gather-level composition theorem (the global section = local sections + closure
  cocycle).
C (the landing): the gather-level endpoint + fixed point, computed from whatever B
  forced. If it reproduces the old brackets, the 137.0359990778 landing returns as a
  DERIVED gather-level object with the seam law as its provenance — the auditor's
  question answered at its root. If not, the new number stands.

## KILL CONDITIONS (frozen)
K1: if the banked engine provably does NOT apply at thresholds (the removal frame's
algebra genuinely deletes accumulated screening and no gluing-defect instantiation
exists), the reading DIES; the corrected 136.21 stands; report plainly.
K2: if Q1-Q4 force values that MISMATCH N574's deltas, the reading as stated dies;
bank what was forced instead.
K3: any step that selects among candidate laws by where the endpoint lands = instant
kill of that step (NO-FIT absolute; the measured value appears in NO inference).

## OWNER DIRECTIVE (binding): no drift. No generic sweeps. No standard-field reaches.
This line, driven to theorem or death.
