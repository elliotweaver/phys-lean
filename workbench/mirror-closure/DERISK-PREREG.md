# DE-RISK PREREG — THE SELF-INSERTION LAW (frozen BEFORE any landing computation)
# Rule of this session: NO candidate is evaluated against the experimental cluster until
# EVERY target-independent discriminator has been run. The landing is computed ONCE, at the
# end, for all surviving candidates simultaneously. If the surviving law's coefficient is
# NOT 16/9, then N586-N588's mirror coefficient is a fit and gets retracted.

## THE QUESTION
The fixed-point TYPE is motivated (the One) and the α² SCALE CLASS is forced (elimination
theorems: α¹ off by 46-1700x, α³ by 137x — target-independent, already banked knowledge).
What is NOT derived: the coefficient c in  1/α = F0 − c·κ·α².
A law must DERIVE c from banked structure, with each factor having a banked home.

## CANDIDATE LAWS (enumerated before testing; no additions after landing computation)
L1  LOOK-AS-NEW-CENSUS-ENTRY: the look is one more entry in the census, weight = its own
    squared coupling. Term: 1·κ·α².  c = 1.
L2  LOOK-SCATTERS-OFF-CENSUS AT ONE DEPTH: the self-look attaches its two ends (α²) to the
    full charged census (16/3), one re-entry depth down (w = 1/3), one return (κ).
    Term: (16/3)·w·κ·α².  c = 16/9.
L3  LOOK-SCATTERS-OFF-CENSUS, RESUMMED DEPTH: as L2 but the depth tower is hand-resummed
    1/(1−w) = 3/2 instead of single w. Term: (16/3)·(3/2)⁻¹?? — no: (16/3)·(3/2)·κ·α²?
    Two variants: c = 8 (16/3·3/2) and c = 16/3 (no depth factor at all).
L4  LOOK-THROUGH-OBSTRUCTED-GATHER: the self-look passes the gather, so it carries the
    coherence deficit: (3/5)-weighted variants. c = 3/5, or (16/3)·(3/5) = 16/5.
L5  UNARY LAW APPLIED TO THE ENDPOINT ITSELF: D = κ₀ + w·D with the endpoint as content:
    correction = w·κ·α²·(endpoint-census)? Degenerates to L2 or produces w²: c = 16/27.

## TARGET-INDEPENDENT DISCRIMINATORS (run these FIRST, in this order)
D-A  BANKED-HOME CHECK: for each factor of each law, find the banked theorem/def where that
     factor plays EXACTLY that role. A factor with no banked home kills the candidate.
     Specifically resolve: (i) is the base's 16/9 gem semantically (16/3)·w (census-through-
     one-weight — precedent for L2's composite) or κ·2·(8π/3) (unrelated numerology)?
     Read BaseNormalFormAudit + reattest_base_is_resummed + the D2 census files.
     (ii) does ANY banked κ-term carry a bare w factor (precedent for L2/L5's w-on-κ), or
     are all banked κ-terms log/way-point shaped (which would make w-on-κ NOVEL = flag)?
D-B  NO-DOUBLE-COUNT: the mirror term must be content NOT already in F0. Check cInner's
     4 weights (2, 7/3, 11/3, 5/3) and the base decomposition: does any existing term
     already carry census×w content that a self-insertion would duplicate?
D-C  SCOPE (the One): the law must explain why ONLY the total determination self-inserts
     (banked obstruction/base/screening are exact with NO mirror corrections of their own).
     A law that corrects sub-terms contradicts banked exactness. State each law's answer.
D-D  REPRODUCTION: the law's grammar applied to the three EXISTING term classes must
     reproduce their banked shapes (screening = census×κ×log; obstruction = deficit×κ;
     base = κ-free). Any law that mis-parses an existing term dies.

## ONLY THEN: THE SINGLE LANDING COMPUTATION
Solve 1/α = F0 − c·κ·α² for every surviving candidate. Report all. No re-runs, no new
candidates after seeing numbers. Survivor ∧ lands = candidate derivation (still needs the
w-factor formalized in Lean). Survivor ∧ misses = the law is the prediction and 16/9 is
RETRACTED. No survivor = route-not-yet-found; retract the coefficient, keep type + scale class.
