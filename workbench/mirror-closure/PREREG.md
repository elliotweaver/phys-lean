# PREREG — MIRROR CLOSURE PRODUCTION PORT (frozen before compute)
# Owner-directed; manager-as-prover (worker retired). Two-node chain.

## NODE 1 — GatherCoherenceObstruction (the −1/(5π) term)
GOAL: bank the octonion bracketing-coherence obstruction as theorems over the
banked CD octonions O ℚ:
  (A) per-triple coherence: for basis units, ⟨(e_i e_j)e_k, e_i(e_j e_k)⟩ = ±1,
      +1 exactly on the 42 Fano-ordered triples, −1 on the 168 others — PROVED
      against the banked CD multiplication (no free-floating table).
  (B) the mean over the 210 ordered distinct triples = −3/5 (exact ℚ).
  (C) the obstruction term: (3/5)·kappaLeading = 1/(5·cutPi) (exact Cut identity).
  (D) the obstructed endpoint: obstructedEndpoint :=
      recomposedEndpointDressed − (3/5)·kappaLeading, with certified bracket
      inherited from N584 dressed bracket + banked inv_cutPi_bracket_ext8:
      expected ≈ [137.03600912247, 137.03600912249] (verify exact rationals at
      lock time from the two banked brackets — NO new instrument needed).
  TEETH: coherence not all +1 (non-vacuity: a −1 witness); obstruction strictly
      positive; obstructedEndpoint strictly below dressed endpoint; bracket
      two-sided and width < 1e-10.
GO/NO-GO: GO iff CD-basis products reduce by decide/rfl in < 90s per small batch.
KILL: if kernel cannot evaluate CD products in reasonable time → INSTRUMENT
      failure → route via structure-constant lemmas proved individually (rfl per
      product, 42+168 lemmas scripted), NOT a theory failure.

## NODE 2 — MirrorFixedPoint (the α² closure)
GOAL: bank the mirror equation over Cut:
  (A) mirrorCoeff := chargeTraceDepth · depthWeight, with mirrorCoeff = 16/9
      (from banked generation_charge_trace 16/3 and depthWeight_eq 1/3).
  (B) the grammar selection layer (ℚ-decidable): term-space {census, wPow, aPow},
      legality predicates anchored to banked principles (completeness/unary/
      return), selection theorem: unique legal term = (16/3, 1, 2); anti-vacuity
      controls (2, 10/3, 16/3-no-w, two-w, 3/5 each fail a named predicate).
  (C) the fixed-point bracket: with F0 := obstructedEndpoint (Node 1) and
      certified F0 bracket [flo, fhi], prove existence of α in the rational
      interval [aL, aR] (aL = 1/13703599909, scaled) with
      1/α = F0 − mirrorCoeff·kappaLeading·α² — via monotonicity: define
      h(a) := a·F0 − mirrorCoeff·kappaLeading·a³ − 1 ... equivalently prove
      sign change of the cubic residual at the two rational ends using the
      banked F0/κ brackets. Uniqueness on the interval by strict monotonicity
      of the residual (derivative sign via bracket arithmetic).
  (D) the solution bracket: 137.0359990777 ≤ 1/α* ≤ 137.0359990779 (certified).
  TEETH: mirror term strictly positive; fixed point strictly below F0; strictly
      above F0 − upper(mirror); each negative-control coefficient lands outside
      the solution bracket (five exclusion theorems); grammar predicates bite.
GO/NO-GO: GO iff Node 1 lands and the cubic-residual sign change certifies with
      existing bracket arithmetic (no new transcendental instrument).
KILL: if the Cut-level IVT application fails on the cubic → fall back to
      banking the ℚ-layer (grammar + coefficient + controls) + the certified
      INTERVAL statement (F0 − upper ≤ 1/α* ≤ F0 − lower) and hand the exact
      fixed-point existence to a directed successor. Never assert at grade.

## NO-FIT DECLARATION
Measured 1/α(0) ≈ 137.036 appears ONLY as removable orientation prose. No
empirical number in any statement or proof. The −3/5 falls out of the banked
CD multiplication; 16/9 falls out of two banked theorems; the brackets fall
out of banked instruments. The grammar's legality predicates are stated as
definitions whose anchors are named banked theorems; the honest grade of the
grammar layer (extracted-not-yet-derived) is declared in the docstring.

## COSTUMES
Node 1: GatherCoherenceObstructionVacuityCostume — a fake coherence table with
  all +1 must FAIL the mean theorem (kernel rejects −3/5 for it).
Node 2: MirrorFixedPointVacuityCostume — the lepton sub-census 2 in place of
  16/3 must FAIL the selection theorem and land outside the solution bracket.
