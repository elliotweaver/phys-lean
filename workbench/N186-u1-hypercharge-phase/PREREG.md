# N186 PREREG — THE U(1) HYPERCHARGE FACTOR (the so(2) ≅ u(1) phase-rotation algebra)

Frozen BEFORE compute. The third SM gauge factor's rank, complementary to the banked
colour `SU(3) ⊂ G₂ = Der(O)` (dim 14, N42/N20) and weak-isospin `SU(2) = Der(H)` (dim 3,
N184/N185).

## THE TARGET (front (i), measured-first)
`Dbl ℚ` is the COMPLEX-UNIT rung (one CD doubling below `H ℚ`), `finrank ℚ (Dbl ℚ) = 2`
(banked N19 `finrank_dbl_eq_two`). It is COMMUTATIVE, so its INNER derivations VANISH
(`ad_q = 0`) — the U(1) is NOT an inner-derivation story (unlike su(2)/g₂). The correct
theory-faithful object is the **skew-rotation algebra of the Born self-overlap form**:
`so(Born on Dbl ℚ) = so(2) ≅ u(1)`, the 1-dimensional abelian algebra GENERATED by the
DERIVED complex unit `J` (the fold-root `mulJ_isFoldRoot`, N2), exactly the phase rotation
`z ↦ J·z`.

GO (the node lands iff ALL hold):
  - `bForm : BilinForm ℚ (Dbl ℚ)`, `bForm v w := (v * star w).re`, is SYMMETRIC and
    NONDEGENERATE (anisotropic: `bForm v v = v.re² + v.im²`, sum of 2 ℚ-squares).
  - `L_J : Module.End ℚ (Dbl ℚ)`, `z ↦ J·z`, is SKEW-ADJOINT for `bForm` and NONZERO
    (`L_J 1 = J ≠ 0`).
  - `finrank ℚ bForm.skewAdjointSubmodule = 1` — via the banked so(n)-bound
    `finrank_skewAdjoint_le_choose` (N28, `(finrank V).choose 2 = (2).choose 2 = 1`) for the
    upper bound, and the nonzero skew generator `L_J` for the lower bound `1 ≤ dim`.
  - ABELIAN signature: a 1-dim Lie algebra is necessarily abelian = the defining-signature of
    `u(1) ≅ so(2)` (the dim-1 analog of `dim Der(O)=14`+simple for g₂, `dim Der(H)=3`+non-abelian
    bracket for su(2)). `dim = 1` IS the FULL classification-sufficient signature (only one
    1-dim Lie algebra up to iso).

NO-GO (block / reframe, do NOT bank):
  - `bForm` fails symmetric or nondegenerate over `Dbl ℚ`.
  - `L_J` is not skew, or the dimension comes out ≠ 1, or the statement is vacuous (`dim = 0`).

KILL (W9 instrument wall): 90s per single obligation, 150s per whole file. Every obligation is
light finite algebra (mirrors `gBil`/`finrank_skewAdjoint_le_choose`, ~instant). If any one
obligation exceeds 90s → instrument wall → MEASURE/DECOMPOSE, do NOT inflate maxHeartbeats.

## W4.5 SUFFICIENCY VERDICT
OPENS genuinely-new structure: the THIRD and final SM gauge factor's rank, on the `Dbl ℚ` rung
NEVER banked. NOT a re-pin of su(2) (different rung, different object: skew-rotation vs inner
derivation) and NOT a g₂ re-pin. Reuses the EXISTING banked lever `finrank_skewAdjoint_le_choose`
(W2: import-and-USE a banked theorem) applied to a NEW object — this is the honest way the
so(n)-bound, already built for the d∈{2,3,4,5} exclusion, pays off again at the smallest rung.

## MOAT
The division-algebra-physics field POSITS the complex line ℂ and reads `U(1)` phase off by hand.
Here `Dbl ℚ` is the cascade's OWN FIRST doubling (N2, the forced complex unit), `J` is the DERIVED
fold-root, and `dim u(1) = 1` DESCENDS from the so(n)-bound (N28) + `finrank_dbl_eq_two` (N19) — NO
posited u(1)/so(2)/U(1)/Lie group, NO Mathlib ℝ/ℂ as content (ℚ is the coefficient field the
finrank/BilinForm statements are WRITTEN IN, the N6/N16/N42/N184/N185 precedent; the OBJECT is the
derived complex-unit rung).

## PHYSICS-WORDS-REMOVABLE
Delete "hypercharge / U(1) / so(2) / phase / gauge": what remains is the pure statement that the
skew-adjoint operators of the self-overlap form `(v,w) ↦ (v·star w).re` on the 2-dimensional
algebra `Dbl ℚ` form a 1-dimensional space, generated by multiplication by the element `J` with
`J·J = −1`. No theorem statement needs a physics word.

## COSTUME (anti-vacuity, W8)
C218 — a WRONG dimension claim forcing `1 = 186` in ℕ: the dimension `finrank ℚ
bForm.skewAdjointSubmodule` GENUINELY equals `1`; the WRONG claim (the phase algebra trivial /
dim 0 / the wrong rank) forces `1 = 186`. DISTINCT from the battery (C215 1=183, C216 2=184,
C217 3=185 — use 1=186).
