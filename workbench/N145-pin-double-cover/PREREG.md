# N145 PREREG — THE PIN GROUP + THE SURJECTIVE DOUBLE COVER WITH ORDER-2 KERNEL

FROZEN before compute. KILL classifies a fight as INSTRUMENT failure, not theory.

## MEASURE-FIRST FINDING (the decisive reframe — THE ONE LAW)
The ticket floated "MEASURE whether {±1} = ker coverMap lifts to MonoidHom.ker".
MEASURED: this is **FALSE on CoversSub**. `coverMap : CoversSub →* bvIsomLinMonoid` has
`CoversSub` = ALL units covering some isometry. Every nonzero scalar unit `algebraMap λ`
(λ : Cut, λ ≠ 0) is central in CliffC and grade-0 (so `involute(algebraMap λ) = algebraMap λ`),
hence `involute(λ)·ι v·λ⁻¹ = λ·λ⁻¹·ι v = ι v` — i.e. EVERY scalar unit covers `id`. So
`ker coverMap` on CoversSub is the FULL scalar group `Cutˣ`, NOT `{±1}`. The literal candidate
is wrong because CoversSub is the scalar-bloated domain.

THE THEORY-NATIVE FRAME: the honest double cover's DOMAIN is the **Pin group** = the submonoid
GENERATED BY THE REFLECTION UNITS `cliffUnitOf a` (matching N132's `houseHolderMSet`), not all
units. On the Pin group the genuine order-2 kernel element `-1` is a REFLECTION-SQUARE
(`(cliffUnitOf mSpaceUnit)² = algebraMap (QvC mSpaceUnit) = algebraMap(-1) = -1`), a genuine
Pin element — and the restricted cover is STILL surjective onto SO⁺(1,9). This OPENS the honest
two-to-one structure N136 (set-level on arbitrary units) and N144 (cover on bloated CoversSub)
did not carry: the cover restricted to the Pin DOMAIN as a surjective MonoidHom with a
nontrivial order-2 `MonoidHom.mker`.

## TARGET (N145) — bank, foundations-only over the derived ℝ Cut + terminal O Cut + CliffC
Production: `Phys/Algebra/LorentzContinuumCliffordPinGroup.lean`.
  - `pinGenSet`        : Set CliffCˣ := { u | ∃ a (ha : BvC a a ≠ 0), u = cliffUnitOf a ha }.
  - `PinSub`           : Submonoid CliffCˣ := Submonoid.closure pinGenSet (the reflection-unit
                          monoid — the honest double-cover domain).
  - `cliffUnitOf_mem_pinSub` : each reflection unit ∈ PinSub (subset_closure).
  - `pin_covers_word`  : ∀ g ∈ closure houseHolderMSet, ∃ u ∈ PinSub, CliffCovers u g
                          (closure_induction landing IN PinSub).
  - `pinSub_le_coversSub` : PinSub ≤ CoversSub (each generator covers houseHolderM a).
  - ★★ `coverMapPin`   : PinSub →* bvIsomLinMonoid := coverMap.comp (Submonoid.inclusion …) —
                          THE PIN DOUBLE COVER as a MonoidHom on the honest domain.
  - ★★ `coverMapPin_surjective` : surjective ↠ SO⁺(1,9) (Pin group, not all units, covers all).
  - ★ `negOne_mem_pinSub` : (-1 : CliffCˣ) ∈ PinSub — the kernel element is a REFLECTION SQUARE
                          `(cliffUnitOf mSpaceUnit)² = -1` (genuine Pin element, new vs N136).
  - `negOne_mem_mker_coverMapPin` : ⟨-1,_⟩ ∈ MonoidHom.mker coverMapPin (covers id).
  - ★ `coverMapPin_mker_nontrivial` : (⟨-1,_⟩ : PinSub) ≠ 1 ∧ ∈ mker — the kernel is GENUINELY
                          order ≥ 2 at the group-map level (the two-to-one datum).
  - W8: `coverMapPin_boost_apply` : the concrete boost (5/3,4/3) is the image under coverMapPin
                          of a Pin covering unit (surjectivity realized on a real boost).

## GO / NO-GO
GO if: all decls compile foundations-only ({propext, Classical.choice, Quot.sound}); costume
C177 (1=145) bites a WRONG Pin/double-cover/kernel/reflection-square claim; surjectivity +
nontrivial-kernel both proved; physics-words-removable holds. Then bank + child N146 (the EXACT
kernel mker coverMapPin = {1,-1} — the injectivity-mod-center direction, a genuine W1 node).
NO-GO if: the Pin-restricted surjectivity or `-1 ∈ PinSub` resists after the trunk reframe →
honest W1 dissolution ticket + block; do NOT weaken to coverMap-on-CoversSub re-pin.

## KILL (W9 instrument budget)
All obligations are closure-induction + Units.ext + ι_sq_scalar scalar algebra — NO tsum, NO
exponential, NO coordinate brute. KILL = 90s / single obligation. If any obligation exceeds 90s
or the probe RSS climbs into GB, STOP: it is an INSTRUMENT failure (decompose/reframe), not a
theory failure. Expect a LIGHT node (~10-20s probe, cached production build).

## ANTI-DRIFT
NO posited Pin/Spin group, NO posited cover, NO posited kernel, NO Mathlib ℝ/ℂ as content, NO
bridge. The Pin group is DERIVED (Submonoid.closure of the cascade's own reflection units), the
cover is the banked twisted adjoint, the kernel element is a DERIVED reflection square.
