# N363 — arc-E E7 (structural core): THE SINGULARITY = THE CLOSURE-SURFACE CAP — the fold's return-section degenerates at a SINGLE smooth tip, finite depth, no chaotic edge

## SELECT (fresh worker, theory-native FIRST — QUIET DRIFT + anti-bullshit gate)

**MEASURED reality (verified against the actual bank, NOT the ticket's framing):**
- The C-arc "cosmology" capstone (N338 `CosmologyContactConsolidation`) banks the generation-space
  RESOLUTION OF IDENTITY + ν masses + sterile DM — **NOT** a big-bang / Kasner / closure surface.
  The SEED's "cosmology already owns the answer" is **NOT backed** by the bank. VERIFIED by grep +
  reading the file in full.
- **NO banked radial Kretschmann profile** K(r)=48M²/r⁶ and **NO K(r_cross)=1**. The banked curvature
  is `bornCurv` (N349) — a weak-field Born-self-overlap scalar on `MetricEnd`, no radial coordinate.
  Asserting a numeric "K=1" would be G1 fishing (the 2/9 death).
- The banked `eucPlanePoint` (N360) is the near-horizon **Euclidean** section; its tip ρ=0 is the
  HORIZON cap, not the r=0 interior singularity. The full nonlinear interior metric was CHILDED at
  N349 (no `STVC` topology; heavy W1 build).

**Conclusion:** the literal E7 (K(r_cross)=1 on the interior metric, single Kasner leg, cosmology
cross-tie) is genuinely gated on machinery that is not banked → the forced beat that IS grounded on
banked objects is the STRUCTURAL CORE; the numeric/interior-metric/cross-tie is a decompose-remainder.

**Theory-native hook (fold-causal):** the theory has NO true singularity — the classical divergence is
a COORDINATE ARTEFACT, the SAME divergent-static-coordinate face N360 (Euclidean closure) and N362
(Page turnover) already resolved. The fold's RETURN SURFACE (the derived Born turn `azimuthalTurn =
2·cutPi`, N328) caps at a SINGLE smooth tip: the angular coordinate degenerates EXACTLY at the tip ρ=0
(all directions collapse to one point — ONE closure, no cascade of distinct oscillating directions =
the theory-native "single leg, no BKL chaos"), and is FAITHFUL off the tip. And the interior
information depth is FINITE (banked N361/N362 bit-count `N·s₀`) ⟹ no infinite-curvature singularity.
At the tip the section is angular-BLIND — the fold's SELF-BLINDNESS reappearing at the closure center
(one cause, many terminations) — flagged mechanism-grade.

**Anti-bullshit gate:**
- STANDARD version: the Schwarzschild r=0 curvature singularity where K=48M²/r⁶ → ∞ (a physical
  infinity); the BKL chaotic mixmaster approach (infinitely many oscillating Kasner epochs).
- What the THEORY does: NO physical infinity — the classical divergence is a coordinate artefact (the
  SAME face N360/N362 resolved); the fold's return-section CAPS at a single smooth tip (degeneration
  characterized to EXACTLY ρ=0, `section_constant_iff_tip` — genuinely NEW, N360 only showed
  closure/non-closure under angular shifts at fixed ρ, never characterized the degeneration locus);
  ONE closure point reached by all angular approaches (no cascade); FINITE depth ⟹ no true singularity.
- Words-removable: delete singularity/interior/horizon/Kasner/BKL/black-hole → for the derived-Cut map
  `θ ↦ (ρ·cutCos θ, ρ·cutSin θ)`: it is angular-constant IFF ρ=0; at ρ=0 the image is the single point
  (0,0); it is angular-faithful at ρ=1 (`cutCos 0 = 1 ≠ cutCos cutPi = −1`); it closes under one full
  `azimuthalTurn`; and the banked bit-count `bhEntropy M = (bhArea M/cellArea)·cellEntropy` is finite.
  Pure derived-Cut trig/ring/order algebra. NO physics word does logical work.
- Temptations REJECTED: (a) assert K(r_cross)=1 — REJECTED (G1, no banked radial curvature; would be
  fishing); (b) posit a cosmology big-bang closure surface + assert the cross-tie — REJECTED (not
  banked; C-arc banked ν structure); (c) claim the Euclidean tip IS the interior r=0 singularity —
  REJECTED (physically wrong: the Euclidean tip is the horizon; the identification is flagged
  mechanism-grade); (d) import Mathlib Real/trig as content — REJECTED (built on the banked derived Cut
  + cutCos/cutSin/cutPi); (e) empty re-export of N362's finite-depth bound — REJECTED (the NEW content
  is `section_constant_iff_tip`, the degeneration-locus characterization N360/N362 never banked).

## HONEST GRADE (mandatory flags — SEED §E7 "theorem-grade route"; MEASURED reality is more modest)

- **STRUCTURAL/THEOREM-GRADE (genuine theorems on the banked `eucPlanePoint`/`bhEntropy`):**
  `section_constant_iff_tip` (degeneration EXACTLY at the tip), `closureTip_eq_origin`/
  `closureTip_single` (single closure point), `closureSection_faithful_off_tip` (banked N360 contrast),
  `closureSection_closes` (banked N360 full-turn closure), `interior_finite_depth` (banked N361/N362
  finite bit-count). All foundations-only over the derived Cut.
- **MECHANISM-GRADE (flagged prose, NEVER asserted):** the identification "coordinate degeneration at
  the closure tip = the singularity resolved as a coordinate artefact" and "the tip is angular-blind =
  the fold's self-blindness at the closure center" — like N360's WHY-thermal and N362's min.
- **ROUTE-NOT-YET-FOUND (childed, NEVER asserted here):** the numeric K(r_cross)=1; the full nonlinear
  Lorentzian INTERIOR radial metric + its Kretschmann (childed at N349); the single-Kasner-leg on that
  interior metric; the cosmology big-bang closure-surface cross-tie (needs a banked cosmology closure
  surface — C-arc built ν structure, not a big-bang surface). CHILDED as the decompose-remainder.

## MEASURE (W9)

Probe `probe.lean` (7 theorems incl. `section_constant_iff_tip` iff) type-checked clean: user 2.6s
(19s wall = olean load). No instrument-cost risk. `Cut` is `LinearOrder + Field + IsStrictOrderedRing`;
banked `cutCos_zero`/`cutSin_zero`/`cutCos_cutPi`/`cutSin_cutPi`/`eucPlane_half_turn_conical`/
`eucPlane_closes`/`bhEntropy_cell_decomposition` all apply. All decl names collision-checked via grep.

## GUARDS

- G1 no asserted value (the tip/degeneration DERIVED; NO numeric K asserted — deferred/childed).
- G2 no empirical number in any proof (any measured curvature/temperature: removable prose only).
- G3 ρ/M symbolic; the degeneration locus / faithfulness dimensionless-structured.
- G4 derived not posited (degeneration = single tip PROVED; finite depth banked; the singularity-as-
  coordinate-artefact identification flagged mechanism, NOT a theorem).
- G5 physics-words-removable (delete singularity/interior/horizon/Kasner/BKL → pure derived-Cut trig).
- G6 foundations-only ⊆ {propext, Classical.choice, Quot.sound}; no sorry/axiom/native-decide/
  maxHeartbeats-raise. Built on the banked derived Cut + eucPlanePoint/cutCos/cutSin/cutPi/bhEntropy;
  NO Mathlib Real/trig as content.
- W8 non-vacuity: `closureSection_not_degenerate_everywhere` — a WRONG "singular everywhere / BKL
  never-faithful" mis-reading (the section degenerate at ρ=1 too) FAILS; costume C388 pins it.

## ONE SUCCESSOR (decision at finalize)

Directed decompose-remainder child toward E8 (inflation: the plateau/α-attractor class forced by
exclusion, n_s=1−2/N, candidate-grade — uses banked arc-B scales + D1 trig) — because the E7 numeric
(K=1) genuinely requires the childed full nonlinear interior metric (a large build), so the productive
forward beat is E8. The E7 numeric/interior-metric remainder is NAMED in the handoff, not silently
dropped. Fork check: exactly 1 non-terminal successor on the chain tail.
