# N69 PREREG — CONCRETE EIGENBASIS-EXISTENCE WITNESS (the existence lever)

Frozen BEFORE compute. KILL classifies a fight as INSTRUMENT failure, not theory.

## NODE (specified against what the chain ACTUALLY produced — ROADMAP §N69 frontier)

N68 banked the GENERAL rank-n spectral operator `specOpN l u := ∑ i, (l i)•projC (u i)`:
the diagonal `Σᵢ λᵢ Pᵢ` and its `cutSqrt`-per-eigenvalue square root hold at EVERY finite
rank GIVEN a mutually-EvC-orthonormal eigen-family. The HEAVY group-manifold remainder is the
FULL n-dim spectral EXISTENCE (an ARBITRARY EvC-self-adjoint operator HAS such a family) +
global polar/KAK assembly + connectedness + reverse KAK surjectivity + Spin(9)→SO(9) exhaustion.

MEASURE-FIRST priority (i) — the cleanest concrete entry to the existence content:
exhibit a banked self-adjoint operator GIVEN IN COORDINATE (NON-EIGEN) FORM as `specOpN c u`
for an explicit EvC-orthonormal family `u`, so N68 `specOpN_op_sqrt` reads off `√(g)` for an
operator NOT presented in eigen-form. This is the genuine lever from "the diagonal law holds at
every rank" (N68) to "an arbitrary operator IS in that diagonal form" (the existence content).

N64/N65 did this for the PURE (t,x) plane (txOp). N67's mixA/mixB couple time↔octonion but the
spectral object was BUILT from projections (already in eigen-form). The NEW content: a
TIME↔OCTONION-MIXING operator written purely in COORDINATES (no projC), proven equal to
`specOpN c (mix3)` — the existence direction in a genuinely 10-dim-mixing sector.

## THE WITNESS (concrete, derived from banked objects)

`mix3 := ![mixA, mixB, mixW3]` (Fin 3): the banked time/octonion-real/space EvC-orthonormal frame
  (mixA=(3/5,0,(4/5)•1), mixB=(4/5,0,(−3/5)•1), mixW3=(0,1,0)).

For eigenvalues 9 (mixA), 4 (mixB), 25 (mixW3), the spectral operator specOp3 9 4 25 mixA mixB mixW3
acts on (t,x,v) — with r := gFormC(1,v) the octonion-real part — as the COORDINATE map:
  txMix (t,x,v) := ( (29/5)·t + (12/5)·r ,  25·x ,  ((12/5)·t + (36/5)·r)•1 )
(verified by hand: 9·(3/5)·(3/5)+4·(4/5)·(4/5)=29/5; 9·(3/5)·(4/5)+4·(4/5)·(−3/5)=12/5;
 9·(4/5)·(4/5)+4·(−3/5)·(−3/5)=36/5; mixW3 gives 25·x; off-diag t↔octreal coupling 12/5 symmetric.)

CLAIMS to bank:
  D1  txMix : Module.End Cut STVC               — the coordinate (NON-eigen-form) operator.
  D2  txMix_apply                                — its coordinate action.
  D3  mix3 := ![mixA,mixB,mixW3]; mix3_orthonormal : EvC (mix3 i)(mix3 j) = δᵢⱼ (9 cases).
  D4  txMix_eq_specOpN : txMix = specOpN ![9,4,25] mix3   — ★ THE EXISTENCE/DIAGONALIZATION:
        a coordinate operator IS in spectral diagonal form over the explicit mixing eigen-family.
        (route: = specOp3 9 4 25 mixA mixB mixW3 via specOp3_apply, then specOpN_eq_specOp3.)
  D5  txMix_op_sqrt : IsEvCOpSqrt (specOpN ![3,2,5] mix3) txMix   — ★★ √(txMix) READ OFF:
        N68 specOpN_op_sqrt on c=![9,4,25] (cutSqrt 9=3, 4=2, 25=5) through D4.
  (optional, if budget) txMixE l1 l2 l3 — the whole-family symbolic version = specOp3 l1 l2 l3 ….

## COSTUME C100 (must BITE; false numeric DISTINCT from all banked incl C99 36=64)

Claim `specOpN ![3,2,6] mix3` (WRONG mixW3 eigenvalue 6, genuine is 5) is an op square root of
txMix. Via D4 txMix = specOpN ![9,4,25] mix3; specOpN_comp gives (specOpN ![3,2,6] mix3)² =
specOpN ![9,4,36] mix3; specOpN_read at mixW3 (index 2) forces 36 = 25. **C100: `36 = 25`**
(distinct from C84 8=4 … C97 25=9, C98 49=9, C99 36=64). The wrong eigenbasis-existence/root claim
fails to compile, rejected at the math field.

## GO / NO-GO

GO if: D1–D5 compile clean foundations-only over the DERIVED Cut + O Cut, from banked
  specOp3/specOpN/projC/EvC/gFormC/cutSqrt + the banked mix frame; costume C100 bites 36=25.
NO-GO / reframe if: txMix_eq_specOpN does not close by the txOp_eq_specOp coordinate pattern.

## KILL (instrument budget — W9)

KILL = 30s wall per single obligation (matches N56–N68; those never approached it). If any one
lemma's elaboration exceeds 30s, it is an INSTRUMENT failure (W9): MEASURE the sub-obligation,
DECOMPOSE (octonion-component lemma separately), prefer structural reduction (specOp3_apply +
specOpN_eq_specOp3) over coordinate brute. NO inflated maxHeartbeats, NO native_decide, NO brute
`decide`/`ring` on a fully-expanded ground goal. Bank-as-you-go: productionize each lemma the
instant it compiles. Checkpoint ~50% turns; finalize (commit + child + complete) with turns to spare.

## STANDARD (the second gate)

UNBROKEN: every object from banked mixA/mixB/mixW3/specOp3/specOpN/projC/EvC/gFormC/cutSqrt +
  derived Cut/O Cut. No new posit. Fin 3 arity = substrate. Module.End/LinearMap = machinery.
COMPLETE: #print axioms ⊆ {propext, Classical.choice, Quot.sound}, audited vs olean + gate D6.
WORDS-REMOVABLE: delete "Lorentz/boost/spectral/eigen*/diagonal/orthonormal/mixing/existence/
  square-root/self-adjoint/octonion-real" → a theorem that the coordinate Cut-linear map txMix
  equals `specOpN ![9,4,25] ![mixA,mixB,mixW3]` and `specOpN ![3,2,5] mix3` squares to it. Pure math.

DECOMPOSE (W3): the FULL n-dim existence for an ARBITRARY self-adjoint operator + global polar/KAK
  assembly + connectedness + reverse KAK surjectivity + Spin(9)→SO(9) exhaustion → childed N70.
