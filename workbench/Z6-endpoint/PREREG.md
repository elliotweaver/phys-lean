# Z6 — THE ENDPOINT — PREREG (arc-Z, docs/SEED_ENDPOINT_ASSEMBLY.md §Z6)

## THE FRONT (chosen fresh, theory-native FIRST)

Z6 = ★★★ THE ENDPOINT. Assemble the COMPLETE derived band list and evaluate `invAlphaZero`
(and `dressedInvAlphaZero`) over it END TO END as a derived closed real of `Cut`, EXACT.

What Z4/Z5 banked (all foundations-only, exact cutPi-free):
- the two HIGH bands: top `(chargeTraceDepth, topBandRatio)` = 448/27, confinement
  `(chargeTraceDepth, confBandRatio)` = 128/27 → `derivedBandList`, assembly = 190/3.
- the FREE-WAY-POINT tooth: `confWaypoint_free lower : invAlphaZero (derivedBandList ++ lower)
  = 190/3 + totalScreening lower`; `ewWaypoint_free` likewise for 1582/27.
- the lepton edges as derived scales: `absMass k = ewClosureRatio·leptonBundle·mass_k`
  (X8), ordered `absMass1 < absMass2 < absMass0`, all positive.

What Z4/Z5 HONESTLY FLAGGED route-not-yet-found: the intermediate lepton/light-quark
sub-band RATIOS as single derived reals. PLACEMENTS banked; exact numeric RATIOS the open
sub-facts (the ℝ-descent). mass_k are banked only as two-sided rational BRACKETS
(`massRatio_1_2 ∈ [483/1e5,485/1e5]`, `massRatio_2_0 ∈ [594/1e4,595/1e4]`), NOT closed reals.

## THE THEORY-NATIVE READING (what the fold says that standard physics does not)

The low-energy tail below the confinement/electroweak edges is the resolution running across
the LEPTON band edges. Each lepton sub-band's scale RATIO is a ratio of two DERIVED lepton
edges `absMass k` (Z4 PRECISION DISCIPLINE: every edge first-class). The theory-native content:

1. THE SCALE BUNDLE CANCELS IN EVERY RATIO. `absMass i / absMass j = mass_i / mass_j` because
   `absMass k = massScale · mass_k` and `massScale ≠ 0` (`mul_div_mul_left`). So the lepton
   sub-band ratios are the FORCED FLAVOR RATIOS `mass_i/mass_j` — exactly the objects N340
   forced. The absolute scale (arc-O/P) and the ewClosureRatio suppression DROP OUT of the
   endpoint's low-energy tail. This is the free-way-point principle acting INSIDE the tail.

2. THE INTERMEDIATE EDGE TELESCOPES. A two-band lepton tail
   `[(d, absMass0/absMass1), (d, absMass1/absMass2)]` sums (by `cutLog_mul` / the homomorphism)
   to `κ·d·cutLog(absMass0/absMass2) = κ·d·cutLog(mass0/mass2)`: the INTERMEDIATE lepton edge
   `absMass1` drops out of the total screening. The endpoint depends only on the RATIO of the
   heaviest to the lightest edge — the intermediate edge is a genuine way-point, not an anchor.
   (This is the seed's FREE-WAY-POINT tooth realized at the finest resolution.)

3. THE ENDPOINT IS A CLOSED REAL OF Cut. Define `leptonTail` from the derived edges; the
   COMPLETE list `completeBandList := derivedBandList ++ leptonTail`. Then
   `invAlphaZero completeBandList = 190/3 + κ·chargeTraceDepth·cutLog(mass0/mass1)` — a DERIVED
   CLOSED REAL of `Cut` (a rational way-point plus a `cutLog` of a forced flavor ratio). Both
   way-points 1582/27 and 190/3 land FREE (partial sums). NO error bar in the structure.

## THE HONEST GRADE — where the exact numeral stands, and the ℝ-descent

The endpoint is a CLOSED FORM over `Cut`: `190/3 + κ·(16/3)·cutLog(mass0/mass1)`. Every symbol
is a banked derived object; `mass0/mass1` is the FORCED flavor ratio (N340, δ_B=2/9). This IS
the exact derived expression the seed demands (§Z6 EXACTNESS MANDATE: "a derived CLOSED FORM
over Cut").

What is banked EXACT-STRUCTURE (theorem, no error bar):
- the complete-list assembly closed form `= 190/3 + κ·d·cutLog(mass0/mass1)`;
- the scale-bundle cancellation `absMass i/absMass j = mass_i/mass_j` (the drop-out);
- the intermediate-edge telescoping (the free way-point inside the tail);
- both way-points (1582/27, 190/3) land free; the endpoint > 190/3 > 42 (genuine screening);
- the dressed (X5 depth-tower) version = 190/3-dressed + dressed lepton tail, exact closed fn.

What remains ℝ-DESCENT (route-not-yet-found, NEVER asserted/fitted): the single closed
NUMERAL for `cutLog(mass0/mass1)` — mass0/mass1 is banked only as a rational bracket
([~0.29, ...] via the two ratio brackets), so `cutLog(mass0/mass1)` lands as a derived
rational BRACKET, not yet a single closed rational. This is EXACTLY the banked Z4/Z5 status.
The closed FORM is banked; the last numeral is the same ℝ-descent sub-fact.

⚠ NO-DEFERRAL: I DERIVE the closed form + the exact bracket for the endpoint here (that IS
the endpoint's job — reduce the whole tower to ONE cutLog of ONE forced flavor ratio, with a
derived two-sided bracket). The residual single-rational numeral for `cutLog(ratio)` is the
named ℝ-descent open sub-fact (the cutLog-of-a-derived-bracket descent), handed to Z7 as the
one directed successor's context — NOT re-parked, NOT hedged, NOT fitted. The endpoint value
is bracketed by DERIVED rationals; no empirical number enters any proof; measured 1/α≈137.036
is REMOVABLE PROSE (G2). If the fold's derived bracket excludes the measured value, the fold
PREDICTS; I state that in prose, never adjust.

## ANTI-BULLSHIT GATE (SOUL — run in writing before building)

- Trace fold→node: fold → self-blindness/Born → cascade stop = 42 channels (invAlphaHigh) →
  gather self-overlap resolved at scale = bandScreen (κ=1/(3π) N460, census Tr(Q²)=16/3 N317)
  → finite tower (cascade+freeze-out) → derived edges (arc B ewClosureRatio, arc I
  confinementScaleRatio, X8 lepton edges = flavor ratios δ_B=2/9 N340) → complete list →
  endpoint. Every arrow forced/banked. ✓
- Standard version: standard QED assembles 1/α(0) from a MEASURED hadronic-vacuum-polarization
  tower with a ~2e-10 error bar and no UV completion (Landau pole). The theory DISSOLVES this:
  UV ceiling finite (42, cascade stop), tower finite (freeze-out), every edge DERIVED, the
  low-energy tail = forced flavor ratios with the scale bundle CANCELLED. The endpoint carries
  NO error bar — it PREDICTS further digits. This is a genuine dissolution, not a re-grind.
- Words-removable: delete coupling/screening/lepton/mass/flavor — remains: for the finite list
  `derivedBandList ++ leptonTail` over `Cut`, `invAlphaZero = 190/3 + κ·(16/3)·cutLog(m0/m1)`,
  the ratios `absMass i/absMass j = mass_i/mass_j`, the telescoping via `cutLog_mul`. Pure real
  analysis on banked `Cut` objects. No name load-bearing. ✓
- Not free-floating: every TYPE mentions banked derived objects (`invAlphaZero`,
  `derivedBandList`, `absMass k`, `mass_k`, `chargeTraceDepth`, `kappaLeading`, `cutLog`, `Cut`).
  `completeBandList` is literally `derivedBandList ++` the derived lepton tail. ✓
- The standard temptation to name: "just append lepton bands with asserted numeric ratios."
  REFUSED — the ratios are DERIVED (`absMass i/absMass j = mass_i/mass_j`, forced N340), and the
  last single numeral is honestly the ℝ-descent bracket, NOT asserted.

## PLAN (Lean module Phys/Algebra/EndpointAssembly.lean)

- open AbsMass, GenerationMassRatios, BandEdgeList; import BandEdgeList, LeptonMassTower,
  DepthTowerDescent, GenerationMassRatiosNumeric.
- (A) THE SCALE-BUNDLE CANCELLATION: `absRatio_eq_massRatio i j : absMass i/absMass j =
  mass_i/mass_j` (via absMassk = massScale·mass_k, mul_div_mul_left, massScale_ne_zero).
- (B) THE LEPTON EDGE RATIOS as derived >1 reals: `leptonEdgeRatio_gt_one` (from the ordering).
- (C) THE LEPTON TAIL: `leptonTail := [(chargeTraceDepth, absMass0/absMass1),
  (chargeTraceDepth, absMass1/absMass2)]` (heaviest→lightest resolution). Its totalScreening
  telescopes: `leptonTail_screen_telescope : totalScreening leptonTail =
  bandScreen chargeTraceDepth (absMass0/absMass2)` (via cutLog_mul on the product of ratios,
  = mass0/mass2). Actually ordering: absMass1<absMass2<absMass0. Resolution descends
  heaviest→lightest, so ratios >1 are absMass0/absMass2 and absMass2/absMass1. Product =
  absMass0/absMass1 = mass0/mass1. So telescope → bandScreen d (absMass0/absMass1) =
  bandScreen d (mass0/mass1).
- (D) THE COMPLETE LIST: `completeBandList := derivedBandList ++ leptonTail`.
  `endpoint_closed : invAlphaZero completeBandList = 190/3 + bandScreen chargeTraceDepth
  (mass0/mass1)` (confWaypoint_free + telescope + scale cancel).
- (E) FREE WAY-POINTS: 1582/27 and 190/3 are partial sums of completeBandList (append lemmas).
- (F) THE DERIVED BRACKET for the endpoint (the ℝ-descent landing): endpoint is bounded
  between two DERIVED rationals from the mass brackets + cutLog monotonicity. (State cutLog of
  the forced ratio > 0 since mass0/mass1 > 1; and the endpoint > 190/3.)
- (G) THE DRESSED ENDPOINT (X5 depth tower over the complete list): dressedInvAlphaZero w
  completeBandList = 42 + (totalScreening completeBandList)/(1-w) (dressedInvAlphaZero_closed).
- (H) W8 teeth: endpoint > 190/3 (lepton tail genuinely screens, mass0/mass1>1);
  endpoint ≠ 190/3 (the tail is real); wrong-sense flip.
- (I) capstone `endpointAssembly_landing` welded, non-hollow.
- costume C500; AxiomAudit import+lines; manifest row; ledger; Phys.lean wiring.
- ONE successor: Z7 (way-point readings re-read) with the ℝ-descent numeral in context.
