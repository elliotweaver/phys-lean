block = """  - **\u2605 \u00a7N190 LANDED (the FULL ISOSPIN BRANCHING OF THE `7` \u2014 `7 = 1 \u2295 3 \u2295 3` under the embedded
    `su(2)`).** N189 childed N190 to look WIDE beyond the singlet split. THE W4.5/W9 MEASURE-FIRST
    VERDICT (frozen in PREREG): of the wide fronts (centralizer of `su(2)` in `g\u2082` / complementary
    `su(2)`-action on the `6` / full `SU(3)\u00d7SU(2)\u00d7U(1)` product / hypercharge normalization), front
    (ii) the COMPLEMENTARY action on the remaining `6` MEASURED LIGHT + GENUINE \u2014 the full `1\u22953\u22953`.
    THE MECHANISM (W9.4 structure over expansion): the octonion rung `O \u211a = CD (H \u211a) = \u27e8H,H\u27e9` has TWO
    complementary coordinate embeddings of the quaternion rung \u2014 the OLD line `\u03b9O` (`a\u21a6\u27e8a,0\u27e9`) and the
    NEW plane `\u03baO` (`b\u21a6\u27e80,b\u27e9`, N189). The inner derivations act COMPONENTWISE (N6 `innerDeriv_apply`),
    so BOTH copies carry the adjoint action: `innerDeriv q (\u03b9O a) = \u03b9O (adHom q a)` (`innerDeriv_iotaO`)
    and `innerDeriv q (\u03baO b) = \u03baO (adHom q b)` (`innerDeriv_kappaO_full`) \u2014 generalizing N189's `e\u2084`
    special case. The imaginary quaternion subspace `ImH := ker(starLH+1)` (mirror of N22's `ImO` one
    rung down) has `finrank \u211a ImH = 3` (`finrank_ImH_eq_three`, rank\u2013nullity on `starLH+1` with range
    `\u211a\u00b71` via `range_starLH_add_one`+`reH_trace`, `finrank H = 4` banked N19). Then `ImO` (finrank `7`,
    N22) is the internal sup of THREE `su(2)`-invariant pieces (`fullBranching_sup_eq_ImO`): the OLD
    adjoint `3 = \u03b9O(ImH)`, the singlet `1 = \u211a\u00b7e\u2084`, the NEW adjoint `3 = \u03baO(ImH)` (each invariant via
    `invariant_iotaImH`/`invariant_kappaImH`, `adHom_mem_ImH`; the singlet fixed by N189), with
    dimensions `3 + 1 + 3 = 7` (`branching_dim_sum`). \u2605\u2605 THE BRANCHING IS COMPLETE: `7 = 1 \u2295 3 \u2295 3`.
    N190 banks (20 decls, `Phys/Algebra/DerivationRep7FullBranching.lean`, foundations-only
    `[propext, Classical.choice, Quot.sound]`, audited 20/20 vs the built olean, costume C222 `3 = 190`,
    gate D0\u2013D6 GREEN): `iotaO`/`iotaO_apply`/`iotaO_injective`/`kappaO_injective`,
    `innerDeriv_iotaO`/`innerDeriv_kappaO_full`, `starLH`/`starLH_apply`/`ImH`/`mem_ImH`/`reH`/`reH_trace`/
    `one_ne_zero_H`/`range_starLH_add_one`, `finrank_ImH_eq_three`, `adHom_mem_ImH`,
    `invariant_iotaImH`/`invariant_kappaImH`, `fullBranching_sup_eq_ImO`,
    `finrank_iotaImH`/`finrank_kappaImH`/`finrank_singletLine`, `branching_dim_sum`, and W8
    `action_on_iotaImH_ne_zero` (the embedded `su(2)` acts NONTRIVIALLY on the OLD adjoint `3` \u2014
    `adHom hI hJ = 2hK \u2260 0` \u2014 the two `3`'s are genuine nonzero invariant summands). THE MOAT
    (unbroken-strand novelty): the field reads `7 = 1\u22953\u22953` off the octonions by hand; here the `1` is
    the upper-doubling unit `\u27e80,1\u27e9` and the two `3`'s are the imaginary quaternions in the OLD line
    `\u27e8ImH,0\u27e9` and the NEW plane `\u27e80,ImH\u27e9` \u2014 the SAME doubling that stops the cascade (N2c) and embeds
    `su(2) \u21aa g\u2082` (N187). One cause (the doubling): the embedding (N187), the lower-rung intertwiner
    (N188), the singlet (N189), the full branching (N190). NO posited
    su(2)/su(3)/g\u2082/SU(2)/SU(3)/G\u2082/Lie-group/rep as content, NO Mathlib \u211d/\u2102 as content, NOT a re-pin of
    any dimension, NOT a re-statement of N189's componentwise split, NOT a juxtaposition, NO bridge.
    Childed N191.

  - **\u2605 \u00a7N190-superseded FORWARD FRONTIER (specify against what the chain ACTUALLY produced).** N190
    banked the FULL isospin branching of the `7` (`7 = 1 \u2295 3 \u2295 3`: the singlet `\u211a\u00b7\u27e80,1\u27e9` N189 plus the
    two adjoint `3`'s `\u03b9O(ImH)` and `\u03baO(ImH)` N190). The rung-inclusion tower `u(1) \u21aa su(2) \u21aa g\u2082` is
    COMPLETE (N187/N188), the three factors are pinned individually (N42/N20, N184/N185, N186), and
    the isospin branching of the `7` is COMPLETE (N189 singlet + N190 full `1\u22953\u22953`). \u26a0 W4.5
    SUFFICIENCY: do NOT re-pin any factor, do NOT re-state either rung, do NOT re-derive the branching
    by another invariant, do NOT re-state the `1\u22953\u22953` in another guise. The independent W1 fronts
    (MEASURE the smallest obligation of each BEFORE committing \u2014 W9 measure-first; \u26a0 ALWAYS grep/inspect
    what is already banked, W4.5; \u26a0\u26a0 look WIDE): **(i) the CENTRALIZER / NORMALIZER of the embedded
    `su(2) = \u27e8innerDeriv hI/J/K\u27e9` inside `g\u2082 = Der(O \u211a)`** \u2014 the commutant of the embedded generators
    in the banked `derivation` (N6/N187) \u2014 does it open the colour\u2013isospin commutant `su(3) \u2194 su(2)`
    relation, a genuine NEW `g\u2082`-subalgebra structure? \u26a0 NOTE the type split: `innerDeriv hX` lives in
    `derivation` (N6, over `\u2124`), while N42's stabilizer lives in `derivationLieQ` (N20/N42, over `\u211a`) \u2014
    MEASURE whether the centralizer is naturally posed in one or needs a bridge; \u26a0 a centralizer that is
    trivially the whole algebra / zero / abelian is VACUOUS (reject, W8) \u2014 MEASURE the actual commutant
    dimension first. **(ii) the FULL `SU(3)\u00d7SU(2)\u00d7U(1)` product / commuting-subalgebra structure** \u2014 is
    there a PROVED direct-sum / commuting-subalgebra structure assembling the three factors inside a
    single derivation algebra, or does it merely juxtapose the three banked factors (reject)? **(iii)
    hypercharge normalization / the electroweak `U(1)` embedding**, or a Yang\u2013Mills / a different
    unbanked physics joint entirely. MEASURE the smallest obligation of each before committing; REFRAME
    THROUGH THE TRUNK FIRST; a relation that is NOT a hom / vacuous / a juxtaposition / a re-pin must be
    REJECTED, not forced (W8/THE ONE LAW). NO posited su(2)/so(3)/su(3)/u(1)/SU(2)/SU(3)/U(1)/Lie group
    as content, NO Mathlib \u211d/\u2102 as content \u2014 DERIVE from the banked N49\u2013N190 + the derived \u211a/\u211d `Cut` +
    the Cayley\u2013Dickson tower + the three gauge-algebra factors + the COMPLETE inclusion tower
    (`isospin_intertwines_hypercharge` N188, `innerDeriv_bracket`/`struct_ij` N187) + the COMPLETE
    branching (`singlet_fixed_by_isospin`/`g2_moves_singletLine` N189, `fullBranching_sup_eq_ImO`/
    `branching_dim_sum` N190) + the inner-derivation engine `adHom`/`innerDeriv`/`liftEnd` (N6) + the
    `g\u2082`-irreducibility levers (`no_one_dim_invariant_ImO` N25, `ImO`/`imRep` N22).

"""
import io
with io.open("docs/ROADMAP.md", "r", encoding="utf-8") as f:
    lines = f.readlines()
# insert before line 7350 (1-indexed) -> index 7349
target = None
for i, l in enumerate(lines):
    if l.startswith("  - **\u2605 \u00a7N189 LANDED"):
        target = i
        break
assert target is not None, "anchor not found"
lines.insert(target, block)
with io.open("docs/ROADMAP.md", "w", encoding="utf-8") as f:
    f.writelines(lines)
print("inserted N190 block before line", target+1)
