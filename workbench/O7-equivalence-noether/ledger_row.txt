| N422 THE EQUIVALENCE PRINCIPLE + THE NOETHER CORRESPONDENCE (arc-O O7) — the two marquee laws of the derived (1,3) arena, DERIVED off the ONE banked N420 energy-momentum pairing `bForm4`, neither posited. Standard physics posits BOTH: the equivalence principle as an experimental coincidence (Eötvös) built into GR by hand, and Noether's theorem via variational calculus on a posited Lagrangian. THE THEORY DISSOLVES BOTH into properties of the one banked pairing. (a) THE EQUIVALENCE PRINCIPLE (m_inertial ≡ m_gravitational, no dial): the mass is a boost-invariant symmetric quadratic scalar of the momentum vector; on the inertial time–space plane the space of boost-invariant symmetric forms is ONE-DIMENSIONAL. `plane_uniqueness` proves any boost-invariant `p t²+2q tx+r x²` has `q=0, r=−p` by PURE ℚ Gram forcing from ONE rational boost `(5/4,3/4)` on the unit hyperbola — no ℝ, no second invariant. So `mass_channel_one_dim`: every invariant plane form `= p·massForm2` (`= p(t²−x²)`), and `massForm2` IS the diagonal of the banked `bForm4` on the plane (`massForm2_eq_bForm4_diag`) and the banked `Q4` with zero ℂ-rung (`massForm2_eq_Q4`). Consequences: `equivalence_principle` (two invariant mass forms have a STATE-INDEPENDENT ratio — the universal Eötvös ratio, never a per-body dial); ★★★ `ep_unique_channel` (an invariant form NORMALIZED to `1` at rest IS the mass form everywhere — the gravitational channel, once required boost-covariant AND rest-normalized, is FORCED to equal the inertial one, m_g = m_i, no freedom); ★★★ `ep_no_dial` (a "violation" — a boost-invariant, rest-normalized form differing from the mass form on some boosted state — is UNWRITABLE, the N372 no-dial shape; the EP "problem" assumes a ratio the invariant structure provably lacks). (b) THE NOETHER CORRESPONDENCE (conservation = the invariant forms of the forced symmetry): `noetherCharge P ξ := bForm4 P ξ`, the invariant contraction of the momentum vector with a translation generator. `noetherCharge_energy` (energy = the time-translation charge `= P.1`), `noetherCharge_momentum` (x-momentum = the space-translation charge `= −P.2.1`), banked N420. ★ `noetherCharge_conserved` (BOOST-INVARIANT — the conservation law, banked `bForm4_boost_invariant`), `noetherCharge_add`/`noetherCharge_smul` (LINEAR in the generator), ★★★ `noether_faithful` (the full set of translation charges DETERMINES the momentum vector — non-degeneracy) + `noether_charge_determines_state` (the charge map is injective). NO Lagrangian, NO variation. ★★★ THE ONE-STRUCTURE MOAT (`equivalence_and_noether`): both laws are faces of the ONE pairing `bForm4` — its DIAGONAL is the unique invariant (the mass — EP), its OFF-DIAGONAL pairings with the symmetry generators are the conserved charges (Noether). One structure, two laws. SELECT ran S1–S4 IN FULL (comments 365/366); the GOAL lens (after N420's "mass = the one (1,3) self-norm," the next beat is: is there a SECOND invariant to house a distinct gravitational mass? → no, the channel is one-dimensional) and the seed §O7 ALIGN (no redirect); the REFRAMING lens sharpened it into the never-banked one-pairing-two-laws unification (EP = Casimir uniqueness, Noether = invariant contraction, both `bForm4`). METHOD: ascent-flavoured (invariant/Casimir uniqueness), chosen over the gather side (nothing sharper here). | Emergent (depth 3) | ✅ (LANDED) | ★★★ THE EQUIVALENCE PRINCIPLE + NOETHER, DERIVED off one banked pairing, no posited coincidence, no posited Lagrangian, no bridge. `Phys/Algebra/EquivalenceNoether.lean` (namespace `Phys.Algebra.EquivNoether`), 22 decls foundations-only `[propext, Classical.choice, Quot.sound]` (independently axiom-audited 22/22 via workbench/O7-equivalence-noether/reaudit.lean against the built olean). `genForm2`/`massForm2`/`IsPlaneBoostInvariant` (the mass channel machinery), `massForm2_eq_Q4`/`massForm2_eq_bForm4_diag` (the mass form IS the banked `Q4`/`bForm4`-diagonal on the plane), `massForm2_boost_invariant`, ★★ `plane_uniqueness` (the one-dimensionality — `q=0, r=−p` by ℚ Gram forcing from one rational boost), `mass_channel_one_dim`, ★★ `equivalence_principle` (state-independent ratio), ★★★ `ep_unique_channel` (m_g = m_i forced), ★★★ `ep_no_dial` (violation unwritable), `timeForm_not_boost_invariant` (W8 the channel has teeth — `t²` is excluded), `noetherCharge`, `noetherCharge_energy`/`noetherCharge_momentum` (banked N420 charges), ★ `noetherCharge_conserved` (the conservation law), `noetherCharge_add`/`noetherCharge_smul` (linear), ★★★ `noether_faithful`/`noether_charge_determines_state` (the charges determine the state), ★★★ `equivalence_and_noether` (THE CAPSTONE), `equivNoether_nonvacuous` ([W8]: the mass form passes but `t²` fails, and a nonzero state has a nonzero charge). THE MOAT: EP = the uniqueness of the Lorentz invariant scalar (a ℚ Gram fact), Noether = the invariance + non-degeneracy of the banked pairing — both dissolve the standard posits; the one-pairing-two-laws unification (diagonal = mass = EP, off-diagonal = charges = Noether) is never-banked. GRADE: THEOREM-route for both legs; the EP is proved on the time–space (boost) PLANE — full-4D rotational-invariant-form uniqueness would additionally need the rotational isometries N420 did not bank (FLAGGED, not overclaimed). Costume C447 (`EquivalenceNoetherVacuityCostume`: the non-vacuity flag tied to `equivNoether_nonvacuous` — an "every plane form is boost-invariant / no unique mass channel / EP has a dial / the charge map is trivial" mis-reading forces `1 = 447`) bites `⊢ 1 = 447`. Physics-words-removable (STANDARD §2): delete "equivalence/inertial/gravitational/mass/energy/momentum/conservation/Noether/charge/boost/spacetime/rest frame" → the space of `{(a t+b x, b t+a x)}`-invariant symmetric quadratic forms on `ℚ²` (over `a²−b²=1`) is one-dimensional spanned by `t²−x²`; a member normalized to `1` at `(1,0)` equals `t²−x²` everywhere; the symmetric bilinear `bForm4` is linear and non-degenerate in each slot and invariant under the banked shear; pairing with the coordinate axes recovers the coordinates — no theorem STATEMENT needs a physics word. NOT free-floating (STANDARD §3): every TYPE mentions the banked derived objects (`bForm4`/`boost4`/`Q4`/`STV4` from N419/N420, over ℚ); the abstract §1 uniqueness lemma is MACHINERY instantiated to the banked mass form at the EP capstone. ℝ-vigilance: over the cascade ground ring ℚ and the banked reduced arena `ℚ×ℚ×Dbl ℚ` — NO Mathlib ℝ/ℂ as content (the boost is a rational hyperbola point, the uniqueness a ℚ Gram fact, the charges ℚ contractions). W9 MEASURE-FIRST: a LIGHT node — probe1 whole-file `lake env lean` 10s; production whole-file 14s (well under KILL=90s/obligation); olean via `lake build` 16s; independent axiom audit 22/22 clean vs the built olean. STANDARD applied (unbroken / complete / words-removable). Gate-fast D0–D6 GREEN. 🔨→✅ | gate-fast green; foundations-only (22/22 vs built olean); words-removable (as above). | THE NEXT FORCED NODE — N423 (arc-O O8, the single directed successor): SELECT + derive the λ_n = −n/(84π) RG-eigenvalue ladder as a Lean theorem — currently mechanism-grade prose (G2/G5); with the banked derived calculus (N369/H1), STATE AND PROVE the ladder over `Cut`, the spine tying the scale tower (B), universality (G2), renormalizability (G5) into ONE spectrum, n ∈ {7,9,28} the banked counts; per SEED_COMPLETION_SWEEP §O7 → §O8 — childed. |