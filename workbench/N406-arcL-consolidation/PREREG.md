# N406 PREREG — arc-L L5: CONSOLIDATION OF THE EMERGENT MATTER LADDER

TARGET (selected via SELECT protocol, comments 322/324): L5, the LAST node of arc L.
Bundle L1–L4 (nuclear residual colour → atomic shell → chemical periodicity → condensed
matter) as ONE fold engine — the terminal-algebra non-associativity + its imaginary dim 7 +
its structure-3-form norm ‖φ‖²=42 — threading every rung. AVOID THE HOLLOW-AND (N390): bank
GENUINE never-banked CROSS-TIE TYPES, not a bare ∧ of the four capstones.

MODULE: Phys/Algebra/EmergentMatterLadderConsolidation.lean, ns Phys.Algebra.MatterLadder.

THE CROSS-TIES (de-risked, probe1+probe2 EXIT 0 @ 8s each):
1. exponent_is_structure_form_norm_reciprocal — criticalExponent = 1/assoc3NormSq (=1/42);
   assoc3NormSq = channel count. [L4 exponent IS bottom-of-ascent invariant ‖φ‖².]
2. terminal_dim_threads_ladder — the ONE finrank ℚ ImO = 7 forces L2 closure (⋀^N=0 for N>7),
   L3 recurrence (mod 7), L4 channel count 42 = 7·6.
3. ladder_one_cause_nonassociativity — L1 residual associator ≠ 0 (cascade-stop witness) AND
   not_associative AND criticalExponent = 1/assoc3NormSq. [ONE cause, two rungs.]
CAPSTONE: emergent_matter_ladder — three cross-ties + one representative landing/rung, NON-HOLLOW.
W8: ladder_number_nonvacuous — assoc3NormSq = 42 ≠ 0, ≠ 49.

GRADE: cross-ties = FULL theorems foundations-only over banked concrete objects (assoc/
assoc3NormSq/ImO/fermiSector/valence/criticalExponent). The IDENTIFICATION that this IS the
physical matter ladder = MECHANISM-grade flagged prose (beyond-key, SEED §0/§L5). NO emergent
number fitted (G1/G2 NO-FIT). Physics-words-removable (G4): all types are pure statements about
the octonion associator, its 3-form norm, the exterior powers of ImO, and the totalization
projector entry over ℚ / derived Cut.

DEPS (all banked, foundations-only): NuclearResidualColour (N402), AtomicShellPauli (N403),
ChemicalPeriodicity (N404), CondensedMatterPhaseTransition (N405), InteractionChannelTower
(imChannelCount/channels_O/channels_O_eq_assoc3NormSq), OctonionStructure3FormNorm (assoc3NormSq/
_eq_42/_ne_zero/_ne_49), Octonion (not_associative), UniversalityCriticalExponent
(criticalExponent_eq_one_div_42). Mathlib as MACHINERY only; derived ℝ Cut, NO Mathlib-ℝ content.

COSTUME: C431 (next after C430) — bites the threaded number 42 (a bogus value fails).
SUCCESSOR: ONE directed child → SEED_INPUT_CLOSURE.md target M1 (arc M, conditional-grade).
