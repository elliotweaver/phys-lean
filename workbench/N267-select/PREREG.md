# N267 PREREG — THE FORCED THREE-FOLD MATTER-CARRIER MULTIPLICITY OF THE MAXIMAL HERMITIAN OCTONIONIC JORDAN ORDER

SELECT S1–S5 complete (comments 119–122 on t_0d7a5f6d). Target chosen: candidate A; Γ, β, C rejected.
Anti-bullshit gate passed IN FULL incl. the NUMEROLOGY-BRIDGE confrontation (S4).

## TARGET (words-removable headline)
The maximal Hermitian octonionic Jordan order is 3 (H₃ Jordan ∧ H₄ NOT — banked cap, N5). At that
maximal order the general self-adjoint element decomposes as
    Hm d0 d1 d2 a b c = Dg d0 d1 d2 + slotA a + slotB b + slotC c
— a real-diagonal part plus EXACTLY THREE position-disjoint, injective, ℚ-linear embeddings of the
DERIVED matter carrier `O ℚ` (N261-266), total dim 3 + 3·8 = 27. The three-fold multiplicity is
C(3,2) at the FORCED maximal order 3 = the cascade-stop non-associativity. ★4↔★15/★3, one cause.

## PRODUCTION FILE
Phys/Algebra/OctonionJordanThreeMatterSlots.lean  (namespace Phys.Algebra.HJ)
Name-collision checked FREE: slotA/slotB/slotC/Xz_slot_decomp/Hm_slot_decomp/three_matter*/
matterCarrierMultiplicity/maximal_matter_arena — none exist in Phys/.

## DECLS (ALL de-risked EXIT 0 foundations-only this run — probe1_slots.lean, probe2_decomp.lean)
- slotA/slotB/slotC : O ℚ → Matrix (Fin 3)(Fin 3)(O ℚ)  (the three off-diagonal matter-carrier slots)
- slotA_entry/slotB_entry/slotC_entry  (each recovers its octonion at a matrix entry)
- slotA_inj [+ B,C]  (injective — the embeddings are faithful)
- slotA_add [+ smul]  (ℚ-linear)
- slotA_ne_zero  (W8: nonzero octonion → nonzero slot)
- slot_disjoint (slotA a = slotB b ⟹ a=0 ∧ b=0; pairwise position-disjoint)
- Xz_slot_decomp  (Xz a b c = slotA a + slotB b + slotC c — the off-diagonal core = 3 slots)
- Hm_slot_decomp  (Hm = Dg + slotA + slotB + slotC — the maximal element = diagonal ⊕ 3 slots)
- maximal_matter_arena / three_matter_slots  (THE JOINT: (H₃ Jordan for all pairs ∧ the 3-slot
    decomposition) ∧ (H₄ NOT Jordan) — the maximal Jordan arena is order 3, carrying exactly 3
    matter-carrier slots; = jordan_cap_pinned_at_three assembled with the decomposition)
- capstone matter_multiplicity_three  (bundles decomposition + injectivity + disjointness + the
    cap-forced count).
- OPTIONAL W8 teeth: finrank of the off-diagonal core = 24 (= 3·8) if it measures LIGHT; else a
    concrete nonzero-slot witness (already have slotA_ne_zero) suffices for non-vacuity.

## COST BUDGET + KILL (W9)
Measured: probe1 26s, probe2 27s — both LIGHT, all fin_cases/simp on matrix entries + banked cap
citation. NO octonion coordinate ring, NO native_decide, NO maxHeartbeats raise. KILL = if any single
obligation exceeds 120s or needs a heartbeat/tstack raise, STOP and reframe (do not grind). The
finrank-24 teeth is the only possible heavy piece — MEASURE it in a bounded probe BEFORE production;
if heavy, drop it (slotA_ne_zero is sufficient W8) — do NOT grind.

## FREE-FLOATING CHECK (SOUL rail)
Every headline TYPE mentions the banked objects: Hm/Dg/Xz (J₃(O), N5), the cap
jordan_cap_pinned_at_three (N5), and O ℚ (the derived matter carrier). NOT carrier-agnostic: the
COUNT is pinned by the octonion-SPECIFIC cap (jordan_cap_iff_nonassoc) — false for an associative
coordinate ring (all orders Jordan, no cap, no forced maximal). This is what distinguishes A from
the REJECTED β.

## HONEST SCOPE
Banks: the three matter-carrier slot-embeddings + the H₃ = diagonal ⊕ 3·(O ℚ) decomposition + the
COUNT forced by the cap (the ★4↔★15 one-cause joint) + non-vacuity ONLY. Does NOT bank: "= three
physical generations" (removable prose — novelty ONLY for the forced count + joint, NOT the
generations reading, exactly as N266's colour and N43's spacetime); the masses/mixings of the three
(★5 open); the Jordan/weight ACTION structure on the slots (N212 has one slot's weight; a later
node); which "3" (off-diagonal vs diagonal) maps to physical families (prose). NOT a re-pin of N5
(cap without slot-decomposition/count) or N212 (one slot's weight, no multiplicity, no carrier tie).

## COSTUME
C298 — bites at the forced matter-carrier multiplicity / the decomposition count. A WRONG reading
(the maximal arena carries ≠3 slots / the decomposition is wrong / the cap does not force the count)
reduces through a banked numeric to a false core (e.g. 3 = 298). Pair (298, 3) fresh.
