# N100 PREREG — THE ALGEBRAIC GENERATION qvIsomMonoidC ↔ genIsomMonoidC2 VIA THE BRIDGE
*Frozen BEFORE compute. KILL classifies a fight as INSTRUMENT failure (W9), not theory failure.*

## TARGET (priority (i), the immediately-bankable NO-topology generation joint)
N99 banked `endToFunEnd : Module.End Cut STVC →* Function.End STVC` (faithful monoid hom,
reflects the isometry predicate), `biMulLin u u' : Module.End Cut STVC` (the Cut-linear
operator with `endToFunEnd (biMulLin u u') = biMulFun u u'`), and `biMulLin_mem_gen2` (the
bridged generator ∈ genIsomMonoidC2). The generators biMulFun live in Function.End; the
compact part k lives in Module.End. The bridge made the GLOBAL surjectivity well-posed.

N100 banks THE GENERATION TRANSPORT proper, the ONE LAW reframe: the Module.End-side
two-sided generators close into a submonoid `genIsomMonoidLin` whose bridge-image lands in
the Function.End-side `genIsomMonoidC2` — every finite product of the Cut-LINEAR generators,
pushed across the faithful bridge, IS a finite product of the bare-self-map generators. The
generation question transports across the bridge as a monoid-hom image. This is the algebraic
core of `qvIsomMonoidC ≤ genIsomMonoidC2`: the compact part, IF it is a finite biMulLin
product on the Module.End side, lands in genIsomMonoidC2 on the Function.End side — the
membership the global surjectivity needs, made structural via the monoid hom, NO topology.

### Decls to bank (production Phys/Algebra/LorentzContinuumGeneration.lean)
1. `biMulGenSetLin : Set (Module.End Cut STVC)` — the Module.End two-sided Born-unit generator set.
2. `genIsomMonoidLin : Submonoid (Module.End Cut STVC)` — its closure (Module.End generated submonoid).
3. `endToFunEnd_biMulGenSetLin_image` — the bridge maps biMulGenSetLin into biMulGenSetC (⊆ gen2).
4. ★★ `endToFunEnd_genIsomMonoidLin_mem_gen2` — THE TRANSPORT: ∀ K ∈ genIsomMonoidLin,
   endToFunEnd K ∈ genIsomMonoidC2 (every Module.End word bridges to a Function.End word in gen2).
5. ★ `endToFunEnd_map_genIsomMonoidLin_le` — submonoid form: map endToFunEnd genIsomMonoidLin ≤ genIsomMonoidC2.
6. `genTwoPlaneLin (u w) : Module.End Cut STVC := biMulLin w w * biMulLin u u` — concrete linear 2-plane rotation.
7. `endToFunEnd_genTwoPlaneLin` — its bridge-image = biMulFun w w ∘ biMulFun u u (the N95 composite), via map_mul.
8. ★ `genTwoPlaneLin_mem_genLin` / `genTwoPlaneLin_mem_gen2` — concrete: ∈ genIsomMonoidLin and bridged ∈ genIsomMonoidC2.
9. W8 non-vacuity: `genTwoPlaneLin_e2_je2_fixes_one` (bridged concrete rotation fixes real axis (0,0,1))
   and W8 teeth `genTwoPlaneLin_e2_je2_coord` — a deep coordinate reading distinguishing the genuine value.

## GO / NO-GO
- GO if: the transport theorem (#4) + concrete witness (#7,#8) + W8 teeth (#9) all compile
  foundations-only [propext, Classical.choice, Quot.sound], gate D0–D6 green, costume C132 bites.
- NO-GO / decompose if: the Submonoid.closure_induction / Submonoid.map_closure API resists →
  bank the concrete witness (#6,#7,#8) alone, child the general transport.
- HONEST W1 BLOCK if: the generation genuinely needs the operator topology / the converse
  (qvIsomMonoidC member ⟹ IS a biMulLin product) — that is the W1 HEAVY remainder, child it.

## KILL BUDGET (W9 — INSTRUMENT failure threshold)
- Each obligation here is pure monoid-hom/closure algebra on ALREADY-COMPILED objects: expect
  < 10s each (no coordinate grind, no heavy compile). KILL any single obligation at 90s — if a
  closure-induction obligation exceeds 90s the FRAMING is wrong (structural reduction skipped),
  NOT the theory. Do NOT inflate maxHeartbeats. Do NOT brute a coordinate goal.
- The W8 coordinate teeth: a single deep-scalar read, expect < 5s. KILL at 60s → reframe.
- ANTI-TIMEOUT: checkpoint/commit production+wiring by ~50% turns; run gate in BACKGROUND; child
  the next node + complete with turns to spare. Bank-as-you-go: move each compiling lemma to
  production immediately.

## COSTUME C132 (W8 — MUST bite a WRONG generation claim, false numeric DISTINCT from battery)
A WRONG bridged-generation-composite coordinate: the bridge-image of the concrete linear
2-plane rotation genTwoPlaneLin e₂ ιe₂, read at a chosen vector, has its genuine deep
coordinate; the bogus value forces a false numeric distinct from C115 1=7 … C131 −1=29.
Pick a fresh RHS (battery RHS used: 1,5,7,8,9,12,13,17,29,40,64; choose e.g. 33 or 23).

## PHYSICS-WORDS-REMOVABLE
Delete "Lorentz/rotation/isometry/generation/generator/Moufang/bimultiplication/Spin/SO(9)/
plane/compact/bridge": what remains is pure monoid/linear-algebra over Cut + O Cut — the
Cut-linear maps v↦u·(v·u') close into a submonoid of Module.End whose image under the monoid
hom endToFunEnd lies in the submonoid of Function.End generated by the self-maps (t,x,v)↦
(t,x,u·(v·u')); a concrete product of two such operators bridges to the composite self-map. NO
theorem STATEMENT needs a physics word.
