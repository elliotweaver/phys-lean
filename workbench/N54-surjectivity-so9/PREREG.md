# N54 — PREREG (frozen before finalize). The generated isometry submonoid + forward KAK soundness.

## NODE
The next forced node after N53 (rotation one-parameter subgroup landed). The HEAVY remaining
core named by the roadmap is the GLOBAL finite-product surjectivity (Cartan/polar KAK) onto the
connected component + the non-associative SO(9)/Spin(9) rotation completion.

## MEASURE-FIRST RESULT (done this run, scratch Phys/ProbeN54.lean, exit 0)
The FORWARD direction of the finite-product surjectivity is immediately bankable and compiles
clean foundations-only-style:
  - genIsomMonoidC := Submonoid.closure (boostOPS ∪ {all rotations around imaginary Born-units})
  - boostOPS ≤ genIsomMonoidC, rotGenSetC ⊆ genIsomMonoidC  (the two generator families embed)
  - ★ genIsomMonoidC ≤ qvIsomMonoidC  (FORWARD SOUNDNESS: every finite product of the boost +
    rotation one-parameter subgroups is a continuum QvC-isometry — via boostOPS_le_isom +
    rotCircFun_mem, Submonoid.closure_le)
  - STRICT EXTENSION (non-vacuity W8): quarterTurn ∈ gen but ∉ boostOPS (v-block moves, e₂≠1);
    irrBoost ∈ gen but ∉ rotGenSetC (x-block moves, √2/4≠0); hence boostOPS < genIsomMonoidC.

## SCOPE DECISION (W3 decompose + W1)
BANK: the generated isometry submonoid (combining BOTH one-parameter generator families) + the
forward soundness genIsomMonoidC ≤ qvIsomMonoidC + the strict-extension non-vacuity. This is the
FORWARD half of the KAK / finite-product surjectivity — a gap-free derivation from the banked
trunk (N49 qvIsomMonoidC/STVC/QvC/gFormC, N52 boostOPS/boostOPS_le_isom, N53 rotCircFun/
rotCircFun_mem/rotCircOPS, e2 facts) over the derived ℝ Cut + terminal algebra O Cut.

CHILD (honest W1 dissolution remainder, NOT grindable here): the REVERSE direction — that EVERY
orientation/time-preserving continuum isometry IS such a finite product (genIsomMonoidC ⊇
qvIsomMonoidC on the connected component). This is the genuine polar/KAK decomposition (square
root of a positive-definite operator over Cut) + the non-associative SO(9)/Spin(9) completion
(rotations in arbitrary planes around non-associating units, Moufang/2:1 cover) + connectedness/
topology over the derived ℝ. Mathlib has NO SO⁺(1,9), no octonionic spinor cover, no Lie-group
integration over Cut. To be attacked from the BORN POSITIVITY (the self-overlap form / BvC
polarization) rather than a coordinate KAK grind.

## GO / NO-GO
GO if: production file LorentzContinuumGeneratedGroup.lean compiles, all decls foundations-only
(#print axioms ⊆ {propext, Classical.choice, Quot.sound}), genIsomMonoidC_le_isom + strict
extension proven, costume C85 bites, gate D0–D6 green.
NO-GO / DECOMPOSE-and-child if the forward soundness or strict extension resists.

## KILL (instrument budget, W9)
No single tactic > 60s. No maxHeartbeats inflation. No monolithic brute on expanded coordinates.
The proofs are structural (Submonoid.closure_le, coordinate projection for strict extension) —
if any obligation needs a brute normalizer over O Cut coordinates, STOP: it is the wrong framing.

## COSTUME C85 (W8, must bite, reduces to a false numeric)
The KAK-factor invariant confusion: the boost (time-space) block of the generated/KAK structure
carries the HYPERBOLA invariant a²−b²=1, NOT the rotation's CIRCLE invariant a²+b²=1. The WRONG
claim applies the circle invariant to the boost block's form-change factor:
  (a·t+b·x)²−(b·t+a·x)² = (a²+b²)(t²−x²)   [WRONG; correct is (a²−b²)(t²−x²)]
At a=b=1, t=1, x=0: LHS = 1−1 = 0; RHS = (1+1)·1 = 2. Reduces to the false numeric 0 = 2.
Distinct from C84 (rotation circle-closure sign, space block, 8=4): this is the boost/time block.

## WORDS-REMOVABLE
Delete Lorentz/boost/rotation/Spin/SO⁺(1,9)/SO(9)/isometry/generator/one-parameter/KAK/Cartan/
finite-product/surjectivity → over the derived complete ordered field Cut and the CD-double³
O Cut, the submonoid generated by {(t,x,v)↦(at+bx,bt+ax,v) | a²−b²=1} ∪ {(t,x,v)↦(t,x,c•v+s•(a·v))
| a·(a·v)=−v, gFormC 1 a=0, gFormC a a=1, c²+s²=1} is contained in the submonoid preserving
t²−x²−gFormC v v, and strictly contains members of each generating family not in the other. Pure
math, no physics word in any statement.
