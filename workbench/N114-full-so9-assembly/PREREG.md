N114 PREREG — THE FULL-SO(9) ASSEMBLY THROUGH THE GENERAL-AXIS FAMILY
(the concrete higher-sector witness; ROADMAP §N114 priority (ii))
FROZEN BEFORE PRODUCTION. Run 298.

═══════════════════════════════════════════════════════════════════════
THE NODE (specified against what the chain ACTUALLY produced)
═══════════════════════════════════════════════════════════════════════
N113 banked the structural complement N112's finding demanded: the general-axis
ninth rotation `axisRotLin e c s` (a QvC-isometry, in the enlarged family
`genIsomMonoidLinFamily`) that does NOT commute with an octonion-block word
(`axisRotLin_word_not_commute`). N112's `enlargedTwoPlaneNinth u w c s :=
planeRotLin c s · genTwoPlaneLin u w` is the SINGLE-A-PLANE enlarged element
(planeRotLin = axisRotLin 1) — it DEGENERATES to the SO(8)·A product sector
(kakWordLin_degenerate, the words commute with the single real-axis plane).

N114 banks the NON-DEGENERATE analogue: the GENERAL-AXIS enlarged element
  `enlargedAxisNinth e u w c s := axisRotLin e c s · genTwoPlaneLin u w`,
and the concrete full-SO(9) HIGHER-SECTOR WITNESS that it moves BOTH x AND a
DEEP octonion slot — reaching strictly beyond the SO(8)·A sector that the single
A-plane (enlargedTwoPlaneNinth) generates. THE SEPARATOR theorem proves the
general-axis witness differs from EVERY single-A-plane element (for s ≠ 0).

This is the immediately-bankable structural piece of ROADMAP §N114 priority (ii)
— a concrete full-SO(9) higher-sector witness through the general-axis family
(an element that moves BOTH x AND a deep octonion slot, beyond what either the
SO(8) words or the single A-plane reach). The GLOBAL exhaustion (every QvC-
isometry is a finite enlarged-family word) + the operator topology over Cut stay
the childed heavy W1 remainder N115.

═══════════════════════════════════════════════════════════════════════
GO / NO-GO (frozen)
═══════════════════════════════════════════════════════════════════════
GO if, over the derived ℝ Cut + the terminal algebra O Cut, foundations-only:
  (1) enlargedAxisNinth e u w c s := axisRotLin e c s * genTwoPlaneLin u w defined.
  (2) enlargedAxisNinth_mem — ∈ genIsomMonoidLinFamily (SOUND, mul_mem of
      axisRotLin_mem_genIsomMonoidLinFamily + the word, via the le-chain
      genIsomMonoidLin ≤ ...Plus ≤ ...Family).
  (3) enlargedAxisNinth_isQvIsomC — a QvC-isometry (via family soundness).
  (4) enlargedAxisNinth_e2_apply — (0,1,0) ↦ (0, c, s•e₂): moves BOTH x AND a
      deep octonion slot (the word fixes the zero-v point, the e₂-rotation acts).
  (5) enlargedAxisNinth_e2_moves_x — x-image = c.
  (6) ★ enlargedAxisNinth_e2_reads_s — the e₂-component of the moved v-block = s
      (the DEEP slot the general axis reaches).
  (7) ★ enlargedTwoPlaneNinth_e2_reads_zero — the single A-plane element leaves
      the e₂-component at 0 (the COMPLEMENT: it cannot reach the e₂ slot).
  (8) enlargedAxisNinth_e2_not_mem_genIsomMonoidLin — not a pure SO(8) word (c≠1).
  (9) ★★ enlargedAxisNinth_ne_enlargedTwoPlaneNinth — THE SEPARATOR: for s ≠ 0
      the general-axis witness differs from EVERY single-A-plane element
      (reads s vs 0 in the e₂ deep slot) — strictly beyond the SO(8)·A sector.
  (10) W8 teeth: enlargedAxisNinth_e2_deep_val — concrete (5/13,12/13) reads 12/13.

NO-GO / DECOMPOSE if the structural core resists: bank what compiles, child the
remainder. (Probe measured ALL of (1)-(10) compiling clean — GO confirmed.)

═══════════════════════════════════════════════════════════════════════
KILL (W9 — instrument-failure classifier)
═══════════════════════════════════════════════════════════════════════
KILL = 90s per single obligation. If any one lemma's elaboration exceeds 90s,
it is an INSTRUMENT failure (not a theory failure): STOP, measure the smallest
sub-obligation, decompose / reframe through banked identities. NO inflated
maxHeartbeats. NO native_decide. NO monolithic brute over expanded coordinates.
MEASURED: probeA.lean (all 14 decls) elaborates in 7.6s total — KILL never
approached. This is a LIGHT node: rational coordinates over Cut, structural
rewrites through banked gFormC bilinearity + the fix-zero-v lemmas.

═══════════════════════════════════════════════════════════════════════
COSTUME C146 (W8 — must BITE)
═══════════════════════════════════════════════════════════════════════
A WRONG full-SO(9)-assembly / higher-sector claim: the general-axis witness's
DEEP e₂-slot reads 12/13 (enlargedAxisNinth_e2_deep_val); the BOGUS claim that
it reads 101 (a stand-in for any wrong "the assembly degenerates to SO(8)·A / the
general axis cannot reach the deep slot / the witness equals a single-A-plane
element") forces the false numeric 12 / 13 = 101 — must NOT compile.
DISTINCT from the banked battery (… C143 −1=83, C144 3/5=89, C145 4/5=97).
PASS_SIGNATURE (ERE, trailing substring, no leading sign): `12 / 13 = 101`.

═══════════════════════════════════════════════════════════════════════
STANDARD (the second gate)
═══════════════════════════════════════════════════════════════════════
UNBROKEN — every object derives from the banked trunk: axisRotLin /
axisRotLin_apply / axisRotLin_mem_genIsomMonoidLinFamily /
genIsomMonoidLinFamily_isQvIsomC (N113); genTwoPlaneLin / genTwoPlaneLin_mem_genLin
/ genTwoPlaneLin_fixes_zero_v (N100/N106); genIsomMonoidLin_le_genIsomMonoidLinPlus
/ genIsomMonoidLinPlus_le_genIsomMonoidLinFamily / enlargedTwoPlaneNinth / planeRotLin
/ planeRotLin_apply / genIsomMonoidLin_fixes_snd (N105/N106); gFormC / gFormC_zero_left
/ gFormC_smul_left / e2_gFormC_self / e2_gFormC_one / reQC_zero (N-LieAlgebra/N53/N-Rotation)
— over the derived ℝ ContinuumQ.Cut and O Cut := CD (H Cut). NO posited Lorentz group,
NO Mathlib ℝ/ℂ as content, NO bridge.
COMPLETE — foundations-only [propext, Classical.choice, Quot.sound], independently
axiom-audited vs the built olean; no sorry/axiom/native_decide.
PHYSICS-WORDS-REMOVABLE — delete Lorentz/rotation/axis/isometry/compact/SO(9)/SO(8)/
A-plane/ninth-direction/octonion-block/word/sector/assembly/family/separator: what
remains is pure linear algebra over Cut — for e with gFormC e e = 1, the operator
axisRotLin e c s · genTwoPlaneLin u w of Module.End Cut STVC, in the submonoid
generated by the axis-rotations and bimultiplications, sends (0,1,0) to (0,c,s•e₂)
so that gFormC of its v-block with e₂ is s, whereas planeRotLin c s · genTwoPlaneLin u w
sends it to a v-block whose gFormC with e₂ is 0; hence the two operators differ for s≠0.
No theorem STATEMENT needs a physics word.
