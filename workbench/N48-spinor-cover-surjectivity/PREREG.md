# N48 — PREREG (frozen before compute)

## NODE
THE SURJECTIVITY / 2:1 COVERING DEGREE SL(2,𝕆) ↠ SO⁺(1,9): that the matrix action generated
by the banked boosts (N46) + rotations (N47) covers the connected isometry component.

## THE HONEST DECOMPOSITION (W1/W3 — ticket-mandated)
- (a) LIGHT, BANK HERE: the matrix (spinor) conjugations of N46/N47, read in the herm2≅STV
  coordinatization, ARE the abstract N45 isometry generators (boost a b with a²−b²=1, rot u with
  gForm u u=1) — the EQUIVARIANT INTERTWINING `actBy A ∘ H = H ∘ (abstract isom)`; and the
  SUBMONOID GENERATED by {boost a b | a²−b²=1} ∪ {rot u | gForm u u=1} sits inside qvIsomMonoid
  (the "cover group" object), is NON-TRIVIAL, and both generator families lie in it. Rational,
  function-composition (associative — NO octonionic matrix multiplication of two matrices, so the
  W9 non-associativity trap is structurally avoided). This is a NEW joint (N46/N47 matrix picture
  ↔ N45 abstract isometry monoid + the generated cover submonoid), not a re-pin of any banked node.
- (b) HEAVY, CHILD: the SURJECTIVITY onto the connected component SO⁺(1,9) — that EVERY
  orientation/time-preserving isometry is a finite product of these generators — needs a DERIVED
  SO⁺(1,9) object + the derived ℝ ContinuumQ.Cut + the Aut(𝕆)/exp stack (a continuum/Lie argument).
  NOT banked, NOT Mathlib ℝ/ℂ. Childed onto the chain tail (or honest W1 dissolution ticket).

## GO / NO-GO (frozen)
GO (bank N48 part (a)) iff ALL of:
  1. `act_boost_eq_abstract` : actBy (boostMat p) (herm2 t x v) = herm2 (a*t+b*x) (b*t+a*x) v
     with (a,b)=boostParam p, proved foundations-only (from banked act_herm2 + ℚ ring).
  2. `boostParam_hyperbola` : a²−b² = 1 for (a,b)=boostParam p (p≠0), pure ℚ.
  3. The EQUIVARIANCE: actBy (rotMat u) (Hmap q) = Hmap (rot u q) [Born-unit u];
     actBy (boostMat p) (Hmap q) = Hmap (boost a b q) [p≠0], where Hmap (t,x,v)=herm2 t x v.
  4. `spinorCoverMonoid := Submonoid.closure coverGens` with
     `spinorCoverMonoid ≤ qvIsomMonoid` (closure_le from generators being isometries).
  5. NON-TRIVIALITY: a concrete boost (a,b)=(5/4,3/4), a²−b²=1, b≠0, ∈ spinorCoverMonoid and ≠ 1,
     hence spinorCoverMonoid ≠ ⊥.
  6. #print axioms ⊆ {propext, Classical.choice, Quot.sound} for every decl.
  7. Costume C79 BITES a WRONG covering claim (arbitrary boost in cover without a²−b²=1).
  8. Physics-words-removable: delete Lorentz/boost/spinor/rotation/cover/isometry/surjectivity →
     pure math about herm2/Qform/qvIsomMonoid/the generated submonoid + the terminal algebra.

NO-GO / BLOCK: if even part (a) resists after measure+reframe (it should not — it is rational
function-composition), honest W1 dissolution ticket + block. Do NOT weaken, do NOT bridge.

## KILL BUDGET (W9 — instrument-failure threshold)
This is a LIGHT node (rational arithmetic, Submonoid.closure, function composition; NO octonionic
2-matrix product, NO derived-ℝ analysis here). KILL: if ANY single obligation in part (a) exceeds
~60s compile OR needs maxHeartbeats raised above default OR a monolithic brute normalizer on a
fully-expanded octonion-coordinate goal — that is the W9 signature: STOP, MEASURE the smallest
sub-obligation, DECOMPOSE/reframe through the banked act_herm2/act_herm2_rot (which already did the
coordinate work once), never grind. The heavy continuum surjectivity is (b), already childed —
never attempt it inside this node's budget.

## ANTI-TIMEOUT (W9 — N43/N44/N46 timed out on gate ceremony)
Gate D6 (806+ #print axioms cold) ~6 min; `lake build Phys` cached ~8s. BANK-AS-YOU-GO: the instant
the production file compiles clean → wire (Phys.lean + AxiomAudit import AT END + #print axioms) →
commit BEFORE the full gate. Run gate in BACKGROUND. Finalize (commit + child) with turns to spare.
