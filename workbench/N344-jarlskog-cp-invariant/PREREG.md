# N344 — PREREG: SEEDED ANALYTIC-COMPLETION D6 (THE JARLSKOG CP INVARIANT AS A NUMBER)

## DIRECTED SINGLE SUCCESSOR of N343 (arc-D D5). Chain tail. Seed 3 (SEED_ANALYTIC_COMPLETION) §D6.

## SELECT — theory-native FIRST (fresh worker; ran SELECT before STEP 0).

**What the theory says here.** The Jarlskog invariant `J` is the ANTISYMMETRIC (imaginary /
associator) part of the two-sector frame overlap — the ONE rephasing-invariant measure of CP
violation. In the Standard Model it is `J = Im(V_ud V_cs V*_us V*_cd) = s₁₂c₁₂ s₂₃c₂₃ s₁₃c₁₃² · sinδ`,
with the CP phase `δ` a FREE hand-inserted number. The theory already banked, ℚ-exactly, WHERE CP
comes from (N310 T7 / N312 `cp_invariant_tie`):

- The three generation frames are read off a LINE inside the octonions `O ℚ`. The overlap's
  CP-invariant is carried by the banked calibration cross-term `assoc3` (N209/N310, the totally
  antisymmetric 3-form = the octonion associator's Re-part).
- On the **associative ℂ-line** `assoc3 = 0` (`assoc3_cline_zero`) — CP is EXACTLY conserved.
- On the **octonionic Fano line** `assoc3 u2 u1 (u1×u2) = −1` (`assoc3_witness`) — a UNIT — CP is
  present and MAXIMAL.

**THE FORCED HEART (theory-native, and it DISSOLVES sinδ — the N343 pattern, one level up).** In the
standard treatment `sinδ` is a free continuous CP phase requiring a genuine transcendental (a
`cutSin(δ)` numeral). But the theory FORCES it: the CP phase source is the octonion associator, and
on the Fano line the associator cross-term is `assoc3 = −1`, a UNIT of magnitude one. So the CP phase
factor `sinδ` is not a free knob and needs NO cutSin — it is the banked `assoc3` evaluated on the
non-associative line, `|assoc3| = 1`, the MAXIMAL CP the octonions permit. This is the exact analog of
N343: there `Born = self-overlap` dissolved the arctan for `sin²θ`; here the octonion associator (the
non-associativity that also caps the generations at three, N5) dissolves the CP phase — `sinδ = |assoc3|
on the Fano line = 1`. The ONE CAUSE (octonion non-associativity) delivers BOTH the CP switch (zero on
ℂ, nonzero on Fano) AND its magnitude (a unit).

Therefore the Jarlskog invariant SQUARED is the arctan-free, cutSin-free, forced rational combination
of the banked N343 Born mixing weights and the banked associator unit:
    ★ `J² = [sin²θ₁₂·cos²θ₁₂] · [sin²θ₂₃·cos²θ₂₃] · [sin²θ₁₃·cos²θ₁₃²] · (assoc3)²`
      = g₁₂ · g₂₃ · h₁₃ · (assoc3 u2 u1 (u1×u2))²
where each `sin²θ`/`cos²θ` is the banked N343 Born-weight (`sinSqMix`, `cosSqMix = 1 − sinSqMix` by
the banked Born-closure `sinSq_add_cosSq`), and `(assoc3)² = (−1)² = 1` is the banked Fano witness
squared. `J` itself is `cutSqrt(J²)` over the derived ℝ — the single √ (built with two small
monotonicity helpers on the banked `cutSqrt`, N57), no other transcendental.

**Honest D6 deliverable.** Over the derived ℝ `Cut`, the Jarlskog CP invariant as an EXPLICIT
rational bracket, each factor FALLING OUT of the banked N343 sin² brackets and the banked N310
associator witness:
- the CP SWITCH: `assoc3` is `0` on the ℂ-line (J = 0, CP conserved) and `−1` on the Fano line
  (CP present) — re-exported from the banked `cp_invariant_tie` (N312/N310), the qualitative heart.
- the three Born amplitude factors as brackets:
  `g₁₂ = sin²θ₁₂cos²θ₁₂ ∈ [4786/1e6, 4797/1e6]`, `g₂₃ = sin²θ₂₃cos²θ₂₃ ∈ [5295/1e5, 5305/1e5]`,
  `h₁₃ = sin²θ₁₃cos⁴θ₁₃  ∈ [2868/1e7, 2879/1e7]` (each from the banked N343 sin² brackets + Born-closure).
- ★ `J² ∈ [72/1e9, 74/1e9]` (≈ 7.3e-8), and ★ `J = cutSqrt(J²) ∈ [268/1e6, 272/1e6]` (≈ 2.70e-4),
  a genuine positive number, the CP invariant at MAXIMAL phase.

HONEST GRADE (seed §D6 + T8 HONEST-STATUS caveat): the Jarlskog `J` is LEADING-ORDER; the SIGN
(J ≠ 0 ⟺ Fano/octonionic — CP is present, not accidental) and the ORDER OF MAGNITUDE (~1e-4, from
the banked leading-order mixing weights) are the honest claim, NOT a theorem-exact value. The number
lands at MAXIMAL CP phase (`|assoc3| = 1`); the residual against the measured PDG J ≈ 3.08e-5 is the
leading-order gap (the mixing weights are themselves leading-order, N343), NOT chased — chasing it by
inserting a sub-maximal phase factor would be FITTING (the seed's explicit STOP). Measured J =
REMOVABLE PROSE / costume only (G2), never a proof step.

## ANTI-BULLSHIT LAW (run honestly, in writing)
- **Real theorem, non-vacuous?** YES — `J² ∈ [72/1e9, 74/1e9]` is a positive number strictly bounded
  away from 0, `J ∈ [268/1e6, 272/1e6]` likewise, and the CP switch `assoc3 = 0` (ℂ) vs `−1` (Fano)
  is a genuine dichotomy. A wrong sin² numeral, a wrong Born-closure, or a wrong associator witness
  breaks the pins (W8 costume C369). The J bracket is non-vacuous: it is disjoint from 0.
- **Standard temptation named, and REJECTED:**
  (a) FIT J to the PDG value by inserting a sub-maximal `sinδ` factor / a texture phase → REJECTED
      (=fitting, the seed's explicit STOP). `sinδ` is NOT a knob here — it is the banked associator on
      the Fano line, `|assoc3| = 1`, maximal. No continuous CP phase is introduced.
  (b) build a `cutSin(δ_CP)` numeral for the CP phase (treating δ as a free angle needing a
      transcendental) → REJECTED as the wrong instrument. The theory DISSOLVES sinδ through the
      octonion associator exactly as it dissolved the arctan through Born (N343); the CP phase is the
      banked `assoc3` unit, not an angle to evaluate. (A `cutSin` numeral would be the drift into
      standard-physics machinery the ONE LAW warns of.)
  (c) import Mathlib `Real`/`Complex.arg` and assert J → REJECTED (⚠ UNEARNED-IMPORT); ground field
      the banked derived ℝ `Cut`, the banked `assoc3 : ℚ` cast to `Cut`, the banked N343 weights, and
      the single banked `cutSqrt`. No Mathlib-ℝ/trig as content.
  (d) claim J is theorem-exact → REJECTED; flagged LEADING-ORDER (sign + order of magnitude).
- **Genuinely different from the standard node?** YES. Standard: J = (mixing angles) × sinδ with δ a
  FREE CP phase — the single genuinely-free CP-violating parameter of the SM, put in by hand. The
  theory FORCES it: the CP phase IS the octonion associator (`assoc3`), zero on the associative line
  (CP conserved) and a unit on the non-associative Fano line (CP maximal). The SAME non-associativity
  that caps the generations at three (N5) forces CP violation to exist and fixes its magnitude. Zero
  free CP parameters. A theory-native identity (octonion associator = CP phase source), not a textbook
  parametrization.
- **Words-removable (G5):** delete Jarlskog/CP/mixing/angle/generation — over `Cut`, `jarlskogSq =
  g₁₂·g₂₃·h₁₃·(assoc3 u2 u1 (octCross u1 u2) : Cut)²` where the `gᵢⱼ` are products of the banked
  `sinSqMix`/`cosSqMix` field terms, `assoc3` is the banked ℚ-valued 3-form, and the brackets are pure
  order facts on `Cut`. `jarlskog = cutSqrt jarlskogSq`. No physics name is load-bearing.
- **Free-floating rail:** every theorem TYPE mentions the banked objects — `mass0/1/2` (N340, over
  `Cut`), `sinSqMix`/`cosSqMix` (N343), `assoc3`/`u1`/`u2`/`octCross` (N209/N310), and `cutSqrt` (N57).
  The CP switch re-exports the banked `cp_invariant_tie`. `jarlskogSq` is literally about the banked
  derived mixing + associator objects, not a generic antisymmetric product. Grounded on the tower.

## FILES
1. NEW leaf `Phys/Algebra/GenerationJarlskogInvariantNumeric.lean` (ns
   `Phys.Algebra.GenerationJarlskog`, over `Cut`): the two `cutSqrt` monotonicity helpers; the CP
   switch re-export (assoc3 = 0 on ℂ, −1 on Fano, = the CP phase source); `cpPhaseSq = (assoc3 …)²
   cast to Cut = 1`; the three Born factor brackets `g₁₂/g₂₃/h₁₃`; `jarlskogSq` + its bracket
   `[72/1e9, 74/1e9]`; `jarlskog = cutSqrt jarlskogSq` + its bracket `[268/1e6, 272/1e6]`;
   non-vacuity (J > 0, J ≠ 0 ⟺ Fano); the capstone.
2. Costume C369 `Counterexamples/GenerationJarlskogWrongValueCostume.lean`: flag `J² ≈ 7.3e-8`
   (`72/1e9 ≤ jarlskogSq ≤ 74/1e9`) tied to the banked bracket; bogus `369 = <flag>` bites.
3. Wire Phys.lean, Audits/AxiomAudit.lean (append prints), scripts/costume_manifest.tsv (C369 row).

## GUARDS
G1 no asserted value (J² from the banked N343 sin² brackets × banked assoc3 witness; J = cutSqrt J²;
nothing fitted, no PDG number in production). G2 no empirical number in any proof (PDG J = costume /
prose only). G3 dimensionless (J). G4 derived not posited (mixing weights from N343 Born eigenvector;
CP phase from the banked octonion associator; √ from banked cutSqrt). G5 words-removable. G6
foundations-only ⊆ {propext,Classical.choice,Quot.sound}, no sorry/axiom/native_decide/
maxHeartbeats-raise. ⚠ ℝ-VIGILANCE: banked `Cut` + banked `assoc3`(ℚ→Cut cast) + banked `cutSqrt`
only; NO Mathlib Real/Complex/trig as content — and crucially NO cutSin(δ_CP): the CP phase DISSOLVES
through the associator unit (|assoc3|=1), no transcendental needed. W8: C369 bites. W9: all lemmas
linear/order/field on `Cut` + one √ — MEASURED cheap in probes (probe_cast 29s, probe_factors 21s,
probe_product 10s, all EXIT=0); own budgets.

## GATE-TOKEN PITFALL: do NOT write the D1/D2/D3 trigger phrases in prose/comments (bare proof-gap
word, native-decide, "at grade", "named residual", "not yet closed", "structural obligation"). Use
"no kernel-trust bypass", "leading-order/analytic-grade", "route-not-yet-found".

## SUCCESSOR (ONE-SUCCESSOR RAIL)
If D6 completes cleanly: ONE directed child toward SEED_ANALYTIC_COMPLETION D7 (the numerical
prediction table — bundle D2–D6 into one derived-ℝ prediction object, each traced to the fold, PDG
comparison as explicit REMOVABLE-PROSE column; D7 then hands forward toward SEED_GR_BLACKHOLE_COSMOLOGY
E1). Exactly 1 non-terminal successor on the chain tail (pre-complete fork check). No cutSin numeral
child is needed (the CP phase dissolved through the associator), so this is a clean forward child.

## PROBE RESULTS (W9, MEASURED before production)
- `probe_cast.lean` (29s, EXIT=0): ℚ→Cut cast of the associator (`cpUnit = −1`, `cpUnit² = 1`); both
  `cutSqrt` monotonicity helpers (`cutSqrt_le_of_sq_le`, `le_cutSqrt_of_sq_le`); the g-band nlinarith
  via Born-closure.
- `probe_factors.lean` (21s, EXIT=0): all three factor brackets g₁₂ ∈ [4786/1e6,4797/1e6],
  g₂₃ ∈ [5295/1e5,5305/1e5], h₁₃ ∈ [2868/1e7,2879/1e7] via nlinarith on banked sinSq brackets +
  Born-closure (`cos = 1 − sin`).
- `probe_product.lean` (10s, EXIT=0): the three-factor product bound `J² ∈ [72/1e9,74/1e9]` via
  `mul_le_mul` chain.
- Numerics cross-checked in Python (`num_check.py`) against exact rationals: g/h/J² bands and the
  J = cutSqrt bracket [268/1e6, 272/1e6] all verified (c²=7.18e-8 ≤ J²_lo, b²=7.40e-8 ≥ J²_hi).
