| C3 | arc-C MIXING C3 (docs/SEED_MIXING.md §C3; directed successor of C2/CabibboMixingNumeric; owner-binding board order D→M→C→GR; SELECT + anti-bullshit gate run FIRST, fresh, confirmed C3 theory-native, kanban comment 482) — THE FULL ANGLE STRUCTURE (the 1-3 and 2-3 scale mixings evaluated as certified brackets, the mixing HIERARCHY ORDER a theorem) | Phys/Algebra/FullMixingAngleStructureNumeric.lean | THE FULL MIXING ANGLE STRUCTURE + THE HIERARCHY ORDER A THEOREM OVER THE DERIVED ℝ Cut. LEADING ORDER (seed §C3 / T8 caveat), route-COMPLETE for the C3 charter; all 18 audited decls foundations-only ⊆ {propext,Classical.choice,Quot.sound}. ── ★ THE THEORY-NATIVE HEART / ONE CAUSE (the three mixings are ONE monotone function of ONE amplitude hierarchy): C1 made the two-sector frame misalignment a FORCED object (down frame = up frame with its sub-leading amplitude damped by one self-blind edgeSurvival=2/3); C2 evaluated ONE of the three inter-generation edges (the leading (b0,b2) Cabibbo edge). This node reads the SAME C1 survival-dressed overlap machinery across ALL THREE cap-forced edges of the banked ℝ-descent amplitudes (N339/N340, b0,b1,b2 at δ_B=2/9, p=cutCos(2/9); genVec slots (0,1,2)=(heaviest b0, lightest b1, middle b2)): for each ordered pair (A,B) the mixing FRACTION is eMix2 A B = eCross²/(eNu·eNd), eCross=A·B·(1−survivalCut), eNu=A²+B², eNd=A²+(survivalCut·B)². Standard flavor physics POSITS three independent CKM angles / FITS a Wolfenstein λ,λ²,λ³ tower by hand; the chain reads all three off ONE amplitude hierarchy × ONE self-blind survival factor. ── ★★ THE THREE MIXING FRACTION BRACKETS over the derived ℝ (each an ACTUAL certified two-sided rational bracket, ZERO free continuous parameters): mix12 = eMix2 b0 b2 ∈ [60740/1e7, 60770/1e7] (≈0.0060753, the C2 leading edge, mix12_eq_cabMix2 the exact tie to the banked C2 cabMix2), mix23 = eMix2 b2 b1 ∈ [5332/1e7, 5342/1e7] (≈0.0005337, mix23_lo/hi via cross23/Nu23/Nd23 brackets), mix13 = eMix2 b0 b1 ∈ [3191/1e8, 3197/1e8] (≈0.00003194, mix13_lo/hi). ── ★★★ THE HIERARCHY ORDER A THEOREM (the C3 heart, DERIVED not observed): mix13 < mix23 < mix12 (mixing_hierarchy_order), FORCED by the banked amplitude order b0>b2>b1 through the monotone eMix2_lt — the per-edge fraction is scale-invariant and STRICTLY INCREASING in the sub/leading amplitude ratio; the crux cross-multiplied difference factors EXACTLY as (1−survivalCut)²·(A₁²B₂²−A₂²B₁²)·(A₁²A₂²−survivalCut²B₁²B₂²), a product of two positive factors (mix12_gt_mix23, mix23_gt_mix13 each supplying the two factor-positivity brackets from mass0/1/2). The three angles are ONE monotone function of ONE amplitude hierarchy, where the standard framework posits three independent angles. ── ★ FREE TOOTH (arctan-free unitarity, EVERY edge): eCross²+eAligned²=eNu·eNd (e_brahmagupta, a ring identity of the survival-dressed frames for all A,B) gives eMix2 A B + eAln2 A B = 1 EXACTLY (e_unitarity, via ←add_div + div_self eDen_pos) — the sin²+cos²=1 of each edge angle reproduced from the evaluated fractions. ── ⭐ ONE CAUSE MANY TERMINATIONS: the SAME self-blind edgeSurvival=2/3 whose DEFICIT (1−edgeSurvival)=1/3 is the C1 frame misalignment sets EVERY edge cross-term eCross=A·B·(1/3); the SAME amplitude hierarchy b0>b2>b1 that fixes the Koide mass spectrum here fixes the mixing hierarchy through the one monotone eMix2. ── W8 NON-VACUITY: all three mixings positive (all_mix_pos, real mixing at every edge); the three brackets pairwise DISJOINT (hierarchy_brackets_disjoint: 5342/1e7 < 60740/1e7 and 3197/1e8 < 5332/1e7 — the ordering is genuine, not a bracket-overlap artefact). CAPSTONE full_mixing_structure_numeric welds the C2 tie, the three fraction brackets, the hierarchy-order theorem, the per-edge unitarity tooth, and the non-vacuity — statable from no single conjunct alone (non-hollow). ── ⚠ HONEST GRADE (seed §C3): LEADING ORDER. The three derived fractions are the survival-dressed overlap magnitudes; the measured CKM angles / Wolfenstein λ,λ²,λ³ powers are the arc-C LIVE-fight residual — REMOVABLE PROSE, in NO proof, NEVER fitted, NEVER chased (reaching for a measured angle to steer any bracket or the ordering is the fit, seed G2 — forbidden). THE ONE-PEG LAW (inherited from M1): every mixing fraction is DIMENSIONLESS (a ratio of amplitudes), no new peg. ── NO-FIT (G2/G7 sharpened): NO empirical CKM number (measured angles, Wolfenstein λ, PDG values) in ANY statement or proof — comparison to data removable-prose ONLY; the beyond-measurement digits are the STANDING PREDICTION; no route/bracket/ordering choice steered by where a measured value sits. ── FOUNDATIONS: 18 audited decls #print axioms ⊆ {propext,Classical.choice,Quot.sound}; imports CabibboMixingNumeric + Mathlib.Tactic (transitively the banked survivalCut/edgeSurvival + b0/b1/b2 ℝ-descent amplitudes + mass0/mass1/mass2 brackets + survivalCut_val + the C2 cabMix2/cabMix2_lo/hi); the ground field is the BANKED derived ℝ Cut, the banked cutCos(2/9)-descent amplitudes, and the banked ℚ edgeSurvival cast to Cut — NO Mathlib ℝ/ℂ as content, NO Real.*, NO floats, NO native_decide, NO sorry, NO posited axiom; NEVER Phys/OneAxiom/*, NEVER MassGapOne. PHYSICS-WORDS-REMOVABLE: delete quark/Cabibbo/CKM/mixing/angle/hierarchy — over Cut with the banked b0,b1,b2 and survivalCut=2/3, eMix2 A B=(A·B·(1/3))²/((A²+B²)(A²+(2/3·B)²)) is scale-invariant and strictly increasing in B/A on the physical regime, so at b0>b2>b1 one has eMix2 b0 b1 < eMix2 b2 b1 < eMix2 b0 b2, each in its certified bracket, and eCross²+eAligned²=eNu·eNd (a ring identity) — pure rational-interval algebra + a monotonicity theorem over the derived ℝ; no physics word does logical work; a physicist reads the full mixing structure OUT of the theorems. NOT FREE-FLOATING: every production TYPE cites the banked derived objects (b0/b1/b2 N339/N340 over Cut, survivalCut/edgeSurvival, the C2 cabMix2 via mix12_eq_cabMix2). GATE-FAST D0–D6: PASSED (append-only leaf; C557 bites 1=557 ordFlag: the three derived mixing fractions are strictly ordered mix13<mix23<mix12 with pairwise-disjoint brackets via mixing_hierarchy_order — not a hollow/collapsed unordered set; 18 newly-banked decls foundations-only; full Phys builds). Build clean (lake build Phys.Algebra.FullMixingAngleStructureNumeric EXIT 0, Built 11s, LIGHT — nlinarith/div_lt_div_iff₀/le_div_iff₀/div_le_iff₀/mul_le_mul on Cut order + banked b0/b1/b2/mass0/1/2 brackets + ring, no brute, no native_decide, no heartbeat raise; the monotone crux eMix2_lt via one nlinarith on the factored product, measured 9.5s in Lean probe before build; full module measured 12s standalone; crux + all bracket endpoints verified provable in Python (workbench/C3-full-angle/{probe1,probe2,probe3,endpoints,verify_identity}.py — the factorization P=(1−s)²(A₁²B₂²−A₂²B₁²)(A₁²A₂²−s²B₁²B₂²) verified exact by sympy), no W9 risk). Scratch probes workbench/C3-full-angle/ (all verified; ALL ASSERTIONS PASSED). Directed ONE successor toward C4 (SEED_MIXING §C4 — THE CP INVARIANT EVALUATED: the chain's convention-free Jarlskog-type CP invariant tied to assoc3, derived and evaluated to a certified bracket; the dichotomy tooth carried to numeral grade — the SAME evaluation at the lepton texture yields EXACTLY zero, theorem not small). NO generic SELECT (rail: a directed child REPLACES the generic SELECT); do NOT hand GR1, do NOT hand D7. NOT a runway (autonomous theory-native evaluation work remains — arc C is a buildable structure+evaluation arc). Carries the deferred-D7 note (arc-D standing debt, resumes when the owner/board directs). |
