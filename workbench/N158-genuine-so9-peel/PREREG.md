# N158 PREREG — frozen GO/NO-GO + KILL (before compute)

## Target (re-read ROADMAP §N158 FORWARD FRONTIER, priority (i))
THE GENUINE SO(9) GIVENS-WORD EXHAUSTION on the TIME-FIXING compact factor, into the
NON-VACUOUS `JoinedIdQvC` (N157). MEASURE-FIRST decided the achievable, honest scope.

## MEASURE-FIRST findings (the smallest obligations, probed)
1. **The first datum (does the compact factor fix time?).** The compact factor `k` of
   `bvIsomLin_compact_factor_spec` (N131) is `IsQvIsomC k` AND EvC-orthogonal
   (`kadj∘k = id`, `IsEvCAdjoint k kadj`). EvC is positive-definite (signature (10,0)),
   QvC indefinite (1,9). Preserving BOTH ⇒ k preserves t² (so time fixed up to SIGN) and the
   spatial 9-block form. So k is in the maximal compact O(1)×O(9); it fixes the timelike axis
   ONLY on the orthochronous component. ⇒ TIME-FIXING is the correct scoping hypothesis (an
   orthochronous-component restriction), NOT automatic — exactly as the task flagged.
2. **One peel reduces SO(9)→ x-fixing.** Given k fixes time and is EvC-orthogonal, its x-axis
   image `k(0,1,0)` has ZERO time component (EvC-orthogonality + time-fixing: EvC(k(1,0,0),
   k(0,1,0)) = EvC((1,0,0),(0,1,0)) = 0, and k(1,0,0)=(1,0,0) ⇒ (k(0,1,0)).1 = 0). So
   `k(0,1,0) = (0,x',v')`. If v'≠0, the banked `deflate_factorization` (N116) gives
   `k = inflateNinth x' v' · (deflateNinth x' v' · k)`, with `inflateNinth x' v'` ONE explicit
   unit-axis Givens rotation. The antipode condition `1+x'≠0` is FREE: unit-sphere law
   x'²+gFormC v' v'=1 with v'≠0 ⇒ gFormC v' v'>0 ⇒ x'²<1 ⇒ −1<x'<1 ⇒ 1+x'>0. So
   `inflateNinth x' v'` is `JoinedIdQvC` (N157 `joinedIdQvC_axisRotLin`), and
   `deflateNinth x' v' · k` FIXES the x-axis (N115 `deflateNinth_step`).
3. **The octonion-block SO(8) residual is HEAVY.** After peeling, `deflateNinth·k` fixes time
   AND x-axis, acting purely on the octonion block 𝕆 (8-dim). `gFormC_isom_mem_closure` (N124,
   BANKED) gives a REFLECTION word — but `axisRotLin` only pivots around the x-axis (couples
   p.2.1 with the e-component), so a pure octonion-internal rotation needs the x-pivot
   CONJUGATION generation (SO(8) generated by R_{1k}). Plus a single reflection has det −1 (NOT
   JoinedIdQvC). This is the genuine heavy group-manifold remainder Mathlib lacks over Cut.

## GO (what THIS run banks — the W3 decomposition, bank-as-you-go)
- **L1 `evCOrth_xaxis_image_spatial`** (DERIVED): an EvC-orthogonal, time-fixing IsQvIsomC k
  sends (0,1,0) to a spatial vector (0,x',v') — the spatial-image lemma (k preserves EvC).
- **L2 `joinedIdQvC_peel_reduce`** (THE CORE): IsQvIsomC k → k(0,1,0)=(0,x',v') → v'≠0 →
  JoinedIdQvC(deflateNinth x' v' * k) → JoinedIdQvC k. (deflate_factorization +
  inflateNinth-is-JoinedIdQvC + joinedIdQvC_mul.) The SO(9)→x-fixing reduction wired into the
  NON-VACUOUS JoinedIdQvC target for the FIRST time.
- **L3 (assembly)** the genuine-compact-factor one-peel statement combining L1+L2 over the
  time-fixing compact QvC-isometry, reducing its JoinedIdQvC to the x-fixing stabilizer's.
- W8 costume C190: a WRONG peel / spatial-image / inflateNinth-Givens / JoinedIdQvC read-off
  forcing a false numeric DISTINCT from the battery (use 1=158).

## CHILD (the heavy remainder, onto the chain tail)
The octonion-block SO(8)-rotation-is-axisRotLin-word generation (x-pivot conjugation generation
of SO(8) from the banked reflection word `gFormC_isom_mem_closure`, into JoinedIdQvC) — the
genuine group-manifold remainder. OR full G₂ via expOEquiv. OR Klein-four (W4.5 optional).

## NO-GO / KILL (classify a fight as INSTRUMENT failure, not theory)
- KILL any single tactic running > 90s on one obligation → it is W9 instrument-wall; MEASURE/
  decompose/reframe, do NOT inflate maxHeartbeats, do NOT grind.
- If L1/L2 fight (won't close), it is FRAMING drift (THE ONE LAW): return to the banked engines
  (deflate_factorization, joinedIdQvC_mul) — the residual is algebra, not a posited connectedness.
- DO NOT regress to the vacuous JoinedIdC. The target predicate is JoinedIdQvC ONLY.
- Checkpoint ~50% iterations; finalize (commit + gate-bg + child + complete) with budget to spare.

## Foundations-only, words-removable, single-flight chain. NO posited group/topology/connectedness.
