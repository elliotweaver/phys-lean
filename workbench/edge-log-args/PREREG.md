# N576 PREREG — AUDIT THE EDGE-LOG ARGUMENTS AB INITIO (the LAST un-audited layer)

TASK t_ad38fbfc (OWNER R2 v7). Method: ab-initio COUNTING/tracing of banked objects (same as N574/N575),
NOT solving-for-137.036. NO-FIT. Whatever the kernel forces is the answer.

## FRONT (chosen, theory-native — NOT solving-for-endpoint)
Audit the three cutLog arguments of the dissolved freeze-out summand
`40/27 + κ·[3·L_mb + (10/3)·L_b0 − (5/3)·L_b2]` — trace each edge-log to its banked pinning and
adjudicate the double-count question the parent (N575) surfaced.

## PRE-REGISTERED PREDICTIONS (before building; the kernel decides)
P1 (mbRatioDerived exact): `mbRatioDerived = (1−q)²/(3(1+q))`, q²=1/2, IS the cross-sector heaviest
   ratio (Md/Mu)² (banked crossSector_heaviest_ratio, shape bMass0 cancels). The charm edge inverts it
   (rc=1/mbRatioDerived). CONFIRMED at THEOREM grade on `hud` (up/down leg) ALONE. → no mis-assignment.
P2 (bottom/charm edge logs): `cutLog(edgeBottom) = L_mb + L_b0 − L_b2`, `cutLog(edgeCharm) = −L_mb`,
   from edgeBottom_pinned/edgeCharm_pinned on `hud` ALONE. NEITHER carries the confinement rung 8π/3.
P3 (light edge log): `cutLog(edgeLight) = L_mb + 8π/3 + L_b2`, from edgeLight_pinned which REQUIRES the
   ADDITIONAL hypothesis `huc: (Mu/Mλ)²=1/confBundle` — the top-at-anchor identification FLAGGED
   CANDIDATE (arc-M/O2), NOT theorem-grade. The 8π/3 enters the edge-logs SOLELY here, SOLELY via huc.
P4 (double-count localization): the rung in the light edge equals `cutLog confBandRatio = 8π/3` — the
   SAME rung value the confBand screens at census 16/3 (bandScreen chargeTraceDepth confBandRatio=128/27).
   Census partition: chargeTraceDepth 16/3 = colouredCensus 10/3 + leptonCensus 2, and the light census
   censusActiveLightSpan 5/3 < colouredCensus 10/3 — the light census is a STRICT SUBSET of the coloured
   slice the confBand carries. So the rung VALUE is screened in both places at overlapping census.
P5 (verdict): the two THEOREM-GRADE norm arguments (mbRatioDerived on rb, rc; bMass0/bMass2 indices)
   CONFIRM EXACTLY. The double-count is NOT a counting question — counting is consistent with
   (i) independent rung, (ii) double-count, (iii) mis-power (16π/3) simultaneously. The discriminator is
   the CANDIDATE top-at-anchor `huc`. → confirmed-exactly branch → ESCALATE to owner (needs_input): the
   sole non-theorem-grade edge-log joint is the candidate scale-identification, an owner strategic fork.

## NON-VACUITY (W8, must bite)
- The light edge GENUINELY carries the rung: `cutLog(edgeLight) − (L_mb + L_b2) = 8π/3 > 0`.
- The bottom/charm edges GENUINELY do NOT (their logs have no cutPi term).
- The light-edge rung EQUALS the confBand rung `cutLog confBandRatio` (same value screened twice).
- The census slices are genuinely ordered: 5/3 < 10/3 < 16/3.
A bogus "light edge carries no rung / rung ≠ confBand's / censuses not nested" reading bites (C575).

## DISCIPLINE
Foundations-only ⊆ {propext,Classical.choice,Quot.sound}; DERIVED ℝ Cut only; no floats; no value
asserted-at-grade / bridge / named-residual (D3 scans /- -/ blocks — keep markers out of prose); costume
bites + manifest row + AxiomAudit import line AND #print axioms lines; single-flight; never block-wait
gate (commit → bg gate-fast → poll). NO-FIT: measured 137.036 removable prose, no empirical number in any
proof. TERMINAL: bank the audit findings, then ESCALATE (needs_input) — the task's explicit
confirmed-exactly branch; the resolution is the owner-gated candidate top-at-anchor (arc-M/O2).
