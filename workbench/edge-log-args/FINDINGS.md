# N576 FINDINGS — the edge-log arguments audited ab initio

## (1) mbRatioDerived — CONFIRMED theorem-grade
mbRatioDerived = (1−q)²/(3(1+q)), q²=1/2, DERIVED from the fold phase P=cutPi/8
(mbRatioDerived_closed; = foldPhaseBorn·2/3). It IS (Md/Mu)² (crossSector_heaviest_ratio:
sectorMass Md 0/sectorMass Mu 0 = (Md/Mu)², shape bMass0 cancels). The rc inversion
rc=1/mbRatioDerived is correct (charm edge up-mid/down-light). NO scale/inversion mis-assignment.

## (2) rb, rc shape indices — CONFIRMED theorem-grade ON hud ALONE
edgeBottom_pinned, edgeCharm_pinned pin rb=mbRatio·(bMass0/bMass2), rc=1/mbRatio from the
up/down leg hypothesis hud=(Md/Mu)²=mbRatioDerived ALONE. No candidate hypothesis. cutLog(rb),
cutLog(rc) carry NO confinement rung 8π/3 (Lrb=L_mb+L_b0−L_b2, Lrc=−L_mb).

## (3) THE ASYMMETRY + DOUBLE-COUNT (the audit's bite)
The light edge rl is STRUCTURALLY DIFFERENT:
 - rl=edgeLight pins via edgeLight_pinned which requires an ADDITIONAL hypothesis
   huc=(Mu/Mλ)²=1/confBundle — the top-at-anchor identification FLAGGED CANDIDATE in
   DownConfinementRelativeScale (arc-M/O2 charter), NOT theorem-grade.
 - The confinement rung 8π/3 enters the edge-logs SOLELY through rl:
   cutLog(rl)=cutLog(downConfBundle·bMass2)=L_mb + 8π/3 + L_b2 (cutLog_downConfBundle), where
   downConfBundle=mbRatio·(v/Λ) and 8π/3=ln(v/Λ)=cutLog confBandRatio.
 - THE SAME rung-value 8π/3 is screened by the confBand at the FULL charge census 16/3
   (bandScreen chargeTraceDepth confBandRatio=128/27, WAY-POINT-LOCKED, N575). 16/3 is the
   cap-forced TOTAL charge census (census_budget_cap_forced: =ladderChargeTrace(finrank Uhol),
   finrank Uhol=3). The light census (5/3 active / 2 plateau) is a strict SUBSET (< 16/3).

## THE UNDECIDABILITY (counting alone cannot force the resolution)
On the (Λ,v) rung 8π/3, whether the light gem κ·census·(8π/3) is
 (i) a genuine INDEPENDENT rung (the light edge relates m_s to Λ via the candidate v/Λ),
 (ii) a DOUBLE-COUNT of content already in the confBand's 16/3 (remove → gem 40/27→0, DOWN),
 (iii) MIS-POWERED (top-at-anchor should be (v/Λ)² → 16π/3 → gem 80/27, UP),
reduces ENTIRELY to the candidate top-at-anchor identification (Mu/Mλ)²=?1/confBundle.
Pure census counting is consistent with ALL THREE; the choice is the candidate scale-identification.
NONE may be chosen by direction (NO-FIT): removal moves DOWN, doubling moves UP; the candidate
top-at-anchor is the fit-danger locus.

## OUTCOME
The edge-log NORM arguments (mbRatioDerived, bMass0, bMass2, rb, rc) CONFIRM exactly (theorem-grade).
The SOLE non-theorem-grade edge-log joint is the light-edge confinement rung via the candidate
top-at-anchor identification. The double-count the ticket surfaced is REAL but its resolution is NOT
a counting question — it is the candidate scale-identification (arc-M/O2), a genuine owner-level
strategic fork. Per the task's confirm-exactly clause → BANK the audit + ESCALATE to owner
(needs_input): report the active-span bracket as the fold's standing prediction, OR derive the
top-at-anchor ab-initio next (settling independent/double-count/mis-power), OR the freeze-out
composition needs deeper re-derivation. NO-FIT: nothing bent toward measured; counting landed where
the census structure forced it.
