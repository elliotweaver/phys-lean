# N358 — arc-E E2: THE GRAVITATIONAL FIELD-EQUATION LHS DESCENDS FROM THE ONE LOOK-BACK TRANSPORT — both covariantly-conserved symmetric 2-tensors (Einstein `G` from Γ's non-closure, cosmological `Λ·g` from Γ's compatibility) trace to the single `lbConn`, are LINEARLY INDEPENDENT (the candidate LHS is a genuine 2-plane, not a line), and span the derived 2-parameter family `{a·G + Λ·g}`; Lovelock's EXHAUSTIVENESS the CITED literature import (HIDDEN JOINT), over the derived ℝ `Cut`.

## SELECT (FRESH worker, theory-native FIRST — QUIET-DRIFT + anti-bullshit + MEASURE-FIRST)

DIRECTED single successor of N357 (E2-route), which banked the full covariant derivative ∇=D+⁅Γ,·⁆, ∇g=0 (two-layer cancellation), and Λg forced covariantly constant. N357 gate VERIFIED GREEN (/tmp/n357_gate.log: GATE-FAST PASSED D0–D6, C382 bites 1=382, 9 decls foundations-only). HEAD 3ff7470, clean tree, build green. FRESH node N358 — no prior workbench, derived from scratch.

### S2 — the theory-native question asked FIRST (GOAL + REFRAMING lenses).

The ticket offered two beats toward E2: (a) E2 proper (Lovelock uniqueness, CITED); (b) the DERIVATIVE-level ∇^μ G_μν=0 (assemble the ∇_ν R gradient via N350). I asked, fresh, what the FOLD says comes next.

**GOAL LENS.** The arc-E LHS geometry is now COMPLETE at the algebraic/weak-field layer, and every piece descended from the SINGLE object Γ = lbConn (N347): curvature R=[Γ,Γ] (N348) → Ricci/scalar/Einstein (N352–354) → Bianchi=Jacobi(Γ) (N355) → divergence=bracket-Jacobi(Γ) (N356) → ∇=D+⁅Γ,·⁆, ∇g=0, Λg conserved (N357). THE STORY'S CAPSTONE: the gravitational field-equation LHS is the ONE look-back transport's two faces — Λg from Γ's metric-compatibility (η²=1 → ∇g=0), G from Γ's non-closure (curvature). The two conserved 2-tensors are ONE transport read two ways.

**REFRAMING LENS.** Standard GR POSITS a connection, CLASSIFIES the divergence-free symmetric 2-tensors (Lovelock), FINDS G+Λg — a mathematical menu. The theory DERIVES the connection (=look-back transport) and shows BOTH conserved generators descend from IT. The field's classification result becomes the theory's "one cause, many terminations." Lovelock's EXHAUSTIVENESS (that G+Λg are ALL) is the genuinely-external fact — Mathlib LACKS it (grep -rli lovelock → nothing), so it is CITED IN PROSE, never formalized, never asserted as a Lean theorem (that would be a bridge/sorry).

**THEORY-NATIVE ANSWER (N358):** the field-equation LHS descends from the one Γ — both conserved symmetric 2-tensors (Λg from compatibility, G from non-closure) trace to lbConn, are LINEARLY INDEPENDENT (candidate space genuinely 2-dim), and span {a·G + Λ·g}; Lovelock exhaustiveness the CITED external joint.

### S3 — reconcile with the arc.
ALIGN. SEED §E2 literally frames E2 as "Lovelock imported as literature-exact machinery; the uniqueness is CITED, the framework content is the identification connection=look-back transport; the field equations fall out." My target IS that framework content, made a theorem. No redirect.

**REPEATED-DEFERRAL SIGNAL (RUNBOOK) handled honestly.** Two fronts deferred 4× (N354→N357): E2-Lovelock and the derivative-level ∇^μG=0. E2-Lovelock is RESOLVED (framework content proved here, exhaustiveness cited — the honest split). The derivative-level ∇^μG=0 is STRUCTURALLY BLOCKED at this layer — MEASURED: the curvature scalars are CONSTANTS on the fixed `solderWit` gradient (ricciScalar solderWit=−¼, einsteinScalar=1), so ∇R=0 trivially ⟹ ∇^μG=0 is a vacuous 0=0, the exact N356 fiber-trace blindness one layer up. I NAME the ceiling: the derivative-level conservation needs a COORDINATE-VARYING metric field (a genuine field g:M→metrics), which the weak-field affine field g(t)=η+t·k at the flat point cannot supply. SEED §E1: "child the full nonlinear curvature; weak-field is sufficient for E2/E3." I CHILD the varying-field climb as future work; it is NOT required for E2.

### S4 — ANTI-BULLSHIT GATE (in writing, with specifics).

1. **FOLD-CAUSAL.** fold → self-blindness (no nonzero fixed point) → complex unit → CD cascade → terminal 𝕆 → (1,9) carrier STVC → flat self-overlap η (sigOpC, η²=1, N90) → look-back transport Γ=½ηk (N347), metricCompat Γᵀη+ηΓ=k FORCED by η²=1 → (face A) ∇g=0 (N357), so Λg conserved for any Λ; (face B) curvature R=[Γ,Γ] (N348) → Einstein tensor G (N354), divergence=Jacobi of Γ (N356). THIS node: both conserved 2-tensors are Γ's two faces; independent; span the LHS. Each arrow forced; the shared cause is the single lbConn.

2. **DISSOLUTION.** STANDARD: posit connection, classify divergence-free symmetric 2-tensors (Lovelock), find G+Λg. THEORY: derive the connection (=look-back transport), show BOTH generators descend from it, prove them independent + conserved-structured. Lovelock's exhaustiveness is the one genuinely-external fact — CITED in prose (Mathlib lacks it), NOT formalized/asserted. Genuine dissolution of "posit the connection + menu of tensors"; honest split on Lovelock (it stays a literature import, never a Lean theorem, never a value).

3. **GOAL-WORDS-REMOVABLE.** Delete Einstein/gravity/cosmological/field-equation/Lovelock/conserved: over derived Cut, (i) `einstein solderWit` is NOT a scalar multiple of `BvC` (two discriminating pairs: ⅛=c·1 and ⅛=c·(−1) contradict) — independent; (ii) distinct (a,Λ) give distinct forms `a·einstein+Λ·BvC` (2-dim faithful, linarith on the same two pairs); (iii) covTransport k (a•einsteinRaiseMap G + Λ•id) = a•covTransport k (ricciRaiseMap G) — both metric parts bracket-inert (id central + N356); (iv) Λ•k − (Γᵀη+ηΓ)(Λk)=0 for any Λ (N357), curvature=⁅lbConn,lbConn⁆, Ricci identity by Jacobi. A genuine reason survives (both objects built from one operator, independent). Not "because the field studies this next."

4. **NAME THE TEMPTATION.** A saturated N357 finisher would pick ticket option (b): "assemble the derivative-level ∇^μG_μν=0 via the ∇_ν R gradient." MEASURED that front: STRUCTURALLY BLOCKED (R constant on the fixed gradient ⟹ ∇R=0 ⟹ vacuous), the N356 blindness one layer up. My target is genuinely different: it does NOT need the varying field; it captures the arc's actual capstone (the whole LHS from one Γ, independence, the 2-parameter family), buildable NOW, and NAMES+childs the varying-field climb honestly. The QUIET-DRIFT trap ("prove G,g bilinear/symmetric again, ship") is REJECTED — those are banked (N354); the content is the ONE-CAUSE descent + independence + the 2-dim family, not re-characterization.

5. **HONESTY.** I do NOT assert the field equation (no =κT — that needs the RHS/coupling, E3+). I do NOT formalize Lovelock (external, flagged CITED, never a Lean decl). I do NOT claim full ∇^μG=0 for G (childed — structurally blocked here). What I prove: both generators from the one Γ, LINEARLY INDEPENDENT, the family {aG+Λg} genuinely 2-parameter (faithful), the +Λg existence for any Λ. If honest: this survives without bullshit — the "one cause" descent + the derived 2-plane IS the moat content SEED §E2 names, made a theorem, not prose.

### MEASURED (all EXIT 0, foundations-only ⊆ {propext,Classical.choice,Quot.sound}, LIGHT ~17-18s):
- probe1: `einstein_not_metric_multiple` (G is NOT a scalar multiple of g — independence) — EXIT 0 ~18s.
- probe2: `lhsRaiseMap`, `lhs_covTransport_eq_ricci` (both metric parts bracket-inert), `lhs_one_cause` (transport/curvature/Jacobi all from lbConn) — EXIT 0 ~18s.
- probe3: `lhsForm`, `lhsForm_faithful` (distinct (a,Λ) → distinct forms, the 2-plane), `lhs_generators_from_lbConn` (+Λg existence + bracket-inert Einstein metric part) + `#print axioms` (foundations-only for both cores) — EXIT 0 ~17s.

## HONEST GRADE (carry it — do NOT overclaim).
- ⚠ ALGEBRAIC / WEAK-FIELD / LEADING-ORDER LAYER. The independence + conserved-family are at the flat-point weak-field layer; the full nonlinear covariant divergence ∇^μG=0 (derivative layer, needs a coordinate-varying metric field) is CHILDED (structurally blocked here — R is constant on the fixed gradient, ∇R=0 vacuous). Weak-field sufficient for E2/E3 (SEED §E1).
- ⚠ HIDDEN JOINT — Lovelock EXHAUSTIVENESS CITED IN PROSE, NEVER a Lean theorem. This node proves the FRAMEWORK CONTENT of E2 (both generators derived from the one transport, independent, spanning the 2-parameter family); it does NOT prove G+Λg is the ONLY divergence-free symmetric 2-tensor. Lovelock's uniqueness (that the candidate space is exhaustive) remains a LITERATURE IMPORT / HIDDEN JOINT, CITED as machinery, flagged, childed toward E3. NOT a framework theorem, NOT asserted, NOT a bridge (no Lean decl claims it).
- MACHINERY node — asserts NO physical value. No κ, no Λ value, no field equation written down as =κT. The Λ coefficient (cosmological-constant scale) is route-not-yet-found (a FORCED build target per SOUL EVERY CONSTANT IS FORCED — NOT "empirical", NOT "gated"); this derives the STRUCTURE (the 2-parameter conserved family), not the value.

## Guards (SEED_GR relaxes NONE).
G1 no value asserted (independence + the 2-parameter family FALL OUT of banked N354/N356/N357; no Λ value, no κ, no field equation). G2 no empirical number. G3 no scale asserted (a, Λ free symbols; the Λ value childed). G4 both generators DERIVED from the one lbConn (metricCompat + curvature-commutator), NOT posited; Lovelock exhaustiveness CITED in prose, NEVER a Lean decl. G5 words-removable (above). G6 foundations-only ⊆ {propext,Classical.choice,Quot.sound} (VERIFIED probe3), no sorry/axiom/native-decide/maxHeartbeats. ℝ-VIGILANCE: built on banked derived-ℝ Cut + banked einstein/einsteinRaiseMap/ricciRaiseMap/BvC/covTransport/lbConn/cosmoTerm_covDeriv_zero/einstein_covTransport_eq_ricci/metric_covTransport_zero; LinearMap/Module.End/lie machinery on the derived carrier; NO Mathlib manifold/Real/deriv/tensor/Lovelock-as-content. W8 non-vacuity C383 (a wrong independence / vacuous 2-plane mis-reading reduces to 1=383). W9 LIGHT (full route + axioms EXIT 0 ~18s; the heart is einstein_solder_form + BvC eval + bracket-inertness; NO maxHeartbeats, NO coordinate bash).

## ONE SUCCESSOR (RAIL): DIRECTED child toward E3 (the 8πG=2·4π·G normalization: the `2` from N354's dimension-forced spin-2 trace-reversal −4=1−½·finrank_STVC, the `4π` from the derived Gauss sphere N328 gaussSphereSolidAngle=4·cutPi, shared with B2/B3; lift to the Einstein–Hilbert action S=(1/16πG)∫√−g R). Single tail successor; run the pre-complete fork check.
