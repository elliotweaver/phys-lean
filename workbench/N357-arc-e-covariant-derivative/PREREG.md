# N357 — arc-E E2-route: FULL METRIC-COMPATIBILITY ∇g=0 AS THE TWO-LAYER CANCELLATION (the fold's two faces cancel), and the COSMOLOGICAL TERM Λ·g FORCED as covariantly constant — the theory-native "+Λ g" of Lovelock (existence DERIVED; exhaustiveness CITED as the HIDDEN JOINT), over the derived ℝ Cut.

## SELECT (FRESH worker, theory-native FIRST — QUIET-DRIFT + anti-bullshit + MEASURE-FIRST)

DIRECTED single successor of N356 (E2-route), which banked the divergence STRUCTURE as the ONE
bracket-Jacobi (`ricci_identity`), the naive scalar divergence IDENTICALLY BLIND (`divFiberTrace_blind`,
trace_lie — self-blindness at conservation), and the trace-reversal metric term BRACKET-INERT
(`einstein_covTransport_eq_ricci`, localizing ∇^μG_μν=0's remaining content to the DERIVATIVE layer).
N356 gate VERIFIED GREEN (/tmp/n356_gate.log: GATE-FAST PASSED, D0–D6, C381 bites 1=381, 10 decls
foundations-only). HEAD 12f7a0c, clean tree, build green. FRESH node N357 — no prior workbench, derived
from scratch.

### S2 — the theory-native question asked FIRST (GOAL + REFRAMING lenses), do NOT run on the ticket framing.

The ticket offered two beats toward E2: (a) E2 proper (Lovelock uniqueness, CITED); (b) the
DERIVATIVE-level ∇^μ G_μν=0 (assemble the ∇_ν R gradient via N350). I asked, fresh, what the FOLD says
comes next.

**GOAL LENS.** The arc-E story: the connection is the fold's LOOK-BACK TRANSPORT (N347), the curvature
its commutator (N348), and the fold has TWO faces that were built SEPARATELY — the C6 eternal-approach
DIRECTIONAL DERIVATIVE (N350 `HasOpDerivAt0`, the `dΓ` face) and the LOOK-BACK TRANSPORT COMMUTATOR
(N356 `covTransport = ⁅Γ,·⁆`, the `Γ∧Γ` face). N356 defined the covariant transport as the BRACKET
half ONLY, and localized ∇^μG_μν=0's remaining content to the derivative layer — but did NOT unite the
two faces into the FULL covariant derivative ∇ = D + ⁅Γ,·⁆. THE STORY'S NEXT BEAT: unite the two faces.
And when you do, the fold says something forced: the metric g's OWN variation `D(g)=k` (the derivative
face, N350) is EXACTLY UNDONE by the look-back transport action `Γᵀη+ηΓ=k` (the transport face, N347).
The self-overlap carrying itself forward is exactly unwound by the look-back — so ∇g = k − k = 0. The
fold's two faces CANCEL on the metric. This is FULL metric-compatibility, from the ONE CAUSE η²=1.

**REFRAMING LENS.** What does the theory say about "the LHS of Einstein's equations" that the field does
not? Standard GR IMPOSES ∇g=0 as the DEFINING axiom of the Levi-Civita connection and solves for the
Christoffel symbols. Here ∇g=0 is DERIVED — the two-layer cancellation, both layers = k from η²=1. And
the theory reframes Lovelock's "+Λ g_μν": the field notes a SECOND divergence-free symmetric 2-tensor
(Λg) as a mathematical possibility to be classified. Here it is FORCED: because g is covariantly
constant (∇g=0), ANY scalar multiple Λ·g is covariantly constant too — the cosmological term EXISTS as a
divergence-free 2-tensor, forced by the fold. The theory DERIVES the "+Λ g" half of Lovelock's answer;
Lovelock's EXHAUSTIVENESS (that G + Λg is the ONLY such tensor) stays a CITED literature import — the
HIDDEN JOINT, flagged.

**THE THEORY-NATIVE ANSWER:** the full covariant derivative ∇ = D + ⁅Γ,·⁆ (uniting the fold's two
faces), FULL metric-compatibility ∇g=0 (the two layers cancel), hence the cosmological term Λ·g forced
as covariantly constant — the "+Λ g" of Lovelock, existence DERIVED, exhaustiveness CITED.

### S3 — reconcile with the arc.
ALIGN. The arc heads E1→E2. The honest E1→E2 bridge is: complete the covariant-derivative machinery
(the full ∇, the two-layer ∇g=0 that N347 EXPLICITLY childed — "the derivative producing k is childed to
the curvature build" — and N356 EXPLICITLY flagged — "metric_covTransport_zero is the id-central shadow;
the deeper content is N347 metricCompat"), and derive the "+Λ g" half of Lovelock. This node RESOLVES
two explicitly-childed pieces (N347's derivative layer, N356's full metric term) and delivers the
theory-native content toward E2 (the divergence-free tensors are G [N356 structure] and Λg [here];
Lovelock exhaustiveness CITED). No redirect needed — the theory and arc agree, and this is the honest
preceding beat before E2 proper.

### S4 — ANTI-BULLSHIT GATE (in writing, with specifics).

1. **FOLD-CAUSAL.** fold → self-blindness has no nonzero fixed point → the complex unit → Cayley–Dickson
   → terminal octonions → the (1,9) signature carrier STVC → the flat self-overlap η (`sigOpC`, η²=1,
   N90) → the look-back transport Γ=½ηk (N347) with metric-compat Γᵀη+ηΓ=k FORCED by η²=1 → the C6
   directional derivative on the operator field (N350, the eternal-approach one level up) → THIS node:
   the full covariant derivative ∇=D+⁅Γ,·⁆ unites the two faces, and the metric's derivative D(g)=k is
   exactly cancelled by the transport action Γᵀη+ηΓ=k (BOTH = k, from η²=1), so ∇g=0, hence Λg
   covariantly constant. Each arrow forced; the cancellation is the ONE CAUSE η²=1 delivering both = k.

2. **DISSOLUTION.** STANDARD version: GR IMPOSES ∇g=0 as the Levi-Civita defining axiom and solves for
   Christoffels; Lovelock CLASSIFIES the divergence-free symmetric 2-tensors and finds G_μν + Λ g_μν.
   What the theory DOES: (i) ∇g=0 is DERIVED, not imposed — the two banked layers cancel, both = k from
   η²=1 (the field's axiom becomes our theorem); (ii) the "+Λ g" is FORCED — Λg is covariantly constant
   because g is (∇g=0), so the cosmological term's EXISTENCE as a divergence-free 2-tensor falls out of
   the fold, no free parameter posited. Lovelock's EXHAUSTIVENESS (nothing else) is the genuinely
   external fact — CITED, flagged as the HIDDEN JOINT. Genuine dissolution of the "impose ∇g=0" axiom;
   honest split on Lovelock.

3. **GOAL-WORDS-REMOVABLE.** Delete "metric/connection/covariant/cosmological/gravity/Levi-Civita":
   over the derived complete ordered field Cut, for the involution η (η²=1) and the linear map Γ=½η(·)
   on Module.End Cut STVC, the affine field t↦η+t•k has directional derivative k (in the banked operator
   topology), and k − (½kη·η + η·½ηk) = k − k = 0; and Λ•k − (…) = 0 for any Λ; and the full derivative
   D+⁅Γ,·⁆ is unique + additive; non-vacuity on concrete timeProj/offDiagVar. A genuine reason survives:
   the derivative of the self-overlap field equals the transport action, so their difference (the
   covariant derivative) vanishes, and scalar multiples stay covariantly constant. Not "because the field
   studies this next."

4. **NAME THE TEMPTATION.** The saturated N356 finishing worker would pick ticket option (b): "assemble
   the full derivative-level ∇^μ G_μν=0 via the ∇_ν R gradient." That target hits the SAME fiber-trace
   BLINDNESS N356 measured (the scalar divergence contraction is identically 0, trace_lie); the genuine
   ∇^μ G_μν=0 would need the divergence index SOLDERED (like N352's Ricci), a heavier build that may be
   blind again — and you CANNOT state ∇^μ G_μν=0 at all until the FULL ∇ operator (both layers) exists.
   My target is the honest PRECEDING beat: build the full ∇ (uniting the two faces), which delivers the
   contentful ∇g=0 + the forced "+Λ g" directly feeding E2, and sets up the soldered ∇^μ G_μν=0 as a
   later child. It is genuinely different (not the blind scalar contraction) and genuinely forced (the
   two-layer cancellation is η²=1). The QUIET-DRIFT trap ("define ∇, prove unique+additive, ship the
   routine adjacent lemmas") is NAMED and REJECTED: uniqueness/additivity ARE recorded but are NOT the
   content — the content is the two-layer cancellation ∇g=0 (resolving N347/N356's childed pieces) and
   the forced cosmological term.

5. **HONESTY.** The second conjunct of the metric-compat theorem (k − (Γᵀη+ηΓ) = 0) is N347 metricCompat
   rearranged — I do NOT claim it as new. The NEW content is (i) the derivative-layer fact D(g)=k (N350,
   explicitly childed at N347), (ii) the UNION giving the full ∇g=0, (iii) the cosmological term Λg
   covariantly constant for ANY Λ (the "+Λ g" existence), (iv) the full ∇ operator uniting the two faces.
   This is genuinely theory-native (the fold's two faces cancel on the metric), not a relabeled textbook
   step. If I'm honest: this survives the gate without bullshit.

### MEASURED (all EXIT 0, foundations-only, LIGHT ~10-14s):
- probe1: `metric_covDeriv_zero` (∇g=0 via the id-central shadow — RECORDED but NOT the headline).
- probe2: `covDeriv_const`, `metric_covDeriv_zero`, `cosmoTerm_covDeriv_zero` (shadow form),
  `covDeriv_unique`, `covDeriv_nonvacuous_bracket`, `covDeriv_nonvacuous_deriv` — all closed.
- probe3: `metricField_opderiv` (D(g)=k), `metricCompat_full` (∇g=k−k=0, GENUINE two-layer),
  `cosmoTerm_compat` (∇(Λg)=0), `cosmoField_opderiv` (D(Λg)=Λk) — the CONTENTFUL forms.
- probe4: consolidated final shapes — `HasCovDerivAt0`, `covDeriv_const`, `covDeriv_unique`,
  ★★★ `metricField_covDeriv_zero` (D(g)=k ∧ ∇g=0 two-layer), ★★ `cosmoTerm_covDeriv_zero`
  (D(Λg)=Λk ∧ ∇(Λg)=0), ★ `covDeriv_nonvacuous`, ★ `metricCompat_half_load_bearing` (dropping ½ ⟹
  k−2k=−k≠0, the ½ load-bearing at the ∇g level).
- probe_axioms: all core theorems ⊆ {propext, Classical.choice, Quot.sound}. EXIT 0.

## HONEST GRADE (carry it — do NOT overclaim).
- ⚠ ALGEBRAIC / LEADING-ORDER (WEAK-FIELD) LAYER. The full ∇g=0 is at the flat point (g=η, variation k);
  the full nonlinear covariant derivative is childed (weak-field sufficient for E2/E3, SEED_GR §E1).
- ⚠ HIDDEN JOINT — Lovelock EXHAUSTIVENESS CITED. This node DERIVES the "+Λ g" EXISTENCE (Λg
  covariantly constant, forced) but does NOT prove G+Λg is the ONLY divergence-free symmetric 2-tensor
  — Lovelock's uniqueness (E2 proper) remains a LITERATURE IMPORT / HIDDEN JOINT, childed as the
  successor. Flag it CITED, NOT a framework theorem.
- MACHINERY node — asserts NO physical value. The VALUE of Λ (the cosmological constant scale) is
  route-not-yet-found (a FORCED build target, per SOUL EVERY CONSTANT IS FORCED — NOT "empirical", NOT
  "gated"); this node derives the STRUCTURE (Λg is divergence-free for any Λ), not the number.

## Guards (SEED_GR relaxes NONE).
G1 no value asserted (∇g=0 and Λg-covariantly-constant FALL OUT of banked N347/N350; no Λ value, no κ,
no field equation). G2 no empirical number. G3 no scale asserted (Λ is a free symbol; its value is a
childed build target). G4 ∇g=0 is DERIVED (two-layer cancellation, η²=1), NOT the imposed Levi-Civita
axiom; the "+Λ g" is FORCED (g covariantly constant); Lovelock exhaustiveness CITED, NOT asserted.
G5 words-removable (above). G6 foundations-only ⊆ {propext,Classical.choice,Quot.sound} (VERIFIED
probe_axioms), no sorry/axiom/native-decide/maxHeartbeats. ℝ-VIGILANCE: built on banked derived-ℝ Cut +
banked HasOpDerivAt0/covTransport/lbConn/lbConnAdj/sigOpC/metricCompat/MetricEnd; Tendsto/Module.End/
LinearMap machinery on the derived carrier; NO Mathlib manifold/Real/HasDerivAt/deriv/fderiv as content.
W8 non-vacuity C382 (a wrong ∇g / vacuous mis-reading reduces to 1=382). W9 LIGHT (full route + axioms
EXIT 0 in ~14s; the heart is metricCompat + affine_opderiv, everything else bracket/limit algebra + one
concrete component; NO maxHeartbeats, NO coordinate bash).

## ONE SUCCESSOR (RAIL): DIRECTED child toward E2 proper (Einstein's equations UNIQUE via Lovelock
literature import applied to the banked einstein tensor + the derived Λg — a HIDDEN JOINT, CITED), OR a
decompose-remainder child if E2 needs the soldered ∇^μ G_μν=0 as a preceding beat. Single tail
successor; run the pre-complete fork check.
