# PREREG — R2 RECOMPOSE, node 1: DECOUPLING AS REMOVAL (the census-conserving descent frame theorem)

Task t_552c142e (owner R2, NO-BLOCK mandate). First node of the endpoint recompose.

## THE R2 FINDING (owner-confirmed), restated precisely
The additive-summand assembly (single 16/3 matter-census tower + second ceiling as CONSTANT +70
+ bolt-on freeze-out ADDED) is the wrong composition ARCHITECTURE. The consistent worse-with-
completeness overshoot (+0.91 bare-recombined, worse at +1.94 leadingCarrier) is a ONE-LAW signal.

## THE FORCED THEORY-NATIVE CORRECTION (this node)
The banked `census_conserves_depth` (D2/X3, `census_generation_is_depth`) says:
    2·((colouredCensusTerm 2 + colouredCensusTerm 1) + 1) = chargeTraceDepth  (16/3),
i.e. the ONE charge census `16/3` is RE-PARTITIONED across the bands — nothing added, nothing lost.
Split:  coloured slice = 2·(4/3+1/3) = 10/3 ;  singlet (lepton) slice = 2·1 = 2 ;  sum = 16/3.

The additive frame's bug: the lepton tail (`leptonTail chargeTraceDepth`, census 16/3) carries the
FULL charge census — INCLUDING the coloured quark content that has already DECOUPLED below the
confinement / quark thresholds. Below those edges the coloured content DECOUPLES: it must be
REMOVED from the running census (a subtraction of active content), never re-carried as a fresh
positive summand. The census-conserving lepton band therefore carries the SINGLET census `= 2`
(= chargeTraceDepth − colouredCensus), and the difference `16/3 − 2 = 10/3 = colouredCensus` is
exactly the decoupled coloured content REMOVED (positive removal → runs 1/α DOWN → the ONE-LAW
direction that corrects the overshoot).

## WHAT THIS NODE BANKS (forced, grounded, no asserted value, no fit)
- `colouredCensus = 10/3`, `leptonCensus = 2`, forced from banked `census_conserves_depth`
  (leptonCensus = chargeTraceDepth − colouredCensus).
- The census-conserving lepton tail telescopes (banked `leptonTail_screen_telescope`) to
  `bandScreen leptonCensus (mass0/mass1)`.
- THE REMOVAL: `totalScreening (leptonTail chargeTraceDepth) − totalScreening (leptonTail leptonCensus)
   = bandScreen colouredCensus (mass0/mass1) > 0` — the decoupled coloured screening removed
  (linear-in-census `bandScreen`, banked).
- WAY-POINTS REPRODUCE FREE: both high bands are UNTOUCHED by the low-band census choice
  (banked `confWaypoint_free`/`ewWaypoint_free` hold for ANY lower list) → `1582/27`, `190/3`
  land free on the census-conserving assembly exactly as on the additive one.
- The census-conserving endpoint sits STRICTLY BELOW the additive one (removal direction).
- TEETH: `leptonCensus ≠ chargeTraceDepth` (2 ≠ 16/3); removal strictly positive;
  `colouredCensus ≠ 0`; the two frames genuinely differ.

## ANTI-BULLSHIT GATE (run FIRST, in writing — SOUL)
- Trace fold→node: census_conserves_depth (D2) ⟸ finrank/occupation (X3) ⟸ … ⟸ fold. The
  re-partition identity IS a banked theorem; the split into coloured/singlet is `norm_num` on it.
- Standard version & what the theory DOES: standard QED IMPORTS the decoupling theorem + measures
  each threshold. The fold COUNTS the census and PROVES conservation, so decoupling is a REMOVAL
  forced by census conservation, not a matching condition. Genuinely theory-native.
- Delete physics words: pure statement that `2·((cCT2+cCT1)+1)=chargeTraceDepth` splits as
  `10/3 + 2`, that `totalScreening(leptonTail 16/3) − totalScreening(leptonTail 2) =
  bandScreen (10/3) (mass0/mass1) > 0`, and that appending any lower list leaves `190/3`/`1582/27`
  as partial sums. Survives with all physics words gone → grounded on banked Cut objects.
- NOT a fit: NO endpoint value asserted; the census `2` is FORCED by census conservation, not
  chosen to hit `137.036`. (Numerically the pure-singlet lepton tail alone UNDERSHOOTS; this node
  does NOT claim it as the endpoint — it banks the FRAME correction only. The full recomposed
  endpoint = per-channel running descent (banked TwoChannelDescent) + this decoupling removal +
  recombination, evaluated in the successor nodes.) The measured value stays removable prose.
- Free-floating check: every production TYPE mentions `chargeTraceDepth`/`colouredCensusTerm`/
  `leptonTail`/`totalScreening`/`bandScreen`/`mass0`/`mass1`/`confWaypoint_free` over `Cut`.

## SUCCESSOR (one directed child)
Toward the per-channel RUNNING-DESCENT recombination over the census-conserving descent (the
iso channel decoupling below the electroweak edge via banked netIsoDepth, hyper abelian running
full; recombine via the banked Z2/X7 weight), assembling toward the D7 restate AS 1/α(0).

## foundations-only; costume + AxiomAudit appended; gate-fast.
