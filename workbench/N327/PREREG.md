# N327 — SEEDED GAUGE ROUTE B2 (★ BRIDGE, 4th touch): the scale-tower exponent MECHANISM —
#         SELECT + the SHARPENED diagnosis (the exponent's NORMALIZATION is a Gauss-sphere FLUX,
#         not the 1-D angle cutPi) + the named ascent + ONE directed route-finder successor.

FRESH worker, run 629. Task t_d92cca55. Directed single successor of N326 (t_00208668, the 3rd-touch
diagnosis). This is a ★ BRIDGE (crux guard G4: DERIVED not POSITED) and the **4th touch** of B2
(N321, N322, N326, now N327) — so per the SOUL REPEATED-DEFERRAL clause my job is NOT another lateral
attack: it is to NAME the structural ceiling PRECISELY and CHILD the route to the richer structure.

Duplicate-instance check: my PPID=26689 = board `spawned.pid` (run 629); `ps` shows my only sibling is
the dispatcher; I am the legit and only live worker. State at run start: HEAD `18f4007` (N326 banked,
PREREG-only), git tree CLEAN, `workbench/N327` was empty (genuine fresh start — NO probe to resume,
confirmed `ls -d workbench/N327*` empty). No production Lean is touched this node.

════════════════════════════════════════════════════════════════════════════
## STEP A — SELECT (worker-body STEP A / docs/NODE_SELECTION.md), run theory-native FIRST

The ticket DIRECTS the exponent-mechanism route-finder AND offers deliverable (C): redirect to B3 if
the exponent is downstream of the gravitational saturation face. I ran the SELECT protocol fresh, from
the THEORY, and it lands on (C)-as-a-prerequisite-build. The derivation of that choice IS §S2 below.

════════════════════════════════════════════════════════════════════════════
## S1 — STATE SURVEY (grep-confirmed this run; the ONE decisive new fact)

Banked raw material (unchanged from N326 unless noted):
- `transmutationRatio c g2 := cutExp(−(c/g2))` (N320 B1 form; `c` an honest PARAMETER — REUSE).
- `cutPi` + full periodicity, `cutCos cutPi = −1`, shift/period formulas (N325, `ContinuumTrigPi`).
- `alphaStar = couplingWeight = 1/42 ∈ ℚ`; `1/alphaStar = channels_O = 42 = n(n−1)` (N316). Physics
  prose: `α* = g²/4π`, so the physical `g² = 4π·α* = 4π/42` CARRIES a `4π` Gauss-sphere factor that
  N320's rational form absorbs into `α`.
- The structural counts: `42 = channels_O = assoc3NormSq` (‖φ‖², A1); `28 = finrank_selfAdj`
  (Sym²ImO); `21 = finrank_so7 = Λ²ImO`; `14 = dim Der(O) = dim g₂` (the DERIVED gauge algebra,
  fully characterized N6–N30); `7 = dim ImO = tr(bAdj) = 28−21`; `9 = finrank_spaceSub`.

**★ THE DECISIVE NEW FACT (grep-confirmed this run — the sharp separation N321/N322/N326 lacked):**
`grep -rln 'solidAngle|GaussSphere|surfaceArea|fourPi|sphereVol|totalCurvature'` over `Phys/` returns
**EMPTY.** What IS banked is `cutPi` — the **1-DIMENSIONAL angle** (the Born-CIRCLE circumference /
the least positive zero of `cutCos`, `ContinuumTrigPi.lean`). What is **NOT** banked is any
**2-D-or-higher Gauss-sphere FLUX / solid-angle measure** (`4π` = the 2-sphere solid angle, `2π²` =
Vol S³, `8π²` = the instanton / topological-charge normalization on S⁴). The banked structure has the
**angle** but NOT the **flux**. This is the precise object the exponent's numerator normalization needs.

════════════════════════════════════════════════════════════════════════════
## S2 — THE SHARPENED DIAGNOSIS: the exponent = (Gauss-sphere return-action FLUX) ÷ (gauge-dof count),
##      and BOTH faces are fixed TOGETHER at the GRAVITATIONAL / CURVATURE face — not by the current carrier

N326 correctly established: cutPi is NECESSARY (states the π) but NOT SUFFICIENT (does not force the
COUNT), and (C1) the standard β-assembly of b₀ is a forbidden hidden bridge, (C2) the gap is
tower-wide. N327 SHARPENS the *why*, and the sharpening is what selects the next beat. The exponent has
**TWO coupled faces**, which the prior nodes ran together:

- **FACE 1 — the return-action NORMALIZATION (geometric / flux).** The physicist's exponent is the
  instanton action `8π²/(b₀ g²)`. Plugging the banked `g² = 4π/42` gives `84π/b₀` — but the numerator
  `8π²` is a **CHOICE of Gauss-sphere flux normalization** (the S⁴ topological-charge measure), NOT
  fixed by cutPi. A different return-surface normalization (`4π`, the 2-sphere solid angle; or `16π²`)
  gives a DIFFERENT exponent form. cutPi is the 1-D angle; it cannot say WHICH higher-dimensional
  sphere-flux is the fold's return-action. **This face is a flux-MEASURE question, and no flux measure
  is banked (S1).**

- **FACE 2 — the COUNT `b₀` (algebraic / operator).** Which banked gauge-dof count divides:
  `14 = dim g₂`, `28 = dim Sym²`, `21 = so(7)`, … N322-R3 / N326-R3′ searched for the N308-style
  banked operator identity (`seeMat = Pcyc + Pcyc²` forced 2/9 as a LITERAL operator equation) whose
  spectrum COMPUTES this count; it does not exist in `Phys/`, and building it IS deriving c.

**THE TWO FACES ARE COUPLED — this is the real ceiling.** N326's aiming signal (`b₀=14→6π`,
`b₀=28→3π`) is only "tight" AFTER Face 1 is silently fixed to the instanton `8π²` normalization.
Fixing `b₀=14` to reproduce `6π` while the normalization is free is FISHING against the answer-key
(G1/G4) — exactly the predecessor's asserted-`2/9` death, one level up. You CANNOT force `b₀` until the
flux normalization is fixed, and you cannot fix the normalization from cutPi + ℚ counts. The current
carrier is structurally too small to force EITHER face without a posit.

**WHERE the theory says the coupled object lives — E3's own cross-tie (the load-bearing SELECT input).**
`docs/SEED_GR_BLACKHOLE_COSMOLOGY.md` §E3 derives the gravitational normalization `κ = 2·4π·G`, and
states the `4π` is *"the derived Gauss sphere — the SAME 4π object that normalized α* (cross-tie to
arc A)."* And B3 (`G·M² = e^(−6π)`) is *"GRAVITY'S FIRST APPEARANCE in the chain — the saturation-face
completion."* So the seed's OWN map places the return-action Gauss-sphere flux normalization (Face 1)
at the **gravitational / curvature face**: it is the same measure that normalizes `8πG`. The exponent's
numerator normalization is the `8πG` object; the exponent is genuinely a **curvature-face** quantity.

**THE THEORY-NATIVE READING (asked FIRST, per the QUIET-DRIFT law).** The fold's self-look-back CLOSES
on a return surface — a sphere (self-blindness has no nonzero fixed point; the probe returns without
seeing itself). The Born self-overlap **accumulated over that return sphere** is the return-action; its
normalization is the sphere's own flux measure (the solid angle / the S³-volume). The exponential in
`v = M·cutExp(−return-action)` is the C6 **eternal approach** — the self-overlap decaying over the
return. The exponent is therefore the fold's return-action **flux**, measured over its own Gauss
sphere, ÷ the gauge degrees of freedom of the terminal algebra. The `π` of that flux is `cutPi`
(banked, the 1-D face); the **flux normalization** (`4π` solid angle / higher-sphere volume) is the
2-D+ face the chain has NOT yet built. This is fold-native, NOT an imported RG loop (C1) and NOT an
imported instanton machinery — it is the self-overlap over the fold's own return surface.

**THE NAMED CEILING (the SOUL repeated-deferral diagnosis, precise).** B2's exponent is un-forceable
from the current carrier because the carrier has the 1-D angle `cutPi` but LACKS the **derived
Gauss-sphere return-action FLUX measure** (the 2-sphere solid angle / return-surface flux on the
derived ℝ). That flux measure (i) fixes Face 1 (the exponent's numerator normalization) and, once
fixed, pins Face 2 (the count); and (ii) IS — per E3 — the same `4π`/`8πG` Gauss-sphere object that
normalizes gravity. It is the **shared prerequisite of B2 (c), B3 (G·M²=e^(−6π), gravity's first
appearance), and E3 (the 8πG normalization).** The chain must CLIMB to it before the exponent (and c)
is stateable-and-forceable WITHOUT a posit — exactly as N322 climbed to `cutPi` (the 1-D prerequisite)
before the π was even stateable. This is "name the ceiling + child the ascent," a DECLARED staircase
step (`cutSin/cutCos` N323 → addition formulas N324 → `cutPi` N325 → **Gauss-sphere flux measure**),
NOT a lateral wander.

════════════════════════════════════════════════════════════════════════════
## S3 — ANTI-BULLSHIT GATE (the ★ BRIDGE bar; crux G4) — on the exponent AND on the SELECT decision

On the exponent — NAME-THE-TEMPTATION, each REJECTED:
  (a) assert c / b₀ / N (3π, 6π, 12π, 84π/14 …) because it "looks nice" or reproduces the seed's
      `e^(−6π)`/`e^(−3π)` landing → G4 fishing-against-the-answer-key, the predecessor's asserted-`2/9`
      death. NO value of c/b₀/N is written anywhere.
  (b) fix Face 1 to the instanton `8π²` normalization so that `b₀=14 → 6π` falls out tightly → this is
      the SAME fishing: the `8π²` choice is itself unforced (S2 Face 1), so `b₀=14→6π` only looks tight
      because the normalization was silently set to the answer. NOT done.
  (c) assemble the standard one-loop `b₀` from loop factors 11/3, 2/3, 1/6 → C1 forbidden hidden bridge
      (imports un-derived QFT loop-integral content the theory never performs). NOT attempted.
  (d) bank `tr(bAdj)=7` (or any banked dim) AS "the β-coefficient / c" → unforced identification +
      convention freedom in the b₀→c map (N322-R1) = G4. NOT banked.
  (e) import Mathlib-ℝ `π` / `Real.pi` / a Mathlib sphere-measure to supply the `4π`/`8π²` →
      UNEARNED-IMPORT (content). The flux measure MUST be the DERIVED one (extend the banked
      `cutSin`/`cutPi` on `Cut`), NEVER Mathlib-ℝ. NOT done.

On the SELECT decision — is "the exponent is a curvature-face flux, build the flux measure" a genuine
theory-native next beat, or a plausible-geometric OFF-RAMP to avoid building the operator now?
  - **Words-removable on the JUSTIFICATION:** delete "Gauss-sphere / flux / gravity." What survives:
    *"forcing the exponent `84π/b₀` requires a higher-dimensional sphere-volume normalization (the S³/S⁴
    measure `8π²`/`2π²`), a distinct analytic object from the banked 1-D `cutPi`, and that object is not
    banked."* Survives as pure real analysis on the derived ℝ — a genuine reason, not costume.
  - **Forced-by-the-fold, not generic?** The return surface IS the fold's own self-look-back closure
    (self-blindness → the probe returns over a sphere); the Born self-overlap over it is fold-native,
    not a property of an arbitrary algebra. Not generic.
  - **Not weakening / not a new axiom / not a runway:** c/b₀/N stay unasserted (the bar held); the flux
    measure is DERIVED (integrate the banked `cutSin` over `[0,cutPi]`, the `cutPi` template one
    dimension up), not axiomatized and not an empirical number ⇒ route-not-yet-found ≠ new axiom ⇒ NOT
    a CHAIN-RUNWAY, autonomous forward work remains ⇒ directed child, not a block.
  - **Follows the theory, not convenience:** it follows E3's EXPLICIT "SAME 4π object" cross-tie and the
    fold's return-surface geometry — the seed's own map, not an easy exit. And it is REAL forward work
    (analytic machinery on the derived ℝ), the opposite of an off-ramp.
  - **Why NOT a bare redirect to B3:** B3 (`G·M²=e^(−6π)`) itself needs this same flux measure (its
    `6π` is the same normalization ÷ count). Redirecting to B3 without the measure would DEFER a 5th
    time. The repeated-deferral clause demands childing the RICHER STRUCTURE the front requires — the
    flux measure IS that structure, and it is the shared B2/B3/E3 prerequisite. So the successor targets
    the flux-measure route-finder (deliverable C realized as a prerequisite BUILD), not a bare B3 node.

  - **The (D-algebraic) trap, explicitly rejected:** "cutPi already handles π (exponent `84π/b₀`, one
    power); just force the two integers now via a bespoke operator." REJECTED — the `84π` form pre-fixes
    Face 1 to the instanton normalization (unforced, S2), so forcing `b₀` on top of it is fishing (b);
    and N322/N326 already found the forcing operator does not exist and building it IS deriving c. The
    honest gap is the COUPLED (flux-normalization, count) pair, fixed at the curvature face — not an
    isolated algebraic count.

════════════════════════════════════════════════════════════════════════════
## DECISION — W1 step 3 / W3 DECOMPOSE + the SOUL repeated-deferral clause: child ONE route-finder
##            toward the DERIVED GAUSS-SPHERE RETURN-ACTION FLUX MEASURE (the shared B2/B3/E3
##            prerequisite). NO c/b₀/N asserted. NO production Lean this node.

B2's deliverable (c derived) is NOT achievable from the current carrier (`cutPi` + ℚ counts) without a
posit; the exponent's numerator normalization is a Gauss-sphere FLUX measure that, per E3, is the same
`4π`/`8πG` object entering the chain at the gravitational / curvature face, and it is not banked. Per
W1 step 3 + W3 + the repeated-deferral clause (4th touch): enqueue EXACTLY ONE directed successor — a
route-finder whose stated job is to BUILD (or take the smallest forced structural step toward) the
**derived Gauss-sphere return-action FLUX measure** on the derived ℝ `Cut` — the 2-sphere solid-angle /
return-surface flux, extending the banked 1-D `cutSin`/`cutCos`/`cutPi` (integrate `cutSin` over
`[0,cutPi]` for the meridian, assemble the solid angle) — the object that (i) fixes the exponent's
numerator normalization (Face 1), then pins the count (Face 2), giving c; and (ii) IS the `4π`/`8πG`
Gauss-sphere normalization of E3, so it is the shared prerequisite of B2 (c), B3 (`G·M²=e^(−6π)`,
gravity's first appearance), and E3. Its deliverable is either the flux measure → derive the exponent
normalization → c (bank B2, ★ BRIDGE completes), OR the smallest forced structural step + the precise
next child (recursive W1). It must DERIVE the flux measure from the banked trig (never posit `4π`,
never import Mathlib-ℝ/Real.pi), and DERIVE why the return-action ÷ gauge-dof identification is
fold-native (the fold's self-look-back return surface), never posit it to reproduce `6π`/`3π`.

Deliverable this node: this PREREG (the SELECT + the sharpened two-face/coupled diagnosis + the named
ascent + the flux-measure ceiling) + the ONE directed child. Ground-state unchanged (HEAD `18f4007`,
tree clean; no production Lean, no bank, no assertion — a DEFER node like N321/N322/N326).

ONE-SUCCESSOR RAIL: exactly one non-terminal successor on the chain tail (the flux-measure
route-finder). B3 is NOT separately emitted (it needs the same measure; the child decides
flux-measure-build-vs-further-decompose). Pre-complete fork check run before `kanban_complete`.

════════════════════════════════════════════════════════════════════════════
## APPENDIX — PRIVATE AIMING SIGNAL (⚠ FLAGGED NON-LOAD-BEARING, NEVER a proof step — G1/G2)

A MAP for the flux-measure child (like a category sketch), NOT a derivation and NOT a license to
assert. The 2-sphere solid angle `4π = ∫₀^π sinθ dθ · ∫₀^{2π} dφ = 2·2π` and Vol S³ `= 2π²` are the
candidate return-surface normalizations; the instanton normalization `8π² = 4·Vol S³`. IF the forced
return-action normalization turns out to be the `8π²` S⁴-charge measure, THEN with `g²=4π/42` the
exponent reads `84π/b₀`, and `b₀ = dim g₂ = 14` (the DERIVED gauge algebra Der(O)) would give `6π`
(=B3), `b₀ = dim Sym² = 28` would give `3π` (=B4), ratio `2 = 28/14`. This STRONGLY suggests the
mechanism is real and fold-native (return-action flux ÷ derived-gauge-dof), which is WHY the child is
concrete and well-posed. BUT: matching to the seed's `e^(−6π)`/`e^(−3π)` is FISHING against the
answer-key (G1/G4); the child must DERIVE both the flux normalization (why THAT sphere measure is the
fold's return-action) and the count (why b₀ = the derived gauge-dof), never posit them to reproduce
`6π`/`3π`. Recorded ONLY to aim the child; NO value of c/b₀/N is asserted.
