"""
N294 SELECT probe2 — DE-RISK the tractable route for the colour<->flavour commuting-pair / rigidity.
Key questions for a coordinate-free Lean route:
 Q1. Is the whole flavour su(3) (dim 8) Lie-GENERATED by the fold-root-dressed coset {kAB,kBC,kCA}?
     If yes: each kX is colour-neutral (banked N291/292) => by Jacobi the WHOLE flavour su(3) is
     colour-neutral => colour subset Z_{f4}(flavour). Clean, no exhaustion.
 Q2. Does colour commute with ALL 8 flavour generators (incl. the 2 diagonal u1-dressed coset dirs)?
 Q3. Disjointness witness: is a flavour generator OUTSIDE jActL(g2)?  (colour cap flavour = 0)
 Q4. The FULL mutual centralizer Z_{f4}(flavour)=colour (the hard <= exhaustion) — measure dim only,
     decide whether to bank or child.
"""
import numpy as np
from fractions import Fraction as F
np.set_printoptions(suppress=True, precision=4, linewidth=200)
exec(open("workbench/N294/_helpers.py").read())
