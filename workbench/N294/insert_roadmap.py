import io

ROADMAP = "docs/ROADMAP.md"
LEDGER = "docs/LEDGER.md"

n294_roadmap = r"""## ✅ N294 LANDED — THE DERIVED COLOUR GAUGE AND FAMILY-FLAVOUR su(3) ARE A COMMUTING PAIR IN `f₄ = Der(H₃(O))` (one fold-root, two orthogonal roles; the ★1↔★4 joint — the capstone of the N287→N293 f₄ sub-arc). Inside the SINGLE derived `f₄ = Der(H₃(O))` (N285/N286) two derived `su(3)`'s live: the GAUGE-COLOUR `su(3)` = the entrywise lift `jActL(colourCentralizer)` of the derived colour subalgebra (= the g₂-stabilizer of the fold's √−1 `JO = L_{u1}`, N201), and the FAMILY-FLAVOUR `su(3)` = `Z_{f4}(colour)` = family-so(3) {famA,famB,famC} (real-unit slots, N288/N289) ⊕ the fold-root-dressed coset {kAB,kBC,kCA} (u1-dressed slots, N292/N293). THIS node banks the RELATION between them: they form a genuine COMMUTING PAIR. ★★ WHOLE-ALGEBRA COMMUTING — for every colour `D ∈ colourCentralizer`, `⁅jActL D, famX⁆ = 0` (X=A,B,C, via N288 `fam_gauge_central`: family is gauge-CENTRAL, both slots real-unit) AND `⁅jActL D, kXY⁆ = 0` (XY=AB,BC,CA, via N291/N292 colour-neutrality of the coset), so colour centralizes all six flavour generators. ★★ THE ONE CAUSE `colour_kills_foldcomplex_core` — every colour `D` annihilates BOTH generators of the fold's colour-neutral ℂ-core `span{1,u1}` (`D 1 = 0` by `derivQ_one`; `D u1 = 0` by `commutes_JO_iff`/`mem_colourCentralizer`, N201/N266), and the whole flavour `su(3)` is generated by `innerMul` of slots dressed with exactly `{1,u1}` — so colour is BLIND to everything flavour is built from; the single blindness (the fold-root's colour-blindness, N266) IS the whole commuting relation. ★★ DISTINCTNESS (W8) `famC_ne_gauge` — the family generator `famC = innerMul (slotA 1)(slotB 1)` moves matter ACROSS generation slots (`famC (slotA 1) = − slotB 1`, N288 `famC_slotA`) whereas every entrywise gauge `jActL D` PRESERVES each slot (`jActL D (slotA a) = slotA (D a)`, N268) and kills `slotA 1` when `D 1 = 0`; so `famC ≠ jActL D` for every derivation `D` — no flavour generator is an entrywise gauge element, `flavour ⊄ jActL(g₂) ⊇ colour`. A genuine commuting PAIR, not one algebra. THE DISSOLUTION/MOAT: the field POSITS `F₄ ⊃ (SU(3)_c × SU(3)_flavour)/ℤ₃` as the maximal-rank subgroup and CHOOSES which factor is colour, which is flavour, and how each embeds; the theory DERIVES both factors as mutual centralizers inside the derived `f₄` and FORCES their commuting from the fold-root's colour-blindness — no embedding, no factor assignment. THE ONE CAUSE MANY TERMINATIONS: the SAME fold-root `u1` wears two orthogonal roles — colour = the g₂-stabilizer of `u1` (blind to `span{1,u1}`, acting WITHIN each generation slot, block-diagonal in the generation index) and flavour = the `u1`-dressing that MIXES the generation slots — so the one axis the fold makes is at once the colour complex structure and the generation-mixing dressing, and the two cannot see each other. THE OBJECTS (all in `Phys/Algebra/OctonionJordanColourFlavourCommutingPair.lean`, ns `Phys.Algebra.HJ`, 10 decls): `colour_centralizes_famA/famB/famC` (family gauge-central ⟹ colour-central); `colour_centralizes_kAB/kBC/kCA` (coset colour-neutral, `kCA` re-proved via the N291 Leibniz lever); ★★ `colour_kills_foldcomplex_core` (the ONE CAUSE, colour kills `span{1,u1}`); ★★ `famC_ne_gauge` (the W8 distinctness, `famC ∉ jActL(g₂)`); `flavour_nonvacuous` (`famC (slotB 1) = slotA 1 ≠ 0`); ★★★ capstone `colour_flavour_commuting_pair` (bundles: the six commuting brackets ∧ the ℂ-core lever ∧ `famC ≠ jActL D` for all derivations ∧ non-vacuity ∧ carries `jordan_fails_H4`). 10 decls foundations-only `[propext, Classical.choice, Quot.sound]` (INDEPENDENTLY axiom-audited 10/10 vs the freshly-built olean `workbench/N294/reaudit.lean` EXIT 0 + gate D6-fast). Costume C323 (`⊢ 1 = 323`) bites at the distinctness certificate `((innerMul (slotA 1)(slotB 1)(slotA 1)) 0 2).re.re.re = −1` (tied via `famC_slotA`; a WRONG reading — colour and flavour are the same algebra / flavour ⊆ gauge / they do not commute / there is no genuine second su(3) — reduces to the false numeric `1 = 323`; pair (323,1) fresh, RHS 323 distinct from every prior). Physics-words-removable (delete colour/family/flavour/generation/gauge: for every `D ∈ colourCentralizer`, `jActL D` commutes with each of the six `innerMul (slotX c)(slotY c')` (`c,c' ∈ {1,u1}`) because `D` kills `1` and `u1`; and `innerMul (slotA 1)(slotB 1)` sends `slotA 1 ↦ − slotB 1 ≠ 0` while `jActL D` fixes `slotA 1`; pure math, stands). NOT free-floating (every headline TYPE cites the banked `jActL`/`colourCentralizer`/`innerMul`/`slotA/B/C`/`famX`/`kXY`/`u1` on `Matrix (Fin 3)(Fin 3)(O ℚ)`; capstone carries `jordan_fails_H4` — NOT carrier-agnostic). MEASURE-FIRST (W9): the whole route de-risked BEFORE production (`workbench/N294/probe_measure.py` + `probe2..probe11`, exact-Fraction octonion + numpy, all EXIT 0: `Z_{f4}(flavour) = colour` EXACTLY dim 8, residual 2.2e-15; max|[colour,flavour]| = 8.6e-16; colour ⊕ flavour disjoint dim 16, rank 4 = rank F₄; colour block-diagonal in the generation index; famC crosses slots while colour fixes all diagonals ~1e-52); whole-module olean LIGHT (~10s; NO native_decide, NO octonion coordinate ring, NO proof-kernel maxHeartbeats raise). Advances ★1 (the gauge group — colour placed as one factor of the derived maximal-rank su(3)×su(3) ⊂ F₄) ↔ ★4 (family replication — flavour su(3) as the commuting partner), the ★1↔★4 joint. SCOPE (docs/RUNBOOK.md W4.5): the whole-algebra commuting pair (6 generators) + the one-cause ℂ-core lever + the distinctness + non-vacuity ONLY. NOT the mutual-centralizer RIGIDITY `Z_{f4}(flavour) = colour` EXACTLY (measured 2e-15 — needs an `f₄` coordinate/basis handle, the 52-dim exhaustion; CHILDED as a directed W1 route-finder); NOT the bundled `flavourLie` LieSubalgebra / the colour⊕flavour direct-sum dim count; NOT the ★2 mixing-angle VALUE (route-not-yet-found — a FORCED build target, NOT invented, NOT human-gated); NOT the ★5 mass texture VALUE (N269: all of `f₄` preserves the spectrum → needs a spectrum-breaking object beyond `f₄`); NOT "= physical SU(3)_c × SU(3)_flavour GUT" (removable prose). RESUME: single-flight run; SELECT + PREREG + de-risk + production + costume + audit + wiring + gate all this run. Next: the ONE successor — a directed W1 route-finder for the mutual-centralizer rigidity `Z_{f4}(flavour) = colour` (needs the f₄ coordinate handle).

"""

with io.open(ROADMAP, "r", encoding="utf-8") as f:
    lines = f.readlines()
# insert before the N293 LANDED line
for i, ln in enumerate(lines):
    if ln.startswith("## \u2705 N293 LANDED"):
        idx = i
        break
else:
    raise SystemExit("N293 anchor not found")
lines[idx:idx] = [n294_roadmap]
with io.open(ROADMAP, "w", encoding="utf-8") as f:
    f.writelines(lines)
print("ROADMAP inserted before line", idx+1)
