# N154 PREREG — THE KAK-PRODUCT ASSEMBLY → full SO⁺(1,9) path-connectedness reduced to one residual

Frozen BEFORE compute. KILL classifies a fight as INSTRUMENT failure, not theory.

## NODE (specified against what the chain ACTUALLY produced)
Front (i): THE KAK-PRODUCT ASSEMBLY. Both KAK factors are path-joined to id in `endOpC`
(boost N152 `specPathOp_joins_id_sqrt` / `specPathOp_cont`; compact/rotation N153
`rotPath_joins_id_rotation` / `rotPath_cont`), and the right-mult glue
`continuous_mul_right_path` is banked. N130 `bvIsomLin_polar_path_decomp` gives the FULL polar
split `S = k·γ1` of an ARBITRARY BvC-isometry (k a QvC-isometry, γ = specPathOp the boost path
joining id→γ1). Assemble these into:

  (A) THE ABSTRACT ASSEMBLY ENGINE `joined_id_of_factor_paths` — pure `endOpC` topology: from a
      continuous boost path id→p AND a continuous compact path id→k, the product `k*p` lies in a
      single PRECONNECTED subset of `endOpC` with the identity (`IsPreconnected.union` at the
      shared point `p`, gluing `β '' uIcc` and `(fun t => κ t * p) '' uIcc`). NO posited
      connectedness — preconnected-set arithmetic.

  (B) THE FULL REDUCTION `bvIsom_joined_id_of_compact_joined` — an ARBITRARY BvC-isometry `S` lies
      in a preconnected subset of `endOpC` with the identity, PROVIDED its polar compact factor is
      path-joined to id (the SINGLE residual = compact-group path-connectedness, exactly mirroring
      N104's reduction of the converse generation to one word-membership residual). Uses N130's
      decomposition + N152's boost path + the assembly engine (A).

  (C) CONCRETE UNCONDITIONAL WITNESS (W8 non-vacuity) — the assembly fires with NO residual on a
      genuine product `planeRotLin c s * p` where the rotation factor's path is N153 `rotPath`
      (joined to id) and the boost path is N152 `specPathOp` (joined to id): `1` and the product
      lie in one preconnected set. Demonstrates the engine genuinely fires on a nontrivial
      rotation × boost SO⁺(1,9) element.

## GO / NO-GO
GO if (A)+(B)+(C) compile foundations-only, gate D0–D6 green, costume C186 bites a wrong
KAK-product / path-connectedness / Joined / preconnected-union value (distinct false numeric, not
1=153). The genuinely-new datum: the full SO⁺(1,9) connectedness CLOSED to a single residual.

NO-GO / DECOMPOSE if the union-glue or the polar-decomp adapter resists after measure+reframe:
bank the assembly engine (A) + the concrete witness (C) (immediately bankable), child the
full-reduction (B) adapter as the next node.

## KILL (W9 instrument budget)
KILL = 120s per obligation. Each obligation is topology over endOpC + preconnected-union
arithmetic + rewriting by banked operator equalities — NO native-kernel-bypass, NO inflated
maxHeartbeats, NO monolithic brute. If any single obligation exceeds 120s, it is an INSTRUMENT
failure → measure/decompose, never grind. Probes bounded; whole-file isolated typecheck before
production wiring.

## THE ONE LAW (W5)
If the assembly fights: both factor-paths + the right-mult glue + the polar decomp ARE banked; the
assembly is `IsPreconnected.union` at the shared point `p` — NOT a posited connectedness, NOT a
fresh path construction. Return to the trunk.

## WORD-REMOVAL
Delete Lorentz/boost/rotation/Minkowski/isometry/KAK/polar/compact/path-connected/topology/
continuity/Joined/SO(1,9)/SO(9)/Spin/octonion → the statements stand as pure math about the
banked operators + derived ℝ Cut + terminal algebra O Cut + operator topology endOpC:
preconnected-union arithmetic of continuous operator-paths.
