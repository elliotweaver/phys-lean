# N408 PREREG — arc-M M2: THE BORN-SURVIVAL GEAR DECOMPOSITION + S1/S2 GEAR SELECTION

NODE: N408 (arc-M M2, SEED_INPUT_CLOSURE §M2). Grade: CANDIDATE (Born gear FORCED on banked
N308; S2 exclusion a THEOREM; phase gear deferred-candidate with a PERMANENT INCEPTION FLAG).

## TARGET
The seed's m_b route `m_b/m_t = (tan²P·cosP·√(2/3))²` decomposes an amplitude into a PHASE gear
(the fold cascade angle, tan²P·cosP) × a BORN gear (√(2/3)). This node banks:
- the BORN-GEAR half, grounded on the BANKED self-seen fraction `Matrix.trace Poth / 3 = 2/3` (N308);
- the S1/S2 gear-SELECTION: S1's √(2/3) is a derived √ of a BANKED RATIONAL Born weight (admissible);
  S2's Born weight 3^{−1/2} is IRRATIONAL (no rational squares to 1/3), so the S2 gear-search is EMPTY;
- the decomposition FORM `(phaseGear·bornGearP)² = phaseGear²·(self-seen fraction)`, with `phaseGear`
  an EXPLICIT candidate parameter honestly deferred to arc-O's fold-phase P derivation (O3).

## SELECT REDIRECT (see kanban comments 331/334)
Fold phase P (=π/8) is NOT banked — it is an arc-O target (O3, AFTER arc M). Deriving it now is out
of order AND phantom-cap-forbidden. So FOLLOW THE THEORY + the grade cap: bank the Born-gear + the
gear-selection at candidate grade, carry P as a deferred candidate parameter. Honest conditional
landing, not a fitted scale, not a phantom clean identity.

## ANTI-BULLSHIT GATE — PASSED (comment 334, in full)
fold-causal ✓ / dissolution ✓ (free Yukawa dial → decidable gear-search, S2 empty) /
goal-words-removable ✓ / name-the-temptation ✓ (rejected assert+fit+P) / honesty ✓ (candidate grade).

## MEASURE-FIRST (W9)
- probe_irr.lean EXIT 0 — pure-ℚ `¬∃q:ℚ, q²=3` (NO Real), the S2-exclusion core.
- probe_m2.lean EXIT 0 (~9s, LIGHT) — the whole route: bornGearP_sq, s1_grammar, no_rat_sq_three,
  no_rat_sq_third, s2_bornweight_irrational, amplitude_decomposition, W8 teeth.
COST BUDGET / KILL: each obligation ≤ 15s (they are all ℚ-cast + banked-lemma + linarith/norm_num).
If any single obligation exceeds ~60s → decompose (W9). No native_decide, no maxHeartbeats.

## GUARDS
G1 no asserted values (2/3 from banked N308; irrationality a theorem; phaseGear a parameter, not a value).
G2 no empirical number in any proof (measured m_b/m_t = removable prose only; NO-FIT).
G3 derived not posited. G4 physics-words-removable (delete mass/quark/gear → statements about a
derived √ of a banked rational + an irrationality obstruction + a product form over Cut).
G5 foundations-only ⊆ {propext,Classical.choice,Quot.sound}; DERIVED ℝ `Cut` + banked N308, NEVER
Mathlib-ℝ as content (cutSqrt is the banked derived √).
⚠ ℝ-VIGILANCE + FREE-FLOATING: every TYPE cites banked `Matrix.trace Poth` / `cutSqrt` / `Cut` —
NOT generic.
W8 non-vacuity: C433 (next Cid). W4.5: exactly M2 (Born gear + selection), NOT M3.

## COSTUME C433
`ScaleTowerBornGearDecompositionVacuityCostume.lean` bites `1 = 433`: a certificate over the
gear-selection (S1 admissible AND S2 excluded AND self-seen ≠ democratic) reduces the bogus to
`1 = 433`. A WRONG reading (S2 admissible / Born gear² ≠ 2/3 / self-seen = 1/3) fails.

## MODULE
`Phys/Algebra/ScaleTowerBornGearDecomposition.lean`, ns `Phys.Algebra.Winding` (arc-M sibling of N407)
or a fresh sub-ns. Import: OctonionJordanGenerationCyclePhase (N308 Poth/self-seen) +
LorentzContinuumSqrt (cutSqrt) + Mathlib.Tactic. Wire into Phys.lean AND Audits/AxiomAudit.lean.

## ONE-SUCCESSOR RAIL
Directed successor: arc-M M3 (the z0/m_e-scale chain closure, conditional). One child, chain tail.
