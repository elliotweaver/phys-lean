| N408 THE S1 GEAR DECOMPOSITION FOR `m_b` — owner-authorized input-closure seed (docs/SEED_INPUT_CLOSURE.md), target M2, DIRECTED successor of N407 (arc-M M1). The SECOND node of arc M (the absolute-scale / input closure — the hardest, least-certain arc, conditional/candidate grade only). Arc D banked the within-sector mass RATIOS theorem-exact; arc M attempts the last input walls the SM leaves as free Yukawa dials. m_b/m_t is a CROSS-SECTOR (down/up) scale ratio. THE THEORY-NATIVE READING (fresh-worker SELECT, docs/NODE_SELECTION.md IN FULL, both GOAL + REFRAMING lenses weighed, anti-bullshit S4 + QUIET-DRIFT passed IN FULL comments 329/333; W6 RESUME — a prior run left a complete uncommitted production module + costume + PREREG + probes for this exact node, INDEPENDENTLY RE-VERIFIED here vs the built olean before banking, NOT rubber-stamped): the key's Born ratio `m_b/m_t = (tan²P · cosP · √(2/3))²` FACTORS over the Born square into `phaseBorn · (2/3)`, where the SURVIVAL Born factor `(√(2/3))² = 2/3` is EXACTLY the banked self-blind "others" Born-survival fraction `s1BornProb = tr Poth / 3 = 2/3` (N308 `selfseen_two_thirds`, the Born self-overlap of a generation-probe blind to itself on the ℤ₃ cycle N305). Because Born = self-overlap (the trunk primitive), the irrational amplitude `√(2/3)` DISSOLVES through the Born SQUARE to the RATIONAL probability 2/3 — no ℝ needed, ground ℚ. THE GEAR SELECTION IS A BORN-RATIONALITY THEOREM (the ONE-LAW reframe of "which gear?"): a gear factor is admissible iff its Born probability is a RATIONAL self-overlap of the banked rational cycle. S1 (Born prob 2/3, RATIONAL, = the banked fraction) is SELECTED; S2 (amplitude `3^{−1/4}`, Born prob `(3^{−1/4})² = 1/√3` IRRATIONAL — no `x:ℚ` with `x²=1/3`, `three_not_isSquare_rat` via `Nat.prime_three.not_isSquare`) is EXCLUDED: the S2 gear-search is EMPTY. The continuous free Yukawa dial DISSOLVES into a discrete SELECTED gear. THE OBJECTS (Phys/Algebra/ScaleTowerGearDecomposition.lean, ns Phys.Algebra.Gear, 11 audited theorems + 2 defs over ℚ grounded on banked `Matrix.trace Poth`): s1BornProb (def, = tr Poth/3), ★ s1BornProb_value (= 2/3, FALLS OUT of `selfseen_two_thirds`), mbBornRatio (def), ★★ mbBornRatio_decomp (the M2 headline: ratio = phaseBorn · (tr Poth/3), the banked survival factored out), mbBornRatio_value (= phaseBorn·(2/3)), three_not_isSquare_rat (√3 irrational over ℚ), no_rat_sqrt_three (¬∃q:ℚ, q²=3), ★★ s2_gear_search_empty (¬∃x:ℚ, x²=1/3 — the empty S2 gear-search), ★★★ gear_selection (S1 rational=banked fraction ∧ S2 empty), s1BornProb_ne_one (W8 genuine Born suppression), selection_bites (W8 S1 rational vs S2 irrational genuinely distinguished), mbBornRatio_one_nontrivial (W8 survival is real content), ★★★ gear_decomposition (the capstone: decomposition ∧ survival=banked fraction ∧ =2/3 ∧ S2 empty ∧ survival≠1 — the candidate scale VALUE and phase factor deliberately EXCLUDED, the phantom cap). HONEST GRADE (mandatory flag, SEED_INPUT_CLOSURE §0/§3 — the PHANTOM CAP): THEOREM-grade for the STRUCTURE (the Born-square decomposition, the S1 survival = the banked self-blind fraction, the S2 exclusion / empty gear-search, the genuine S1-vs-S2 contrast — pure ℚ arithmetic on banked `Matrix.trace Poth`, nothing fished). CANDIDATE with a PERMANENT INCEPTION FLAG (prose only, NEVER structural, NEVER ground, NEVER fit): that this decomposition IS m_b/m_t's absolute scale; the numeric value; the fold-PHASE Born factor `phaseBorn = (tan²P·cosP)²` (needs the fold phase P = O3 of SEED_COMPLETION_SWEEP, route-not-yet-found, DEFERRED as a free parameter). The clean forced identity for the absolute scale DOES NOT EXIST (the phantom cap); the numerical check confirms P is a tuned angle (≈24.6°, NOT a clean fraction of π), so it is honestly deferred, never asserted. NOT a CHAIN-RUNWAY block (the structure derives) — a GRADE CAP: land the structure + gear selection, flag the phase, no grind, no fit. THE ONE CAUSE: the SAME ℤ₃ self-blind generation cycle (N305) whose "others" Born-survival fraction is 2/3 (N308, the Koide balance N307) supplies the SELECTED S1 survival gear; the SAME Born=self-overlap primitive that runs the whole flavor sector dissolves the irrational √ into the rational 2/3 and provides the rationality criterion that empties the S2 search. One self-blind survival, one Born-rationality criterion. THE DISSOLUTION/MOAT (STANDARD §0): the field leaves m_b, m_t as two independent free Yukawa inputs; the theory reads the down/up Born ratio as (a deferred phase factor) × the FORCED Koide self-blind Born-survival 2/3, with the gear DISCRETELY SELECTED by Born-rationality — the continuous dial DISSOLVES into a selected gear (honest limit: only the decomposition STRUCTURE + gear selection is theorem-grade; the absolute scale + phase factor candidate/inception-flagged, no phantom identity). | Algebra (arc-M M2 — the S1 gear decomposition for m_b: the Born ratio = phaseBorn · (tr Poth/3=2/3), the survival factor IS the banked self-blind Koide fraction, S1 selected / S2 empty by Born-rationality; over ℚ, the √ dissolving through the Born square; the input-closure seed) | ✅ | gate-fast pending (launched background). UNBROKEN (Poth/selfseen_two_thirds N305/N308; Matrix.trace/IsSquare/Nat.prime_three/Rat.isSquare_natCast_iff standard Mathlib MACHINERY on the derived rational objects; ground ℚ, NO Mathlib-ℝ as content — the √ dissolves through the Born square) + COMPLETE (11/11 theorems foundations-only [propext,Classical.choice,Quot.sound], INDEPENDENTLY audited vs the built olean, workbench/N408-arcM-gear-decomp/reaudit.lean EXIT 0; no proof-hole/axiom/native_decide/heartbeat) + WORDS-REMOVABLE (delete mass/quark/bottom/top/gear/scale/survival/Born: over ℚ, `Matrix.trace Poth/3 = 2/3`, `phaseBorn ↦ phaseBorn·(Matrix.trace Poth/3)` factors the banked number out, no `x:ℚ` with `x²=1/3` (hence none with `x²=3`), `Matrix.trace Poth/3 ≠ 1` — pure arithmetic on the banked trace) + NOT free-floating (every headline TYPE cites the banked `Matrix.trace Poth`, never a generic rational) + ANTI-VACUITY (C433 bites 1=433; s1BornProb=2/3≠1 genuine suppression, S1-rational vs S2-irrational genuinely distinguished). GRADE: THEOREM for the decomposition structure + gear selection; CANDIDATE with permanent inception flag for the scale value + the deferred phase factor (phantom cap honored — no grind, no fit). SEED GUARDS (§4): G1 (the decomposition + survival FALL OUT of `selfseen_two_thirds` + ring, never premised; the S2 emptiness a theorem, never asserted); G2 (no measured m_b/m_t/m_e in any proof — prose only, NO-FIT LOUD, no residual chase); G3 (survival = banked self-blind fraction, rationality = Born=self-overlap criterion, not posited); G4 words-removable; G5 foundations-only + ℝ-vigilance (ℚ only, the √ dissolved through the Born square, NO Mathlib-ℝ as content). SELECTED (DIRECTED seed successor toward M2; FRESH worker ran S1 arc synthesis + S2 GOAL/REFRAMING lenses + S3 ALIGN + S4 anti-bullshit gate IN FULL, comments 329/333: the genuine content is the down/up Born ratio = (deferred phase) × the FORCED Koide self-blind survival 2/3, with the gear DISCRETELY selected by Born-rationality; goal-words-removable pure ℚ statement on the banked trace; NAME-THE-TEMPTATION rejected (a) assert P≈24.6°/π/8 + compute m_b/m_t + compare to PDG [G1/G2 + phantom cap], (b) grind for a clean forced phase [the PHANTOM], (c) √3-irrationality grind for S2 [unnecessary — rational arithmetic suffices]; HONESTY — √(2/3) IS the banked Koide fraction, the phase honestly un-forced/deferred). W6 RESUME/VERIFY: a prior run (same node) left a complete uncommitted production module + costume + PREREG + probes; this run INDEPENDENTLY re-verified — rebuilt the module (olean 7.5s), re-audited all 11 theorems foundations-only vs the built olean, confirmed the costume bites 1=433, applied the STANDARD (unbroken/complete/words-removable/free-floating/anti-vacuity) — NOT rubber-stamped, then wired + committed. W4.5 SUFFICIENCY: exactly M2 (the gear decomposition + selection), NOT M3/M4. SCOPE: the Born-square decomposition + the S1 survival = banked fraction + the S2 empty gear-search + non-vacuity ONLY. NOT banked: the z0/m_e chain closure (M3 — the single directed successor), the arc consolidation (M4), the fold phase P (route-not-yet-found, deferred). THE NEXT NODE — the ONE successor: a DIRECTED child toward seed target M3 (the z0/m_e-scale chain closure: v → m_t → m_b → m_s/m_d → m_e → z0 as derived-CONDITIONAL, tying the absolute lepton scale to the banked tower via the winding rung; conditional/candidate grade, phantom-cap flagged), childed on the tail, max_retries=25, --max-runtime 120m, assignee phys-novelty. |
