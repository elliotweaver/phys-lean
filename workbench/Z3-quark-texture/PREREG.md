# Z3 — THE QUARK TEXTURE (pay the X10 debt). PREREG / anti-bullshit gate (in writing).

Node: arc-Z ENDPOINT-ASSEMBLY Z3 (docs/SEED_ENDPOINT_ASSEMBLY.md §Z3). Directed successor of
Z2/TwoChannelDescent. NO-DEFERRAL arc. Module: `Phys/Algebra/QuarkTexture.lean`, namespace
`Phys.Algebra.HJ` (same as N305–N312), costume C497.

## THE X10 DEBT, PRECISELY
X10 (`XArcValidationCapstone`) carries ONE `namedOpenStructural`: `quarkYukawaTexture` — "there is
NO banked quark-sector Yukawa texture (only the LEPTON Koide N340/N309)." Z3 PAYS it: bank the
quark-sector mass-ratio TEXTURE as derived structure, from the SAME banked arena.

## THE THEORY-NATIVE TARGET (what the fold says, chosen FIRST)
The flavor route (SEED_FLAVOR §1 step 5, N310/T7) already fixes the mechanism: **leptons sit on the
associative ℂ-line (associator VANISHES → CP-trivial at leading order); quarks span the octonionic
Fano line (associator NONZERO → carry CP).** N310's SEPARATION THEOREM is load-bearing here:
`Q ⟵ norms (I₂) ⊥ I₃ ⟵ associator` are INDEPENDENT invariants. So:

- The mass CUBIC is a NORM-ONLY fact: it is the SAME forced structure for quarks and leptons
  (masses = Born squares of a ℤ₃-Fourier cycle amplitude `genVec`, σ₁=6M² forced by the Koide
  balance A²=2M², phase carried by the single e₃ — N309). Q=2/3 is a norm identity holding on ANY
  balanced cycle (N307 `koide_genVec`), on either line. I must NOT claim the associator deviates the
  quark MASSES — that would contradict N310's separation and be bullshit.
- The quark sector is DISTINGUISHED from the lepton sector by exactly ONE octonion invariant: the
  associator I₃ = `assoc3`, which is `0` on the ℂ-line (lepton, `assoc3_cline_zero`) and `−1 ≠ 0`
  on the Fano line (quark, `assoc3_witness`/`fano_assoc3_ne_zero`). This nonzero associator IS the
  CP-carrying object that drives the two-sector CKM (N312 overlap / N418 Jarlskog).

**Z3 = THE QUARK TEXTURE = the forced-cubic mass spectrum (shared structure, the mass RATIOS as
derived functions of Q=2/3 + the per-sector phase, zero free continuous shape parameters)
WELDED to the forced quark-vs-lepton distinction (the associator I₃: −1 quark / 0 lepton — the
SOLE distinguisher) and the two-sector overlap (CKM, cited N311/N312).** The named banked object
`quark_texture_structure` is the X10-debt payment.

## THE ONE CAUSE (the marquee weld, statable from no single conjunct)
The SAME octonion non-associativity that stops the cascade (N2c), caps the Jordan tower / generation
count at three (`jordan_fails_H4`, N5), and opens the coupling chain (N311 corner-zero) is HERE the
sole invariant distinguishing the quark texture (Fano, I₃=−1) from the lepton texture (ℂ-line, I₃=0)
— while both share the same forced mass cubic (Q=2/3). One cause, another termination.

## ANTI-BULLSHIT GATE (run honestly, in writing)
- **Trace fold→node, every arrow forced/banked:** fold → self-blindness ℤ₃ cycle (N305) → cycle
  form (N306) → Koide Q=2/3 norm identity (N307) → forced cubic (N309) → the quark sector = the
  same cubic on the Fano line, distinguished by the associator (N310 separation) → two-sector
  overlap = CKM (N311/N312). No posit; every object banked. ✓
- **Name the standard version + what the theory DOES to it:** the SM has ~10 free quark Yukawas
  and inserts the CKM angles + CP phase by hand. The theory FORCES the quark spectrum as a cubic
  with Q=2/3 + one phase (zero free continuous shape params) AND forces the quark-lepton distinction
  to be EXACTLY the octonion associator (−1 vs 0), and the CKM CP to be that same associator.
  Genuinely different: it dissolves ~10 free numbers into structure + names the sole distinguisher. ✓
- **Delete every physics word:** what remains — over ℚ and `O ℚ`: for `genVec` the Born-square
  masses are the roots of a cubic `t³−6M²t²+σ₂t−e₃²` on the balance locus A²=2M² with `Qkoide=2/3`;
  the trilinear `assoc3` is `0` on the `u1`-line and `−1` on the Fano triple `(u2,u1,u1×u2)`; the
  overlap `mixV` is unitary-up-to-scale and Brahmagupta–Fibonacci normalized; and these are two
  DIFFERENT loci of the same `O ℚ`. No theorem STATEMENT needs a physics word. ✓
- **Standard temptation named:** "just re-export N309/N310/N312 with quark labels" (gold-plating /
  W4.5). My target is genuinely different + on the critical path: it produces the NAMED banked
  quark-texture object X10 said was MISSING (the discharge Z8 needs), and the marquee weld (the
  associator is the SOLE quark-lepton distinguisher, both sharing Q=2/3) is a NEW forced ONE-CAUSE
  claim, statable from no single banked conjunct. Not lateral re-pinning; it OPENS the X10 discharge. ✓
- **Free-floating rail:** every headline TYPE mentions the banked derived objects on `O ℚ`
  (`genVec`, `massVec`, `Qkoide`, `assoc3`, `mixV`) and carries `jordan_fails_H4` (the
  octonion-specific cap) — NOT a generic algebra statement. ✓
- **W8 non-vacuity:** the distinction bites (quark I₃=−1 ≠ 0 = lepton I₃; a "quark texture = lepton
  texture / I₃=0" reading is excluded); the quark cubic is non-degenerate at a generic phase; the
  costume C497 must fail (1=497).

## HONEST GRADE (per piece)
- THEOREM-EXACT NOW: the forced-cubic mass STRUCTURE for the quark sector (ratios as forced
  functions of Q=2/3 + phase, zero free shape params); the associator texture distinction (I₃ = −1
  quark / 0 lepton, the sole distinguisher); the two-sector overlap structure (unitary-up-to-scale,
  Brahmagupta–Fibonacci, corner-zero from cascade — cited N311/N312).
- ROUTE-NOT-YET-FOUND (enumerated, NEVER asserted, NEVER fitted): the exact NUMERICAL quark mass
  ratios (need the ℝ-descent of the per-sector phase `cos(3δ)`, exactly the N309 leptonic deferral)
  and the numerical CKM angles/Jarlskog magnitude (N418 lands the CP factor 1/2 at leading order;
  the full frame-angle-dependent numerics are the honestly-unclaimed leading-order remainder).
- The observed quark Koide deviations from 2/3, the PDG quark masses, the measured CKM — REMOVABLE
  PROSE only. NO empirical number in any proof (G2). NO FIT (G7).

## PLAN (light, ℚ-based like N309–N312; NO Cut import needed)
Import ONLY `Phys.Algebra.OctonionJordanGenerationMixingOverlap` (N312, transitively N305–N311 +
N310 associator) + `Mathlib.Tactic`. New content:
1. `quarkTextureCP := assoc3 u2 u1 (octCross u1 u2)`, `leptonTextureCP x b d := assoc3 x (b•u1)(d•u1)`.
2. `quark_texture_cp_value : quarkTextureCP = -1`; `lepton_texture_cp_zero : ∀ x b d, ... = 0`.
3. `texture_distinguisher : quarkTextureCP ≠ 0 ∧ ∀ x b d, leptonTextureCP x b d = 0` (the SOLE distinguisher).
4. `quark_shared_koide`, `quark_spectrum_cubic` — the forced cubic / Q=2/3 for a quark amplitude
   vector (cite N307/N309, framed as quark).
5. two-sector overlap re-export (mixV unitary-up-to-scale, cabibbo normalized, corner-zero).
6. W8: `quark_texture_ne_lepton_texture` (−1 ≠ 0), quark cubic non-degenerate at generic phase.
7. Capstone `quark_texture_structure` welding 1–6 + `jordan_fails_H4`.

Costume C497: `min 497 xFlag = 1` with `xFlag` tied to `texture_distinguisher`; bogus `= 497` bites `1=497`.

## COST BUDGET / KILL (W9)
All ℚ-algebra / `decide` on small objects / cite banked. Expected LIGHT (like N309–N312, ~7–10s
build). KILL: if any single obligation exceeds ~90s or the build exceeds ~3min, MEASURE-decompose;
no monolithic brute, no heartbeat inflation. No ℝ/Cut content (leptons/quarks numerics deferred).
