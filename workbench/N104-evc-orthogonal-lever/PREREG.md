# N104 PREREG — THE GENERAL EvC-ORTHOGONAL LEVER (abstract converse generation)

Frozen BEFORE compute. KILL classifies a fight as INSTRUMENT failure, not theory failure.

## NODE (priority (i) of the ROADMAP §N104 forward frontier — MEASURE-FIRST, no topology)

N102 (single half-turn → itself) and N103 (two-half-turn product → itself) BOTH closed via
the IDENTICAL structural fact:

    an operator g that is a QvC-isometry, EvC-adjoint to h, AND EvC-ORTHOGONAL (h∘g = id)
    has polar positive part p = h∘g = id, so p^{1/2} = √(id) = id, (p^{1/2})⁻¹ = id, and
    the polar/KAK compact part k := g∘(p^{1/2})⁻¹ = g∘id = g ITSELF.

N104 FACTORS that common body into ONE ABSTRACT, word-length-INDEPENDENT lemma, then derives
the corollary that reduces the FULL converse generation to a SINGLE clean residual
(the surjectivity "is every EvC-orthogonal isometry a biMulLin word?"), and demonstrates the
lever scales PAST length two with a concrete THREE-generator word (priority (ii)).

## DELIVERABLES (production, foundations-only)

1. `evCOrthogonal_compact_part_eq_self` — ★ THE ABSTRACT LEVER. For ANY
   g h : Module.End Cut STVC with `IsQvIsomC g`, `IsEvCAdjoint g h`, and `h.comp g = id`
   (EvC-orthogonality as a HYPOTHESIS), there is an EvC-orthonormal eigen-family with
   `h∘g = specOpN c uu` and `g∘(specOpN (cutSqrt c)⁻¹ uu) = g` — the compact part is g itself.
   (The factored N102/N103 body, length-INDEPENDENT.)

2. `evCOrthogonal_word_compact_part_mem_gen2` — ★★ THE REDUCTION. For ANY EvC-orthogonal
   g (as above) that is ADDITIONALLY a biMulLin word (`g ∈ genIsomMonoidLin`), the bridged
   compact part `endToFunEnd (g∘(p^{1/2})⁻¹) ∈ genIsomMonoidC2` — trivially, since the compact
   part IS g, already a word. This reduces the FULL `qvIsomMonoidC ≤ genIsomMonoidC2` to the
   single residual: "every EvC-orthogonal QvC-isometry IS a biMulLin word" (the Spin(9)
   generation surjectivity — the genuine W1 HEAVY remainder, childed N105).

3. N102 + N103 re-derivable as instances (the lever SUBSUMES both endpoints) — a sanity
   corollary `genTwoPlaneLin_compact_part_eq_self_via_lever` showing the two-generator product
   is an instance.

4. A concrete THREE-generator word (priority (ii)) `triHalfTurnLin u w x` (a product of three
   half-turns) is EvC-orthogonal hence its own compact part — a three-generator word ∈
   genIsomMonoidLin, bridged ∈ genIsomMonoidC2. W8 teeth: the concrete e2,je2,e2 three-word's
   compact part NEGATES the real axis (0,0,1) reading re.re.re = −1 (three half-turns: 1→−1→+1→−1,
   distinct from N103's two-word which fixes it at +1, and from N102's single which reads −1 but
   single-generator). Costume forces −1 = (distinct false numeric).

## GO / NO-GO

- GO if the abstract lever closes in a bounded probe (< 30s, expected ~7s like N103 body), and
  the corollary + three-generator word close similarly.
- NO-GO (decompose) if the three-generator EvC-orthogonality is heavier than budget: bank the
  abstract lever (1) + reduction (2) + an instance teeth on the ALREADY-banked genTwoPlaneLin
  (distinct costume coordinate), child the three-word + the residual surjectivity.

## KILL BUDGET (W9 — INSTRUMENT failure threshold)

- Any single obligation > 90s compile = INSTRUMENT failure → measure/decompose, do NOT grind.
- Total production build (cached upstream) expected < 30s. If the file's cold elaboration
  exceeds 120s, STOP, decompose into separately-banked sub-lemmas.
- The abstract lever is a COPY of an already-fast body (N103, ~7s) with hypotheses lifted —
  it MUST be at least as fast. If it is not, the framing is wrong (W9), not the theory.

## ANTI-VACUITY (W8)

- The lever is NOT vacuous: N102 and N103 are genuine instances (the costume + the three-word
  teeth read a genuine non-trivial coordinate −1).
- Costume C136 must BITE: a WRONG abstract-EvC-orthogonal-converse / three-generator-word
  compact-part coordinate claim, reducing to a false numeric DISTINCT from C115 1=7 … C135 1=47.

## PHYSICS-WORDS-REMOVABLE

Delete Lorentz/boost/rotation/half-turn/compact/orthogonal/KAK/polar/spectral/isometry/
generation/generator/Spin/SO(9)/word/involution/product/bridge → pure linear algebra over the
derived complete ordered field Cut and O Cut: a Cut-linear operator g that preserves gFormC, has
a form-adjoint h with h∘g = id, equals its own inverse-spectral-root composite; if g is a finite
product of the two-sided generators, its bridge-image lies in the target generated submonoid.
