# N318 PREREG — SEEDED GAUGE ROUTE A5 REMAINDER + A6

Target: derive `Tr(T3²)=2` and `Tr(Y·T3)=0` over one chiral generation from a DERIVED
su(2)_L doublet carrier + the banked colour dim, then land `Tr(Y²)/Tr(T3²)=5/3` (A5) and
`sin²θ_W=3/8` (A6) by rw into the banked N317 assembly engine.

## S1 ARC SYNTHESIS (fresh worker — I am NOT the N317 worker)
- N313 A1 (φ, ‖φ‖²=42) → N314 A2 (channel tower 42=n(n−1)) → N315 A3 (★ 42·α=1) →
  N316 A4 (α*=1/42) → N317 A5 pillar 1 (Tr(Q²)=16/3, the GUT denominator + the parametric
  assembly engine). This node = A5 remainder (the two isospin pillars) + A6.
- N317 BANKED (REUSE, W2): `ladderChargeTrace`, `generation_charge_trace` (Tr Q²=16/3 grounded
  to finrank Uhol), `hyper_ratio_reduction`, `hyperRatio`, `weinberg`, `assembly_delivers_5_3`
  (hyperRatio (ladderChargeTrace (finrank Uhol)) 2 = 5/3), `weinberg_delivers_3_8`. So the
  moment Tr(T3²)=2 is DERIVED, the 5/3 and 3/8 fall out by rw.

## THE DERIVATION (theory-native, NOT an SM tabulation)
The two isospin pillars over ONE chiral generation:

(2) **Tr(T3²) = 2.** T3 lives on the su(2)_L DOUBLETS (the fundamental-2 / spin-½ carrier,
    N279/N280 leftReg — NOT the N277 fundamental-7 triplet). Structure:
    - **per-doublet Tr(T3²) = 1/2, FORCED**: the fundamental-2 Cartan is the unique traceless
      2-weight system with charge-gap 1 (the raising operator carries 1 unit of charge = N317's
      Q normalization Q(|S|=n_c)=1). traceless (w₀+w₁=0) + gap-1 (w₀−w₁=1) ⟹ w=±1/2 UNIQUELY
      ⟹ Σwᵢ²=1/2. The ±1/2 FALLS OUT, never premised. (Corroborated in prose: the derived
      Cartan lregI²=−id (leftReg_sq+gForm_u1M=1), a complex structure whose Hermitian T3=lregI/(2i)
      has eigenvalues ±1/2 — the ℚ-native surrogate is traceless+gap-1.)
    - **doublet count = 4, GROUNDED to derived colour**: n_c quark-doublets (one per colour,
      n_c = finrank ℚ Uhol = 3) + 1 lepton-doublet (the colour singlet, finrank ℚ singlet = 1).
      count = finrank Uhol + finrank singlet = 3 + 1 = 4.
    - Tr(T3²) = count · (1/2) = 4·(1/2) = **2**. FALLS OUT; type mentions finrank Uhol/singlet.

(3) **Tr(Y·T3) = 0.** Y is CONSTANT on each doublet; Σ_doublet T3 = +1/2+(−1/2) = 0 (traceless);
    so Σ_doublet Y·T3 = Y·(Σ T3) = 0 for ANY per-doublet hypercharge y (NO SM Y-table needed —
    the orthogonality is forced by tracelessness alone). Singlets have T3=0. → Tr(Y·T3)=0.

Then (N317 reduction): Tr(Y²)=Tr(Q²)−Tr(T3²)=16/3−2=10/3, so ★ Tr(Y²)/Tr(T3²)=(10/3)/2=5/3 (A5),
and ★★ sin²θ_W=Tr(T3²)/Tr(Q²)=2/(16/3)=3/8=Tr(T3²)/(Tr(T3²)+Tr(Y²)) (A6).

## ANTI-BULLSHIT GATE (SOUL)
- **Fold→node forced?** fold→cascade→terminal O→g₂-colour SU(3) (finrank Uhol=3) + colour singlet
  + leftReg su(2)_L fundamental-2 (spin-½) → the count 4 and the per-doublet weights ±1/2. Each arrow
  derived/banked. The assembly is N317's (banked). No posit.
- **Standard version & what the theory DOES to it.** SM ASSIGNS each fermion (T3,Y,Q) from the
  hypercharge table (empirical) and imposes multiplets by hand. Theory DERIVES: doublet count from
  colour finranks, per-doublet trace from the fundamental-2 (traceless+gap1), orthogonality from
  tracelessness alone (any Y). Dissolves the table.
- **Words-removable?** Delete isospin/T3/Y/doublet/generation/colour/weak/Weinberg: what remains —
  the unique traceless 2-weight ℚ-system with gap 1 has square-sum 1/2; (finrank Uhol + finrank
  singlet)·(1/2)=2; Σ_d Σ_i y_d·wᵢ=0 for any y (tracelessness); (16/3−2)/2=5/3; 2/(16/3)=3/8. All
  pure ℚ/finrank statements, proved. ✓
- **Free-floating?** Headline `generation_isospin_trace` TYPE mentions `Module.finrank ℚ Uhol`
  (+ `finrank ℚ singlet`) — banked derived colour dims, NOT a generic 3. Per-doublet 1/2 and
  orthogonality are LEMMAS (about Fin 2 weights) feeding the grounded headline — exact mirror of
  N317's `sum_card_sq_three` (decide on Fin 3) feeding the grounded `generation_charge_trace`. ✓
- **Temptation rejected.** (a) Tabulate the 15-state SM (Q,T3,Y) and `decide` 2 — G1 + FREE-FLOATING,
  the exact trap N317's discarded ProbeGen hit. REJECTED: I use the STRUCTURE (count × fundamental-2
  trace), not an enumerated table. (b) Extract ±1/2 from lregI's real skew eigenvalues ±i via
  T3=lregI/(2i) — needs `i` (not over ℚ) + a factor-2 convention = the posited normalization N253
  refused. REJECTED: traceless+gap-1 is the ℚ-native convention-free surrogate. (c) Reuse N253's
  fundamental-7 triplet 3:8 — WRONG rep (adjoint/triplet, not the matter doublet). REJECTED.

## GUARDS
G1 (2,0,5/3,3/8 fall out — never premised) ✓ · G2 (no empirical number; 0.231 is prose only) ✓ ·
G5 words-removable ✓ · G6 foundations-only ⊆{propext,Classical.choice,Quot.sound}, no
sorry/axiom/native_decide/maxHeartbeats-raise ✓ · G7 ONE LAW (if 2 fights, the framing — triplet
rep or posited count — is wrong; return to fundamental-2 + colour finranks) · FREE-FLOATING rail ✓ ·
UNEARNED-IMPORT: ground ℚ (all ℚ-exact), NO Mathlib-ℝ/ℂ as content ✓.

## W9 COST BUDGET / KILL
All ℚ-arithmetic + tiny `Fin 2` sums + rw into banked assembly. No octonion coordinate bash, no
`decide` on large finsets (the Fin 2 sum is trivial). Expected: fast. KILL: if any single obligation
> 30s, measure/decompose. Import only `ChiralGenerationChargeTrace` (+ Mathlib.Tactic) — gets the
assembly + Uhol + singlet + finranks transitively; do NOT import the heavy N280 (synthInstance bumps).

## COSTUME C347
Marquee A6: `8·sin²θ_W = 8·(3/8) = 3` (numerator cleared). `eightWeinberg_forced` ties the derived
`isospinTrace` through `weinberg_lands`. cert:ℕ:=3; `min 347 3 = 3` true; bogus `= 347` reduces to
`3 = 347`, bites. Pair (347,3) fresh.

## ONE-SUCCESSOR RAIL
A5+A6 COMPLETE (5/3 and 3/8 land) → EXACTLY ONE directed child toward A7 (iso-stiffness /
one-coupling coherence). No fork. Pre-complete: kanban_list, count non-terminal successors = 1.
