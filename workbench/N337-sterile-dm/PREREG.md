# N337 — SEEDED GAUGE ROUTE C3 (arc-C): dark matter = sterile ν_R (the ONE theorem). ρ_Λ 26-channel DEFERRED with a NAMED route.

FRESH worker, run 639, task t_08c7fb48. DIRECTED single successor of N336 (t_f672cebf, C2) — chain tail.
Duplicate-instance check: my PPID=73777 = board `spawned.pid` (run 639); `ps` siblings are only the
launching hermes TUI + the dashboard python — NO interloper lean/lake/gate process. I am the LEGIT
worker; did NOT stand down. State at run start: HEAD `76525bf` (N336 banked), git tree clean (only an
untracked `workbench/N336-nu-spectrum/gate-fast.log`), `workbench/N337-sterile-dm` fresh (genuine fresh
start — no prior probe). Build green at HEAD (`Phys.Algebra.NeutrinoMasslessGeneration` compiles, 3504 jobs).

════════════════════════════════════════════════════════════════════════════
## STEP A — SELECT (worker-body STEP A), run theory-native FIRST, ANTI-BULLSHIT held brutally.

The ticket offers (a) the gen-1 ν_R as stable+sterile+dark as ONE theorem riding N336's massless
self-direction + N335's family-blind Majorana, (b) toward ρ_Λ = (Σm_ν/26)⁴ via the 26 traceless
J₃(𝕆) channels, or (c) a smaller structural prerequisite / redirect.

★ THE THEORY-NATIVE READING (the fold's own next beat here).
N336 banked the MASSLESS self / DC generation-direction `s = (1,1,1)`: its Born self-overlap through
the LIGHT seesaw operator VANISHES (`self_generation_massless`, `selfDir_zero_mode`) because the fold
cannot see itself (`Poth·s = 0`). N335 banked the FAMILY-BLIND HEAVY Majorana scale `heavyMajorana M
= M•1` and its universality (`heavyMajorana_family_blind`, `heavyMajorana_ne_zero`). C3's theory-native
question is the CONVERSE PAIR that turns "massless" into "dark": what does the SAME self-blind
self-direction do to (i) the HEAVY operator and (ii) the DECAY portal?

The fold answers directly, and it is the SAME blindness twice:
  (i) HEAVY, NOT LIGHT. The self-direction is the EIGENVECTOR of the family-blind heavy scale with the
      FULL eigenvalue `M`: `heavyMajorana M · s = M • s` (`heavyMajorana_selfDir_eigen`), and for `M≠0`
      it is NONZERO (`heavyMajorana_selfDir_ne_zero`). So `s` is NOT massless in the heavy sector — it
      carries the whole sterile scale `M`. Contrast with the LIGHT sector, where `s` is a zero-mode
      (N336). The one direction the light Dirac coupling is blind to is the one the heavy scalar sees
      in full: it is HEAVY-STERILE, LIGHT-MASSLESS. This is the sterile ν_R's defining split, DERIVED.
  (ii) NO DECAY PORTAL. The physical decay channel of a right-handed neutrino runs through the SAME
      Dirac portal `D†D` that gives the light mass (Born = self-overlap, the trunk). But that portal
      annihilates the self-direction: `(othersProj·D₀)(othersProj·D₀)ᵀ · s = 0`
      (`dirac_portal_selfDir_zero`), so EVERY transition amplitude from `s` to any active direction `v`
      vanishes: `⟨v, (M⁻¹•(D Dᵀ))·s⟩ = 0` (`transition_to_active_vanishes`), the raw portal too
      (`dirac_transition_vanishes`). No decay channel exists — STABILITY is a THEOREM, the SAME
      `Poth·s = 0` self-blindness that made the state massless. One blindness: massless + sterile-light +
      no-decay all at once.

  So the ONE theorem `sterile_nu_dark_matter` bundles: `s` is (a) a heavy-scale eigenvector (M•s,
  nonzero) — HEAVY-STERILE; (b) LIGHT-MASSLESS (`self_generation_massless`, `m₁=0` from N336); (c) has
  ZERO decay amplitude to every active direction (`transition_to_active_vanishes`) — STABLE. Stable +
  light-massless + carries a heavy sterile scale = the sterile-ν_R dark-matter identification, as ONE
  theorem over the derived `Cut`, riding the banked N335 heavy scale + N336 massless self-direction.

★ WHY THIS IS THE GENUINE C3(a), NOT DECORATIVE (anti-bullshit crux). A C3 that POSITS "the gen-1 ν_R
is stable/sterile/dark" is decorative. This deliverable PROVES:
  • the self-direction is the heavy operator's eigenvector with eigenvalue M and NONZERO (a proved
    identity `heavyMajorana_selfDir_eigen` on the banked `heavyMajorana`), so heavy-sterile — DERIVED;
  • it is a light zero-mode (`self_generation_massless`, banked N336) — massless — DERIVED;
  • the decay portal `D Dᵀ` annihilates it (`dirac_portal_selfDir_zero`), so every transition amplitude
    to an active direction is `0` (`transition_to_active_vanishes`) — no decay channel — STABILITY as a
    THEOREM, not an assertion.
The ONLY candidate-grade identifications (flagged, NEVER asserted, exactly like N336's `D=Poth·D₀`):
(i) the physical READING — `s` IS the physical sterile ν_R, its stability IS cosmological darkness;
(ii) that the decay portal IS `D Dᵀ` (the Born self-overlap portal). The MECHANISM — self-blind ⟹
massless ∧ no-decay, heavy-blind ⟹ full sterile scale — is derived.

★ THE MOAT / DISSOLUTION. The Standard Model + literature POSIT a stable sterile ν_R (impose a texture
zero for masslessness AND impose the absence of a decay portal by hand). The theory DERIVES BOTH from
ONE fact: the fold's self-blindness `Poth·s = 0`. The texture zero IS self-blindness (N336); the
absence of a decay portal IS the SAME self-blindness (the portal `D Dᵀ` is built from `D=Poth·D₀`, so
it too annihilates `s`). One cause — self-blindness — gives massless + sterile-light + stable, the
same blindness that caps the cascade (N5) and blinds the family so(3) (N290).

★ WHY (b) ρ_Λ IS DEFERRED with a NAMED route (the honest SELECT, held against the anti-bullshit law).
Part (b) — ρ_Λ = (Σm_ν/26)⁴ via the 26 traceless J₃(𝕆) channels — CANNOT be honestly banked in this
node, and forcing it here would be the exact 2/9-death fishing the seed warns of:
  1. THE COUNT "26" IS NOT BANKED AS A FINRANK. The J₃(𝕆)=27-dim arena is banked ONLY as an explicit
     algebraic decomposition `Hm = Dg + slotA + slotB + slotC` (N267 `Hm_slot_decomp`, "3 + 3·8 = 27"
     in a COMMENT), never as a Lean `Module.finrank … = 27`. N299/N300 EXPLICITLY AVOIDED computing
     `dim hermSub = 27` ("automatically nonzero … with NO need to compute dim = 27") because the
     coordinate expansion is a W9 instrument-cost wall (a 27-dim basis "times out at 200k heartbeats").
     `26 = 27 − 1` (traceless) appears only as MEASURED PROSE (`dim 78 = 52 + 26`, "measured N295"),
     NOT a derived finrank. So `26` is not in hand as a proved number — banking `(Σm_ν/26)⁴` here would
     ASSERT the channel count (G1 violation) or require first PROVING `finrank(ker jTr) = 26`, a
     genuine un-banked W9 prerequisite on a DIFFERENT carrier (`O ℚ`/`ℚ`, namespace `HJ`).
  2. THE FOURTH POWER + Σm_ν/26 IDENTIFICATION IS A CANDIDATE WITH A PERMANENT INCEPTION FLAG (the seed
     is explicit: the magnitude was known BEFORE the mechanism; ρ_Λ^{1/4}=2.30 vs obs 2.24). Even with
     `26` banked, `ρ_Λ = (Σm_ν/26)⁴` is an identification to DERIVE (why the fourth power? why divided
     by the channel count?), not a number to reproduce. Producing it now, before the mechanism is
     forced, would be fitting Λ — explicitly forbidden.
  3. CROSS-CARRIER FREE-FLOATING RISK. Σm_ν lives over `Cut` (ν-sector); the 26 channels live over
     `O ℚ` (Jordan arena). A ρ_Λ theorem must bind BOTH derived carriers with a DERIVED map, not a
     posited numeric coincidence. That binding is itself a structural prerequisite.
  ⟹ The genuine C3 beat is (a) — the sterile-ν_R ONE theorem, fully forceable NOW over `Cut` on the
  banked N335/N336 objects. Part (b) is a REAL un-banked prerequisite: `finrank(traceless J₃(𝕆)) = 26`
  and the (Σm_ν/26)⁴ mechanism. Per the RUNBOOK W1 escalation and the seed's C4 order, I hand forward
  ONE directed successor: a route-finder / C4-consolidation child that NAMES the 26-channel finrank as
  the prerequisite and sets up the analytic-completion (D1) handoff — NOT a ρ_Λ node grinding an
  asserted 26 or a fitted fourth power.

★ WHY NOT (c)-only. Part (a) IS fully forceable now — no prerequisite is missing for the sterile-ν_R
theorem (N335 heavy scale + N336 massless self-direction + `Cut`/`Matrix` machinery all in hand). So
this is NOT a pure redirect; it is (a) banked + (b) named-deferred.

════════════════════════════════════════════════════════════════════════════
## S2 — THE BUILD (deliverable: `Phys/Algebra/SterileNeutrinoDarkMatter.lean`, ns `Phys.Algebra`)

Over the derived ℝ `Cut`. Import `Phys.Algebra.NeutrinoMasslessGeneration` (N336 banked; pulls N335).
PROBE `workbench/N337-sterile-dm/probe1.lean` COMPILES CLEAN (verified, 3505 jobs, 49s; all 7 new decls
foundations-only ⊆ {propext, Classical.choice, Quot.sound}).

- `heavyMajorana_selfDir_eigen : (heavyMajorana M).mulVec selfDir = M • selfDir` — the self-direction
  is the heavy scale's eigenvector with the FULL eigenvalue M (heavy-STERILE).
- `heavyMajorana_selfDir_ne_zero (M≠0) : (heavyMajorana M).mulVec selfDir ≠ 0` — nonzero in the heavy
  sector (contrast N336's light zero-mode).
- `dirac_portal_selfDir_zero : ((othersProj*D₀)(othersProj*D₀)ᵀ).mulVec selfDir = 0` — the decay
  portal annihilates the self-direction (self-blindness, banked `dirac_others_self_blind`).
- `transition_to_active_vanishes : ⟨v, (M⁻¹•(D Dᵀ)).mulVec selfDir⟩ = 0` — EVERY transition amplitude
  from the self-direction to an active direction vanishes (STABILITY, via banked `selfDir_zero_mode`).
- `dirac_transition_vanishes` — the same on the raw portal (no scale).
- ★★ `sterile_nu_dark_matter` — THE C3(a) ONE THEOREM: heavy-eigen ∧ heavy-nonzero ∧ light-massless
  (banked `self_generation_massless`) ∧ zero-transition (stable). Heavy-sterile + light-massless +
  stable = the sterile-ν_R dark-matter identification.
NON-VACUITY (W8, C362): `heavyMajorana_no_massless (M≠0) (v≠0) : (heavyMajorana M).mulVec v ≠ 0` — the
heavy operator has NO massless direction (full-rank), so the light zero-mode is GENUINELY a light-only
phenomenon, not automatic; the self-blind light/heavy split is load-bearing. Costume flag
`sterileScale := 1` (the ONE heavy sterile scale M), `min 362 sterileScale = 1`; bogus `= 362` ⟹ `1=362`.

All tactics cheap (`smul_mulVec_eq` banked, `Matrix.one_mulVec`, `Matrix.mulVec_mulVec`, `dotProduct_zero`,
`Fin.sum_univ_three` / `simp`) on banked objects + `Fin 3` — no W9 risk (probe built in 49s).

════════════════════════════════════════════════════════════════════════════
## S3 — ANTI-BULLSHIT GATE (crux) — held brutally honest

  (a) DECORATIVE POSIT "gen-1 ν_R is stable/sterile/dark" → REJECTED. Every clause is PROVED: heavy
      eigenvector (`heavyMajorana_selfDir_eigen` on banked `heavyMajorana`), light-massless (banked
      `self_generation_massless`), zero decay amplitude (`transition_to_active_vanishes` via banked
      `selfDir_zero_mode`). Stability is a THEOREM (no decay portal), not an assertion.
  (b) ASSERT ρ_Λ / the 26-count / the fourth power → NOT done, DEFERRED with a NAMED route. `26` is not
      a banked finrank (only measured prose + a W9-avoided 27-dim); `(Σm_ν/26)⁴` is a permanent-inception
      candidate. No decimal/literal ρ_Λ value; measured Λ / w / DM abundance = REMOVABLE PROSE ONLY.
      Handed forward as a route-finder prerequisite (finrank 26 + the mechanism), not fished here.
  (c) POSIT the coupling texture (D=Poth·D₀) → guarded, inherited from N336 exactly as flagged there;
      the mechanism (self-blind ⟹ massless ∧ stable) is proved, the identification flagged candidate.
  (d) import Real.exp/Real.pi/Mathlib-ℝ as content → UNEARNED-IMPORT, NOT done. Ground field derived
      `Cut`; banked `heavyMajorana`/`selfDir`/`massOfDir`/`othersProj`. `Matrix` is MACHINERY.
  (e) new dimensionful Cut atom → NOT done (G3). `M` the single anchor (the heavy sterile scale = M•1,
      banked N335); no new scale posited.
  (f) FREE-FLOATING generic linear algebra → guarded. Every theorem TYPE mentions banked `heavyMajorana`
      (N335), `selfDir`/`othersProj`/`massOfDir`/`self_generation_massless`/`selfDir_zero_mode` (N336)
      over the derived `Cut`. NOT generic over an arbitrary field — about the banked seesaw objects.

WORDS-REMOVABLE (G5): delete neutrino/sterile/dark-matter/stable/decay/mass/generation. What survives:
for `s = ![1,1,1]`, `heavyMajorana M = M•1`, `D = othersProj*D₀` over `Cut`: `(M•1)·s = M•s`, `(M•1)·s
≠ 0` for `M≠0`, `(D Dᵀ)·s = 0`, `⟨v, (M⁻¹•(D Dᵀ))·s⟩ = 0`, and `(M•1)·v ≠ 0` for `M≠0, v≠0`. Pure
Cut-matrix analysis. NO name is load-bearing.

HONEST GRADE (mandatory flags):
  • IDENTITY-grade (forced math): `heavyMajorana_selfDir_eigen` (eigenvector), `heavyMajorana_selfDir_ne_zero`
    (heavy-nonzero), `dirac_portal_selfDir_zero` (portal kills self), `transition_to_active_vanishes`
    (zero amplitude), `heavyMajorana_no_massless` (heavy full-rank). Pure Cut-matrix arithmetic.
  • CANDIDATE-grade (arc-C, prose + identification, NEVER asserted): (i) `s` IS the physical sterile
    ν_R, its stability IS cosmological darkness; (ii) the decay portal IS `D Dᵀ`. Both flagged; the
    whole arc C candidate-grade.
  • ⚠️⚠️ STANDING + JOINT KILL-LINE (prose-only, do NOT soften): the m₁=0 sterile-ν_R dark-matter
    identification is JOINTLY FALSIFIABLE with the ν-spectrum law (C2): any m₁>0 detection kills BOTH.
    The DESI DR2 Σ<58 meV tension (C1, ~2σ) stands. ρ_Λ carries a PERMANENT INCEPTION FLAG; the w=−1
    stance is a registered bet against DESI's mild evolving-DE lean. NEVER fit, NEVER an escape hatch.
  • ⚠ HIDDEN JOINT OUT OF SCOPE: the freeze-in Boltzmann integrator (T_RH/η_B/Ω_DM, ×3×10⁷
    convention-sensitive) is NOT this node — flagged out of scope; this node is the ν/DM STRUCTURE only.

NOT a runway / not a new axiom: every object is a banked-object / derived-Cut combination; no empirical
number in any statement/proof. BUILD node (production Lean banked), landing C3(a) (candidate + joint
kill-line); C3(b) ρ_Λ DEFERRED with a named prerequisite (finrank-26 + the (Σm_ν/26)⁴ mechanism).

════════════════════════════════════════════════════════════════════════════
## DECISION — BUILD: bank `Phys/Algebra/SterileNeutrinoDarkMatter.lean` — the sterile-ν_R ONE theorem.
##   The fold's self-blind self-direction is HEAVY-STERILE (heavy-scale eigenvector, eigenvalue M,
##   nonzero), LIGHT-MASSLESS (banked N336 zero-mode), and STABLE (the decay portal `D Dᵀ` annihilates
##   it — every transition amplitude to an active direction vanishes) — all from the SAME self-blindness
##   `Poth·s = 0`. Heavy-sterile + light-massless + stable = the sterile-ν_R dark-matter identification,
##   as ONE theorem over the derived `Cut`. CANDIDATE-grade; STANDING+JOINT KILL-LINE flagged prose.
##   ρ_Λ / the 26-channel fourth power DEFERRED with a NAMED prerequisite (finrank-26 + mechanism).

ONE-SUCCESSOR RAIL: exactly one directed successor — a C4-consolidation / route-finder node that (i)
NAMES `finrank(traceless J₃(𝕆)) = 26` as the ρ_Λ prerequisite (a W9-guarded dim node on the `O ℚ`
carrier), (ii) bundles the cosmology contact reached, and (iii) sets up the analytic-completion (D1)
handoff per seed order C1→C2→C3→C4. Pre-complete fork check (kanban_list, non-terminal successors = 1
on the tail). Never both, never zero.
