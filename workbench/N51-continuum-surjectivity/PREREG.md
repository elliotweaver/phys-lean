# N51 PREREG — frozen before compute

## Node
N51 — the next forced node after N50(a) (the continuum rotation generator). The ticket TARGET is
the FULL SURJECTIVITY onto the connected isometry component SO⁺(1,9): that EVERY orientation/time-
preserving continuum isometry of the banked `QvC` is a finite product of the banked continuum boost
(N49 `boostC_isom`) + rotation (N50 `rotC_isom`) one-parameter generators — i.e. `qvIsomMonoidC`
restricted to the connected component IS generated by the one-parameter subgroups.

## SPEC against what the chain ACTUALLY produced (W1 + ROADMAP §N51 frontier)
The FULL surjectivity is a deep global Lie-group theorem (exp-integration of the tangent algebra,
Cartan/polar KAK decomposition, connectedness + topology of the isometry group over the derived ℝ).
NONE of the global machinery is banked: Mathlib has no `SO⁺(1,9)`, no octonionic spinor cover, no
Lie-group integration over the derived `Cut`; the banked `expO` stack (N41) is a per-point vector
exponential of derivations of `O Cut`, NOT a flow on the 11-dim coordinate space `STVC`, and there
is no banked connectedness / "finite product of one-parameter subgroups" theorem. The global
surjectivity is therefore a genuine MULTI-NODE arc, exactly as the ROADMAP §N51 paragraph and the
parent N50 handoff both anticipate ("decompose into the immediately-bankable infinitesimal/Lie-
algebra-closure piece (bank it) + child the exp-integration / global surjectivity").

## W3 DECOMPOSITION (frozen)
THIS node (N51, the bankable piece): **THE INFINITESIMAL ISOMETRY LIE ALGEBRA** — the tangent
algebra of the connected isometry component, over the derived ℝ `Cut`. The connected component is
generated by the one-parameter subgroups; their tangent (the infinitesimal generators) is the
form-skew endomorphisms, and the claim "generated by boost + rotation one-parameter subgroups"
has as its infinitesimal/tangent shadow "the boost + rotation infinitesimal generators lie in, and
close under the Lie bracket within, the infinitesimal-isometry algebra." That tangent algebra is
immediately bankable from the banked continuum form + the banked Born composition law `gFormC_comp`.

CHILD (N52, the heavy remainder): the global exp-integration of this tangent algebra to the
connected component + the Cartan/polar (KAK) decomposition + that EVERY connected continuum isometry
is a finite product of the one-parameter subgroups (the actual surjectivity). Honest W1 remainder.

## WHAT N51 PROVES (each a theorem, foundations-only, forward from banked objects)
1. `BvC (p q : STVC) : Cut := p.1*q.1 − p.2.1*q.2.1 − gFormC p.2.2 q.2.2` — THE POLARIZED Minkowski
   form (the bilinear polarization of the banked `QvC`).
2. `BvC_self : BvC p p = QvC p` (polarization recovers the quadratic form on the diagonal).
3. `BvC` is symmetric + biadditive in each slot (via `gFormC` symmetry/biadditivity over `Cut`,
   mirroring the banked ℚ proofs `gForm_symm`/`gForm_add_*` in `DerivationCompact.lean`).
4. `IsInfIsomC (T : Module.End Cut STVC) : Prop := ∀ p q, BvC (T p) q + BvC p (T q) = 0` — the
   infinitesimal isometry (form-skew) predicate: the tangent condition `d/ds QvC(e^{sT} p)|₀ = 0`.
5. THE TANGENT ALGEBRA closed under: `0`, `+`, `Cut`-`smul`, and the LIE BRACKET
   `⁅S,T⁆ = S∘T − T∘S` — packaged as a `LieSubalgebra Cut (Module.End Cut STVC)` (fallback:
   `Submodule` + standalone `infIsom_bracket_mem` if the LieSubalgebra bundle fights — W5/THE ONE
   LAW: do not fight the instrument). The bracket-closure proof is GENERIC (biadditivity of `BvC` +
   skewness of `S`,`T`), NO coordinate brute.
6. `boostGenC : Module.End Cut STVC`, `(t,x,v) ↦ (x, t, 0)` — the INFINITESIMAL boost generator
   (the off-diagonal shear, `d/ds boostFunC|₀`). Membership: pure field arithmetic (`BvC` of the
   `(t,x)` block, the `v`-block untouched).
7. `rotGenC (a : O Cut) : Module.End Cut STVC`, `(t,x,v) ↦ (0,0,a·v)` — the INFINITESIMAL rotation
   generator (left-mult by `a` on the space block). Membership when `a` is IMAGINARY
   (`gFormC a 1 = 0`, i.e. `reQC a = 0`), via THE SKEW IDENTITY (theory-native reframe):
   `gFormC (a·v) w + gFormC v (a·w) = 2·(gFormC a 1)·gFormC v w`, derived by POLARIZING the banked
   Born composition law `gFormC_comp` in the generator slot — NO adjoint3 (wrong CD-depth), NO brute.
8. THE HEADLINE: `⁅boostGenC, rotGenC a⁆ ∈` the tangent algebra (immediate from bracket-closure +
   memberships) — the boost + rotation infinitesimal generators CLOSE under the bracket within the
   infinitesimal-isometry algebra: the Lie-algebra-of-the-connected-component structure, the tangent
   shadow of "generated by the one-parameter subgroups."
9. NON-VACUITY (W8): the tangent algebra contains a NONZERO element (`boostGenC ≠ 0`), so it is not
   the trivial `{0}` algebra.

## GO / NO-GO
GO if: BvC + its bilinearity compile cheap; the bracket-closure theorem compiles (generic, no brute);
the boost generator membership (field arithmetic) + the rotation generator membership (polarized
`gFormC_comp`) compile; the LieSubalgebra (or Submodule + bracket theorem) packaging compiles;
costume C82 BITES; foundations-only.
NO-GO / decompose-further if: the `Module.End Cut STVC` + LieSubalgebra instance plumbing or the
polarization fights past a measured KILL → narrow to the predicate + bracket-closure + one generator
membership, bank that, child the rest.

## KILL BUDGET (W9 — instrument failure, not theory)
- No `maxHeartbeats` inflation. Default ceiling only. If any single obligation needs an inflated
  ceiling → it is the WRONG framing (a coordinate brute where a structural route exists); STOP,
  reframe through `gFormC_comp`/biadditivity, do NOT raise the ceiling.
- No monolithic brute (`ring`/`decide`/`simp`) on a fully-expanded 11-coordinate goal. The skew
  identity is via polarization; the generators' maps are `ext <;> simp <;> ring` on the small blocks.
- Per-obligation KILL: if one lemma does not close in < ~60s of a bounded probe, it is an instrument
  wall → measure/decompose/reframe, never a longer grind.
- MEASURE FIRST (this run): probe (P1) `Module.End Cut STVC` + bracket + `LieSubalgebra` typecheck;
  (P2) `gFormC` biadditivity/symmetry over Cut; (P3) the polarized skew identity; (P4) a Cut-linear
  generator endo. Only after the probes are green do I write production.

## ANTI-TIMEOUT (W9 — N43/44/46/49/50 all timed out on the gate ceremony)
- Bank-as-you-go: the instant the core lemmas compile, MOVE to a production file under Phys/Algebra/
  (`LorentzContinuumLieAlgebra.lean`), wire (Phys.lean + AxiomAudit import AT END OF IMPORT LIST +
  #print axioms), commit BEFORE chasing the full packaging.
- Gate in BACKGROUND; do not block the turn budget on D6 (cold ~6 min). Cached `lake build Phys` ~13s.
- Checkpoint ~50% turns; finalize (commit + child + complete) with turns to spare.

## STANDARD CHECK (to apply before "done")
- UNBROKEN: BvC/QvC/gFormC/gFormC_comp/qvIsomMonoidC (banked N49/N50), O Cut = CD (H Cut) (generic
  terminal algebra), Cut (derived ℝ N11–13), Module.End/LieSubalgebra machinery on the DERIVED
  objects. NO posited Lorentz group, NO Mathlib ℝ/ℂ as content.
- COMPLETE: foundations-only `[propext, Classical.choice, Quot.sound]`; no sorry/axiom/native_decide/
  inflated-heartbeats.
- WORDS-REMOVABLE: delete "Lorentz/boost/rotation/isometry/SO⁺(1,9)/generator/Lie/tangent/spinor" →
  over the derived complete ordered field `Cut` and the CD-double³ `O Cut`, the additive/Cut-linear
  self-maps `T` of `Cut × Cut × O Cut` with `∀ p q, BvC(T p)q + BvC p (T q) = 0` form a
  bracket-closed `Cut`-submodule of `Module.End Cut (Cut×Cut×O Cut)`, containing `(t,x,v)↦(x,t,0)`
  and `(t,x,v)↦(0,0,a·v)` for `reQC a = 0`, pure math.

## Costume C82 (W8 — must BITE a WRONG claim)
The WRONG claim: `rotGenC a` is an infinitesimal isometry (form-skew) WITHOUT the imaginary
hypothesis `gFormC a 1 = 0` — taking `a = 1` (`gFormC 1 1 = 1 ≠ 0`), the skew identity gives
`gFormC v w + gFormC v w = 2·gFormC v w`, NOT `0`, so `IsInfIsomC (rotGenC 1)` is FALSE (it would
force `2·gFormC v w = 0` for all v,w, e.g. `2·1 = 0` at the unit). The wrong claim must fail to
compile.
