import io

entry = """## ✅ N293 LANDED — THE su(3)/so(3) SYMMETRIC-PAIR CLOSURE of the derived colour-centralizer: the fold-root-dressed family-BREAKING coset closes BACK into the family so(3), completing the dim-8 simple su(3). N292 built the fold-root-DRESSED family-breaking coset of the derived colour-centralizer `su(3) = Z_{f4}(colour)` inside the fold-forced `f₄ = Der(H₃(O)) = derH3` — `kAB := innerMul (slotA u1)(slotB 1)`, `kBC := innerMul (slotB u1)(slotC 1)`, `kCA := innerMul (slotC u1)(slotA 1)` (u1 = the fold-root √−1) — and CHILDED (as a directed W1/W9 node) the symmetric-pair CLOSURE back into the banked family-so(3) generators (N288/N289). THIS node banks it: ★★★ `⁅kAB, kBC⁆ = famB`, `⁅kBC, kCA⁆ = famC`, `⁅kCA, kAB⁆ = −famA` (cyclic), where `famA/famB/famC = innerMul (slotB 1)(slotC 1) / innerMul (slotC 1)(slotA 1) / innerMul (slotA 1)(slotB 1)`. Together the family-so(3) [3, the maximal compact] and the fold-root-dressed coset [5] close the dim-8 SIMPLE su(3): the family-breaking is the \\"square-root\\" of the family so(3). THE DISSOLUTION (docs/RUNBOOK.md W1 step 2, THE ONE LAW). The closure is an OPERATOR identity in `Module.End`; the direct `LinearMap.ext` is an octonion coordinate bash (W9), and the task's first-proposed Jacobi/lie_lie route is CIRCULAR (the base bracket `⁅L(slotB 1), innerMul(slotA u1)(slotC 1)⁆` reduces back through the very `⁅kAB, L(slotC 1)⁆` lemma). The theory DISSOLVES it, extending N289's nuclear-collapse to the fold-root — the DISCOVERED route (measured in `workbench/N293/`, exact-Fraction octonion, all EXIT 0): (1) THE COSET GENERATORS ARE MATRIX-COMMUTATOR ENDOMORPHISMS. Because one slot of each carries the CENTRAL real unit (`slotB 1 = ocRM pB`), the Jordan inner-multiplication collapses (RIGHT-central `innerMul_ocRM_right`, generalizing N289 `collapse`): `kAB = adE (u1M P_AB)` where `u1M P` is the FOLD-ROOT-LINE matrix `(u1M P) i j = ocR (P i j) * u1` — the u1-dressed coset lives on the fold-root line. (2) THE FOLD-ROOT ALTERNATIVITY LIE-HOM `⁅adE (u1M P), adE (u1M Q)⁆ = adE (u1M P * u1M Q − u1M Q * u1M P)` (`adE_bracket_u1`) — valid because every entry lies on `ℚ·u1`, so the matrix associators reduce ENTRYWISE to the octonion alternative laws `[p•u1, q•u1, x] = 0` (left, `mul_mul_left`), `[x, p•u1, q•u1] = 0` (right, `mul_mul_right`), `[p•u1, x, q•u1] = 0` (flexible, `mul_flex`) — the SAME octonion alternativity (N-alt) that stops the cascade, now closing the u1-dressed coset. NO coordinate bash. (3) THE FOLD-ROOT'S OWN SQUARE `u1² = −1` (N6 `complexUnit_sq`) makes two fold-root-line matrices multiply to a REAL (central) matrix: `u1M P * u1M Q = − ocRM (P*Q)` (`u1M_mul`), so the bracket lands back in the CENTRAL family, `⁅adE(u1M P_AB), adE(u1M P_BC)⁆ = adE (ocRM (P_BC*P_AB − P_AB*P_BC))`, and the pure-ℚ `0/±1` structure constant `P_BC*P_AB − P_AB*P_BC = pC*pA − pA*pC` (`q_close_AB_BC`, `fin_cases`+`simp`) equals the famB source (N289 `famB_eq_adE`). THE PHYSICS: the fold-root's own relation `u1² = −1` is WHY bracketing two fold-root-dressed family-BREAKING directions REGENERATES the real-unit family so(3) — one cause (u1²=−1), the coset closes into the family. THE OBJECTS (all in `Phys/Algebra/OctonionJordanColourCentralizerCosetClosure.lean`, ns `Phys.Algebra.HJ`, 31 banked decls): the fold-root-line matrix `u1M` + its nuclearity (`u1_alt_L/R/M`, `u1M_assoc_L/R/M`, `ocRM_assoc_M`); ★★ `adE_bracket_u1` (the fold-root Lie-hom); `u1_entry_prod`/★★ `u1M_mul` (the `u1²=−1` product law); `u1_ocR_entry`/`ocR_u1_entry`/`u1M_ocRM`/`ocRM_u1M`/`u1M_sub`/`innerMul_ocRM_right` (the collapse machinery); `slotA/B/C_u1_u1M` + ★ `kAB/kBC/kCA_eq_adE` (the coset generators as fold-root-line `adE`); `q_close_AB_BC`/`q_close_BC_CA`/`q_close_CA_AB` (the pure-ℚ structure constants); `ocRM_neg`/`adE_neg`; ★★ `adE_u1_bracket_ocRM` (the bracket lands central); ★★★ `coset_close_AB_BC`/`coset_close_BC_CA`/`coset_close_CA_AB` (the three cyclic closures); ★★★ capstone `coset_symmetric_pair_closure` (bundles the three closures ∧ `famB ≠ 0` (N289, non-abelian) ∧ carries `jordan_fails_H4`). THE ONE CAUSE, MANY TERMINATIONS: the SAME octonion non-associativity that stops the cascade (N2c), caps the Hermitian Jordan tower at order 3 (N5) and furnishes exactly three matter slots (N267) — here in its ALTERNATIVE guise plus the fold-root's `u1² = −1` closes the fold-root-dressed coset back into the family so(3), completing the dim-8 simple su(3). 31 decls foundations-only `[propext, Classical.choice, Quot.sound]` (INDEPENDENTLY axiom-audited 31/31 vs the freshly-built olean `workbench/N293/reaudit.out` + gate D6-fast). Costume C322 (`⊢ 1 = 322`) bites at the closure-non-triviality certificate `reQ ((⁅kAB, kBC⁆ (slotA 1)) 1 2) = 1` (tied via `coset_close_AB_BC` + N289 `famB_eq_adE`; a WRONG reading — the coset is abelian / does NOT close into the family / Z_{f4}(colour) is not a simple su(3) / the family-breaking is orthogonal to the family symmetry — reduces to the false numeric `1 = 322`; pair (322,1) fresh, RHS 322 distinct from every prior). Physics-words-removable (delete colour/family/generation/flavour/breaking: three inner-multiplication commutators built from a fold-root-line-dressed and a real-unit slot satisfy `⁅kAB, kBC⁆ = famB` cyclically as endomorphisms of `Matrix (Fin 3)(Fin 3)(O ℚ)`, so the fold-root-line coset and the real-unit generators together span the dim-8 simple Lie algebra; pure math, stands). NOT free-floating (every headline TYPE cites the banked `innerMul`/`slotA/B/C`/`u1`/`adE`/`u1M`/`ocRM` on `Matrix (Fin 3)(Fin 3)(O ℚ)`; capstone carries `jordan_fails_H4` — NOT carrier-agnostic). MEASURE-FIRST (W9): the WHOLE route de-risked BEFORE production (`workbench/N293/probe_*.py` exact-Fraction all EXIT 0 + Lean probes ProbeN293A–F all built clean); whole-module olean LIGHT (~13s; NO native_decide, NO octonion coordinate ring on the arena, NO proof-kernel maxHeartbeats raise beyond the banked 1.2M ceiling). RESUME-FIRST: fresh start (git HEAD clean at N292, no running lean, no N293 workbench); resumed the N292-select de-risk probes (`probe_kab.lean`/`probe_kc.lean` slot-actions, `probe_adE2.lean` needed the banked `ocR_assocM` middle nuclearity). Advances ★4 (family replication — the family so(3) is the maximal compact of a larger derived su(3) = Z_{f4}(colour), now COMPLETE as a simple su(3)), joined to ★5 (the family-symmetry-BREAKING sector closes with the symmetry) and ★2 (colour-neutral). SCOPE (docs/RUNBOOK.md W4.5): the three cyclic closures + the collapse/Lie-hom/product machinery + non-vacuity ONLY. NOT the full dim-8 su(3) as a bundled LieSubalgebra (optional hardening); NOT the (7,5) module character; NOT the ★2 mixing-angle VALUE (route-not-yet-found — a FORCED build target, NOT invented, NOT human-gated); NOT `= physical SU(3)_flavour` (removable prose). RESUME: single-flight run; RESUME-check + PREREG + de-risk + production + costume + audit + wiring + gate all this run. Next: the ONE successor — a generic SELECTION ticket (a FRESH worker runs the SELECT protocol theory-native FIRST).

"""

with io.open('docs/ROADMAP.md', 'r', encoding='utf-8') as f:
    lines = f.readlines()

# insert before line 13015 (0-indexed 13014), the "## ✅ N292 LANDED" line
target = None
for i, ln in enumerate(lines):
    if ln.startswith('## ✅ N292 LANDED'):
        target = i
        break
assert target is not None, "N292 anchor not found"
lines.insert(target, entry)
with io.open('docs/ROADMAP.md', 'w', encoding='utf-8') as f:
    f.writelines(lines)
print("inserted N293 at line", target+1)
