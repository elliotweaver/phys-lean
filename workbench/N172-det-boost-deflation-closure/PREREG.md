# N172 — PREREG (frozen before compute)

## TARGET (chosen front, against the ROADMAP §N172 frontier)
The DET-OF-BOOST / DET-OF-DEFLATION / FINAL-ASSEMBLY route (priority (i)) into the FULLY
UNCONDITIONAL SO⁺(1,9) identity-component closure: JoinedIdQvC g for EVERY proper (det g = 1)
orthochronous (0 ≤ (g e₀).1) QvC-isometry g.

## THE REFRAME (THE ONE LAW / W1 — measured before grinding)
genBoostLin and axisRotLin (= deflateNinth's shape) are BOTH `id + (rank-2 perturbation)` that
factors through a 2-plane:
  - genBoostLin wx wv a b = id + G∘F, F:STVC→Cut² reads (t, σ=wx·x+gFormC v wv),
    F∘G = [[a-1,b],[b,a-1]] (using unit wx²+gFormC wv wv=1) ⇒ 1+F∘G = [[a,b],[b,a]], det a²−b²=1.
  - axisRotLin e c s = id + G∘F, F:STVC→Cut² reads (x, gFormC v e),
    F∘G = [[c-1,-s],[s,c-1]] (using unit gFormC e e=1) ⇒ 1+F∘G = [[c,-s],[s,c]], det c²+s².
This is the SAME Sylvester/Weinstein–Aronszajn flip det_houseHolder ALREADY uses (banked,
compiling): det(id + G∘F) = det(1_{Cut²} + F∘G) via Matrix.det_one_sub_mul_comm. NO 10×10
expansion (the W9 trap), NO det-continuity (endOpC pointwise, MEASURED DEAD N171), NO det_conj.

## GO / NO-GO
GO if the Sylvester-flip skeleton closes det_axisRotLin (the smallest/rotation case) in a bounded
probe. Then det_genBoostLin (parallel), det_deflateNinth (circle condition x'²+gFormC v' v'=1),
then the final assembly.

## THE FINAL ASSEMBLY (det-multiplicativity, derived not smuggled)
p proper (det p = 1) orthochronous ⟹ p = B·k (N169 boost peel, B=genBoostLin, k time-fixing).
  det B = 1 (det_genBoostLin) ⇒ det k = det p / det B = 1.
  k moves x-axis (v'≠0): deflateNinth x' v' · k FIXES both axes (deflateNinth_step + time-fixing),
    so KEYSTONE det_fixingTX_eq_vBlock: det(vBlock(deflateNinth·k)) = det(deflateNinth·k)
    = det(deflateNinth)·det(k) = 1·1 = 1. Feed joinedIdQvC_of_timeFixing_SO9 (N170).
  k fixes x-axis (v'=0 antipode x'=+1): joinedIdQvC_of_xtFixing_det_one (N163), det(vBlock k)
    = det k = 1 by keystone.
  ANTIPODE x'=−1: (0,1,0)↦(0,−1,0). MEASURE whether properness forces this out OR needs the
    π-rotation det-path. If genuinely hard ⇒ W3 DECOMPOSE: bank det_genBoostLin/det_deflateNinth/
    det_axisRotLin + the v'≠0 and x'=+1 cases now; child the antipode/full-unconditional remainder.
Then joinedIdQvC_of_timeFixing_residual (N169) lifts to p. The det p=1 is taken ONLY as the
hypothesis (proper), propagated — NEVER smuggled as a v-block det input.

## KILL (instrument-failure thresholds)
- Any single tactic > 90s, or a probe needing maxHeartbeats inflation = INSTRUMENT failure (W9):
  STOP, the framing is wrong, return to the structural flip; do NOT grind a 10×10.
- If the Sylvester flip skeleton does not close det_axisRotLin in ≤ ~6 probe iterations ⇒ the
  toMatrix-basis route is the wrong instrument; reconsider (det_conj to coordinate plane).
- Antipode x'=−1 resisting after measure+reframe ⇒ W3 decompose + child (NOT assume, NOT weaken).

## W8 ANTI-VACUITY
New costume C204 must bite a WRONG det-of-boost / det-of-deflation / unconditional-closure /
JoinedIdQvC claim, reducing to a false numeric (1=172) DISTINCT from the banked battery
(C201 1=169, C202 1=170, C203 1=171). The final theorem must be phrased in JoinedIdQvC (NOT the
vacuous JoinedIdC) and take only "proper orthochronous QvC-isometry".

## PHYSICS-WORDS-REMOVABLE
Delete boost/rotation/deflation/Lorentz/orthochronous/proper/det/SO/octonion/time-fixing/Joined:
the statements stand as pure linear algebra over the derived ℝ Cut — det of an explicit
id+rank-2 endomorphism of Cut×Cut×O Cut equals an explicit 2×2 determinant; det multiplicative.
