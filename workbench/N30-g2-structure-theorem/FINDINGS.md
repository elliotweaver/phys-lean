# N30 FINDINGS — measured route + decomposition boundary (frozen at decomposition)

## WHAT N30 BANKED (commit 2475133 + this run, Phys/Algebra/DerivationSimpleStructure.lean)
FORWARD, foundations-only ⊆ {propext, Classical.choice, Quot.sound}, NO posited G₂:
- `exists_atom_ne_top` : ¬IsSimpleOrder ⟹ ∃ atom I, I ≠ ⊤ (≥2 atoms). Entry point.
- `atom_compl_decomp`  : the Boolean complement J=Iᶜ ⟹ I≠⊥, Iᶜ≠⊥, I⊔Iᶜ=⊤, I⊓Iᶜ=⊥, ⁅I,Iᶜ⁆=⊥.
- `imRep_commute_of_lie_zero` : ⁅a,b⁆=0 ⟹ imRep a, imRep b commute in End ℚ ImO (imRep a Lie hom).
- `atom_isPerfect`     : ⁅⊤,⊤⁆=⊤ in an atom I (atom simple ⟹ perfect). Kills dim W=1 branch.
- `atom_center_eq_bot` : center ℚ I = ⊥ for an atom I (atom simple ⟹ trivial radical). Kills dim Δ=7.
Costume C50 (`DerivationSimpleStructureWrongDecompCostume`) bites ⊢ ⊥ = ⊤.

## THE MEASURED CRUX (W9 reconnaissance — why decomposed, NOT scope reduction)
The remaining gap to `IsSimpleOrder (LieIdeal ℚ derivationLieQ)` is the prime-7 / isotypic-Schur
STRUCTURE THEOREM. Its STEP A — "V = ImO is I-isotypic" — fundamentally needs the
Lie→associative semisimple-module BRIDGE that Mathlib LACKS for Lie modules:
  • MEASURED: `A := Algebra.adjoin ℚ (Set.range (fun x : I => imRep (I.incl x)))` forms cleanly
    (probe6, ~13s); `ImO` is an `A`-module via `A.moduleLeft` automatically.
  • MEASURED: Mathlib HAS the associative machinery once `IsSemisimpleModule A ImO` is in hand:
    `Mathlib/RingTheory/SimpleModule/Isotypic.lean` (`IsIsotypicOfType`, `IsIsotypic`,
    `IsIsotypicOfType.linearEquiv_fun : M ≃ₗ (Fin n → S)` for finite isotypic-of-type),
    `Mathlib/RingTheory/SimpleModule/Basic.lean` (`IsSemisimpleModule` = `ComplementedLattice
    (Submodule A M)`), `Mathlib/RingTheory/SimpleModule/Rank.lean`
    (`isSimpleModule_iff_finrank_eq_one`), and Schur `Module.End.instDivisionRing` for a simple
    A-module. These are the EXACT tools the prime-7 count consumes.
  • The BRIDGE itself (`IsSemisimpleModule A ImO`) is the substantial Mathlib-absent BUILD. THE
    TRUNK ROUTE (THE ONE LAW, reuse N27): an A-submodule of ImO is exactly an I-invariant
    subspace (A is generated by the I-action); its gBil-orthogonal complement (banked
    `isCompl_gBil_orthogonal`) is ALSO I-invariant because each element of I acts skew-adjointly
    (banked `deriv_mapsTo_orthogonal` holds for every derivation). So `Submodule A ImO` is a
    COMPLEMENTED lattice ⟹ `IsSemisimpleModule A ImO`. This is N27's complete reducibility
    transported from the WHOLE algebra to the SUBalgebra A_I — a real but trunk-native build.

## THE DECOMPOSITION BOUNDARY (matches the ticket's pre-authorization)
- N30a (the IMMEDIATE next node) = the Lie→associative semisimple-module BRIDGE:
  build `A_I`, prove `IsSemisimpleModule (A_I) ↥ImO` via the Born-orthogonal complemented
  lattice (the dictionary: A-submodule ⟺ I-invariant subspace ⟺ orthogonal complement
  invariant), and the SUBMODULE DICTIONARY lemmas (A-submodule ↔ I-invariant; the J-action
  lands in `End_{A_I}(ImO)`). This is the genuinely Mathlib-absent infrastructure.
- N30b (childed onto N30a's tail by N30a) = the PRIME-7 COLLAPSE: with the bridge banked, run
  isotypic (`IsIsotypicOfType.linearEquiv_fun`) ⟹ 7 = r·dim W; the dim W=1 branch via
  `atom_isPerfect` + faithfulness (`lie_ImLie_ne_bot_of_ne_bot`); the dim W=7 / Schur branch via
  `Module.End.instDivisionRing` + the skew-traceless scalar argument (`derivationLieQ_le_skewAdjoint`)
  + `atom_center_eq_bot` + `finrank_derivationQ_eq_14`; ⟹ `IsSimpleOrder` ⟹ `LieAlgebra.IsSimple`
  via banked `isSimple_of_isSimpleOrder`. Then the automorphism Lie GROUP.

## WHY THIS IS HONEST DECOMPOSITION (W3/W9, NOT a bridge, NOT scope reduction)
The full collapse is a multi-lemma build whose crux (the bridge) is Mathlib-absent and which the
ticket EXPLICITLY pre-authorizes splitting ("decompose further if the bridge is itself
multi-lemma — e.g. child 'N30a: the Lie→associative semisimple-module bridge' then 'N30b: the
prime-7 collapse'"). N28 timed out TWICE on a comparable monolith; per W9 anti-timeout the clean
forward levers are banked and the bridge scoped to a dedicated node, finalized with turns to
spare. NOTHING asserted; the IsSimple target is NEVER claimed here, only its reduction levers.
