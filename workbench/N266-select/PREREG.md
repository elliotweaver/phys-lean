# N266 PREREG — THE COLOUR-NEUTRAL CORE OF THE MATTER CARRIER IS THE FOLD'S OWN COMPLEX LINE

## TARGET (selected via SELECT S1–S5, anti-bullshit gate passed, comments 114–117 on t_3fa978c1)
The DERIVED colour subalgebra `colourCentralizer` (the g₂-stabilizer of the fold's √−1
`JO = L_{u1}`, N201) acts on the full INDIVISIBLE matter carrier `O ℚ` (N265) with joint kernel
EXACTLY `span{1, u1}` — the fold's OWN complex line ℂ (`u1²=−1`, closed under multiplication, a
2-dim subalgebra) — and complement the banked `Vsub = 3 ⊕ 3̄`. So `O ℚ = ℂ ⊕ (3 ⊕ 3̄)` as
colour-modules: colour is BLIND to precisely the fold's generative √−1 and unit, and charges the
rest. The ★1↔★3 joint (how the derived gauge acts on the derived matter carrier).

Production file: `Phys/Algebra/TowerWorldMapColourNeutralCore.lean`.

## DECLS (all de-risked EXIT 0 foundations-only this run, workbench/N266-select/)
- `colour_kills_u1` — every colour derivation annihilates u1 on the full carrier (lift N245).
- `matterColourNeutral` — the colour joint-kernel ⨅ D:colourCentralizer, ker (D on O ℚ).
- `span_one_u1_le_matterColourNeutral` (⊇) — 1 (derivQ_one) + u1 (colour_kills_u1).
- `matterColourNeutral_le_span_one_u1` (⊆, THE CRUX) — reQ-split + N252 colourInvariants_eq_span_sgl.
- `matterColourNeutral_eq_span_one_u1` (HEADLINE) — le_antisymm.
- `one_u1_linIndep` + `finrank_span_one_u1` = 2 (W8: the neutral core is a genuine plane, not a line).
- `span_one_u1_mul_mem` — span{1,u1} CLOSED under multiplication (u1²=−1): the fold's ℂ subalgebra.
- `neutral_disj_Vsub` — span{1,u1} ⊓ Vsub = ⊥.
- capstone `matter_colour_neutral_core` — bundles: neutral core = span{1,u1}, dim 2, mult-closed,
  disjoint from Vsub, complement = Vsub (the full carrier splits 2+6=8 under colour).

## COST BUDGET + KILL (W9)
Every crux compiled EXIT 0 in bounded probes (each ≤ ~17s wall net-of-import; structural rewrites
over banked lemmas, NO octonion coordinate bash, NO native_decide, NO maxHeartbeats raise). KILL: if
the whole-module olean exceeds ~90s or any obligation needs a maxHeartbeats raise, STOP — measure and
decompose; it is a W9 signal. (Not expected — all pieces measured light.)

## FREE-FLOATING CHECK (SOUL rail) — PASSED
Every headline TYPE mentions the banked tower objects: `colourCentralizer` (the g₂-stabilizer of
the fold's √−1), the concrete `O ℚ`, `u1`, `Vsub`. The ⊆ crux routes through N252
(`colourInvariants_eq_span_sgl`), which is g₂/octonion-SPECIFIC (proved via a regular colour element
forcing rank) — NOT a carrier-agnostic fact. Contrast the REJECTED β candidate (star∘L=−R∘star),
which survives word-deletion as a fact about ANY *-ring (free-floating) — that is why β was rejected.

## HONEST SCOPE
Banks the colour-decomposition of the full matter carrier + the neutral-core-IS-the-fold's-ℂ
identification + the direct sum with Vsub ONLY. NOT the charge/hypercharge VALUES on each multiplet
(a later assignment node), NOT the full SM multiplet classification, NOT parity, NOT the mixing angle
(≥9× deferred, ascent tower — untouched). NOVELTY claimed ONLY for the forced fold-derivation (the
moat: we derive what Furey posits — Furey imposes ℂ⊗O and chooses SU(3); here span{1,u1} FALLS OUT
as the colour-neutral core and IS the fold's own √−1, colour IS the g₂-stabilizer of that same √−1),
NOT for the 1⊕3⊕3̄-type decomposition pattern itself.

## COSTUME
C297 — anchor on the neutral-core dimension `finrank ℚ matterColourNeutral = 2` (W8 non-vacuity: the
colour-invisible core is a genuine PLANE — the fold's ℂ — not the 1-dim vector-7 singlet and not 0).
Claim 297 = 2, bites 2 = 297.
