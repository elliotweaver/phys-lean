# PREREG — Z8 THE AIRTIGHT VALIDATION CAPSTONE (arc-Z ENDPOINT-ASSEMBLY, TERMINAL, anti-Lovelock)

Node: Z8 (directed successor of Z7/WayPointReadings). Task t_e6b6fabd. Owner seed
docs/SEED_ENDPOINT_ASSEMBLY.md §Z8. Module: `Phys/Algebra/ZArcValidationCapstone.lean`.

## FRONT (theory-native, chosen fresh — not the adjacent easy lemma)
Z8 is the arc's TERMINAL consolidation node. Its theory-native job: PROVE nothing was left over —
the anti-Lovelock guard. The non-hollow HEART (statable from no single conjunct) is the ONE-TOWER
WELD: the SM measures 1/α at unrelated scales (α(M_oct), α(v), α(M_Z), α(Λ), α(0)) as separate
inputs, each with its own error bar. The fold reads ALL of them as PARTIAL SUMS of the ONE completed
band tower `completeBandList d` from the octonion ceiling — one derived object, five readings at one
descent. That weld + a total-partition ledger audit (coverage ∧ disjoint over the ENLARGED ledger)
+ the paid X10 quark-texture debt + honest per-value grades (theoremExact vs routeNotYetFound) is
the capstone. This is NOT a bare ∧ of banked capstones; it is the SHARING (one tower) + the PARTITION.

## WHY THIS IS THE RIGHT NODE (anti-bullshit gate)
- Fold→node forced: every summand is a banked derived object back to the fold (κ=1/(3π), the census
  Tr(Q²), the octonion ceiling 42, cutLog on derived Cut). The weld re-uses `invAlphaZero`/
  `completeBandList`/`leptonTail` (Z6), the way-points (Z4), the M_Z refinement (Z7), the dressing
  (Z5), the census sense (Z1), the channel break (Z2), the quark texture (Z3). Nothing new posited.
- Standard version & what the theory DOES to it: SM = 5 unrelated measured coupling values at 5
  scales. Theory = 1 tower, 5 partial sums, exact closed reals; the "running" IS the census-in-census.
  The capstone dissolves the "5 independent inputs" framing into ONE object.
- Words-removable: delete coupling/mass/hadronic/Weinberg/confinement/electroweak → pure real
  analysis over Cut (partial sums of a finite list `invAlphaZero`, `cutLog`, `bandScreen`) + a
  finite-enum ledger whose coverage/disjointness/no-upgrade hold by kernel `decide`.
- NOT free-floating: every production TYPE mentions the banked derived objects (`invAlphaZero`,
  `completeBandList`, `leptonTail`, `derivedBandList`, `confBandRatio`, `mzBelow`, `invAlphaMZ`,
  `dressedInvAlphaZero`, `massVec`, `ckmTexture`, `traceForm7`, `sinSqRun`, `cutLog`, `Cut`).
- Temptation named: the easy adjacent move is a HOLLOW `∧` of the seven banked landings. Refused —
  the weld is the one-tower SHARING (a NEW theorem `one_tower_weld`), the audit is the PARTITION
  (a NEW total-coverage/disjointness pair over the enlarged ledger), and C502 bites on the load-
  bearing content.

## DESIGN (mirror + UPGRADE X10 `XArcValidationCapstone`)
1. `ZPrediction` structure over Cut: {ceiling, ewWaypoint, confWaypoint, endpoint}, populated by
   `zPrediction` = {invAlphaHigh, invAlphaZero [(chargeTraceDepth,topBandRatio)], invAlphaZero
   derivedBandList, invAlphaZero (completeBandList chargeTraceDepth)}.
2. Field-value theorems: ceiling=42 (invAlphaHigh_eq), ewWaypoint=1582/27 (em_waypoint_consistency),
   confWaypoint=190/3 (derivedBandList_assembly), endpoint=190/3+κ·d·cutLog(mass0/mass1)
   (endpoint_expanded chargeTraceDepth).
3. ★ `one_tower_weld (d)` — NON-HOLLOW HEART: the completed tower reads 190/3 (confWaypoint_in_complete),
   1582/27 (ewWaypoint_in_complete), the endpoint closed real (endpoint_expanded), AND the M_Z reading
   (endpoint_reads_MZ, ∀ vz zL under the forced product constraint) — all partial sums of the ONE tower.
4. Enlarged ledger: `ZValue` inductive (theoremExact structures: ceiling, ewWaypoint, confWaypoint,
   topBandScreen, confBandScreen, weinbergHigh, gatherSelfCensus, twoChannelBreak, dressingExact,
   endpointClosedForm, mzReadingStructure, quarkTextureStructure ← PAYS X10 debt; anchoredOnDerivedScale:
   leptonAbsMasses, confScaleAbs; routeNotYetFound: vzSubBandNumeral, lightQuarkHadronicEdges,
   perBandCensusFreezeout, cutLogMass01Numeral, quarkMassRatioNumerals; residual/removableProse:
   physicalNumeral, leptonLabelling, measuredComparison). `Grade`/`Disposition` inductives as X10.
5. `zGradeLedger` / `zResidualLedger`; `gradedValues`/`residualValues`.
6. ★★ `ledger_coverage` (∀ v, v∈graded ∨ v∈residual) ∧ `ledger_disjoint` (∀ v, ¬(both)) — by
   `cases v <;> decide`. NO-RESIDUAL is a theorem over the ENLARGED enum.
7. W8 teeth: `quark_texture_structure_earned` ((quarkTextureStructure,theoremExact)∈zGradeLedger — the
   PAID debt); `quark_numerals_not_upgraded` / `rdescent_not_upgraded` / `absolutes_not_upgraded`
   ((…,theoremExact)∉zGradeLedger for the routeNotYetFound + anchored values — NO silent upgrade);
   `ledgers_nonempty`.
8. `zMeasuredComparison` removable-prose column (referenced by NO theorem).
9. ★★★ `zArcValidationCapstone (w) (hw0)(hw1)` — welded landing bundling (I) one-tower weld, (II)
   field values, (III) Z1 census sense (<0, opposite to matter >0), (IV) Z2 break (∀ strict <3/8) +
   switch-off 3/8, (V) Z5 dressing 42+(64/3)/(1−w), (VI) Z3 quark texture (koide_total + mixing_nonzero
   + Fano-CP present), (VII) partition (coverage ∧ disjoint), (VIII) no silent upgrade + paid debt,
   (IX) teeth (endpoint>190/3; M_Z reading strictly below endpoint). Statable from no conjunct alone.

## GRADE (honest, per-value — the whole point of §Z8(b))
- THEOREM-EXACT: the one-tower weld, all field values (42, 1582/27, 190/3, endpoint closed form),
  the census sense, the channel break, the dressing closed function, the quark-texture STRUCTURE
  (forced cubic/Koide/geometric-mean/Fano-CP/CKM — Z3, this PAYS X10's namedOpenStructural), the
  partition/no-upgrade audit.
- ANCHORED-ON-DERIVED-SCALE: absolute lepton masses, absolute confinement scale (cited X8/X9).
- ROUTE-NOT-YET-FOUND (enumerated ℝ-descent, NEVER upgraded/asserted/fitted): vz numeral;
  complete-tower light-quark/hadronic sub-band edges; per-band census freeze-out; cutLog(mass0/mass1)
  numeral; individual numerical quark mass ratios. Carried in the grade ledger as routeNotYetFound.
- REMOVABLE PROSE: physical MeV/GeV numeral, e/μ/τ labelling, measured comparison (137.036/0.23122/…).

## HARD GUARDS
G1 values fall out (no asserted value). G2 no empirical number (measured = zMeasuredComparison prose
+ costume only). G3 no bridge. G4 ONE successor (→ SEED_DIGITS.md D1 per mgr routing update). G5
words-removable. G6 foundations-only #print axioms ⊆ {propext,Classical.choice,Quot.sound}; derived
Cut only, NO Mathlib ℝ/ℂ as content; import guard: banked Z1–Z7 + X10 modules, NEVER MassGapOne/
OneAxiom; NO foldRetention. G7 ONE LAW. G8 teeth (C502 costume + the ∉ upgrade teeth + strict ordering).

## COMPILE-COST BUDGET / KILL
All conjuncts are `exact`/citation of banked theorems + finite-enum `decide` (≤22-constructor). No
brute `ring`/`decide` on expanded coordinates. Costume `decide` on a `min N flag` numeric. KILL: if
any single obligation exceeds ~30s or the module build exceeds ~5 min, STOP, measure the offending
`decide`/citation, decompose. Expected: fast (all citations). Enum `decide` over ~22 constructors is
cheap (X10 did 12; scales fine).

## COSTUME C502
`Counterexamples/ZArcValidationCapstoneVacuityCostume.lean` — flag=1 tied to the one-tower weld being
REAL (endpoint > confWaypoint via `endpoint_gt_conf` at a witness) AND the paid-debt row present;
bogus claim `min 502 flag = 502` reduces to `1 = 502`, bites. Manifest row C502.

## WIRING
Phys.lean import; AxiomAudit top-block import + appended #print axioms lines (the new decls);
costume_manifest.tsv C502 row; LEDGER row via temp+cat. Commit ALL before gate-fast launch; poll
non-blocking.
