# M8 PREREG — ARC-M TERMINAL: THE AIRTIGHT CAPSTONE (SEED_MASSES §M8)

## SELECT (fresh, theory-native, run FIRST)
- S1 arc synthesis: M1 banked the ONE unit peg + uniqueness; M2–M6 evaluated every dimensionful
  row (leptons, confinement scale, scale tower, heavy & light quarks) as certified two-sided
  rational brackets against that one peg; M7 closed the cross-check web (Koide straddle, way-points
  from bracketed edges, confBandRatio triple reading) — the overconstraint at numeral grade, FREE.
  Arc M's full dimensionful skeleton is banked and cross-checked.
- S2 goal lens: on the theory's own terms the fold forces ONE scale ladder cutExp(cutPi/3); every
  dimensionful constant is a READING of that one ladder against one peg. The story's next beat after
  "all readings banked + web closes" is the CLOSURE object: bundle the readings into one derived
  object that is statable from no single reading alone, and ATTEST that no numeral target is unmet.
  This is the terminal gather of arc M — the "one cause, many terminations" made a single theorem.
- S2 reframing lens: the SM's ~20 independent dials make an "everything-forced-by-one-peg"
  attestation impossible to even state (each dial is its own input). The theory DISSOLVES that:
  the capstone is a single conjunction that no proper subset implies, precisely because the
  conjuncts are readings of one ladder. The airtight capstone is theory-native, not textbook.
- S3 reconcile: the owner-binding board order is D→M→C→GR; SEED_MASSES §M8 is the arc-M terminal;
  it aligns with the theory's gather beat. Proceed.
- S4 anti-bullshit gate:
  1. fold-causal: fold → self-blindness → Born-square eigenvalues → ONE ladder cutExp(cutPi/3) →
     every dimensionful row is a reading of it against ONE peg → the closure object that welds the
     readings and attests completeness IS the terminal node. Each arrow forced.
  2. dissolution: standard framework cannot state a cross-row completeness attestation (independent
     dials); the theory makes it ONE non-hollow conjunction. Named.
  3. goal-words-removable: delete mass/quark/lepton/confinement — a single conjunction of certified
     Cut brackets + peg-uniqueness that no proper subset implies, plus a completeness certificate.
     The reason survives as pure math.
  4. temptation named: a saturated worker would "add one more cross-check". The terminal beat is not
     another check — it is the CLOSURE + the completeness gate + the handoff. Chosen deliberately.
  5. honesty: the capstone bundles ONLY banked theorems; no new crux. The two honest carried
     deferrals (M5 within-sector individual quark ratio route-not-yet-found; M6 placement
     identification carrying its two banked relative-scale hypotheses) are stated with what they
     carry — never faked, never posited.

## TARGET (M8, terminal)
Phys/Algebra/MassSkeletonCapstone.lean — ONE derived object bundling M1–M7:
- `massSkeleton_bundle`  — welds all 7 landings (M1 unitPeg_landing … M7 crossCheckWeb_landing),
  non-hollow.
- `noResidual_attestation` — every M2–M6 numeral TARGET met: each dimensionful row a certified
  two-sided rational bracket over Cut with strictly-positive width (M2 three leptons, M3 confinement,
  M4 M/v top-band, M5 heavy m_b/m_t, M6 light strange+down). The GATE: statable ONLY because every
  target is met.
- `oneP­eg_law` — (a) any two pegs differ by a single global positive rescale (M1 uniqueness);
  (b) every dimensionful row scales by the ONE positive peg p.val (pegged rows). No second peg.
- `honest_carried_named` — the two honest deferrals stated with what they carry: (i) the M6 light-band
  placement identification, re-exported hypothesis-carrying (given the two banked relative-scale
  premises, edgeLight = lightBandStrange); (ii) the M5 heavy cross-sector ratio theorem-exact bracket
  + the self-blind survival tooth (mbRatioDerived < 2/3). The M5 within-sector individual quark ratio
  is route-not-yet-found — stated in prose, never a faked theorem.
- `massSkeletonCapstone_landing` — the CAPSTONE welding bundle + attestation + peg law + honest
  carried, statable from no single conjunct alone.

## GRADE LEDGER (upgraded, honest per row)
- M2 lepton: theorem-exact-evaluated-against-peg (three certified brackets, ratios reproduce N340).
- M3 confinement: theorem-exact-evaluated-against-peg.
- M4 scale tower: theorem-exact-evaluated-against-peg (edges + way-points free).
- M5 heavy: cross-sector ratios theorem-exact-evaluated-against-peg; within-sector individual quark
  ratio route-not-yet-found (NAMED, not faked).
- M6 light: within-splittings theorem-exact (banked N340 descent); placement identification
  hypothesis-carrying re-export (two banked relative-scale premises), NOT a posited identity.
- M7 web: theorem-exact, free.

## COST (W9): bundle of banked theorems, no new crux. Probe built 18s (light). No W9 risk.

## WIRING: costume C554 (numeral-targets-met flag), AxiomAudit import+block, Phys.lean import,
manifest row, LEDGER row, gate-fast D0–D6.

## HANDOFF: exactly ONE directed successor toward docs/SEED_MIXING.md target C1 (arc C — THE MIXING,
the phase-alignment derivation). NOT GR1, NOT D7, NOT a generic SELECT.
