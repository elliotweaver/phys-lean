# D-RENARROW — s CAMPAIGN RESOLVED (owner-directed, t_0b31c1e9)

## FINAL LANDING: s = 1/8 (CasimirProjectionSelectsS, commits 513f445 + 7afd604)

The interior anti-screening magnitude `s` is DERIVED (not fitted, not a raw-census value):

    s := projectedS = (funCas / adjCas) · depthWeight = (3/8) · (1/3) = 1/8

- funCas/adjCas = 3/8  — the Casimir fraction: the fraction of the gather's ADJOINT
  quadratic self-overlap the FUNDAMENTAL doublet carries. 8 and 3 are FORCED (uniqueness
  teeth) from the banked eigenvalues N277 (casimirOp u1M = −8•u1M) and N279
  (Σ(leftReg)² = −3•id).
- depthWeight = 1/3  — the banked D3 return-surface weight, ONE depth of self-look-back
  (the mean Born ⟨cos²⟩ self-overlap that dresses every channel).

## WHY NOT 3/2 (the corrected error)

The prior intermediate `s = gatherIsoSpinCensus·(3/8) = 4·(3/8) = 3/2` was the SAME
raw-census category error the overshoot theorem already refuted — multiplying the Casimir
fraction into the RAW cross-census 4 is STILL COUNTING THE CENSUS. Banked-arithmetic
sanity: at s=3/2 the D2c anti-screening drives the composed endpoint ≈127, nine units below
every honest way-point, failing the composed structure's OWN consistency. The Casimir
fraction acts on the DEPTH layer (one depth of self-look-back), not on the raw census.

## CEILING RESOLVED

The predecessor's census-layer ceiling {1, 3/2} is RESOLVED: BOTH are raw-census values
(tempered but still counted); s is the census PROJECTED through the depth layer, landing
STRICTLY BELOW both (1/8 < 1 < 3/2) and below the Casimir fraction itself (1/8 < 3/8 — the
depth weight 1/3 is ONE depth, not the whole stack).

## ONE CAUSE (two independent banked corroborations)

(a) the SAME 3/8 splits the DEPTHS and the CASIMIRS: funCas/adjCas = isoDepth/chargeTraceDepth
    = 2/(16/3) = 3/8 (casimirRatio_eq_depthRatio); only the Dynkin (trace) reading dissents.
(b) the SAME 1/3 dresses every channel and IS the single depth the doublet sees the census
    through — Born self-overlap on both sides.

## NEXT (owner-directed course)

Compose s=1/8 into D2c → clear the ComposedNumeralObstruction (the ~14.6-wide s-spread that
made a ≥15-digit bracket impossible while s was free now COLLAPSES to a point) → proceed to
the join/freeze-out route-finders → re-narrow the complete composed sum → D7 restate AS
1/α(0) at ≥15 significant digits → D8 capstone → hand M1. NO deviation without a new owner block.
