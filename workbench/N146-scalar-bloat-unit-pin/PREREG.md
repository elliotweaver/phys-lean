# N146 PREREG — THE SCALAR-BLOAT OBSTRUCTION + THE UNIT-NORMALIZED PIN GROUP

FROZEN before compute. KILL classifies a fight as INSTRUMENT failure, not theory.

## THE ONE LAW MEASURE-FIRST FINDING (the decisive reframe)

The ticket's priority (i) literal target is `MonoidHom.mker coverMapPin = {1, -1}`
(the exact kernel / injectivity-mod-center of the Pin double cover). N145 banked the
`⊇` direction. MEASURE-FIRST on the `⊆` direction:

  For EVERY anisotropic `a` (`BvC a a ≠ 0`), `cliffUnitOf a ha ∈ PinSub` (a generator),
  so `(cliffUnitOf a ha)^2 ∈ PinSub`. Its VALUE is `ι a * ι a = ι_sq_scalar =
  algebraMap (QvC a)`, a SCALAR unit. And it COVERS `houseHolderM a ∘ houseHolderM a`
  = `id` (`houseHolderM_involutive`, banked), so it lies in `mker coverMapPin`.
  Take `a = (2:Cut) • mTimeUnit`: `QvC a = BvC a a = 4 • BvC mTimeUnit mTimeUnit = 4`,
  so `(cliffUnitOf a)^2 = algebraMap 4`, a Pin element in `mker coverMapPin` that is
  NEITHER `1` NOR `-1` (`algebraMap 4 ≠ ±1`, char-0 injectivity of algebraMap).

  ⟹ `MonoidHom.mker coverMapPin = {1,-1}` is MATHEMATICALLY FALSE on `PinSub`. The
  scalar bloat that killed the `CoversSub`-kernel (N145 reframe) PERSISTS on the
  reflection-generated `PinSub`, because the generators `cliffUnitOf a` are the RAW
  `ι a` (norm `QvC a` arbitrary), not unit-normalized. THE ONE LAW catches the literal
  target as FALSE a SECOND time, one level deeper.

  Returning to the theory: the honest kernel-{±1} domain is the UNIT-NORMALIZED Pin
  group — generated by the `cliffUnitOf a` of UNIT vectors (`QvC a = ±1`, i.e.
  `BvC a a = ±1`), where the reflection square collapses to `algebraMap (±1) = ±1`.
  This is the literature's Pin(p,q) (reflections in UNIT-norm vectors). On this domain
  the scalar bloat is gone and the order-2 element `-1` survives.

## TARGET (N146) — DERIVED, forward, the honest reframe

THE OBSTRUCTION (the headline ONE LAW finding) + THE UNIT-NORMALIZED PIN GROUP:
  1. cliffUnitOf_sq — `((cliffUnitOf a ha)^2 : CliffC) = algebraMap Cut CliffC (QvC a)`
     (generalizes N145's cliffUnitOf_mSpaceUnit_sq; ι_sq_scalar + QvCQuad_apply).
  2. cliffUnitOf_sq_mem_mker — `(⟨(cliffUnitOf a ha)^2, _⟩ : PinSub) ∈ mker coverMapPin`
     (covers id, houseHolderM_involutive + cliffCovers_unique).
  3. ★ coverMapPin_mker_has_nonpm_one — THE OBSTRUCTION: ∃ a Pin element in
     `mker coverMapPin` distinct from BOTH `1` and `-1` (witness `algebraMap 4` via
     `a = (2:Cut)•mTimeUnit`). Hence the literal exact-kernel candidate is FALSE.
  4. pinUnitGenSet / PinUnitSub — the unit-normalized Pin group (generators of UNIT
     vectors `BvC a a = 1 ∨ BvC a a = -1`).
  5. pinUnitSub_le_pinSub — `PinUnitSub ≤ PinSub` (unit generators are Pin generators).
  6. cliffUnitOf_unit_sq — for a unit generator, `(cliffUnitOf a)^2 = ±1` (bloat
     COLLAPSES: algebraMap (QvC a) = algebraMap (±1) = ±1).
  7. negOne_mem_pinUnitSub — `-1 ∈ PinUnitSub` (square of the UNIT spacelike reflection
     survives in the normalized group).
  8. coverMapPinUnit : PinUnitSub →* bvIsomLinMonoid (restriction of coverMap).
  9. negOne_mem_mker_coverMapPinUnit + coverMapPinUnit_mker_nontrivial — the order-2
     kernel element survives in the unit-normalized group.

## GO / NO-GO / KILL

GO (all must hold):
  - All core decls 1–9 compile, foundations-only `{propext, Classical.choice, Quot.sound}`.
  - Obstruction (3) genuinely exhibits a kernel element ≠ 1 and ≠ -1.
  - Bloat-collapse (6) holds for unit generators; -1 survives in PinUnitSub (7).
  - Costume C178 (`1 = 146`) bites a WRONG scalar-bloat / unit-Pin / kernel claim;
    false numeric distinct from C115 1=7 … C177 1=145.

NO-GO (reframe / block, do NOT weaken):
  - If `cliffUnitOf_sq` resists (would be an instrument fight on ι_sq_scalar — not
    expected; pure algebra).
  - If `algebraMap 4 ≠ ±1` resists (char-0 injectivity — banked machinery).

KILL (W9 instrument budget):
  - 90s per single obligation. Everything here is pure scalar/closure algebra (no tsum,
    no exponential, no brute coordinate normalizer) — expected instant. If any single
    obligation exceeds 90s compile, declare INSTRUMENT failure → measure/decompose, do
    NOT inflate maxHeartbeats.

## CHILD (N147) — the W1 dissolution the arc now points at
  The EXACT kernel `mker coverMapPinUnit = {1,-1}` (the ⊆ injectivity-mod-center on the
  UNIT-normalized domain, where the bloat is gone): every unit-Pin element covering id
  is ±1 — the centralizer-forces-scalar argument (involute(u)·ι v = ι v·u over the
  faithful spinor embedding cliff_iota_injective + the even/odd grading). The genuine
  two-to-one content; a candidate W1 node. + the surjectivity of coverMapPinUnit (needs
  a general unit-normalization, sqrt over the derived Cut). Child onto the chain tail.

## STANDARD pre-commit
  UNBROKEN: PinUnitSub = Submonoid.closure of the cascade's own UNIT reflection units
  cliffUnitOf; the obstruction is a DERIVED reflection square; coverMapPinUnit = coverMap
  restricted; -1 a DERIVED reflection square; no posited Pin/cover/kernel, no Mathlib ℝ/ℂ
  as content, no bridge.
  COMPLETE: foundations-only, audited vs built olean, no sorry/axiom/native-kernel-bypass.
  WORDS-REMOVABLE: delete Pin/cover/reflection/kernel/double/SO⁺(1,9)/unit-normalized →
  pure algebra over the derived field Cut, STVC, CliffC, ι, CliffCovers.
