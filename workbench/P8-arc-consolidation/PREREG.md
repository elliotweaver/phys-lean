# PREREG — N### arc-P P8: THE ARC-P CONSOLIDATION + THE ARC-Q HANDOFF

## TARGET (selected via SELECT protocol, comments 390/391)
The FINAL node of arc P (the reckoning). Exhibit arc P as ONE structure over the fold's TWO
self-blindness measures — the unresolved-direction COUNT (`finrank ℚ Uhol = 3`) and the fold's
RETURN TURN (`azimuthalTurn = 2·cutPi`) — with GENUINE never-co-stated cross-node ties (NOT a bare
∧ of the six capstones; HOLLOW-AND ban). Then hand ONE directed successor toward SEED_OWED_FACES §Q1.

## CROSS-TIES (measured green in probe.lean, EXIT 0) — each NEW, extending P7 not repeating it
- §1 SELF-BLIND COUNT thread (P3⊗P4⊗P6): the SAME `finrank ℚ Uhol` reads the anomaly cancellation
  (P3 `su2_mixed_anomaly_cancels`), the Weinberg normalization (P4 `weinberg_normalization_forced`),
  AND the triality cycle order (P6 `trialityHom_gcyc_order3` at `finrank_Uhol`). P7 WELD 2 tied only
  P3⊗P6 abstractly, no P4; §1 adds P4.
  MULTI-NODE TEETH (new): the SAME wrong count 2 breaks BOTH P3 (`su2_mixed_anomaly_wrong_colour`)
  AND P6 (`trialityHom_gcyc_sq_ne_one`, a NEW lemma proved in-module: `trialityHom gcyc ^ 2 ≠ 1`).
- §2 FOLD-TURN thread (P1⊗P2): the absolute mass scale is exhibited EXPLICITLY routing through the
  fold turn: `massScale = cutExp(−(azimuthalTurn·(1/α*)/comarkCount))·((cutCos foldPhase·s1BornProb)·1·cutCos(2/3))`.
  P7 WELD 1 stopped at the opaque `minimalWrapRatio`; §2 unfolds azimuthalTurn inside it (via
  `minimalWrap_exponent`).
- §3 TWO DISTINCT DERIVED DIMENSIONS (honest guard): `finrank ℚ spaceSub (=9) ≠ finrank ℚ Uhol (=3)`
  — the reckoning uses a small STRUCTURED set of separately-derived integers, not one magic number.
- CAPSTONE `arcP_consolidation` bundling §1+§2+§3.
- P5 (gravity/the ½) is the structurally-distinct sector NAMED in mechanism-grade closure prose,
  NOT force-conjoined (N425 O6 pattern).

## NEW IN-MODULE LEMMA
`trialityHom_gcyc_sq_ne_one : trialityHom gcyc ^ 2 ≠ 1` (via trialityHom_injective + `decide` on `gcyc^2≠1`).

## GRADE
THEOREM-route for the STRUCTURE (§1/§2/§3 all full theorems on banked derived Cut / ℚ-arena).
MECHANISM-grade flagged prose for "arc P is the reckoning; the SM inputs are readings of two
self-blindness measures; zero free parameters" — never load-bearing.

## GUARDS
G1 no asserted values; G2 no empirical number (v=246, masses, sin²θ_W=0.231 = removable prose);
G3 derived; G4 words-removable (rational functionals at a shared finrank, a cutExp factoring, two
unequal finranks); G5 foundations-only ⊆ {propext,Classical.choice,Quot.sound}, no proof-hole /
native-decide / heartbeat-raise. ℝ-vigilance: banked Cut + ℚ-arena, no Mathlib-ℝ/ℂ as content.
FREE-FLOATING: every TYPE mentions banked objects (finrank Uhol/spaceSub, azimuthalTurn, massScale,
comarkCount, trialityHom/gcyc, weinberg, su2MixedCoeff). NOT hollow-AND: §1 adds P4 + multi-node teeth,
§2 exhibits azimuthalTurn inside massScale, §3 states count distinctness — all genuinely never co-stated.
W8 non-vacuity: next Cid = C459. W4.5: derive exactly P8, not arc-Q content.

## MODULE
Phys/Algebra/ArcPConsolidation.lean (ns Phys.Algebra.ArcPConsolidation).
Costume: Counterexamples/ArcPConsolidationVacuityCostume.lean, C459 bites 1=459.
Wiring: Phys.lean + Audits/AxiomAudit.lean (+imports & #print axioms) + manifest + ledger, ONE commit.

## SUCCESSOR (ONE, directed): arc-Q Q1 (parity violation) per SEED_OWED_FACES §Q1.
