# OWNER-AUTHORIZED SEED — ARC K: QFT DYNAMICS / THE S-MATRIX

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every value is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc J,
precedes arc L.*

## 0. DESTINATION — what this arc is for

Arcs A–J derived the STATIC content of quantum field theory (the gauge groups, the constants, the SM
structure, the Born rule) and the emergent-statistical layer. Arc K derives the DYNAMICAL half: how
particles interact and scatter — the S-matrix, unitarity, cross-sections, the optical theorem. This is
"what a detector actually sees." It is genuinely harder than the static arcs — parts of it live at the
frontier of the answer key itself (the Clay Yang-Mills constructive-QFT statement) — so this arc carries a
MIXED grade and heavier honest flags. Derive what derives; flag the frontier honestly; NEVER fake a
constructive-QFT closure the key itself did not reach.

## 1. THE ROUTE (theory-native — the joints, as a MAP not a proof)

- **Unitarity = monad idempotency.** The banked sheafification monad `T` acts as the IDENTITY on physical
  states (physical states ARE sheaves, `T² ≅ T`), so probability is conserved under evolution — unitarity is
  the idempotency of the gather, not a separately-imposed postulate.
- **Asymptotic states = colour singlets.** From arc I, the only sheafifiable (physical, asymptotic) states
  are colour singlets — so the S-matrix is defined between the banked hadron/singlet states, not free
  quarks.
- **The optical theorem / cross-sections = the Born-square of the derived transition amplitude** (arc J's
  derived Born rule), summed over the sector category (arc G). The amplitude is the derived look-back
  transport between asymptotic sheaf states.

## 2. THE TARGETS (in order — each a separate node, each a real theorem)

- **K1 — unitarity from monad idempotency (theorem-route; the anchor).** DERIVE that physical evolution is
  unitary because physical states are sheaves and the gather monad acts as identity on them (`T² ≅ T`,
  banked). **DERIVE unitarity from idempotency; do NOT posit a unitary S-matrix.** Grade: theorem-route.

- **K2 — the asymptotic-state space (theorem).** DERIVE that the S-matrix's in/out states are exactly the
  banked colour singlets (arc I) dressed by the derived Born structure (arc J). **DERIVE the asymptotic
  completeness structure over the singlet sheaves.** Grade: theorem-route.

- **K3 — the transition amplitude + the optical theorem (theorem-route).** DERIVE the scattering amplitude
  as the derived look-back transport between asymptotic states, and the optical theorem (unitarity relating
  the forward amplitude's imaginary part to the total cross-section) as a consequence of K1's unitarity.
  **DERIVE the amplitude structure + the optical theorem; cross-section NUMBERS are candidate, never fit.**
  Grade: theorem-route for the structure; numbers candidate.

- **K4 — the constructive-QFT frontier (FLAG, mostly route-not-yet-found).** The full constructive S-matrix
  (Wightman axioms, Haag–Ruelle, the rigorous interacting-QFT existence) is the CLAY Yang-Mills frontier —
  OWNER-HELD. DERIVE the framework's STRUCTURAL statement (unitary S-matrix on singlet sheaves with a mass
  gap from arc I) and FLAG the full constructive statement as route-not-yet-found / owner-steered. **Do NOT
  attempt the Clay constructive proof; do NOT fake a closure.** Grade: mechanism/structural; Clay flagged
  out.

- **K5 — consolidation + the arc-L handoff.** Bundle K1–K4: QFT dynamics = unitary (monad-idempotent)
  evolution of colour-singlet sheaf states with Born-square amplitudes, mass-gapped by arc I. Hand ONE
  directed successor toward `SEED_MATTER_LADDER.md` target L1.

## 3. THE LOAD-BEARING JOINT (DERIVE or CHAIN-RUNWAY-block)

- **J-K1 (the unitarity joint):** "physical states are sheaves ⇒ the gather monad is identity on them ⇒
  evolution is unitary." DERIVE from the banked `T² ≅ T` idempotency. Do NOT posit unitarity. GO IN WITH NO
  FEAR; the block valve is a LAST RESORT against ASSERTING, after the full W1 attack. ⚠ K4 is EXPECTED to be
  partly route-not-yet-found (the Clay frontier) — that is HONEST STATUS, not a block; flag it and move on,
  do NOT grind the constructive proof.

## 4. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** Unitarity, the amplitude structure, the optical theorem, cross-sections —
  every one a DERIVED landing or an honestly-flagged frontier. Never a premise.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured cross-sections, widths, the Clay bound — REMOVABLE
  PROSE only. NO-FIT guard: K3/K4 numbers are candidate/frontier — do NOT chase residuals; that is fitting.
- **G3 — DERIVED, NOT POSITED.** Unitarity from idempotency (not "assume unitary S-matrix"); asymptotic
  states from confinement (not "assume free-particle in/out states").
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "scattering", "S-matrix", "cross-section" — pure statements about
  an idempotent monad acting as identity, transport between sheaf states, and the square of a derived inner
  product.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`. ⚠️ ℝ-VIGILANCE: banked derived `Cut` + banked monad/sheaf/Born objects — NEVER
  `import Mathlib...Real` / a posited QFT axiom as content.
- **G6 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe, build,
  decompose, switch tactics. Block ONLY as a last resort against asserting — never early. K4's frontier is
  route-not-yet-found (honest status), never a fake closure.

## 5. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Every value is earned through the identical gate.
Walk every step. When arc K completes, hand ONE directed successor toward `SEED_MATTER_LADDER.md` target L1.
The Clay constructive-QFT frontier (K4) stays OWNER-HELD — flag it, never attempt it.
