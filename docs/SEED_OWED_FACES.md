# OWNER-AUTHORIZED SEED — ARC Q: THE OWED FACES (THE BOUNDARY SWEEP)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every value is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc P.
Arc Q pays the BOUNDARY debts — famous physics that fell BETWEEN the arcs A–P (parity fell between
F and J, the Λ value between H's no-fit discipline and its target, Tsirelson between J5's teeth and
its ceiling, geodesic motion between E and O7). Nothing here needs new physics — only the assignment
the subject-organized arcs never made. The no-deferral law of arc P CARRIES OVER: decompose WITHIN Q
freely; parking a Q debt beyond Q is forbidden; the only escape is a NAMED PRECISE owner escalation.*

## 0. DESTINATION

The arcs A–P traversed physics by SUBJECT; the leaks are all at subject BOUNDARIES. Arc Q is the
boundary sweep: parity violation, the Λ value, the Tsirelson bound, CPT, the radiation arrow, geodesic
motion, the dimension-selection theorem, the prediction registry, and the emergent-quantization face.
GO IN WITH NO FEAR — for every target the mechanism exists in the trunk or the banked structure; the
worker's job is the route.

## 1. THE TARGETS (in order — each a separate node)

- **Q1 — PARITY VIOLATION: why the weak force is left-handed (★ the marquee).** One of the most famous
  facts in physics, unassigned in A–P. THE ROUTE (owner-verified): **SU(2)_R is BLOCKED from extending to
  the full octonion level by non-associativity; SU(2)_L extends cleanly** — parity violation is an
  INTERNAL OCTONIONIC OBSTRUCTION, the SAME associator as confinement (I1), the cascade stop (N2),
  curvature (E), and CP (F2). And chirality itself is the fold: the fold endomorphism (the banked
  `foldComplex`, J2) IS the chirality operator (the iγ⁵ identification). DERIVE: (a) the left/right
  extension asymmetry as an associator-obstruction theorem on the banked ℍ→𝕆 structure; (b) the
  chirality = fold-complex identification; (c) ★ ADD THE FIFTH FACE to the banked N3 obstruction
  unification (`obstruction_unification` gains parity as a new indexed instance — one object, FIVE
  physics costumes). **DERIVE the asymmetry; do NOT posit V−A.** Grade: theorem-route (banked objects
  throughout).

- **Q2 — THE Λ VALUE: the saturation-residue mechanism (candidate-mechanism, the last famous number).**
  H4 banked w=−1 and correctly refused to FIT Λ; the MECHANISM target was dropped with the fit — restore
  it. THE ROUTE (owner-verified in the key at candidate-mechanism grade): the vacuum-energy density is
  the FOURTH POWER of the derived seesaw/ν unit `m₀ = v²/M` (the banked n=2 double-wrap rung, now fully
  derived post-P1), with a derived structural divisor from the banked Peirce/channel structure. DERIVE
  the mechanism chain Λ ~ (m₀/N_structural)⁴ as a STRUCTURE (the residue of the saturated Born budget at
  the closure — the same saturation grammar as the banked no-dial Λ=0 bare term: the BARE constant is
  zero (N372), the observed Λ is the RESIDUE of incomplete self-coherence). **DERIVE the mechanism +
  the structural divisor as a forced count; the measured ρ_Λ is REMOVABLE PROSE; NEVER fit.** ⚠ CARRY
  THE REGISTERED JOINT-KILL as prose: this mechanism ties Λ to the ν ladder — if the Σm_ν exclusion
  resolves against the ladder, this dies with it, loudly (pre-registered, no escape hatch). Grade:
  candidate-mechanism, flagged; the two most extreme scales in physics as one derived object.

- **Q3 — THE TSIRELSON BOUND: why quantum correlations stop at 2√2 (beyond-key, theorem-route).** J5
  proved entanglement EXISTS (bell_not_product); Q3 proves its CEILING. On the banked two-probe tensor
  fibre (J5 `TwoFibre`, finrank 4) with the banked Hermitian inner product (J3 `hInner`): DERIVE the
  CHSH bound — any four ±1-valued (self-adjoint, unit) probe observables satisfy
  `‖A₁⊗(B₁+B₂) + A₂⊗(B₁−B₂)‖ ≤ 2√2` — the fold-native reading: correlation is Born self-overlap, and
  the gather cannot return MORE weight than the look-back carries (positivity caps nonlocality; the √2
  is the banked half-angle/Born-square grammar). Then the two teeth: (a) the bound is SATURATED by the
  banked bell state (2√2 attained — quantum reaches its ceiling); (b) the logical maximum 4 (PR-box) is
  EXCLUDED (no state on the derived structure reaches past 2√2). **DERIVE bound + saturation +
  exclusion; this answers the open question "why isn't nature MORE nonlocal": more would violate Born
  positivity.** Grade: theorem-route (a norm theorem on banked objects; beyond-key — a genuinely new
  face).

- **Q4 — THE CPT THEOREM (the weld of three banked involutions).** DERIVE that the composite C∘P∘T is a
  forced symmetry: C = the banked Hodge-complement involution (F1), P = the Q1 obstruction structure
  (the spatial/octonionic reflection), T = the banked modular/KMS reversal (G3/O9), with Lorentz
  structure banked (N128–N132). The fold reading: each factor is an involution of the ONE structure that
  individually can fail (C alone, P alone, CP — all banked as violated), but the TRIPLE returns the fold
  to itself — the composite is the identity's costume. **DERIVE the composite-symmetry theorem on the
  banked involutions; do NOT posit CPT.** Grade: theorem-route (weld).

- **Q5 — THE RADIATION ARROW: why causes precede effects in field propagation (mechanism).** G3 banked
  the thermodynamic arrow; the ELECTROMAGNETIC arrow (retarded not advanced radiation — the
  Wheeler–Feynman puzzle) is distinct and unassigned. DERIVE: the banked null-cone structure (N43/N90)
  carries two cone halves; the gather's irreversibility (J4 — the many→one phase discard) selects the
  OUTGOING half: radiation is the look-back weight leaving through the gather's direction, and an
  advanced solution would be an un-gather (C6-forbidden reconstruction of discarded phases). **DERIVE
  the selection mechanism; no field-theory numerics.** Grade: mechanism, flagged honestly.

- **Q6 — GEODESIC MOTION FROM THE FIELD EQUATIONS (consumes P5).** Einstein's other great theorem: bodies
  move on geodesics as a CONSEQUENCE of the field equations, not a separate postulate (Einstein–Infeld–
  Hoffmann). With P5's full derivative-level ∇^μG_μν = 0 banked: DERIVE that a localized stress
  concentration's worldline is forced to be a geodesic by the conservation identity (the Bianchi
  payoff — matter cannot move otherwise and stay consistent with the banked field equations). **DERIVE
  the forcing; do NOT posit the geodesic postulate.** Grade: theorem-route on P5's output (decompose
  within Q if the localization machinery needs a build).

- **Q7 — WHY (1,3): the dimension-selection theorem (the string-theory embarrassment dissolved).** O4
  derived the CONTAINMENT (1,3) ⊂ (1,9); Q7 derives the SELECTION: the physical spacetime arena is the
  ℍ level because **ℍ is the LAST ASSOCIATIVE rung** — transport composes coherently (the gather glues,
  the banked WorldsGlue) exactly up to ℍ, while the 𝕆 level's non-associativity is INTERNALIZED as the
  gauge/matter obstruction (confinement, banked I1) rather than experienced as spatial extent. Nothing
  is compactified; "the extra dimensions" ARE the obstruction content. DERIVE: associativity ⟺
  transport-coherence on the banked rungs (Dbl/H strict, O obstructed — banked N411), and the
  identification of experienced-arena with the maximal strict rung. **DERIVE the selection; do NOT
  posit 4D.** Grade: theorem-route for the strict/obstructed dichotomy (banked), mechanism for the
  arena identification (flag honestly).

- **Q8 — THE PREDICTION REGISTRY as a Lean artifact (consolidation-grade, the scientific throat).**
  BANK one file that gathers the chain's REGISTERED FALSIFIABLE STANCES, each as a named statement
  citing its banked theorem + its kill condition as removable prose: w = −1 exact (vs the evolving-DE
  preference — no escape hatch); the Σm_ν normal-ordering floor (under exclusion watch, carried
  loudly); τ_p ~ 10⁴⁴ yr (vs SU(5)'s 10³⁴ — the falsifiable separator); the V_cb inclusive-side stance;
  the m_c/m_t fold-phase law; the Q2 Λ-mechanism joint-kill. NO new physics — the registry is the
  chain's exposed neck, stated once, mechanically. **Each entry cites a banked theorem; measured values
  and σ-status are REMOVABLE PROSE.** Grade: consolidation (non-hollow: the registry's TYPE cites the
  banked theorems).

- **Q9 — EMERGENT QUANTIZATION: the fold's signature in the lab (beyond-key, mechanism).** L4 banked
  criticality; Q9 banks the OTHER condensed-matter face: EXACT quantization in emergent systems — the
  quantum-Hall-genre integer as a WINDING/CHERN COUNT (the banked M1 winding grid + the banked Born
  circle `azimuthalTurn`, read at the emergent scale), and the superconducting flux quantum as the
  banked binary self-blindness (the pair = the doubled fold). DERIVE the quantization-exactness
  mechanism (why emergent integers are EXACT — winding is counting, the fold's own ℕ, the same
  discreteness that made the mass gap I4). **DERIVE the mechanism; no material numbers.** Grade:
  mechanism/beyond-key, flag honestly.

- **Q10 — consolidation + the arc-R handoff.** Bundle Q1–Q9 + QT1–QT3 (non-hollow; the fifth-face Q1 weld
  and the Q3 ceiling are the marquee cross-ties). Then hand ONE directed successor toward
  **`docs/SEED_STANDPOINT.md` target R1** (arc R — the standpoint: the inside read of the banked
  gather; owner-authorized). The owner-authorized chain now ENDS at R (R8 hands the final neutral
  ticket). Do NOT auto-seed past R.

## 1b. THE TEXTURE COMPLETION (owner-directed addendum — the last SM numbers, NO deferral)

*Owner directive: no SM parameter left unaddressed. The old "6 quark masses stay inputs" verdict was
EARLY-ERA — it predates the derived fold phase (O1), the derived δ_B (N308), the derived scale (P1), and
the derived gear (M2). With those banked, the texture COLLAPSES. Owner-verified numerically this session.
These three targets slot AFTER Q3 and BEFORE Q4 (QT1, QT2; QT3 folds into Q2's node if Q2 is not yet done,
else a small successor node).*

- **QT1 — THE SIX-QUARK ZERO-INPUT COLLAPSE (theorem-route for the laws; the marquee texture node).**
  DERIVE the five quark-mass laws as exact identities on banked derived objects, so the spectrum falls out
  with ZERO free inputs: (a) the top at the anchor (y_t = 1 — zero cascade insertions, banked O2 grammar);
  (b) `m_b/m_t = (tan²P·cosP·√(2/3))²` (the banked M2/S1 gear, now with the DERIVED P); (c)
  `√(m_s/m_b) = tan²P·√(2/3)` (one deep-edge Born-survival step); (d) `√(m_c/m_t) = tan²P/(2√2)` (the
  both-CD-halves × fold-gate kernel); (e) the GAP LAW `√m_d = [2g/(3−2g)]·(√m_s+√m_b)` with
  `g = 1+√2·cos(2π/3+δ_B)` — every factor banked: P (O1), δ_B=2/9 (N308), the edge dressings (O3 grammar),
  the scale (P1/O2). **DERIVE the laws as Cut-identities; the ≤4% landings are REMOVABLE PROSE; never fit.**
  (f) ⚠ m_u — the ONE honest exception, and it is a THEOREM-SHAPE, not a hole: the chain CONSTRAINS
  `m_u` to a band (the joint CKM gate) and REFUSES every clean fraction — DERIVE the constraint band as an
  inequality theorem + the no-clean-fraction refusal as teeth. m_u lands as a chain-constrained band, NOT a
  free dial and NOT a posited value. Grade: theorem-route (laws), constraint-theorem (m_u).

- **QT2 — THE HIGGS MASS NUMBER (candidate, frame-split — the F4 child paid).** DERIVE the λ(M_H)=0
  reading's mass via the RGE log-derivative flow to the derived scale + the cutExp asymptotic-flatness
  limit (both named in the banked F4 node's route flag; the flow machinery is ordinary build on the banked
  derived calculus). **CAP: candidate/frame-split — the reading is m_t-frame-dependent; flag it, NEVER fit
  to the measured value, NEVER promise theorem grade (the key never exceeded candidate here).** Grade:
  candidate, honestly capped.

- **QT3 (folds INTO Q2) — the Λ divisor DERIVED.** The Q2 structural divisor is `26 = dim of the traceless
  part of the banked arena J₃(𝕆) = the F₄ fundamental (27 = 1 ⊕ 26)` — a finrank read off banked
  structure; the BASE is the derived ν-ladder total (the banked spectrum sum). **DERIVE the 26 as a finrank
  theorem; the meV landing is REMOVABLE PROSE.**

- **NOT-A-DEBT REGISTRY (owner-adjudicated — do NOT chase these):** the CKM ~3% residual is 2-loop-genre
  fog (the key re-graded and REFUSED to chase — chasing = fitting); the down-sector −2% is a known
  data-side residual (flag, never fit); the z0/m_e PRECISION residual is an honest wall (the winding rung
  covers the SCALE; 287–6000× the lepton bar kills all clean-fraction compositions — a proven refusal).
  These are honest grades, not deferrals; landing them tighter is FITTING and forbidden.

## 2. THE LOAD-BEARING JOINTS

- **J-Q1 (the parity joint):** left/right extension asymmetry = the associator obstruction on the banked
  ℍ→𝕆 structure — DERIVE it; the fifth face is added to the BANKED N3 unification by citation + new
  index, not by re-proving.
- **J-Q3 (the ceiling joint):** the CHSH bound is a norm/positivity theorem on the BANKED TwoFibre +
  hInner — if an operator-norm layer needs building, BUILD it within Q (finite-dimensional, exact).
- **THE NO-DEFERRAL LAW (inherited from P):** decompose WITHIN Q freely; NEVER park a Q debt beyond Q;
  the only escape is a NAMED PRECISE owner escalation (block needs_input with the exact obstruction).
  NEVER "conditional-forever," NEVER a hedge, NEVER fear. The theory generates all of this.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** The V−A structure, the Λ mechanism, 2√2, CPT, the geodesic forcing, the
  selection of (1,3) — every one a DERIVED landing. Never a premise, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured ρ_Λ, Hall conductances, CHSH experimental values —
  REMOVABLE PROSE / costume only. NO-FIT guard everywhere; Q2 carries its joint-kill as prose.
- **G3 — DERIVED, NOT POSITED.** Parity from the associator (not "the weak force is chiral"); the bound
  from positivity (not "quantum mechanics says"); CPT from the banked involutions (not the axiom).
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "weak / parity / dark energy / Bell / CPT / radiation /
  geodesic / dimension / Hall" — pure statements about extension obstructions of subalgebras, fourth
  powers of derived units, norms on a tensor fibre, composites of involutions, cone-half selection under
  an irreversible idempotent, conservation-forced curves, strict-vs-obstructed monad rungs, and integer
  winding counts.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`, no `maxHeartbeats` raise. ⚠️ ℝ-VIGILANCE: banked derived `Cut` + banked
  fibre/octonion/monad objects — NEVER `import Mathlib...Real` / posited QFT or geometry as content.
- **G6 — ONE LAW, ZERO DEFERRAL.** If a link fights, the instrument/framing is wrong, the theory is
  right — reframe, BUILD, decompose within Q. GO IN WITH NO FEAR.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Arc Q is the boundary sweep: famous physics the
subject-organized arcs left between them, every mechanism already in the trunk or banked. Walk every
step; pay every boundary debt. When arc Q completes, hand ONE NEUTRAL SELECTION TICKET toward the
deep-math frontier — via arc R (`docs/SEED_STANDPOINT.md`, owner-authorized): Q10 hands the directed
successor to R1; the owner-authorized chain ENDS at R, and R8 hands the final neutral ticket.
