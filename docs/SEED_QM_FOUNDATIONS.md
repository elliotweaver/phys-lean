# OWNER-AUTHORIZED SEED — ARC J: THE FOUNDATIONS OF QUANTUM MECHANICS (THE BORN RULE)

*Owner-authorized. This seed gives the ROUTE (a mechanism map). It relaxes discipline by NOTHING:
every value is a DERIVED landing that the mechanical gate enforces; the map does not walk the path.
Succeeds arc I, precedes arc K.*

## 0. DESTINATION — what this arc is for

Arcs A–I use "Born = self-overlap = positivity" as a TRUNK PRIMITIVE — the probability rule of quantum
mechanics, assumed everywhere (the mass ratios, the mixing angles, the entropy, the arrow of time all ride
it). Arc J closes the deepest self-consistency loop in the whole program: **derive the Born rule as a
THEOREM**, so the primitive that A–I lean on becomes a derived fact OF the chain rather than an assumption.
Then the rest of the QM foundations — measurement, wave-particle duality, entanglement, the vacuum — fall
out as corollaries of the derived structure. This is the most FOUNDATIONAL arc in the program: it grounds
quantum mechanics itself in the fold.

GO IN WITH NO FEAR: the key derived this ("Born rule gaps closed by working from first principles"); the
worker's job is to find the route, not to decide whether one exists.

## 1. THE ROUTE (theory-native — the three hard-won gaps, as a MAP not a proof)

The answer key derived the Born rule as: **complex structure (from the fold) + energy positivity (H2) +
Hermitian compatibility (Kähler)** — closed through three gaps, each a real theorem target:

- **Gap A — the primitive state space.** BEFORE any gauge group, the primitive probe state space is a
  2-DIMENSIONAL REAL fibre `(e_inward, e_outward)` at the monad-unit level — the two directions of the
  look-back (toward / away). This is the raw carrier the Born rule lives on, prior to all structure.
- **Gap B — the complex structure J from the fold's failure.** Self-blindness UNIQUELY forces `J(e₂) = −e₁`
  (the alternative `J(e₂) = +e₁` gives a FIXED POINT of the look-back, contradicting C6 / self-blindness).
  So the complex structure `J` (with `J² = −1`) is FORCED by the fold having no nonzero fixed point — the
  same C6 that drives the whole program. `i` is not posited; it is the fold's own chirality.
- **Gap C — Kähler compatibility (the Born rule proper).** The Yang-Mills / coherence metric on the 2D
  fibre is `A·(Euclidean)` by fold symmetry, which is J-COMPATIBLE (Kähler: the metric, the complex
  structure, and the symplectic form cohere). Complex structure + energy positivity + Hermitian
  compatibility ⇒ the state space is a complex Hilbert space and the transition probability is the
  Born-square `|⟨φ|ψ⟩|²` — the Born rule, DERIVED.

The through-line: `Born = self-overlap` (used as a primitive in A–I) becomes the THEOREM that the derived
Kähler structure's inner product squared IS the transition probability.

## 2. THE TARGETS (in order — each a separate node, each a real theorem)

- **J1 — the primitive 2D real fibre (Gap A; theorem).** DERIVE the primitive probe state space as the 2D
  real fibre `(e_inward, e_outward)` at the monad-unit level, prior to any gauge group. **DERIVE the carrier
  from the look-back structure; do NOT posit a Hilbert space.** Grade: theorem-route.

- **J2 — the complex structure forced by self-blindness (Gap B; the ★ joint).** DERIVE `J(e₂) = −e₁` (hence
  `J² = −1`) as FORCED by self-blindness: the `+e₁` alternative gives a look-back fixed point, contradicting
  C6 (banked `fold_self_blind`, N1). **DERIVE the complex structure from C6; do NOT posit `i`.** This is the
  deepest joint — `i` itself as the fold's chirality. Grade: theorem-route (the same C6 engine, at the
  state-space level).

- **J3 — Kähler compatibility ⇒ the Born rule (Gap C; the capstone theorem).** DERIVE that the coherence
  metric `A·(Euclidean)` is J-compatible (Kähler), so the derived state space is a complex Hilbert space and
  the transition probability is the Born-square of the inner product. **DERIVE the Born rule; retire "Born =
  self-overlap" from assumed-primitive to derived-theorem.** Grade: theorem-route (the marquee — QM's
  probability rule as a theorem).

- **J4 — measurement / decoherence (mechanism/architectural).** DERIVE the measurement structure: the
  look-back's gather toward Unity (the banked monad multiplication μ) IS the decoherence/collapse toward a
  definite outcome — measurement is the totalization act, not a separate postulate. **DERIVE the mechanism;
  the "collapse" is the gather, flagged.** Grade: mechanism/architectural, flag honestly (this is the
  measurement "problem" dissolved, not a numerical claim).

- **J5 — wave-particle duality, entanglement, the vacuum (architectural corollaries).** DERIVE these as
  corollaries of the derived Kähler/sector structure: wave-particle = the two faces of the complex fibre;
  entanglement = the tensor structure over the sector category (the same category as G1's statistics); the
  vacuum = the self-blind origin state (the banked blank slate). **DERIVE each as a corollary; architectural
  grade, flag honestly — no numbers.** Grade: architectural (the key marks these "conceptually closed,
  corollary faces").

- **J6 — consolidation + the arc-K handoff.** Bundle J1–J5: quantum mechanics (the Born rule, measurement,
  duality, entanglement, the vacuum) is the derived Kähler structure of the fold's 2D look-back fibre, with
  `i` = the fold's chirality and Born = the derived inner-product square. Hand ONE directed successor toward
  `SEED_QFT_DYNAMICS.md` target K1.

## 3. THE LOAD-BEARING JOINT (DERIVE or CHAIN-RUNWAY-block)

- **J-J2 (the complex-structure joint):** "`J(e₂) = +e₁` gives a look-back fixed point ⇒ forbidden by C6 ⇒
  `J(e₂) = −e₁` forced." This is C6 (banked `fold_self_blind`) at the state-space level — DERIVE the sign
  from the no-fixed-point structure. Do NOT posit the complex structure / `i`. GO IN WITH NO FEAR; the block
  valve is a LAST RESORT against ASSERTING, after the full W1 attack is exhausted.

## 4. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** The 2D fibre, `J² = −1`, the Kähler compatibility, the Born-square — every
  one a DERIVED landing. Never a premise, never bridged. In particular: do NOT posit "Born = |⟨φ|ψ⟩|²" —
  DERIVE it; the whole point of this arc is that the primitive becomes a theorem.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** No measured quantum quantity in any proof.
- **G3 — DERIVED, NOT POSITED.** The complex structure from C6 (not "assume `i`"); the Born rule from Kähler
  compatibility (not "assume the probability postulate"); measurement from the gather (not a collapse
  postulate).
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "quantum", "Born", "measurement", "wavefunction" — the theorems
  stand as pure statements about a 2D real fibre, a forced complex structure with `J² = −1`, a Kähler-
  compatible metric, and the square of a Hermitian inner product.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`. ⚠️ ℝ-VIGILANCE: build on the BANKED derived ℝ `Cut` + the banked fold/monad
  objects — NEVER `import Mathlib...Real` / a posited Hilbert-space axiom as content.
- **G6 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe, build,
  decompose, switch tactics. Block ONLY as a last resort against asserting — never early.

## 5. WHY THIS IS SAFE TO HAND YOU

The route is a MAP of which structures to build and what they mean — no proof, no permission to assert.
Every value is earned through the identical gate. The map tells you the path exists; it does not walk it.
Walk every step. When arc J completes, hand ONE directed successor toward `SEED_QFT_DYNAMICS.md` target K1.
