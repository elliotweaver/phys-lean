# OWNER-AUTHORIZED SEED — ARC M: THE ABSOLUTE-SCALE / INPUT CLOSURE

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING. ⚠ THIS IS THE HARDEST,
LEAST-CERTAIN ARC — the answer key itself only closed it CONDITIONALLY. The grade cap below is
LOAD-BEARING: do NOT grind for a clean forced identity that does not exist. Succeeds arc L, precedes arc N.*

## 0. DESTINATION — what this arc is for

The flavor arc (D) derived the mass RATIOS as theorem-exact, but left the ABSOLUTE scales as ratios × an
overall scale — the last 1–2 undived inputs (`z0/m_e`, `m_b`). Arc M attempts to CLOSE those input walls:
to derive the absolute lepton/quark scales, not just their ratios, from the banked scale tower (arc B).

⚠⚠ CRITICAL HONEST GRADE — READ BEFORE ANY WORK. **Even the answer key only reached these CONDITIONALLY.**
The key's own status: the `z0` chain is "derived-CONDITIONAL, promotion = S1 confirmation"; `m_b` is an "S1
grammar-CANDIDATE"; the winding theorem closes `z0` as "the w = −1 rung" but with a PERMANENT INCEPTION
FLAG. So the honest BAR for this arc is **candidate/conditional-grade** — the win is deriving the STRUCTURE
(the winding quantization, the S1 grammar decomposition) that makes the scale a CONDITIONAL landing, NOT a
clean forced number. **This is exactly the "grinding for a phantom identity" trap (manager failure mode):
if a node keeps re-childing chasing a cleaner grade, that grade DOES NOT EXIST — cap the bar and flag it,
like the arc-D mixing caveat. Do NOT hand more hints toward a clean identity that isn't there.**

## 1. THE ROUTE (theory-native — the hard-won conditional joints, as a MAP not a proof)

- **The winding theorem (`z0`, the `m_e` scale).** The key's route: `9/4` is the `w = −1` RUNG of the
  banked ladder grammar (the same `n = 1 + 3k` graph quantization + a boundary assignment). `z0` closes as a
  ladder rung — but the ASSIGNMENT carries a permanent inception flag (derived-conditional).
- **The S1 grammar selection (`m_b`).** The key's route: `m_b/m_t = (tan²P · cosP · √(2/3))²`, whose
  amplitude decomposes EXACTLY into banked ladder factors — S1 is SELECTED by the grammar (S2's `3^{−1/4}`
  is meaningless, the gear-search is empty), but it remains a grammar-CANDIDATE pending confirmation.
- The whole `z0` chain in the key: `v → m_t [firm] → m_b [S1 grammar-candidate] → m_s/m_d [laws] → m_e [the
  w = −1 winding rung] → z0` — a chain that is derived-CONDITIONAL end to end.

## 2. THE TARGETS (in order — each a separate node, honest conditional grade)

- **M1 — the winding / ladder-rung quantization (theorem-route for the STRUCTURE).** DERIVE the ladder
  grammar's `n = 1 + 3k` quantization + the `w = −1` rung structure as an exact structural fact about the
  banked ladder. **DERIVE the quantization STRUCTURE; the rung ASSIGNMENT is candidate with a permanent
  inception flag — flag it, do NOT claim a clean forced scale.** Grade: theorem for the quantization,
  candidate for the assignment.

- **M2 — the S1 gear decomposition (`m_b`; candidate).** DERIVE that `m_b`'s amplitude decomposes exactly
  into banked ladder factors (the S1 grammar selection), with S2 excluded (meaningless / empty gear-search).
  **DERIVE the decomposition; `m_b` is a grammar-CANDIDATE, NEVER fit to the measured value.** Grade:
  candidate.

- **M3 — the `z0` / `m_e`-scale chain closure (conditional).** DERIVE the `v → m_t → m_b → m_s/m_d → m_e →
  z0` chain as derived-CONDITIONAL, tying the absolute lepton scale to the banked tower via the winding rung.
  **DERIVE the chain STRUCTURE; the absolute scale is conditional/candidate, NEVER fit.** Grade: conditional/
  candidate.

- **M4 — consolidation + the arc-N handoff.** Bundle M1–M3: the absolute scales close CONDITIONALLY on the
  banked ladder grammar + the winding quantization, with the honest inception flags carried. State plainly:
  the RATIOS (arc D) are theorem-exact; the ABSOLUTE scales are conditional-grade. Hand ONE directed
  successor toward `SEED_GATHER_TELOS.md` target N1.

## 3. THE LOAD-BEARING JOINT (DERIVE or CHAIN-RUNWAY-block) + THE PHANTOM-GRADE CAP

- **J-M1 (the winding joint):** "`9/4` is the `w = −1` rung of the banked ladder graph-quantization." DERIVE
  the quantization structure. Do NOT assert the scale.
- ⚠⚠ **THE PHANTOM CAP (the real risk of this arc):** the CLEAN forced identity for the absolute scale DOES
  NOT EXIST — the key reached only conditional/candidate grade. If a node keeps re-childing chasing a cleaner
  number, STOP: that is grinding for a phantom. The correct landing is the STRUCTURE (winding quantization +
  S1 grammar) at candidate grade WITH the inception flag. This is NOT a CHAIN-RUNWAY block (the structure
  DOES derive) — it is a GRADE CAP: land the conditional structure, flag it honestly, move on. Do NOT block,
  do NOT grind, do NOT fit.

## 4. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** The winding rung, the S1 decomposition, the scale chain — every one a DERIVED
  landing (structure) or an honestly-flagged conditional. Never a premise, never fit.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured `m_e`, `m_b`, `z0` — REMOVABLE PROSE only. NO-FIT
  guard (LOUDEST here): the absolute scale is conditional-grade — do NOT chase the residual; that is fitting
  AND grinding for a phantom.
- **G3 — DERIVED, NOT POSITED.** The quantization from the banked ladder graph; the S1 selection from the
  grammar — not a fitted scale.
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "mass", "scale", "electron" — pure statements about ladder-graph
  quantization, an amplitude's exact factor decomposition, and a rung assignment.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`. ⚠️ ℝ-VIGILANCE: banked derived `Cut` + banked ladder/scale-tower objects —
  NEVER `import Mathlib...Real` / a fitted scale as content.
- **G6 — ONE LAW + THE GRADE CAP.** If a link fights, reframe/build/decompose. But the absolute-scale
  identity is CONDITIONAL by the key's own reckoning — cap the bar at conditional/candidate, flag the
  inception, and do NOT grind for a phantom clean identity. Mechanism/conditional grade with an honest flag
  is SUCCESS for this arc.

## 5. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. ⚠ This is the hardest, least-certain arc; the key
itself only closed it conditionally, so the honest landing is the conditional STRUCTURE with a permanent
inception flag — NEVER a fitted scale, NEVER a phantom clean identity. Walk every step, then STOP at the
honest grade. When arc M completes, hand ONE directed successor toward `SEED_GATHER_TELOS.md` target N1.
