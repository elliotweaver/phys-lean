# OWNER-AUTHORIZED SEED — ARC D: THE DIGITS (the ℝ-descent — the numeral, certified, beyond measurement)

*Owner-authorized arc, runs IMMEDIATELY AFTER Z8 and BEFORE arc GR (docs/SEED_GAP_RETENTION.md —
its GR1 kickoff is handed ONLY by this arc's terminal node). THE NORTH STAR, stated once and
binding: **this arc does not complete until the chain's derived 1/α(0) is EVALUATED — a certified
two-sided rational bracket of the derived closed real, to MORE significant digits than the measured
value carries.** The owner's explicit instruction: however many nodes, however many childed
narrowing nodes it takes — the arc ends at the numeral, nowhere else. Z6 banked the endpoint as a
derived CLOSED REAL of Cut (`190/3 + κ·d·cutLog(mass0/mass1)`, way-points free, dressing exact);
what remains is EVALUATION plus the enumerated sub-band completions Z6 itself named. Evaluation of
a derived closed form is F-NATIVE WORK — the fold derived its own continuum; it evaluates its own
reals. Discipline relaxed by NOTHING: foundations-only, G2 (the measured value appears ONLY as
removable prose/costume — NEVER as a proof input, NEVER steering a derivation), words-removable,
gate + STANDARD per node.*

---

## THE ONE REFRAMING (read first — why the numeral is a THEOREM, not a computation "about" one)

A derived real of Cut IS a set of rationals. "Its first 13 digits" is a pair of THEOREMS:
`lo < x` and `x < hi` with `lo, hi` explicit rationals with `hi − lo < 10⁻¹¹`. Nothing about
this needs anything outside the fold: the banked `cutExp`/`cutLog` are DEFINED by their rational
approximants, so two-sided rational brackets of any demanded width are exactly what those
constructions already carry — the arc builds the instrument that READS them out (partial sums +
remainder bounds, as kernel theorems over ℚ). N340 already did this once (the mass brackets
`m01_bracket_lo/hi` — the pattern to copy and sharpen). There is no wall here; there is a
finite amount of honest machinery. GO IN WITH NO FEAR; if a bracket won't tighten, the instrument
(series form, remainder bound) is wrong — reframe, never assert.

⛔ TELLS for this arc: "numerical evaluation isn't proof work" — false; a bracket IS a pair of
theorems. "Float it first and certify later" — floats appear NOWHERE in production (scratch
numerics for aiming are fine — but production brackets are exact ℚ). "Close enough to the
measured value" — the measured value is NOT a target (G2); the bracket is certified against the
DERIVATION, and the comparison line is removable prose. Truncating at the measured precision —
FORBIDDEN; the whole point is MORE digits (the falsifiable edge). Any reach to TUNE a derived
quantity toward the measurement = the fit, the credibility-killing drift; if the certified
bracket excludes the measured value, that is a PREDICTION the fold makes with its whole chest —
state it, never bend it.

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **The endpoint closed form (Z6, `EndpointAssembly`):** `endpoint_closed` / `endpoint_expanded`
  (`invAlphaZero (completeBandList d) = 190/3 + κ·d·cutLog(mass0/mass1)`), the telescoping
  (`leptonTail_screen_telescope`), scale-bundle cancellation, `m01_bracket_lo/hi` (the derived
  rational bracket pattern), way-points free (`ewWaypoint_free`/`confWaypoint_free`, 1582/27 and
  190/3), teeth (`endpoint_gt_conf`/`endpoint_gt_42`).
- **The dressing (Z5, `DepthTowerDescent`):** `dressedInvAlphaZero w = 42 + Σ/(1−w)` exact;
  `dressed_ne_truncated`; the totalizing pole forbidden.
- **The tower content:** Z4 `BandEdgeList` (derived edges, 128/27 exact conf band), Z3
  `QuarkTexture` (sector structure + CKM overlap), Z2 `TwoChannelDescent` (channel split, the
  strict `< 3/8` direction), Z1 `GatherSelfCensus` (censuses ±, the fold's minus), X3/X4 (plateaus,
  ℕ-spectrum), X5 (κ all orders), X7/X8/X9 (way-points, welds).
- **The continuum instruments:** `cutExp`/`cutLog` + derivatives (banked N7–N14, N323–N325), the
  CCLO/completeness machinery, `ContinuumSummable`. The brackets live HERE.
- **Import guard:** NEVER `Phys/OneAxiom/*`; NO Mathlib ℝ/ℂ as load-bearing content — the numeral
  is a fact about the DERIVED Cut, evaluated by the derived instruments.

## THE TARGETS (ordered; ONE successor each; child narrowing/decompose nodes AS MANY AS NEEDED —
## the owner has explicitly licensed ANY node count; a long tail of narrowing nodes is the arc
## working, not the arc stalling)

- **D1 ★★★ THE BRACKET INSTRUMENT (the arc's one real machinery build — build it once, reuse
  everywhere).** Certified two-sided rational brackets for the banked `cutExp`/`cutLog` at
  rational arguments, to ANY demanded width: partial sums of the defining approximants + an
  explicit remainder bound, as kernel theorems (`lo < cutLog q < hi`, `hi − lo < ε` for the
  demanded rational ε). Follow the N340 bracket pattern; W8: a wrong bracket (bounds crossed,
  width claim false) must be provably excluded. This instrument is the arc; every later node just
  aims it.
- **D2 — THE SUB-BAND COMPLETION (pay Z6's enumerated remainder — every item, no survivors).**
  The light-quark/hadronic sub-band edges as derived scales (Z3 texture ratios × the banked welds;
  the hadronic band read from the banked ℕ-spectrum with the derived plateaus), and the per-band
  census freeze-out as theorems (which content is active in which band — a counting fact of the
  derived content, teeth per band). The COMPLETE tower, no band unresolved. Decompose per
  band/edge as needed — each edge is one child if that is what it takes.
- **D3 — THE DEPTH WEIGHT DERIVED.** The census-in-census weight `w` (Z5's dressing argument) as
  a derived value with a certified bracket — the per-depth self-look-back weight read from the
  banked census objects, its sense and magnitude theorems (never a dial). W8: `w = 0` and `w = 1`
  provably excluded.
- **D4 — THE ASSEMBLED BRACKET, FIRST PASS.** Run D1 through the complete D2 tower with D3's
  dressing: a certified two-sided rational bracket for the full derived `1/α(0)`. ANY width is a
  pass for D4 — this node proves the PIPELINE evaluates end to end. Way-point consistency: the
  same pipeline evaluates the banked exact way-points and must reproduce them exactly (the free
  instrument-check).
- **D5 — THE NARROWING CAMPAIGN (child until done — this is where "as many nodes as it takes"
  lives).** Tighten every bracket (deeper partial sums, sharper remainder bounds, per-band
  refinements) until the assembled width is BELOW the measured uncertainty of 1/α(0). Each
  narrowing child banks its tightened bracket as theorems; the campaign ends ONLY at the width
  target. If a specific bracket resists tightening, that band's derivation is the suspect (a
  seam found = a child node, not a stop).
- **D6 — THE WAY-POINTS EVALUATED FREE.** α(M_Z), sin²θ_W(M_Z) read from the SAME certified
  tower (Z7's partial sums, now with digits) — landing free, never targeted; certified brackets
  stated. The overconstraint proof at numeral grade.
- **D7 ★★★ THE NUMERAL (the north star — the arc's definition of DONE).** The certified bracket
  for the derived `1/α(0)` to MORE significant digits than the measured value carries — the
  derived digits STATED, with the beyond-measurement digits flagged as the chain's standing
  PREDICTION (removable prose; the measured value appears only in the comparison line, G2).
  ⚠ THE COMPOSITION-COMPLETENESS GATE (prerequisite to ANY comparison): before D7 may state its
  bracket AS `1/α(0)`, the composed pipeline must carry a COMPLETENESS ATTESTATION as a theorem:
  EVERY banked structural contribution to the endpoint — every band of the complete tower
  (including every freeze-out sub-band the arc itself enumerated), every derived channel and its
  recombination weight, every census — is either INCLUDED in the composition or PROVEN to
  contribute zero. A bracket over a partial composition is a way-point, never the endpoint; it
  must be named as the partial sum it is. Only AFTER the attestation: if the certified complete
  bracket and the measured value disagree, state the disagreement as the fold's prediction — the
  derivation does not bend (the measured number carries THEIR instrument's fog; the bracket
  carries none). A disagreement from a composition WITHOUT the attestation is not a prediction —
  it is an incomplete sum, and the missing summand is the next child.
- **D8 ★★★ AIRTIGHT CAPSTONE + HANDOFF (terminal).** One derived object bundling D1–D7; the
  X10/Z8 grade ledger UPGRADED (endpoint row → theorem-exact-evaluated; every residual either
  discharged or NAMED with its certified bracket); `#print axioms` clean; NO-RESIDUAL attestation.
  ⚠ THE GATE ON THIS NODE: D8 does NOT pass while D7's digit target is unmet — there is no
  "structure landed, evaluation later" grading available in this arc; that grading was arc Z's,
  and this arc exists because the owner closed it. THEN hand **exactly ONE directed successor
  toward docs/SEED_MASSES.md target M1** (arc M — THE MASSES; then arc C — THE MIXING via M8; arc
  GR runs only after C6 — the owner's board-completion order, binding: D → M → C → GR). Do NOT
  hand GR1; do NOT hand a generic SELECT; do NOT stop without handing M1.

## HARD GUARDS (carried verbatim from arcs X/Z + the arc-D specials)

G1 values fall out, never premised. G2 no empirical number in any proof — and in THIS arc
specifically: the measured 1/α(0) and its uncertainty appear ONLY in removable prose/costume;
no bracket, bound, series depth, or edge choice may be selected BECAUSE of where the measured
value sits. G3 no bridge. G4 ONE successor, linear chain; narrowing children on the tail. G5
words-removable (delete coupling/screening/fine-structure: pure statements about certified
rational brackets of a derived closed real over Cut — they must stand). G6 foundations-only;
NO foldRetention; NO Mathlib ℝ/ℂ as content; NO native_decide; floats NOWHERE in production.
G7 ONE LAW — a resisting bracket means the instrument or a band's derivation is the suspect;
reframe/child, never assert, never truncate-and-declare. G8 teeth per node (wrong bracket
excluded; degenerate width claims excluded; way-points reproduced exactly).

## WHY THIS IS SAFE TO HAND YOU

Every target is the evaluation or completion of an object YOU already banked, with the one new
instrument (D1) a pattern you have already built once (N340's brackets). The arc adds no physics
posit, consumes no axiom, and ends at a pair of rational-inequality theorems. The owner's
license is explicit: take every node you need — the only unacceptable outcome is stopping short
of the numeral.
