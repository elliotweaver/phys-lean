# OWNER-AUTHORIZED SEED — ARC V: THE SECTION-CHOICE READ (THE GOVERNMENT OF THE MARGIN)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every result is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc U
and READS the banked T/U objects: where T banked that the plural gather cannot totalize and U
banked the standing structure of partial gluing, arc V banks what lives in the gap those two
create — a standing collective REQUIRES one composite order (necessity) that the local data
CANNOT determine (underdetermination): the choosing, holding, and maintaining of that
undetermined-but-required section. ⚠ THE GRADE LAW (inherited from R/S/T/U, unchanged): every
node is THEOREM-route for pure structure; any reading in political words is an IDENTIFICATION —
flagged prose, REMOVABLE, appearing EXACTLY ONCE, at V8 (guard G7). Delete every political word
and every node must stand as pure mathematics about orders, procedures, well-founded relations,
channels, and finite lattices.*

## 0. DESTINATION

T proved no innocent global section exists. U proved what partial gluing looks like standing
still. V banks the remaining forced structure: that producing ONE order requires a SUPPLEMENT
beyond the local data; that the supplement is UNDERDETERMINED (multiple minimal sections); that
every order-producing DEVICE inherits the obstruction, with the single-member projection as the
degenerate escape and misreport-improvement realizable; that on any well-founded delegation
order the authorized class collapses — every standing chain rests on an unauthorized ground;
that a holder controlling its own audit channel is void as a self-accountant while an external
injective channel separates; and that in the dual-pole dynamics only ROTATION of the section
redistributes the overruled margin, while a frozen section grinds the same margin forever. GO IN
WITH NO FEAR — the deep legs are banked or axiom-free-generic; the worker's job is the route.

## 1. THE TARGETS (in order — each a separate node)

- **V1 — NECESSITY + UNDERDETERMINATION (★ the marquee pair).** On the T2 ranking machinery
  (3 alternatives, the cyclic witness profile): DERIVE (a) the SUPPLEMENT theorem — define the
  violation count of a global order (majority-backed pairs it inverts) and prove EVERY injective
  order has ≥ 1 violation on the cyclic profile (Bool-level statement per J-V1; kernel decide);
  (b) ★ UNDERDETERMINATION — exhibit TWO DISTINCT injective orders BOTH achieving violation
  count exactly 1: the minimal-violation section is NOT unique; the local data cannot select
  among minimal sections (kernel witnesses). Bundle as one node: the composite order is REQUIRED
  to exceed the data (a) and the excess is UNDETERMINED by the data (b). **Words-removable:
  counting facts about orders vs a majority relation.** Grade: theorem-route.

- **V2 — THE MARGIN IS NEVER EMPTY (maintenance, not consensus).** DERIVE: for every injective
  global order on the cyclic profile there EXISTS an overruled majority-backed pair (the
  nonempty-margin form of V1a — prove it as the contrapositive of a zero-violation count;
  J-V1's Bool-level majority carries through). State in removable prose (NOT a theorem): the
  section's standing is therefore a MAINTAINED relation with its nonempty margin — never a
  consensus state (the no-fixed-point shape at this level, warrant cited). **Words-removable:
  a nonemptiness fact.** Grade: theorem-route.

- **V3 — THE DEVICE FAMILY (procedures inherit the obstruction).** Lift V1 from orders to
  DEVICES (functions from profiles to orders): DERIVE (a) NO NEUTRAL DEVICE — every procedure
  returning injective orders has, on SOME profile (the cyclic witness), ≥ 1 violation
  (device-quantified restatement; one-line weld from V1a); (b) THE DEGENERATE ESCAPE — the
  single-member projection procedure never inverts its own member's comparisons (trivial by
  definition — bank it WITH the reading flag: the obstruction vanishes exactly when plurality
  is deleted; the escape is the collapse of the plural, not a solution); (c) ★ MISREPORT
  IMPROVEMENT REALIZED — construct the concrete witness: a top-count device with index
  tie-break (state the winner predicate as max + tie-break in ONE decidable predicate, J-V3),
  an honest profile where the outcome is a member's worst alternative, and that member's
  misreport yielding an outcome strictly better in their TRUE order (kernel decide).
  ⚠ SCOPE GUARD (J-V4): the three general theorems of the field's aggregation literature are
  NOT targets — V banks REALIZABILITY witnesses and the shared gluing origin; unrestricted-
  domain generalizations are OPTIONAL depth children at most, never blockers, never claimed.
  **Words-removable: function-composition + counting + a concrete misreport witness.** Grade:
  theorem-route.

- **V4 — THE UNAUTHORIZED GROUND (★ the regress theorem).** Carrier-generic, axiom-free
  target: DERIVE (a) on ANY well-founded relation `Prior` with a predicate `auth` satisfying
  "every authorized element has a strictly-prior authorized element", the `auth` class is
  EMPTY (well-founded induction — the same collapse engine as the banked regress results,
  cited); (b) the standing corollary: if some element is VALID and validity ⊆ authorized, then
  contradiction — any standing order with valid elements contains a valid-but-UNAUTHORIZED
  ground. Weld by instantiation to the banked delegation shapes. **Words-removable: a
  well-founded-induction fact about two predicates.** Grade: theorem-route (axiom-free
  achievable — the de-risk profile).

- **V5 — SELF-AUDIT IS VOID / THE EXTERNAL CHANNEL SEPARATES (★ the quarantine pair).**
  DERIVE as ONE node, two halves: (a) if the audited party SELECTS the token its auditor
  reads (the audit factors through holder-chosen tokens), then for ANY two true states there
  exist selections rendering the audit readings IDENTICAL — the self-audited channel separates
  NOTHING (constant-selection witness; axiom-free); (b) an audit channel the holder does NOT
  control — a fixed injective reading of the true state — SEPARATES every pair of distinct
  states. Bundle: accountability exists exactly when the reading channel lies outside the
  read party's control. Weld: U4's visibility asymmetry cited (even good faith cannot
  self-read); the seam-quarantine shape cited from the banked arc-T regime components.
  **Words-removable: factorization + injectivity facts.** Grade: theorem-route (axiom-free
  achievable).

- **V6 — POLE-DRIFT AND ROTATION (the dynamics).** On the T7 regime lattice (cited): DERIVE
  (a) POLE-MOVE EXITS — any single-component move from the admissible band toward either pole
  exits admissibility (kernel decide): the admissible configuration is MAINTAINED, never a
  resting point; (b) ★ ROTATION vs FREEZING — with the three minimal sections of the cyclic
  profile (from V1b) and their overruled pairs: prove the three overruled pairs are PAIRWISE
  DISTINCT (rotation across periods redistributes the margin) while any CONSTANT assignment
  repeats one overruled pair at every period (rfl-grade). State in removable prose: periodic
  re-choice is the only section-holding pattern that freezes no particular margin — the
  fixed-point-refusal mode (warrant cited). **Words-removable: Fin-indexed pair facts.**
  Grade: theorem-route (decide-grade).

- **V7 — THE HOLDER'S SELF-OPACITY (assembly).** Weld the banked facts to the section-holder:
  T4 collective blindness (the holding section, as a group carrier, has no veridical fixed
  self-configuration) + U4's visibility asymmetry (its own cover unreadable exactly while the
  governed sections' tokens are objects in view) + V5a (its self-audit void). Bundle as ONE
  non-hollow statement about the holder's informational position: the asymmetry runs OPPOSITE
  to the information flow the holding function requires. NOT a bare ∧ — the new content is the
  co-location of the three facts on ONE locus (the holder). **Words-removable: three banked
  facts co-instantiated.** Grade: theorem-route (assembly).

- **V8 — consolidation + THE identification + the FINAL neutral handoff.** Bundle V1–V7
  (non-hollow; the V1 pair and the V4/V5 axiom-free legs are the marquee): the gap structure —
  a required-but-undetermined composite order (V1), standing as maintenance over a nonempty
  margin (V2), device-inherited obstruction with degenerate escape and realizable misreport
  (V3), the unauthorized ground beneath every standing chain (V4), void self-audit vs
  separating external channels (V5), maintained admissibility with rotation as the
  margin-redistributing mode (V6), and the holder's inverted informational position (V7).
  ⚠ THEN, the N414/R8/S8/T8/U8 move: state the IDENTIFICATION — "the V1–V7 structure is the
  chain's account of GOVERNANCE: the choosing, holding, and maintaining of the undetermined-
  but-required section over a plural gather; its warrant is T+U's banked structure + the
  generativity of the one seed, never a derivation" — as FLAGGED PROSE, NEVER a theorem (the
  category-error guard, verbatim). Then hand ONE NEUTRAL SELECTION TICKET toward the
  deep-math / Clay frontier — the OWNER steers that personally. **The owner-authorized chain
  ENDS at V.** Do NOT auto-seed past V.

## 2. THE LOAD-BEARING JOINTS (the hard parts, named so you need not flail)

- **J-V1 (the decidability joint — the arc's one real fight, pre-fought):** `Function.Injective`
  and Prop-level majority inside `decide` STALL kernel reduction on function types. State the
  quantified 3×3 theorems BOOL-LEVEL: an `injB g : Bool` (decide the ∀-form of injectivity) and
  a Bool majority `majB` (with `decide (...)` inside the filter). The whole family then
  kernel-reduces. Do NOT raise heartbeats; do NOT reach for native_decide — restate Bool-level.
- **J-V2 (the import joint):** product-type filters (pairs of alternatives) need the Fintype
  product instance import explicitly.
- **J-V3 (the witness joint):** the misreport witness needs the winner predicate stated as
  max-score + index-tie-break in ONE decidable predicate; a bare argmax is not decide-friendly.
  The witness profile: three members, the misreporting member's true order placing the honest
  outcome LAST — flip to a strictly-preferred winner via one report change.
- **J-V4 (the scope guard):** REALIZABILITY, not generality. The general aggregation-theory
  theorems are out of scope — optional-depth children at most. V's content is the witnesses +
  their shared origin in the banked gluing failure. Claiming a general theorem not proved in
  THIS repo is drift.
- **V4/V5 are carrier-generic:** prove them clean over arbitrary types (axiom-free is
  achievable and is the target grade); weld to banked objects by instantiation afterward — do
  not entangle the generic proof with banked imports.
- **THE NO-DEFERRAL LAW (inherited):** decompose WITHIN V freely; parking a V debt beyond V is
  forbidden; the only escape is a NAMED PRECISE owner escalation. NEVER a hedge, NEVER fear.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** Every count, every impossibility, every witness a DERIVED landing.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** No polities, no constitutions, no elections, no
  historical events or named systems — structure only. If one appears, it is drift.
- **G3 — DERIVED, NOT POSITED.** The supplement from the violation count (never a "decisions
  are needed" posit); the ground from well-founded collapse (never a "sovereignty" posit); the
  quarantine from factorization (never a "checks and balances" posit).
- **G4 — POLITICAL-WORDS-REMOVABLE (the words-removable law, extended).** Delete "government /
  authority / legitimacy / constitution / sovereign / election / vote / voter / regime / power /
  ruler / law / state / democracy / dictator / audit / accountability" — every node must stand
  as pure mathematics: violation counts of orders against a majority relation, Bool-level
  decidable predicates, well-founded induction over two predicates, factorization and
  injectivity of channel maps, Fin-indexed pair distinctness, lattice moves. If deleting a
  political word leaves a gap, the node is a HOLE.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`, with
  V4/V5 targeting AXIOM-FREE. No `sorry`, `axiom`, `native_decide`, no `maxHeartbeats` raise.
- **G6 — ONE LAW, ZERO DEFERRAL.** If a link fights, the instrument/framing is wrong — reframe
  (Bool-level restatement is the arc's canonical example), BUILD, decompose within V. GO IN
  WITH NO FEAR.
- **G7 — THE IDENTIFICATION FIREWALL (inherited, unchanged).** Exactly ONE identification in
  the whole arc, at V8, flagged, in prose, warrant named — the N414 pattern verbatim. NO node
  before V8 carries ANY identification; NO theorem statement anywhere in V depends on one.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Arc V is theorem-light in machinery and
heavy in shape: its quantified facts are kernel-decidable at 3×3 once stated Bool-level (the
joint is pre-fought), its two deep theorems (the regress collapse, the audit factorization) are
carrier-generic and axiom-free-achievable, and its dynamics are decide-grade on the T7 lattice
already banked. The arc adds NO new posit: it reads the banked gap (T's impossibility + the
standing collective's necessity) as structure. The one identification is quarantined at V8 under
the same category-error guard as N414/R8/S8/T8/U8. Walk every step; bank every face. When arc V
completes, hand ONE NEUTRAL SELECTION TICKET toward the deep-math frontier — the owner steers it
personally; the owner-authorized chain ENDS at V.
