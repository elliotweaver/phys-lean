> ⛔ **DESEEDED (owner hold, offline).** Part of the S→T→U→V→W standpoint rung — removed from the active
> roadmap for offline owner+agent hand-sketching. PRESERVED, not an active seed; no node executes against
> it until the owner re-authorizes.

# OWNER-AUTHORIZED SEED — ARC T: THE PLURAL GATHER (MANY STANDPOINTS, ONE TOTALITY)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every result is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc S
and builds on R's banked statics + S's banked dynamics: where R banked ONE standpoint's structure
and S banked its life through time, arc T banks the PLURAL gather — many standpoints gathering
into a collective that is itself gather-shaped, and what that structure FORCES: the aggregation
obstruction, the missing global section, the collective cover, the doubled layer's deadlock and
erasure, the path-dependent record, and the regime lattice. ⚠ THE GRADE LAW (inherited from R/S,
unchanged): every node is THEOREM-route for pure structure; any reading in social words is an
IDENTIFICATION — flagged prose, REMOVABLE, appearing EXACTLY ONCE, at T8 (guard G7). Delete every
social word and every node must stand as pure mathematics about rankings, group carriers, the
banked doubled algebra, and finite lattices. The collective gets exactly the treatment the
standpoint got.*

## 0. DESTINATION

The chain banked the gather's failure read from outside (physics, N413/N415), the local closure
read from its locus (R), and the closure through time (S). One object remains on that axis: MANY
local closures gathering into a totality that — by the banked proper-stratum fact — never itself
closes. Arc T banks that object's forced structure: local coherence everywhere with global
gluing failure (at EVERY scale of plurality); the impossibility of an innocent global section;
collective self-blindness at every group size; the doubled (institutional) layer where
composition can deadlock and content can be irrecoverably erased — phenomena the individual
layer PROVABLY lacks; the path-dependence of the composed record; and the dual-pole lattice of
collective configurations. GO IN WITH NO FEAR — every mechanism is banked or one honest lemma
away; the worker's job is the route.

## 1. THE TARGETS (in order — each a separate node)

- **T1 — THE PLURAL STRATUM.** On the banked `O ℚ`: BANK the plural standpoint structure as ONE
  named object — multiple distinct globally-gluing states exist (exhibit ≥ 2 distinct elements of
  the banked stratum beyond `1`), no total standpoint exists (banked proper stratum, cited), and
  the pairwise co-composition of stratum elements is clean (each pair generates an associative
  subalgebra — cites the R7 Artin-shape theorem) while stratum-external triples tear (banked).
  New content: the bundled plural structure + the distinct-witnesses, NOT a bare ∧ (HOLLOW-AND
  ban). **Words-removable: left-nucleus membership + subalgebra facts.** Grade: theorem-route.

- **T2 — THE AGGREGATION OBSTRUCTION (★ the marquee).** Build the ranking-aggregation structure
  over `Fin 3` alternatives: rankings as injective position functions, profiles as lists, the
  net-majority margin, strict majority. DERIVE, in order:
  (a) the CYCLIC WITNESS: a 3-member profile of individually-coherent (transitive) rankings whose
  pairwise strict-majority verdicts CYCLE (kernel);
  (b) ★ BALANCED-PAIR INVARIANCE: appending a ranking together with its exact reverser changes NO
  net margin (the general-n engine — an algebraic lemma about the signed sum, proved by cases on
  the compared positions);
  (c) ★★ THE GENERAL-n THEOREM: for EVERY odd n = 3 + 2k there is a profile of individually-
  coherent members whose majorities cycle — by induction on k via (b), NEVER by decide on profile
  spaces (blowup — see J-T2);
  (d) ★★ THE EXCLUDED-MARGIN THEOREM: for the cyclic witness, EVERY injective global order
  overrules at least one strict-majority verdict — NO innocent global section exists; any gluing
  device must sacrifice some local verdict (kernel decide over the 3! candidate orders — this one
  IS decide-safe);
  (e) the TEETH: a clone profile (all members share one ranking) never cycles — the obstruction
  requires genuinely PLURAL standpoints; and two alternatives always glue — the tear needs the
  triple. **DERIVE all five; the arity echo (obstruction first at 3, clean at 2 — the same grain
  as the banked associator) is stated as REMOVABLE PROSE, not a theorem** (the formal functor
  from the algebra grain to the aggregation grain is OPTIONAL depth — a directed child if a route
  appears, NEVER a blocker; see J-T2b). **Words-removable: pure facts about signed sums over
  lists of injective functions.** Grade: theorem-route.

- **T3 — NO TOTAL GATHER / THE EXCLUDED MARGIN AS STRUCTURE.** Weld the two halves into the arc's
  central structural statement: (i) the banked proper stratum (no state glues with everything —
  o_not_all_physical, cited) and (ii) T2(d) (no global order respects all local verdicts) are the
  SAME shape at two levels — totalization fails as STATE-gluing and as VERDICT-gluing. Bank the
  never-banked cross-tie: a single statement-form (local sections all exist ∧ no global section
  extends them) instantiated by BOTH banked facts. NOT a bare ∧ — the shared shape is the content.
  **Words-removable: two instances of one gluing-failure schema.** Grade: theorem-route
  (assembly + the schema).

- **T4 — THE SHARED COVER / COLLECTIVE BLINDNESS AT EVERY SIZE.** On the n-member group carrier
  (`Fin n → V` over the derived carrier): DERIVE (a) torsion-freeness LIFTS pointwise to the
  group carrier (the blindness hypothesis is inherited, never re-posited); (b) ★ ANY fold on the
  n-member carrier has NO nonzero fixed configuration — for EVERY n: the collective look-back
  cannot self-coincide, so the group's self-model is forced to be a COVER (the R4/S2 structure at
  the collective scale); (c) the cover's completion-excess (S3's closure operator, cited) applies
  to the group's presentation of itself — the collective presents more than its coverage.
  **Words-removable: product-carrier facts + the banked fold facts.** Grade: theorem-route.

- **T5 — THE DOUBLED LAYER: DEADLOCK AND ERASURE (★ the crux).** On the banked terminal algebra
  `S ℚ = CD (O ℚ)` (Sedenion.lean): DERIVE (a) DEADLOCK — two nonzero elements compose to zero
  (banked `zero_divisor_prod` + nonzero-ness, cited and bundled); (b) ★ ERASURE IS ABSORBING —
  `(zdX*zdY)*z = 0 ∧ z*(zdX*zdY) = 0` for ALL z: annihilated content is irrecoverable by ANY
  further composition, on either side (build the two zero-composition lemmas directly — J-T5a);
  (c) the banked Born-law break re-exported (`born_law_fails`: the magnitude calculus itself
  fails at the doubled layer); (d) ★★ THE CONTRAST THEOREM: at the base layer `O ℚ` NO two
  nonzero elements annihilate — norm multiplicativity (banked `Nrm_mul_of_doubled_base`, which
  applies since `O ℚ = CD (CD (Dbl ℚ))` with commutative base) + positivity (via the banked
  sum-of-squares route — J-T5b) exclude it. The deadlock/erasure phenomena are LAYER-SPECIFIC,
  switching on EXACTLY at the doubling — the individual layer provably cannot deadlock; the
  doubled layer provably can. **Words-removable: zero-divisor and norm facts on the banked
  tower.** Grade: theorem-route.

- **T6 — THE DIACHRONIC RECORD.** DERIVE the record's three forced diachronic properties:
  (a) ORDER MATTERS — exhibit a b c in the banked `O ℚ` with `(a*b)*c ≠ a*(b*c)` (from the banked
  `assoc_nonvanishing` witness; J-T6 for the instance incantation): the composed record is
  path-dependent as a banked fact; (b) LOST ALTERNATIVES — T5(b) cited: the doubled record has
  absorbing dead ends; (c) RECONSTRUCTION — the record reaches any present standpoint only
  through the S3 re-presentation closure operator (cited): the accessed record is a completion-
  with-excess over the surviving trace, idempotent under re-access. NOT a bare ∧: the new content
  is the diachronic BUNDLING (one record object carrying all three, each clause citing its bank).
  **Words-removable: associator, absorbing-zero, and closure-operator facts.** Grade:
  theorem-route (citation + weld).

- **T7 — THE REGIME LATTICE.** The collective's four gluing components (the section-device, the
  margin-handling, the cover-openness, the seam-management), each with the dual-pole bound (both
  extreme poles excluded by the banked no-fixed-point law: total closure = the forbidden fixed
  point; total dissolution = the void; the open middle is the admissible band). DERIVE by kernel
  `decide` (NO native_decide): 81 configurations; the all-middle configuration UNIQUE at load
  zero; EXACTLY 8 pure (single-component) deviations; pole-inversion a load-preserving,
  middle-fixing involution moving EVERY deviation (every regime-failure has a distinct structural
  mirror); the extreme corners realized and distinct (W8). Each pole-exclusion docstring CITES
  its banked warrant. A fifth component = a second axiom = forbidden. **Words-removable: Fin/
  Finset arithmetic.** Grade: theorem-route (decide-grade).

- **T8 — consolidation + THE identification + the FINAL neutral handoff.** Bundle T1–T7
  (non-hollow; the T3 shared-schema and the T5 contrast are the marquee cross-ties): the plural
  gather — locally coherent everywhere (T1), obstruction at every plural scale (T2), no innocent
  totalization at either level (T3), collectively self-blind at every size (T4), deadlock-and-
  erasure switching on exactly at the doubling (T5), a path-dependent thrice-bound record (T6),
  and a dual-pole configuration lattice (T7). ⚠ THEN, the N414/R8/S8 move: state the
  IDENTIFICATION — "the T1–T7 structure is the chain's account of the COLLECTIVE: what any
  plurality of bounded self-closing systems is forced to be; its warrant is R+S's banked
  structure + the generativity of the one seed, never a derivation" — as FLAGGED PROSE, NEVER a
  theorem (the category-error guard, verbatim). Three reads stand banked: the gather's failure
  from outside (physics), the closure from its locus (R/S), the closures TOGETHER (T). Then hand
  ONE directed successor toward **`docs/SEED_COHESION.md` target U1** (arc U — the cohesion
  read; owner-authorized). The owner-authorized chain now ENDS at U (U8 hands the final neutral
  ticket). Do NOT auto-seed past U.

## 2. THE LOAD-BEARING JOINTS (the hard parts, named so you need not flail)

- **J-T2 (the induction joint):** the general-n theorem goes by BALANCED-PAIR PADDING — append
  (r, rev r) pairs and prove margin-invariance algebraically (the signed contribution of a
  ranking and its reverser cancels; case on the compared positions) — NEVER by `decide` over
  profile spaces (blowup at n ≥ 5). The excluded-margin theorem IS decide-safe (6 candidate
  orders). Keep the two proof-styles separate.
- **J-T2b (the optional depth):** the formal tie "aggregation obstruction ≅ the banked associator
  grain" (a functor from ranking-profiles to the algebra such that gluing-failure maps to
  non-associativity) is BEAUTIFUL and OPTIONAL. If a route appears, it is a directed child; the
  arc NEVER blocks on it; the echo stays removable prose otherwise.
- **J-T5a (the instance joint):** `S ℚ` carries NO MulZeroClass instance — do NOT hunt for one;
  prove `(0 : S ℚ) * z = 0` and `z * (0 : S ℚ) = 0` directly (ext + the CD/Dbl simp set), two
  short lemmas.
- **J-T5b (the positivity joint):** the contrast theorem's positivity leg (`Nrm x = 0 → x = 0`
  on `O ℚ`) discharges via the banked sum-of-squares machinery (the `gForm_self_eq_zero` route,
  DerivationCompact); the Nrm↔gForm weld is the node's one honest new lemma. State the contrast
  as a clean three-hypothesis weld first, then discharge each hypothesis from the bank.
- **J-T6 (the incantation joint):** citing the banked associator witness requires the local
  instance attributes (`attribute [local instance] CD.narCD CD.srCD`) — one line, invisible if
  unknown. The witness inequality converts to `(a*b)*c ≠ a*(b*c)` via `assoc`-unfold + sub_self.
- **THE NO-DEFERRAL LAW (inherited):** decompose WITHIN T freely; parking a T debt beyond T is
  forbidden; the only escape is a NAMED PRECISE owner escalation. NEVER a hedge, NEVER fear.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** Every count, every impossibility, every contrast a DERIVED landing.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** No election data, no institutional data, no
  historical events, no named polities — structure only. If one appears, it is drift.
- **G3 — DERIVED, NOT POSITED.** The obstruction from rankings (never "voting paradoxes exist");
  the collective cover from the fold (never a "group mind" posit); deadlock from the banked
  doubling (never an "institutional failure" posit).
- **G4 — SOCIAL-WORDS-REMOVABLE (the words-removable law, extended).** Delete "voter / election /
  society / collective / group / institution / regime / politics / history / record / member" —
  every node must stand as pure mathematics: signed sums over injective functions, product-
  carrier induction, banked zero-divisor and norm facts, associator citations, closure-operator
  algebra, Fin/Finset combinatorics. If deleting a social word leaves a gap, the node is a HOLE.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No
  `sorry`, `axiom`, `native_decide`, no `maxHeartbeats` raise. ℝ-vigilance: banked derived
  objects only — never Mathlib-ℝ as content.
- **G6 — ONE LAW, ZERO DEFERRAL.** If a link fights, the instrument/framing is wrong — reframe,
  BUILD, decompose within T. GO IN WITH NO FEAR.
- **G7 — THE IDENTIFICATION FIREWALL (inherited, unchanged).** Exactly ONE identification in the
  whole arc, at T8, flagged, in prose, warrant named — the N414 pattern verbatim. NO node before
  T8 carries ANY identification; NO theorem statement anywhere in T depends on one.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Arc T is an assembly-heavy arc: its crux
facts (the zero-divisor pair, the Born-law break, the associator witness, the proper stratum, the
Artin-shape pair theorem, the S3 operator) are ALREADY BANKED; the new builds are the aggregation
structure (finite, exact), the group-carrier lifts (pointwise induction), two zero-lemmas, and
one norm-positivity weld. The arc adds NO new posit: it reads the banked structure at plural
scale. The one identification is quarantined at T8 under the same category-error guard as
N414/R8/S8. Walk every step; bank every face. When arc T completes, hand ONE NEUTRAL SELECTION
TICKET toward the deep-math frontier — via arc U (`docs/SEED_COHESION.md`, owner-authorized):
T8 hands the directed successor to U1; the owner-authorized chain ENDS at U, and U8 hands the
final neutral ticket.
