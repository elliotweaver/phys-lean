# OWNER-AUTHORIZED SEED — THE GAUGE-COUPLING → SCALE-TOWER → COSMOLOGY ARC
*The second owner-authorized route seed. It succeeds the flavor seed (T1–T8) and is the chain's
next course. Same law as the flavor seed: it hands you the MECHANISM and the ordered targets; it
relaxes NO discipline. Every value is a landing you must DERIVE, never assert, never cite at grade,
never bridge. No empirical number enters any proof. Read `docs/STANDARD.md`, `docs/RUNBOOK.md`
(W1/W9), `docs/NODE_SELECTION.md`, and SOUL before each node.*

**GO IN WITH NO FEAR.** Every target below DERIVES. When a node resists, the instrument or framing
is wrong (W5) — never the theory. The load-bearing links (marked ★ BRIDGE) are DERIVATIONS, not
assertions and not places to expect a block: attack each with the full W1 arsenal — **build what
Mathlib lacks, DECOMPOSE into child nodes across as many nodes as it takes, reframe through the
trunk, switch tactic families — and pursue relentlessly.** A hard multi-node derivation is a NORMAL
shape (the continuum took ~8 nodes; the g₂ characterization ~15). The block/CHAIN-RUNWAY valve is a
LAST RESORT against *asserting a value*, earned only after the attack is genuinely exhausted across
one or more nodes and the single remaining gap is a specific identification you can name — NEVER an
early off-ramp because a node looks hard.

---

## LAUNCH PLATFORM (what is already banked — REUSE, do not re-derive)

- **g₂ = Der(𝕆)** fully characterized: dim 14, semisimple, simple, compact `so(7)` embedding, the
  **irreducible faithful 7-rep** on `ImO` (N6–N30+). `Phys/Algebra/Derivation*.lean`.
- **Colour SU(3)** (N42), **isospin SU(2)** (N187), spacetime signature / Lorentz / Clifford, and the
  **continuum ℕ→ℝ** (`ContinuumQ.Cut`, completeness + exp `cutExp` + derivatives — N7–N14). ℝ is
  DERIVED and BANKED; use it freely. It is NOT a gate.
- The **flavor sector** (T1–T8): the ℤ₃ self-blind cycle, Koide Q=2/3, δ_B=2/9, the mass ratios, the
  quark associator deviation, the CKM/PMNS frame overlap.

---

## THE TARGETS (in order — each a separate node, each a real theorem)

### ARC A — THE GAUGE COUPLINGS (α and the Weinberg angle; exact, current territory)

- **A1 — the octonion structure 3-form and its norm ‖φ‖² = 42.** Derive the G₂-invariant associative
  calibration 3-form φ on `ImO` (the fully-antisymmetric structure tensor of the banked octonion
  product) and its squared norm = the count of nonzero structure constants = **n(n−1) = 42** for n=7.
  *Pure octonion/g₂ algebra on banked objects. Structural; the 42 is a counting landing.*
- **A2 — the interaction-channel tower ℂ:0, ℍ:6, 𝕆:42 as n(n−1).** Derive that the number of
  independent interaction channels at each cascade rung is `n(n−1)` (0, 6, 42), the 42 being the
  octonion rung — the same 42 as A1, now read as the channel/pair count of the terminal algebra.
- **A3 — ★ BRIDGE: Born-isometry completeness, `42·α = 1`.** Derive that the total Born weight summed
  over the 42 channels is 1 (the self-overlap/return normalization Σ_channels α = 1) — the
  per-channel coupling weight identified with `g²/4π` as a THEOREM of the self-overlap structure, the
  way holonomy=statistics was derived in the flavor crux. This is the load-bearing identification of
  Arc A. DERIVE it from the trunk (Born self-overlap = the banked norm form); do not assert it.
- **A4 — α\* = 1/42 lands.** The fine-structure-scale coupling `α* = 1/42` falls out of A3. The first
  marquee constant of this arc. Compare to the measured `1/137` (running) as REMOVABLE PROSE only —
  `1/42` is the un-dressed structural value at the octonion scale, exactly like the answer-key grade.
- **A5 — the chiral hypercharge trace `Tr(Y²)/Tr(T3²) = 5/3`.** Derive over ONE chiral generation of
  the banked SU(3)×SU(2)×U(1) content. ⚠ THE TRAP: the naive gauged-F₄ normalization gives the WRONG
  `3/4` — derive via the chiral-generation trace (the correct route), and let the `5/3` fall out.
- **A6 — sin²θ_W = 3/8 lands.** From A5: `sin²θ_W = 3/(3+5) = 3/8`. The second marquee constant. EXACT
  (identity-grade). Compare to measured `0.231` (running) as removable prose only.
- **A7 — iso-stiffness / one-coupling coherence.** Derive that the three gauge layers cohere at one
  structural coupling (the unification consequence of A3/A4) — the "one α" reading. *(Bank the
  structural coherence; the running/threshold dressing is downstream watch-item, not this node.)*

### ARC B — THE SCALE TOWER (dimensionful scales; where GRAVITY enters)

- **B1 — dimensional transmutation `v = M·exp(−c/g²)`.** Derive the electroweak scale as an
  exponentially-suppressed ratio of the closure scale, the same mechanism as Λ_QCD, using the banked
  derived-ℝ `exp` (`cutExp`). *(Uses the Arc-A coupling. Structural form first.)*
- **B2 — ★ BRIDGE: the transmutation coefficient c.** Derive the exponent coefficient c from the
  cascade β-function structure (the integers {28, 9, 4π} enter here). DERIVE c; do not assert it.
  This is Arc B's load-bearing link — attack with the full arsenal, decompose as needed.
- **B3 — Newton's constant: `G·M² = e^(−6π)`.** Derive the bare-G / closure-scale relation. **THIS IS
  GRAVITY'S FIRST APPEARANCE IN THE CHAIN** — the saturation-face completion `G·M² = e^(−6π)`.
- **B4 — M_Pl = G^(−1/2), M = M_Pl·e^(−3π).** The Planck mass from G; the closure scale as the
  exp-suppressed Planck scale.
- **B5 — the tower assembly G → M_Pl → M → v.** Bundle B1–B4 into the ordered scale ladder, each rung
  a derived ratio.
- **B6 — m₀ = v²/M (the Born-square seesaw scale).** Derive the neutrino seesaw scale as the
  Born-square of the VEV over the closure scale — the ν mass scale.
- **B7 — the tower as one object.** Every dimensionful scale from G + the integers {42, 28, 9, 7, 4π},
  zero measured dimensionful inputs. The capstone of the scale sector.

### ARC C — FIRST COSMOLOGY CONTACT

- **C1 — Σm_ν = m₀.** Derive that the neutrino mass sum rides the tower (= B6's m₀) — a genuine
  cosmological observable from the chain. *(Structural identity; the data-comparison Σ≈58 meV stays
  removable prose — it is under a live DESI exclusion-watch and NEVER enters a proof.)*
- **C2 — the ν spectrum m₁=0 / normal ordering.** Derive (or lift from the T8 PMNS construction) the
  massless lightest neutrino and normal ordering as a structural consequence.
- **C3 — toward ρ_Λ = (Σm_ν/26)⁴.** Derive the trace-channel structure relating the cosmological
  constant density to the ν scale via the 26 traceless J₃(𝕆) matter channels. ⚠ HONEST GRADE: even in
  the private answer key this is CANDIDATE/post-hoc grade — derive the MECHANISM (the 26-channel
  structure, the fourth-power law) and bank that; do NOT claim the value as identity-grade, and NEVER
  fit to the measured Λ.
- **C4 — consolidation + the cosmology on-ramp.** Bundle the cosmology contact; name the next frontier
  (the GR sector — Einstein-equation uniqueness, the 8πG normalization — which needs differential-
  geometry machinery built on the banked ℝ, a real multi-node build, NOT a one-node leap). Hand
  forward ONE successor: a generic SELECTION ticket (a fresh worker runs SELECT theory-native FIRST)
  toward the GR/cosmology frontier.

---

## HARD GUARDS (identical to the flavor seed — these make the discipline structural)

- **G1 — NO ASSERTED VALUES, EVER.** 42, 1/42, 5/3, 3/8, e^(−6π), the scales, m₀ — every one is a
  DERIVED landing that FALLS OUT, never a premise, never cited at grade, never bridged. The gate
  rejects an asserted value exactly as always.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Comparison to `1/137`, `0.231`, `58 meV`, the measured Λ,
  etc. is REMOVABLE PROSE / costume only — NEVER a proof step (exactly like `sin²θ_W = 3/8` vs
  `0.231`).
- **G3 — SCALES vs RATIOS.** Arc A produces pure dimensionless numbers (identity-grade). Arc B
  produces dimensionful scales as RATIOS to G/M_Pl — G (or one closure scale) is the single legitimate
  dimensionful anchor, not a fitted input; every other scale is a derived ratio to it.
- **G4 — THE BRIDGES ARE DERIVED, NOT POSITED.** A3 (Σα=1) and B2 (the transmutation coefficient) are
  the two places an ansatz would smuggle the answer. Derive them from the trunk (Born self-overlap /
  cascade β-structure); if you catch yourself positing and checking against data, STOP.
- **G5 — PHYSICS-WORDS-REMOVABLE.** Delete "coupling", "charge", "mass", "gravity", "Newton",
  "cosmological" — the theorems must stand as pure statements about the octonion 3-form, the g₂/channel
  counts, the exp-ratios of the derived ℝ, and their invariants.
- **G6 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`, no
  `axiom`, no `native_decide`. ⚠ ℝ-VIGILANCE: use the BANKED derived ℝ (`ContinuumQ.Cut`) and its
  banked `exp`/derivatives — NEVER `import Mathlib...Real` as load-bearing content (that would be the
  hidden second axiom). The continuum itself is banked; only a NEW node substituting Mathlib-ℝ is the
  drift.
- **G7 — ONE LAW.** If a link fights — a value won't converge, a form won't derive, you reach for a fit
  — you are drifting. The instrument is wrong, the theory is right. Reframe through the trunk, build
  the missing machinery, decompose into child nodes, switch tactics. Block ONLY as a last-resort valve
  against asserting, after the attack is exhausted — never early, never because a node looks hard.

---

## WHY THIS IS SAFE TO HAND YOU

Same as the flavor seed: the route is a MAP of which structures to build and what they mean — it
contains **no proof and no permission to assert.** Every value is earned through the identical gate.
If the mechanism is right, the values are forced and you will derive them. The map does not walk the
path for you; it tells you the path exists. Walk every step.
