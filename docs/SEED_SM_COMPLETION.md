# OWNER-AUTHORIZED SEED — ARC F: STANDARD MODEL COMPLETION
*The fifth owner-authorized route seed. It succeeds the GR/BH/cosmology seed
(`SEED_GR_BLACKHOLE_COSMOLOGY.md`, arc E). Same law: MECHANISM and ordered targets given, discipline
relaxed by NOTHING. Every value a DERIVED landing, never asserted/cited-at-grade/bridged/fitted. No
empirical number in any proof — data comparison is REMOVABLE PROSE / costume only.*

**WHAT THIS ARC IS.** The remaining Standard-Model structural observables that are NOT masses or
couplings (those are arcs A/D): the **anomaly-cancellation conditions**, **strong CP (θ̄=0)**, **proton
stability**, the **Higgs quartic / mass**, the **neutrino mixing completion (TBM/PMNS)**, and the
**"dissolution" theorems** (hierarchy, strong CP, Λ, initial entropy = "the problem assumes a dial the
framework provably lacks"). This arc is answer-key-RICH and mostly clean-to-candidate — but it has
sharp hidden joints flagged per node. GO IN WITH NO FEAR; attack with the full W1 arsenal, decompose
into child nodes, never assert, block only as a last resort against asserting.

⚠️ **HONEST GRADE MIX (carry per node):** F1 (anomalies) and F2 (strong CP) are theorem-grade/clean.
F3 (proton stability) is a clean derived PREDICTION. F4 (Higgs) is CANDIDATE with a live frame-split.
F5 (ν mixing) is leading-order like the CKM. F6 (dissolutions) is a gorgeous theorem-SHAPE but each
leg cites banked structure — flag what's identification vs theorem.

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **The full derived gauge content:** g₂=Der(𝕆), colour SU(3), isospin SU(2), the chiral generation
  with `Tr(Q²)=16/3`, `Tr(T3²)=2`, hypercharges (arc A: `ChiralGeneration*.lean`).
- **J₃(𝕆) / F₄** (the matter arena, the 26 traceless channels), the octonion associator/3-form
  (`OctonionStructure3FormNorm.lean`), the banked flavor sector (T1–T8: Koide, δ_B, mixing).
- **The derived ℝ + trig** (`ContinuumQ.Cut`, `cutExp`, `cutSin`/`cutCos`/`cutPi`), the scale tower
  (arc B), the Higgs-relevant `v` (arc B).
- **Spacetime Cl(1,3) / Lorentz**, the sphaleron/topological B-violation structure (if banked).

---

## THE TARGETS (in order — each a separate node, each a real theorem)

- **F1 — anomaly cancellation (the cleanest target; theorem-grade).** Derive that all Standard-Model
  gauge+gravitational anomalies cancel per generation with the F₄-derived hypercharges: the five
  conditions `U(1)³`, `SU(2)²×U(1)`, `SU(3)²×U(1)`, `grav²×U(1)`, and the Witten `SU(2)` global anomaly.
  ⚠️ TEETH (the private program's control, reuse it): **B−L *without* the right-handed neutrino FAILS at
  exactly −1** — i.e. the banked ν_R is REQUIRED by anomaly freedom. Bank that as a costume: dropping ν_R
  must make an anomaly non-zero. *Exact-rational, 16 conditions all zero — this is a clean, high-yield
  landing.*
- **F2 — strong CP / θ̄ = 0 (theorem-grade, the flavor half).** Derive `θ̄ = 0` from the structure:
  the Hermitian J₃(𝕆) matter arena ⇒ the mass matrix has an F₄-invariant REAL determinant ⇒ no
  physical θ-phase to shuffle (a no-RH-rotation theorem), with the Jarlskog `J ≠ 0` alongside (CP is
  violated in flavor but θ̄ stays zero). ⚠️ The full θ-term construction is a Tier-2 formal debt in the
  key (θ-weight = det ≡ 1 per configuration; `−I ∉ Aut(J₃(𝕆))`) — derive the flavor half cleanly; the
  gauge-θ half may need a child. This is one of the framework's "no-dial" dissolutions (see F6).
- **F3 — proton stability (a clean derived PREDICTION that distinguishes from SU(5)).** Derive that
  proton decay `p→e⁺π⁰` is TOPOLOGICALLY FORBIDDEN: the only gauge B-violation is the sphaleron/H³
  channel with `ΔB=ΔL=n_gen ∈ 3ℤ`, so `ΔB=−1` is forbidden; only gravity (dim-6/M_Pl²) gives
  `τ_p ~ 10⁴⁴ yr`. ⚠️ This is a genuine framework PREDICTION (proton effectively stable, distinguishes
  from the SU(5) `10³⁴ yr` — flag it as a prediction, the number is removable prose). Needs the
  topological B-violation structure; if not banked, build the `ΔB=3ℤ` selection rule as a child.
- **F4 — the Higgs quartic / mass (CANDIDATE, live frame-split — flag honestly).** Derive the Higgs as
  the EW fold's radial mode, its quartic = the fold's self-coupling, critically FLAT (`λ=0`) at a
  DERIVED scale (the criticality grammar, `w=1`), converting the SM's unexplained near-criticality into
  a scale selection. ⚠️⚠️ HONEST GRADE: this is **CANDIDATE with a permanent inception flag** — the
  reading `λ(M_H)=0` gives `m_H≈127` (+2.1σ direct frame / +0.7σ anchor frame) and the score is
  m_t-frame-dependent (the same inclusive-vs-direct m_t fight that adjudicates V_cb). Derive the
  criticality STRUCTURE (λ=0 at a fold-selected scale); do NOT claim `m_H` as identity-grade; the RGE
  running to a number needs the derived-ℝ machinery and is a real build. NEVER fit to `125.25`.
- **F5 — the neutrino mixing completion (PMNS/TBM, leading-order).** Derive the PMNS mixing structure
  (tri-bimaximal-like leading order, `m₁=0`, normal ordering) from the same frame-overlap machinery as
  the CKM (T8), with the fold phase. ⚠️ LEADING-ORDER like the CKM (~few %); `m₁=0` and normal ordering
  are the structural claims. Carries the SAME standing DESI Σm_ν fight as arc C — flag it, never fit.
- **F6 — the dissolution theorems (one gorgeous theorem-shape) + the arc handoff.** Derive the unified
  "no-dial" theorem-shape: the hierarchy problem (no `m_H²` dial), strong CP (no shuffleable θ), the
  cosmological constant (no bare Λ term), and Penrose's initial entropy (`S_initial=0` exact, no initial
  dial) are ALL the same shape — *the "problem" is an artifact of assuming a parameter the framework
  provably lacks*. ⚠️ Each leg cites banked structure (C6, F₄-invariance, the closure uniqueness) — flag
  which legs are theorems vs identifications. Then consolidate Arc F and hand forward ONE successor: a
  DIRECTED child toward `SEED_EMERGENT_QUANTUM_STATISTICAL.md` target G1 (see AFTER-F6 below).

## AFTER F6 — THE CHAIN'S NEXT COURSE IS ALREADY SET (arc G, then arc H)

Two further owner-authorized seeds succeed arc F, in order — the chain does NOT stall at F6 and does NOT
hand a generic SELECTION ticket:

1. **Arc G — `docs/SEED_EMERGENT_QUANTUM_STATISTICAL.md`** (the emergent/statistical layer: spin-statistics
   + Pauli, universality/critical exponents, the arrow of time, Landauer, the path-integral measure). F6's
   ONE successor is a DIRECTED child toward arc G target **G1** (spin-statistics + Pauli exclusion).
2. **Arc H — `docs/SEED_COSMOLOGY_COMPLETION.md`** (cosmology completion + the fate/genealogy frontier).
   Arc G's last node (G6) hands ONE directed child toward arc H target **H1** (the derived-ℝ `cutExp`
   calculus). Arc H's last node (H6) hands ONE NEUTRAL SELECTION TICKET toward the deep-math / Clay frontier
   (RH, Yang-Mills) — which the OWNER is holding for explicit direction; do NOT auto-seed it, hand a neutral
   SELECTION ticket and let the owner steer.

Discipline UNCHANGED across both: every value a DERIVED landing, foundations-only gate, ℝ-vigilance, honest
grade caps (candidate/mechanism/leading-order flagged, never fit). Successor-ticket title template:
`arc-G G1 (DIRECTED SUCCESSOR of N###, per SEED_EMERGENT_QUANTUM_STATISTICAL.md §G1): …` — assignee
phys-novelty, workspace dir:/Users/elliotweaver/phys-lean, max-retries 25, max-runtime 120.

---

## HARD GUARDS (identical law)

- **G1 — NO ASSERTED VALUES.** The anomaly zeros, `θ̄=0`, `τ_p`, `m_H`, the mixing angles — every one a
  DERIVED landing that falls out. Never a premise, never cited at grade, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** The measured `m_H=125.25`, `τ_p` bounds, PMNS angles —
  REMOVABLE PROSE / costume only. NEVER a proof step.
- **G3 — SCALES vs RATIOS.** `m_H` (if reached) = the ladder scale × the criticality condition —
  inherits the G-anchored ladder-grade; the mixing angles are dimensionless leading-order.
- **G4 — DERIVED, NOT POSITED.** The anomaly conditions from the F₄-hypercharges (not the measured Y);
  θ̄=0 from J₃(𝕆) Hermiticity (not asserted); proton stability from the topological selection rule
  (not a chosen operator basis).
- **G5 — PHYSICS-WORDS-REMOVABLE.** Delete "proton", "anomaly", "Higgs", "CP" — the theorems stand as
  pure statements about trace conditions on the F₄-charge assignment, the reality of a J₃(𝕆)
  determinant, a `3ℤ` selection rule, and the zero of a fold self-coupling on the derived ℝ.
- **G6 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`. ⚠️ ℝ-VIGILANCE: use the BANKED derived ℝ + arc-D trig — NEVER Mathlib-ℝ as
  load-bearing content.
- **G7 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe,
  build the missing machinery, decompose, switch tactics. Block ONLY as a last resort against asserting,
  after the attack is exhausted — never early, never because a node looks hard.

## WHY THIS IS SAFE TO HAND YOU

The route is a MAP of which structures to build and what they mean — no proof, no permission to assert.
Every value is earned through the identical gate. The map does not walk the path for you; it tells you
the path exists. Walk every step. And when Arc F completes, HAND A NEUTRAL SELECTION TICKET toward the
deep-math frontier — the owner is steering that sector personally.
