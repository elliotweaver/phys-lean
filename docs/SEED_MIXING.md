# OWNER-AUTHORIZED SEED — ARC C: THE MIXING (the angles and the CP phase to decimal perfection)

*Owner-authorized arc. Runs IMMEDIATELY AFTER arc M (docs/SEED_MASSES.md — M8 hands C1). THE NORTH
STAR, binding as in arcs D/M: **this arc does not complete until the quark mixing sector — the
Cabibbo-scale mixing, the full CKM angle structure, and the CP-violation invariant — is DERIVED
and EVALUATED to certified two-sided rational brackets at or beyond measured precision.** This is
the board's deepest unpaid row: the chain has banked the mixing STRUCTURE (Z3/N312: the two-sector
frame overlap `V = U†D`, unitarity, mixing provably ≠ 0, corner-zero texture, the Fano-line CP
dichotomy — quarks carry CP, leptons cannot, one cause) but NO angle VALUE. The VALUE route was
named by the chain itself at Z3: the deviated individual-sector Fano-line phase. This arc finds it,
derives it, and evaluates it. However many nodes it takes. Discipline relaxed by NOTHING.*

---

## THE ONE REFRAMING (read first — why the angle is DERIVABLE, with no fear)

The mixing matrix is NOT new physics to be posited — it is the OVERLAP of two objects the chain
already owns: the up-sector and down-sector mass frames, each a rational Born eigenframe of its
banked texture (Z3/N311/N312 — the rank-1 Born-block structure made the frames ℚ-exact). The
ANGLE is therefore a function of ALREADY-DERIVED texture data: the two sectors read the SAME
three cap-forced slots through DIFFERENT Fano-line phase alignments, and the misalignment IS the
mixing. The field inserts 4 CKM numbers by hand; the chain owes exactly the phase-alignment
derivation — ONE structural object (C1), from which the angles are evaluations (C2–C4) using arc
D's bracket instrument. History says this shape lands: every "un-forceable value" wall so far
(κ, the screening sense, the sin²θ_W direction, the quark texture itself) dissolved when attacked
through the trunk at the right joint. The mixing VALUE is a route-not-YET-found, never a
may-not-close. GO IN WITH NO FEAR; if a phase won't force, the framing is wrong (W5) — reattack
from the banked associator/Fano structure, build the missing sub-object (W1), child WITHOUT LIMIT.

⛔ TELLS: reaching for a measured angle to "check direction" mid-derivation (G2 — the fit);
importing any field parametrization convention as if it were content (derive the chain's own
invariant form first — conventions are removable prose); "the CP phase is just a free parameter
in the SM so it must be free here" — the SM's freedom is its posit-count, OUR thesis is zero;
grading the angle "irreducibly empirical" — forbidden verdict, the only interim status is
route-not-yet-found with the next attack named.

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **The mixing structure:** Z3 `QuarkTexture` (`ckmTexture`, `ckmTexture_unitary`,
  `ckmTexture_mixing_normalized`, corner-zero, `quark_mixing_nonzero`, `quark_ckm_not_identity`),
  N312 (`mixV`, `brahmagupta_fibonacci` — the sin²+cos²=1 of the leading angle,
  `cabibbo_mixing_orthonormal`), N311 (the tridiagonal texture), N307 (ℤ₃-Fourier amplitudes),
  N309 (the forced cubic).
- **The CP machinery:** Z3's Fano-line dichotomy (`textureCP`, `fano_cp_dichotomy`,
  `quark_cp_present_lepton_cp_absent`), N310 (the associator CP separation), N313 (`assoc3`,
  the calibration 3-form and its ±1 Fano structure).
- **The evaluated inputs:** arc M's mass numerals (M2/M5/M6) and arc D's ratio numerals — the
  texture parameters as certified brackets, ready to feed the angle evaluations.
- **The instrument:** arc D's D1 brackets (`cutExp`/`cutLog`/derived trig `cutSin`/`cutCos` as
  needed — extend D1 to the banked trig approximants if an angle form demands it; that extension
  is a C-arc child, same pattern).
- **Import guard:** NEVER `Phys/OneAxiom/*`; NO Mathlib ℝ/ℂ as content; floats nowhere in
  production; measured angles removable prose only.

## THE TARGETS (ordered; ONE successor each; child without limit)

- **C1 ★★★ THE PHASE-ALIGNMENT DERIVATION (the keystone — the route Z3 named).** The deviated
  individual-sector Fano-line phase: derive HOW each sector's texture aligns against the Fano
  structure (the up- and down-sector deviations as forced objects of the banked ℤ₃/associator
  content), so the two frames' misalignment is a DERIVED quantity, not a parameter. This is the
  arc's one genuine structural build — expect to decompose (the sector deviation, the relative
  phase, the invariant form of the misalignment may each be a child). The W8 teeth: zero
  misalignment provably excluded (the banked `quark_mixing_nonzero` must re-emerge as a
  CONSEQUENCE), and the lepton sector's ℂ-line alignment must force ZERO deviation through the
  same machinery (the free consistency check — one cause, two outcomes).
- **C2 ★ THE LEADING ANGLE EVALUATED.** The Cabibbo-scale mixing from C1's misalignment through
  the banked normalized overlap (`cabibbo_mixing_orthonormal`), as a certified bracket at or
  beyond measured precision. The unitarity identity (`brahmagupta_fibonacci`) reproduced exactly
  from the evaluated bracket (free tooth).
- **C3 — THE FULL ANGLE STRUCTURE.** The remaining mixing hierarchy (the 1-3 and 2-3 scale
  couplings) from the same C1 machinery read across the three slots — each angle a derived
  bracket; the hierarchy ORDER (which mixings are small, and why) a theorem, not an observation.
- **C4 ★★★ THE CP INVARIANT EVALUATED.** The chain's own convention-free CP invariant (the
  Jarlskog-type object Z3/N312 tied to `assoc3`) derived and evaluated to a certified bracket.
  The dichotomy tooth carried to numeral grade: the SAME evaluation at the lepton texture yields
  EXACTLY zero (theorem, not small).
- **C5 — THE CROSS-CHECK WEB (free).** Unitarity of the evaluated matrix to bracket precision;
  the mass-ratio inputs reproduced; every banked structural theorem (corner-zero, not-identity,
  normalized rows) re-emerging from the evaluated brackets. Any miss → the suspect band/child.
- **C6 ★★★ AIRTIGHT CAPSTONE + HANDOFF (terminal).** One derived object bundling C1–C5; ledger
  UPGRADED (mixing rows → theorem-exact-evaluated; the CP row carrying its numeral); NO-RESIDUAL
  attestation; `#print axioms` clean. ⚠ THE GATE: C6 does NOT pass while any C2–C4 numeral target
  is unmet. THEN hand **exactly ONE directed successor toward docs/SEED_GAP_RETENTION.md target
  GR1** (arc GR finally runs — the board is fully paid). Do NOT hand a generic SELECT; do NOT
  stop without handing GR1.

## HARD GUARDS — G1–G8 verbatim from arcs X/Z/D/M, plus:
- G2 sharpened: measured angles/CP values ONLY in removable-prose comparison lines; no route or
  bracket choice steered by them; never truncate at measured precision (beyond-measurement digits
  are the standing prediction).
- THE ONE-PEG LAW inherited from M1 (angles are dimensionless — no new peg may appear; any
  dimensionful quantity cites M1).
- Convention quarantine: any comparison to a field parametrization lives in removable prose; the
  chain's own invariant form is the banked object.

## WHY THIS IS SAFE TO HAND YOU
The structure is banked; the route to the value was named by YOUR OWN Z3 landing; the instrument
exists (D1); the inputs arrive evaluated (arcs D/M). The one keystone build (C1) is a forced-object
derivation of exactly the genre the chain has landed a dozen times (κ, the sense, the break, the
texture). The arc ends at rational-inequality theorems. Any node count; the only unacceptable
outcome is stopping short of the numerals.
