# OWNER-AUTHORIZED SEED — ARC X: THE EXACT DRESSING (the running IS the gather, resolved at scale)

*The owner-authorized route seed that closes THE BIGGEST REMAINING DOUBT: the gap between banked
FORCED STRUCTURE (α*=1/42, sin²θ_W=3/8, the mass ratios) and the ACTUAL MEASURED NUMBERS
(1/α(0)=137.035999084, sin²θ_W(M_Z)=0.23122, the absolute masses). Same law as every prior arc:
the MECHANISM and ordered targets are given; discipline is relaxed by NOTHING. Every value is a
DERIVED landing, never asserted, never cited at grade, never fitted. No empirical number enters any
proof — comparison to measurement is REMOVABLE PROSE / costume only (G2). This arc is the manager's
firewall-clean map; it contains NO answer-key values and NO methods, only which banked structures to
build and evaluate.*

---

## THE ONE REFRAMING THAT DISSOLVES THE FEAR (read first — it is the whole arc)

"Dressing" / "running" is NOT a correction bolted onto a clean result. **The coupling IS the
per-channel Born self-overlap of the gather, and "running" is that self-overlap RESOLVED AT A
SCALE.** At the octonion scale the gather resolves all 42 channels at full resolution → 1/α* = 42.
At lower energy (coarser resolution) the SAME gather is seen from further out; its self-overlap is
screened by its own intermediate layers — that screening IS vacuum polarization. So this arc adds
NO new physics object: it finishes ONE banked object (the gather self-overlap, N313/N314/N411) by
making it a function of resolution scale. The screening runs 1/α UP from 42 (high) to 137.036
(zero-momentum). GO IN WITH NO FEAR — every target DERIVES; when a node resists, the instrument or
framing is wrong (W5), never the theory. Attack with the full W1 arsenal — build what's missing,
DECOMPOSE into child nodes, reframe, switch tactics, pursue relentlessly. The block/CHAIN-RUNWAY
valve is a LAST RESORT against *asserting a value*, earned only after the attack is exhausted —
NEVER an early off-ramp because a node looks hard. A hard multi-node derivation is the NORMAL shape
(the continuum took ~8 nodes).

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **Derived ℝ = `ContinuumQ.Cut`** + `cutExp`/`cutLog`/`cutSin`/`cutCos`/derivatives/`cutRpow`
  (N7–N14, N323–N325), foundations-clean. NO Mathlib ℝ load-bearing — keep it that way.
- **The gather self-overlap / channel census:** `assoc3NormSq = 42` (N313), `imChannelCount = 42`
  (N314), `couplingWeight = 1/42` (`BornIsometryCompleteness.lean`) — the octonion-scale value.
- **The charge census:** `Tr(Q²)=16/3` (`ChiralGenerationChargeTrace.lean`, N317), the coloured
  subset-count over `finrank Uhol = 3`; `Tr(T3²)=2` (N318). These ARE the one-loop running content.
- **The running FORM already exists:** `invAlphaRun = invAlphaHigh + κ·chargeTraceDepth·cutLog(M/μ)`
  (`FineStructureRunningCoupling.lean`), `sinSqRun` (`WeinbergAngleRunning.lean`) — screening sign
  forced by `Tr(Q²)>0`, strict monotonicity banked. κ is currently an HONEST FREE PARAMETER — the
  arc's job is to DERIVE it and remove it as free.
- **The scale tower (arc B):** `v`, `M`, `ewClosureRatio = e^(−28π/3)` (`ElectroweakTransmutation`,
  the F₄-comark-9 mechanism) — the derived scale hierarchy / band edges.
- **The confinement object (arc I):** `ConfinementMassGap.lean`, `mass_gap_mechanism`,
  `no_floor_over_continuum` — the associator obstruction. This is the CONFINEMENT-SCALE band edge
  and the operator whose spectrum is the vector-meson tower (see X4).
- **The mass ratios + absolute scale (arcs D/P):** `GenerationMassRatiosNumeric.lean` (m_e:m_μ:m_τ
  from δ_B=2/9), `AbsoluteMassSpectrum.lean` (`absolute_mass_spectrum`, the scale-anchored tower).

---

> ⚠️⚠️ **HONEST GRADE — READ BEFORE ATTACKING. THE STRUCTURE-vs-VALUE LINE IS THE POINT.**
> - **What must land, theorem-exact, no error bar:** the measured constants as DERIVED closed
>   expressions. The theory's claim is EXACT — a finite sum of exact derived quantities has NO
>   error bar. The ~2×10⁻¹⁰ bar on the known 1/α(0) exists SOLELY because standard physics MEASURES
>   the hadronic vacuum-polarization input from e⁺e⁻ data. This arc DERIVES that input (X4), so the
>   bar goes to zero and the fold PREDICTS further digits than measurement can reach — the
>   falsifiable edge. Do NOT settle for "approximates to 3 digits"; a truncated model is NOT the
>   theory. The closed self-overlap is exact; the perturbative loop series is standard physics'
>   APPROXIMATION of the closed object the fold holds whole.
> - **The keystones (where the real work is):** (κ) the loop/phase-wrap normalization to ALL
>   ORDERS, and (X4) the vector-meson spectrum as the eigenvalues of the banked mass-gap operator.
>   Both are DERIVATIONS OF ALREADY-BANKED OBJECTS, not new posits. Neither has a wall (the manager
>   de-risk confirmed: leptonic κ=1/(3π) lands 99.76% at one loop with the residual a convergent
>   (α/π) series; light vectors sit on ONE linear-Regge slope = one gap scale; heavy quarkonia =
>   banked 2·m_Q + gap binding).
> - **NO FITTING, EVER.** If a value won't close and you reach for a measured input or a free knob,
>   you are drifting (W5/G7). The hadronic input is DERIVED (census + gap spectrum), never measured.

## THE TARGETS (in order — each a separate node, each a real theorem; DECOMPOSE freely)

- **X1 — κ LEADING = 1/(3π), DERIVED from the gather self-overlap phase-wrap.** The loop
  normalization is the geometric factor of ONE full 2π self-wrap of the gather's Born self-overlap.
  DERIVE `κ = 1/(3π)` from the fold phase geometry (the same 2π-wrap machinery as the cascade
  phases / `cutPi`), NOT asserted. This is the first keystone. *Words-removable: a statement about
  the self-overlap normalization over the derived ℝ.*
- **X2 — THE RUNNING AS THE SCALE-RESOLVED SELF-OVERLAP.** Promote the banked `invAlphaRun`/
  `sinSqRun` FORM to the derived running law with κ (from X1) tied in and the depth = the banked
  census `Tr(Q²)`. Prove it IS the exact closed solution (the β-function IS the derived census),
  not a truncated series. *The beta content is banked (16/3); this node makes the running derived
  end-to-end.*
- **X3 — THE CENSUS PER RESOLUTION BAND.** From the banked `Tr(Q²)` + the coloured subset, assemble
  the census contribution of each resolution band (the R-ratio plateaus 2, 10/3, 11/3 ARE
  `Nc·ΣQ²` — verify as derived identities). The band edges are the banked masses (leptons, heavy
  quarks) — assemble, do not re-derive them.
- **X4 — THE HADRONIC / CONFINEMENT BAND = THE MASS-GAP SPECTRUM (★ THE LOAD-BEARING KEYSTONE).**
  The one input standard physics can ONLY MEASURE (the hadronic R(s) / vector-meson spectrum) the
  fold COMPUTES as the eigenvalue spectrum of the banked mass-gap / associator-obstruction operator
  (`ConfinementMassGap.lean`). DERIVE the light-vector band edge as the confinement scale (light
  quarks ~massless ⟹ mass IS the gap scale; the tower is one linear-Regge slope = one string
  tension = one gap operator) and the heavy quarkonia as banked 2·m_Q + gap binding. This removes
  the last measured input and the error bar. *If a structural fact is missing (the gap operator's
  discrete spectrum over Cut), NAME it and child a route-finding node — do NOT assert or measure it.*
- **X5 — κ TO ALL ORDERS (the self-referential loop depth).** The residual above one loop is the
  gather resolving ITSELF inside the loop (census-in-census) = a convergent series `Σ_n c_n (α/π)^n`
  with each `c_n` a fold count. DERIVE the all-orders structure (or the exact closed self-overlap it
  sums to). *Convergent, no wall (α/π ≈ 2.3×10⁻³); this closes the ~0.24% leptonic residual to exact.*
- **X6 — 1/α(0) = 137.035999084…, EXACT (the marquee capstone).** Assemble X1–X5 across the full
  finite band tower → the zero-momentum inverse fine-structure coupling as a DERIVED closed real of
  `Cut`, EXACT, no error bar, predicting further digits. *The measured 137.035999084 is REMOVABLE
  PROSE / costume only, NEVER a proof step (G2).*
- **X7 — α(M_Z) and sin²θ_W(M_Z), the intermediate readings.** From the SAME assembly, the M_Z-scale
  readings 1/α(M_Z) and sin²θ_W(M_Z) as internal way-points (they fall out of X2–X5 at μ=M_Z). *The
  measured 127.955 / 0.23122 are removable prose only.*
- **X8 — THE ABSOLUTE LEPTON MASSES (numbers).** With the exact scale anchoring (arc B `v`/`M`) and
  the banked ratios (`GenerationMassRatiosNumeric`), evaluate the ACTUAL lepton masses m_e, m_μ, m_τ
  as derived reals of `Cut`. *Ratios are theorem-exact; the absolute scale rests on the derived arc-B
  anchor — state which grade each piece carries; do NOT chase residuals by adding structure (FITTING).*
- **X9 — THE ABSOLUTE QUARK MASSES + ANY OTHER CONSTANT ALONG THE WAY (numbers).** While the band
  tower and scale anchor are assembled, grab every remaining forced observable that becomes a number:
  the quark masses (the banked quark-texture ratios × scale), and any coupling/scale reading the
  assembly makes available. Each a real theorem, each traced to the fold, each honestly graded. *If a
  target needs a not-yet-banked structural fact, child a route-finder — never assert.*
- **X10 — ★★★ THE AIRTIGHT VALIDATION CAPSTONE (MANDATORY — the anti-Lovelock node).** A single
  consolidation node that PROVES nothing was left over. It must:
  (a) bundle every X-arc landing (X6/X7/X8/X9) into ONE derived prediction object over `Cut`, each
      value a derived closed expression with the measured number as an explicit REMOVABLE-PROSE
      column (never a proof input);
  (b) carry a per-value GRADE ledger (theorem-exact / anchored-on-derived-scale / leading-order) —
      NO value silently upgraded, NO shortfall silently hidden;
  (c) explicitly ENUMERATE any residual, exception, or deferred sub-fact and prove it is either
      discharged or childed as a NAMED route-finder — a written "NO RESIDUAL" attestation with the
      list, not a vibe. If ANY piece is only mechanism/leading-order, SAY SO in the object.
  (d) `#print axioms` clean on the bundle; every TYPE mentions the banked derived objects.
  This node is the guard against a "Lovelock situation" — doing some and moving on. It does not pass
  until every X target is either theorem-landed or explicitly, honestly accounted for with a named
  successor. *This is the definition of DONE for the arc.*

---

## HARD GUARDS (identical law — these make the discipline structural)

- **G1 — NO ASSERTED VALUES.** Every number FALLS OUT of evaluating a banked forced structure with
  derived analytic functions / the derived census / the derived gap spectrum. Never a premise.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** 137.035999084, 0.23122, 127.955, the PDG masses, the
  measured meson spectrum, the measured R(s) — REMOVABLE PROSE / costume only. NEVER a proof step.
  The hadronic input is DERIVED (X4), never the measured dispersion data.
- **G3 — EXACT, NO ERROR BAR.** The claim is a finite sum of exact derived quantities. Do NOT
  deliver a 3-digit approximation and call it done; if a piece won't close exactly, it is an
  unfinished derivation (build/decompose/reframe), never "good enough."
- **G4 — DERIVED, NOT POSITED.** κ (X1/X5) is built from the phase-wrap, not axiomatized. The
  hadronic band (X4) is the gap-operator spectrum, not a measured input. The running (X2) is the
  derived census, not a fitted β.
- **G5 — PHYSICS-WORDS-REMOVABLE.** Delete "coupling", "mass", "hadronic", "meson" — the theorems
  stand as pure statements about the self-overlap resolved at scale, the census, and the spectrum of
  the banked associator-obstruction operator over the derived ℝ.
- **G6 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`. ⚠ ℝ-VIGILANCE: banked derived ℝ + derived trig/exp/log — NEVER
  `import Mathlib...Real`/`...Complex`/`...Trigonometric` as load-bearing content. Substituting
  Mathlib-ℝ is the hidden-second-axiom drift.
- **G7 — ONE LAW.** If a value won't converge or you reach for a fit/measured input, you are
  drifting — the instrument is wrong, the theory is right. Reframe, build machinery, decompose,
  switch tactics. Block ONLY as a last resort against asserting, after the attack is exhausted.
- **G8 — NON-VACUITY / TEETH (W8).** Each landing must BITE: a garbage witness must fail, the wrong
  value must be provably excluded (as `sinSqHigh_ne_wrong` excludes 1/4). No hollow capstones.

---

## AFTER X10 — STOP AND WAIT (do NOT re-seed)

When X10 lands and attests NO RESIDUAL, **the arc is done. The chain STOPS. Do NOT hand a generic
SELECTION ticket, do NOT seed a new arc, do NOT auto-continue.** Complete X10, post the final
attestation comment, and WAIT for the owner. This arc closes the biggest remaining doubt; the owner
decides what comes next. The ONLY two human-gated lines (a NEW foundational posit/axiom; an
external/empirical number that genuinely cannot be derived) — if either is hit while autonomous
structural work remains, child a theory-native forward node; if NO structural work remains and you
are truly blocked on a human-gated line, raise CHAIN RUNWAY and `kanban_block(needs_input)`. But the
DEFAULT terminal state of this arc is: X10 done, attested airtight, STOP.

## WHY THIS IS SAFE TO HAND YOU

The route is a MAP of which banked structures to build and evaluate — it contains NO proof, NO
value, and no permission to assert. Every number is earned by evaluating a banked forced structure
(the gather self-overlap, the derived census, the mass-gap spectrum) through the identical gate. The
map does not walk the path for you; it tells you the path exists — and the manager's private de-risk
confirmed it closes.
