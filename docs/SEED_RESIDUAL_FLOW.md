# OWNER-AUTHORIZED SEED — ARC W: THE RESIDUAL-FLOW READ (THE METABOLISM OF THE GATHER)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every result is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc V
and READS the banked chain: where T banked the plural gather's obstructions, U the standing
structure of association, and V the governance of the margin, arc W banks the FLOW — how bounded
standpoints with ineliminable residuals and finite budgets reduce those residuals THROUGH EACH
OTHER: exchange, price, scarcity, coordination, measurement, composition failure, and the unit
regime. ⚠ THE GRADE LAW (inherited from R/S/T/U/V, unchanged): every node is THEOREM-route for
pure structure; any reading in economic words is an IDENTIFICATION — flagged prose, REMOVABLE,
appearing EXACTLY ONCE, at W8 (guard G7). Delete every economic word and every node must stand
as pure mathematics about shortfall sums, token projections, budget arithmetic, banked
zero-divisor and norm facts, and finite unit-type lattices.*

## 0. DESTINATION

The chain banked that an alive state ALWAYS carries residual (the S-arc residual theorem) and
that its zeroing is the void. W banks what follows when MANY such states provision each other:
mutual-reduction exchange exists and requires DIFFERENCE; the flow is measured in tokens that
are structurally blind to the inside of valuation; every allocation under the finite budget
forgoes an alternative; any metric whose measured party selects the token is void; at the
doubled layer composed positions can annihilate unrecoverably while the base layer provably
cannot; and NO single unit can serve the three layer-requirements of the flow's standard — while
a quarantined multi-unit regime serves all three. GO IN WITH NO FEAR — two clusters read
physics-side banked objects directly; the rest is kernel arithmetic; the worker's job is the
route.

## 1. THE TARGETS (in order — each a separate node)

- **W1 — THE UNCLOSABLE RESERVOIR (assembly).** Weld the banked ground: the residual theorem
  (res x = 0 ↔ x = 0 — an alive state always carries residual, cited from the S-arc bank) +
  the zero-residual-is-void fact (cited) into ONE named statement: the flow's source term is
  ineliminable — there is NO terminal state of the provisioning process short of the void.
  NOT a bare ∧: the bundling names the reservoir. **Words-removable: two banked facts
  co-stated.** Grade: theorem-route (assembly).

- **W2 — MUTUAL REDUCTION + THE DIFFERENCE CONDITION (★ the founding pair).** On a finite
  goods model (shortfall = max(need − holding, 0); residual = total shortfall): DERIVE
  (a) MUTUAL REDUCTION REALIZED — complementary holdings/needs where ONE transfer strictly
  reduces BOTH residuals (concrete witness; simp only [defs] + norm_num per J-W2 — unfold
  fails under binders);
  (b) ★ THE DIFFERENCE CONDITION (teeth, general form) — for IDENTICAL standpoints (same
  holdings, same needs), NO transfer improves the total: prove for ALL t (the general-t
  theorem via max-lower-bounds — le_max_left/right — plus norm_num + linarith; do NOT
  case-split the max, J-W2); the mutual gain lives in the DIFFERENCE and vanishes with it.
  **Words-removable: shortfall-sum arithmetic.** Grade: theorem-route.

- **W3 — THE TOKEN PROJECTION (★ the blindness of the flow's measure).** DERIVE (a) any read
  of the presented token identifies phase-distinct valuation states over one token (rfl-grade,
  axiom-free target); (b) the projection is genuinely many-to-one (exhibit two states, same
  token, different phase). Bundle: the flow's measure is the magnitude-quotient of a
  phase-side valuation; what crosses the wire NEVER carries the inside (cite the banked
  channel facts from the U-arc). **Words-removable: factorization + witness facts.** Grade:
  theorem-route (axiom-free achievable on (a)).

- **W4 — THE BINDING BUDGET (scarcity + the coordination bound).** DERIVE (a) under a binding
  budget, increasing one allocation strictly shrinks the feasible remainder, and a forgone
  alternative EXISTS for every strict reallocation (linarith-grade pair); (b) the coordination
  bound: internal coordination runs on the phase channel (cited from the U-arc taxonomy), whose
  budget is finite (cited U-arc bound) ⟹ a bound on internally-coordinated scale EXISTS while
  token-mediated (quotient) scale is unbounded (cited) — bank the CONTRAST as the node's
  content. ⚠ J-W4 (the value trap, inherited): ALL bounds are EXISTENCE-only; a numeric
  anywhere = G2 violation. **Words-removable: budget arithmetic + cited channel facts.**
  Grade: theorem-route.

- **W5 — THE VOID METRIC (★ the measure problem).** DERIVE as ONE node, two halves (the V5
  engine instantiated on the flow's channel — cite V5, do not re-derive): (a) when the
  MEASURED party selects the token the metric reads, ANY two true states admit selections
  with identical readings — the metric-become-target separates NOTHING (axiom-free target);
  (b) a fixed injective channel outside the measured party's control separates every distinct
  pair. Bundle: measurement of the flow is valid EXACTLY when the reading channel is outside
  the read party — seam-design, not virtue. **Words-removable: factorization + injectivity.**
  Grade: theorem-route (axiom-free achievable).

- **W6 — THE COMPOSITION FAILURE (★ the crisis grammar, banked).** On the banked terminal
  algebra (cite Sedenion/ForcedStop; J-W3 instance discipline inherited from the T-arc:
  direct ext+simp zero-lemmas, no MulZeroClass hunt): DERIVE/RE-EXPORT as ONE bundled node:
  (a) the banked Born-law failure — the magnitude of a composition is NOT the product of the
  parts' magnitudes at the doubled layer: composites do not inherit their parts' valuation;
  (b) the banked deadlock — two individually nonzero positions composing to ZERO — plus
  ABSORBING loss both sides: the annihilated composite is unrecoverable by ANY further
  composition (recovery must come from OUTSIDE the composition — bank this as its own
  lemma pair);
  (c) ★ the LAYER CONTRAST — at the base layer the magnitude IS multiplicative (cite the
  banked doubled-commutative-base law): the failure is a property of COMPOSITION DEPTH, not
  of exchange. **Words-removable: zero-divisor and norm facts on the banked tower.** Grade:
  theorem-route (citation + weld).

- **W7 — THE UNIT REGIME (★ the capstone cluster).** Build the unit-type (two structural
  booleans: issuance-discretionary?, owned?) with THREE definitional facts encoded in the
  type and its well-formedness predicate (⚠ J-W5: these are DEFINITIONAL — one dial per unit;
  discretion has an agent (disc → owned); stable = non-discretionary — state them as the
  structure of the type, NEVER as empirical premises). Three layer-requirements as decidable
  predicates: requires-stable (disc = false); requires-flexible-owned (disc ∧ owned);
  requires-unowned (owned = false). DERIVE by kernel decide (NO native_decide):
  (a) ★ NO single well-formed unit satisfies all three requirements;
  (b) ★ the impossibility decomposes into TWO INDEPENDENT contradictions (stable∧flexible;
  owned∧unowned) — and the stable+unowned pairing CAN share one unit while strictly failing
  the flexible-owned requirement (the three pairwise facts);
  (c) ★ a THREE-ASSIGNMENT regime (one unit per requirement) satisfies every requirement
  natively — existence;
  (d) ★ in ANY satisfying regime the flexible-owned unit is DISTINCT from both others (the
  quarantine law — cite V5's shape in removable prose), and the stable + unowned requirements
  MAY share one unit (the minimal 2-unit form);
  (e) teeth: each requirement is non-vacuous (well-formed units failing each exist).
  **Words-removable: kernel facts about a 2-boolean structure type and Fin-indexed
  assignments.** Grade: theorem-route (decide-grade).

- **W8 — consolidation + THE identification + the FINAL neutral handoff.** Bundle W1–W7
  (non-hollow; the W2 difference condition, the W6 layer contrast, and the W7 impossibility+
  existence pair are the marquee): the flow structure — an unclosable reservoir (W1), mutual
  reduction living on difference (W2), a welfare-blind token measure (W3), binding-budget
  allocation with a bounded phase-coordination scale (W4), void self-selected metrics (W5),
  composition-depth valuation failure with absorbing loss and a crisis-free base (W6), and
  the three-requirement unit impossibility with its quarantined multi-unit resolution (W7).
  ⚠ THEN, the N414/R8/S8/T8/U8/V8 move: state the IDENTIFICATION — "the W1–W7 structure is
  the chain's account of PROVISIONING: how bounded self-closing systems reduce their
  ineliminable residuals through each other, what the flow's measure can and cannot carry,
  and what its composed instruments and unit standards are forced to be; its warrant is
  T+U+V's banked structure + the generativity of the one seed, never a derivation" — as
  FLAGGED PROSE, NEVER a theorem (the category-error guard, verbatim). Then hand ONE NEUTRAL
  SELECTION TICKET toward the deep-math / Clay frontier — the OWNER steers that personally.
  **The owner-authorized chain ENDS at W.** Do NOT auto-seed past W.

## 2. THE LOAD-BEARING JOINTS (the hard parts, named so you need not flail)

- **J-W1 (the import joint):** Mathlib.Data.Rat.Order does NOT exist in this Mathlib — use
  Mathlib.Data.Rat.Defs + Mathlib.Algebra.Order.Field.Basic for ℚ order facts.
- **J-W2 (the tactic joint — pre-fought):** `unfold` FAILS on defs under binders inside the
  shortfall compositions — use `simp only [<defs>]` + norm_num for witnesses. For the
  general-t difference condition the winning pattern is max-LOWER-BOUNDS (le_max_left,
  le_max_right as hypotheses) + norm_num + linarith; case-splitting the max via rcases
  branches hits tactic snags under <;> — do not fight that route.
- **J-W3 (the instance joint, inherited T-arc):** the terminal algebra carries no
  MulZeroClass — prove the two zero-composition lemmas directly (ext + the CD simp set).
- **J-W4 (the value trap, inherited):** every capacity/scale bound is EXISTENCE-only. NO
  numeric — not in prose, not as costume, not as an "e.g.". A number here is a G2 violation.
- **J-W5 (the definitional-axiom joint):** W7's three unit-facts are DEFINITIONAL structure
  of the type (encoded in fields + the well-formedness predicate), never empirical claims.
  State the attack surface honestly in removable prose: they are conceptual truths about
  what discretion/stability/ownership MEAN. Any strengthening beyond the three = a second
  posit = forbidden.
- **THE NO-DEFERRAL LAW (inherited):** decompose WITHIN W freely; parking a W debt beyond W
  is forbidden; the only escape is a NAMED PRECISE owner escalation. NEVER a hedge, NEVER
  fear.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** Every impossibility, every bound, every witness a DERIVED
  landing.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** No prices, no market data, no historical crises,
  no named currencies or institutions, no interest rates — structure only. If one appears, it
  is drift.
- **G3 — DERIVED, NOT POSITED.** The reservoir from the banked residual theorem (never a
  "wants are unlimited" posit); mutual reduction from shortfall arithmetic (never an
  "exchange is beneficial" posit); the unit impossibility from the type structure (never a
  "trilemma" posit).
- **G4 — ECONOMIC-WORDS-REMOVABLE (the words-removable law, extended).** Delete "trade /
  exchange / price / market / value / money / currency / wealth / scarcity / cost / firm /
  demand / supply / welfare / crisis / instrument / bank / credit / inflation" — every node
  must stand as pure mathematics: shortfall sums, transfer arithmetic, token factorizations,
  budget inequalities, banked zero-divisor/norm facts, kernel facts about a 2-boolean type.
  If deleting an economic word leaves a gap, the node is a HOLE.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`, with
  W3(a)/W5(a) targeting AXIOM-FREE. No `sorry`, `axiom`, `native_decide`, no `maxHeartbeats`
  raise. ℚ and banked derived objects only — never Mathlib-ℝ as content.
- **G6 — ONE LAW, ZERO DEFERRAL.** If a link fights, the instrument/framing is wrong —
  reframe (J-W2's simp-only pattern is the arc's canonical example), BUILD, decompose within
  W. GO IN WITH NO FEAR.
- **G7 — THE IDENTIFICATION FIREWALL (inherited, unchanged).** Exactly ONE identification in
  the whole arc, at W8, flagged, in prose, warrant named — the N414 pattern verbatim. NO node
  before W8 carries ANY identification; NO theorem statement anywhere in W depends on one.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Arc W closes the reader triptych at
the place the chain's algebra works hardest: two clusters (W1, W6) read the banked
physics-side objects DIRECTLY (the residual theorem; the terminal algebra's deadlock and
Born-break), the arithmetic clusters are linarith/kernel-grade with their tactic joints
pre-fought, and the capstone cluster is decide-grade on a 4-element type. The arc adds NO new
posit: the three W7 definitional facts are type-structure, and everything else reads banked
material. The one identification is quarantined at W8 under the same category-error guard as
every arc before it. Walk every step; bank every face. When arc W completes, hand ONE NEUTRAL
SELECTION TICKET toward the deep-math frontier — the owner steers it personally; the
owner-authorized chain ENDS at W.
