# OWNER-AUTHORIZED SEED — ARC H: COSMOLOGY COMPLETION + THE FATE / GENEALOGY FRONTIER

*Owner-authorized. This seed gives the ROUTE (a mechanism map). It relaxes discipline by NOTHING:
every value is a DERIVED landing that the mechanical gate (costume-bite + `#print axioms`
foundations-only) enforces; the map does not walk the path. Succeeds arc G.*

## 0. DESTINATION — what this arc is for

Arc E derived gravity and the black-hole thermodynamic laws; arc G derived the emergent/statistical layer.
Arc H completes CONCRETE COSMOLOGY — the inflationary observables, the baryon asymmetry, dark energy — and
then reaches the theory's deepest cosmological readings: the **fate of the universe** (the eternal approach)
and the **no-genealogy / far-side** frontier. The through-line: the SAME no-fixed-point (C6) engine that
forbade the cascade's completion is WHY the universe inflates, WHY it can never reach heat death, and WHY it
began from a blank slate. One axiom, the whole cosmic arc.

The FIRST node is a MACHINERY BUILD (H1, the derived-ℝ `cutExp` calculus) that ALSO discharges a debt arc E
left childed — exactly as arc D built the trig before turning structure into numbers. Build the machinery,
then consume it. GO IN WITH NO FEAR: multi-node builds are the NORMAL shape here.

## 1. LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- The derived ℝ `Cut` + `cutExp`/`cutLog` (N174) and `cutSin`/`cutCos`/`cutPi` (N323–N325). The Born-positive
  `cutExp` that suppresses the scale tower (N320) and flattens the inflaton plateau are the SAME object.
- The banked **plateau/α-attractor class SELECTED by self-blindness** (N364): the potential
  `(1 − cutExp(−cφ))²` is bounded yet NEVER attains its bound on φ≥0 (no fixed point); monomial (unbounded)
  and hilltop (attained maximum) EXCLUDED. The class-universal `n_s = 1 − 2/N`, `r = 8/(c²N²)` relations are
  DEFINED candidate-grade — their full slow-roll DERIVATION is the childed debt H2 discharges.
- The banked B+L complement-even channel + `ΔB ∈ n_gen·ℤ` (N366/N368), the CP associator `assoc3` (N209),
  and the derived expansion — the ingredients of baryogenesis (H3).
- The banked de Sitter / cosmological-constant term `Λ·g` forced covariantly-constant (N357), the return-turn
  `azimuthalTurn = 2·cutPi` (N328), and `S_init = 0` (the blank-slate cross-tie for H6).
- The banked black-hole interior → closure-transition surface (arc E) — the anchor for the far-side H6.

## 2. THE TARGETS (in order — each a separate node, each a real theorem)

- **H1 — the derived-ℝ `cutExp` CALCULUS (a MACHINERY BUILD; discharges the N364 childed debt).** Build the
  differential/integral calculus of the banked `cutExp`/`cutLog`/trig on the derived ℝ `Cut` that the
  slow-roll derivation needs: the derivative (chain rule) and the antiderivative on `Cut`, extending the
  banked `ContinuumScalarDeriv`/`ContinuumDerived`. Mathlib lacks this ON `Cut` — BUILD it (W1), never
  import `Mathlib...Real` analysis as content. This is the arc-D-trig move again: a machinery node that
  unblocks a backlog of deferred evaluations. Grade: machinery / theorem.

- **H2 — inflation slow-roll: `n_s = 1 − 2/N`, `r = 8/(c²N²)` (candidate-class; consumes H1).** Using the
  H1 calculus, DERIVE the slow-roll observables from the banked plateau potential `(1 − cutExp(−cφ))²`
  (N364, the class already FORCED by C6). The spectral index and tensor-to-scalar ratio fall out of the
  slow-roll parameters (first/second derivatives of the potential) — `N` is the e-fold COUNT. **DERIVE the
  relations from the banked potential; do NOT fit `n_s ≈ 0.965` or `r`.** The measured values are REMOVABLE
  PROSE only. Grade: candidate-class (the plateau CLASS is forced, the relations derived, `N` a count) —
  flag it like the T8 mixing caveat; do NOT chase the measured `n_s` to zero residual (that is fitting).

- **H3 — baryogenesis / the baryon asymmetry `η_B` (mechanism / candidate).** DERIVE the mechanism for the
  matter-antimatter asymmetry: the banked B+L complement-even channel (N366/N368, where baryon number
  changes) + the CP source `assoc3` (nonzero, N367) + the derived out-of-equilibrium expansion = the three
  Sakharov conditions, all banked or derived. **DERIVE the mechanism and its SIGN/order; do NOT fit
  `η_B ≈ 6×10⁻¹⁰`.** The measured value is REMOVABLE PROSE only. Grade: mechanism / candidate (like the
  Jarlskog — sign + order-of-magnitude are the honest claim, the precise value needs scales; flag honestly,
  never fit).

- **H4 — dark energy `w = −1` / de Sitter self-similarity (mechanism; the VALUE is candidate/post-hoc).**
  DERIVE that the late-time equation of state is `w = −1` and that the de Sitter endpoint is
  TIME-TRANSLATION SELF-SIMILAR: the scale-factor ratio `a(t+Δ)/a(t)` is epoch-independent, the proper
  event-horizon distance `1/H` is constant, the Gibbons–Hawking temperature is constant — "always in the
  middle." **DERIVE the `w = −1` mechanism + the self-similarity; NEVER fit the value of `Λ`** (the Λ VALUE
  is post-hoc / candidate even in the answer key — derive the mechanism, flag the value out of scope).
  Grade: mechanism-grade for `w=−1`/self-similarity; the Λ value candidate/post-hoc, flagged, never fit.

- **H5 — the fate of the universe: the eternal approach (theorem-grade; C6 deployments).** DERIVE that the
  universe never reaches heat death. The route: heat death = completed totalization = the KL divergence to
  maxent reaching 0 = completed self-coincidence = C6-FORBIDDEN (the same engine as G3); and independently,
  a smooth C6-compatible flow cannot reach a fixed point in finite time (Picard–Lindelöf). So the ending is
  always AHEAD, for every probe, by theorem. The past end is a BOUNDARY (`S_init = 0` at finite look-back,
  banked); the future end is an ASYMPTOTE (no ceiling) — finite-past / infinite-future IS the arrow,
  restated. **DERIVE the no-arrival theorem from C6; do NOT posit the arrow of time.** Grade: theorem-grade
  (the key closed FT1–FT5 as theorems).

- **H6 — the no-genealogy / far-side frontier + the arc handoff (assembly).** DERIVE that a far-side face
  carries no observable trace of its parent: the far side receives a BLANK, ψ-independent state (nothing
  passes THROUGH the closure — information goes OUT as the sheaf, not through), which re-derives `S_init =
  ln 1 = 0` FROM the black-hole side (a cross-check of the banked cosmological row); the forced plateau
  inflation (H2) washes anisotropy at `e^{−6N}`; so parentage is unobservable IN PRINCIPLE — "are we inside
  a black hole?" has zero empirical content, every face begins identically. **DERIVE the blank-slate + the
  isotropy wash; the ontological "one transition vs many universes" question is architectural-grade — state
  the stance, claim no machine face.** Grade: assembly (TR1–TR4 theorems; TR5 stance-only). Then hand ONE
  NEUTRAL SELECTION TICKET toward the deep-math frontier — **the owner is steering that sector personally.**

## 3. THE LOAD-BEARING JOINTS (DERIVE or CHAIN-RUNWAY-block)

- **J-H5 (the no-arrival joint):** "completed totalization = completed self-coincidence = C6-forbidden." The
  same C6 deployment as G3/H, at the cosmological scale. DERIVE it; do NOT posit the eternal approach.
- **J-H6 (the blank-slate joint):** "nothing passes through the closure ⇒ the far side is ψ-independent ⇒
  `S_init = 0`." DERIVE it from the banked information-out-not-through / sheaf structure; do NOT assert the
  blank slate. Both are DERIVABLE — GO IN WITH NO FEAR; the block valve is a last resort against ASSERTING,
  earned only after the full W1 attack (build, decompose, reframe) is exhausted.

## 4. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** `n_s`, `r`, `η_B`, `w=−1`, `Λ`, `S_init=0` — every one a DERIVED landing.
  Never a premise, never cited at grade, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured `n_s≈0.965`, `η_B≈6e-10`, `Ω_Λ`, `Λ`, `Ω_DM h²` —
  REMOVABLE PROSE / costume only. NEVER a proof step. Explicit NO-FIT guard: H2/H3/H4 land at candidate /
  mechanism grade — do NOT chase residuals to zero; that is fitting.
- **G3 — DERIVED, NOT POSITED.** Slow-roll from the banked plateau potential (not fitted); baryogenesis from
  the banked B+L channel + associator (not a chosen operator); the eternal approach from C6 (not "assume the
  arrow"); the blank slate from the sheaf structure (not asserted).
- **G4 — SCALES vs RATIOS.** Dimensionless observables (`n_s`, `r`, `w`) are derived; any dimensionful scale
  (`Λ`, the reheating scale) inherits the G-anchored ladder-grade and is flagged, never fit.
- **G5 — PHYSICS-WORDS-REMOVABLE.** Delete "inflation", "baryon", "dark energy", "heat death", "universe" —
  the theorems stand as pure statements about a bounded-never-attained potential on `Cut`, a quantized
  charge channel, a self-similar exponential flow, a KL divergence that never reaches 0, and a ψ-independent
  boundary state.
- **G6 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`, `axiom`,
  `native_decide`, no `maxHeartbeats` raise. ⚠️ ℝ-VIGILANCE: build the calculus and everything on it on the
  BANKED derived ℝ `Cut` — NEVER `import Mathlib...Real` analysis as load-bearing CONTENT.
- **G7 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe, build
  the missing machinery (the H1 calculus is ordinary construction, never a wall), decompose, switch tactics.
  Block ONLY as a last resort against asserting, after the attack is exhausted — never early, never because a
  node looks hard.

## 5. WHY THIS IS SAFE TO HAND YOU

The route is a MAP of which structures to build and what they mean — no proof, no permission to assert.
Every value is earned through the identical mechanical gate. The map tells you the path exists; it does not
walk it. Walk every step. When arc H completes, hand ONE NEUTRAL SELECTION TICKET toward the deep-math
frontier — the owner is steering that sector personally.
