# OWNER-AUTHORIZED SEED — ARC U: THE COHESION READ (TIES, SECTIONS, TRANSMISSION, MODES)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every result is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc T
and READS its banked objects: where T banked the plural gather's obstruction facts (no total
gather, no innocent section, collective blindness, the doubled layer's deadlock), arc U banks the
STANDING STRUCTURE of partial gluing — the two channel types between standpoints and their forced
asymmetries, the architecture of sections and boundaries, the transmission of the shared cover
across members, and the dual-pole lattice of association modes. ⚠ THE GRADE LAW (inherited from
R/S/T, unchanged): every node is THEOREM-route for pure structure; any reading in social words is
an IDENTIFICATION — flagged prose, REMOVABLE, appearing EXACTLY ONCE, at U8 (guard G7). Delete
every social word and every node must stand as pure mathematics about maps, finite sets, sums,
and the banked carriers. The association gets exactly the treatment the collective got.*

## 0. DESTINATION

T banked that the plural gather cannot totalize. U banks what the PARTIAL gluings that remain
are forced to look like: exactly TWO channel types between standpoints (one phase-carrying and
budget-bounded, one magnitude-only and unbounded — with novelty crossing only the second); the
in/out architecture of any binding-and-excluding section, with its forced visibility asymmetry
and rank inconsistency; the transmission law (tokens copy, covers rebuild — drift forced, the
frozen token the only persistence); and the two-axis association lattice with its four excluded
corners and opponent structure. GO IN WITH NO FEAR — every mechanism is banked, seeded upstream,
or one honest lemma away; the worker's job is the route.

## 1. THE TARGETS (in order — each a separate node)

- **U1 — THE TWO-CHANNEL TAXONOMY (★ the atom).** Over a two-coordinate state carrier (magnitude
  × phase, inhabited): DERIVE (a) the BINARY EXHAUSTIVE taxonomy — every map from partner-states
  either factors through the magnitude coordinate or fails to (and then some phase-pair it
  separates); there is no third channel type (build the factorization disjunction; J-U1:
  [Nonempty] on the phase carrier — the empty case is noise); (b) the factoring channel is
  PHASE-BLIND — identical delivery on phase-differing states over one magnitude (a `rfl`-grade
  fact; bank it, it is the load-bearing half); (c) the non-factoring channel is defined by joint
  phase-sensitivity. **Words-removable: pure factorization facts about maps on a product
  carrier.** Grade: theorem-route.

- **U2 — THE ASYMMETRIC CAPACITY THEOREM.** DERIVE (a) the BUDGET BOUND: if each phase-channel
  tie consumes at least c > 0 from a finite budget B (the residual/closure-work budget — cites
  the S1 residual structure as the budget's warrant), then the phase-tie degree n satisfies
  n ≤ B/c — a capacity bound EXISTS on the phase channel (Finset.sum comparison; J-U2 imports);
  (b) ★ the ASYMMETRY (W8 teeth): the magnitude channel admits UNBOUNDED families — exhibit, for
  every n, n distinct factoring ties from one standpoint (zero phase-work each); (c) ⚠ THE VALUE
  FLAG, in the node text itself: the bound's EXISTENCE is the theorem; its VALUE is substrate —
  NO number is derivable and none may be stated (not in prose, not in costume). **Words-
  removable: sum bounds + an injective family.** Grade: theorem-route.

- **U3 — THE BRIDGE THEOREM (novelty crosses the thin channel).** DERIVE (a) phase-channel
  ties HOMOGENIZE: two standpoints holding a JOINT cover (co-cover: their presented content is
  one object) have zero content-difference — nothing novel passes between them because nothing
  differs; (b) magnitude-channel ties impose NO cover-identity — cover-difference is realizable
  across them (exhibit it); (c) ★ the bundled bridge statement: content-difference (novelty) is
  realizable ONLY across non-co-cover ties, and every co-cover tie has none — the thin channel
  is the novelty channel. **Words-removable: equality/difference facts about presented
  objects.** Grade: theorem-route.

- **U4 — SECTIONS: THE IN/OUT ARCHITECTURE + THE VISIBILITY ASYMMETRY.** DERIVE (a) a section
  that binds some standpoint and excludes some standpoint realizes the full in/out structure
  (distinctness + nonempty inside + nonempty outside — bundled); (b) ★ THE VISIBILITY
  ASYMMETRY: the section's OWN cover is unrecoverable from its own token-channel (two candidate
  covers over one token stream are indistinguishable — the no-meter shape, cited from the
  R3-family, relocated to the section's self-relation) WHILE other sections' tokens are
  faithfully in view (the identity channel separates them) — bank the PAIR as one theorem: the
  inside is invisible to itself exactly where the outside is visible to it; (c) collective
  blindness at the section scale (T4, cited — no fold on the section's carrier has a nonzero
  fixed configuration). **Words-removable: indistinguishability + separation facts.** Grade:
  theorem-route.

- **U5 — RANK INCONSISTENCY ACROSS SECTIONS.** Re-use the T2 ranking machinery at the section
  level: DERIVE that with two ranking-sections over the same standpoints, OPPOSITE rankings are
  realizable (the concrete two-section witness, kernel) and NOTHING in the structure enforces
  cross-section consistency — consistency of rank across sections is a CONTINGENT achievement,
  never a default (weld to T2's excluded-margin: rank-aggregation inherits the aggregation
  obstruction). **Words-removable: two injective functions disagreeing on order.** Grade:
  theorem-route (kernel witness + weld).

- **U6 — TRANSMISSION: NO-COPY, FORCED DRIFT, THE FROZEN TOKEN (★ the persistence theorem).**
  DERIVE (a) NO PHASE-COPY: transmission factors through the token — two covers with one token
  transmit identically; the receiver's cover is REBUILT, never received (the quotient theorem as
  a transmission chain); (b) ★ DRIFT FORCED: re-covering is completion over the receiver's OWN
  trace (the S3 operator, cited); two receivers with different traces produce DIFFERENT covers
  from the SAME token — divergence is architecture (concrete kernel witness on the finite
  completion model); (c) ★ TOKEN-FREEZING: token equality survives arbitrary verbatim hops
  (iterate-of-identity) while cover equality does not survive one re-covering — the token is
  the ONLY drift-free stratum, so freezing tokens is the unique persistence mechanism the
  structure permits (the doubled layer's frozen-token role, weld to T5/T6). **Words-removable:
  factorization + finite-union + iterate facts.** Grade: theorem-route.

- **U7 — THE ASSOCIATION LATTICE (the modes and the four corners).** Two independent axes of
  association, each 3-valued with the dual-pole exclusion (each pole-exclusion docstring CITES
  its banked warrant: the maxed pole = a totalized direction = the forbidden fixed point; the
  zeroed pole = the void): the COVER axis (shared-cover intensity) and the SEAM axis
  (composition-regulation). DERIVE by kernel decide (NO native_decide): the 9-cell space; the
  all-middle cell unique in the pole-free band; the FOUR CORNERS distinct, each violating its
  named pole(s); the opponent involution swapping the corners pairwise and fixing the middle.
  ⚠ J-U7 (a de-risk finding, carried honestly): at 3 levels/axis the two healthy MODES resolve
  only as DIRECTIONS from the middle (the pole-free band is a single cell) — EITHER bank the
  3-level skeleton with the direction-reading stated in removable prose, OR build the 5-level
  refinement where the modes are regions (both are honest; the worker chooses; do NOT
  overclaim the 3-level version). **Words-removable: Fin/Finset arithmetic.** Grade:
  theorem-route (decide-grade).

- **U8 — consolidation + THE identification + the FINAL neutral handoff.** Bundle U1–U7
  (non-hollow; the U2 asymmetry and the U6 persistence theorem are the marquee cross-ties): the
  standing structure of partial gluing — two channels only (U1), bounded thick / unbounded thin
  (U2), novelty through the thin (U3), sections in/out with self-invisibility (U4), rank
  inconsistency default (U5), token-copy/cover-rebuild with forced drift and frozen-token
  persistence (U6), and the two-axis mode lattice with four excluded corners (U7). ⚠ THEN, the
  N414/R8/S8/T8 move: state the IDENTIFICATION — "the U1–U7 structure is the chain's account of
  ASSOCIATION: how bounded self-closing systems hold together, differentiate, and persist; its
  warrant is T's banked plural structure + the generativity of the one seed, never a
  derivation" — as FLAGGED PROSE, NEVER a theorem (the category-error guard, verbatim). Then
  hand ONE NEUTRAL SELECTION TICKET toward the deep-math / Clay frontier — the OWNER steers
  that personally. **The owner-authorized chain ENDS at U.** Do NOT auto-seed past U.

## 2. THE LOAD-BEARING JOINTS (the hard parts, named so you need not flail)

- **J-U1 (the inhabitation joint):** the taxonomy disjunction needs [Nonempty] on the phase
  carrier (the empty-phase degenerate case is noise, not content) — assume inhabited carriers
  throughout; do not chase the vacuous branch.
- **J-U2 (the imports joint):** the budget-bound sum comparison needs the Finset big-operators
  + ordered-sum machinery (`Finset.sum_le_sum`, `Finset.sum_const`, card lemmas) — the
  BigOperators and Order.BigOperators import families; the proof is sum-of-constants ≤ sum +
  linarith, nothing deeper.
- **J-U2b (the value trap — the arc's one drift hazard):** the capacity bound's VALUE is
  substrate. NO number appears anywhere — not as prose, not as costume, not as an "e.g.". The
  EXISTENCE theorem is the whole content. This is the arc's equivalent of the no-fit guard;
  treat a numeric here as a G2 violation.
- **J-U6 (the operator joint):** drift re-uses the S3 completion operator — CITE it; the drift
  witness is a finite-set union disagreement (kernel), not a rebuild of S3.
- **J-U7 (the resolution joint):** the 3-level lattice resolves the two healthy modes as
  directions only — state that honestly in prose or build the 5-level refinement; NEVER claim
  the modes as regions of the 3-level space (the de-risk caught this; do not re-trip it).
- **THE NO-DEFERRAL LAW (inherited):** decompose WITHIN U freely; parking a U debt beyond U is
  forbidden; the only escape is a NAMED PRECISE owner escalation. NEVER a hedge, NEVER fear.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** Every bound, every asymmetry, every corner a DERIVED landing.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** No network sizes, no group counts, no named
  studies, no tie-count figures (see J-U2b) — structure only. If one appears, it is drift.
- **G3 — DERIVED, NOT POSITED.** The taxonomy from factorization (never "there are two kinds of
  relationships"); the bound from the budget (never a "cognitive limit" posit); drift from the
  completion operator (never a "cultures change" posit).
- **G4 — SOCIAL-WORDS-REMOVABLE (the words-removable law, extended).** Delete "tie / friend /
  acquaintance / network / section / group / status / rank / culture / norm / ritual / tradition
  / solidarity / community / member / society" — every node must stand as pure mathematics:
  factorization of maps on product carriers, sum bounds, injective families, equality/difference
  of presented objects, indistinguishability facts, finite-union witnesses, iterate identities,
  Fin/Finset combinatorics. If deleting a social word leaves a gap, the node is a HOLE.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No
  `sorry`, `axiom`, `native_decide`, no `maxHeartbeats` raise. ℝ-vigilance: derived carriers and
  ℚ only — never Mathlib-ℝ as content.
- **G6 — ONE LAW, ZERO DEFERRAL.** If a link fights, the instrument/framing is wrong — reframe,
  BUILD, decompose within U. GO IN WITH NO FEAR.
- **G7 — THE IDENTIFICATION FIREWALL (inherited, unchanged).** Exactly ONE identification in
  the whole arc, at U8, flagged, in prose, warrant named — the N414 pattern verbatim. NO node
  before U8 carries ANY identification; NO theorem statement anywhere in U depends on one.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Arc U is the lightest arc yet: its
theorems are factorization facts, sum bounds, kernel witnesses, and decide-grade lattices, with
its deep legs (the S3 operator, T4 collective blindness, T2 ranking machinery, T5/T6 doubled-
layer facts) ALREADY BANKED and cited. The arc adds NO new posit: it reads the banked partial-
gluing structure at the association scale. The one identification is quarantined at U8 under
the same category-error guard as N414/R8/S8/T8. Walk every step; bank every face. When arc U
completes, hand ONE NEUTRAL SELECTION TICKET toward the deep-math frontier — the owner steers
it personally; the owner-authorized chain ENDS at U.
