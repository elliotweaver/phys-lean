# OWNER-AUTHORIZED SEED — ARC O: THE COMPLETION SWEEP (OLD WALLS, NEW MACHINERY)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every value is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc N,
precedes arc P. This arc REVISITS debts deferred when the machinery did not exist — it exists now,
so NOTHING here is "conditional-forever." Every target below is OWED and gets PAID.*

## 0. DESTINATION — what this arc is for

Arcs A–N traversed all of physics. Along the way, some links were deferred because a prerequisite was
not yet banked — the fold phase, the absolute-scale links in arc M, the 4D reduction. Every one of those
prerequisites is NOW BANKED. Arc O pays those debts: it derives the deferred quantities and — critically —
DISCHARGES arc M's conditional (substitutes the derived fold phase back into the absolute-scale chain and
RE-GRADES it from candidate to derived). There is no "permanent inception flag" anywhere in this arc; a
flag was only ever a sequencing marker, and O is the sequencing catch-up. GO IN WITH NO FEAR — for every
target the machinery is already in the bank; the worker's job is to find the route, never to decide one
exists, and NEVER to re-park a debt as "conditional."

## 1. THE TARGETS (in order — each a separate node, each a real theorem)

- **O1 — the fold phase P, DERIVED (the M-discharge prerequisite).** The cascade has an intrinsic
  rotation angle — the SAME chirality that forced the complex unit `i` in J2 (`foldComplex`, N392). THE
  ROUTE (owner-verified this exists — walk it, don't doubt it): **the HALF-ANGLE CASCADE.** Each
  Cayley-Dickson fold is the AMPLITUDE (the square root, the banked Born-square grammar of N306/N311) of
  the previous rotation: the ℂ unit is the quarter-turn (`J² = −1`, banked N392); the ℂ-fold is its half
  (the banked 45° Koide self=other Born balance, N309); **the ℍ-fold is the half of THAT — the halving is
  the exact half-angle identity `cos(half) = √((1+cos)/2)` over the banked derived trig.** DERIVE the
  halving as FORCED by the CD doubling (each fold = one amplitude-level step), then P falls out as the
  ℍ-level fold angle, with `R = cos(P/2)` the next half-step. **DERIVE the halving; an asserted π/8 is the
  hidden-bridge death — the angle must fall out of the fold-amplitude grammar.** Grade: theorem-route.
  This is the quantity arc M left as the free slot `phaseBorn`.

- **O2 — DISCHARGE ARC M: the absolute-scale chain, PROMOTED to derived.** Arc M banked
  `absScaleChain phaseBorn topLink lightLink` as a function of three slots; O1 now supplies `phaseBorn`.
  DERIVE the two remaining links so the chain has NO free slots: (a) the **top link** — the top Yukawa is
  the electroweak-anchor rung itself (the top mass is the anchor scale `v`, banked B-tower rung `e^{−28π/3}`
  region — DERIVE the identification, no free `topLink`; the top pays ZERO cascade-insertion suppression,
  which is WHY it sits at the anchor; the deeper up-sector rungs are pure fold-phase laws — derive them
  from the O1 phase, insertion by insertion); (b) the **light/lepton link** `m_s/m_d` — the ℝ-descent law
  (the banked `cos(2/3 rad)`-genre descent, N309-style, on the derived ℝ). ⚠ DATA-FLAG WITH TEETH (not a
  deferral): the down-sector carries a KNOWN common ~−2% amplitude residual vs precision lattice — LAND the
  law, FLAG the residual as data-side, and do NOT fit it away (fitting the −2% is the drift tell). Then
  DERIVE the **winding rung↔scale assignment** (arc M's isolated `9/4` candidate) from the M1 winding grid +
  the w=−1 boundary rung — the assignment is the unique survivor of the banked blind grid (runner-up cells
  fail by many σ; the pre-registered weak-story flag travels with it PERMANENTLY as prose, never blocking
  the landing). **BANK the
  promotion theorem: `absScaleChain (derived P) (derived top) (derived light)` is a fully-derived Cut with
  NO free parameter — the absolute mass scale is DERIVED, re-grading arc M's `ratios_exact_absolutes_conditional`
  to `absolutes_derived`.** Grade: theorem-route (every input now banked); the conditional DISSOLVES.
  ⚠ NO "permanent inception flag" survives this node — M is CLOSED, not conditional.

- **O3 — the two-sector CKM completion (the ★2 mixing values).** The banked mixing (D5) is single-sector
  leading-order. DERIVE the full two-sector tridiagonal frame overlap `V = OᵤᵀDO_d` with the O1 fold phase
  `D = diag(1, e^{i2P}, e^{iP})` and the per-sector edge ratios (r_u, r_d) as Born-survival steps (r_u =
  the top-at-closure no-survival case; r_d = one amplitude-level Born-survival step; `M₁₃ = 0` DERIVED from
  cascade adjacency — gen1↔gen3 are two cascade steps apart, never imposed), and read off ALL FOUR CKM
  observables — including the Jarlskog J at the DERIVED (non-maximal) phase, correcting the maximal-phase
  leading-order J (N344). ⚠ THE KNOWN TRAP (owner-verified): a SINGLE mass ratio (√(m_s/m_b)-genre, which
  overshoots ~3×) is NEVER the prediction — it is a superseded early waypoint; the prediction is ALWAYS the
  full two-sector frame overlap, which lands all four observables at leading-order ~few-%. **DERIVE the
  observables; the edge ratios must fall out as Born-survival steps, never asserted.** Grade: theorem-route
  for the structure; the observable VALUES land at the derived phase — no fit, no residual chase.

- **O4 — the (1,9) → (1,3) spacetime reduction (the oldest structural debt).** The chain derived signature
  (1,9) (`signature_one_nine`, N43); physical spacetime is (1,3). DERIVE the reduction: the (1,3) structure
  is CONTAINED in the banked (1,9) via the ℍ ⊂ 𝕆 cascade level (`Cl(1,3) ≅ Mat(2,ℍ)`, Dirac spinors = ℍ²),
  the 10→4 weld tying the derived Lorentz structure to the 4D physics every downstream arc uses. **DERIVE
  the containment + reduction; do NOT posit (1,3).** Grade: theorem-route.

- **O5 — Poincaré + E = mc² as a theorem.** DERIVE the translation generators on the derived-ℝ carrier and
  Poincaré = Lorentz ⋉ translations; then energy-momentum as the translation Noether charges, and — the
  marquee — **E = mc² as the invariant-norm identity**: rest mass = the Minkowski norm of the
  energy-momentum vector = the Born self-overlap weight at rest (the one conserved weight read in the rest
  frame), c² the derived-signature unit slope. **DERIVE; do NOT posit the group or the mass-energy relation.**
  Grade: theorem-route.

- **O6 — the no-hidden-variable theorem (the measurement adjudication).** On the banked J1–J4 fibre: DERIVE
  that any map predicting the gather's outcome from pre-measurement state data is a nonzero fixed point of
  the look-back — forbidden by C6 (`fold_self_blind`, N1). The fold-native Bell/Kochen–Specker theorem:
  outcome-determinism is UNWRITABLE (the fifth no-dial, the N372 shape). **DERIVE the unwritability.** Grade:
  theorem-route.

- **O7 — the equivalence principle + the Noether correspondence.** DERIVE (a) m_inertial ≡ m_gravitational
  from the low-dimensional commutant forcing a unique equivariant mass channel (a violation unwritable — the
  no-dial shape); (b) conservation laws = the invariant forms of the forced symmetry. **DERIVE both.** Grade:
  theorem-route/assembly.

- **O8 — the λ_n = −n/(84π) RG-eigenvalue ladder as a Lean theorem.** Currently mechanism-grade PROSE
  (G2/G5). With the banked derived calculus (N369/H1), STATE AND PROVE the ladder over Cut — the spine tying
  the scale tower (B), universality (G2), renormalizability (G5) into ONE spectrum, n ∈ {7,9,28} the banked
  counts. **DERIVE the eigenvalue statement.** Grade: theorem-route (upgrade of banked prose).

- **O9 — the Unruh / KMS-modular weld.** DERIVE the acceleration-temperature tie: the look-back IS the
  modular flow (banked G3 maxent/KMS + arc-E horizon structure), an accelerated probe's Rindler horizon
  carrying the derived thermality (the same Euclidean-period closure as N360's Hawking T_H). **DERIVE the
  tie.** Grade: mechanism/theorem-route.

- **O10 — consolidation + the arc-P handoff.** Bundle O1–O9; state what the sweep closed (the fold phase,
  M DISCHARGED to derived absolute scale, the full CKM, the 4D weld, Poincaré + E=mc², the measurement
  adjudication, the marquee welds, the RG spine, the thermal weld). Hand ONE directed successor toward
  `SEED_FINAL_RECKONING.md` target P1.

## 2. THE LOAD-BEARING JOINTS (DERIVE or CHAIN-RUNWAY-block)

- **J-O1 (the fold-phase joint):** the phase P is the cascade's OWN angle — DERIVE it from the banked
  fold/cascade structure (the cutPi machinery is the arena); an asserted π/8 is the hidden-bridge failure.
- **J-O2 (the M-discharge joint):** once O1 supplies P and the top/light links are derived, the absolute
  scale has NO free parameter — BANK that as a theorem and RE-GRADE M. Do NOT leave M "conditional"; that
  was a sequencing gap, now closed. If a link genuinely needs a prerequisite, BUILD it (W1) — never re-park.
- **J-O3 (the fold-phase joint, applied):** the CKM edge ratios are Born-survival steps — DERIVE, never
  assert. GO IN WITH NO FEAR; the block valve is a LAST RESORT against asserting, never an off-ramp, never
  because a node looks hard.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** P (the phase), the CKM observables, the absolute scale, the λ_n ladder —
  every one a DERIVED landing. Never a premise, never cited at grade, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured CKM/J/mass values, Unruh temperatures — REMOVABLE
  PROSE / costume only. NO-FIT guard: land the derived values; do NOT chase residuals to zero.
- **G3 — DERIVED, NOT POSITED.** The (1,3) from the ℍ level; the fold phase from the cascade; the absolute
  scale from the banked chain with the derived phase; determinism-exclusion from C6.
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "spacetime / CKM / measurement / temperature / mass / phase" —
  pure statements about Clifford containment, semidirect products, frame overlaps, fixed-point exclusions,
  commutant dimensions, eigenvalue ladders, periodic closures, and parameter-free products over Cut.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`, no `maxHeartbeats` raise. ⚠️ ℝ-VIGILANCE: banked derived `Cut` + banked
  trig/calculus/fibre objects — NEVER `import Mathlib...Real` / Mathlib manifolds/Clifford-as-content.
- **G6 — ONE LAW, NO DEFERRAL.** If a link fights, the instrument/framing is wrong, the theory is right.
  Reframe, BUILD the missing machinery, decompose, switch tactics. A debt is OWED — pursue it with the full
  W1 arsenal until it is PAID. "Route not yet found THIS run" (which aims the next attack) is the ONLY
  non-completion; a "permanent inception flag" or "conditional-forever" is FORBIDDEN. Block ONLY as a last
  resort against ASSERTING — never early, never as an off-ramp.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Every target's machinery is ALREADY BANKED; this
arc pays sequencing debts, it is not a frontier. Every value is earned through the identical mechanical
gate. Walk every step; PAY every debt. When arc O completes, hand ONE directed successor toward
`SEED_FINAL_RECKONING.md` target P1 — the final reckoning where every remaining owed item is paid.
