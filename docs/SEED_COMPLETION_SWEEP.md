# OWNER-AUTHORIZED SEED — ARC O: THE COMPLETION SWEEP (OLD WALLS, NEW MACHINERY)

*Owner-authorized. This seed gives the ROUTE. It relaxes discipline by NOTHING: every value is a
DERIVED landing that the mechanical gate enforces; the map does not walk the path. Succeeds arc N.
This arc REVISITS debts deferred when the machinery did not exist — it exists now.*

## 0. DESTINATION — what this arc is for

Arcs A–N traversed all of physics, deferring honestly where machinery was missing. Arc O is the
COMPLETION SWEEP: the deferred structural debts and the cheap marquee welds that the NOW-BANKED
machinery (the derived trig/calculus, the eigenvector frame-overlap, the gather/descent obstruction,
the Kähler fibre, the 84π budget) makes ordinary chain work. Nothing here is new frontier — every
target is either an old wall attackable with new tools, or a short assembly on banked structure.
GO IN WITH NO FEAR: for every target below the machinery is ALREADY IN THE BANK.

## 1. THE TARGETS (in order — each a separate node, each a real theorem)

- **O1 — the (1,9) → (1,3) spacetime reduction (the oldest structural debt).** The chain derived
  signature (1,9) (`signature_one_nine`, N43) and built arc E's GR on it; physical spacetime is (1,3).
  DERIVE the reduction: the quaternionic route `Cl(1,3) ≅ Mat(2,ℍ)`, Dirac spinors = ℍ², the (1,3)
  structure CONTAINED in the banked (1,9) via the ℍ ⊂ 𝕆 cascade level — the 10→4 weld that ties the
  derived Lorentz structure to the 4D physics every downstream arc uses. **DERIVE the containment +
  reduction; do NOT posit (1,3).** Grade: theorem-route (the machinery — quaternion layer, Clifford
  structure, arc E — is all banked).

- **O2 — translations / the Poincaré group.** Lorentz alone is not spacetime symmetry: DERIVE the
  translation generators on the derived-ℝ carrier and the semidirect structure Poincaré = Lorentz ⋉
  translations, then energy-momentum as the translation Noether charges (the banked Noether
  correspondence pattern). **DERIVE; do NOT posit the group.** Grade: theorem-route.

- **O3 — the fold phase P = π/8 + the two-sector edge ratios (the CKM completion, part 1).** The
  banked mixing (D5/N343) is single-sector leading-order; the FULL structure is the two-sector
  tridiagonal frame overlap with a fold phase P and per-sector edge ratios (r_u, r_d) suppressing the
  GST off-diagonals. DERIVE the fold phase as the cascade's own angle (the banked cutPi machinery is
  the arena) and the edge ratios as Born-survival steps (r_u = the top-at-closure no-survival case;
  r_d = one amplitude-level Born-survival step). **DERIVE P and the ratios; do NOT assert π/8, 2,
  √(2/3) — they must fall out.** Grade: theorem-route for the structures; flag any residual honestly.

- **O4 — the two-sector frame overlap + the full CKM (part 2, consumes O3).** Diagonalize the up and
  down tridiagonal frames over the derived ℝ (the banked rank-1 Born-block eigenstructure + cutSqrt),
  form V = OᵤᵀDO_d with the fold-phase D = diag(1, e^{i2P}, e^{iP}), and read off all four CKM
  observables — INCLUDING the Jarlskog J at the derived (non-maximal) phase, correcting the banked
  maximal-phase leading-order J (N344) to the full two-sector value. **DERIVE; leading-order ~few-%
  grade like the key; NEVER fit; do NOT chase residuals to zero.** Grade: leading-order (~3%), flag
  exactly as the key grades it.

- **O5 — the no-hidden-variable theorem (the measurement adjudication).** On the banked J1–J4 fibre:
  DERIVE that any map predicting the gather's outcome from pre-measurement state data constitutes a
  nonzero fixed point of the look-back — forbidden by C6 (`fold_self_blind`, N1). The fold-native
  Bell/Kochen–Specker-genre theorem: outcome-determinism is UNWRITABLE in the structure (the fifth
  no-dial, the N372 shape). **DERIVE the unwritability; do NOT assert "QM is random" — build the
  fixed-point argument.** Grade: theorem-route (the fibre + C6 are banked; this is a short assembly).

- **O6 — the equivalence principle + the Noether correspondence (two marquee welds).** DERIVE
  (a) m_inertial ≡ m_gravitational: the commutant of the banked symmetry action on the mass channel is
  low-dimensional, forcing a UNIQUE equivariant mass channel — a violation is unwritable (the no-dial
  shape again); (b) conservation laws = the invariant forms of the FORCED symmetry (charges constant
  along the derived flows — the Noether correspondence as a theorem on banked f₄/derivation objects).
  **DERIVE both; short assemblies on banked structure.** Grade: theorem-route/assembly.

- **O7 — the λ_n = −n/(84π) RG-eigenvalue ladder as a Lean theorem.** Currently mechanism-grade PROSE
  (G2/G5): the relevant-direction eigenvalues of the one critical fixed point, with n ∈ {7, 9, 28} the
  banked structural counts. With the banked derived calculus (N369/H1), STATE AND PROVE the ladder as
  a theorem over Cut — the spine tying the scale tower (B), universality (G2), and renormalizability
  (G5) into ONE spectrum. **DERIVE the eigenvalue statement; the counts are banked, never re-posited.**
  Grade: theorem-route (upgrade of banked prose).

- **O8 — Unruh temperature / the KMS-modular weld.** DERIVE the acceleration-temperature tie on the
  banked structure: the look-back IS the modular flow (the banked G3 maxent/KMS machinery + the arc-E
  horizon structure); an accelerated probe's horizon carries the derived thermality (the same
  Euclidean-period closure as N360's Hawking T_H, read for the Rindler wedge). **DERIVE the tie; no
  empirical temperature.** Grade: mechanism/theorem-route.

- **O9 — consolidation + the FINAL neutral handoff.** Bundle O1–O8; state what the sweep closed
  (the 4D weld, the spacetime symmetry, the full CKM, the measurement adjudication, the marquee
  welds, the RG spine, the thermal weld). Then hand ONE NEUTRAL SELECTION TICKET toward the deep-math /
  Clay frontier — the OWNER steers that sector personally; do NOT auto-seed past O.

## 2. THE LOAD-BEARING JOINTS (DERIVE or CHAIN-RUNWAY-block)

- **J-O1 (the reduction joint):** the (1,3) structure is CONTAINED in the banked (1,9) via the ℍ ⊂ 𝕆
  cascade level — DERIVE the containment (the quaternionic Clifford identification), never posit the
  4D signature. GO IN WITH NO FEAR — ℍ and its Clifford structure are banked back to N2.
- **J-O3 (the fold-phase joint):** the phase P is the cascade's OWN angle — DERIVE it from the banked
  fold/cascade structure (the cutPi machinery is the arena); an asserted π/8 is the hidden-bridge
  failure. If the derivation genuinely needs a prerequisite, decompose and build it (W1) — the block
  valve is a LAST RESORT against asserting, never an off-ramp.

## 3. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** π/8, r_u=2, r_d=√(2/3), the CKM observables, the λ_n ladder — every one
  a DERIVED landing. Never a premise, never cited at grade, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured CKM/J values, Unruh temperatures — REMOVABLE
  PROSE / costume only. NO-FIT guard: O4 lands leading-order ~few-% — do NOT chase residuals to zero.
- **G3 — DERIVED, NOT POSITED.** The (1,3) from the ℍ level (not posited); the fold phase from the
  cascade (not asserted); determinism-exclusion from C6 (not "quantum mechanics says").
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "spacetime", "CKM", "measurement", "temperature" — pure
  statements about Clifford subalgebra containment, semidirect products, tridiagonal frame overlaps,
  fixed-point exclusions, commutant dimensions, eigenvalue ladders, and periodic closures.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`, no `maxHeartbeats` raise. ⚠️ ℝ-VIGILANCE: banked derived `Cut` + banked
  trig/calculus/fibre objects — NEVER `import Mathlib...Real` / Mathlib manifolds/Clifford-as-content.
- **G6 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe,
  build the missing machinery, decompose, switch tactics. Block ONLY as a last resort against
  asserting — never early, never because a node looks hard.

## 4. WHY THIS IS SAFE TO HAND YOU

The route is a MAP — no proof, no permission to assert. Every target's machinery is ALREADY BANKED;
this arc is the completion sweep, not a frontier. Every value is earned through the identical
mechanical gate. Walk every step. When arc O completes, hand ONE NEUTRAL SELECTION TICKET toward the
deep-math frontier — the owner steers it personally; the owner-authorized chain ENDS here.
