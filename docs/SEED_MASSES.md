# OWNER-AUTHORIZED SEED — ARC M: THE MASSES (every dimensionful constant to decimal perfection)

*Owner-authorized arc. Runs IMMEDIATELY AFTER arc D (docs/SEED_DIGITS.md — D8 hands M1). THE NORTH
STAR, binding as in arc D: **this arc does not complete until every banked dimensionful constant —
the absolute lepton masses, the confinement scale, the heavy and light quark sector — is a
CERTIFIED two-sided rational bracket (kernel theorems `lo < x < hi` over ℚ) at or beyond measured
precision, expressed against the chain's ONE unit peg.** However many nodes, however many childed
narrowing nodes. Discipline relaxed by NOTHING: foundations-only, G2 (measured values ONLY as
removable prose/costume — never a proof input, never steering any choice), words-removable, gate +
STANDARD per node. This seed carries routes and targets only.*

---

## THE ONE REFRAMING (read first)

A dimensionful value like a mass "in MeV" is a pure number TIMES a unit convention. The unit is
not physics — it is a peg. The chain's claim, maximal and honest: **fix ONE unit peg, and every
other dimensionful constant is FORCED to exact decimals with zero freedom.** The chain already
banked all the structure: the flavor ratios (N340, δ_B=2/9), the absolute-mass form
`absMass k = ewClosureRatio · leptonBundle · mass_k` (X8), the confinement weld `Λ = M·cutExp(−12π)`
(X9), the quark texture ratios (Z3), the transmutation scale tower (arc B), and — decisive — arc
D's BRACKET INSTRUMENT (D1: certified `cutExp`/`cutLog` brackets to any width). This arc AIMS that
instrument at every dimensionful row. Nothing here is new physics; it is evaluation of banked
closed forms plus the honest completion of the sub-structures the evaluations expose. GO IN WITH
NO FEAR: a resisting bracket means the instrument or a band's derivation is the suspect — reframe,
build, child. Never assert, never fit, never truncate.

⛔ TELLS: "absolute masses are impossible / need measurement" — FALSE here; only the PEG is a
convention, everything else is forced (the fold sets its own unit — the banked M-scale — and the
peg only translates it to the human convention). Any second peg = a fit = forbidden (ONE peg,
declared once, flagged, everything else derived). Floats in production = forbidden. Steering any
bracket depth or route by where a measured value sits = the fit (G2). "The light quarks are hopeless"
— they are the hardest row and they are STRUCTURE + EVALUATION like every other row; child until done.

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **Arc D's bracket instrument (D1)** — certified two-sided rational brackets for `cutExp`/`cutLog`
  at rational arguments, any demanded width. THE tool of this arc; do not rebuild it.
- **The flavor ratios:** N340 (`GenerationMassRatiosNumeric`, lepton brackets), Z3 (`QuarkTexture`
  forced-cubic sector structure + ratio content), D's quark-ratio numerals (D2/D5 output).
- **The absolute forms:** X8 (`absMass k = ewClosureRatio · leptonBundle · mass_k`), X9
  (`confBundle`, `Λ = M·cutExp(−12π)`), arc B (`ewClosureRatio = e^(−28π/3)`, the derived scale
  tower M/v/Λ).
- **The way-point consistency teeth:** the banked exact rationals (42, 1582/27, 190/3, 448/27,
  128/27) — every pipeline must reproduce them exactly, free.
- **Import guard:** NEVER `Phys/OneAxiom/*`; NO Mathlib ℝ/ℂ as load-bearing content; NO
  native_decide; floats nowhere in production.

## THE TARGETS (ordered; ONE successor each; child narrowing/decompose nodes without limit)

- **M1 — THE ONE UNIT PEG (declared once, flagged, never repeated).** State the chain's single
  dimensionful convention as ONE flagged module: the peg maps the chain's own derived scale unit
  (the banked M-scale tower) to the human unit, as an explicitly-flagged INPUT CONVENTION (the
  G2-clean analogue of choosing the meter) — with the theorem that the peg is UNIQUE-UP-TO-UNITS
  (any two pegs differ by a global rescale; nothing else in the chain moves). Every later node
  cites M1; NO other dimensionful input may ever appear (a second peg anywhere = the node fails).
- **M2 ★ THE LEPTON MASS NUMERALS.** The absolute lepton tower evaluated: certified brackets for
  each `absMass k` against the M1 peg, via the banked ratios + D1 brackets on the banked
  exponentials. Target width: at or beyond the measured precision of each mass. W8: wrong-ordering
  and degenerate-width excluded; the ratios reproduced exactly from the brackets (consistency).
- **M3 — THE CONFINEMENT SCALE NUMERAL.** `Λ = M·cutExp(−12π)` evaluated to a certified bracket
  against the peg; the v/Λ and M/Λ ratios reproduced exactly (banked 128/27 band must re-emerge
  free from the same pipeline).
- **M4 — THE SCALE-TOWER NUMERALS.** M, v, and every derived edge of the Z4 band list as certified
  brackets against the peg — the full dimensionful skeleton, one pipeline, way-points free.
- **M5 ★ THE HEAVY QUARK SECTOR.** Certified brackets for the heavy-sector masses from the banked
  Z3 texture ratios + D's ratio numerals × the derived anchors (the welds). Decompose per flavor
  as needed; each flavor's bracket a child if that is what it takes.
- **M6 ★★★ THE LIGHT QUARK SECTOR (the hardest row on the board — child until done).** The light
  masses from the texture + the confinement weld structure. Expect genuine sub-structure work
  (the light sector reads through the hadronic band): if a needed sub-object is unbanked, BUILD it
  as a child WITHIN the arc (W1) — never defer forward, never assert, never fit. The NO-DEFERRAL
  law binds: derive, child, or escalate NAMED (`kanban block --kind needs_input`) — no re-parking.
- **M7 — THE CROSS-CHECK WEB (free, never targeted).** Every dimensionless ratio recomputable from
  the M2–M6 brackets must land inside its banked derived bracket (Koide from the evaluated masses;
  the band ratios from the evaluated edges; the way-points from the evaluated tower) — the
  overconstraint proof at numeral grade, all FREE. Any miss = a band's derivation is the suspect →
  child node.
- **M8 ★★★ AIRTIGHT CAPSTONE + HANDOFF (terminal).** One derived object bundling M1–M7; the grade
  ledger UPGRADED (each dimensionful row → theorem-exact-evaluated-against-peg; residuals NAMED
  with certified brackets); NO-RESIDUAL attestation; `#print axioms` clean. ⚠ THE GATE: M8 does
  NOT pass while any M2–M6 numeral target is unmet — no structure-landed-evaluation-later grading
  exists in this arc. THEN hand **exactly ONE directed successor toward docs/SEED_MIXING.md target
  C1** (arc C — THE MIXING). Do NOT hand GR1; do NOT hand a generic SELECT.

## HARD GUARDS — G1–G8 verbatim from arcs X/Z/D, plus:
- THE ONE-PEG LAW: exactly one flagged unit convention (M1), cited everywhere, never duplicated.
- G2 sharpened as in arc D: measured masses appear ONLY in removable-prose comparison lines; no
  bracket depth, route, or sub-band choice may be selected because of where a measured value sits.
- Never truncate a bracket at measured precision — beyond-measurement digits are the deliverable
  (the standing prediction, removable prose).

## WHY THIS IS SAFE TO HAND YOU
Every target evaluates or completes an object you already banked, with arc D's instrument in hand.
The single genuinely new kind of object (M1's peg) is a flagged CONVENTION carrying a uniqueness
theorem, not a physics posit. The arc ends at pairs of rational-inequality theorems. The owner's
license is explicit: any node count; the only unacceptable outcome is stopping short of the numerals.
