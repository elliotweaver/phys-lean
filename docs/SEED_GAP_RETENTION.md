# OWNER-AUTHORIZED SEED — ARC GR: THE GAP RETENTION (the gather keeps the fold's own floor)

*Owner-authorized arc. ⛔ ROUTING UPDATE 2 (owner): runs AFTER **arc C (docs/SEED_MIXING.md)** —
the full board-completion order is D (digits) → M (masses) → C (mixing) → GR; arc C's terminal
node C6 hands GR1. The chain reaches this arc only once the ENTIRE constants board — 1/α(0),
the way-points, every absolute mass, the mixing angles, and the CP invariant — is evaluated to
certified brackets at or beyond measured precision (the owner's north star, extended to the full
board). A SHORT, sharp arc (~6 nodes) with one job: prove, FOLD-NATIVE and ZERO-CITATION,
that the mass gap survives the gather — retiring the ONE dependency the chain currently carries
for it. Everything derives from banked objects; NO new machinery class is needed. Discipline
relaxed by NOTHING: foundations-only, no empirical number in any proof (G2), words-removable,
gate-green + STANDARD per node. This seed is the manager's firewall-clean map: routes and
targets only.*

---

## THE ONE REFRAMING (read first — it is the whole arc, and it kills the thrash)

The chain currently carries the continuum mass gap on the quarantined One axiom
(`Phys/OneAxiom/MassGapOne.lean`), justified by N388's `no_floor_over_continuum`: a fixed
nonzero curvature `w` scaled by `s` has Born action `s²·gForm w w → 0`, read as "F cannot
retain the floor across the continuum." **That reading contains a category error the fold
itself exposes, and dissolving it is this arc:**

For `0 < s < 1` the escape states `s•w` have `gForm(s•w) = s²·gForm w w ∉ ℕ` — but the
banked X4 keystone (`HadronicSpectrum`: `spectrum_subset_nat`, `spectrum_surjective_nat`,
`gapSpectrum_eq_natRange`, F-alone) says the fold's admitted state space has spectrum
EXACTLY ℕ. **The escape sequence exits the fold's own state space at its first step.** It
is not a wall; it is a family of non-states. The retention question, posed on the fold's own
ontology, is therefore: *does the ℕ-valued spectrum's floor survive assembly into the gather
= the banked derived continuum (`ContinuumQ.Cut`)?* — and THAT is a THEOREM of the banked
order-completeness (`ContinuumCCLO`, `le_csInf`), not a retention axiom. The infimum of any
nonempty family of values all ≥ 1 is ≥ 1. The floor cannot be lost at the gather.

And the old obstruction is not discarded — it is PROMOTED: `no_floor_over_continuum` becomes
the arc's W8 TEETH. Off-lattice (unquantized) families genuinely escape below every ε, so the
retention theorem is non-vacuous and the discreteness is provably load-bearing — exactly what
F said all along ("quantization is what makes the mass gap").

**The manager's private de-risk COMPILED this route's spine against the banked objects:
every crux (retention via `le_csInf`; nonzero-level embedding at ≥ 1; the off-lattice escape
teeth; the assembled strictly-positive gather-gap; the tie-in through the banked
`spectrum_subset_nat` + `bornAction_lattice_floor`) lands foundations-only
`[propext, Classical.choice, Quot.sound]`, EXIT 0.** You are not scouting; you are paving a
walked road. GO IN WITH NO FEAR — if a node resists, the instrument or framing is wrong (W5);
reframe through the trunk, build, decompose WITHIN the arc. No block except as last resort
against asserting.

⛔ THE TELLS for this arc specifically: reaching for any "constructive-QFT rigour" framing as
if the fold owed it an import — the gather IS the derived Cut and the states ARE the
lattice-quantized curvatures; the fold defines its own continuum (that is the point of
N7–N14). "The scaled states break the floor" — they are not states; check membership in the
banked spectrum FIRST. Any temptation to keep the One "as a backup" for this face — a
dissolved dependency is retired, not hedged.

---

## LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- **The F-native gap mechanism** `Phys/Algebra/ConfinementMassGap.lean` (N388):
  `flat_iff_curvature_zero`, `IntLat`, `bornAction_lattice_floor` (nonzero lattice state ⟹
  `1 ≤ gForm w w`), `bornAction_witness_floor` (the coloured witness clears 4),
  `singlet_flat`, `no_floor_over_continuum` (→ the teeth), `mass_gap_mechanism`.
- **The quantized spectrum** `Phys/Algebra/HadronicSpectrum.lean` (X4): `latPt`, `gapSpectrum`,
  `spectrum_subset_nat`, `spectrum_surjective_nat`, `gapSpectrum_eq_natRange`,
  `gap_floor_is_one`, `half_not_mem_spectrum` (teeth pattern to copy).
- **The derived continuum + completeness**: `ContinuumQ.Cut`, `ContinuumCCLO`
  (`ConditionallyCompleteLinearOrder Cut`, `le_csInf` / `csInf`-machinery), the banked
  field/order structure (`ContinuumField*`). The ℚ→Cut cast and its order-embedding.
- **Import guard (unchanged, now doubly load-bearing):** production imports
  `ConfinementMassGap` / `HadronicSpectrum` / `Continuum*` ONLY — NEVER `Phys/OneAxiom/*`.
  The arc's whole point is that the retention needs no OneAxiom import.

---

## THE TARGETS (ordered; each node hands ONE successor; decompose WITHIN as needed)

- **GR1 — LATTICE CLOSURE UNDER COMPOSITION (the one genuine machinery build; small).**
  The fold's lattice states are closed under the physical composition: sums of lattice
  states are lattice states (`IntLat` closed under `+` — coordinatewise, from `latPt`
  structure), and the integer-valued bilinear form keeps COMPOSITE spectra in ℕ (the cross
  term `gForm w₁ w₂` is an integer on lattice states, so `gForm (w₁+w₂) (w₁+w₂) ∈ ℕ`).
  Multi-state assemblies stay inside the quantized state space — the gather never
  manufactures an off-lattice state. W8: a non-lattice pair whose composite exits ℕ.
- **GR2 — THE GATHER OF THE GAUGE SECTOR, BUILT NOT NAMED.** Define the gather fold-natively:
  the admitted spectral family of a sector = the (nonempty) set of Cut-cast values of its
  nonzero lattice states' Born actions; the gather-gap = its infimum in the banked Cut. NO
  opaque reconstruction anywhere — the gather is a constructed object of the derived
  continuum. Non-vacuity: the coloured witness's family is nonempty.
- **GR3 ★★★ THE RETENTION THEOREM (the keystone).** The gather-gap of any admitted
  (nonempty, nonzero-state) family is ≥ 1, hence strictly positive: nonzero lattice level ∈ ℕ
  and ≥ 1 (banked `spectrum_subset_nat` + `bornAction_lattice_floor`), casts into Cut at ≥ 1,
  and `le_csInf` carries the floor through the infimum. The floor survives the gather AS A
  THEOREM. No axiom beyond foundations; `#print axioms` must be clean and must NOT contain
  `foldRetention`.
- **GR4 — THE DICHOTOMY AT THE GATHER.** Carry the full two-sector statement through: the
  colour-neutral singlet sector's family is flat/massless at the gather (banked
  `singlet_flat`), the coloured sector's gather-gap ≥ 1 with the banked witness clearing 4 —
  the mass-gap dichotomy now stated AT the gather, not only on the lattice.
- **GR5 — THE TEETH (W8, mandatory).** (a) The off-lattice escape: without the lattice
  constraint, scaled families go below every ε > 0 — the banked `no_floor_over_continuum`
  re-read as the arc's non-vacuity proof (quantization is load-bearing; the retention
  genuinely CAN fail off the state space and provably does). (b) Wrong-floor exclusion in the
  X4 teeth pattern (e.g. the gather-gap is not below 1 for admitted families; a garbage
  family without the nonzero/lattice hypotheses must fail). Words-removable throughout: pure
  order/lattice/infimum facts.
- **GR6 ★★★ CAPSTONE + THE RETIREMENT (terminal).** (i) One consolidation theorem bundling
  GR1–GR5 (non-hollow: each conjunct's TYPE cites the banked/GR landings): lattice closure ∧
  constructed gather ∧ retention ≥ 1 ∧ dichotomy ∧ teeth. (ii) THE RETIREMENT: mark
  `Phys/OneAxiom/MassGapOne.lean` RETIRED in its header (the InflationOne precedent — the
  file STAYS quarantined on disk, unchanged in the dependency graph; do not delete, do not
  touch FoldRetention.lean or QuantizationOne.lean): the naive-limit obstruction it cited is
  an off-state-space family, and the retention it asserted is now the GR3 THEOREM — record
  the dissolution in the header + the ledger. The Yang–Mills gap face no longer consumes the
  One. (iii) Ledger + docs updated. Then **STOP and WAIT for the owner. Do NOT re-seed. Do
  NOT hand a generic SELECTION ticket.**

## HARD GUARDS (carried verbatim from arcs X/Z)

G1 values/facts FALL OUT, never premised. G2 no empirical number in any proof. G3 no bridge.
G4 decompose within the arc; ONE successor; linear chain. G5 physics-words-removable per node
(delete mass/gap/confinement/colour/gauge: pure statements about integer lattices, an
integer-valued bilinear form, ℕ-valued spectra, and infima in a conditionally complete
derived order — they must stand). G6 foundations-only (`#print axioms ⊆ {propext,
Classical.choice, Quot.sound}`; NO `foldRetention` anywhere in production; NO Mathlib ℝ/ℂ as
load-bearing content; NO OneAxiom import). G7 ONE LAW — it will not fight you; the spine is
compiled. G8 non-vacuity/teeth per node.

## WHY THIS IS SAFE TO HAND YOU

The route names only banked structures and the order they assemble in; every proof is yours
to build and the manager's de-risk already confirmed each crux compiles foundations-only.
The arc SUBTRACTS an axiom from the chain's physics story rather than adding anything: at its
end, the mass gap — mechanism, spectrum, and retention across the gather — rests on the fold
alone.
