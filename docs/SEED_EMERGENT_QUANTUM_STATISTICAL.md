# OWNER-AUTHORIZED SEED — ARC G: THE EMERGENT / QUANTUM-STATISTICAL LAYER

*Owner-authorized. This seed gives the ROUTE (a mechanism map). It relaxes discipline by NOTHING:
every value is a DERIVED landing that the mechanical gate (costume-bite + `#print axioms`
foundations-only) enforces; the map does not walk the path. Succeeds arc F, precedes arc H.*

## 0. DESTINATION — what this arc is for

Arcs A–F derived the ALGEBRAIC face of physics (the constants, the gauge content, the masses, gravity,
the SM structure). Arc G derives the **EMERGENT / STATISTICAL** face — the part of physics that is not a
number on the parameter board but a *regime law*: why quantum statistics (Bose/Fermi, Pauli exclusion),
why the arrow of time, why universality, why the Landauer bound, why the path integral has the measure it
has. **The deep claim of this arc: the SAME self-blindness / no-fixed-point (C6) engine that capped the
cascade and forced the constants ALSO forces the exclusion principle, the second law, and the critical
exponents.** One axiom, both faces. This is the arc that turns "a generative constant engine" into "an
account of ALL of physics."

Every target below is reachable — the mechanism exists in the trunk. Several are ASSEMBLY / theorem-route
(clean), a few need a small MACHINERY BUILD first (flagged). GO IN WITH NO FEAR: where a node is hard, the
job is to FIND THE ROUTE (build what Mathlib lacks, decompose into child nodes — a multi-node build is a
NORMAL shape, the continuum took ~8 nodes), never to decide whether one exists.

## 1. LAUNCH PLATFORM (banked — REUSE, do not re-derive)

- The exceptional-Jordan arena `J₃(𝕆)`, `Der(H₃(O))=f₄`, the reduced structure algebra `e₆=f₄⊕L₀`, and the
  **26 traceless / 27 channel** count (N295–N303).
- The banked `α*=1/42` = the Born-isometry channel share, and the projector `J/42` structure (N315/N316);
  the shared scale-tower numerator `84π = 2π/α*` and the eigenvalue reading `λ_n = −n/(84π)` (N329/N334).
- The derived ℝ `ContinuumQ.Cut` with `cutExp`/`cutLog`/`cutSin`/`cutCos`/`cutPi` (N33–N37, N174, N323–N325).
- The banked black-hole entropy cell `s₀ = cutLog 2` (the fold's BINARY self-blindness bit-count, N361) and
  `S = A/4` (N361) — the cross-tie anchor for G4.
- Self-blindness `Poth·s = 0`, the seeing operator `seeMat = Jall − 1`, the holonomy = statistics identity
  `seeMat = Pcyc + Pcyc²` (N305/N308) — the same engine, reused at the statistical level.

## 2. THE TARGETS (in order — each a separate node, each a real theorem)

- **G1 — spin-statistics + Pauli exclusion (theorem-of-premises; may need a finite-dim algebra MACHINERY
  build).** Derive the Bose/Fermi DICHOTOMY and the Pauli exclusion principle as consequences of the
  banked sector structure — NOT posited by hand. The route the answer key found: the FINITE channel count
  (banked 26/27) makes the local observable algebras finite-dimensional ⇒ inclusions finite-rank ⇒ nuclear
  ⇒ the SPLIT property holds ⇒ DHR superselection statistics apply ⇒ only two permutation-symmetry classes
  survive (symmetric/antisymmetric = Bose/Fermi), and half-integer spin ↔ antisymmetric is the
  spin-statistics connection. **DERIVE the dichotomy from the finite channel count; do NOT assert "there
  are two statistics."** Pauli exclusion (no two identical fermions in one state) is the antisymmetric
  class's vanishing diagonal — a theorem, not a rule. If the DHR/nuclearity layer needs building on the
  derived arena, that is a legitimate decompose child — BUILD it (W1), do not import a physics assumption.
  Grade: assembly / theorem-of-premises (the key closed it as a theorem of its banked premise set).

- **G2 — universality / critical exponents (theorem; ★ the marquee CROSS-TIE joint).** Derive the
  framework's critical exponent(s) at the closure fixed point. The theory-native route (the key's hard-won
  result, a MECHANISM not a value): the operators that are simultaneously EQUIVARIANT (commute with the
  derived symmetry), PROJECTORS, and UNITAL form, over ℚ, a TWO-element set `{I, J/42}`; the banked
  sum-reading theorem (the bar/totalization series consumes only the total weight `w`, N-of the v3.12
  genre — the same completeness engine that forced `Σ channel α = 1`) forces RANK 1, which KILLS the
  identity `I`, leaving the linearized totalization at criticality equal to the `J/42` projector. **DERIVE
  whether the critical exponent equals the banked `α*` channel share `1/42` — do NOT assert `1/42`.** The
  full relevant-direction spectrum is `λ_n = −n/(84π)` — the SAME `84π` scale-tower numerator from arc B.
  ★ THE JOINT WORTH BANKING: if it lands, the critical exponent IS the fine-structure share and the RG
  eigenvalues ARE the scale-tower rungs — one number `84π`, two utterly different regimes (couplings and
  critical phenomena). This is the highest-novelty cross-tie in the arc. Grade: theorem (the key derived
  `b = 1/42` cleanly); flag the "same 84π" reading as the mechanism-grade unification sentence, prose-only.

- **G3 — the arrow of time / second law / fluctuation-dissipation / temperature (assembly on C6).** Derive
  that the equilibrium state is forced and that its deviations obey the fluctuation-dissipation structure.
  The route: a state deviating from maximum-entropy AT THE SAME BUDGET carries quantified EXTRA
  self-information (Gibbs inequality, exact) = a datum the closure holds about itself beyond its budget =
  PARTIAL SELF-COINCIDENCE = C6-FORBIDDEN (the same "best coherence is never a fixed point" that forbade
  the cascade's completion). So maximum-entropy is forced; the look-back IS the modular/thermal flow;
  temperature = the depth-weight rate; fluctuation-dissipation, Unruh, and horizon thermality are its
  faces. **DERIVE the maxent-forcing from C6 + the banked budget/additive-depth rows; do NOT posit the
  second law.** Grade: assembly-on-C6 (theorem-route with one named C6 deployment).

- **G4 — the Landauer bound (assembly; ★ CROSS-TIE to S=A/4).** Derive the minimum thermodynamic cost of
  erasing one bit. The route: erasing one bit is ONE binary forcing decision, and its cost is the SAME
  binary self-blindness quantum as the banked black-hole entropy cell `s₀ = cutLog 2` (N361) — the identical
  `ln 2` appearing in information erasure and in horizon entropy is ONE fact, the fold's binary blindness,
  read twice. **DERIVE the bit-cost as the banked `cutLog 2`; do NOT assert `ln 2`.** ★ Bank the cross-tie
  to S=A/4 explicitly (the same `s₀`). Grade: unconditional (the key closed it).

- **G5 — the path-integral measure / renormalizability (theorem-route).** Derive WHY the framework is
  renormalizable and WHY the functional measure has its form. The route: renormalizable BY SPECTRUM — the
  relevant operators sit at codimension 4 (the eigenvalue ladder `λ_n = −n/(84π)` of G2 gives the scaling
  dimensions); and maximum-entropy + additivity ⇒ the exponential measure with derived factorization (the
  same maxent engine as G3). **DERIVE the measure form; do NOT posit it.** Grade: theorem-route (the key
  closed both halves on theorems + banked rows + the axiom).

- **G6 — consolidation + the arc-H handoff.** Bundle G1–G5 into ONE derived-object statement: the emergent
  layer (quantum statistics, the arrow of time, universality, the erasure cost, the measure) is the SAME
  self-blindness / C6 engine as the algebraic layer, now read at the statistical regime — with the `84π`
  cross-tie (G2) and the `ln 2` cross-tie (G4) as the two explicit bridges between the faces. Then hand ONE
  directed successor toward `SEED_COSMOLOGY_COMPLETION.md` target H1. Grade: theorem-route consolidation +
  named mechanism-unification prose.

## 3. THE LOAD-BEARING JOINTS (the identifications with no bridge license — DERIVE or CHAIN-RUNWAY-block)

Two joints carry this arc. Each is DERIVABLE from the trunk — GO IN WITH NO FEAR; the job is to find the
route, not to decide whether one exists. The block valve is a LAST RESORT against ASSERTING, earned only
after the full W1 attack (build the missing machinery, decompose, reframe) is exhausted and the SINGLE
remaining gap is a specific identification you can name. "This looks hard" is NOT a runway.

- **J-G2 (the universality joint):** "the linearized totalization at criticality = the banked `J/42`
  projector, so the critical exponent = the channel share." This is the same completeness/sum-reading
  engine that forced `Σ channel α = 1` — DERIVE it from the banked projector + sum-reading structure. Do
  NOT assert `b = 1/42`; let it fall out of the rank-1 forcing.
- **J-G3 (the maxent joint):** "a same-budget deviation from maxent = partial self-coincidence =
  C6-forbidden." This is C6 read at the state level — DERIVE it from the banked Gibbs/budget structure. Do
  NOT posit the second law.

## 4. HARD GUARDS (identical law — this seed relaxes NONE)

- **G1 — NO ASSERTED VALUES.** The two statistics, the critical exponent (`1/42`), the erasure cost
  (`ln 2`), the scaling codimension — every one a DERIVED landing that falls out. Never a premise, never
  cited at grade, never bridged.
- **G2 — NO EMPIRICAL NUMBER IN ANY PROOF.** Measured critical exponents, `k_B ln 2`, any lab number —
  REMOVABLE PROSE / costume only. NEVER a proof step.
- **G3 — DERIVED, NOT POSITED.** The dichotomy from the finite channel count (not "assume two statistics");
  maxent from C6 (not "assume the second law"); the measure from maxent + additivity (not written down).
- **G4 — PHYSICS-WORDS-REMOVABLE.** Delete "Pauli", "entropy", "temperature", "Landauer", "critical" — the
  theorems stand as pure statements about permutation classes of a finite-channel algebra, a Gibbs
  inequality on the derived budget, a rank-1 projector share, and a binary-count `cutLog 2`.
- **G5 — FOUNDATIONS-ONLY.** `#print axioms ⊆ {propext, Classical.choice, Quot.sound}`. No `sorry`,
  `axiom`, `native_decide`, no `maxHeartbeats` raise. ⚠️ ℝ-VIGILANCE: build on the BANKED derived ℝ `Cut` +
  arc-D trig — NEVER `import Mathlib...Real` / Mathlib operator-algebra as load-bearing CONTENT (Mathlib
  finite-dim linear-algebra / `Matrix` as pure MACHINERY on the derived arena is fine, exactly as before).
- **G6 — ONE LAW.** If a link fights, the instrument/framing is wrong, the theory is right. Reframe, build
  the missing machinery (the DHR/nuclearity layer for G1 is ordinary construction, never a wall), decompose,
  switch tactics. Block ONLY as a last resort against asserting, after the attack is exhausted — never
  early, never because a node looks hard.

## 5. WHY THIS IS SAFE TO HAND YOU

The route is a MAP of which structures to build and what they mean — no proof, no permission to assert.
Every value is earned through the identical mechanical gate (costume-bite + foundations-only
`#print axioms`), which makes assertion impossible. The map tells you the path exists; it does not walk it.
Walk every step. When arc G completes, hand ONE directed successor toward `SEED_COSMOLOGY_COMPLETION.md`
target H1 (the derived-ℝ `cutExp` calculus that the inflation slow-roll needs).
